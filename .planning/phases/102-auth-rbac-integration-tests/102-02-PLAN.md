---
phase: 102-auth-rbac-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/routes/rbac.test.js
autonomous: true
requirements: [BIT-02]

must_haves:
  truths:
    - "Konfi kann /api/konfi/* aufrufen aber erhaelt 403 auf /api/admin/*"
    - "Teamer kann /api/teamer/* aufrufen aber erhaelt 403 auf /api/admin/users"
    - "Admin kann /api/admin/* aufrufen aber erhaelt 403 auf /api/konfi/*"
    - "OrgAdmin kann /api/admin/* und User-Verwaltung aufrufen"
    - "SuperAdmin kann Org-Verwaltung aufrufen"
    - "Admin von Org A kann keine Daten von Org B lesen"
    - "Konfi von Org A sieht nur eigene Org-Daten"
  artifacts:
    - path: "backend/tests/routes/rbac.test.js"
      provides: "RBAC-Matrix + Cross-Org Isolation Tests"
      min_lines: 200
  key_links:
    - from: "backend/tests/routes/rbac.test.js"
      to: "backend/middleware/rbac.js"
      via: "supertest requests gegen geschuetzte Endpoints"
      pattern: "request\\(app\\)\\.(get|post|put|delete).*api"
---

<objective>
RBAC-Matrix und Cross-Org-Isolation Tests: Jeder geschuetzte Endpoint wird mit allen 5 Rollen getestet. Cross-Org-Zugriffe werden explizit geprueft.

Purpose: BIT-02 erfuellen -- kein Rollen-Bypass und kein Cross-Org-Zugriff geht unbemerkt durch
Output: backend/tests/routes/rbac.test.js mit RBAC-Matrix und Org-Isolation Tests
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-auth-rbac-integration-tests/102-CONTEXT.md
@.planning/phases/101-test-infrastruktur-server-js-refactoring/101-02-SUMMARY.md

<interfaces>
<!-- Test-Infrastruktur aus Phase 101 -->

From backend/tests/helpers/seed.js:
```javascript
const USERS = {
  // Org 1
  konfi1:    { id:1,  username:'konfi1',    role_id:1, org_id:1, type:'konfi' },
  konfi2:    { id:2,  username:'konfi2',    role_id:1, org_id:1, type:'konfi' },
  teamer1:   { id:3,  username:'teamer1',   role_id:2, org_id:1, type:'teamer' },
  admin1:    { id:4,  username:'admin1',    role_id:3, org_id:1, type:'admin' },
  orgAdmin1: { id:5,  username:'orgadmin1', role_id:4, org_id:1, type:'admin' },
  // Org 2
  konfi3:    { id:6,  username:'konfi3',    role_id:6, org_id:2, type:'konfi' },
  teamer2:   { id:7,  username:'teamer2',   role_id:7, org_id:2, type:'teamer' },
  admin2:    { id:8,  username:'admin2',    role_id:8, org_id:2, type:'admin' },
  orgAdmin2: { id:9,  username:'orgadmin2', role_id:9, org_id:2, type:'admin' },
  // Global
  superAdmin:{ id:10, username:'superadmin', role_id:5, org_id:1, type:'admin', is_super_admin:true },
};
```

From backend/middleware/rbac.js (Rollen-Hierarchie):
```javascript
// requireSuperAdmin = requireRole('super_admin')           -- NUR Org Create/Delete
// requireOrgAdmin   = requireRole('org_admin')             -- User-Verwaltung in Org
// requireAdmin      = requireRole('org_admin', 'admin')    -- Konfis, Requests, Badges, etc.
// requireTeamer     = requireRole('org_admin', 'admin', 'teamer') -- Events, Punkte vergeben
```

From backend/createApp.js (Route-Mounts):
```javascript
app.use('/api/konfi', ...)          // konfi routes (verifyTokenRBAC)
app.use('/api/admin/activities', ...)  // requireTeamer
app.use('/api/admin/badges', ...)     // requireAdmin
app.use('/api/admin/konfis', ...)     // requireAdmin
app.use('/api/admin/jahrgaenge', ...) // requireAdmin
app.use('/api/admin/categories', ...) // requireAdmin
app.use('/api/admin/users', ...)      // requireOrgAdmin
app.use('/api/teamer', ...)           // teamer routes (verifyTokenRBAC)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RBAC-Matrix Tests -- alle 5 Rollen gegen geschuetzte Endpoints</name>
  <files>backend/tests/routes/rbac.test.js</files>
  <read_first>
    backend/middleware/rbac.js
    backend/createApp.js
    backend/routes/konfi.js (erste 30 Zeilen fuer Route-Definitionen)
    backend/routes/activities.js (erste 30 Zeilen fuer Route-Definitionen)
    backend/routes/users.js (erste 30 Zeilen fuer Route-Definitionen)
    backend/routes/teamer.js (erste 30 Zeilen fuer Route-Definitionen)
    backend/tests/routes/smoke.test.js
    backend/tests/helpers/seed.js
    backend/tests/helpers/auth.js
  </read_first>
  <action>
Erstelle backend/tests/routes/rbac.test.js mit RBAC-Matrix-Tests (per D-08: alle 5 Rollen pro Endpoint, per D-09: Rollen-basierte Zugriffstabelle).

**Setup (identisch zu smoke.test.js):**
```javascript
const request = require('supertest');
const { getTestApp } = require('../helpers/testApp');
const { getTestPool, truncateAll, closePool } = require('../helpers/db');
const { seed, USERS, ORGS, ACTIVITIES, EVENTS, JAHRGAENGE, CHAT_ROOMS } = require('../helpers/seed');
const { generateToken } = require('../helpers/auth');
```
beforeAll/beforeEach/afterAll wie gewohnt.

**Token-Setup in beforeEach (nach seed):**
```javascript
tokens = {
  konfi:      generateToken('konfi1'),
  teamer:     generateToken('teamer1'),
  admin:      generateToken('admin1'),
  orgAdmin:   generateToken('orgAdmin1'),
  superAdmin: generateToken('superAdmin'),
  // Org 2
  konfi_org2:    generateToken('konfi3'),
  admin_org2:    generateToken('admin2'),
  orgAdmin_org2: generateToken('orgAdmin2'),
};
```

**describe('RBAC-Matrix: Konfi-Routes'):**
RBAC-Erwartung: Konfi-Routes sind fuer alle authentifizierten User zugaenglich, aber die Konfi-Routes pruefen intern ob der User ein Konfi ist. Teste:
- konfi1 Token auf GET /api/konfi/dashboard -> 200
- admin1 Token auf GET /api/konfi/dashboard -> pruefe ob 200 oder 403 (Route-spezifisch, lese konfi.js um zu verstehen)
- Ohne Token auf GET /api/konfi/dashboard -> 401

**describe('RBAC-Matrix: Admin-Routes (requireTeamer)'):**
Endpoints die requireTeamer nutzen: /api/admin/activities
- konfi Token -> 403
- teamer Token -> 200 (GET /api/admin/activities)
- admin Token -> 200
- orgAdmin Token -> 200
- superAdmin Token -> pruefe ob 200 oder 403 (super_admin ist NICHT in requireTeamer Liste, aber pruefen)

**describe('RBAC-Matrix: Admin-Routes (requireAdmin)'):**
Endpoints die requireAdmin nutzen: /api/admin/badges, /api/admin/konfis, /api/admin/jahrgaenge, /api/admin/categories
- konfi Token -> 403
- teamer Token -> 403
- admin Token -> 200
- orgAdmin Token -> 200
Teste mit GET /api/admin/badges als Repraesentant (oder GET /api/admin/jahrgaenge falls badges komplexer)

**describe('RBAC-Matrix: Admin-Routes (requireOrgAdmin)'):**
Endpoints die requireOrgAdmin nutzen: /api/admin/users
- konfi Token -> 403
- teamer Token -> 403
- admin Token -> 403
- orgAdmin Token -> 200 (GET /api/admin/users)

**describe('RBAC-Matrix: Teamer-Routes'):**
- teamer1 Token auf GET /api/teamer/dashboard (oder aehnlichen GET Endpoint) -> 200
- konfi Token -> pruefe ob 403 oder anders geschuetzt
- admin Token -> pruefe ob 200 oder 403

**describe('RBAC-Matrix: Unauthentifizierte Zugriffe'):**
- GET /api/konfi/dashboard ohne Token -> 401
- GET /api/admin/activities ohne Token -> 401
- GET /api/admin/users ohne Token -> 401
- GET /api/teamer/dashboard ohne Token -> 401

WICHTIG: Lies die Route-Dateien (erste 30-50 Zeilen) um die tatsaechlichen Middleware-Ketten und verfuegbaren GET-Endpoints zu identifizieren. Passe die Endpoints an die tatsaechliche Implementierung an. Nutze nur Endpoints die ohne zusaetzliche Path-Parameter funktionieren (z.B. GET /api/admin/activities statt GET /api/admin/activities/:id).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && npx vitest run --config tests/vitest.config.ts tests/routes/rbac.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" backend/tests/routes/rbac.test.js zeigt mindestens 4 describe-Bloecke und 15+ Tests
    - npm test zeigt 0 failed fuer rbac.test.js
    - Tests pruefen alle 5 Rollen (konfi, teamer, admin, org_admin, super_admin) gegen repraesentative Endpoints
    - Jeder Test prueft HTTP Status-Code (200/403/401)
  </acceptance_criteria>
  <done>
    RBAC-Matrix deckt alle 5 Rollen gegen repraesentative Endpoints ab. Unberechtigte Zugriffe erhalten 403.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-Org-Isolation Tests</name>
  <files>backend/tests/routes/rbac.test.js</files>
  <read_first>
    backend/tests/routes/rbac.test.js (aus Task 1)
    backend/routes/activities.js (organisation_id Filterung)
    backend/routes/konfi.js (organisation_id Filterung)
    backend/routes/jahrgaenge.js (organisation_id Filterung)
  </read_first>
  <action>
Ergaenze backend/tests/routes/rbac.test.js um Cross-Org-Isolation Tests (per D-10 und D-11).

**describe('Cross-Org-Isolation: Admin kann nur eigene Org sehen') (per D-10):**
- admin1 (Org 1) GET /api/admin/activities -> Response darf NUR Activities aus Org 1 enthalten (IDs 1-4), NICHT aus Org 2 (IDs 5-6)
  - Pruefe: res.body ist Array, jedes Element hat KEINE activity mit org_id=2 (oder pruefe dass die Org-2-Activity-Namen nicht vorkommen)
- admin2 (Org 2) GET /api/admin/activities -> Response darf NUR Activities aus Org 2 enthalten
- admin1 (Org 1) GET /api/admin/jahrgaenge -> nur Jahrgang mit org_id=1
- admin2 (Org 2) GET /api/admin/jahrgaenge -> nur Jahrgang mit org_id=2

**describe('Cross-Org-Isolation: Konfi sieht nur eigene Org') (per D-11):**
- konfi1 (Org 1) GET /api/konfi/dashboard -> Response enthaelt nur Org-1-Daten
- konfi3 (Org 2) GET /api/konfi/dashboard -> Response enthaelt nur Org-2-Daten
- konfi1 kann Org-2-Events NICHT sehen: GET /api/konfi/events -> kein Event mit Org-2-ID

**describe('Cross-Org-Isolation: Admin kann Org-fremde Daten nicht aendern') (per D-10):**
- admin1 (Org 1) versucht Activity aus Org 2 zu loeschen oder zu bearbeiten -> 403 oder 404
  - DELETE /api/admin/activities/5 (Org-2 Activity) mit admin1 Token -> nicht 200
  - Oder: PUT /api/admin/activities/5 mit admin1 Token -> nicht 200
- Teste mindestens einen schreibenden Cross-Org-Zugriff der abgelehnt wird

WICHTIG: Lies die Route-Dateien um zu verstehen WIE die Org-Filterung implementiert ist (meistens WHERE organization_id = req.user.organization_id). Passe die Assertions an die tatsaechliche Response-Struktur an. Manche Endpoints geben 404 statt 403 bei Org-fremden Daten (weil sie die Daten gar nicht finden) -- beides ist akzeptabel.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && npx vitest run --config tests/vitest.config.ts tests/routes/rbac.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Cross-Org" backend/tests/routes/rbac.test.js zeigt mindestens 3 describe-Bloecke mit "Cross-Org"
    - npm test zeigt 0 failed fuer rbac.test.js
    - Tests verifizieren dass Admin aus Org 1 keine Org-2-Daten lesen kann
    - Tests verifizieren dass Konfi nur eigene Org-Daten sieht
    - Mindestens ein schreibender Cross-Org-Zugriff wird getestet
  </acceptance_criteria>
  <done>
    Cross-Org-Isolation verifiziert: Admin von Org A kann keine Daten von Org B lesen oder aendern. Konfi von Org A sieht nur eigene Org-Daten. BIT-02 komplett erfuellt.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/Konfipoints/backend && npx vitest run --config tests/vitest.config.ts tests/routes/rbac.test.js
```
Alle Tests muessen gruen sein. RBAC-Matrix und Cross-Org-Isolation abgedeckt.
</verification>

<success_criteria>
- backend/tests/routes/rbac.test.js existiert mit RBAC-Matrix (alle 5 Rollen) + Cross-Org-Isolation Tests
- Alle Tests gruen gegen echte PostgreSQL Test-DB
- RBAC nie gemockt (per D-02)
- BIT-02 erfuellt
</success_criteria>

<output>
After completion, create `.planning/phases/102-auth-rbac-integration-tests/102-02-SUMMARY.md`
</output>
