---
phase: 48-admin-struktur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ChatOverview.tsx
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04
  - ADM-05

must_haves:
  truths:
    - "Admin-Chat zeigt Segmente Alle/Direkt/Konfis/Team"
    - "Konfis-Segment filtert Jahrgangs- und Gruppen-Chats mit Konfis"
    - "Team-Segment filtert Admin-interne Chats"
    - "Zertifikate, Dashboard-Settings und Badges sind als Unterseiten erreichbar"
    - "Badge-Erstellung uebergibt den aktiven Segment-Typ (Konfi/Teamer) als target_role"
    - "Events-Tab zeigt Badge mit Anzahl unverbuchter Events"
  artifacts:
    - path: "frontend/src/components/chat/ChatOverview.tsx"
      provides: "Chat-Filter mit Konfis/Team Segmenten"
      contains: "Konfis"
  key_links:
    - from: "frontend/src/components/chat/ChatOverview.tsx"
      to: "filterType state"
      via: "IonSegment mit neuen Labels und Filterlogik"
      pattern: "Konfis|Team"
---

<objective>
Chat-Filter-Segmente im Admin-Bereich auf sinnvolle Labels umstellen und bestehende ADM-Implementierungen verifizieren.

Purpose: ADM-05 ist der einzige offene Punkt -- Chat-Filter zeigt aktuell "Gruppe" und "Jahrgang" als Segmente, soll aber "Konfis" (Jahrgangs- und Gruppen-Chats) und "Team" (Admin-interne Chats) zeigen. ADM-01 bis ADM-04 sind bereits implementiert und werden nur verifiziert.

Output: Angepasste ChatOverview.tsx mit neuen Segment-Labels und Filterlogik.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-admin-struktur/48-CONTEXT.md

Hinweis zu ADM-01, ADM-02, ADM-03, ADM-04:
Diese Requirements sind bereits vollstaendig implementiert:
- ADM-01: AdminCertificatesPage existiert als Unterseite
- ADM-02: AdminDashboardSettingsPage existiert als Unterseite
- ADM-03: AdminBadgesPage hat selectedRole Tabs und uebergibt targetRole an BadgeManagementModal (Zeile 59)
- ADM-04: MainTabs.tsx zeigt pendingEventsCount als IonBadge auf Events-Tab (Zeile 178-182)
Nur ADM-05 braucht Implementierung.

<interfaces>
<!-- Relevante bestehende Strukturen in ChatOverview.tsx -->

Zeile 67: const [filterType, setFilterType] = useState<string>('alle');

Zeile 196-206 (aktuelle Filterlogik):
```typescript
const filteredRooms = rooms.filter(room => {
  if (filterType === 'alle') return true;
  if (filterType === 'direkt') return room.type === 'direct';
  if (filterType === 'gruppe') return room.type === 'group' || room.type === 'admin';
  if (filterType === 'jahrgang') return room.type === 'jahrgang';
  return true;
});
```

Zeile 356-362 (aktuelle Segment-Labels):
```typescript
<IonSegment value={filterType} onIonChange={(e) => setFilterType(String(e.detail.value))}>
  <IonSegmentButton value="alle"><IonLabel>Alle</IonLabel></IonSegmentButton>
  <IonSegmentButton value="direkt"><IonLabel>Direkt</IonLabel></IonSegmentButton>
  <IonSegmentButton value="gruppe"><IonLabel>Gruppe</IonLabel></IonSegmentButton>
  <IonSegmentButton value="jahrgang"><IonLabel>Jahrgang</IonLabel></IonSegmentButton>
</IonSegment>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat-Filter Segmente auf Konfis/Team umstellen</name>
  <files>frontend/src/components/chat/ChatOverview.tsx</files>
  <read_first>frontend/src/components/chat/ChatOverview.tsx</read_first>
  <action>
In ChatOverview.tsx die IonSegment-Buttons und Filterlogik aendern:

1. Segment-Labels aendern (ca. Zeile 356-362):
   - "Gruppe" -> "Konfis" (value bleibt "konfis" statt "gruppe")
   - "Jahrgang" -> "Team" (value wird "team" statt "jahrgang")
   - Reihenfolge: Alle, Direkt, Konfis, Team

2. Filterlogik anpassen (ca. Zeile 202-205):
   - `filterType === 'konfis'` -> filtert `room.type === 'jahrgang' || room.type === 'group'` (Chats die Konfis enthalten)
   - `filterType === 'team'` -> filtert `room.type === 'admin'` (Team-interne Chats)
   - `filterType === 'direkt'` -> bleibt `room.type === 'direct'`
   - `filterType === 'alle'` -> bleibt `return true`

WICHTIG: Die Segment-Aenderung gilt nur fuer Admins. Pruefen ob die Segmente auch fuer Konfis/Teamer gerendert werden -- falls ja, dort die alten Labels beibehalten oder ebenfalls anpassen (Konfis und Teamer sehen keine Admin-Chats, daher entfaellt "Team" bei ihnen). Falls die ChatOverview fuer alle Rollen gleich ist, die neuen Labels nur im Admin-Fall anzeigen oder generisch halten.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "Konfis\|Team" frontend/src/components/chat/ChatOverview.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "Konfis" frontend/src/components/chat/ChatOverview.tsx findet IonSegmentButton Label
    - grep "Team" frontend/src/components/chat/ChatOverview.tsx findet IonSegmentButton Label
    - grep "Gruppe" frontend/src/components/chat/ChatOverview.tsx findet KEINE IonSegmentButton Labels mehr
    - grep "Jahrgang" frontend/src/components/chat/ChatOverview.tsx findet KEINE IonSegmentButton Labels mehr (nur noch in Code-Kommentaren/room.type erlaubt)
    - grep "filterType === 'konfis'" frontend/src/components/chat/ChatOverview.tsx findet die neue Filterlogik
    - grep "filterType === 'team'" frontend/src/components/chat/ChatOverview.tsx findet die neue Filterlogik
  </acceptance_criteria>
  <done>Chat-Filter im Admin-Bereich zeigt "Alle", "Direkt", "Konfis", "Team" als Segmente. "Konfis" filtert Jahrgangs- und Gruppen-Chats, "Team" filtert Admin-interne Chats.</done>
</task>

<task type="auto">
  <name>Task 2: ADM-01 bis ADM-04 Verifizierung</name>
  <files></files>
  <read_first>
    frontend/src/components/admin/pages/AdminBadgesPage.tsx
    frontend/src/components/layout/MainTabs.tsx
    frontend/src/contexts/BadgeContext.tsx
  </read_first>
  <action>
Verifiziere dass ADM-01 bis ADM-04 korrekt implementiert sind. Keine Code-Aenderungen noetig -- nur lesen und bestaetigen:

1. ADM-01 (Zertifikate als Unterseite): Pruefe dass AdminCertificatesPage in MainTabs.tsx als Route importiert und registriert ist
2. ADM-02 (Dashboard-Settings als Unterseite): Pruefe dass AdminDashboardSettingsPage in MainTabs.tsx als Route importiert und registriert ist
3. ADM-03 (Badge-Typ-Abfrage): Pruefe dass AdminBadgesPage.tsx `selectedRole` als `targetRole` an BadgeManagementModal uebergibt (Zeile 59)
4. ADM-04 (Events-Tab Badge): Pruefe dass MainTabs.tsx `pendingEventsCount` aus BadgeContext nutzt und als IonBadge auf dem Events-Tab anzeigt (Zeile 178-182)

Falls etwas fehlt oder nicht korrekt ist: fixen. Erwartung: alles ist bereits korrekt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "AdminCertificatesPage\|AdminDashboardSettingsPage\|targetRole.*selectedRole\|pendingEventsCount" frontend/src/components/layout/MainTabs.tsx frontend/src/components/admin/pages/AdminBadgesPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "AdminCertificatesPage" frontend/src/components/layout/MainTabs.tsx findet Import und Route
    - grep "AdminDashboardSettingsPage" frontend/src/components/layout/MainTabs.tsx findet Import und Route
    - grep "targetRole: selectedRole" frontend/src/components/admin/pages/AdminBadgesPage.tsx findet die Uebergabe
    - grep "pendingEventsCount" frontend/src/components/layout/MainTabs.tsx findet Badge-Anzeige
  </acceptance_criteria>
  <done>Alle vier Requirements ADM-01 bis ADM-04 sind als implementiert bestaetigt. Keine Aenderungen noetig.</done>
</task>

</tasks>

<verification>
- Chat-Filter im Admin-Chat zeigt 4 Segmente: Alle, Direkt, Konfis, Team
- "Konfis" filtert korrekt auf jahrgang + group Chats
- "Team" filtert korrekt auf admin Chats
- Alte Labels "Gruppe" und "Jahrgang" erscheinen nicht mehr als Segment-Buttons
- Events-Tab Badge zeigt Anzahl unverbuchter Events
- Badge-Erstellung nutzt den aktiven Konfi/Teamer Tab als Typ
- Zertifikate und Dashboard-Settings sind als Unterseiten erreichbar
</verification>

<success_criteria>
- ADM-05: Chat-Segmente heissen "Alle", "Direkt", "Konfis", "Team" mit korrekter Filterlogik
- ADM-01 bis ADM-04: Bereits implementiert und verifiziert
- Frontend baut fehlerfrei (npm run build)
</success_criteria>

<output>
After completion, create `.planning/phases/48-admin-struktur/48-01-SUMMARY.md`
</output>
