---
phase: 85-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi-managment.js
  - backend/server.js
  - backend/migrations/077_activity_requests_rename_konfi_id.sql
  - backend/routes/activities.js
  - backend/routes/konfi.js
  - backend/routes/organizations.js
  - backend/routes/material.js
  - backend/routes/teamer.js
autonomous: true
requirements:
  - CLN-07
  - CLN-08
  - CLN-09

must_haves:
  truths:
    - "Die Route-Datei heisst konfi-management.js (kein Tippfehler mehr)"
    - "server.js importiert require('./routes/konfi-management') statt konfi-managment"
    - "activity_requests.konfi_id heisst in der Datenbank user_id"
    - "Alle SQL-Queries auf activity_requests.konfi_id sind auf user_id umgestellt"
    - "material.js validiert name, title und tag_ids per express-validator"
    - "teamer.js validiert name und icon per express-validator"
  artifacts:
    - path: "backend/routes/konfi-management.js"
      provides: "Umbenannte Konfi-Verwaltungs-Route"
    - path: "backend/migrations/077_activity_requests_rename_konfi_id.sql"
      provides: "Idempotente Migration fuer den Spalten-Rename"
    - path: "backend/routes/material.js"
      provides: "Route mit express-validator Validierung"
    - path: "backend/routes/teamer.js"
      provides: "Route mit express-validator Validierung fuer name/icon"
  key_links:
    - from: "backend/server.js"
      to: "backend/routes/konfi-management.js"
      via: "require('./routes/konfi-management')"
    - from: "backend/migrations/077_activity_requests_rename_konfi_id.sql"
      to: "activity_requests Tabelle"
      via: "ALTER TABLE activity_requests RENAME COLUMN konfi_id TO user_id"
---

<objective>
Datei-Typo korrigieren (konfi-managment -> konfi-management), activity_requests.konfi_id in
user_id umbenennen inkl. Migration und aller Query-Stellen, express-validator auf material.js
und teamer.js ergaenzen.

Purpose: Beseitigt irreführenden Dateinamen, stellt Schema-Konsistenz mit anderen user_*-Tabellen
her, und schliesst fehlende Input-Validierung auf zwei Route-Dateien.
Output: Korrekt benannte Route-Datei, neue Migration SQL, bereinigte Query-Stellen, Validierung
auf material.js und teamer.js.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-code-cleanup/85-CONTEXT.md
</context>

<interfaces>
Referenz-Pattern fuer express-validator aus categories.js (Zeilen 1-30):

  const { body, param } = require('express-validator');
  const { handleValidationErrors, commonValidations } = require('../middleware/validation');

  const validateCreate = [
    commonValidations.name,
    handleValidationErrors
  ];

  router.post('/', rbacVerifier, requireAdmin, validateCreate, async (req, res) => { ... });

Alle activity_requests.konfi_id Stellen (nach Migration heissen sie user_id):
- activities.js: ~16 Treffer (konfi_id in SELECT, WHERE, JOIN, INSERT, lokale Variable request.konfi_id)
- konfi.js: ~10 Treffer (konfi_id in SELECT, WHERE, INSERT, lokale Variable request.konfi_id)
- konfi-managment.js (wird zu konfi-management.js): ~2 Treffer (WHERE konfi_id = $1)
- organizations.js: ~2 Treffer (JOIN kp ON ar.konfi_id = kp.user_id)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: konfi-managment.js umbenennen und server.js Import korrigieren</name>
  <files>backend/routes/konfi-managment.js, backend/routes/konfi-management.js, backend/server.js</files>
  <action>
Umsetzt CLN-07.

1. backend/routes/konfi-managment.js lesen.
2. Datei als backend/routes/konfi-management.js neu schreiben (gleicher Inhalt, korrekter Dateiname).
3. backend/routes/konfi-managment.js loeschen (die alte Datei mit Typo).
4. backend/server.js lesen, dann Zeile ~438 aendern:
   const adminKonfisRoutes = require('./routes/konfi-managment');
   => const adminKonfisRoutes = require('./routes/konfi-management');

Keine inhaltlichen Aenderungen an der Route-Logik.
  </action>
  <verify>
    <automated>ls backend/routes/konfi-management.js && echo "OK: neue Datei vorhanden"; ls backend/routes/konfi-managment.js 2>/dev/null && echo "FEHLER: alte Datei noch vorhanden" || echo "OK: alte Datei geloescht"; grep "konfi-managment" backend/server.js && echo "FEHLER: alter Import noch in server.js" || echo "OK: Import korrigiert"</automated>
  </verify>
  <done>
    - backend/routes/konfi-management.js existiert mit korrektem Inhalt
    - backend/routes/konfi-managment.js existiert nicht mehr
    - server.js require('./routes/konfi-management') ohne Tippfehler
  </done>
</task>

<task type="auto">
  <name>Task 2: activity_requests.konfi_id -> user_id Migration schreiben und alle Query-Stellen anpassen</name>
  <files>
    backend/migrations/077_activity_requests_rename_konfi_id.sql,
    backend/routes/activities.js,
    backend/routes/konfi.js,
    backend/routes/konfi-management.js,
    backend/routes/organizations.js
  </files>
  <action>
Umsetzt CLN-08.

**Schritt 1 — Migration anlegen:**
Neue Datei backend/migrations/077_activity_requests_rename_konfi_id.sql erstellen mit folgendem
idempotenten Inhalt:

  -- Umbenennung activity_requests.konfi_id -> user_id (Schema-Konsistenz mit user_activities, user_badges)
  DO $$
  BEGIN
    IF EXISTS (
      SELECT 1 FROM information_schema.columns
      WHERE table_name = 'activity_requests' AND column_name = 'konfi_id'
    ) THEN
      ALTER TABLE activity_requests RENAME COLUMN konfi_id TO user_id;
    END IF;
  END $$;

**Schritt 2 — activities.js anpassen:**
Datei lesen. Jedes Vorkommen von "ar.konfi_id" oder "konfi_id" im Kontext der activity_requests
Tabelle ersetzen durch "ar.user_id" bzw. "user_id". Stellen:

  - SELECT-Statements: "ar.konfi_id" -> "ar.user_id"
  - WHERE-Klauseln: "ar.konfi_id = ..." oder "konfi_id = $1" -> "ar.user_id = ..." / "user_id = $1"
  - INSERT: "INSERT INTO activity_requests (konfi_id, ..." -> "(user_id, ..."
  - JOIN: "JOIN users u_konfi ON ar.konfi_id = u_konfi.id" -> "ON ar.user_id = u_konfi.id"
  - Lokale Variablen wie "request.konfi_id" muessen NICHT umbenannt werden (JavaScript-Objekt,
    nicht Datenbankfeld). Nur SQL-String-Stellen aendern, die den Spaltennamen referenzieren.
  - Zeile ~287: request.konfi_id als JS-Variable bleibt, aber der SQL-Query-String darueber muss
    die Spalte "user_id" verwenden wenn activity_requests abgefragt wird.

  AUSNAHME: konfi_id in anderen Tabellen (bonus_points.konfi_id, event_points.konfi_id,
  konfi_profiles.user_id) NICHT aendern — diese Tabellen sind nicht betroffen.

**Schritt 3 — konfi.js anpassen:**
Datei lesen. Gleiche Vorgehensweise wie activities.js:
  - Alle SQL-Strings mit activity_requests: "konfi_id" -> "user_id"
  - SELECT-Feld-Liste in Zeile ~618: "konfi_id" in der Spaltenliste -> "user_id"
  - INSERT in Zeile ~638: "INSERT INTO activity_requests (konfi_id, ..." -> "(user_id, ..."
  - WHERE-Klauseln: "ar.konfi_id = $1" -> "ar.user_id = $1"
  - JS request.konfi_id Variablen in Zeile ~691: Umbenennen zu request.user_id, da dieser Wert
    aus dem SELECT gelesen wird (der nun user_id heisst). Alle Stellen pruefen wo request.konfi_id
    als JS-Variable aus einem DB-Ergebnis von activity_requests kommt.

**Schritt 4 — konfi-management.js anpassen:**
Datei lesen. Gleiche Vorgehensweise:
  - "WHERE konfi_id = $1" auf activity_requests -> "WHERE user_id = $1"
  - "DELETE FROM activity_requests WHERE konfi_id = $1" -> "WHERE user_id = $1"

**Schritt 5 — organizations.js anpassen:**
Datei lesen. Zwei Stellen (~Zeile 568-569):
  - "JOIN konfi_profiles kp ON ar.konfi_id = kp.user_id" -> "ar.user_id = kp.user_id"
  - Zweite Stelle analog.

  WICHTIG: "DELETE FROM activity_requests WHERE organization_id = $1" (Zeile ~367) braucht
  keine Aenderung, da organization_id nicht betroffen ist.
  </action>
  <verify>
    <automated>grep -rn "activity_requests" backend/routes/activities.js backend/routes/konfi.js backend/routes/konfi-management.js backend/routes/organizations.js | grep "konfi_id" && echo "FEHLER: konfi_id noch in activity_requests Queries" || echo "OK: alle konfi_id Stellen umbenannt"; ls backend/migrations/077_activity_requests_rename_konfi_id.sql</automated>
  </verify>
  <done>
    - backend/migrations/077_activity_requests_rename_konfi_id.sql existiert mit idempotenter DO-Block Migration
    - Kein "konfi_id" mehr in SQL-Strings der vier Route-Dateien im Kontext von activity_requests
    - JS-Variablen aus activity_requests-Ergebnissen nutzen user_id als Feldname
  </done>
</task>

<task type="auto">
  <name>Task 3: express-validator auf material.js und teamer.js ergaenzen</name>
  <files>backend/routes/material.js, backend/routes/teamer.js</files>
  <action>
Umsetzt CLN-09.

Referenz-Pattern aus categories.js (bereits korrekt implementiert):
  const { body, param } = require('express-validator');
  const { handleValidationErrors, commonValidations } = require('../middleware/validation');

**material.js anpassen:**
Datei lesen. Dann:

1. Import oben ergaenzen:
   const { body, param } = require('express-validator');
   const { handleValidationErrors } = require('../middleware/validation');

2. Validierung fuer Tag-Endpunkte (POST /tags, PUT /tags/:id):
   const validateCreateTag = [
     body('name').notEmpty().trim().isLength({ min: 1, max: 100 }).withMessage('Name erforderlich (1-100 Zeichen)'),
     handleValidationErrors
   ];
   const validateUpdateTag = [
     param('id').isInt({ min: 1 }).withMessage('Ungueltige ID'),
     body('name').notEmpty().trim().isLength({ min: 1, max: 100 }).withMessage('Name erforderlich (1-100 Zeichen)'),
     handleValidationErrors
   ];

3. Validierung fuer Material-Endpunkte (POST /, PUT /:id):
   const validateCreateMaterial = [
     body('title').notEmpty().trim().isLength({ min: 1, max: 255 }).withMessage('Titel erforderlich (1-255 Zeichen)'),
     body('tag_ids').optional().isArray().withMessage('tag_ids muss ein Array sein'),
     handleValidationErrors
   ];
   const validateUpdateMaterial = [
     param('id').isInt({ min: 1 }).withMessage('Ungueltige ID'),
     body('title').optional().trim().isLength({ min: 1, max: 255 }).withMessage('Titel max. 255 Zeichen'),
     handleValidationErrors
   ];

4. Validate-Middleware in die Router-Definitionen einfuegen:
   router.post('/tags', rbacVerifier, requireOrgAdmin, validateCreateTag, async (req, res) => {
   router.put('/tags/:id', rbacVerifier, requireOrgAdmin, validateUpdateTag, async (req, res) => {
   router.post('/', rbacVerifier, requireOrgAdmin, materialUpload.single('file'), validateCreateMaterial, async (req, res) => {
   router.put('/:id', rbacVerifier, requireOrgAdmin, validateUpdateMaterial, async (req, res) => {

   HINWEIS: Bei File-Upload Routen (post mit materialUpload) kommt die Validierung NACH dem
   Multer-Middleware, da req.body erst nach dem Parsen befuellt ist.

**teamer.js anpassen:**
Datei lesen. Dann:

1. Import oben ergaenzen:
   const { body, param } = require('express-validator');
   const { handleValidationErrors } = require('../middleware/validation');

2. Validierung fuer die relevanten mutierbaren Endpunkte in teamer.js.
   Anhand der gefundenen req.body-Zugriffe (name, icon, is_active):
   const validateUpdateTeamer = [
     param('id').isInt({ min: 1 }).withMessage('Ungueltige ID'),
     body('name').optional().trim().isLength({ min: 1, max: 100 }).withMessage('Name max. 100 Zeichen'),
     body('icon').optional().trim().isLength({ max: 50 }).withMessage('Icon max. 50 Zeichen'),
     body('is_active').optional().isBoolean().withMessage('is_active muss boolean sein'),
     handleValidationErrors
   ];
   const validateCertificate = [
     param('id').isInt({ min: 1 }).withMessage('Ungueltige ID'),
     body('certificate_type_id').isInt({ min: 1 }).withMessage('certificate_type_id erforderlich'),
     body('issued_date').notEmpty().isISO8601().withMessage('Gueltiges Datum erforderlich'),
     body('expiry_date').optional().isISO8601().withMessage('Gueltiges Ablaufdatum'),
     handleValidationErrors
   ];

3. Validate-Middleware in Router-Definitionen einfuegen fuer PUT /:id und POST /:id/certificates.

Keine Logik-Aenderungen, nur Validierungs-Middleware vorschalten.
  </action>
  <verify>
    <automated>grep -n "express-validator\|handleValidationErrors" backend/routes/material.js | head -5; grep -n "express-validator\|handleValidationErrors" backend/routes/teamer.js | head -5</automated>
  </verify>
  <done>
    - material.js: express-validator Import vorhanden, validateCreateTag/validateCreateMaterial/etc. definiert, Middleware in Router-Definitionen
    - teamer.js: express-validator Import vorhanden, validateUpdateTeamer/validateCertificate definiert, Middleware in Router-Definitionen
    - Keine Breaking Changes an bestehender Logik
  </done>
</task>

</tasks>

<verification>
Backend startet ohne Fehler nach allen Aenderungen.
Kein "Cannot find module './routes/konfi-managment'" Fehler.
Migration 077 wird beim Server-Start ausgefuehrt (database.js liest alle .sql aus migrations/).
</verification>

<success_criteria>
- konfi-management.js existiert, konfi-managment.js nicht mehr
- server.js: require('./routes/konfi-management') ohne Typo
- backend/migrations/077_activity_requests_rename_konfi_id.sql mit idempotenter ALTER-Migration
- Keine "konfi_id" Spaltennamen mehr in SQL-Strings der activity_requests-Queries
- material.js und teamer.js: express-validator Import + Validierungs-Middleware aktiv
</success_criteria>

<output>
Nach Abschluss: .planning/phases/85-code-cleanup/85-02-SUMMARY.md anlegen.
</output>
