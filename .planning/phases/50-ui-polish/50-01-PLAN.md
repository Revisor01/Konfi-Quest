---
phase: 50-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/theme/variables.css
  - frontend/src/components/admin/views/KonfiDetailView.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-08]

must_haves:
  truths:
    - "Toggle-Switches stehen rechts aussen in Jahrgang-Modal"
    - "QR-Scanner-Button ist oben rechts im Header"
    - "Badge-Fortschritt zeigt keine Nachkommastellen"
    - "Chat-Tab-Badge wird nicht abgeschnitten"
    - "Befoerdern-Hinweistext steht ueber dem Button"
    - "Beschreibungstexte in Event-Details und Material-Details sind lesbar"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx"
      provides: "Toggle slot=end in Jahrgang-Modal"
      contains: "slot=\"end\""
    - path: "frontend/src/components/konfi/views/EventsView.tsx"
      provides: "QR-Button im Header statt FAB"
      contains: "slot=\"end\""
    - path: "frontend/src/components/konfi/views/BadgesView.tsx"
      provides: "Badge-Fortschritt ohne Nachkommastellen"
      contains: "Math.round"
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Chat-Tab-Badge sichtbar"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "Befoerdern-Hinweistext ueber dem Button"
  key_links:
    - from: "frontend/src/theme/variables.css"
      to: "MainTabs.tsx"
      via: "ion-tab-button ion-badge CSS"
      pattern: "ion-tab-button.*ion-badge|overflow.*visible"
---

<objective>
Sechs unabhaengige UI-Fixes in einem Plan: Toggle-Positionierung, QR-Button-Platzierung, Badge-Fortschritt-Rundung, Chat-Badge-Overflow, Befoerdern-Hinweistext und Beschreibungstext-Schriftgroesse.

Purpose: Kleinere UI-Inkonsistenzen beseitigen, App visuell konsistenter machen.
Output: 9 geaenderte Dateien, alle UI-Fixes shipped.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-ui-polish/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle-Position, QR-Button, Badge-Rundung</name>
  <files>
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx,
    frontend/src/components/konfi/views/EventsView.tsx,
    frontend/src/components/konfi/views/BadgesView.tsx,
    frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx,
    frontend/src/components/konfi/views/EventsView.tsx,
    frontend/src/components/konfi/views/BadgesView.tsx,
    frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  </read_first>
  <action>
    **UI-01: Toggle slot="end" in AdminJahrgaengeePage.tsx**
    In der Jahrgang-Modal Punkte-Konfiguration (ca. Zeile 226 und 256): Den beiden IonToggle-Elementen fuer "Gottesdienst-Punkte aktiviert" und "Gemeinde-Punkte aktiviert" das Attribut `slot="end"` hinzufuegen. Die Toggles in AdminDashboardSettingsPage.tsx haben bereits slot="end" — hier fehlt es nur.

    Ebenso bei den optionalen Ziel-Wert-Toggles pruefen ob dort auch slot="end" fehlt.

    **UI-02: QR-Scanner-Button von FAB in Header verschieben (EventsView.tsx)**
    1. Den IonFab-Block (ca. Zeile 436-440) mit IonFabButton und qrCodeOutline Icon komplett entfernen
    2. Im IonHeader/IonToolbar einen IonButtons slot="end" Block hinzufuegen mit einem IonButton, der onClick={() => presentScannerModal()} aufruft und das qrCodeOutline Icon zeigt
    3. IonFab und IonFabButton aus den Imports entfernen (nicht mehr benoetigt)

    **UI-03: Badge-Fortschritt ohne Nachkommastellen**
    In BadgesView.tsx:
    - Zeile ~251: `{badge.progress_percentage}%` aendern zu `{Math.round(badge.progress_percentage)}%`
    - Zeile ~582: `{badge.progress_percentage}%` aendern zu `{Math.round(badge.progress_percentage)}%`

    In TeamerBadgesPage.tsx:
    - Zeile ~261: `{badge.progress_percentage}%` aendern zu `{Math.round(badge.progress_percentage)}%`
    - Zeile ~626: `{badge.progress_percentage}%` aendern zu `{Math.round(badge.progress_percentage)}%`

    Hinweis: Die Kategorie-Prozente (progressPercent) nutzen bereits Math.round — nur die Badge-Einzelfortschritte fehlen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n 'slot="end"' frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx | grep -i toggle && grep -c 'IonFab' frontend/src/components/konfi/views/EventsView.tsx | grep '^0$' && grep 'Math.round(badge.progress_percentage)' frontend/src/components/konfi/views/BadgesView.tsx | head -2 && grep 'Math.round(badge.progress_percentage)' frontend/src/components/teamer/pages/TeamerBadgesPage.tsx | head -2</automated>
  </verify>
  <acceptance_criteria>
    - grep 'slot="end"' AdminJahrgaengeePage.tsx findet mindestens 2 Treffer bei IonToggle
    - grep 'IonFab' EventsView.tsx findet 0 Treffer
    - grep 'slot="end".*qrCode\|qrCode.*slot="end"' EventsView.tsx ODER grep 'presentScannerModal' im Header-Bereich
    - grep 'Math.round(badge.progress_percentage)' BadgesView.tsx findet mindestens 2 Treffer
    - grep 'Math.round(badge.progress_percentage)' TeamerBadgesPage.tsx findet mindestens 2 Treffer
  </acceptance_criteria>
  <done>
    Toggle-Switches in Jahrgang-Modal stehen rechts, QR-Button ist im Header oben rechts, Badge-Fortschritt zeigt ganze Zahlen ohne Nachkommastellen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Chat-Badge-Overflow, Befoerdern-Hinweis, Beschreibungstexte</name>
  <files>
    frontend/src/components/layout/MainTabs.tsx,
    frontend/src/theme/variables.css,
    frontend/src/components/admin/views/KonfiDetailView.tsx,
    frontend/src/components/admin/views/EventDetailView.tsx,
    frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  </files>
  <read_first>
    frontend/src/components/layout/MainTabs.tsx,
    frontend/src/theme/variables.css,
    frontend/src/components/admin/views/KonfiDetailView.tsx,
    frontend/src/components/admin/views/EventDetailView.tsx,
    frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  </read_first>
  <action>
    **UI-04: Chat-Tab-Badge Overflow fixen (variables.css)**
    In variables.css eine neue CSS-Regel hinzufuegen, die sicherstellt dass IonBadge auf IonTabButton nicht abgeschnitten wird:
    ```css
    /* Tab-Badge Overflow Fix */
    ion-tab-button {
      overflow: visible;
    }
    ion-tab-button ion-badge {
      position: absolute;
      top: 4px;
      right: calc(50% - 24px);
      z-index: 10;
      font-size: 0.65rem;
      min-width: 16px;
      height: 16px;
      line-height: 16px;
      padding: 0 4px;
      border-radius: 8px;
    }
    ```
    Falls es bereits Tab-Badge-Styling gibt, dieses erweitern statt duplizieren. Hauptsache: overflow: visible auf ion-tab-button und z-index: 10 auf die Badge.

    **UI-08: Befoerdern-Hinweistext UEBER dem Button (KonfiDetailView.tsx)**
    Im Befoerdern-Bereich (ca. Zeile 1493-1505): Den bestehenden `<p>` Hinweistext ("Konfi-Rolle wird dauerhaft gewechselt") von UNTER dem IonButton nach UEBER den IonButton verschieben. Ausserdem den Text erweitern zu einem informativen Hinweis:
    ```
    "Beim Befoerdern bleiben Konfi-Punkte und Badges als Historie erhalten. Event-Buchungen und offene Antraege werden geloescht. Diese Aktion kann nicht rueckgaengig gemacht werden."
    ```
    Den Hinweis als `<p>` mit fontSize 0.8rem, color var(--ion-color-medium), textAlign center, marginBottom 12px VOR dem IonButton platzieren. Den alten `<p>` unter dem Button entfernen.

    **Beschreibungstexte lesbare Schriftgroesse**
    1. EventDetailView.tsx (ca. Zeile 844): Die Beschreibung nutzt `className="app-info-row__sublabel"`. Das ergibt 0.9rem — das ist zu klein. Stattdessen inline style `style={{ fontSize: '0.95rem', lineHeight: '1.5', color: '#374151', whiteSpace: 'pre-wrap', margin: 0 }}` verwenden (wie in TeamerMaterialDetailPage).
    2. TeamerMaterialDetailPage.tsx: Die Beschreibung hat bereits inline-Styles mit lineHeight 1.5 und color #374151 — das ist gut. Nur pruefen ob fontSize mindestens 0.95rem ist, und ggf. anpassen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep 'ion-tab-button' frontend/src/theme/variables.css | head -3 && grep -B5 'handlePromoteToTeamer\|Befördern' frontend/src/components/admin/views/KonfiDetailView.tsx | grep -i 'beim\|bleiben\|historie' | head -1 && grep 'fontSize.*0.95\|font-size.*0.95' frontend/src/components/admin/views/EventDetailView.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - grep 'overflow.*visible' variables.css findet mindestens 1 Treffer im Kontext von ion-tab-button
    - grep 'z-index' variables.css findet einen Treffer im Kontext von ion-tab-button ion-badge
    - In KonfiDetailView.tsx steht der Hinweistext-Paragraph VOR dem IonButton mit onClick={handlePromoteToTeamer}
    - In EventDetailView.tsx hat die Beschreibung fontSize 0.95rem oder groesser
  </acceptance_criteria>
  <done>
    Chat-Tab-Badge ist vollstaendig sichtbar, Befoerdern-Hinweis steht informativ ueber dem Button, Beschreibungstexte sind in lesbarer Groesse.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript-Kompilierung ohne Fehler
2. `cd frontend && npm run build` — Build erfolgreich
3. Visuell: Toggle-Switches in Jahrgang-Modal rechts aussen
4. Visuell: QR-Button oben rechts im Header der Konfi-Events
5. Visuell: Badge-Fortschritt zeigt "75%" statt "75.3%"
6. Visuell: Chat-Badge auf Tab nicht abgeschnitten
7. Visuell: Befoerdern-Hinweistext ueber dem Button
</verification>

<success_criteria>
- Alle 5 Requirements (UI-01, UI-02, UI-03, UI-04, UI-08) sind implementiert
- Build kompiliert ohne Fehler
- Keine neuen TypeScript-Fehler eingefuehrt
</success_criteria>

<output>
After completion, create `.planning/phases/50-ui-polish/50-01-SUMMARY.md`
</output>
