---
phase: 92-sicherheit-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/backgroundService.js
  - backend/middleware/rbac.js
autonomous: true
requirements: [PERF-08, PERF-09]

must_haves:
  truths:
    - "Der backgroundService Badge-Cron laeuft mit einer festen Anzahl SQL-Queries unabhaengig von der User-Zahl fuer den Chat-Unread-Teil"
    - "Wiederholte API-Requests desselben Users innerhalb von 30 Sekunden loesen keine erneute DB-Abfrage in verifyTokenRBAC aus"
  artifacts:
    - path: "backend/services/backgroundService.js"
      provides: "Bulk Chat-Unread-Query statt N+1"
      contains: "chatUnreadMap"
    - path: "backend/middleware/rbac.js"
      provides: "LRU-Cache fuer User-Objekte"
      contains: "userCache"
  key_links:
    - from: "backend/services/backgroundService.js"
      to: "backend/routes/badges.js"
      via: "checkAndAwardBadges Import"
      pattern: "checkAndAwardBadges"
    - from: "backend/middleware/rbac.js"
      to: "userCache"
      via: "LRU Map mit TTL"
      pattern: "userCache"
---

<objective>
Badge-Cron von N+1 auf Bulk-Query optimieren und verifyTokenRBAC mit LRU-Cache versehen.

Purpose: Backend skaliert bei steigender User-Zahl (EKD-Verkauf, 4000+ User) und vermeidet unnoetige DB-Last.
Output: Performanter backgroundService und gecachte Token-Validierung
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Aus backend/services/backgroundService.js — updateAllUserBadges N+1 Pattern -->
```javascript
// Aktuell: Pro User eine Chat-Unread-Query (Zeilen 78-90) + checkAndAwardBadges Aufruf
// Chat-Unread-Query pro User: ~15 Zeilen SQL mit JOINs
// checkAndAwardBadges pro User: 5-10 weitere Queries je nach Badge-Typen
```

<!-- Aus backend/middleware/rbac.js — verifyTokenRBAC ohne Cache -->
```javascript
// Zeilen 43-91: Jeder Request fuehrt 2 DB-Queries aus:
// 1. User-Query mit LEFT JOINs (users + organizations + roles)
// 2. Jahrgaenge-Query (user_jahrgang_assignments + jahrgaenge)
// Bei 100 Requests/min = 200 DB-Queries/min nur fuer Auth
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge-Cron Chat-Unread Bulk-Query (PERF-08)</name>
  <files>backend/services/backgroundService.js</files>
  <read_first>backend/services/backgroundService.js, backend/routes/badges.js (Zeilen 99-310 checkAndAwardBadges)</read_first>
  <action>
In `backend/services/backgroundService.js` die Methode `updateAllUserBadges` refactorn:

1. Den Chat-Unread-Teil (Zeilen 75-98, die for-Schleife mit per-User badgeQuery) durch eine einzige Bulk-Query ersetzen:

```javascript
// BULK: Chat-Unread fuer ALLE User mit Push-Tokens auf einmal berechnen
const chatUnreadQuery = `
  SELECT cp.user_id, cp.user_type, COUNT(DISTINCT cm.id)::int as chat_unread
  FROM chat_participants cp
  JOIN chat_messages cm ON cm.room_id = cp.room_id
  LEFT JOIN chat_read_status crs ON cm.room_id = crs.room_id
    AND crs.user_id = cp.user_id AND crs.user_type = cp.user_type
  WHERE cm.created_at > COALESCE(crs.last_read_at, '1970-01-01')
    AND cm.deleted_at IS NULL
    AND NOT (cm.user_id = cp.user_id AND cm.user_type = cp.user_type)
  GROUP BY cp.user_id, cp.user_type
  HAVING COUNT(DISTINCT cm.id) > 0
`;
const { rows: unreadRows } = await db.query(chatUnreadQuery);
const chatUnreadMap = {};
for (const row of unreadRows) {
  chatUnreadMap[`${row.user_id}_${row.user_type}`] = row.chat_unread;
}
```

2. Die for-Schleife vereinfachen — statt per-User SQL nur noch Map-Lookup:

```javascript
for (const user of users) {
  try {
    const badgeCount = chatUnreadMap[`${user.user_id}_${user.user_type}`] || 0;

    if (badgeCount > 0) {
      await PushService.sendBadgeUpdate(db, user.user_id, badgeCount);
      updatedCount++;
    }

    // Badge-Check pro User bleibt (checkAndAwardBadges hat eigene Bulk-Queries)
    const awardResult = await checkAndAwardBadges(db, user.user_id);

    // ... Rest wie bisher (neue Badge-Count Vergleich und Push)
  } catch (error) {
    console.error(`Badge update failed for user ${user.user_id}:`, error);
  }
}
```

3. Die separate `badgeCounts` Vorlade-Query (Zeilen 62-70) und den Vergleich beibehalten — das ist bereits effizient (1 Query fuer alle User).

4. WICHTIG: checkAndAwardBadges bleibt pro User — die Funktion hat komplexe Badge-Logik mit verschiedenen criteria_types. Nur den Chat-Unread-Teil (die groesste N+1-Quelle) in eine Bulk-Query extrahieren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "chatUnreadMap" backend/services/backgroundService.js && grep -c "badgeQuery" backend/services/backgroundService.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "chatUnreadMap" backend/services/backgroundService.js findet mindestens 2 Treffer (Definition + Usage)
    - grep "badgeQuery" backend/services/backgroundService.js findet 0 Treffer (alte per-User Query entfernt)
    - grep "chat_participants cp" backend/services/backgroundService.js findet 1 Treffer (Bulk-Query)
    - Die for-Schleife enthaelt keinen db.query Aufruf fuer Chat-Unread mehr
  </acceptance_criteria>
  <done>Chat-Unread-Berechnung im Badge-Cron laeuft mit 1 Bulk-Query statt N einzelnen Queries</done>
</task>

<task type="auto">
  <name>Task 2: verifyTokenRBAC LRU-Cache mit 30s TTL (PERF-09)</name>
  <files>backend/middleware/rbac.js</files>
  <read_first>backend/middleware/rbac.js</read_first>
  <action>
In `backend/middleware/rbac.js` einen einfachen LRU-Cache mit TTL einbauen (kein externes Paket noetig — Map + setTimeout reicht):

1. Am Anfang der Datei (nach den requires) den Cache definieren:

```javascript
// LRU-Cache fuer User-Objekte (30s TTL, max 500 Eintraege)
const USER_CACHE_TTL = 30 * 1000; // 30 Sekunden
const USER_CACHE_MAX = 500;
const userCache = new Map();

const getCachedUser = (userId) => {
  const entry = userCache.get(userId);
  if (!entry) return null;
  if (Date.now() - entry.timestamp > USER_CACHE_TTL) {
    userCache.delete(userId);
    return null;
  }
  return entry.data;
};

const setCachedUser = (userId, data) => {
  // Evict aelteste Eintraege wenn Max erreicht
  if (userCache.size >= USER_CACHE_MAX) {
    const firstKey = userCache.keys().next().value;
    userCache.delete(firstKey);
  }
  userCache.set(userId, { data, timestamp: Date.now() });
};

// Cache invalidieren bei User-Aenderungen (Export fuer andere Module)
const invalidateUserCache = (userId) => {
  if (userId) {
    userCache.delete(userId);
  } else {
    userCache.clear();
  }
};
```

2. In der `verifyTokenRBAC` Funktion nach dem JWT-Verify (Zeile ~41) den Cache-Check einfuegen:

```javascript
// Cache-Check VOR DB-Query
const cached = getCachedUser(decoded.id);
if (cached) {
  // Soft-Revoke Check auch mit cached Data
  if (cached.token_invalidated_at) {
    const tokenIssuedAt = decoded.iat;
    const invalidatedAt = Math.floor(new Date(cached.token_invalidated_at).getTime() / 1000);
    if (tokenIssuedAt < invalidatedAt) {
      return res.status(401).json({ error: 'Token invalidated' });
    }
  }
  req.user = cached.userObj;
  return next();
}
```

3. Nach der erfolgreichen DB-Query (nach Zeile ~110, vor `next()`) den Cache befuellen:

```javascript
// User-Objekt cachen
setCachedUser(decoded.id, {
  token_invalidated_at: user.token_invalidated_at,
  userObj: req.user
});

next();
```

4. `invalidateUserCache` zum Export hinzufuegen:

```javascript
module.exports = {
  verifyTokenRBAC,
  invalidateUserCache,
  // ... bestehende Exports
};
```

5. WICHTIG: is_active und organization_active Checks MUESSEN auch fuer gecachte User gelten. Da der Cache nur 30s haelt, ist das akzeptabel — ein gesperrter User hat maximal 30s Zugriff nach Sperrung.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "userCache" backend/middleware/rbac.js && grep -c "getCachedUser" backend/middleware/rbac.js && grep "invalidateUserCache" backend/middleware/rbac.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - grep "userCache" backend/middleware/rbac.js findet mindestens 5 Treffer (Definition, get, set, evict, export)
    - grep "getCachedUser" backend/middleware/rbac.js findet mindestens 2 Treffer (Definition + Usage in verifyTokenRBAC)
    - grep "setCachedUser" backend/middleware/rbac.js findet mindestens 2 Treffer (Definition + Usage)
    - grep "invalidateUserCache" backend/middleware/rbac.js findet mindestens 2 Treffer (Definition + Export)
    - grep "USER_CACHE_TTL" backend/middleware/rbac.js findet 1 Treffer mit Wert 30000 oder 30 * 1000
    - grep "USER_CACHE_MAX" backend/middleware/rbac.js findet 1 Treffer mit Wert 500
  </acceptance_criteria>
  <done>verifyTokenRBAC cached User-Objekte 30s lang — wiederholte Requests desselben Users sparen 2 DB-Queries pro Request</done>
</task>

</tasks>

<verification>
- grep "chatUnreadMap" backgroundService.js >= 2 Treffer
- grep "badgeQuery" backgroundService.js = 0 Treffer (N+1 entfernt)
- grep "userCache" rbac.js >= 5 Treffer
- node -e "require('./backend/middleware/rbac')" laeuft fehlerfrei
</verification>

<success_criteria>
1. Badge-Cron Chat-Unread laeuft mit 1 Bulk-Query statt N einzelner Queries
2. verifyTokenRBAC hat LRU-Cache mit 30s TTL und max 500 Eintraegen
</success_criteria>

<output>
After completion, create `.planning/phases/92-sicherheit-performance/92-02-SUMMARY.md`
</output>
