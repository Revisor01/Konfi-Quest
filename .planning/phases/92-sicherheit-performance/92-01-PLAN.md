---
phase: 92-sicherheit-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/auth.js
  - backend/server.js
  - backend/package.json
  - backend/middleware/uploadValidation.js
  - backend/routes/chat.js
  - backend/routes/konfi.js
  - backend/routes/material.js
autonomous: true
requirements: [SEC-07, SEC-08]

must_haves:
  truths:
    - "Ein gesperrter/deaktivierter User erhaelt bei change-password, update-email, update-role-title, me, invite-code, invite-codes, invite-codes/:id/extend, invite-codes/:id (DELETE), logout sofort 401 statt Erfolg"
    - "Eine Datei mit falschem MIME-Header (z.B. .exe umbenannt in .jpg) wird beim Upload abgelehnt"
  artifacts:
    - path: "backend/routes/auth.js"
      provides: "Auth-Routes mit verifyTokenRBAC statt verifyToken"
      contains: "rbacVerifier"
    - path: "backend/middleware/uploadValidation.js"
      provides: "Magic-Bytes-Validierung Middleware"
      contains: "fileTypeFromBuffer"
    - path: "backend/package.json"
      provides: "file-type Dependency"
      contains: "file-type"
  key_links:
    - from: "backend/routes/auth.js"
      to: "backend/middleware/rbac.js"
      via: "rbacVerifier Parameter"
      pattern: "rbacVerifier"
    - from: "backend/routes/chat.js"
      to: "backend/middleware/uploadValidation.js"
      via: "validateMagicBytes nach multer"
      pattern: "validateMagicBytes"
    - from: "backend/routes/konfi.js"
      to: "backend/middleware/uploadValidation.js"
      via: "validateMagicBytes nach multer"
      pattern: "validateMagicBytes"
    - from: "backend/routes/material.js"
      to: "backend/middleware/uploadValidation.js"
      via: "validateMagicBytes nach multer"
      pattern: "validateMagicBytes"
---

<objective>
Auth-Routes auf RBAC-Middleware migrieren und Upload-MIME-Validierung mit Magic-Bytes haerten.

Purpose: Gesperrte User koennen keine Auth-Aktionen mehr ausfuehren, manipulierte Uploads werden erkannt.
Output: Gehaertete auth.js, uploadValidation.js Middleware, angepasste Upload-Routes
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Aus backend/middleware/rbac.js — wird von auth.js konsumiert -->
```javascript
// verifyTokenRBAC(db) returns Express middleware
// Setzt req.user mit: id, organization_id, username, display_name, role_name,
//   role_title, role_display_name, organization_name, organization_slug,
//   assigned_jahrgaenge, type, is_super_admin, is_org_admin
const { verifyTokenRBAC } = require('../middleware/rbac');
```

<!-- Aus backend/server.js — wie auth.js eingebunden wird -->
```javascript
// server.js Zeile 471:
const rbacVerifier = verifyTokenRBAC(db);
// server.js Zeile 487:
app.use('/api/auth', authRoutes(db, verifyToken, transporter, SMTP_CONFIG, { authLimiter, registerLimiter }));
```

<!-- Upload-Stellen in Route-Dateien -->
```
backend/routes/chat.js:671    — chatUpload.single('file')
backend/routes/konfi.js:743   — requestUpload.single('photo')
backend/routes/material.js:525 — materialUpload.array('files', 10)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth-Routes von verifyToken auf verifyTokenRBAC migrieren (SEC-07)</name>
  <files>backend/routes/auth.js, backend/server.js</files>
  <read_first>backend/routes/auth.js, backend/server.js, backend/middleware/rbac.js</read_first>
  <action>
1. In `backend/server.js` Zeile 487: `rbacVerifier` als 6. Parameter uebergeben:
   ```
   app.use('/api/auth', authRoutes(db, verifyToken, transporter, SMTP_CONFIG, { authLimiter, registerLimiter }, rbacVerifier));
   ```

2. In `backend/routes/auth.js` Zeile 28: Signatur um `rbacVerifier` erweitern:
   ```
   module.exports = (db, verifyToken, transporter, SMTP_CONFIG, rateLimiters = {}, rbacVerifier) => {
   ```

3. Alle 9 Stellen ersetzen — `verifyToken` Middleware-Referenz durch `rbacVerifier`:
   - `/change-password` (Zeile ~177)
   - `/update-email` (Zeile ~213)
   - `/update-role-title` (Zeile ~241)
   - `/me` (Zeile ~268)
   - `/invite-code` POST (Zeile ~336)
   - `/invite-codes` GET (Zeile ~384)
   - `/invite-codes/:id/extend` POST (Zeile ~413)
   - `/invite-codes/:id` DELETE (Zeile ~452)
   - `/logout` POST (Zeile ~799)

4. In `/update-role-title`: `req.user.type === 'konfi'` aendern zu `req.user.role_name === 'konfi'` — verifyTokenRBAC setzt `role_name` statt `type` als primaeres Feld.

NICHT aendern: Login, register-konfi, check-username, validate-invite, request-password-reset, reset-password — oeffentliche Endpoints ohne Token.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -cP "verifyToken[,)]" backend/routes/auth.js && grep -c "rbacVerifier" backend/routes/auth.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -P "verifyToken[,)]" backend/routes/auth.js findet 0 Treffer (nur noch in module.exports Signatur als Parameter-Name)
    - grep "rbacVerifier" backend/routes/auth.js findet mindestens 9 Treffer (9 Routes + 1 Signatur)
    - grep "rbacVerifier" backend/server.js | grep authRoutes findet 1 Treffer
  </acceptance_criteria>
  <done>Alle 9 Auth-Routes nutzen verifyTokenRBAC — gesperrte/deaktivierte User erhalten sofort 401</done>
</task>

<task type="auto">
  <name>Task 2: Magic-Bytes Upload-Validierung erstellen und einbinden (SEC-08)</name>
  <files>backend/middleware/uploadValidation.js, backend/routes/chat.js, backend/routes/konfi.js, backend/routes/material.js, backend/package.json</files>
  <read_first>backend/server.js (Zeilen 300-398 multer configs), backend/routes/chat.js (Zeile ~671), backend/routes/konfi.js (Zeile ~743), backend/routes/material.js (Zeile ~525), backend/package.json</read_first>
  <action>
1. `cd backend && npm install file-type@19` — ESM-only Paket, muss per dynamic import geladen werden.

2. Neue Datei `backend/middleware/uploadValidation.js` erstellen:

```javascript
const fs = require('fs');

const validateMagicBytes = async (req, res, next) => {
  if (!req.file && !req.files) return next();
  const files = req.file ? [req.file] : (req.files || []);
  const { fileTypeFromBuffer } = await import('file-type');

  for (const file of files) {
    // Text-basierte Formate haben keine Magic Bytes — Header vertrauen
    const textMimes = ['text/plain', 'text/csv'];
    if (textMimes.includes(file.mimetype)) continue;

    const buffer = fs.readFileSync(file.path);
    const detected = await fileTypeFromBuffer(buffer);

    if (!detected) {
      fs.unlinkSync(file.path);
      return res.status(415).json({ error: 'Dateityp konnte nicht verifiziert werden' });
    }

    const allowedPrefixes = [
      'image/', 'video/', 'audio/', 'application/pdf',
      'application/vnd.openxmlformats', 'application/msword',
      'application/vnd.ms-excel', 'application/vnd.ms-powerpoint',
      'application/zip', 'application/x-cfb' // Office-Formate: ZIP (docx) oder CFB (doc/xls/ppt)
    ];
    const isAllowed = allowedPrefixes.some(p => detected.mime.startsWith(p));

    if (!isAllowed) {
      fs.unlinkSync(file.path);
      return res.status(415).json({ error: 'Dateityp nicht erlaubt: ' + detected.mime });
    }
  }
  next();
};

module.exports = { validateMagicBytes };
```

3. In `backend/routes/chat.js` Zeile ~671:
   - Am Dateianfang (bei den requires): `const { validateMagicBytes } = require('../middleware/uploadValidation');`
   - Route aendern: `chatUpload.single('file'), validateMagicBytes, validateSendMessage, async (req, res) => {`
     (validateMagicBytes als Middleware NACH multer.single und VOR dem Handler einfuegen)

4. In `backend/routes/konfi.js` Zeile ~743:
   - Am Dateianfang: `const { validateMagicBytes } = require('../middleware/uploadValidation');`
   - Route aendern: `requestUpload.single('photo'), validateMagicBytes, async (req, res) => {`

5. In `backend/routes/material.js` Zeile ~525:
   - Am Dateianfang: `const { validateMagicBytes } = require('../middleware/uploadValidation');`
   - Route aendern: `materialUpload.array('files', 10), validateMagicBytes, async (req, res) => {`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "require('./middleware/uploadValidation')" && grep -c "validateMagicBytes" routes/chat.js routes/konfi.js routes/material.js && grep "file-type" package.json</automated>
  </verify>
  <acceptance_criteria>
    - backend/middleware/uploadValidation.js existiert und ist ohne Fehler require-bar
    - grep "file-type" backend/package.json findet 1 Treffer
    - grep "validateMagicBytes" backend/routes/chat.js findet mindestens 2 Treffer (require + usage)
    - grep "validateMagicBytes" backend/routes/konfi.js findet mindestens 2 Treffer
    - grep "validateMagicBytes" backend/routes/material.js findet mindestens 2 Treffer
    - grep "fileTypeFromBuffer" backend/middleware/uploadValidation.js findet 1 Treffer
  </acceptance_criteria>
  <done>Alle 3 Upload-Pfade (Chat, Konfi-Foto, Material) validieren Dateien anhand echter Magic-Bytes — manipulierte MIME-Header werden erkannt</done>
</task>

</tasks>

<verification>
- grep -P "verifyToken[,)]" backend/routes/auth.js = 0 Treffer
- node -e "require('./backend/middleware/uploadValidation')" laeuft fehlerfrei
- grep "validateMagicBytes" auf 3 Route-Dateien jeweils >= 2 Treffer
</verification>

<success_criteria>
1. Kein Auth-Endpoint nutzt mehr das alte verifyToken als Middleware
2. Upload-Validierung prueft echte Datei-Signatur via file-type Paket in allen 3 Upload-Pfaden
</success_criteria>

<output>
After completion, create `.planning/phases/92-sicherheit-performance/92-01-SUMMARY.md`
</output>
