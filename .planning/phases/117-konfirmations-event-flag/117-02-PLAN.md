---
phase: 117-konfirmations-event-flag
plan: 02
type: execute
wave: 2
depends_on: [117-01]
files_modified:
  - frontend/src/types/event.ts
  - frontend/src/components/admin/modals/EventFormSections.tsx
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
autonomous: false
requirements: [KONF-03, KONF-05, KONF-06]
must_haves:
  truths:
    - "Im Event-Formular gibt es ein Toggle Konfirmation direkt neben dem Toggle Pflicht-Event"
    - "Das Toggle setzt is_konfirmation und wird beim Bearbeiten aus event.is_konfirmation vorbelegt"
    - "Konfirmations-Events werden automatisch lila eingefaerbt"
    - "Konfirmations-Events tragen den Hinweis Konfirmation als zweites Corner-Badge (bestehendes Corner-Badge-System)"
    - "Die Konfi-EventsView erkennt Konfirmations-Events ueber is_konfirmation statt ueber category_names"
    - "Die Admin-EventDetailView erkennt Konfirmation ueber is_konfirmation statt ueber categories-Namen"
  artifacts:
    - path: "frontend/src/types/event.ts"
      provides: "is_konfirmation im Event-Interface"
      contains: "is_konfirmation"
    - path: "frontend/src/components/admin/modals/EventFormSections.tsx"
      provides: "Toggle Konfirmation neben Pflicht-Event + is_konfirmation in EventFormData"
      contains: "is_konfirmation"
    - path: "frontend/src/components/admin/modals/EventModal.tsx"
      provides: "is_konfirmation in formData-Init (neu+edit) und im POST/PUT-Payload"
      contains: "is_konfirmation"
    - path: "frontend/src/components/konfi/views/EventsView.tsx"
      provides: "is_konfirmation-basierte Erkennung + lila Status-Farbe"
      contains: "is_konfirmation"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "is_konfirmation-basierte Erkennung + lila Status-Farbe"
      contains: "is_konfirmation"
  key_links:
    - from: "EventFormSections Toggle Konfirmation"
      to: "EventFormData.is_konfirmation -> EventModal-Payload -> POST/PUT /events"
      via: "setFormData + payload.is_konfirmation"
      pattern: "is_konfirmation"
    - from: "EventsView/EventDetailView Konfirmation-Erkennung"
      to: "event.is_konfirmation Flag"
      via: "Ersatz der category_names/categories-String-Checks"
      pattern: "is_konfirmation"
---

<objective>
Frontend auf das `is_konfirmation`-Flag umstellen:
1. Event-Typ + EventFormData um `is_konfirmation` erweitern.
2. Toggle "Konfirmation" im Event-Formular direkt neben "Pflicht-Event"; Vorbelegung beim Bearbeiten; Durchreichen ins POST/PUT-Payload.
3. Alle bestehenden String-basierten Konfirmations-Filter (category_names / categories-Namen) in der Konfi-EventsView und der Admin-EventDetailView auf das Flag umstellen.
4. Konfirmations-Events automatisch lila einfaerben und das bereits vorhandene zweite Corner-Badge "Konfirmation" ueber das Flag steuern.

Purpose: Robuste Flag-Erkennung im UI; korrekte lila Faerbung + Hinweis-Badge; kein versehentliches Setzen ueber eine Kategorie mehr.
Output: Erweiterte event.ts, EventFormSections.tsx, EventModal.tsx, EventsView.tsx, EventDetailView.tsx. Human-Verify am Ende (visuell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-konfirmations-event-flag/117-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-konfirmations-event-flag/117-01-SUMMARY.md

<interfaces>
<!-- Vertrag aus Plan 117-01: Backend liefert/akzeptiert das Flag `is_konfirmation` (boolean). -->
<!-- GET /api/events (Liste) und GET /api/events/:id liefern es; POST/PUT akzeptieren es. -->

Event-Interface: frontend/src/types/event.ts:67-70 (Pflicht/Optionen-Block, mandatory?:boolean :68). is_konfirmation analog ergaenzen.
EventFormData-Typ: frontend/src/components/admin/modals/EventFormSections.tsx:44 (mandatory:boolean). is_konfirmation:boolean analog ergaenzen.
Toggle Pflicht-Event: frontend/src/components/admin/modals/EventFormSections.tsx:112-121 (IonItem + IonLabel "Pflicht-Event" + IonToggle, className="app-toggle--events"). Neues Toggle "Konfirmation" direkt davor oder dahinter (eigenes IonItem lines="inset").
EventModal formData-Init (Default): EventModal.tsx:54-61 (mandatory:false :60).
EventModal formData-Init (neu-Reset): EventModal.tsx:132-141 (mandatory:false :139).
EventModal formData-Init (edit): EventModal.tsx:88-106 (mandatory: event.mandatory || false :103).
EventModal Payload: EventModal.tsx:205-223 (mandatory: formData.mandatory :220). is_konfirmation: formData.is_konfirmation analog ergaenzen — OHNE die mandatory-Guards (kein Nullsetzen von max_participants/registration etc.).

Konfi-EventsView Konfirmation-Erkennung (UMSTELLEN auf Flag):
  :69-75 konfirmationEvents/nonKonfirmationEvents (filter auf category_names includes 'konfirmation').
  :127 isKonfirmationEvent (category_names includes 'konfirmation').
  Status-Farbe :152 (aktuell C.info=blau fuer Konfirmation) -> LILA umstellen (var(--app-color-konfis)).
  Status-Text :173-174, Tabs/Counts :39/:83-86/:102-106/:214, getFilteredEvents :214.
  Zweites Corner-Badge existiert bereits: :343-354 (flame-Icon, backgroundColor var(--app-color-konfis)) — haengt an isKonfirmationEvent, das nun aus dem Flag kommt. KEIN neues Badge bauen.

Admin-EventDetailView (UMSTELLEN auf Flag):
  :242 isKonfirmationEvent (categories?.some name includes 'konfirmation')) in getStatusColors.
  :233/:248 konfirm-Farbe (aktuell var(--app-color-info)=blau) -> LILA (var(--app-color-konfis)).
  :263/:269 isKonfirmationEvent + "Konfirmation"-Status-Text in getStatusText.

Farb-Token: --app-color-konfis: #5b21b6 (lila), variables.css:66. --app-color-info: #007aff (blau), :100.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: is_konfirmation in Typ + Formular-Toggle + EventModal-Verkabelung</name>
  <files>frontend/src/types/event.ts, frontend/src/components/admin/modals/EventFormSections.tsx, frontend/src/components/admin/modals/EventModal.tsx</files>
  <read_first>
    - frontend/src/types/event.ts:67-74 (Pflicht/Optionen + Teamer-Block)
    - frontend/src/components/admin/modals/EventFormSections.tsx:29-47 (EventFormData-Interface)
    - frontend/src/components/admin/modals/EventFormSections.tsx:112-121 (Toggle Pflicht-Event als Vorlage)
    - frontend/src/components/admin/modals/EventModal.tsx:54-61 (formData-Default), :88-106 (edit-Init), :132-141 (neu-Reset), :205-223 (Payload)
  </read_first>
  <action>
    1. frontend/src/types/event.ts: im Pflicht/Optionen-Block (nach mandatory?:boolean, :68) `is_konfirmation?: boolean;` ergaenzen.

    2. EventFormSections.tsx EventFormData-Interface (:44 mandatory:boolean): `is_konfirmation: boolean;` ergaenzen.

    3. EventFormSections.tsx Toggle: Direkt neben dem "Pflicht-Event"-IonItem (:112-121) ein zweites IonItem mit IonLabel position="stacked" Text "Konfirmation" und IonToggle ergaenzen. Toggle: `checked={formData.is_konfirmation}`, `onIonChange={(e) => setFormData({ ...formData, is_konfirmation: e.detail.checked })}`, `disabled={loading}`, slot="end". Fuer die Toggle-Farbe eine sinnvolle vorhandene Toggle-Klasse aus dem .app-toggle--<color>-Set waehlen (Konfirmation = lila/konfis-Domain; falls eine app-toggle--konfis o.ae. existiert, diese nutzen — sonst app-toggle--events wie das Pflicht-Toggle als Fallback; im SUMMARY notieren welche Klasse gewaehlt wurde). Echte Umlaute, keine Emojis, kein Unicode.

    4. EventModal.tsx: is_konfirmation in ALLEN drei formData-Init-Stellen setzen — Default-State (:54-61, `is_konfirmation: false`), neu-Reset (:132-141, `is_konfirmation: false`) und edit-Init (:88-106, `is_konfirmation: event.is_konfirmation || false`). Im Payload (:205-223) `is_konfirmation: formData.is_konfirmation` ergaenzen. WICHTIG: is_konfirmation darf KEINE der mandatory-Guards ausloesen (max_participants/registration/waitlist bleiben unberuehrt von is_konfirmation) — nur das Flag wird mitgesendet.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "is_konfirmation" frontend/src/types/event.ts -> 1 Treffer.
    - grep -c "is_konfirmation" frontend/src/components/admin/modals/EventFormSections.tsx >= 3 (Interface + checked + onIonChange).
    - grep -c "is_konfirmation" frontend/src/components/admin/modals/EventModal.tsx >= 4 (Default + Reset + Edit + Payload).
    - Das Toggle "Konfirmation" steht im selben Formular-Abschnitt direkt neben "Pflicht-Event".
    - npx tsc --noEmit laeuft ohne neue Fehler durch.
    - Keine Emojis, echte Umlaute.
  </acceptance_criteria>
  <done>is_konfirmation ist im Event-Typ und in EventFormData vorhanden, das Konfirmation-Toggle steht neben Pflicht-Event, wird bei Bearbeitung vorbelegt und ins POST/PUT-Payload uebernommen — ohne mandatory-Guards.</done>
</task>

<task type="auto">
  <name>Task 2: String-Filter auf Flag umstellen + lila Faerbung (EventsView + EventDetailView)</name>
  <files>frontend/src/components/konfi/views/EventsView.tsx, frontend/src/components/admin/views/EventDetailView.tsx</files>
  <read_first>
    - frontend/src/components/konfi/views/EventsView.tsx:69-86 (konfirmationEvents/nonKonfirmationEvents/eventCounts)
    - frontend/src/components/konfi/views/EventsView.tsx:127 (isKonfirmationEvent), :135-163 (Farb-Logik, Konfirmation :152), :173-174 (Status-Text), :207-219 (getFilteredEvents)
    - frontend/src/components/konfi/views/EventsView.tsx:328-369 (Corner-Badges — Konfirmation-Badge :343-354 existiert bereits, NICHT neu bauen)
    - frontend/src/components/admin/views/EventDetailView.tsx:228-258 (getStatusColors, konfirm :233/:248, isKonfirmationEvent :242), :260-269 (getStatusText :263/:269)
    - frontend/src/theme/variables.css:66 (--app-color-konfis lila), :100 (--app-color-info blau)
  </read_first>
  <action>
    EventsView.tsx — alle category_names-basierten Konfirmations-Checks durch das Flag ersetzen:
    - :69-75: `e.category_names?.toLowerCase().includes('konfirmation')` -> `e.is_konfirmation` (konfirmationEvents) bzw. `!e.is_konfirmation` (nonKonfirmationEvents).
    - :127: `isKonfirmationEvent = event.is_konfirmation`.
    - Status-Farbe :152: Konfirmation-Zweig von C.info (blau) auf LILA umstellen. Dazu im C-Objekt (:137-144) einen Eintrag `konfis: 'var(--app-color-konfis)'` ergaenzen und :152 `else if (isKonfirmationEvent && !isPastEvent) statusColor = C.konfis;` setzen. (Locked Decision: Konfirmation automatisch lila.)
    - Status-Text :173-174 unveraendert lassen (haengt bereits an isKonfirmationEvent, das nun aus dem Flag kommt).
    - Tabs/Counts (:83-86) und getFilteredEvents (:214) nutzen konfirmationEvents/nonKonfirmationEvents — durch die umgestellten useMemo-Filter automatisch korrekt. Keine weitere Aenderung an Tab-Keys noetig.
    - Corner-Badge :343-354: UNVERAENDERT (flame-Icon, var(--app-color-konfis)) — wird durch das nun flag-basierte isKonfirmationEvent korrekt getriggert. KEIN neues Badge.

    EventDetailView.tsx — categories-Namen-Checks durch das Flag ersetzen:
    - :242 und :263: `eventData.categories?.some(cat => cat.name.toLowerCase().includes('konfirmation'))` -> `eventData.is_konfirmation`.
    - konfirm-Farbe :233/:248: von var(--app-color-info) (blau) auf var(--app-color-konfis) (lila) umstellen (Kommentar entsprechend anpassen: "Konfirmation = lila").
    - :269 Status-Text "Konfirmation" bleibt.

    Hinweis: event.category_names bleibt als Feld erhalten (andere Kategorien existieren weiter) — nur die Konfirmation-Sonderbehandlung wird vom String auf das Flag umgestellt. Echte Umlaute, keine Emojis.
  </action>
  <verify>
    <automated>cd frontend && echo "=== Rest-Strings (muss leer sein) ===" && grep -rn "includes('konfirmation')" src/components/konfi/views/EventsView.tsx src/components/admin/views/EventDetailView.tsx; echo "=== tsc ===" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "includes('konfirmation')" src/components/konfi/views/EventsView.tsx src/components/admin/views/EventDetailView.tsx -> KEIN Treffer (alle String-Checks entfernt).
    - grep -c "is_konfirmation" frontend/src/components/konfi/views/EventsView.tsx >= 3.
    - grep -c "is_konfirmation" frontend/src/components/admin/views/EventDetailView.tsx >= 2.
    - EventsView Konfirmation-Status-Farbe nutzt var(--app-color-konfis) (lila), EventDetailView konfirm-Farbe nutzt var(--app-color-konfis).
    - npx tsc --noEmit laeuft ohne neue Fehler durch.
  </acceptance_criteria>
  <done>Alle String-basierten Konfirmation-Checks in EventsView und EventDetailView sind auf is_konfirmation umgestellt; Konfirmations-Events sind lila; das bestehende zweite Corner-Badge wird ueber das Flag getriggert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    is_konfirmation-Flag durchgaengig im UI: Toggle "Konfirmation" neben "Pflicht-Event" im Event-Formular, lila Faerbung + zweites Corner-Badge "Konfirmation" (flame-Icon) in der Konfi-EventsView, lila Status in der Admin-EventDetailView. Alle String-Filter auf das Flag umgestellt. Backend-Flag + Migration aus Plan 117-01.
  </what-built>
  <how-to-verify>
    Voraussetzung: Frontend gebaut und auf Geraet/Simulator (nach Frontend-Aenderungen Xcode-Build noetig, siehe MEMORY feedback_xcode_build). Backend mit Migration 091 laeuft (lokal/Dev).
    1. Als Admin/Teamer ein neues Event anlegen: Im Formular muss neben "Pflicht-Event" ein Toggle "Konfirmation" stehen. Konfirmation aktivieren, speichern.
    2. Konfi-Ansicht (EventsView): Das Konfirmations-Event muss lila eingefaerbt sein und ein zweites Corner-Badge mit Flammen-Icon "Konfirmation" tragen. Im Tab "Konfi" muss es erscheinen, im Tab "Alle" NICHT.
    3. Bestehendes Event bearbeiten: Das Toggle "Konfirmation" muss den gespeicherten Zustand korrekt anzeigen; aus- und wieder einschalten und speichern wirkt.
    4. Admin-EventDetailView des Konfirmations-Events oeffnen: Status/Header muss lila sein und "Konfirmation" als Status-Text zeigen.
    5. Falls vor der Migration Events ueber die alte Kategorie "Konfirmation" markiert waren: Diese muessen jetzt (ohne manuelles Zutun) das Flag tragen, lila sein und das Badge zeigen. Die Kategorie "Konfirmation" darf im Kategorie-Auswahl-Dialog NICHT mehr erscheinen.
  </how-to-verify>
  <resume-signal>Schreibe "approved" oder beschreibe Abweichungen (Farbe/Badge/Toggle/Tab-Filter)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/Teamer-Formular -> POST/PUT /events | is_konfirmation wird im Client gesetzt, serverseitig in Plan 117-01 validiert (isBoolean) |
| Backend-Antwort -> Konfi-/Admin-Views | is_konfirmation steuert nur Anzeige (Farbe/Badge/Tab), keine Rechte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117F-01 | Tampering | is_konfirmation aus Client-Payload | mitigate | Serverseitige isBoolean-Validierung (Plan 117-01); Client-Flag ist reine Anzeige-/Eingabehilfe, keine Autorisierungsentscheidung |
| T-117F-02 | Information Disclosure | Konfirmation-Markierung im UI | accept | Konfirmation ist eine oeffentliche Event-Eigenschaft fuer alle Rollen sichtbar; kein sensibler Inhalt |
| T-117F-SC | Tampering | npm-Installs | mitigate | Keine neuen Pakete — nur Bestandskomponenten/Tokens werden geaendert |
</threat_model>

<verification>
- grep -rn "includes('konfirmation')" frontend/src/components/konfi/views/EventsView.tsx frontend/src/components/admin/views/EventDetailView.tsx -> kein Treffer
- grep -c "is_konfirmation" frontend/src/components/admin/modals/EventModal.tsx >= 4
- grep -c "is_konfirmation" frontend/src/components/admin/modals/EventFormSections.tsx >= 3
- npx tsc --noEmit -p tsconfig.json -> ohne neue Fehler
- Human-Verify: Toggle sichtbar, lila Faerbung + zweites Corner-Badge, alte Kategorie-Events ueberfuehrt, Kategorie nicht mehr waehlbar
</verification>

<success_criteria>
- Toggle "Konfirmation" neben "Pflicht-Event" im Formular (KONF-03)
- Konfirmations-Events lila + zweites Corner-Badge "Konfirmation" ueber bestehendes System (KONF-05)
- Alle String-basierten Konfirmation-Filter auf das Flag umgestellt (KONF-06)
- Toggle-Wert wird gespeichert und beim Bearbeiten korrekt vorbelegt
- tsc gruen, keine Emojis, echte Umlaute
</success_criteria>

<output>
Create `.planning/phases/117-konfirmations-event-flag/117-02-SUMMARY.md` when done. Dokumentiere die gewaehlte Toggle-Klasse, die geaenderten Zeilen je Datei und das Ergebnis des Human-Verify. Notiz: nach Frontend-Aenderung Xcode-Build noetig (Capacitor).
</output>
