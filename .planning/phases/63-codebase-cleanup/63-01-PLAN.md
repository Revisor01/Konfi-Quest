---
phase: 63-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/user.ts
  - frontend/src/types/event.ts
  - frontend/src/types/dashboard.ts
  - frontend/src/services/auth.ts
  - frontend/src/services/tokenStore.ts
  - frontend/src/contexts/AppContext.tsx
  - frontend/src/components/chat/modals/DirectMessageModal.tsx
  - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx
  - frontend/src/components/chat/modals/MembersModal.tsx
  - frontend/src/components/admin/UsersView.tsx
  - frontend/src/components/admin/modals/UserManagementModal.tsx
  - frontend/src/components/admin/pages/AdminUsersPage.tsx
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
  - frontend/src/components/konfi/pages/KonfiEventsPage.tsx
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - frontend/src/components/admin/EventsView.tsx
  - frontend/src/components/admin/modals/EventModal.tsx
autonomous: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - "User Interface existiert nur einmal als exportierter Typ in types/user.ts"
    - "Event Interface existiert nur einmal als exportierter Typ in types/event.ts"
    - "Alle Dateien die User/Event nutzen importieren aus types/"
    - "Kein as-any mehr fuer Event-Properties (has_timeslots, waitlist_*, mandatory etc.)"
  artifacts:
    - path: "frontend/src/types/user.ts"
      provides: "Zentrale User-Typen (BaseUser, AdminUser, ChatUser)"
      exports: ["BaseUser", "AdminUser", "ChatUser"]
    - path: "frontend/src/types/event.ts"
      provides: "Zentrale Event-Typen (Event, EventDetail, AdminEvent)"
      exports: ["Event", "EventDetail", "AdminEvent"]
  key_links:
    - from: "frontend/src/services/auth.ts"
      to: "frontend/src/types/user.ts"
      via: "import { BaseUser }"
      pattern: "import.*BaseUser.*from.*types/user"
    - from: "frontend/src/components/konfi/views/EventDetailView.tsx"
      to: "frontend/src/types/event.ts"
      via: "import { EventDetail }"
      pattern: "import.*Event.*from.*types/event"
---

<objective>
Typ-Konsolidierung: User (9x dupliziert) und Event (8x dupliziert) Interfaces in zentrale types/ Dateien zusammenfuehren und as-any bei Event-Properties eliminieren.

Purpose: Doppelte Interface-Definitionen sind die groesste technische Schuld — Aenderungen muessen in 9 bzw. 8 Dateien nachgezogen werden. Die as-any Casts bei Event-Properties (has_timeslots, waitlist_enabled etc.) sind direkte Folge fehlender Felder im Typ.
Output: Zwei neue Type-Dateien (user.ts, event.ts), alle Consumer aktualisiert, ~30 as-any eliminiert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aktuelle User-Interfaces (3 identische Kern-Definitionen) -->

From frontend/src/services/auth.ts (Zeile 7):
```typescript
interface User {
  id: number;
  type: 'admin' | 'konfi' | 'teamer' | 'user';
  display_name: string;
  username?: string;
  email?: string;
  organization?: string;
  organization_id?: number;
  roles?: string[];
  role_name?: string;
  jahrgang?: string;
  is_super_admin?: boolean;
}
```

From frontend/src/services/tokenStore.ts (Zeile 3) — identisch ohne Kommentar.
From frontend/src/contexts/AppContext.tsx (Zeile 66) — identisch mit Kommentar.

Lokale Chat-Varianten (kleiner, nur id/name/type/jahrgang):
- DirectMessageModal.tsx:27
- SimpleCreateChatModal.tsx:42
- MembersModal.tsx:54

Lokale Admin-Varianten (mehr Felder: username, is_active, role_title, created_at etc.):
- UsersView.tsx:38
- UserManagementModal.tsx:34
- AdminUsersPage.tsx:26

<!-- Aktuelle Event-Interfaces (8 Definitionen, verschiedene Feldmengen) -->

Vollstaendigstes Interface in EventDetailView.tsx (hat ~35 Felder inkl. has_timeslots, waitlist_*, booking_status).
EventModal.tsx hat Admin-spezifische Felder (teamer_needed, teamer_only, checkin_window, bring_items).
TeamerEventsPage.tsx hat teamer_count, material_count.
DashboardEvent in types/dashboard.ts hat nur ~15 Felder (Subset).

<!-- Bestehende types/ Dateien -->
frontend/src/types/chat.ts — Message, ChatRoom, Reaction etc. (gut strukturiert)
frontend/src/types/dashboard.ts — Badge, DashboardEvent, RankingEntry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zentrale User- und Event-Type-Dateien erstellen</name>
  <files>frontend/src/types/user.ts, frontend/src/types/event.ts, frontend/src/types/dashboard.ts</files>
  <read_first>
    - frontend/src/services/auth.ts (User Interface Zeile 7-19)
    - frontend/src/components/admin/UsersView.tsx (AdminUser Variante Zeile 38-52)
    - frontend/src/components/admin/modals/UserManagementModal.tsx (AdminUser Variante Zeile 34-48)
    - frontend/src/components/chat/modals/DirectMessageModal.tsx (ChatUser Variante Zeile 27-33)
    - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx (ChatUser Variante Zeile 42-51)
    - frontend/src/components/konfi/views/EventDetailView.tsx (vollstaendigstes Event Interface Zeile 57-100)
    - frontend/src/components/admin/modals/EventModal.tsx (Admin Event Variante Zeile 35-57)
    - frontend/src/components/teamer/pages/TeamerEventsPage.tsx (Teamer Event Variante Zeile 68-97)
    - frontend/src/types/dashboard.ts (bestehende DashboardEvent)
  </read_first>
  <action>
1. **frontend/src/types/user.ts** erstellen mit Union-Strategie:
   ```typescript
   // Kern-User (auth, tokenStore, AppContext)
   export interface BaseUser {
     id: number;
     type: 'admin' | 'konfi' | 'teamer' | 'user';
     display_name: string;
     username?: string;
     email?: string;
     organization?: string;
     organization_id?: number;
     roles?: string[];
     role_name?: string;
     jahrgang?: string;
     is_super_admin?: boolean;
   }

   // Admin-User-Verwaltung (UsersView, AdminUsersPage, UserManagementModal)
   export interface AdminUser {
     id: number;
     username: string;
     email?: string;
     display_name: string;
     role_title?: string;
     is_active: boolean;
     last_login_at?: string;
     created_at: string;
     updated_at: string;
     role_id?: number;
     role_name: string;
     role_display_name: string;
     assigned_jahrgaenge_count?: number;
     assigned_jahrgaenge?: { id: number; name: string }[];
     can_edit?: boolean;
   }

   // Chat-User (DirectMessage, SimpleCreateChat, Members)
   export interface ChatUser {
     id: number;
     name?: string;
     display_name?: string;
     type: 'admin' | 'konfi';
     jahrgang?: string;
     jahrgang_name?: string;
     role_name?: string;
     role_title?: string;
     role_description?: string;
   }
   ```

2. **frontend/src/types/event.ts** erstellen — ein vollstaendiges Event-Interface das ALLE Felder aus allen 8 Definitionen vereint (optional wo noetig). Zusaetzlich AdminEvent mit admin-spezifischen Feldern und ein re-export von Category/Timeslot:
   ```typescript
   export interface Category {
     id: number;
     name: string;
     description?: string;
     type: 'activity' | 'event' | 'both';
   }

   export interface Timeslot {
     id?: number;
     start_time: string;
     end_time: string;
     max_participants: number;
   }

   // Vollstaendiges Event (alle Felder, optionale wo nicht immer vorhanden)
   export interface Event {
     id: number;
     name: string;
     title?: string;           // Dashboard-Alias
     description?: string;
     event_date: string;
     date?: string;             // Dashboard-Alias
     event_end_time?: string;
     location?: string;
     location_maps_url?: string;
     points: number;
     point_type?: 'gottesdienst' | 'gemeinde';
     categories?: Category[];
     category_names?: string;
     type: string;
     max_participants: number;
     registration_opens_at?: string;
     registration_closes_at?: string;
     registered_count?: number;
     registration_status?: 'upcoming' | 'open' | 'closed' | 'cancelled';
     is_registered?: boolean;
     can_register?: boolean;
     waitlist_enabled?: boolean;
     max_waitlist_size?: number;
     waitlist_count?: number;
     waitlist_position?: number;
     cancelled?: boolean;
     attendance_status?: 'present' | 'absent' | null;
     registration_status_detail?: string;
     booking_status?: 'confirmed' | 'waitlist' | 'pending' | 'opted_out' | null;
     is_opted_out?: boolean;
     has_timeslots?: boolean;
     mandatory?: boolean;
     bring_items?: string;
     checkin_window?: number;
     teamer_needed?: boolean;
     teamer_only?: boolean;
     teamer_count?: number;
     material_count?: number;
     is_series?: boolean;
     booked_timeslot_start?: string;
     booked_timeslot_end?: string;
     // Dashboard-Felder
     max_participants_display?: number;
   }
   ```
   Felder aus ALLEN 8 Definitionen vereinen. Jedes Feld das in mindestens einer Definition existiert muss enthalten sein.

3. **frontend/src/types/dashboard.ts** aktualisieren: DashboardEvent entfernen und stattdessen `export { Event as DashboardEvent } from './event'` re-exportieren. Badge und RankingEntry bleiben.

WICHTIG: Beim Erstellen der Interfaces ALLE Felder aus ALLEN Definitionen sammeln. Nichts weglassen. Im Zweifel optional machen (?).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && cat frontend/src/types/user.ts | grep -c "export interface" && cat frontend/src/types/event.ts | grep -c "export interface" && echo "Types created"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface" frontend/src/types/user.ts` ergibt mindestens 3 (BaseUser, AdminUser, ChatUser)
    - `grep -c "export interface" frontend/src/types/event.ts` ergibt mindestens 3 (Category, Timeslot, Event)
    - `grep "has_timeslots" frontend/src/types/event.ts` findet einen Treffer
    - `grep "waitlist_enabled" frontend/src/types/event.ts` findet einen Treffer
    - `grep "booking_status" frontend/src/types/event.ts` findet einen Treffer
    - `grep "teamer_needed" frontend/src/types/event.ts` findet einen Treffer
  </acceptance_criteria>
  <done>Zentrale Type-Dateien existieren mit allen Feldern aus allen bisherigen Definitionen</done>
</task>

<task type="auto">
  <name>Task 2: Alle Consumer auf zentrale Types umstellen + as-any eliminieren</name>
  <files>
    frontend/src/services/auth.ts,
    frontend/src/services/tokenStore.ts,
    frontend/src/contexts/AppContext.tsx,
    frontend/src/components/chat/modals/DirectMessageModal.tsx,
    frontend/src/components/chat/modals/SimpleCreateChatModal.tsx,
    frontend/src/components/chat/modals/MembersModal.tsx,
    frontend/src/components/admin/UsersView.tsx,
    frontend/src/components/admin/modals/UserManagementModal.tsx,
    frontend/src/components/admin/pages/AdminUsersPage.tsx,
    frontend/src/components/konfi/views/EventDetailView.tsx,
    frontend/src/components/konfi/views/EventsView.tsx,
    frontend/src/components/konfi/pages/KonfiDashboardPage.tsx,
    frontend/src/components/konfi/pages/KonfiEventsPage.tsx,
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx,
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx,
    frontend/src/components/admin/EventsView.tsx,
    frontend/src/components/admin/modals/EventModal.tsx
  </files>
  <read_first>
    - frontend/src/types/user.ts (gerade erstellt in Task 1)
    - frontend/src/types/event.ts (gerade erstellt in Task 1)
    - Alle files_modified Dateien — jeweils die lokale `interface User`/`interface Event` Stelle finden
  </read_first>
  <action>
Fuer JEDE Datei:

**User-Consumer (9 Dateien):**
1. `frontend/src/services/auth.ts` — Lokales `interface User` (Zeile 7-19) entfernen, `import { BaseUser } from '../types/user'` hinzufuegen, alle `User` Referenzen durch `BaseUser` ersetzen
2. `frontend/src/services/tokenStore.ts` — Lokales `interface User` (Zeile 3-15) entfernen, `import { BaseUser } from '../types/user'` hinzufuegen, `User` durch `BaseUser` ersetzen
3. `frontend/src/contexts/AppContext.tsx` — Lokales `interface User` (Zeile 66-78) entfernen, `import { BaseUser } from '../types/user'` hinzufuegen, `User` durch `BaseUser` ersetzen. ACHTUNG: AppContextType hat `user: User | null` — muss `user: BaseUser | null` werden
4. `frontend/src/components/chat/modals/DirectMessageModal.tsx` — Lokales `interface User` entfernen, `import { ChatUser } from '../../../types/user'` hinzufuegen, alle `User` durch `ChatUser` ersetzen
5. `frontend/src/components/chat/modals/SimpleCreateChatModal.tsx` — wie DirectMessageModal
6. `frontend/src/components/chat/modals/MembersModal.tsx` — wie DirectMessageModal
7. `frontend/src/components/admin/UsersView.tsx` — Lokales `interface User` entfernen, `import { AdminUser } from '../../types/user'` hinzufuegen, alle `User` durch `AdminUser` ersetzen
8. `frontend/src/components/admin/modals/UserManagementModal.tsx` — wie UsersView
9. `frontend/src/components/admin/pages/AdminUsersPage.tsx` — wie UsersView

**Event-Consumer (8 Dateien):**
1. `frontend/src/components/konfi/views/EventDetailView.tsx` — Lokales `interface Event` + lokales `interface Category` entfernen, `import { Event, Category } from '../../../types/event'` hinzufuegen. ALLE `(eventData as any).has_timeslots` durch `eventData.has_timeslots` ersetzen (kein as-any mehr noetig). Gleiches fuer waitlist_count, waitlist_position, waitlist_enabled, booking_status.
2. `frontend/src/components/konfi/views/EventsView.tsx` — Lokales Event entfernen, import aus types/event
3. `frontend/src/components/konfi/pages/KonfiDashboardPage.tsx` — Lokales Event entfernen, import aus types/event
4. `frontend/src/components/konfi/pages/KonfiEventsPage.tsx` — Lokales Event entfernen, import aus types/event
5. `frontend/src/components/teamer/pages/TeamerEventsPage.tsx` — Lokales Event + Category entfernen, import aus types/event
6. `frontend/src/components/teamer/pages/TeamerDashboardPage.tsx` — Lokales DashboardEvent entfernen, import aus types/event (oder types/dashboard Re-Export)
7. `frontend/src/components/admin/EventsView.tsx` — Lokales Event + Category entfernen, import aus types/event
8. `frontend/src/components/admin/modals/EventModal.tsx` — Lokales Event + Category + Jahrgang + Timeslot entfernen, import aus types/event. ALLE `(event as any).waitlist_enabled` etc. durch `event.waitlist_enabled` ersetzen.

KRITISCH: Nach dem Entfernen der lokalen Interfaces und dem Hinzufuegen der Imports: `(something as any).field` Casts entfernen wo der Typ jetzt das Feld direkt hat. Das betrifft ~20 Stellen in EventDetailView.tsx und ~8 Stellen in EventModal.tsx.

Sicherstellen dass der Build nach allen Aenderungen erfolgreich ist.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "interface User {" frontend/src/services/auth.ts frontend/src/services/tokenStore.ts frontend/src/contexts/AppContext.tsx` ergibt 0 Treffer
    - `grep -rn "interface User {" frontend/src/components/chat/ frontend/src/components/admin/UsersView.tsx frontend/src/components/admin/modals/UserManagementModal.tsx frontend/src/components/admin/pages/AdminUsersPage.tsx` ergibt 0 Treffer
    - `grep -rn "interface Event {" frontend/src/components/` ergibt 0 Treffer
    - `grep -c "as any" frontend/src/components/konfi/views/EventDetailView.tsx` ist kleiner als 5 (vorher 11)
    - `grep -c "as any" frontend/src/components/admin/modals/EventModal.tsx` ist kleiner als 3 (vorher 8)
    - `npx tsc --noEmit` laeuft ohne Errors
  </acceptance_criteria>
  <done>Alle 17 Dateien nutzen zentrale Types, ~30 as-any Casts bei Event-Properties eliminiert, TypeScript-Build erfolgreich</done>
</task>

</tasks>

<verification>
- `grep -rn "interface User {" frontend/src/ --include="*.ts" --include="*.tsx"` zeigt nur noch frontend/src/types/user.ts
- `grep -rn "interface Event {" frontend/src/ --include="*.ts" --include="*.tsx"` zeigt nur noch frontend/src/types/event.ts (plus LiveUpdateEvent in LiveUpdateContext.tsx — anderer Typ)
- `grep -c "as any" frontend/src/components/konfi/views/EventDetailView.tsx` deutlich reduziert
- `cd frontend && npx tsc --noEmit` erfolgreich
</verification>

<success_criteria>
- User Interface existiert nur noch in types/user.ts (3 Varianten: BaseUser, AdminUser, ChatUser)
- Event Interface existiert nur noch in types/event.ts (1 vollstaendiges Interface)
- TypeScript Build erfolgreich ohne Errors
- as-any bei Event-Properties (has_timeslots, waitlist_*, booking_status etc.) eliminiert
</success_criteria>

<output>
After completion, create `.planning/phases/63-codebase-cleanup/63-01-SUMMARY.md`
</output>
