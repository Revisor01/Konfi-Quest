---
phase: 63-codebase-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/services/api.ts
  - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx
  - frontend/src/components/admin/modals/LevelManagementModal.tsx
  - frontend/src/components/admin/modals/BadgeManagementModal.tsx
  - frontend/src/components/admin/modals/ActivityManagementModal.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements:
  - CLEANUP-02
must_haves:
  truths:
    - "Keine auskommentierten console.log Statements mehr in App.tsx"
    - "Kein Debug-console.log in api.ts Retry-Logik"
    - "Keine doppelten console.error Aufrufe bei Error-Handling"
    - "Verbleibende as-any in Nicht-Event-Dateien wo moeglich reduziert"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Bereinigtes App-Root ohne tote Kommentare"
    - path: "frontend/src/services/api.ts"
      provides: "API-Service ohne Debug-Logging"
  key_links: []
---

<objective>
Console-Cleanup und restliche as-any Reduktion: Auskommentierte/Debug console.logs entfernen, doppelte error-Logs bereinigen, verbleibende as-any in Nicht-Event-Dateien reduzieren.

Purpose: Debug-Artefakte aus v2.1 Development entfernen und verbleibende Type-Safety-Luecken schliessen.
Output: Sauberer Code ohne Debug-Reste, weniger as-any Casts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Console-Log Cleanup + doppelte Error-Logs</name>
  <files>frontend/src/App.tsx, frontend/src/services/api.ts, frontend/src/components/chat/modals/SimpleCreateChatModal.tsx</files>
  <read_first>
    - frontend/src/App.tsx (Zeile 150-160 — auskommentierte console.logs)
    - frontend/src/services/api.ts (Zeile 24 — Debug console.log im Retry)
    - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx (Zeile 304+345 — doppeltes console.error)
  </read_first>
  <action>
1. **frontend/src/App.tsx** (Zeile 153, 156): Die auskommentierten `// console.log('AppContent: Starting auto-refresh...')` und `// console.log('AppContent: Stopping auto-refresh')` Zeilen komplett entfernen (nicht nur den Kommentar — die ganze Zeile loeschen).

2. **frontend/src/services/api.ts** (Zeile 24): `console.log(`Retry ${retryCount} fuer ${error.config?.url}`)` entfernen. Das ist ein Debug-Log das bei jedem Retry in Production loggt — unnoetig und informationsarm.

3. **frontend/src/components/chat/modals/SimpleCreateChatModal.tsx**: Zeile 344-345 hat doppeltes Error-Logging:
   ```
   console.error('Error creating group chat:', err);
   console.error('Error response:', err.response?.data);
   ```
   Die zweite Zeile (345) entfernen — die erste reicht, und err.response?.data ist bereits im Error-Objekt enthalten.

WICHTIG: console.error und console.warn in catch-Bloecken NICHT entfernen — die sind legitimate Error-Handling. Nur Debug-Logs und auskommentierte Logs entfernen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "// console.log" frontend/src/App.tsx | wc -l && grep -n "console.log" frontend/src/services/api.ts | wc -l && echo "Done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "// console.log" frontend/src/App.tsx` ergibt 0 Treffer
    - `grep "console.log" frontend/src/services/api.ts` ergibt 0 Treffer
    - `grep -c "console.error.*Error response" frontend/src/components/chat/modals/SimpleCreateChatModal.tsx` ergibt 0
  </acceptance_criteria>
  <done>Debug-Logs und auskommentierte Logs entfernt, doppeltes Error-Logging bereinigt</done>
</task>

<task type="auto">
  <name>Task 2: Verbleibende as-any Reduktion (Nicht-Event-Dateien)</name>
  <files>
    frontend/src/components/admin/modals/LevelManagementModal.tsx,
    frontend/src/components/admin/modals/BadgeManagementModal.tsx,
    frontend/src/components/admin/modals/ActivityManagementModal.tsx,
    frontend/src/components/admin/views/KonfiDetailView.tsx
  </files>
  <read_first>
    - frontend/src/components/admin/modals/LevelManagementModal.tsx (Zeile 423 — `[] as any[]`)
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx (Zeile 907 — `[] as any[]`)
    - frontend/src/components/admin/modals/ActivityManagementModal.tsx (Zeile 152 — `(activity as any).target_role`)
    - frontend/src/components/admin/views/KonfiDetailView.tsx (Zeile 442 — `(activity as any).requestId`)
  </read_first>
  <action>
1. **LevelManagementModal.tsx** (Zeile 423): `[] as any[]` — Den reduce-Accumulator korrekt typisieren. Kontext lesen um den Typ der Gruppe zu verstehen, dann `[] as GroupType[]` oder ein passendes Interface verwenden.

2. **BadgeManagementModal.tsx** (Zeile 907): Gleiche Situation wie LevelManagementModal — reduce-Accumulator typisieren.

3. **ActivityManagementModal.tsx** (Zeile 152): `(activity as any).target_role` — Das `activity` Interface um `target_role?: string` erweitern (lokales Interface in dieser Datei pruefen und Feld hinzufuegen).

4. **KonfiDetailView.tsx** (Zeile 442): `(activity as any).requestId` — Das lokale Activity-Interface um `requestId?: number` erweitern.

NICHT anfassen (akzeptable as-any):
- `window as any` fuer Capacitor Plugin-Zugriffe (keine bessere Loesung)
- `style={...} as any` fuer CSS Custom Properties (Ionic-spezifisch, kein Fix moeglich)
- `e.detail.value as any` bei IonSegment onIonChange (Ionic Typing-Problem)
- `presentCertPopover({ event: e as any })` (Ionic Event-Typing)

Nur die 4 genannten Stellen aendern, wo ein konkreter Typ existiert oder leicht erstellt werden kann.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "as any" frontend/src/components/admin/modals/LevelManagementModal.tsx | grep -v "style\|window\|detail.value"` ergibt 0 Treffer
    - `grep "as any" frontend/src/components/admin/modals/BadgeManagementModal.tsx | grep -v "style\|window\|detail.value"` ergibt 0 Treffer
    - `grep "(activity as any)" frontend/src/components/admin/modals/ActivityManagementModal.tsx` ergibt 0 Treffer
    - `grep "(activity as any)" frontend/src/components/admin/views/KonfiDetailView.tsx` ergibt 0 Treffer
    - `npx tsc --noEmit` erfolgreich
  </acceptance_criteria>
  <done>4 as-any Stellen in Nicht-Event-Dateien eliminiert, TypeScript Build erfolgreich</done>
</task>

</tasks>

<verification>
- `grep -rn "console.log" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules` zeigt nur noch sinnvolle Logs
- `grep -c "as any" frontend/src/ -r --include="*.ts" --include="*.tsx"` ist deutlich reduziert (Ziel: unter 50, vorher 76)
- `cd frontend && npx tsc --noEmit` erfolgreich
</verification>

<success_criteria>
- Keine auskommentierten console.logs mehr
- Kein Debug-console.log in api.ts
- 4 weitere as-any Stellen eliminiert
- TypeScript Build erfolgreich
</success_criteria>

<output>
After completion, create `.planning/phases/63-codebase-cleanup/63-02-SUMMARY.md`
</output>
