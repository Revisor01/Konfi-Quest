---
phase: 46-event-admin-teamer-logik
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/admin/modals/ParticipantManagementModal.tsx
  - backend/routes/events.js
autonomous: true
requirements: [EVT-v19-05, EVT-v19-10, EVT-v19-11, EVT-v19-12]

must_haves:
  truths:
    - "Admin kann ein Event ueber einen Button in den Details absagen"
    - "Teamer-gesucht-Hinweis verschwindet wenn Teamer-Sektion mit Count angezeigt wird"
    - "Konfis hinzufuegen zeigt nur Konfis des Jahrgangs, Teamer hinzufuegen zeigt nur Teamer"
    - "Admin kann aus einem Event einen Chat erstellen mit allen Teilnehmer:innen"
    - "Wenn Chat existiert zeigt Button 'Zum Chat' statt 'Chat erstellen'"
  artifacts:
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "Cancel-Button, Chat-Button, getrennte Add-Buttons, Teamer-Hinweis-Fix"
      contains: "cancel"
    - path: "frontend/src/components/admin/modals/ParticipantManagementModal.tsx"
      provides: "filterRole Prop fuer Teamer/Konfi Filterung"
      contains: "filterRole"
    - path: "backend/routes/events.js"
      provides: "chat_room_id im GET /:id Response"
      contains: "chat_room_id"
  key_links:
    - from: "EventDetailView.tsx"
      to: "PUT /api/events/:id/cancel"
      via: "api.put call on cancel button click"
      pattern: "api\\.put.*cancel"
    - from: "EventDetailView.tsx"
      to: "POST /api/events/:id/chat"
      via: "api.post call on chat button click"
      pattern: "api\\.post.*chat"
    - from: "EventDetailView.tsx"
      to: "ParticipantManagementModal"
      via: "filterRole prop passed to modal"
      pattern: "filterRole"
---

<objective>
EventDetailView: Cancel-Button, Chat-Erstellung, getrennte Teilnehmer-Buttons, Teamer-Hinweis-Fix

Purpose: Admin benoetigt vollstaendige Event-Verwaltung direkt aus den Details — absagen, Chat erstellen, und korrekt getrennte Hinzufuegen-Buttons.
Output: Funktionale Cancel/Chat Buttons, gefixte Teamer-Hinweise, getrennte Participant-Add-Modals
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-event-admin-teamer-logik/46-CONTEXT.md

<interfaces>
<!-- Backend Cancel-Route (bereits vorhanden, events.js:1986-2050) -->
PUT /api/events/:id/cancel
Request: { notification_message?: string }
Response: { message, participants_notified, notification_message }

<!-- Backend Chat-Route (bereits vorhanden, events.js:1930-1983) -->
POST /api/events/:id/chat
Request: (keine body-Parameter noetig)
Response: { chat_room_id, message, participants_added }
Conflict 409: { error: 'Chat existiert bereits fuer dieses Event' }

<!-- GET /api/events/:id Response (events.js:516-640) -->
Returns: { ...event_fields, participants, timeslots, jahrgaenge, categories, unregistrations, ... }
Note: chat_room_id wird noch NICHT zurueckgegeben — muss hinzugefuegt werden

<!-- ParticipantManagementModal Props (aktuell) -->
interface ParticipantManagementModalProps {
  eventId: number;
  onClose: () => void;
  onSuccess: () => void;
  dismiss?: () => void;
}
<!-- Braucht neuen Prop: filterRole?: 'teamer' | 'konfi' | null -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — chat_room_id und Teamer-user_type im GET /:id Response</name>
  <files>backend/routes/events.js</files>
  <read_first>
    - backend/routes/events.js (Zeile 516-640 fuer GET /:id Route)
    - backend/routes/events.js (Zeile 1930-1983 fuer POST /:id/chat Route)
    - backend/routes/events.js (Zeile 1957-1965 fuer participant user_type)
  </read_first>
  <action>
Zwei Aenderungen am GET /:id Endpoint (events.js, ab Zeile 516):

**1. chat_room_id Query hinzufuegen:**
Nach dem `unregistrations` Query (Zeile ~611) und vor dem `registeredCount` Berechnung (Zeile ~614), eine neue Query einfuegen:

```javascript
      // Check if event has an associated chat room
      const { rows: [eventChat] } = await db.query(
        "SELECT id FROM chat_rooms WHERE event_id = $1",
        [eventId]
      );
```

**2. chat_room_id in Response aufnehmen:**
Im `res.json({...})` Block (Zeile ~623-635), nach `available_spots` hinzufuegen:

```javascript
        chat_room_id: eventChat?.id || null
```

**3. Teamer user_type in Chat-Erstellung fixen:**
In der POST /:id/chat Route (Zeile ~1957-1965) werden alle Participants als 'konfi' eingefuegt. Das ist falsch fuer Teamer. Die Query anpassen:

Ersetze den Block ab Zeile 1957:
```javascript
      const { rows: participants } = await client.query(`
        SELECT DISTINCT eb.user_id, r.name as role_name
        FROM event_bookings eb
        JOIN users u ON eb.user_id = u.id
        JOIN roles r ON u.role_id = r.id
        WHERE eb.event_id = $1 AND eb.status = 'confirmed'
      `, [eventId]);

      if (participants.length > 0) {
        for (const p of participants) {
          if (p.user_id === req.user.id) continue; // Creator already added
          const userType = p.role_name === 'teamer' ? 'teamer' : 'konfi';
          await client.query(
            "INSERT INTO chat_participants (room_id, user_id, user_type) VALUES ($1, $2, $3) ON CONFLICT DO NOTHING",
            [chatRoomId, p.user_id, userType]
          );
        }
      }
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "chat_room_id" backend/routes/events.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "chat_room_id: eventChat" backend/routes/events.js findet 1 Treffer
    - grep "SELECT id FROM chat_rooms WHERE event_id" backend/routes/events.js findet mindestens 2 Treffer (GET /:id + POST /:id/chat)
    - grep "role_name === 'teamer'" backend/routes/events.js findet 1 Treffer (user_type fix)
    - grep "ON CONFLICT DO NOTHING" backend/routes/events.js findet mindestens 1 Treffer
  </acceptance_criteria>
  <done>GET /events/:id gibt chat_room_id zurueck. Chat-Erstellung setzt korrekten user_type fuer Teamer.</done>
</task>

<task type="auto">
  <name>Task 2: ParticipantManagementModal — filterRole Prop</name>
  <files>frontend/src/components/admin/modals/ParticipantManagementModal.tsx</files>
  <read_first>
    - frontend/src/components/admin/modals/ParticipantManagementModal.tsx
  </read_first>
  <action>
filterRole Prop zum ParticipantManagementModal hinzufuegen:

**1. Interface erweitern** (Zeile ~63-68):
```typescript
interface ParticipantManagementModalProps {
  eventId: number;
  onClose: () => void;
  onSuccess: () => void;
  dismiss?: () => void;
  filterRole?: 'teamer' | 'konfi' | null;
}
```

**2. Prop destrukturieren** (Zeile ~70-75):
```typescript
const ParticipantManagementModal: React.FC<ParticipantManagementModalProps> = ({
  eventId,
  onClose,
  onSuccess,
  dismiss,
  filterRole
}) => {
```

**3. Filterung in loadAvailableKonfis anpassen** (Zeile ~119-148):
Nach dem bestehenden Filter `const available = allPersons.filter(...)` (Zeile ~134), eine zusaetzliche Filterung hinzufuegen:

```typescript
      // Filter by role if filterRole prop is set
      let roleFiltered = available;
      if (filterRole === 'teamer') {
        roleFiltered = available.filter((p: Konfi) => p.role_name === 'teamer');
      } else if (filterRole === 'konfi') {
        roleFiltered = available.filter((p: Konfi) => p.role_name !== 'teamer');
      }
```

Dann die Variable `available` in den nachfolgenden Zeilen durch `roleFiltered` ersetzen:
- Zeile ~137: `const jahrgaenge = [...new Set(roleFiltered.filter...` statt `available.filter...`
- Zeile ~144: `setAvailableKonfis(roleFiltered);` statt `setAvailableKonfis(available);`

**4. Header-Titel anpassen** basierend auf filterRole:
Im `<IonTitle>` (suche nach dem Titel-Text, wahrscheinlich "Teilnehmer:in hinzufuegen" oder aehnlich), den Titel dynamisch machen:

```typescript
<IonTitle>{filterRole === 'teamer' ? 'Teamer:in hinzufuegen' : filterRole === 'konfi' ? 'Kind hinzufuegen' : 'Teilnehmer:in hinzufuegen'}</IonTitle>
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "filterRole" frontend/src/components/admin/modals/ParticipantManagementModal.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "filterRole" ParticipantManagementModal.tsx findet mindestens 5 Treffer (Interface, Destrukturierung, Filter-Logik, Titel)
    - grep "Kind hinzufuegen" ParticipantManagementModal.tsx findet 1 Treffer
    - grep "Teamer:in hinzufuegen" ParticipantManagementModal.tsx findet 1 Treffer
    - grep "role_name === 'teamer'" ParticipantManagementModal.tsx findet mindestens 1 Treffer
  </acceptance_criteria>
  <done>ParticipantManagementModal akzeptiert filterRole Prop und filtert Personen nach Rolle. Titel passt sich an.</done>
</task>

<task type="auto">
  <name>Task 3: EventDetailView — Cancel, Chat, getrennte Add-Buttons, Teamer-Hinweis-Fix</name>
  <files>frontend/src/components/admin/views/EventDetailView.tsx</files>
  <read_first>
    - frontend/src/components/admin/views/EventDetailView.tsx
    - frontend/src/components/admin/modals/ParticipantManagementModal.tsx (fuer filterRole Interface)
  </read_first>
  <action>
Vier Aenderungen in EventDetailView.tsx:

**A. Teamer-Hinweis-Fix (EVT-v19-10):**
Zeile 710-717: Die Bedingung fuer den "Teamer gesucht" Badge aendern. Statt:
```tsx
{(eventData?.teamer_needed || eventData?.teamer_only) && (
```
Aendern zu:
```tsx
{(eventData?.teamer_needed && teamerParticipants.length === 0) && (
```
Hinweis: `teamerParticipants` ist weiter unten definiert (~Zeile 1020-1040). Pruefen ob die Variable an dieser Stelle im Scope ist. Falls nicht, muss die Logik in den Bereich verschoben werden wo `teamerParticipants` definiert ist, ODER eine separate Variable frueh berechnet werden. Wahrscheinlich ist `teamerParticipants` als State/Variable vor dem JSX return definiert — pruefen und ggf. anpassen.

Falls `teamerParticipants` erst spaeter berechnet wird, eine fruehe Berechnung hinzufuegen (nach dem participants-State/fetch):
```typescript
const teamerParticipantsCount = participants.filter(p => (p as any).role_name === 'teamer' && p.status === 'confirmed').length;
```
Und die Bedingung:
```tsx
{(eventData?.teamer_needed && teamerParticipantsCount === 0) && (
```

**B. Getrennte Hinzufuegen-Buttons (EVT-v19-11):**

Zwei separate useIonModal Hooks fuer Teamer und Konfi erstellen. Der bestehende `presentParticipantModalHook` wird dupliziert.

Imports anpassen — ParticipantManagementModal bereits importiert.

Zwei neue Modal-Hooks definieren (neben dem bestehenden):
```typescript
const [presentTeamerModal, dismissTeamerModal] = useIonModal(ParticipantManagementModal, {
  eventId: eventData?.id,
  onClose: () => dismissTeamerModal(),
  onSuccess: () => { dismissTeamerModal(); loadEventData(); },
  filterRole: 'teamer'
});

const [presentKonfiModal, dismissKonfiModal] = useIonModal(ParticipantManagementModal, {
  eventId: eventData?.id,
  onClose: () => dismissKonfiModal(),
  onSuccess: () => { dismissKonfiModal(); loadEventData(); },
  filterRole: 'konfi'
});
```

In der Konfis-Sektion (Zeile ~1136-1144): Den "Teilnehmer:in hinzufuegen" Button aendern:
- Text: "Kind hinzufuegen" (statt "Teilnehmer:in hinzufuegen")
- onClick: `presentKonfiModal` statt `presentParticipantModalHook`
- Icon bleibt `personAdd`

In der Teamer-Sektion (Zeile ~1163-1171): Den "Teamer:in hinzufuegen" Button aendern:
- onClick: `presentTeamerModal` statt `presentParticipantModalHook`
- Text bleibt "Teamer:in hinzufuegen"

Im "leere Teilnehmer" Button (Zeile ~1092-1099): Dort den alten `presentParticipantModalHook` durch `presentKonfiModal` ersetzen (oder je nach teamer_only Status entscheiden).

Im "keine Konfis aber Teamer" Button (Zeile ~1183-1189): Durch `presentKonfiModal` ersetzen.

**C. Cancel-Button (EVT-v19-05):**

Imports hinzufuegen: `ban` Icon aus ionicons/icons (falls nicht vorhanden), `useIonAlert` aus @ionic/react.

Alert-Hook: `const [presentAlert] = useIonAlert();`

Cancel-Handler Funktion definieren:
```typescript
const handleCancelEvent = async () => {
  presentAlert({
    header: 'Event absagen',
    message: 'Wirklich absagen? Alle Teilnehmer:innen werden benachrichtigt.',
    buttons: [
      { text: 'Abbrechen', role: 'cancel' },
      {
        text: 'Absagen',
        role: 'destructive',
        handler: async () => {
          try {
            await api.put(`/events/${eventData?.id}/cancel`);
            setSuccess('Event wurde abgesagt');
            loadEventData();
          } catch (error: any) {
            setError(error.response?.data?.error || 'Fehler beim Absagen');
          }
        }
      }
    ]
  });
};
```

Cancel-Button VOR dem schliessenden `</IonContent>` (Zeile ~1291), nach dem Material-Bereich einfuegen:
```tsx
{/* Event absagen */}
{eventData && !isCancelled && (
  <IonList className="app-section-inset" inset={true}>
    <IonCard className="app-card">
      <IonCardContent className="app-card-content">
        <IonButton
          expand="block"
          fill="outline"
          color="danger"
          onClick={handleCancelEvent}
        >
          <IonIcon icon={ban} className="app-event-detail__icon-gap" />
          Event absagen
        </IonButton>
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```

Sicherstellen dass `isCancelled` korrekt definiert ist (Zeile ~316 oder ~341, pruefen welche im aktuellen Scope gilt). Falls `cancelled` direkt auf `eventData` liegt (aus `SELECT * FROM events`), dann:
```typescript
const isCancelled = eventData?.cancelled || eventData?.registration_status === 'cancelled';
```

**D. Chat-Erstellung-Button (EVT-v19-12):**

Import hinzufuegen: `chatbubbles` aus ionicons/icons.

Chat-Handler Funktion:
```typescript
const handleCreateEventChat = async () => {
  try {
    const response = await api.post(`/events/${eventData?.id}/chat`);
    setSuccess('Chat erstellt');
    loadEventData(); // Reload to get chat_room_id
  } catch (error: any) {
    setError(error.response?.data?.error || 'Fehler beim Erstellen des Chats');
  }
};

const handleNavigateToChat = () => {
  // Navigate to chat room
  window.location.href = `/chat/${eventData?.chat_room_id}`;
};
```

ACHTUNG: Navigation zum Chat haengt von der App-Routing-Struktur ab. Pruefen wie andere Chat-Navigationen funktionieren (grep nach `history.push.*chat` oder `routerLink.*chat`). Wahrscheinlich ueber Ionic Router: `history.push(\`/admin/chat/${eventData?.chat_room_id}\`)` oder aehnlich. Den useIonRouter oder useHistory Hook verwenden falls vorhanden.

Chat-Button nach dem Cancel-Button und vor `</IonContent>` einfuegen:
```tsx
{/* Event-Chat */}
{eventData && !isCancelled && (
  <IonList className="app-section-inset" inset={true}>
    <IonCard className="app-card">
      <IonCardContent className="app-card-content">
        {eventData.chat_room_id ? (
          <IonButton
            expand="block"
            fill="outline"
            onClick={handleNavigateToChat}
          >
            <IonIcon icon={chatbubbles} className="app-event-detail__icon-gap" />
            Zum Chat
          </IonButton>
        ) : (
          <IonButton
            expand="block"
            fill="outline"
            onClick={handleCreateEventChat}
          >
            <IonIcon icon={chatbubbles} className="app-event-detail__icon-gap" />
            Chat erstellen
          </IonButton>
        )}
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "handleCancelEvent\|handleCreateEventChat\|filterRole\|presentTeamerModal\|presentKonfiModal" frontend/src/components/admin/views/EventDetailView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "handleCancelEvent" EventDetailView.tsx findet mindestens 2 Treffer (Definition + onClick)
    - grep "handleCreateEventChat" EventDetailView.tsx findet mindestens 2 Treffer (Definition + onClick)
    - grep "presentTeamerModal" EventDetailView.tsx findet mindestens 2 Treffer (Hook + onClick)
    - grep "presentKonfiModal" EventDetailView.tsx findet mindestens 2 Treffer (Hook + onClick)
    - grep "Kind hinzufuegen" EventDetailView.tsx findet mindestens 1 Treffer
    - grep "Event absagen" EventDetailView.tsx findet mindestens 1 Treffer
    - grep "Chat erstellen" EventDetailView.tsx findet mindestens 1 Treffer
    - grep "Zum Chat" EventDetailView.tsx findet mindestens 1 Treffer
    - grep "teamerParticipants.length === 0\|teamerParticipantsCount === 0" EventDetailView.tsx findet mindestens 1 Treffer (Teamer-Hinweis-Fix)
    - grep "chatbubbles" EventDetailView.tsx findet mindestens 1 Treffer (Import)
    - grep "useIonAlert" EventDetailView.tsx findet mindestens 1 Treffer (Import)
  </acceptance_criteria>
  <done>Cancel-Button mit Bestaetigung, Chat-Button (erstellen/navigieren), getrennte Teamer/Konfi Add-Buttons, redundanter Teamer-Hinweis entfernt</done>
</task>

</tasks>

<verification>
- Backend: GET /events/:id gibt chat_room_id zurueck
- Backend: POST /events/:id/chat setzt korrekten user_type fuer Teamer
- Frontend: Cancel-Button am Ende der Event-Details mit Bestaetigung
- Frontend: Chat-Button zeigt "Chat erstellen" oder "Zum Chat"
- Frontend: "Kind hinzufuegen" Button in Konfis-Sektion
- Frontend: "Teamer:in hinzufuegen" Button in Teamer-Sektion
- Frontend: Teamer-gesucht Hinweis nur wenn keine Teamer-Sektion sichtbar
- Frontend kompiliert: `cd frontend && npx tsc --noEmit`
- Backend startet: `cd backend && node -e "require('./routes/events')"` (Syntax-Check)
</verification>

<success_criteria>
- Cancel/Chat Buttons funktional in EventDetailView
- ParticipantManagementModal filtert nach Rolle
- Kein redundanter Teamer-Hinweis
- Keine Kompilierungsfehler
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-admin-teamer-logik/46-02-SUMMARY.md`
</output>
