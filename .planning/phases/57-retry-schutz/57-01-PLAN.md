---
phase: 57-retry-schutz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api.ts
  - frontend/src/hooks/useActionGuard.ts
  - frontend/package.json
autonomous: true
requirements: [RET-01, RET-02]

must_haves:
  truths:
    - "Transiente Netzwerk-Fehler (5xx, 408, 429) werden automatisch 3x mit Exponential Backoff wiederholt"
    - "Alle Submit-Buttons sind waehrend laufendem Request disabled und zeigen Loading-Spinner"
    - "4xx Fehler werden NICHT wiederholt (sofort reject)"
  artifacts:
    - path: "frontend/src/services/api.ts"
      provides: "axios-retry Konfiguration"
      contains: "axiosRetry"
    - path: "frontend/src/hooks/useActionGuard.ts"
      provides: "Reusable Hook fuer Double-Submit-Schutz"
      exports: ["useActionGuard"]
  key_links:
    - from: "frontend/src/hooks/useActionGuard.ts"
      to: "alle Modals mit Submit-Buttons"
      via: "Hook-Import in Modals"
      pattern: "useActionGuard"
---

<objective>
axios-retry fuer automatische Wiederholung transienter Fehler einrichten und einen useActionGuard Hook erstellen, der Double-Submit-Schutz in allen Modals/Submit-Flows ermoeglicht.

Purpose: Netzwerk-Resilienz bei instabiler Verbindung (z.B. Tunnelfahrt) und Schutz gegen versehentliches Doppel-Absenden.
Output: axios-retry auf api.ts, useActionGuard Hook, alle ~29 Modals mit Submit-Buttons geschuetzt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/services/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: axios-retry Konfiguration + useActionGuard Hook</name>
  <files>frontend/src/services/api.ts, frontend/src/hooks/useActionGuard.ts, frontend/package.json</files>
  <read_first>
    - frontend/src/services/api.ts (bestehender Axios-Interceptor)
    - frontend/package.json (aktuelle Dependencies)
  </read_first>
  <action>
1. `cd frontend && npm install axios-retry` installieren.

2. In `frontend/src/services/api.ts`:
   - `import axiosRetry from 'axios-retry';` hinzufuegen
   - NACH der `axios.create()` Zeile und VOR den Interceptors:
   ```
   axiosRetry(api, {
     retries: 3,
     retryDelay: (retryCount) => {
       return axiosRetry.exponentialDelay(retryCount) + Math.random() * 200;
     },
     retryCondition: (error) => {
       const status = error.response?.status;
       if (status === 429) return true;
       return axiosRetry.isNetworkOrIdempotentRequestError(error) || (status !== undefined && status >= 500);
     },
     onRetry: (retryCount, error) => {
       console.log(`Retry ${retryCount} fuer ${error.config?.url}`);
     }
   });
   ```
   - WICHTIG: 4xx (ausser 429) werden NICHT wiederholt
   - WICHTIG: Retry-After Header wird von axios-retry automatisch respektiert

3. Neuen Hook `frontend/src/hooks/useActionGuard.ts` erstellen:
   ```typescript
   import { useState, useCallback, useRef } from 'react';

   interface ActionGuardResult {
     isSubmitting: boolean;
     guard: <T>(action: () => Promise<T>) => Promise<T>;
   }

   export function useActionGuard(): ActionGuardResult {
     const [isSubmitting, setIsSubmitting] = useState(false);
     const guardRef = useRef(false);

     const guard = useCallback(async <T>(action: () => Promise<T>): Promise<T> => {
       if (guardRef.current) {
         throw new Error('Aktion laeuft bereits');
       }
       guardRef.current = true;
       setIsSubmitting(true);
       try {
         const result = await action();
         return result;
       } finally {
         guardRef.current = false;
         setIsSubmitting(false);
       }
     }, []);

     return { isSubmitting, guard };
   }
   ```
   - `guardRef` verhindert Race Conditions (synchroner Check vor async setState)
   - Hook gibt `isSubmitting` (fuer UI-Binding) und `guard` (Wrapper-Funktion) zurueck
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -q "axiosRetry" src/services/api.ts && grep -q "useActionGuard" src/hooks/useActionGuard.ts && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "axiosRetry" frontend/src/services/api.ts` findet axios-retry Konfiguration
    - `grep -q "retries: 3" frontend/src/services/api.ts` bestaetigt 3 Retries
    - `grep -q "retryCondition" frontend/src/services/api.ts` bestaetigt custom Retry-Bedingung
    - `grep -q "export function useActionGuard" frontend/src/hooks/useActionGuard.ts` findet den Hook
    - `grep -q "isSubmitting" frontend/src/hooks/useActionGuard.ts` findet den State
    - `grep -q "guardRef" frontend/src/hooks/useActionGuard.ts` findet den Ref fuer Race-Condition-Schutz
    - `grep -q "axios-retry" frontend/package.json` bestaetigt Installation
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>axios-retry wiederholt 5xx/408/429 automatisch 3x mit Exponential Backoff + Jitter. useActionGuard Hook existiert und exportiert isSubmitting + guard.</done>
</task>

<task type="auto">
  <name>Task 2: useActionGuard in alle Modals mit Submit-Buttons einbauen</name>
  <files>
    frontend/src/components/admin/modals/ActivityModal.tsx,
    frontend/src/components/admin/modals/ActivityRequestModal.tsx,
    frontend/src/components/admin/modals/BadgeManagementModal.tsx,
    frontend/src/components/admin/modals/BonusModal.tsx,
    frontend/src/components/admin/modals/ChangeEmailModal.tsx,
    frontend/src/components/admin/modals/ChangePasswordModal.tsx,
    frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx,
    frontend/src/components/admin/modals/EventModal.tsx,
    frontend/src/components/admin/modals/KonfiModal.tsx,
    frontend/src/components/admin/modals/LevelManagementModal.tsx,
    frontend/src/components/admin/modals/MaterialFormModal.tsx,
    frontend/src/components/admin/modals/OrganizationManagementModal.tsx,
    frontend/src/components/admin/modals/ParticipantManagementModal.tsx,
    frontend/src/components/admin/modals/UserManagementModal.tsx,
    frontend/src/components/admin/modals/ActivityManagementModal.tsx,
    frontend/src/components/konfi/modals/ActivityRequestModal.tsx,
    frontend/src/components/konfi/modals/ChangeEmailModal.tsx,
    frontend/src/components/konfi/modals/ChangePasswordModal.tsx,
    frontend/src/components/konfi/modals/UnregisterModal.tsx,
    frontend/src/components/chat/modals/SimpleCreateChatModal.tsx,
    frontend/src/components/chat/modals/PollModal.tsx,
    frontend/src/components/chat/modals/DirectMessageModal.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useActionGuard.ts (aus Task 1)
    - frontend/src/components/admin/modals/ActivityModal.tsx (Referenz fuer bestehendes isSubmitting-Pattern)
    - frontend/src/components/konfi/modals/ActivityRequestModal.tsx (Referenz fuer Konfi-Pattern)
    - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx (Referenz fuer Chat-Pattern)
  </read_first>
  <action>
In JEDEM Modal mit Submit-Button:

1. Import hinzufuegen: `import { useActionGuard } from '../../hooks/useActionGuard';`
   (Pfad je nach Verzeichnistiefe anpassen — admin/konfi Modals: `../../../hooks/useActionGuard`, chat Modals: `../../../hooks/useActionGuard`)

2. Im Funktionskörper: `const { isSubmitting, guard } = useActionGuard();`

3. Falls bereits `isSubmitting`/`setIsSubmitting` State existiert (7 Modals haben das bereits): ERSETZEN durch useActionGuard.
   - Altes Pattern entfernen: `const [isSubmitting, setIsSubmitting] = useState(false);`
   - `setIsSubmitting(true)` und `setIsSubmitting(false)` entfernen
   - Die Submit-Funktion (z.B. `handleSave`, `handleSubmit`) in `guard()` wrappen

4. Falls KEIN isSubmitting existiert: Submit-Handler in `guard()` wrappen:
   ```typescript
   // VORHER:
   const handleSave = async () => {
     try { ... } catch { ... }
   };

   // NACHHER:
   const handleSave = async () => {
     await guard(async () => {
       try { ... } catch { ... }
     });
   };
   ```

5. Am Submit-Button: `disabled={isSubmitting}` hinzufuegen. Falls IonButton: auch `disabled={isSubmitting}` setzen.
   Falls der Button bereits ein `disabled` hat, mit `||` verknuepfen: `disabled={!isValid || isSubmitting}`.

6. KEIN IonSpinner hinzufuegen — das ist Overkill fuer diese Phase. Nur disabled reicht fuer RET-02.

WICHTIG: Nicht anfassen — diese Modals haben KEINEN Submit-Button:
- QRScannerModal, QRDisplayModal, FileViewerModal, ChatOptionsModal, MembersModal, RequestDetailModal, PointsHistoryModal
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -rl "useActionGuard" src/components/ | wc -l && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "useActionGuard" frontend/src/components/` findet mindestens 20 Dateien
    - `grep -rL "useActionGuard" frontend/src/components/admin/modals/ActivityModal.tsx` ist LEER (= gefunden)
    - `grep -rL "useActionGuard" frontend/src/components/konfi/modals/ActivityRequestModal.tsx` ist LEER
    - `grep -rL "useActionGuard" frontend/src/components/chat/modals/SimpleCreateChatModal.tsx` ist LEER
    - `grep -c "isSubmitting" frontend/src/components/admin/modals/EventModal.tsx` zeigt mindestens 2 Treffer (State + disabled)
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Alle ~22 Modals mit Submit-Buttons nutzen useActionGuard. Buttons sind waehrend Request disabled. Kein Double-Submit moeglich.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit --skipLibCheck` kompiliert ohne Fehler
- `grep -q "axiosRetry" frontend/src/services/api.ts` bestaetigt Retry-Konfiguration
- `grep -rl "useActionGuard" frontend/src/components/ | wc -l` zeigt 20+ Dateien
- `grep -q "axios-retry" frontend/package.json` bestaetigt Dependency
</verification>

<success_criteria>
- axios-retry wiederholt 5xx/408/429 automatisch 3x mit Exponential Backoff
- 4xx (ausser 429) werden NICHT wiederholt
- Alle Submit-Modals haben Double-Submit-Schutz via useActionGuard
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/57-retry-schutz/57-01-SUMMARY.md`
</output>
