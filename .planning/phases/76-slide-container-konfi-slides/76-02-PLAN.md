---
phase: 76-slide-container-konfi-slides
plan: 02
type: execute
wave: 2
depends_on: [76-01]
files_modified:
  - frontend/src/components/wrapped/slides/IntroSlide.tsx
  - frontend/src/components/wrapped/slides/PunkteSlide.tsx
  - frontend/src/components/wrapped/slides/EventsSlide.tsx
  - frontend/src/components/wrapped/slides/BadgesSlide.tsx
  - frontend/src/components/wrapped/WrappedModal.tsx
autonomous: true
requirements: [KS-01, KS-02, KS-03, KS-04, KS-09]

must_haves:
  truths:
    - "Intro-Slide zeigt 'Dein Konfi-Jahr [Jahr]' mit dem Namen des Konfis"
    - "Punkte-Slide zeigt animiert hochzaehlende Gesamtpunkte mit Gottesdienst/Gemeinde Aufteilung"
    - "Events-Slide zeigt Anzahl besuchter Events und das Highlight-Event"
    - "Badges-Slide zeigt verdiente Badge-Icons mit Zaehler (verdient/total)"
    - "Keine Vergleiche mit anderen Konfis auf irgendeinem Slide"
  artifacts:
    - path: "frontend/src/components/wrapped/slides/IntroSlide.tsx"
      provides: "Intro-Slide mit Name, Jahrgang, Jahr"
      min_lines: 25
    - path: "frontend/src/components/wrapped/slides/PunkteSlide.tsx"
      provides: "Punkte-Slide mit Count-up Animation und Aufteilung"
      min_lines: 40
    - path: "frontend/src/components/wrapped/slides/EventsSlide.tsx"
      provides: "Events-Slide mit Teilnahme-Count und Highlight-Event"
      min_lines: 35
    - path: "frontend/src/components/wrapped/slides/BadgesSlide.tsx"
      provides: "Badges-Slide mit Icon-Grid und Zaehler"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/wrapped/slides/PunkteSlide.tsx"
      to: "frontend/src/hooks/useCountUp.ts"
      via: "import { useCountUp }"
      pattern: "useCountUp"
    - from: "frontend/src/components/wrapped/WrappedModal.tsx"
      to: "frontend/src/components/wrapped/slides/IntroSlide.tsx"
      via: "import IntroSlide"
      pattern: "import.*IntroSlide"
---

<objective>
Konfi-Slides 1-4 implementieren: Intro, Punkte, Events und Badges. Jeder Slide nutzt die SlideBase-Komponente und CSS-Animationen aus Plan 01.

Purpose: Erste Haelfte der inhaltlichen Slides -- vom Willkommen bis zum Badge-Showcase.
Output: 4 fertige Slide-Komponenten, eingebunden in WrappedModal.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-slide-container-konfi-slides/76-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 erstellte Interfaces (frontend/src/types/wrapped.ts): -->
```typescript
export interface KonfiPunkteSlide {
  gottesdienst: number;
  gemeinde: number;
  total: number;
  bonus: number;
}
export interface KonfiEventsSlide {
  total_attended: number;
  total_available: number;
  lieblings_event: { name: string; date: string } | null;
}
export interface KonfiBadgesSlide {
  total_earned: number;
  total_available: number;
  badges: Array<{ name: string; icon: string; color: string }>;
}
export interface SlideProps {
  isActive: boolean;
}
```

<!-- useCountUp Hook Signatur: -->
```typescript
export function useCountUp(target: number, isActive: boolean, duration?: number): number;
```

<!-- SlideBase Komponente: -->
```typescript
// Props: { isActive: boolean; children: React.ReactNode; className?: string }
// Wrapper: <div className="wrapped-slide wrapped-slide--active">
```

<!-- CSS-Klassen aus Plan 01 (WrappedModal.css): -->
- `.wrapped-anim-fade` — fade-in mit translateY
- `.wrapped-anim-scale` — scale 0.5 -> 1
- `.wrapped-anim-delay-1/2/3` — gestaffeltes Einblenden (0.2s/0.4s/0.6s)
- Grosse Zahlen: nutzen `className="wrapped-big-number"` (clamp 3-6rem, weight 800)
- Untertitel: `className="wrapped-subtitle"`

<!-- Badge-Icon Helper (bestehend): -->
```typescript
import { getIconFromString } from '../../helpers/iconHelper'; // oder aehnlich
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IntroSlide + PunkteSlide erstellen</name>
  <files>frontend/src/components/wrapped/slides/IntroSlide.tsx, frontend/src/components/wrapped/slides/PunkteSlide.tsx</files>
  <read_first>
    - frontend/src/components/wrapped/slides/SlideBase.tsx (Basis-Komponente aus Plan 01)
    - frontend/src/components/wrapped/WrappedModal.css (verfuegbare CSS-Klassen)
    - frontend/src/hooks/useCountUp.ts (Hook-Signatur)
    - frontend/src/types/wrapped.ts (KonfiPunkteSlide Interface)
  </read_first>
  <action>
1. `frontend/src/components/wrapped/slides/IntroSlide.tsx` erstellen (per D-10):
   Props: `SlideProps & { displayName: string; jahrgangName: string; year: number }`

   Layout:
   ```tsx
   <SlideBase isActive={isActive} className="intro-slide">
     <div className="wrapped-anim-fade" style={{ opacity: 0 }}>
       <p className="wrapped-subtitle">Willkommen zu deinem</p>
     </div>
     <div className="wrapped-anim-fade wrapped-anim-delay-1" style={{ opacity: 0 }}>
       <h1 className="wrapped-big-number" style={{ fontSize: 'clamp(2rem, 8vw, 3.5rem)' }}>
         Konfi-Jahr {year}
       </h1>
     </div>
     <div className="wrapped-anim-fade wrapped-anim-delay-2" style={{ opacity: 0 }}>
       <p className="wrapped-subtitle">{displayName}</p>
       <p style={{ color: 'rgba(255,255,255,0.5)', marginTop: '4px' }}>{jahrgangName}</p>
     </div>
   </SlideBase>
   ```
   Hintergrund: Leichter Lila-Gradient (via CSS-Klasse `.intro-slide` mit eigenem Background-Override, z.B. `background: linear-gradient(135deg, #2d1065 0%, #0a0a0a 60%)`).

2. `frontend/src/components/wrapped/slides/PunkteSlide.tsx` erstellen (per D-11):
   Props: `SlideProps & { punkte: KonfiPunkteSlide }`

   Nutzt `useCountUp(punkte.total, isActive, 2000)` fuer die grosse Zahl.
   Nutzt `useCountUp(punkte.gottesdienst, isActive, 1500)` und `useCountUp(punkte.gemeinde, isActive, 1500)` fuer die Aufteilung.

   Layout:
   ```tsx
   <SlideBase isActive={isActive} className="punkte-slide">
     <div className="wrapped-anim-fade" style={{ opacity: 0 }}>
       <p className="wrapped-subtitle">Deine Punkte</p>
     </div>
     <div className="wrapped-anim-scale wrapped-anim-delay-1" style={{ opacity: 0 }}>
       <p className="wrapped-big-number">{animatedTotal}</p>
       <p className="wrapped-subtitle">Punkte gesammelt</p>
     </div>
     <div className="wrapped-anim-fade wrapped-anim-delay-2" style={{ opacity: 0 }}>
       <div className="punkte-split">
         <div className="punkte-split-item">
           <span className="punkte-split-value">{animatedGottesdienst}</span>
           <span className="punkte-split-label">Gottesdienst</span>
         </div>
         <div className="punkte-split-divider" />
         <div className="punkte-split-item">
           <span className="punkte-split-value">{animatedGemeinde}</span>
           <span className="punkte-split-label">Gemeinde</span>
         </div>
       </div>
     </div>
     {punkte.bonus > 0 && (
       <div className="wrapped-anim-fade wrapped-anim-delay-3" style={{ opacity: 0 }}>
         <p style={{ color: 'rgba(255,255,255,0.6)', fontSize: '0.9rem', marginTop: '16px' }}>
           davon {punkte.bonus} Bonuspunkte
         </p>
       </div>
     )}
   </SlideBase>
   ```

   Zusaetzliche CSS-Klassen in WrappedModal.css anfuegen:
   ```css
   .punkte-split { display: flex; gap: 24px; align-items: center; margin-top: 24px; }
   .punkte-split-item { display: flex; flex-direction: column; align-items: center; }
   .punkte-split-value { font-size: clamp(1.5rem, 6vw, 2.5rem); font-weight: 700; color: #a78bfa; }
   .punkte-split-label { font-size: 0.85rem; color: rgba(255,255,255,0.6); margin-top: 4px; }
   .punkte-split-divider { width: 1px; height: 40px; background: rgba(255,255,255,0.2); }
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "Konfi-Jahr" frontend/src/components/wrapped/slides/IntroSlide.tsx findet den Titel
    - grep "displayName" frontend/src/components/wrapped/slides/IntroSlide.tsx findet die Name-Prop
    - grep "useCountUp" frontend/src/components/wrapped/slides/PunkteSlide.tsx findet mind. 2 Aufrufe
    - grep "gottesdienst" frontend/src/components/wrapped/slides/PunkteSlide.tsx zeigt die Aufteilung
    - grep "Bonuspunkte" frontend/src/components/wrapped/slides/PunkteSlide.tsx zeigt den Bonus-Hinweis
    - Keine Vergleichs-Strings ("besser als", "Prozent", "Ranking") in beiden Dateien (per KS-09)
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>IntroSlide zeigt Name + Jahrgang + Jahr mit Fade-in, PunkteSlide zeigt animiert hochzaehlende Gesamtpunkte mit Gottesdienst/Gemeinde-Aufteilung und optionalem Bonus</done>
</task>

<task type="auto">
  <name>Task 2: EventsSlide + BadgesSlide erstellen + in WrappedModal einbinden</name>
  <files>frontend/src/components/wrapped/slides/EventsSlide.tsx, frontend/src/components/wrapped/slides/BadgesSlide.tsx, frontend/src/components/wrapped/WrappedModal.tsx</files>
  <read_first>
    - frontend/src/components/wrapped/slides/IntroSlide.tsx (Pattern aus Task 1)
    - frontend/src/components/wrapped/slides/PunkteSlide.tsx (Count-up Pattern)
    - frontend/src/components/wrapped/WrappedModal.tsx (aktueller Stand mit Dummy-Slides)
    - frontend/src/components/wrapped/WrappedModal.css (verfuegbare Klassen)
    - frontend/src/types/wrapped.ts (KonfiEventsSlide, KonfiBadgesSlide)
  </read_first>
  <action>
1. `frontend/src/components/wrapped/slides/EventsSlide.tsx` erstellen (per D-12):
   Props: `SlideProps & { events: KonfiEventsSlide }`

   Nutzt `useCountUp(events.total_attended, isActive)` fuer die grosse Zahl.

   Layout:
   ```tsx
   <SlideBase isActive={isActive} className="events-slide">
     <div className="wrapped-anim-fade" style={{ opacity: 0 }}>
       <p className="wrapped-subtitle">Deine Events</p>
     </div>
     <div className="wrapped-anim-scale wrapped-anim-delay-1" style={{ opacity: 0 }}>
       <p className="wrapped-big-number">{animatedCount}</p>
       <p className="wrapped-subtitle">Events besucht</p>
       {events.total_available > 0 && (
         <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: '0.85rem', marginTop: '4px' }}>
           von {events.total_available} verfuegbaren
         </p>
       )}
     </div>
     {events.lieblings_event && (
       <div className="wrapped-anim-fade wrapped-anim-delay-2" style={{ opacity: 0 }}>
         <div className="events-highlight">
           <p style={{ color: 'rgba(255,255,255,0.6)', fontSize: '0.85rem' }}>Dein letztes Event</p>
           <p style={{ color: '#a78bfa', fontSize: '1.2rem', fontWeight: 600, marginTop: '4px' }}>
             {events.lieblings_event.name}
           </p>
         </div>
       </div>
     )}
   </SlideBase>
   ```

   Zusaetzliche CSS:
   ```css
   .events-highlight {
     margin-top: 32px;
     padding: 16px 24px;
     background: rgba(124, 58, 237, 0.15);
     border-radius: 16px;
     border: 1px solid rgba(124, 58, 237, 0.3);
   }
   ```

2. `frontend/src/components/wrapped/slides/BadgesSlide.tsx` erstellen (per D-13):
   Props: `SlideProps & { badges: KonfiBadgesSlide }`

   Zeigt Badge-Icons in einem Grid (max 3 Spalten). Jeder Badge bekommt `wrapped-anim-scale` mit gestaffeltem Delay.

   Fuer Badge-Icons: `getIconFromString` Helper verwenden, falls verfuegbar. Sonst IonIcon mit dem `icon`-String direkt. Pruefen was im Projekt verwendet wird (read_first Badge-Komponenten).

   Layout:
   ```tsx
   <SlideBase isActive={isActive} className="badges-slide">
     <div className="wrapped-anim-fade" style={{ opacity: 0 }}>
       <p className="wrapped-subtitle">Deine Badges</p>
     </div>
     <div className="wrapped-anim-scale wrapped-anim-delay-1" style={{ opacity: 0 }}>
       <p className="wrapped-big-number">{badges.total_earned}</p>
       <p className="wrapped-subtitle">von {badges.total_available} verdient</p>
     </div>
     <div className="badges-grid wrapped-anim-fade wrapped-anim-delay-2" style={{ opacity: 0 }}>
       {badges.badges.slice(0, 6).map((badge, i) => (
         <div key={i} className="badge-item" style={{ animationDelay: `${0.4 + i * 0.1}s` }}>
           <div className="badge-icon" style={{ background: badge.color || '#7c3aed' }}>
             <IonIcon icon={getIconFromString(badge.icon)} />
           </div>
           <span className="badge-name">{badge.name}</span>
         </div>
       ))}
     </div>
   </SlideBase>
   ```

   Zusaetzliche CSS:
   ```css
   .badges-grid {
     display: grid;
     grid-template-columns: repeat(3, 1fr);
     gap: 16px;
     margin-top: 24px;
     max-width: 280px;
   }
   .badge-item { display: flex; flex-direction: column; align-items: center; gap: 6px; }
   .badge-icon {
     width: 52px; height: 52px;
     border-radius: 50%;
     display: flex; align-items: center; justify-content: center;
     font-size: 1.4rem; color: #fff;
   }
   .badge-name { font-size: 0.75rem; color: rgba(255,255,255,0.7); max-width: 80px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
   ```

3. **WrappedModal.tsx aktualisieren**: Dummy-Slides durch echte Slide-Komponenten ersetzen (Slides 1-4):
   - Import IntroSlide, PunkteSlide, EventsSlide, BadgesSlide
   - SwiperSlide 0: `<IntroSlide isActive={activeIndex === 0} displayName={displayName} jahrgangName={jahrgangName} year={year!} />`
   - SwiperSlide 1: `<PunkteSlide isActive={activeIndex === 1} punkte={data.slides.punkte} />`
   - SwiperSlide 2: `<EventsSlide isActive={activeIndex === 2} events={data.slides.events} />`
   - SwiperSlide 3: `<BadgesSlide isActive={activeIndex === 3} badges={data.slides.badges} />`
   - Slides 4-7 (Aktivster Monat, Chat, Endspurt, Abschluss) bleiben als Platzhalter fuer Plan 03.

   Badge-Icon-Helper: Pruefen ob `getIconFromString` oder aehnliche Helper-Funktion existiert (lies `frontend/src/` nach Badges/Icons). Falls nicht, Badge-Icon direkt als IonIcon string verwenden. KEINE neuen Emojis oder Unicode-Zeichen (CLAUDE.md Regel #3).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "total_attended" frontend/src/components/wrapped/slides/EventsSlide.tsx zeigt Event-Count
    - grep "useCountUp" frontend/src/components/wrapped/slides/EventsSlide.tsx findet Count-up
    - grep "lieblings_event" frontend/src/components/wrapped/slides/EventsSlide.tsx zeigt Highlight-Event
    - grep "badges-grid" frontend/src/components/wrapped/slides/BadgesSlide.tsx zeigt Badge-Grid
    - grep "total_earned" frontend/src/components/wrapped/slides/BadgesSlide.tsx zeigt Zaehler
    - grep "IntroSlide" frontend/src/components/wrapped/WrappedModal.tsx zeigt Import
    - grep "PunkteSlide" frontend/src/components/wrapped/WrappedModal.tsx zeigt Import
    - grep "EventsSlide" frontend/src/components/wrapped/WrappedModal.tsx zeigt Import
    - grep "BadgesSlide" frontend/src/components/wrapped/WrappedModal.tsx zeigt Import
    - Keine Vergleichs-Strings in EventsSlide oder BadgesSlide (per KS-09)
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>EventsSlide zeigt Event-Count mit Highlight, BadgesSlide zeigt Badge-Grid mit Icons. Alle 4 Slides in WrappedModal eingebunden und per Swipe navigierbar.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — keine TypeScript-Fehler
2. `cd frontend && npm run build` — Vite-Build erfolgreich
3. Slides 0-3 in WrappedModal: IntroSlide, PunkteSlide, EventsSlide, BadgesSlide
4. Keine Unicode-Emojis in Slide-Dateien
5. Keine Vergleiche mit anderen Konfis (KS-09)
</verification>

<success_criteria>
- IntroSlide zeigt "Dein Konfi-Jahr [Jahr]" + Name + Jahrgang
- PunkteSlide zaehlt Gesamtpunkte animiert hoch, zeigt Gottesdienst/Gemeinde-Aufteilung
- EventsSlide zeigt besuchte Events + Highlight-Event
- BadgesSlide zeigt Badge-Icons im Grid mit verdient/total Zaehler
- Alle 4 Slides in WrappedModal eingebunden
- Nur eigene Daten, keine Vergleiche (KS-09)
- Frontend kompiliert und baut ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/76-slide-container-konfi-slides/76-02-SUMMARY.md`
</output>
