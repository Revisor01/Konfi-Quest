---
phase: 65-navigation-state-konsistenz
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - frontend/src/components/admin/pages/AdminEventsPage.tsx
  - frontend/src/components/admin/pages/AdminKonfisPage.tsx
  - frontend/src/components/admin/pages/AdminActivitiesPage.tsx
  - frontend/src/components/admin/pages/AdminBadgesPage.tsx
  - frontend/src/components/admin/pages/AdminUsersPage.tsx
  - frontend/src/components/admin/pages/AdminOrganizationsPage.tsx
  - frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx
  - frontend/src/components/konfi/pages/KonfiEventsPage.tsx
  - frontend/src/contexts/BadgeContext.tsx
autonomous: true
requirements: [NAV-03, NAV-04]

must_haves:
  truths:
    - "Keine Seite nutzt mehr window.addEventListener fuer Daten-Update-Events"
    - "BadgeContext nutzt useLiveRefresh statt window.addEventListener fuer requestStatusChanged und events-updated"
    - "AdminUsersPage und AdminOrganizationsPage nutzen useLiveRefresh statt toter window.addEventListener"
    - "System Events (rate-limit, sync:reconnect, fcmToken, online/offline) sind unberuehrt"
  artifacts:
    - path: "frontend/src/contexts/BadgeContext.tsx"
      provides: "LiveUpdateContext-basierte Subscriptions statt window Events"
      contains: "useLiveRefresh"
    - path: "frontend/src/components/admin/pages/AdminEventsPage.tsx"
      provides: "Kein redundanter window.addEventListener-Block mehr"
    - path: "frontend/src/components/admin/pages/AdminUsersPage.tsx"
      provides: "useLiveRefresh statt totem window.addEventListener"
      contains: "useLiveRefresh"
  key_links:
    - from: "frontend/src/contexts/BadgeContext.tsx"
      to: "frontend/src/contexts/LiveUpdateContext.tsx"
      via: "useLiveRefresh(['requests', 'events'], refreshAllCounts)"
      pattern: "useLiveRefresh"
    - from: "frontend/src/components/admin/pages/AdminUsersPage.tsx"
      to: "frontend/src/contexts/LiveUpdateContext.tsx"
      via: "useLiveRefresh('users', refreshUsers)"
      pattern: "useLiveRefresh\\('users'"
---

<objective>
Redundante window.addEventListener-Bloecke fuer Daten-Events entfernen und durch useLiveRefresh ersetzen wo noch nicht vorhanden.

Purpose: Nach Plan 01 feuern keine Dispatcher mehr CustomEvents. Die Listener-Seite muss bereinigt werden — entweder sind sie bereits durch useLiveRefresh abgedeckt (dann toter Code entfernen) oder muessen migriert werden.
Output: Einheitliches Pub-Sub ueber LiveUpdateContext, kein window.addEventListener fuer Daten-Events mehr.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-navigation-state-konsistenz/65-01-SUMMARY.md

<interfaces>
From frontend/src/contexts/LiveUpdateContext.tsx (nach Plan 01):
```typescript
export type LiveUpdateType =
  | 'dashboard' | 'events' | 'event_booking' | 'badges'
  | 'requests' | 'konfis' | 'points' | 'chat'
  | 'activities' | 'categories' | 'jahrgaenge' | 'levels'
  | 'users' | 'organizations';

export const useLiveRefresh = (types: LiveUpdateType | LiveUpdateType[], onRefresh: () => void) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redundante addEventListener-Bloecke entfernen (Seiten mit useLiveRefresh)</name>
  <files>
    frontend/src/components/admin/pages/AdminEventsPage.tsx,
    frontend/src/components/admin/pages/AdminKonfisPage.tsx,
    frontend/src/components/admin/pages/AdminActivitiesPage.tsx,
    frontend/src/components/admin/pages/AdminBadgesPage.tsx,
    frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx,
    frontend/src/components/konfi/pages/KonfiEventsPage.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminEventsPage.tsx,
    frontend/src/components/admin/pages/AdminKonfisPage.tsx,
    frontend/src/components/admin/pages/AdminActivitiesPage.tsx,
    frontend/src/components/admin/pages/AdminBadgesPage.tsx,
    frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx,
    frontend/src/components/konfi/pages/KonfiEventsPage.tsx
  </read_first>
  <action>
Diese 6 Seiten haben BEREITS `useLiveRefresh()` UND zusaetzlich einen redundanten `useEffect` mit `window.addEventListener`. Den redundanten useEffect-Block komplett entfernen:

1. AdminEventsPage.tsx: useEffect-Block mit `window.addEventListener('events-updated', ...)` entfernen (ca. Zeile 99-110). useLiveRefresh('events', refreshAllEvents) auf Zeile 88 bleibt.

2. AdminKonfisPage.tsx: useEffect-Block mit `window.addEventListener('konfis-updated', ...)` entfernen (ca. Zeile 115-118). useLiveRefresh('konfis', refreshAll) auf Zeile 107 bleibt.

3. AdminActivitiesPage.tsx: useEffect-Block mit `window.addEventListener('activities-updated', ...)` entfernen (ca. Zeile 72-83). useLiveRefresh('activities', refreshActivities) auf Zeile 70 bleibt.

4. AdminBadgesPage.tsx: useEffect-Block mit `window.addEventListener('badges-updated', ...)` entfernen (ca. Zeile 83-94). useLiveRefresh('badges', refreshBadges) auf Zeile 81 bleibt.

5. AdminActivityRequestsPage.tsx: useEffect-Block mit `window.addEventListener('activity-requests-updated', ...)` entfernen (ca. Zeile 85-88). useLiveRefresh('requests', refreshRequests) auf Zeile 77 bleibt.

6. KonfiEventsPage.tsx: useEffect-Block mit `window.addEventListener('events-updated', ...)` entfernen (ca. Zeile 61-64). useLiveRefresh('events', refresh) auf Zeile 53 bleibt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -rn "addEventListener.*events-updated\|addEventListener.*konfis-updated\|addEventListener.*activities-updated\|addEventListener.*badges-updated\|addEventListener.*activity-requests-updated" frontend/src/components/ | wc -l | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "addEventListener.*events-updated" frontend/src/components/ findet 0 Treffer
    - grep "addEventListener.*konfis-updated" frontend/src/components/ findet 0 Treffer
    - grep "addEventListener.*activities-updated" frontend/src/components/ findet 0 Treffer
    - grep "addEventListener.*badges-updated" frontend/src/components/ findet 0 Treffer
    - grep "addEventListener.*activity-requests-updated" frontend/src/components/ findet 0 Treffer
    - grep "useLiveRefresh" in jeder der 6 Dateien findet weiterhin mindestens 1 Treffer
  </acceptance_criteria>
  <done>6 redundante window.addEventListener-Bloecke entfernt. useLiveRefresh bleibt als einziger Mechanismus.</done>
</task>

<task type="auto">
  <name>Task 2: BadgeContext + AdminUsersPage + AdminOrganizationsPage auf LiveUpdateContext migrieren</name>
  <files>
    frontend/src/contexts/BadgeContext.tsx,
    frontend/src/components/admin/pages/AdminUsersPage.tsx,
    frontend/src/components/admin/pages/AdminOrganizationsPage.tsx
  </files>
  <read_first>
    frontend/src/contexts/BadgeContext.tsx,
    frontend/src/components/admin/pages/AdminUsersPage.tsx,
    frontend/src/components/admin/pages/AdminOrganizationsPage.tsx
  </read_first>
  <action>
1. BadgeContext.tsx:
   - Import hinzufuegen: `import { useLiveRefresh } from './LiveUpdateContext';`
   - Den useEffect-Block (ca. Zeile 154-172) mit `window.addEventListener('requestStatusChanged', ...)` und `window.addEventListener('events-updated', ...)` KOMPLETT entfernen
   - Stattdessen: `useLiveRefresh(['requests', 'events'], refreshAllCounts);` einfuegen
   - WICHTIG: Den `sync:reconnect` Listener (Zeile 174-186) BEIBEHALTEN — das ist ein System-Event, kein Daten-Event

2. AdminUsersPage.tsx:
   - Import hinzufuegen: `import { useLiveRefresh } from '../../../contexts/LiveUpdateContext';`
   - Den useEffect-Block mit `window.addEventListener('users-updated', ...)` (ca. Zeile 61-72) entfernen
   - Stattdessen: `useLiveRefresh('users', refreshUsers);` einfuegen

3. AdminOrganizationsPage.tsx:
   - Import hinzufuegen: `import { useLiveRefresh } from '../../../contexts/LiveUpdateContext';`
   - Den useEffect-Block mit `window.addEventListener('organizations-updated', ...)` (ca. Zeile 102-105) entfernen
   - Stattdessen: `useLiveRefresh('organizations', refreshOrganizations);` einfuegen
   - Sicherstellen dass refreshOrganizations als useCallback definiert ist (fuer useLiveRefresh Stability)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -rn "addEventListener.*requestStatusChanged\|addEventListener.*events-updated\|addEventListener.*users-updated\|addEventListener.*organizations-updated" frontend/src/contexts/BadgeContext.tsx frontend/src/components/admin/pages/AdminUsersPage.tsx frontend/src/components/admin/pages/AdminOrganizationsPage.tsx | wc -l | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "addEventListener.*requestStatusChanged" frontend/src/contexts/BadgeContext.tsx findet 0 Treffer
    - grep "addEventListener.*events-updated" frontend/src/contexts/BadgeContext.tsx findet 0 Treffer
    - grep "useLiveRefresh" frontend/src/contexts/BadgeContext.tsx findet mindestens 1 Treffer
    - grep "addEventListener.*users-updated" frontend/src/components/admin/pages/AdminUsersPage.tsx findet 0 Treffer
    - grep "useLiveRefresh" frontend/src/components/admin/pages/AdminUsersPage.tsx findet mindestens 1 Treffer
    - grep "addEventListener.*organizations-updated" frontend/src/components/admin/pages/AdminOrganizationsPage.tsx findet 0 Treffer
    - grep "useLiveRefresh" frontend/src/components/admin/pages/AdminOrganizationsPage.tsx findet mindestens 1 Treffer
    - grep "sync:reconnect" frontend/src/contexts/BadgeContext.tsx findet weiterhin Treffer (System-Event beibehalten)
    - cd frontend && npx tsc --noEmit kompiliert ohne Fehler
  </acceptance_criteria>
  <done>BadgeContext, AdminUsersPage und AdminOrganizationsPage nutzen useLiveRefresh. Kein window.addEventListener fuer Daten-Events mehr im gesamten Frontend (ausser System-Events).</done>
</task>

</tasks>

<verification>
- `grep -rn "addEventListener.*-updated\|addEventListener.*requestStatusChanged" frontend/src/` findet 0 Treffer
- `grep -rn "addEventListener.*rate-limit" frontend/src/App.tsx` findet weiterhin 1 Treffer (System-Event beibehalten)
- `grep -rn "addEventListener.*sync:reconnect" frontend/src/contexts/BadgeContext.tsx` findet weiterhin 1 Treffer (System-Event beibehalten)
- `cd frontend && npx tsc --noEmit` kompiliert fehlerfrei
</verification>

<success_criteria>
Alle window.addEventListener fuer Daten-Update-Events entfernt oder durch useLiveRefresh ersetzt. Nur System-Events (rate-limit, sync:reconnect, fcmToken, online/offline) bleiben als window Events.
</success_criteria>

<output>
After completion, create `.planning/phases/65-navigation-state-konsistenz/65-02-SUMMARY.md`
</output>
