---
phase: 65-navigation-state-konsistenz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/contexts/LiveUpdateContext.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/admin/modals/ActivityRequestModal.tsx
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "Kein window.dispatchEvent mit events-updated, konfis-updated oder requestStatusChanged existiert mehr"
    - "Alle Daten-Update-Signale laufen ueber LiveUpdateContext.triggerRefresh()"
    - "LiveUpdateType enthaelt users und organizations als neue Typen"
  artifacts:
    - path: "frontend/src/contexts/LiveUpdateContext.tsx"
      provides: "Erweiterte LiveUpdateType mit users + organizations"
      contains: "users"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "triggerRefresh statt dispatchEvent"
      contains: "triggerRefresh"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "triggerRefresh statt dispatchEvent"
      contains: "triggerRefresh"
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "triggerRefresh statt dispatchEvent"
      contains: "triggerRefresh"
    - path: "frontend/src/components/admin/modals/ActivityRequestModal.tsx"
      provides: "triggerRefresh statt dispatchEvent"
      contains: "triggerRefresh"
  key_links:
    - from: "frontend/src/components/admin/views/EventDetailView.tsx"
      to: "frontend/src/contexts/LiveUpdateContext.tsx"
      via: "useLiveUpdate().triggerRefresh('events')"
      pattern: "triggerRefresh\\('events'\\)"
    - from: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      to: "frontend/src/contexts/LiveUpdateContext.tsx"
      via: "useLiveUpdate().triggerRefresh('konfis')"
      pattern: "triggerRefresh\\('konfis'\\)"
    - from: "frontend/src/components/admin/modals/ActivityRequestModal.tsx"
      to: "frontend/src/contexts/LiveUpdateContext.tsx"
      via: "useLiveUpdate().triggerRefresh('requests')"
      pattern: "triggerRefresh\\('requests'\\)"
---

<objective>
Alle window.dispatchEvent-Aufrufe fuer Daten-Updates durch LiveUpdateContext.triggerRefresh() ersetzen.

Purpose: CustomEvents fuer Daten-Updates sind ein paralleles, ungetyptes System neben dem bereits bestehenden LiveUpdateContext Pub-Sub. Konsolidierung auf ein einziges System.
Output: Alle Dispatcher nutzen triggerRefresh(), LiveUpdateType um users + organizations erweitert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/contexts/LiveUpdateContext.tsx

<interfaces>
<!-- LiveUpdateContext API die Executor nutzen muss -->

From frontend/src/contexts/LiveUpdateContext.tsx:
```typescript
export type LiveUpdateType =
  | 'dashboard' | 'events' | 'event_booking' | 'badges'
  | 'requests' | 'konfis' | 'points' | 'chat'
  | 'activities' | 'categories' | 'jahrgaenge' | 'levels';

export const useLiveUpdate = () => { subscribe, triggerRefresh };
export const useLiveRefresh = (types, onRefresh) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveUpdateType erweitern + Dispatcher migrieren</name>
  <files>
    frontend/src/contexts/LiveUpdateContext.tsx,
    frontend/src/components/admin/views/EventDetailView.tsx,
    frontend/src/components/admin/views/KonfiDetailView.tsx,
    frontend/src/components/konfi/views/EventDetailView.tsx,
    frontend/src/components/admin/modals/ActivityRequestModal.tsx
  </files>
  <read_first>
    frontend/src/contexts/LiveUpdateContext.tsx,
    frontend/src/components/admin/views/EventDetailView.tsx,
    frontend/src/components/admin/views/KonfiDetailView.tsx,
    frontend/src/components/konfi/views/EventDetailView.tsx,
    frontend/src/components/admin/modals/ActivityRequestModal.tsx
  </read_first>
  <action>
1. In LiveUpdateContext.tsx: LiveUpdateType erweitern um `| 'users' | 'organizations'`. Entsprechende WebSocket-Listener ergaenzen (socket.on usersUpdate, organizationsUpdate).

2. In admin/views/EventDetailView.tsx (5 Stellen):
   - Import hinzufuegen: `import { useLiveUpdate } from '../../../contexts/LiveUpdateContext';`
   - Im Component: `const { triggerRefresh } = useLiveUpdate();`
   - Alle `window.dispatchEvent(new CustomEvent('events-updated'))` ersetzen durch `triggerRefresh('events')`
   - Zeilen ca. 411, 490, 513, 529, 553

3. In admin/views/KonfiDetailView.tsx (6 Stellen):
   - Import hinzufuegen: `import { useLiveUpdate } from '../../../contexts/LiveUpdateContext';`
   - Im Component: `const { triggerRefresh } = useLiveUpdate();`
   - Alle `window.dispatchEvent(new CustomEvent('konfis-updated'))` ersetzen durch `triggerRefresh('konfis')`
   - Zeilen ca. 168, 180, 380, 405, 435, 567

4. In konfi/views/EventDetailView.tsx (4 Stellen):
   - Import hinzufuegen: `import { useLiveUpdate } from '../../../contexts/LiveUpdateContext';`
   - Im Component: `const { triggerRefresh } = useLiveUpdate();`
   - Alle `window.dispatchEvent(new CustomEvent('events-updated'))` ersetzen durch `triggerRefresh('events')`
   - Zeilen ca. 100, 128, 148, 286

5. In admin/modals/ActivityRequestModal.tsx (2 Stellen):
   - Import hinzufuegen: `import { useLiveUpdate } from '../../../contexts/LiveUpdateContext';`
   - Im Component: `const { triggerRefresh } = useLiveUpdate();`
   - Alle `window.dispatchEvent(new CustomEvent('requestStatusChanged'))` ersetzen durch `triggerRefresh('requests')`
   - Zeilen ca. 136, 156

WICHTIG: rate-limit, sync:reconnect, fcmToken Events NICHT anfassen — die bleiben als window Events.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -rn "window\.dispatchEvent.*events-updated\|window\.dispatchEvent.*konfis-updated\|window\.dispatchEvent.*requestStatusChanged" frontend/src/ | grep -v node_modules | wc -l | xargs test 0 -eq && echo "PASS: Keine alten Dispatcher mehr" || echo "FAIL: Alte Dispatcher gefunden"</automated>
  </verify>
  <acceptance_criteria>
    - grep "window.dispatchEvent.*events-updated" frontend/src/components/ findet 0 Treffer
    - grep "window.dispatchEvent.*konfis-updated" frontend/src/components/ findet 0 Treffer
    - grep "window.dispatchEvent.*requestStatusChanged" frontend/src/components/ findet 0 Treffer
    - grep "triggerRefresh" frontend/src/components/admin/views/EventDetailView.tsx findet mindestens 5 Treffer
    - grep "triggerRefresh" frontend/src/components/admin/views/KonfiDetailView.tsx findet mindestens 6 Treffer
    - grep "triggerRefresh" frontend/src/components/konfi/views/EventDetailView.tsx findet mindestens 4 Treffer
    - grep "triggerRefresh" frontend/src/components/admin/modals/ActivityRequestModal.tsx findet mindestens 2 Treffer
    - grep "'users'" frontend/src/contexts/LiveUpdateContext.tsx findet mindestens 1 Treffer
    - grep "'organizations'" frontend/src/contexts/LiveUpdateContext.tsx findet mindestens 1 Treffer
    - cd frontend && npx tsc --noEmit kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Alle 17 window.dispatchEvent-Aufrufe fuer Daten-Updates durch triggerRefresh() ersetzt. LiveUpdateType um users + organizations erweitert.</done>
</task>

</tasks>

<verification>
- Kein `window.dispatchEvent(new CustomEvent('events-updated'))` mehr in frontend/src/components/
- Kein `window.dispatchEvent(new CustomEvent('konfis-updated'))` mehr in frontend/src/components/
- Kein `window.dispatchEvent(new CustomEvent('requestStatusChanged'))` mehr in frontend/src/components/
- `cd frontend && npx tsc --noEmit` kompiliert fehlerfrei
</verification>

<success_criteria>
Alle Daten-Update-Dispatcher nutzen LiveUpdateContext.triggerRefresh() statt window.dispatchEvent.
</success_criteria>

<output>
After completion, create `.planning/phases/65-navigation-state-konsistenz/65-01-SUMMARY.md`
</output>
