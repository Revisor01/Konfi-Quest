---
phase: 42-material
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/material.js
  - frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
  - frontend/src/components/admin/pages/AdminMaterialPage.tsx
autonomous: true
gap_closure: true
requirements:
  - MAT-02
  - MAT-03

must_haves:
  truths:
    - "Teamer kann Material-Tab oeffnen und sieht Materialliste mit Tag-Chips"
    - "Teamer kann nach Jahrgang filtern"
    - "GET /api/material/tags gibt 200 fuer Teamer-Nutzer zurueck"
  artifacts:
    - path: "backend/routes/material.js"
      provides: "requireTeamer auf GET /tags, jahrgang_id Query-Filter auf GET /"
      contains: "requireTeamer"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialPage.tsx"
      provides: "Resiliente loadData-Funktion, Jahrgang-Filter-Chips"
    - path: "frontend/src/components/admin/pages/AdminMaterialPage.tsx"
      provides: "Jahrgang-Filter-Chips"
  key_links:
    - from: "TeamerMaterialPage.tsx"
      to: "/api/material/tags"
      via: "api.get mit requireTeamer"
      pattern: "api\\.get.*material/tags"
    - from: "TeamerMaterialPage.tsx"
      to: "/api/material?jahrgang_id=X"
      via: "api.get mit jahrgang_id Query-Param"
      pattern: "jahrgang_id"
---

<objective>
Gap Closure: requireOrgAdmin auf GET /tags fixen und Jahrgang-Filter ergaenzen

Purpose: Zwei Verifikations-Gaps schliessen — (1) Teamer koennen Material-Tab nicht laden wegen 403 auf /tags, (2) Jahrgang-Filterung fehlt in Backend und UI
Output: Funktionierende Material-Seite fuer Teamer mit Tag- und Jahrgang-Filtern
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-material/42-VERIFICATION.md

<interfaces>
<!-- Bestehende Backend-Middleware aus material.js -->
From backend/routes/material.js:
- `rbacVerifier` — JWT-Token verifizieren
- `requireOrgAdmin` — Nur org_admin Rolle erlaubt (PROBLEM: auf GET /tags)
- `requireTeamer` — Teamer + org_admin Rolle erlaubt (bereits auf GET / verwendet)

From backend/routes/material.js Zeile 76:
```javascript
router.get('/tags', rbacVerifier, requireOrgAdmin, async (req, res) => {
```

From backend/routes/material.js Zeile 160-163:
```javascript
router.get('/', rbacVerifier, requireTeamer, async (req, res) => {
    const orgId = req.user.organization_id;
    const { tag_id, search, event_id } = req.query;
```

From TeamerMaterialPage.tsx Zeile 61-76:
```typescript
const [matRes, tagsRes] = await Promise.all([
  api.get('/material', { params: { ... } }),
  api.get('/material/tags')
]);
setMaterials(matRes.data);
setTags(tagsRes.data);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — requireTeamer auf GET /tags + jahrgang_id Filter</name>
  <files>backend/routes/material.js</files>
  <action>
  Zwei Aenderungen in backend/routes/material.js:

  1. Zeile 76: `requireOrgAdmin` durch `requireTeamer` ersetzen bei GET /tags.
     Nur der GET-Endpoint wird geaendert. POST /tags, PUT /tags/:id und DELETE /tags/:id behalten `requireOrgAdmin`.

  2. Im GET / Endpoint (Zeile ~163): `jahrgang_id` zur Destrukturierung hinzufuegen:
     ```javascript
     const { tag_id, search, event_id, jahrgang_id } = req.query;
     ```
     Und den Filter-Block ergaenzen (nach dem event_id Block, vor dem ORDER BY):
     ```javascript
     if (jahrgang_id) {
       query += ` AND m.jahrgang_id = $${paramIndex}`;
       params.push(jahrgang_id);
       paramIndex++;
     }
     ```

  Kommentar bei GET /tags anpassen: "TAG ENDPOINTS (Lesen: requireTeamer, CRUD: requireOrgAdmin)"
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "requireTeamer" backend/routes/material.js | grep "tags" && grep -n "jahrgang_id" backend/routes/material.js | grep "req.query"</automated>
  </verify>
  <done>GET /tags verwendet requireTeamer. GET / unterstuetzt jahrgang_id als Query-Parameter. POST/PUT/DELETE /tags behalten requireOrgAdmin.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Resiliente loadData + Jahrgang-Filter in Teamer- und Admin-Seite</name>
  <files>frontend/src/components/teamer/pages/TeamerMaterialPage.tsx, frontend/src/components/admin/pages/AdminMaterialPage.tsx</files>
  <action>
  **TeamerMaterialPage.tsx:**

  1. Promise.all durch individuelle try/catch ersetzen, damit ein fehlgeschlagener Tags-Call nicht die gesamte Seite blockiert:
     ```typescript
     const loadData = useCallback(async () => {
       try {
         setLoading(true);
         const matRes = await api.get('/material', {
           params: {
             ...(activeTagId ? { tag_id: activeTagId } : {}),
             ...(search ? { search } : {}),
             ...(activeJahrgangId ? { jahrgang_id: activeJahrgangId } : {})
           }
         });
         setMaterials(matRes.data);
       } catch {
         setError('Fehler beim Laden der Materialien');
       }
       try {
         const tagsRes = await api.get('/material/tags');
         setTags(tagsRes.data);
       } catch {
         // Tags-Fehler nicht kritisch, Liste trotzdem anzeigen
       }
       setLoading(false);
     }, [activeTagId, search, activeJahrgangId]);
     ```

  2. Neuen State `activeJahrgangId` und `jahrgaenge` hinzufuegen. Jahrgaenge ueber bestehenden Endpoint `/jahrgaenge` laden (einmalig beim Mount, nicht bei jedem Filter-Wechsel).

  3. Jahrgang-Filter als zweite Chip-Reihe UNTER den Tag-Chips ergaenzen. "Alle" Chip als Default (kein Filter), dann ein Chip pro Jahrgang. Gleicher Style wie Tag-Chips, aber als eigene Zeile mit Label "Jahrgang:" davor. Akzentfarbe #d97706 beibehalten.

  4. activeJahrgangId in die loadData-Abhaengigkeiten und API-Params aufnehmen.

  **AdminMaterialPage.tsx:**

  1. Gleichen Jahrgang-Filter ergaenzen (State, API-Call, Chip-Reihe). Jahrgaenge laden und als Filter-Chips unter den Tag-Chips anzeigen.
  2. jahrgang_id als Query-Parameter in den Material-API-Call aufnehmen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "jahrgang_id" frontend/src/components/teamer/pages/TeamerMaterialPage.tsx && grep -n "jahrgang_id" frontend/src/components/admin/pages/AdminMaterialPage.tsx && cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <done>TeamerMaterialPage laedt resilient (Tags-Fehler blockiert nicht). Beide Seiten haben Jahrgang-Filter-Chips. jahrgang_id wird als Query-Parameter an GET /material uebergeben. TypeScript kompiliert ohne Fehler.</done>
</task>

</tasks>

<verification>
1. Backend: `grep -n "requireTeamer" backend/routes/material.js` zeigt GET /tags mit requireTeamer
2. Backend: `grep -n "jahrgang_id" backend/routes/material.js` zeigt jahrgang_id in req.query Destrukturierung und SQL-Filter
3. Frontend: TeamerMaterialPage hat keine Promise.all mehr fuer Material + Tags
4. Frontend: Beide Seiten haben activeJahrgangId State und Jahrgang-Chips
5. TypeScript kompiliert ohne Fehler
</verification>

<success_criteria>
- GET /api/material/tags gibt 200 fuer Teamer-Nutzer zurueck (requireTeamer statt requireOrgAdmin)
- GET /api/material?jahrgang_id=X filtert Ergebnisse nach Jahrgang
- TeamerMaterialPage zeigt Materialliste auch wenn Tags-Endpoint fehlschlaegt
- Beide Seiten haben Jahrgang-Filter-Chips
- MAT-02 und MAT-03 sind erfuellt
</success_criteria>

<output>
After completion, create `.planning/phases/42-material/42-03-SUMMARY.md`
</output>
