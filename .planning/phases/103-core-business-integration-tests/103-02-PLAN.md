---
phase: 103-core-business-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/routes/konfi.test.js
  - backend/tests/routes/chat.test.js
  - backend/tests/globalSetup.js
autonomous: true
requirements: [BIT-05, BIT-06]

must_haves:
  truths:
    - "Konfi-Dashboard liefert aggregierte Daten (Punkte, Level, Badges, Events)"
    - "Konfi-Profil liefert persoenliche Daten + Punkte"
    - "Konfi-Punkte-History liefert chronologische Eintraege"
    - "Chat-Raum-Erstellung funktioniert mit korrektem Typ"
    - "Chat-Nachrichten koennen gesendet und abgerufen werden"
    - "Chat-Teilnehmer koennen verwaltet werden"
  artifacts:
    - path: "backend/tests/routes/konfi.test.js"
      provides: "Konfi Integration-Tests"
      min_lines: 120
    - path: "backend/tests/routes/chat.test.js"
      provides: "Chat Integration-Tests"
      min_lines: 150
  key_links:
    - from: "backend/tests/routes/konfi.test.js"
      to: "/api/konfi"
      via: "supertest"
      pattern: "request.*api/konfi"
    - from: "backend/tests/routes/chat.test.js"
      to: "/api/chat"
      via: "supertest"
      pattern: "request.*api/chat"
---

<objective>
Konfi- und Chat-Integration-Tests erstellen.

Purpose: BIT-05 (Konfi Profil, Punkte-History, Dashboard-Aggregation) und BIT-06 (Chat Raeume, Nachrichten, Teilnehmer, Dateien) absichern.
Output: konfi.test.js + chat.test.js mit jeweils 8-15+ Tests gegen echte PostgreSQL Test-DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-core-business-integration-tests/103-CONTEXT.md
@.planning/phases/102-auth-rbac-integration-tests/102-01-SUMMARY.md

<interfaces>
<!-- Test-Infrastruktur Helpers -->

From backend/tests/helpers/testApp.js:
```javascript
function getTestApp(db) // supertest-faehige Express-App
```

From backend/tests/helpers/db.js:
```javascript
function getTestPool()    // PostgreSQL Pool
function truncateAll(db)  // TRUNCATE CASCADE
function closePool()      // Pool schliessen
```

From backend/tests/helpers/seed.js:
```javascript
const USERS = {
  konfi1: { id: 1, org_id: 1, type: 'konfi' },
  konfi2: { id: 2, org_id: 1, type: 'konfi' },
  admin1: { id: 4, org_id: 1, type: 'admin' },
  teamer1: { id: 3, org_id: 1, type: 'teamer' },
  konfi3: { id: 6, org_id: 2, type: 'konfi' },
};
const CHAT_ROOMS = {
  jahrgang: { id: 1, type: 'jahrgang', org_id: 1 },
  direct: { id: 2, type: 'direct', org_id: 1 },
  group: { id: 3, type: 'group', org_id: 1 },
  jahrgang2: { id: 4, type: 'jahrgang', org_id: 2 },
};
// Chat-Teilnehmer: konfi1+konfi2+teamer1+admin1 in room 1, konfi1+admin1 in room 2
const ACTIVITIES, EVENTS, BADGES, LEVELS, JAHRGAENGE, CATEGORIES, PASSWORD;
```

From backend/tests/helpers/auth.js:
```javascript
function generateToken(userKey) // z.B. generateToken('konfi1')
```

Route mount paths:
- Konfi: app.use('/api/konfi', konfiRouter) — verifyTokenRBAC intern, prueft req.user.type === 'konfi'
- Chat: app.use('/api/chat', chatRouter) — verifyTokenRBAC intern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Konfi Integration-Tests (BIT-05)</name>
  <files>backend/tests/routes/konfi.test.js, backend/tests/globalSetup.js</files>
  <read_first>
    - backend/routes/konfi.js (Endpoints: dashboard, profile, points-history, activities, badges, events)
    - backend/tests/routes/auth.test.js (bestehendes Test-Pattern)
    - backend/tests/helpers/seed.js (konfi_profiles, bonus_points Seed-Daten)
    - backend/tests/globalSetup.js (DB-Schema)
  </read_first>
  <action>
    Erstelle `backend/tests/routes/konfi.test.js` mit folgenden describe-Bloecken (per D-10):

    **Standard-Boilerplate:**
    - beforeAll: getTestPool + getTestApp
    - beforeEach: truncateAll + seed
    - afterAll: closePool
    - Token: generateToken('konfi1') fuer Konfi, generateToken('admin1') fuer Admin (soll 403 bekommen)

    **WICHTIG:** Konfi-Routes pruefen intern `req.user.type === 'konfi'`. Admin-Token bekommt daher 403 auf allen /api/konfi Endpoints.

    **1. GET /api/konfi/dashboard (per D-10):**
    - Konfi bekommt 200 + Dashboard-Daten
    - Response enthaelt: gottesdienst_points, gemeinde_points, level, badges, events (Aggregation)
    - Admin bekommt 403 (type !== 'konfi')
    - Ohne Token -> 401

    **2. GET /api/konfi/profile (per D-10):**
    - Konfi bekommt 200 + Profil-Daten (display_name, username, jahrgang)
    - Response enthaelt Punkte-Info

    **3. GET /api/konfi/points-history (per D-10):**
    - Konfi bekommt 200 + Array
    - Seed hat bonus_points fuer konfi1 (3 Punkte Sonderpunkte Weihnachten) — also mindestens 1 Eintrag
    - Pruefe chronologische Sortierung wenn mehrere Eintraege

    **4. GET /api/konfi/activities:**
    - Konfi bekommt 200 + verfuegbare Aktivitaeten der eigenen Org

    **5. GET /api/konfi/badges:**
    - Konfi bekommt 200 + Badge-Liste (custom_badges der Org + Fortschritt)

    **6. GET /api/konfi/events:**
    - Konfi bekommt 200 + Event-Liste der eigenen Org
    - Events der anderen Org NICHT enthalten

    Falls globalSetup.js fehlende Spalten braucht, diese ergaenzen (Pattern: ALTER TABLE IF NOT EXISTS).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config backend/tests/vitest.config.ts backend/tests/routes/konfi.test.js</automated>
  </verify>
  <acceptance_criteria>
    - konfi.test.js existiert mit mindestens 8 Tests
    - Alle Tests grueen (vitest exit code 0)
    - Mindestens 1 Test prueft Dashboard-Aggregation (mehrere Felder im Response)
    - Mindestens 1 Test prueft dass Admin 403 bekommt
    - grep -c "expect(res.status)" konfi.test.js >= 8
  </acceptance_criteria>
  <done>Konfi-Profil, Punkte-History und Dashboard-Aggregation mit Integration-Tests abgesichert</done>
</task>

<task type="auto">
  <name>Task 2: Chat Integration-Tests (BIT-06)</name>
  <files>backend/tests/routes/chat.test.js, backend/tests/globalSetup.js</files>
  <read_first>
    - backend/routes/chat.js (Endpoints: rooms, messages, participants, files, direct)
    - backend/tests/routes/konfi.test.js (gerade erstelltes Pattern)
    - backend/tests/helpers/seed.js (CHAT_ROOMS, chat_participants Seed-Daten)
    - backend/tests/globalSetup.js (DB-Schema — chat_messages, chat_read_status Tabellen)
  </read_first>
  <action>
    Erstelle `backend/tests/routes/chat.test.js` mit folgenden describe-Bloecken (per D-11, D-12):

    **Standard-Boilerplate.**

    **1. GET /api/chat/rooms (per D-11):**
    - Konfi1 bekommt 200 + Raeume in denen er Teilnehmer ist (Room 1 Jahrgang + Room 2 Direct)
    - Admin1 bekommt 200 + seine Raeume
    - Konfi3 (Org 2) sieht NICHT Raeume aus Org 1
    - Ohne Token -> 401

    **2. POST /api/chat/rooms (per D-11):**
    - Admin erstellt Gruppenraum: { name: 'Test-Gruppe', type: 'group' } -> 201
    - Validierung: type muss 'group', 'direct' oder 'jahrgang' sein

    **3. POST /api/chat/direct (per D-11):**
    - Konfi1 erstellt Direct-Chat mit Admin1: { targetUserId: 4, targetUserType: 'admin' } -> 200/201
    - Pruefe dass Chat-Room mit type='direct' erstellt wurde

    **4. GET /api/chat/rooms/:roomId/messages (per D-11):**
    - Zunaechst eine Nachricht senden (POST), dann GET -> 200 + Array mit Nachricht
    - Nicht-Teilnehmer bekommt 403

    **5. POST /api/chat/rooms/:roomId/messages (per D-11):**
    - Konfi1 sendet Nachricht in Room 1: { content: 'Hallo Test' } -> 200/201
    - Leere Nachricht ohne Datei -> 400
    - Nicht-Teilnehmer des Raums bekommt 403

    **6. GET /api/chat/rooms/:roomId/participants (per D-11):**
    - Admin bekommt 200 + Teilnehmer-Liste
    - Pruefe Anzahl entspricht Seed (Room 1 hat 4 Teilnehmer)

    **7. POST /api/chat/rooms/:roomId/participants (per D-11):**
    - Admin fuegt Teilnehmer hinzu (z.B. orgAdmin1 zu Room 3)
    - Pruefe dass Teilnehmer-Anzahl gestiegen

    **8. DELETE /api/chat/rooms/:roomId/participants/:userId/:userType (per D-11):**
    - Admin entfernt Teilnehmer -> 200
    - Pruefe dass Teilnehmer nicht mehr in Liste

    **9. Datei-Endpoints (per D-12):**
    - POST /api/chat/rooms/:roomId/messages mit multipart/form-data (supertest .attach())
    - Mindestens testen dass der Endpoint auf eine Nachricht mit file-Feld reagiert
    - Kein echter Upload noetig — supertest kann Buffer als file senden

    Falls globalSetup.js fehlende Spalten braucht (chat_messages.file_url, chat_messages.file_type, chat_polls, etc.), diese ergaenzen.

    WICHTIG: Chat-Rooms haben Teilnehmer-Pruefung — nur Teilnehmer koennen Nachrichten lesen/senden. Nutze die Seed-Teilnehmer (konfi1 ist in Room 1 und 2).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config backend/tests/vitest.config.ts backend/tests/routes/chat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - chat.test.js existiert mit mindestens 10 Tests
    - Alle Tests grueen (vitest exit code 0)
    - Mindestens 1 Test prueft Nachrichten senden + lesen
    - Mindestens 1 Test prueft Teilnehmer-Verwaltung
    - Mindestens 1 Test prueft Datei-Endpoint (supertest file mock)
    - grep -c "expect(res.status)" chat.test.js >= 10
  </acceptance_criteria>
  <done>Chat-Raeume, Nachrichten-CRUD, Teilnehmer-Verwaltung und Datei-Endpoints mit Integration-Tests abgesichert</done>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config backend/tests/vitest.config.ts backend/tests/routes/konfi.test.js backend/tests/routes/chat.test.js
```
Beide Test-Files muessen durchlaufen. Keine skipped oder failing Tests.
</verification>

<success_criteria>
- konfi.test.js: Dashboard-Aggregation + Profil + Punkte-History + Cross-Org
- chat.test.js: Raum-CRUD + Nachrichten + Teilnehmer + Datei-Mock
- Alle Tests gegen echte PostgreSQL Test-DB (kein Mocking, per D-02)
- Jeder Test mit sauberem Seed (per D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/103-core-business-integration-tests/103-02-SUMMARY.md`
</output>
