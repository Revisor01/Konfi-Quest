---
phase: 101-test-infrastruktur-server-js-refactoring
plan: 03
type: execute
wave: 3
depends_on: [101-01, 101-02]
files_modified:
  - backend/tests/routes/smoke.test.js
autonomous: true
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05, INF-06]

must_haves:
  truths:
    - "npm test startet Vitest und fuehrt Smoke-Test erfolgreich aus"
    - "Health-Endpoint antwortet mit 200 OK"
    - "Login mit Seed-User funktioniert und gibt Token zurueck"
    - "Geschuetzter Endpoint mit gueltigem Token gibt 200 zurueck"
    - "Geschuetzter Endpoint ohne Token gibt 401 zurueck"
  artifacts:
    - path: "backend/tests/routes/smoke.test.js"
      provides: "Erster Integration-Test der die gesamte Infrastruktur validiert"
      min_lines: 40
  key_links:
    - from: "backend/tests/routes/smoke.test.js"
      to: "backend/tests/helpers/testApp.js"
      via: "getTestApp importieren"
      pattern: "require.*testApp"
    - from: "backend/tests/routes/smoke.test.js"
      to: "backend/tests/helpers/seed.js"
      via: "seed() und Konstanten importieren"
      pattern: "require.*seed"
    - from: "backend/tests/routes/smoke.test.js"
      to: "backend/tests/helpers/auth.js"
      via: "generateToken importieren"
      pattern: "require.*auth"
---

<objective>
Smoke-Test schreiben und ausfuehren — validiert die gesamte Test-Infrastruktur Ende-zu-Ende.

Purpose: Der Smoke-Test ist der Beweis dass alle 6 INF-Requirements erfuellt sind: createApp funktioniert (INF-01), createTestApp verdrahtet korrekt (INF-02), Test-DB wird erstellt/migriert/geseeded (INF-03), Auth-Helpers erzeugen gueltige Tokens (INF-04), Seed-Daten sind korrekt (INF-05), Vitest laeuft sequentiell (INF-06). Wenn `npm test` gruen ist, ist Phase 101 komplett.
Output: backend/tests/routes/smoke.test.js — 4-6 Tests die Health, Login, Auth-Guard und RBAC pruefen
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-test-infrastruktur-server-js-refactoring/101-CONTEXT.md
@.planning/phases/101-test-infrastruktur-server-js-refactoring/101-01-SUMMARY.md
@.planning/phases/101-test-infrastruktur-server-js-refactoring/101-02-SUMMARY.md

<interfaces>
From backend/tests/helpers/testApp.js:
```javascript
const { getTestApp } = require('../helpers/testApp');
// getTestApp(db) -> Express App (supertest-faehig)
```

From backend/tests/helpers/db.js:
```javascript
const { getTestPool, truncateAll, closePool } = require('../helpers/db');
// getTestPool() -> { query, getClient, end }
// truncateAll(db) -> TRUNCATE alle Tabellen
// closePool() -> Pool schliessen
```

From backend/tests/helpers/seed.js:
```javascript
const { seed, USERS, PASSWORD } = require('../helpers/seed');
// seed(db) -> Basisdaten einfuegen
// USERS.konfi1.username = 'konfi1'
// PASSWORD = 'testpasswort123'
```

From backend/tests/helpers/auth.js:
```javascript
const { generateToken } = require('../helpers/auth');
// generateToken('konfi1') -> JWT-String
// generateToken('admin1') -> JWT-String
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Smoke-Test schreiben und mit npm test ausfuehren</name>
  <files>backend/tests/routes/smoke.test.js</files>
  <read_first>
    - backend/tests/helpers/testApp.js (getTestApp Signatur)
    - backend/tests/helpers/db.js (getTestPool, truncateAll Signatur)
    - backend/tests/helpers/seed.js (seed Funktion, USERS Konstante, PASSWORD)
    - backend/tests/helpers/auth.js (generateToken Signatur)
    - backend/tests/vitest.config.ts (ENV-Variablen, globalSetup Pfad)
    - backend/createApp.js (Health-Endpoint Pfad, Route-Pfade)
  </read_first>
  <behavior>
    - Test 1: GET /api/health antwortet mit 200 und { status: 'OK' }
    - Test 2: POST /api/auth/login mit gueltigem Konfi-User (username: 'konfi1', password: 'testpasswort123') gibt 200 + token + refreshToken zurueck
    - Test 3: POST /api/auth/login mit falschem Passwort gibt 401 zurueck
    - Test 4: GET /api/konfi/dashboard mit gueltigem Konfi-Token gibt 200 zurueck (beweist RBAC + DB funktioniert)
    - Test 5: GET /api/konfi/dashboard ohne Token gibt 401 zurueck
    - Test 6: Tokens fuer alle 5 Rollen (konfi1, teamer1, admin1, orgAdmin1, superAdmin) sind erzeugbar und gueltig (jwt.verify wirft keinen Error)
  </behavior>
  <action>
**backend/tests/routes/smoke.test.js erstellen:**

```javascript
const { describe, it, expect, beforeAll, beforeEach, afterAll } = require('vitest');
const request = require('supertest');
const { getTestApp } = require('../helpers/testApp');
const { getTestPool, truncateAll, closePool } = require('../helpers/db');
const { seed, USERS, PASSWORD } = require('../helpers/seed');
const { generateToken, getAllTokens } = require('../helpers/auth');

describe('Smoke Tests', () => {
  let app;
  let db;

  beforeAll(async () => {
    db = getTestPool();
    app = getTestApp(db);
  });

  beforeEach(async () => {
    await truncateAll(db);
    await seed(db);
  });

  afterAll(async () => {
    await closePool();
  });

  it('GET /api/health antwortet mit OK', async () => {
    const res = await request(app).get('/api/health');
    expect(res.status).toBe(200);
    expect(res.body.status).toBe('OK');
  });

  it('POST /api/auth/login mit gueltigem Konfi', async () => {
    const res = await request(app)
      .post('/api/auth/login')
      .send({ username: USERS.konfi1.username, password: PASSWORD });
    expect(res.status).toBe(200);
    expect(res.body).toHaveProperty('token');
    expect(res.body).toHaveProperty('refreshToken');
  });

  it('POST /api/auth/login mit falschem Passwort gibt 401', async () => {
    const res = await request(app)
      .post('/api/auth/login')
      .send({ username: USERS.konfi1.username, password: 'falsch' });
    expect(res.status).toBe(401);
  });

  it('GET /api/konfi/dashboard mit gueltigem Token gibt 200', async () => {
    const token = generateToken('konfi1');
    const res = await request(app)
      .get('/api/konfi/dashboard')
      .set('Authorization', `Bearer ${token}`);
    expect(res.status).toBe(200);
  });

  it('GET /api/konfi/dashboard ohne Token gibt 401', async () => {
    const res = await request(app).get('/api/konfi/dashboard');
    expect(res.status).toBe(401);
  });

  it('Auth-Helpers erzeugen gueltige Tokens fuer alle 5 Rollen', () => {
    const jwt = require('jsonwebtoken');
    const tokens = getAllTokens();
    const roleKeys = ['konfi1', 'teamer1', 'admin1', 'orgAdmin1', 'superAdmin'];
    for (const key of roleKeys) {
      expect(tokens[key]).toBeDefined();
      const decoded = jwt.verify(tokens[key], process.env.JWT_SECRET);
      expect(decoded.id).toBe(USERS[key].id);
    }
  });
});
```

**Nach dem Erstellen: Test ausfuehren.**

Falls Docker lokal nicht verfuegbar ist (Research: "Docker: Nicht auf der lokalen Maschine verfuegbar"):
1. Pruefe ob Docker verfuegbar: `docker --version`
2. Falls ja: `cd backend && npm test`
3. Falls nein: Starte Test-DB manuell oder pruefe ob PostgreSQL lokal laeuft auf Port 5433
4. Alternative: `cd backend && docker compose -f docker-compose.test.yml up -d --wait && npx vitest run --config tests/vitest.config.ts`

Bei Fehlern iterativ fixen:
- JWT_SECRET Error -> Env-Variable pruefen
- DB Connection Error -> docker-compose.test.yml pruefen, Port 5433
- Migration Error -> init-scripts / migrations Pfad pruefen
- Seed Error -> FK-Constraints, Spaltennamen aus Migrations verifizieren
- 401 statt 200 -> Token-Payload pruefen (id, type muessen mit Seed uebereinstimmen)
- RBAC Cache -> NODE_ENV=test sollte Cache-TTL beeinflussen (Pitfall 3)

WICHTIG: Dieser Task ist erst erledigt wenn `npm test` (oder `npx vitest run --config tests/vitest.config.ts`) ALLE 6 Tests GRUEN meldet. Iteriere bis alle Tests passen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend &amp;&amp; npx vitest run --config tests/vitest.config.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/routes/smoke.test.js existiert mit mindestens 6 Tests
    - Vitest Ausgabe zeigt "6 passed" (oder mehr) und "0 failed"
    - Test "GET /api/health" prueft Status 200 und body.status === 'OK'
    - Test "POST /api/auth/login" prueft Status 200 mit token und refreshToken
    - Test "falschem Passwort" prueft Status 401
    - Test "mit gueltigem Token" prueft Status 200 auf /api/konfi/dashboard
    - Test "ohne Token" prueft Status 401
    - Test "alle 5 Rollen" verifiziert Tokens fuer konfi, teamer, admin, orgAdmin, superAdmin
  </acceptance_criteria>
  <done>npm test fuehrt Vitest aus, alle 6 Smoke-Tests sind gruen, Phase 101 Success Criteria erfuellt</done>
</task>

</tasks>

<verification>
```bash
cd backend && npx vitest run --config tests/vitest.config.ts
```
Erwartetes Ergebnis: Alle Tests bestanden, keine Fehler, Exit-Code 0.

Phase 101 Success Criteria Checkliste:
1. `npm test` im Backend-Verzeichnis startet Vitest und fuehrt mindestens einen Smoke-Test erfolgreich aus
2. server.js exportiert createApp-Funktion (via createApp.js) die Express-App ohne listen(), Firebase, Cron zurueckgibt
3. Test-DB wird automatisch erstellt, migriert, geseeded und nach dem Testlauf geloescht
4. Auth-Helpers erzeugen gueltige JWT-Tokens fuer alle 5 RBAC-Rollen in 2 verschiedenen Organisationen
</verification>

<success_criteria>
1. `npx vitest run --config tests/vitest.config.ts` zeigt "6 passed, 0 failed"
2. Health, Login, Auth-Guard und RBAC-Token-Validierung funktionieren gegen echte Test-DB
3. Phase 101 ist komplett — Backend ist testbar
</success_criteria>

<output>
After completion, create `.planning/phases/101-test-infrastruktur-server-js-refactoring/101-03-SUMMARY.md`
</output>
