---
phase: 101-test-infrastruktur-server-js-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/createApp.js
  - backend/server.js
  - backend/tests/vitest.config.ts
  - backend/docker-compose.test.yml
  - backend/package.json
  - backend/tests/globalSetup.js
autonomous: true
requirements: [INF-01, INF-06, INF-03]

must_haves:
  truths:
    - "createApp(db, options) gibt eine Express-App zurueck ohne listen(), Socket.IO, SMTP, Cron oder Firebase"
    - "server.js startet weiterhin identisch wie vorher (Produktion unveraendert)"
    - "Vitest Backend-Config existiert mit sequentieller Ausfuehrung und korrekten ENV-Variablen"
    - "docker-compose.test.yml startet postgres:16-alpine auf Port 5433"
  artifacts:
    - path: "backend/createApp.js"
      provides: "Express-App Factory ohne Seiteneffekte"
      exports: ["createApp"]
    - path: "backend/tests/vitest.config.ts"
      provides: "Vitest Backend-Konfiguration"
      contains: "singleFork"
    - path: "backend/docker-compose.test.yml"
      provides: "Test-DB Container Definition"
      contains: "postgres:16-alpine"
  key_links:
    - from: "backend/server.js"
      to: "backend/createApp.js"
      via: "require('./createApp')"
      pattern: "require.*createApp"
    - from: "backend/createApp.js"
      to: "backend/routes/*.js"
      via: "Route-Factory-DI mit uebergebenem db"
      pattern: "require.*routes"
---

<objective>
server.js in createApp-Factory refactoren und Vitest-Infrastruktur aufsetzen.

Purpose: Ohne createApp-Factory sind keine Integration-Tests moeglich — server.js startet beim Import sofort Server, Socket.IO, SMTP, Firebase und Cron. Die Vitest-Config und docker-compose.test.yml sind die technische Grundlage fuer alle folgenden Test-Plans.
Output: backend/createApp.js (testbare Express-Factory), aktualisierte server.js (Wrapper), Vitest-Config, docker-compose.test.yml, npm test Scripts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-test-infrastruktur-server-js-refactoring/101-CONTEXT.md
@.planning/phases/101-test-infrastruktur-server-js-refactoring/101-RESEARCH.md

<interfaces>
From backend/database.js:
```javascript
module.exports = {
  query: (text, params) => pool.query(text, params),
  getClient: () => pool.connect(),
  end: () => pool.end(),
};
```

From backend/middleware/rbac.js (Zeile 1-6):
```javascript
const JWT_SECRET = process.env.JWT_SECRET;
if (!JWT_SECRET) { throw new Error('JWT_SECRET environment variable is required'); }
// Exports: verifyTokenRBAC(db), filterByJahrgangAccess, requireSuperAdmin, requireOrgAdmin, requireAdmin, requireTeamer
```

Route-Factory-Signaturen (alle 18 Routes):
```
auth.js: (db, verifyToken, transporter, SMTP_CONFIG, rateLimiters, rbacVerifier)
konfi.js: (db, { verifyTokenRBAC }, requestUpload)
chat.js: (db, { verifyTokenRBAC }, uploadsDir, chatUpload, io)
events.js: (db, rbacVerifier, roleHelpers, checkAndAwardBadges)
badges.js: (db, rbacVerifier, roleHelpers) — router.checkAndAwardBadges angehaengt
activities.js: (db, rbacVerifier, roleHelpers, checkAndAwardBadges)
konfi-management.js: (db, rbacVerifier, roleHelpers, filterByJahrgangAccess, checkAndAwardBadges)
notifications.js: (db, rbacVerifier)
settings.js: (db, rbacVerifier, roleHelpers)
jahrgaenge.js: (db, rbacVerifier, roleHelpers)
categories.js: (db, rbacVerifier, roleHelpers)
users.js: (db, rbacVerifier, roleHelpers, io)
roles.js: (db, rbacVerifier, roleHelpers)
organizations.js: (db, rbacVerifier, roleHelpers)
levels.js: (db, rbacVerifier, roleHelpers)
teamer.js: (db, rbacVerifier, roleHelpers)
wrapped.js: (db, rbacVerifier, roleHelpers)
material.js: (db, rbacVerifier, roleHelpers, materialUpload)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createApp.js Factory aus server.js extrahieren (INF-01, per D-07/D-08)</name>
  <files>backend/createApp.js, backend/server.js</files>
  <read_first>
    - backend/server.js (komplett lesen — 591 Zeilen, Quelle fuer Extraktion)
    - backend/middleware/rbac.js (erste 10 Zeilen — JWT_SECRET Guard verstehen)
    - backend/database.js (Pool-Export-Struktur)
  </read_first>
  <action>
**Neue Datei `backend/createApp.js` erstellen:**

Funktion `createApp(db, options = {})` mit folgenden options:
- `transporter` — SMTP (Default: Dummy `{ sendMail: async () => ({}) }`)
- `smtpConfig` — SMTP-Config (Default: `{}`)
- `io` — Socket.IO (Default: Dummy mit to/emit/in als No-Ops)
- `rateLimiters` — Objekt mit general, authLimiter, registerLimiter, chatMessageLimiter, eventBookingLimiter, uploadLimiter, orgLimiter (Default: `{}`)
- `uploadsDir` — Upload-Pfad (Default: `path.join(__dirname, 'uploads')`)

Aus server.js in createApp verschieben:
1. Express-App erstellen + `app.set('trust proxy', 1)`
2. Helmet Security Headers (Zeile 269-277 aus server.js)
3. `if (rateLimiters.general) app.use(rateLimiters.general)` — Rate-Limiter NUR wenn vorhanden
4. `app.use(express.json())`
5. Upload-Verzeichnisse erstellen mit `options.uploadsDir` (Zeile 288-298)
6. Multer-Configs (chatUpload, materialUpload, requestUpload) mit `options.uploadsDir`
7. verifyToken-Funktion INLINE definieren (Zeile 405-418 aus server.js) — liest `process.env.JWT_SECRET`
8. RBAC-Middleware: `verifyTokenRBAC(db)`, roleHelpers, filterByJahrgangAccess
9. badgesRouter ZUERST, dann activitiesRouter (Pitfall 5: badges.checkAndAwardBadges wird weitergegeben)
10. Health-Endpoint: `app.get('/api/health', ...)`
11. Alle Route-Mounts mit EXAKT gleichen Pfaden und Parametern wie in server.js Zeile 472-508
12. Spezifische Rate-Limiters auf Routes NUR wenn vorhanden (chatMessageLimiter, eventBookingLimiter, uploadLimiter, orgLimiter)
13. Error-Handler am Ende
14. `app.wrappedRouter = wrappedRouter` (fuer BackgroundService in server.js)
15. `return app`

Dummy-Objekte fuer Tests:
```javascript
const ioOrDummy = io || { to: () => ({ emit: () => {} }), emit: () => {}, in: () => ({ emit: () => {} }) };
const transporterOrDummy = transporter || { sendMail: async () => ({}) };
```

NICHT in createApp (bleiben in server.js):
- Socket.IO Server (`new Server()`)
- Socket.IO auth middleware + connection handler
- SMTP-Transporter erstellen + verify
- Firebase initialisieren
- BackgroundService.startAllServices
- chatUtils.initializeChatRooms
- server.listen()
- Graceful Shutdown
- liveUpdate.init(io)

Export: `module.exports = { createApp };`

**server.js anpassen (per D-08 — Produktions-Verhalten identisch):**

Neue Reihenfolge in server.js:
```javascript
// 1. Requires (express, http, socket.io, nodemailer, etc.)
// 2. JWT_SECRET check
// 3. const db = require('./database');
// 4. const server = http.createServer();  // OHNE app — app kommt spaeter
// 5. Socket.IO Setup: const io = new Server(server, { cors... });
// 6. Socket.IO auth middleware + connection handler (wie bisher Zeile 60-156)
// 7. liveUpdate.init(io)
// 8. SMTP Setup (wie bisher Zeile 166-188)
// 9. Rate-Limiters erstellen (wie bisher Zeile 195-256)
// 10. const { createApp } = require('./createApp');
// 11. const app = createApp(db, { transporter, smtpConfig: SMTP_CONFIG, io, rateLimiters: {...}, uploadsDir });
// 12. server.on('request', app);  // HTTP requests an Express-App weiterleiten
// 13. chatUtils.initializeChatRooms(db)
// 14. BackgroundService.startAllServices(db, { wrappedRouter: app.wrappedRouter })
// 15. Firebase Status
// 16. server.listen(PORT, () => { ... })
// 17. Graceful Shutdown
```

KRITISCH: `http.createServer()` ohne Argument erstellen, dann `server.on('request', app)` NACH createApp. So kann Socket.IO den server haben BEVOR createApp laeuft, und createApp bekommt io als Parameter.

Alle Route-Imports (Zeile 425-445), Route-Mounts (Zeile 472-508), Middleware-Setup (Zeile 258-418), Upload-Setup (Zeile 284-398) und Error-Handler (Zeile 528-531) werden aus server.js ENTFERNT (leben jetzt in createApp.js).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend &amp;&amp; node -e "process.env.JWT_SECRET='test'; process.env.DATABASE_URL='postgresql://x:x@localhost/x'; const { createApp } = require('./createApp'); const db = { query: async () => ({ rows: [] }), getClient: async () => ({ query: async () => ({}), release: () => {} }) }; const app = createApp(db); console.log(typeof app === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/createApp.js existiert und exportiert createApp
    - createApp gibt ein Express-App-Objekt zurueck (typeof === 'function')
    - server.js importiert createApp: grep "require.*createApp" backend/server.js findet Treffer
    - server.js enthaelt KEINE Route-Mounts: grep "app.use.*api/auth" backend/server.js findet KEINEN Treffer
    - server.js enthaelt weiterhin Socket.IO, SMTP, Firebase, BackgroundService, listen, Graceful Shutdown
    - createApp.js enthaelt KEIN listen(), KEIN new Server(), KEIN nodemailer.createTransport, KEIN firebase, KEIN backgroundService
    - Alle 19 API-Pfade in createApp.js: grep "/api/" backend/createApp.js zeigt auth, konfi, chat, events, notifications, settings, admin/activities, admin/badges, admin/konfis, admin/jahrgaenge, admin/categories, admin/users, users, roles, organizations, levels, teamer, wrapped, material
    - Health-Endpoint in createApp.js: grep "api/health" backend/createApp.js findet Treffer
  </acceptance_criteria>
  <done>createApp.js exportiert seiteneffektfreie Express-Factory, server.js ist schlanker Wrapper der Produktion identisch startet</done>
</task>

<task type="auto">
  <name>Task 2: Vitest-Config + docker-compose.test.yml + npm Scripts (INF-06, INF-03 partial, per D-02/D-03/D-04)</name>
  <files>backend/tests/vitest.config.ts, backend/docker-compose.test.yml, backend/package.json, backend/tests/globalSetup.js</files>
  <read_first>
    - backend/package.json (aktuelle Scripts und Dependencies)
  </read_first>
  <action>
**1. Dependencies installieren:**
```bash
cd backend &amp;&amp; npm install -D vitest supertest
```

**2. Verzeichnisse erstellen:**
```bash
mkdir -p backend/tests/helpers backend/tests/routes
```

**3. backend/tests/vitest.config.ts erstellen:**
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    pool: 'forks',
    poolOptions: {
      forks: {
        singleFork: true,
      },
    },
    globalSetup: ['./tests/globalSetup.js'],
    include: ['tests/**/*.test.{js,ts}'],
    env: {
      JWT_SECRET: 'test-secret-key-for-vitest',
      NODE_ENV: 'test',
      TEST_DATABASE_URL: 'postgresql://postgres:postgres@localhost:5433/postgres',
    },
    testTimeout: 10000,
  },
});
```

**4. backend/docker-compose.test.yml erstellen (per D-03):**
```yaml
services:
  test-db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 5
```

**5. backend/package.json Scripts aktualisieren (per D-04):**
```json
"test": "docker compose -f docker-compose.test.yml up -d --wait && npx vitest run --config tests/vitest.config.ts; EXIT=$?; docker compose -f docker-compose.test.yml down; exit $EXIT",
"test:ci": "npx vitest run --config tests/vitest.config.ts",
"test:watch": "npx vitest --config tests/vitest.config.ts"
```

**6. Placeholder backend/tests/globalSetup.js erstellen** (damit Vitest nicht crasht):
```javascript
// backend/tests/globalSetup.js — Wird in Plan 02 vollstaendig implementiert
module.exports = async function globalSetup() {
  console.log('[globalSetup] Placeholder — wird in Plan 02 implementiert');
};
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend &amp;&amp; node -e "const pkg = require('./package.json'); const ok = [pkg.devDependencies.vitest, pkg.devDependencies.supertest, pkg.scripts.test.includes('vitest'), pkg.scripts['test:ci']].every(Boolean); console.log(ok ? 'OK' : 'FAIL')" &amp;&amp; test -f tests/vitest.config.ts &amp;&amp; echo "OK: vitest.config.ts" &amp;&amp; test -f docker-compose.test.yml &amp;&amp; echo "OK: docker-compose.test.yml"</automated>
  </verify>
  <acceptance_criteria>
    - vitest und supertest in devDependencies: grep "vitest" backend/package.json und grep "supertest" backend/package.json
    - package.json "test" Script enthaelt "vitest": grep "vitest" backend/package.json
    - package.json "test:ci" Script existiert
    - backend/tests/vitest.config.ts existiert mit singleFork: true
    - backend/tests/vitest.config.ts enthaelt JWT_SECRET in env
    - backend/docker-compose.test.yml existiert mit postgres:16-alpine und Port 5433
    - backend/tests/globalSetup.js existiert (Placeholder)
    - backend/tests/helpers/ Verzeichnis existiert
    - backend/tests/routes/ Verzeichnis existiert
  </acceptance_criteria>
  <done>Vitest laeuft sequentiell, docker-compose.test.yml bereit, npm test/test:ci Scripts konfiguriert</done>
</task>

</tasks>

<verification>
- createApp.js ist importierbar ohne Seiteneffekte (kein listen, kein Socket.IO, kein SMTP)
- server.js nutzt createApp und startet Produktion wie bisher
- Vitest Config vorhanden mit sequentieller Ausfuehrung (singleFork: true)
- docker-compose.test.yml startet postgres:16-alpine auf Port 5433
- npm test Script verkettet docker-compose + vitest
</verification>

<success_criteria>
1. `node -e "require('./createApp')"` im Backend-Verzeichnis funktioniert ohne Fehler (mit JWT_SECRET env)
2. createApp.js enthaelt alle 19 Route-Mounts, server.js enthaelt keine
3. vitest und supertest sind in devDependencies
4. docker-compose.test.yml ist valides YAML mit postgres:16-alpine
</success_criteria>

<output>
After completion, create `.planning/phases/101-test-infrastruktur-server-js-refactoring/101-01-SUMMARY.md`
</output>
