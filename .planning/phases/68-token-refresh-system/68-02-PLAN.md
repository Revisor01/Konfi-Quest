---
phase: 68-token-refresh-system
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - frontend/src/services/tokenStore.ts
  - frontend/src/services/api.ts
  - frontend/src/services/auth.ts
autonomous: true
requirements: [TOKEN-STORE, TOKEN-REFRESH-FE, TOKEN-RELOGIN, TOKEN-OFFLINE]

must_haves:
  truths:
    - "401-Response triggert automatisch Refresh-Versuch bevor Logout"
    - "Refresh-Token wird in Capacitor Preferences gespeichert"
    - "Bei abgelaufenem Refresh-Token erscheint Re-Login-Dialog (IonAlert)"
    - "Offline + abgelaufener Access-Token: gecachte Daten bleiben sichtbar"
    - "Nach erfolgreichem Re-Login bleibt aktive Page erhalten"
  artifacts:
    - path: "frontend/src/services/tokenStore.ts"
      provides: "refreshToken get/set/clear Funktionen"
      contains: "getRefreshToken"
    - path: "frontend/src/services/api.ts"
      provides: "401-Handler mit Refresh-Logik + Re-Login-Dialog"
      contains: "auth/refresh"
    - path: "frontend/src/services/auth.ts"
      provides: "Login speichert refresh_token, Logout loescht ihn"
      contains: "setRefreshToken"
  key_links:
    - from: "frontend/src/services/api.ts"
      to: "/api/auth/refresh"
      via: "axios.post bei 401"
      pattern: "auth/refresh"
    - from: "frontend/src/services/auth.ts"
      to: "frontend/src/services/tokenStore.ts"
      via: "setRefreshToken/clearAuth"
      pattern: "setRefreshToken"
---

<objective>
Frontend-Integration des Token-Refresh-Systems: tokenStore um Refresh-Token erweitern, api.ts 401-Handler mit automatischem Refresh, auth.ts Login/Logout anpassen, Re-Login-Dialog bei abgelaufenem Refresh-Token.

Purpose: Nahtlose Token-Erneuerung fuer den User — kein Logout bei 15min Access-Token-Ablauf, Re-Login nur nach 90 Tagen Inaktivitaet.
Output: Vollstaendiger Frontend-Refresh-Flow, transparent fuer den User.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-token-refresh-system/68-01-SUMMARY.md

@frontend/src/services/tokenStore.ts
@frontend/src/services/api.ts
@frontend/src/services/auth.ts
</context>

<interfaces>
<!-- Bestehende Interfaces -->

From frontend/src/services/tokenStore.ts:
```typescript
// Memory-Cache + Preferences Pattern
let _token: string | null = null;
let _user: BaseUser | null = null;

export const getToken = (): string | null => _token;
export const setToken = async (token: string): Promise<void> => { ... };
export const clearAuth = async (): Promise<void> => { ... };
export const initTokenStore = async (): Promise<void> => { ... };
```

From frontend/src/services/api.ts:
```typescript
// 401-Handler aktuell: clearAuth() + redirect auf /
// Muss werden: Refresh versuchen, bei Fehler Re-Login-Dialog
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) { ... }
  }
);
```

From frontend/src/services/auth.ts:
```typescript
// Login aktuell: const { token, user } = response.data;
// Muss werden: const { token, refresh_token, user } = response.data;
export const loginWithAutoDetection = async (username, password): Promise<BaseUser> => { ... };
export const logout = async (): Promise<void> => { ... };
```

<!-- Neuer Backend-Endpoint (aus Plan 01) -->
POST /api/auth/refresh
  Body: { refresh_token: string }
  Success 200: { token: string, refresh_token: string }
  Error 401: { error: string } (abgelaufen/ungueltig)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: tokenStore um Refresh-Token erweitern</name>
  <files>frontend/src/services/tokenStore.ts</files>
  <read_first>
    - frontend/src/services/tokenStore.ts (komplette Datei — 73 Zeilen)
  </read_first>
  <action>
    Per D-06: Refresh-Token in Capacitor Preferences speichern (gleicher TokenStore wie Access-Token).

    1. **Neue Memory-Variable** (nach `let _pushTokenTimestamp`):
       ```typescript
       let _refreshToken: string | null = null;
       ```

    2. **Synchroner Getter** (nach getPushTokenTimestamp):
       ```typescript
       export const getRefreshToken = (): string | null => _refreshToken;
       ```

    3. **Async Setter** (nach setPushTokenTimestamp):
       ```typescript
       export const setRefreshToken = async (token: string): Promise<void> => {
         _refreshToken = token;
         await Preferences.set({ key: 'konfi_refresh_token', value: token });
       };
       ```

    4. **clearAuth erweitern** — Refresh-Token ebenfalls loeschen:
       ```typescript
       export const clearAuth = async (): Promise<void> => {
         _token = null;
         _user = null;
         _refreshToken = null;
         await Preferences.remove({ key: 'konfi_token' });
         await Preferences.remove({ key: 'konfi_user' });
         await Preferences.remove({ key: 'konfi_refresh_token' });
       };
       ```

    5. **initTokenStore erweitern** — Refresh-Token beim App-Start laden (nach pushResult):
       ```typescript
       const refreshResult = await Preferences.get({ key: 'konfi_refresh_token' });
       _refreshToken = refreshResult.value;
       ```
  </action>
  <verify>
    <automated>grep -q "getRefreshToken" frontend/src/services/tokenStore.ts && grep -q "setRefreshToken" frontend/src/services/tokenStore.ts && grep -q "konfi_refresh_token" frontend/src/services/tokenStore.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "getRefreshToken" frontend/src/services/tokenStore.ts findet synchronen Getter
    - grep "setRefreshToken" frontend/src/services/tokenStore.ts findet async Setter
    - grep "konfi_refresh_token" frontend/src/services/tokenStore.ts findet 3 Treffer (set, remove, get)
    - grep "_refreshToken = null" frontend/src/services/tokenStore.ts findet Treffer in clearAuth
  </acceptance_criteria>
  <done>tokenStore hat getRefreshToken/setRefreshToken. clearAuth loescht Refresh-Token. initTokenStore laedt Refresh-Token aus Preferences.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts Refresh-Logik + auth.ts Login/Logout anpassen</name>
  <files>frontend/src/services/api.ts, frontend/src/services/auth.ts</files>
  <read_first>
    - frontend/src/services/api.ts (komplette Datei — 80 Zeilen)
    - frontend/src/services/auth.ts (komplette Datei — 112 Zeilen)
  </read_first>
  <action>
    **api.ts aendern** per D-05, D-09, D-12, D-13, D-14, D-15, D-16:

    1. **Imports erweitern**:
       ```typescript
       import { getToken, getRefreshToken, setToken, setRefreshToken, clearAuth } from './tokenStore';
       ```

    2. **Refresh-State** (vor dem Request-Interceptor):
       ```typescript
       // Verhindere parallele Refresh-Requests
       let isRefreshing = false;
       let refreshSubscribers: ((token: string) => void)[] = [];

       const onTokenRefreshed = (token: string) => {
         refreshSubscribers.forEach(cb => cb(token));
         refreshSubscribers = [];
       };

       const addRefreshSubscriber = (cb: (token: string) => void) => {
         refreshSubscribers.push(cb);
       };
       ```

    3. **401-Handler komplett ersetzen** (response interceptor error-Callback):
       ```typescript
       async (error) => {
         const originalRequest = error.config;

         if (error.response?.status === 401) {
           const isLoginRequest = originalRequest?.url?.includes('/login');
           const isRefreshRequest = originalRequest?.url?.includes('/refresh');

           // Login- und Refresh-Requests nicht retry'en
           if (isLoginRequest || isRefreshRequest) {
             return Promise.reject(error);
           }

           // Offline: Token behalten, gecachte Daten nutzen (per D-12)
           if (!networkMonitor.isOnline) {
             console.warn('401 waehrend Offline — Token wird behalten');
             return Promise.reject(error);
           }

           const refreshToken = getRefreshToken();
           if (!refreshToken) {
             // Kein Refresh-Token → direkt Re-Login
             await clearAuth();
             window.dispatchEvent(new CustomEvent('auth:relogin-required'));
             return Promise.reject(error);
           }

           if (isRefreshing) {
             // Anderer Request refresht bereits → warten
             return new Promise((resolve) => {
               addRefreshSubscriber((newToken: string) => {
                 originalRequest.headers.Authorization = `Bearer ${newToken}`;
                 resolve(api(originalRequest));
               });
             });
           }

           isRefreshing = true;

           try {
             // Refresh-Versuch (per D-05, D-13)
             const response = await axios.post(`${API_BASE_URL}/auth/refresh`, {
               refresh_token: refreshToken
             });

             const { token: newToken, refresh_token: newRefreshToken } = response.data;
             await setToken(newToken);
             await setRefreshToken(newRefreshToken);

             isRefreshing = false;
             onTokenRefreshed(newToken);

             // Original-Request mit neuem Token wiederholen
             originalRequest.headers.Authorization = `Bearer ${newToken}`;
             return api(originalRequest);
           } catch (refreshError) {
             isRefreshing = false;
             refreshSubscribers = [];

             // Refresh fehlgeschlagen → Re-Login-Dialog (per D-14, D-15)
             await clearAuth();
             window.dispatchEvent(new CustomEvent('auth:relogin-required'));
             return Promise.reject(refreshError);
           }
         }

         // Rate-Limit-Handling (unveraendert beibehalten)
         if (error.response?.status === 429) {
           const retryAfter = error.response.headers['retry-after'];
           const minutes = retryAfter ? Math.ceil(parseInt(retryAfter) / 60) : 15;
           error.rateLimitMessage = `Zu viele Versuche. Bitte warte ${minutes} Minuten.`;
           if (error.response.data?.error) {
             error.rateLimitMessage = error.response.data.error;
           }
           const isLoginRequest = error.config?.url?.includes('/login');
           if (!isLoginRequest) {
             window.dispatchEvent(new CustomEvent('rate-limit', {
               detail: { message: error.rateLimitMessage }
             }));
           }
         }

         return Promise.reject(error);
       }
       ```

    4. **WICHTIG**: `axios` muss direkt importiert werden (fuer den Refresh-Call ohne Interceptor):
       Am Anfang der Datei ist `axios` bereits importiert (`import axios from 'axios'`). Der Refresh-Call nutzt `axios.post` direkt (NICHT `api.post`), damit der Response-Interceptor nicht den Refresh selbst nochmal abfaengt.

    **auth.ts aendern** per D-18, D-19:

    1. **Import erweitern**:
       ```typescript
       import { getUser, setToken, setUser, setRefreshToken, clearAuth, getDeviceId, setDeviceId } from './tokenStore';
       ```

    2. **loginWithAutoDetection** — Refresh-Token speichern:
       ```typescript
       const { token, refresh_token, user } = response.data;
       if (!token || !user) throw new Error('Fehlender Token oder Benutzer');
       await setToken(token);
       if (refresh_token) await setRefreshToken(refresh_token);
       await setUser(user);
       ```

    3. **Re-Login-Dialog Event-Listener** — Neuer Export fuer App.tsx:
       KEIN neuer Export noetig. Das `auth:relogin-required` CustomEvent wird in App.tsx oder einem globalen Listener behandelt. Das Event dispatcht api.ts bereits.

    4. **Re-Login-Dialog** (per D-15, D-16): In api.ts wird `window.dispatchEvent(new CustomEvent('auth:relogin-required'))` gefeuert. Der Listener dafuer wird in **App.tsx** (oder einem globalen Layout) eingebaut:
       - Erstelle KEINEN separaten Dialog-Component — nutze `alertController` aus `@ionic/react`:
       ```typescript
       // In App.tsx oder AppShell — useEffect mit Event-Listener:
       useEffect(() => {
         const handler = () => {
           // IonAlert programmatisch zeigen
           const alert = document.createElement('ion-alert');
           alert.header = 'Sitzung abgelaufen';
           alert.message = 'Deine Sitzung ist abgelaufen. Bitte melde dich erneut an.';
           alert.buttons = [{
             text: 'Anmelden',
             handler: () => {
               window.location.href = '/';
             }
           }];
           alert.backdropDismiss = false;
           document.body.appendChild(alert);
           alert.present();
         };
         window.addEventListener('auth:relogin-required', handler);
         return () => window.removeEventListener('auth:relogin-required', handler);
       }, []);
       ```
       Finde die richtige Stelle: Suche nach dem bestehenden `rate-limit` Event-Listener in der App — der Re-Login-Listener kommt direkt daneben.

    NICHT aendern: logout-Funktion — clearAuth() loescht bereits den Refresh-Token (durch Task 1 Erweiterung). Der bestehende Push-Token-Removal-Code bleibt unveraendert.
  </action>
  <verify>
    <automated>grep -q "auth/refresh" frontend/src/services/api.ts && grep -q "isRefreshing" frontend/src/services/api.ts && grep -q "setRefreshToken" frontend/src/services/auth.ts && grep -q "auth:relogin-required" frontend/src/services/api.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "auth/refresh" frontend/src/services/api.ts findet Refresh-Endpoint-Aufruf
    - grep "isRefreshing" frontend/src/services/api.ts findet Race-Condition-Schutz
    - grep "setRefreshToken" frontend/src/services/auth.ts findet Token-Speicherung bei Login
    - grep "auth:relogin-required" frontend/src/services/api.ts findet Event-Dispatch
    - grep "onTokenRefreshed" frontend/src/services/api.ts findet Subscriber-Pattern
    - grep "axios.post" frontend/src/services/api.ts findet direkten axios-Call (nicht api.post) fuer Refresh
    - grep "Sitzung abgelaufen" findet Re-Login-Dialog-Text
  </acceptance_criteria>
  <done>api.ts versucht bei 401 automatisch Refresh. Parallele Requests warten auf laufenden Refresh. Login speichert Refresh-Token. Re-Login-Dialog erscheint bei abgelaufenem Refresh-Token. Offline-Verhalten bleibt unveraendert.</done>
</task>

</tasks>

<verification>
- tokenStore hat getRefreshToken/setRefreshToken/clearAuth mit Refresh-Token
- api.ts 401-Handler: Refresh vor Logout, Race-Condition-Schutz, Offline-Check
- auth.ts Login speichert refresh_token
- Re-Login-Dialog via IonAlert bei auth:relogin-required Event
- Kein harter Redirect auf / — Dialog mit "Anmelden" Button
- cd frontend && npx tsc --noEmit (TypeScript-Check)
</verification>

<success_criteria>
Frontend-seitig vollstaendiger Refresh-Flow: Automatischer Refresh bei 401, Race-Condition-sicher, Re-Login-Dialog nach 90 Tagen, Offline-Cache bleibt verfuegbar. Zusammen mit Plan 01 ist das Token-Refresh-System komplett.
</success_criteria>

<output>
After completion, create `.planning/phases/68-token-refresh-system/68-02-SUMMARY.md`
</output>
