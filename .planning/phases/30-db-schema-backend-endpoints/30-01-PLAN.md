---
phase: 30-db-schema-backend-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/jahrgaenge.js
  - backend/routes/settings.js
  - backend/routes/konfi.js
autonomous: true
requirements:
  - PKT-01
  - PKT-02
  - PKT-03
  - DSH-01

must_haves:
  truths:
    - "GET /api/jahrgaenge liefert gottesdienst_enabled, gemeinde_enabled, target_gottesdienst, target_gemeinde pro Jahrgang"
    - "PUT /api/jahrgaenge/:id akzeptiert und speichert Punkte-Typ-Toggles und Zielwerte"
    - "POST /api/jahrgaenge erstellt Jahrgang mit Defaults (beide enabled, target je 10)"
    - "GET /api/konfi/dashboard liefert point_config mit aktiven Punkte-Typen des Konfi-Jahrgangs"
    - "PUT /api/settings akzeptiert Dashboard-Widget-Toggle-Keys und speichert sie"
    - "GET /api/settings liefert Dashboard-Widget-Toggles als Booleans"
    - "target_gottesdienst und target_gemeinde sind aus Settings-Endpoint entfernt"
  artifacts:
    - path: "backend/routes/jahrgaenge.js"
      provides: "Punkte-Typ-Config CRUD auf Jahrgang-Ebene"
      contains: "gottesdienst_enabled"
    - path: "backend/routes/settings.js"
      provides: "Dashboard-Widget-Toggles UPSERT + alte target_*-Keys entfernt"
      contains: "dashboard_show_"
    - path: "backend/routes/konfi.js"
      provides: "Dashboard-Endpoint mit point_config und dashboard_config"
      contains: "point_config"
  key_links:
    - from: "backend/routes/konfi.js"
      to: "jahrgaenge table"
      via: "JOIN in dashboard query"
      pattern: "j\\.gottesdienst_enabled|j\\.gemeinde_enabled"
    - from: "backend/routes/konfi.js"
      to: "settings table"
      via: "SELECT dashboard_show_ keys"
      pattern: "dashboard_show_"
---

<objective>
DB-Schema um Punkte-Typ-Konfiguration auf Jahrgang-Ebene und Dashboard-Widget-Toggles erweitern. Backend-Endpoints anpassen: jahrgaenge CRUD mit neuen Spalten, Settings mit Dashboard-Keys, Dashboard-Endpoint liefert point_config und dashboard_config.

Purpose: Datengrundlage fuer v1.6 Punkte-Logik und Dashboard-Steuerung schaffen
Output: Erweiterte Backend-Endpoints die Config-Daten liefern und speichern
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-db-schema-backend-endpoints/30-CONTEXT.md

@backend/routes/jahrgaenge.js
@backend/routes/settings.js
@backend/routes/konfi.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jahrgaenge-Tabelle erweitern + CRUD anpassen</name>
  <files>backend/routes/jahrgaenge.js</files>
  <action>
1. Idempotente Migration in jahrgaenge.js hinzufuegen (analog zu ensureOrgColumn Pattern in settings.js):
   - Pruefen ob Spalten existieren via information_schema.columns
   - Falls nicht: ALTER TABLE jahrgaenge ADD COLUMN gottesdienst_enabled BOOLEAN DEFAULT true
   - ALTER TABLE jahrgaenge ADD COLUMN gemeinde_enabled BOOLEAN DEFAULT true
   - ALTER TABLE jahrgaenge ADD COLUMN target_gottesdienst INTEGER DEFAULT 10
   - ALTER TABLE jahrgaenge ADD COLUMN target_gemeinde INTEGER DEFAULT 10
   - Einmalige Datenmigration: Bestehende org-weite target_gottesdienst/target_gemeinde Werte aus settings-Tabelle in alle Jahrgaenge der jeweiligen Organisation uebertragen (UPDATE jahrgaenge SET target_gottesdienst = COALESCE((SELECT value::int FROM settings WHERE organization_id = jahrgaenge.organization_id AND key = 'target_gottesdienst'), 10))
   - Migration beim Laden ausfuehren (wie ensureOrgColumn)

2. GET / anpassen: SELECT * liefert die neuen Spalten automatisch mit

3. POST / anpassen: INSERT-Query um die 4 neuen Spalten erweitern. Werte aus req.body lesen, Defaults verwenden wenn nicht angegeben (gottesdienst_enabled=true, gemeinde_enabled=true, target_gottesdienst=10, target_gemeinde=10). RETURNING erweitern um alle Spalten.

4. PUT /:id anpassen: UPDATE-Query um die 4 neuen Spalten erweitern. Werte aus req.body lesen. Validierung: target-Werte muessen >= 0 sein (express-validator body() Regeln hinzufuegen).

5. Validierungsregeln erweitern:
   - validateCreateJahrgang: body('gottesdienst_enabled').optional().isBoolean(), body('gemeinde_enabled').optional().isBoolean(), body('target_gottesdienst').optional().isInt({min: 0}), body('target_gemeinde').optional().isInt({min: 0})
   - validateUpdateJahrgang: gleiche Regeln
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/api/jahrgaenge -H "Authorization: Bearer $TOKEN" | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(d);const r=j[0];if(!('gottesdienst_enabled' in r)||!('target_gottesdienst' in r))process.exit(1);console.log('OK')"</automated>
  </verify>
  <done>GET /api/jahrgaenge liefert gottesdienst_enabled, gemeinde_enabled, target_gottesdienst, target_gemeinde pro Jahrgang. POST erstellt mit Defaults, PUT aktualisiert alle 4 Werte.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Dashboard-Keys + Dashboard-Endpoint point_config</name>
  <files>backend/routes/settings.js, backend/routes/konfi.js</files>
  <action>
1. settings.js anpassen:
   - PUT-Endpoint: target_gottesdienst und target_gemeinde Handling KOMPLETT ENTFERNEN (aus req.body Destructuring, aus UPSERT-Bloecken, aus Validierungsregeln)
   - PUT-Endpoint: 5 neue Dashboard-Widget-Keys akzeptieren: dashboard_show_konfirmation, dashboard_show_events, dashboard_show_losung, dashboard_show_badges, dashboard_show_ranking. Fuer jeden Key: if (KEY !== undefined) { UPSERT mit String(value) }. Validierung: body('dashboard_show_konfirmation').optional().isBoolean() fuer alle 5.
   - GET-Endpoint: Im Parsing-Block die dashboard_show_* Keys als Boolean parsen (row.value === 'true' || row.value === '1'). target_gottesdienst und target_gemeinde aus dem parseInt-Block entfernen.

2. konfi.js Dashboard-Endpoint anpassen (GET /dashboard, ca. Zeile 32):
   - Nach dem Laden der Konfi-Daten (konfiQuery): point_config Objekt bauen aus dem bereits gejointen jahrgaenge-Record. Die Jahrgang-Felder j.gottesdienst_enabled, j.gemeinde_enabled, j.target_gottesdienst, j.target_gemeinde sind durch den bestehenden JOIN bereits verfuegbar.
   - konfiQuery erweitern um: j.gottesdienst_enabled, j.gemeinde_enabled, j.target_gottesdienst, j.target_gemeinde
   - Dashboard-Config aus Settings laden: SELECT key, value FROM settings WHERE organization_id = $1 AND key LIKE 'dashboard_show_%'. Als Object mit Boolean-Werten parsen, fehlende Keys defaulten auf true.
   - Im Response-Objekt (ca. Zeile 227) zwei neue Felder hinzufuegen:
     point_config: { gottesdienst_enabled: boolean, gemeinde_enabled: boolean, target_gottesdienst: number, target_gemeinde: number }
     dashboard_config: { show_konfirmation: boolean, show_events: boolean, show_losung: boolean, show_badges: boolean, show_ranking: boolean }
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/api/settings -H "Authorization: Bearer $TOKEN" | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));if('target_gottesdienst' in d)process.exit(1);console.log('OK: target removed from settings')"</automated>
  </verify>
  <done>Settings-Endpoint akzeptiert/liefert Dashboard-Widget-Toggles, alte target_*-Keys entfernt. Dashboard-Endpoint liefert point_config und dashboard_config.</done>
</task>

</tasks>

<verification>
1. GET /api/jahrgaenge liefert alle 4 neuen Felder pro Jahrgang
2. PUT /api/jahrgaenge/:id mit {gottesdienst_enabled: false} speichert korrekt
3. POST /api/jahrgaenge mit leeren optionalen Feldern setzt Defaults
4. GET /api/settings liefert keine target_gottesdienst/target_gemeinde mehr
5. PUT /api/settings mit {dashboard_show_losung: false} speichert korrekt
6. GET /api/konfi/dashboard liefert point_config und dashboard_config Objekte
</verification>

<success_criteria>
- Jahrgaenge-Tabelle hat 4 neue Spalten mit korrekten Defaults
- Bestehende Org-Zielwerte in Jahrgaenge migriert
- Dashboard-Endpoint liefert Punkte-Config aus Jahrgang und Dashboard-Config aus Settings
- Settings-Endpoint hat keine target_gottesdienst/target_gemeinde mehr
- Settings-Endpoint hat 5 neue dashboard_show_* Keys
</success_criteria>

<output>
After completion, create `.planning/phases/30-db-schema-backend-endpoints/30-01-SUMMARY.md`
</output>
