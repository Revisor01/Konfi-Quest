---
phase: 100-admin-zertifikate-material-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminMaterialPage.tsx
  - frontend/src/components/admin/modals/MaterialFormModal.tsx
autonomous: true
requirements: [AMA-01, AMA-02, AMA-03, AMA-04]

must_haves:
  truths:
    - "Jahrgangs-Auswahl ist ein IonSelect Dropdown statt Chip-Leiste"
    - "Suchleiste folgt dem Chat-Pattern mit ios26-searchbar-classic"
    - "Ohne-Event-Segment zeigt korrekten Text wenn Material vorhanden"
    - "Datei-auswaehlen Button hat korrekten Abstand und ist zentriert"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminMaterialPage.tsx"
      provides: "IonSelect Dropdown fuer Jahrgangs-Filter"
      contains: "IonSelect"
    - path: "frontend/src/components/admin/modals/MaterialFormModal.tsx"
      provides: "Korrekter Datei-Button-Stil"
      contains: "expand=\"block\""
  key_links:
    - from: "AdminMaterialPage.tsx"
      to: "IonSelect"
      via: "interface=\"popover\""
      pattern: "IonSelect.*interface.*popover"
---

<objective>
Material-Verwaltung verbessern: Jahrgangs-Chips durch IonSelect Dropdown ersetzen, Ohne-Event-Text korrigieren, Datei-Button-Styling fixen.

Purpose: Die Chip-Leiste skaliert nicht gut bei vielen Jahrgaengen und ist inkonsistent mit anderen Filtern. Der Datei-Button braucht besseres Spacing.
Output: Verbesserte AdminMaterialPage.tsx + MaterialFormModal.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/admin/pages/AdminMaterialPage.tsx
@frontend/src/components/admin/modals/MaterialFormModal.tsx
</context>

<interfaces>
<!-- AdminMaterialPage.tsx aktuelle Jahrgangs-Filter (Zeile 231-258): -->
<!-- Horizontale IonChip-Leiste mit onClick Handler fuer activeJahrgangId -->
<!-- State: activeJahrgangId: number | undefined -->
<!-- Daten: jahrgaenge: { id: number; name: string }[] -->

<!-- Suchleiste (Zeile 223-229): bereits ios26-searchbar-classic vorhanden -->

<!-- Ohne-Event EmptyState (Zeile 291-294): -->
<!-- "Erstelle dein erstes Material mit dem + Button" - muss kontextabhaengig sein -->

<!-- MaterialFormModal.tsx Datei-Button (Zeile 605-612): -->
<!-- IonButton expand="block" fill="outline" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Jahrgangs-Chips durch IonSelect Dropdown ersetzen + Ohne-Event-Text</name>
  <files>frontend/src/components/admin/pages/AdminMaterialPage.tsx</files>
  <read_first>
    frontend/src/components/admin/pages/AdminMaterialPage.tsx
  </read_first>
  <action>
    Per AMA-01: Die Chip-basierte Jahrgangs-Filterung (Zeile ~231-258) durch ein IonSelect mit interface="popover" ersetzen.

    1. Import IonSelect und IonSelectOption hinzufuegen (aus @ionic/react)

    2. Den gesamten div-Block mit IonChips (Zeile 231-258, der Block mit overflowX: 'auto' und den Jahrgang-Chips) ersetzen durch:
    ```tsx
    {(jahrgaenge || []).length > 0 && (
      <IonItem lines="none" style={{ '--background': 'transparent' }}>
        <IonSelect
          interface="popover"
          label="Jahrgang"
          labelPlacement="stacked"
          value={activeJahrgangId ?? 'alle'}
          onIonChange={(e) => setActiveJahrgangId(e.detail.value === 'alle' ? undefined : e.detail.value)}
          placeholder="Alle Jahrgaenge"
        >
          <IonSelectOption value="alle">Alle Jahrgaenge</IonSelectOption>
          {(jahrgaenge || []).map(jg => (
            <IonSelectOption key={jg.id} value={jg.id}>{jg.name}</IonSelectOption>
          ))}
        </IonSelect>
      </IonItem>
    )}
    ```

    3. IonChip Import entfernen (wenn nicht anderweitig verwendet)

    Per AMA-02: Suchleiste behaelt ios26-searchbar-classic (bereits vorhanden, nichts aendern).

    Per AMA-03: EmptyState-Text fuer "Ohne Event"-Segment anpassen. In der filteredMaterials.length === 0 Sektion (Zeile ~290-296) den Text kontextabhaengig machen:
    - Wenn segment === 'ohne_event' UND (materials || []).length > 0 (es gibt Material, aber keines ohne Event):
      message = "Alle Materialien sind einem Event zugeordnet"
    - Wenn segment === 'mit_event' UND (materials || []).length > 0:
      message = "Kein Material ist einem Event zugeordnet"
    - Default (Segment 'alle' oder tatsaechlich kein Material):
      message = "Erstelle dein erstes Material mit dem + Button" (wie bisher)

    Anpassung des EmptyState-Blocks:
    ```tsx
    <EmptyState
      icon={documentOutline}
      title="Keine Materialien"
      message={
        segment === 'ohne_event' && (materials || []).length > 0
          ? 'Alle Materialien sind einem Event zugeordnet'
          : segment === 'mit_event' && (materials || []).length > 0
            ? 'Kein Material ist einem Event zugeordnet'
            : 'Erstelle dein erstes Material mit dem + Button'
      }
      iconColor="#d97706"
    />
    ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "IonSelect" frontend/src/components/admin/pages/AdminMaterialPage.tsx && grep "interface=\"popover\"" frontend/src/components/admin/pages/AdminMaterialPage.tsx && grep "Alle Materialien sind einem Event zugeordnet" frontend/src/components/admin/pages/AdminMaterialPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IonSelect mit interface="popover" vorhanden
    - IonSelectOption fuer "Alle Jahrgaenge" und dynamische Jahrgaenge
    - Alte IonChip-Leiste fuer Jahrgaenge ENTFERNT
    - EmptyState zeigt kontextabhaengigen Text je nach Segment
    - "Alle Materialien sind einem Event zugeordnet" als String vorhanden
  </acceptance_criteria>
  <done>Jahrgangs-Filter ist ein Dropdown-Popover und Ohne-Event zeigt korrekten Text</done>
</task>

<task type="auto">
  <name>Task 2: MaterialFormModal Datei-Button Styling fixen</name>
  <files>frontend/src/components/admin/modals/MaterialFormModal.tsx</files>
  <read_first>
    frontend/src/components/admin/modals/MaterialFormModal.tsx
  </read_first>
  <action>
    Per AMA-04: Den "Dateien auswaehlen" Button (Zeile ~605-612) styling-maessig anpassen.

    Aktuell:
    ```tsx
    <IonButton expand="block" fill="outline" onClick={...}>
      <IonIcon icon={cloudUploadOutline} slot="start" />
      Dateien auswaehlen
    </IonButton>
    ```

    Aendern zu (per User-Entscheidung: expand="block" fill="solid" mit Primary-Farbe, marginTop 16px, padding 0 16px):
    ```tsx
    <IonButton
      expand="block"
      fill="solid"
      onClick={() => fileInputRef.current?.click()}
      style={{
        '--background': 'var(--ion-color-primary)',
        marginTop: '16px',
        padding: '0 16px'
      }}
    >
      <IonIcon icon={cloudUploadOutline} slot="start" />
      Datei auswaehlen
    </IonButton>
    ```

    Beachte: Text aendern von "Dateien auswaehlen" zu "Datei auswaehlen" (Singular, da der Input multiple unterstuetzt und man mehrfach klicken kann).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "fill=\"solid\"" frontend/src/components/admin/modals/MaterialFormModal.tsx && grep "marginTop" frontend/src/components/admin/modals/MaterialFormModal.tsx && grep "Datei auswählen" frontend/src/components/admin/modals/MaterialFormModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IonButton hat fill="solid" statt fill="outline"
    - Style enthaelt marginTop: '16px'
    - Style enthaelt '--background': 'var(--ion-color-primary)'
    - Text lautet "Datei auswaehlen" (Singular mit echtem Umlaut: "Datei auswählen")
  </acceptance_criteria>
  <done>Datei-Button hat korrekten Abstand, Zentrierung und Solid-Stil</done>
</task>

</tasks>

<verification>
- IonSelect mit interface="popover" in AdminMaterialPage.tsx
- Keine IonChip-Jahrgangs-Leiste mehr
- EmptyState zeigt kontextabhaengigen Text
- MaterialFormModal Button ist fill="solid" mit marginTop
- Frontend kompiliert: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Jahrgangs-Auswahl ist ein Dropdown-Popover mit Suche (AMA-01)
- Suchleiste folgt Chat-Pattern (AMA-02, bereits vorhanden)
- "Ohne Event" Tab zeigt korrekten Text (AMA-03)
- "Datei auswaehlen" Button hat korrekten Abstand und Zentrierung (AMA-04)
</success_criteria>

<output>
After completion, create `.planning/phases/100-admin-zertifikate-material-dashboard/100-02-SUMMARY.md`
</output>
