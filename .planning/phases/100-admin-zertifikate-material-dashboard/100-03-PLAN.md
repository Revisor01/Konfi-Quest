---
phase: 100-admin-zertifikate-material-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - backend/routes/settings.js
autonomous: true
requirements: [ADA-01, ADA-02]

must_haves:
  truths:
    - "Dashboard-Sektionen im Admin sind per Drag-and-Drop sortierbar"
    - "Konfi-Dashboard rendert Sektionen in der vom Admin gespeicherten Reihenfolge"
    - "Teamer-Dashboard rendert Sektionen in der vom Admin gespeicherten Reihenfolge"
    - "Default-Reihenfolge ist bei Admin und Konfi identisch"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx"
      provides: "IonReorderGroup fuer Dashboard-Sortierung"
      contains: "IonReorderGroup"
    - path: "backend/routes/settings.js"
      provides: "section_order Speicherung"
      contains: "section_order"
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Dynamische Sektions-Reihenfolge"
      contains: "section_order"
  key_links:
    - from: "AdminDashboardSettingsPage.tsx"
      to: "backend/routes/settings.js"
      via: "PUT /settings mit section_order"
      pattern: "section_order"
    - from: "DashboardView.tsx"
      to: "dashboard_config"
      via: "section_order Array"
      pattern: "section_order"
---

<objective>
Dashboard-Sektionen sortierbar machen: Admin kann per IonReorder die Reihenfolge der Konfi- und Teamer-Dashboard-Sektionen festlegen, die dann in den jeweiligen Dashboards angewendet wird.

Purpose: Admins wollen steuern, welche Inhalte Konfis und Teamer:innen zuerst sehen. Gleichzeitig soll die Default-Reihenfolge konsistent sein.
Output: Sortierbare Admin-Settings + dynamisches Rendering in Konfi/Teamer-Dashboards + Backend-Persistenz
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
@frontend/src/components/konfi/views/DashboardView.tsx
@frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
@frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
@backend/routes/settings.js
</context>

<interfaces>
<!-- AdminDashboardSettingsPage.tsx: -->
<!-- DashboardConfig: { show_konfirmation, show_events, show_losung, show_badges, show_ranking } -->
<!-- TeamerDashboardConfig: { show_zertifikate, show_events, show_badges, show_losung } -->
<!-- handleDashboardToggle / handleTeamerDashboardToggle speichern einzelne Keys via PUT /settings -->

<!-- DashboardView.tsx Sektions-Reihenfolge (aktuell hardcoded Zeile 316-580): -->
<!-- 1. Konfirmation, 2. Events, 3. Losung, 4. Badges, 5. Ranking -->

<!-- Backend settings.js: -->
<!-- GET /settings liefert alle Key-Value-Paare aus settings-Tabelle -->
<!-- PUT /settings akzeptiert dashboard_show_* Booleans -->
<!-- settings-Tabelle: key/value Paare, organization_id -->

<!-- konfi.js dashboard-Route liefert dashboard_config Objekt an Frontend -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend section_order + Admin IonReorderGroup</name>
  <files>
    backend/routes/settings.js
    frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
  </files>
  <read_first>
    backend/routes/settings.js
    frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
  </read_first>
  <action>
    **Backend (settings.js):**

    1. In der PUT /settings Validierung hinzufuegen:
    ```
    body('dashboard_section_order').optional().isJSON().withMessage('Section-Order muss JSON sein'),
    body('teamer_dashboard_section_order').optional().isJSON().withMessage('Section-Order muss JSON sein'),
    ```

    2. Im PUT-Handler: `dashboard_section_order` und `teamer_dashboard_section_order` aus req.body extrahieren. Fuer jeden Wert, wenn vorhanden, als String in die settings-Tabelle upserten (Key: 'dashboard_section_order' / 'teamer_dashboard_section_order', Value: der JSON-String).

    3. Im GET-Handler: Wenn der Key 'dashboard_section_order' oder 'teamer_dashboard_section_order' ist, den Value als JSON parsen (try/catch mit Fallback auf Default-Array).

    Default-Reihenfolgen:
    - Konfi: ["konfirmation", "events", "losung", "badges", "ranking"]
    - Teamer: ["zertifikate", "events", "badges", "losung"]

    4. In der konfi.js Dashboard-Route: dashboard_section_order aus settings laden und im dashboard_config Objekt mitliefern:
    ```
    dashboard_config: { ...bisherige_felder, section_order: parsedOrder || DEFAULT_KONFI_ORDER }
    ```

    **Frontend (AdminDashboardSettingsPage.tsx):**

    1. Imports hinzufuegen: `IonReorder, IonReorderGroup` aus '@ionic/react', `reorderThreeOutline` aus 'ionicons/icons'

    2. State fuer Reihenfolge:
    ```
    const [konfiOrder, setKonfiOrder] = useState<string[]>(['konfirmation', 'events', 'losung', 'badges', 'ranking']);
    const [teamerOrder, setTeamerOrder] = useState<string[]>(['zertifikate', 'events', 'badges', 'losung']);
    ```

    3. In der onSuccess Callback des useOfflineQuery: section_order aus data laden:
    ```
    setKonfiOrder(data.dashboard_section_order || ['konfirmation', 'events', 'losung', 'badges', 'ranking']);
    setTeamerOrder(data.teamer_dashboard_section_order || ['zertifikate', 'events', 'badges', 'losung']);
    ```

    4. Label-Mapping fuer Anzeige:
    ```
    const KONFI_LABELS: Record<string, string> = {
      konfirmation: 'Konfirmations-Countdown',
      events: 'Events',
      losung: 'Tageslosung',
      badges: 'Badges',
      ranking: 'Ranking'
    };
    const TEAMER_LABELS: Record<string, string> = {
      zertifikate: 'Zertifikate',
      events: 'Events',
      badges: 'Badges',
      losung: 'Tageslosung'
    };
    ```

    5. Bestehende IonList mit IonToggle-Items umbauen: Jedes IonItem erhaelt ein IonReorder am Ende (slot="end" NACH dem IonToggle). Die Items werden dynamisch aus dem Order-Array gerendert statt hardcoded.

    Konfi-Dashboard-Bereich (im Segment 'konfi'):
    ```tsx
    <IonReorderGroup disabled={false} onIonItemReorder={(e) => {
      const newOrder = [...konfiOrder];
      const [moved] = newOrder.splice(e.detail.from, 1);
      newOrder.splice(e.detail.to, 0, moved);
      setKonfiOrder(newOrder);
      e.detail.complete();
      // Speichern
      handleSaveOrder('dashboard_section_order', JSON.stringify(newOrder));
    }}>
      {konfiOrder.map((key, index) => (
        <IonItem key={key} lines={index < konfiOrder.length - 1 ? 'full' : 'none'} style={{ '--background': 'transparent' }}>
          <IonLabel>{KONFI_LABELS[key]}</IonLabel>
          <IonToggle
            slot="end"
            checked={dashboardConfig[`show_${key}` as keyof DashboardConfig]}
            onIonChange={(e) => handleDashboardToggle(`show_${key}` as keyof DashboardConfig, e.detail.checked)}
          />
          <IonReorder slot="end" />
        </IonItem>
      ))}
    </IonReorderGroup>
    ```

    Gleiches Pattern fuer Teamer-Segment mit teamerOrder und TEAMER_LABELS.

    6. handleSaveOrder Funktion:
    ```
    const handleSaveOrder = async (key: string, value: string) => {
      if (!networkMonitor.isOnline) {
        writeQueue.enqueue({
          method: 'PUT', url: '/settings',
          body: { [key]: value },
          maxRetries: 3, hasFileUpload: false,
          metadata: { type: 'fire-and-forget', clientId: `order-${key}-${Date.now()}`, label: 'Dashboard-Reihenfolge' }
        });
        return;
      }
      try {
        await api.put('/settings', { [key]: value });
      } catch {
        setError('Fehler beim Speichern der Reihenfolge');
      }
    };
    ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "IonReorderGroup" frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx && grep "section_order" backend/routes/settings.js && grep "dashboard_section_order" frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IonReorderGroup in AdminDashboardSettingsPage.tsx vorhanden
    - IonReorder auf jedem Toggle-Item
    - dashboard_section_order und teamer_dashboard_section_order in settings.js validiert und gespeichert
    - GET /settings liefert geparste section_order
    - Konfi-Default-Order: ["konfirmation", "events", "losung", "badges", "ranking"]
    - handleSaveOrder speichert Reihenfolge via PUT /settings
  </acceptance_criteria>
  <done>Admin kann Dashboard-Sektionen per Drag-and-Drop sortieren, Reihenfolge wird gespeichert</done>
</task>

<task type="auto">
  <name>Task 2: Konfi- und Teamer-Dashboard dynamische Sektions-Reihenfolge</name>
  <files>
    frontend/src/components/konfi/views/DashboardView.tsx
    frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  </files>
  <read_first>
    frontend/src/components/konfi/views/DashboardView.tsx
    frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    backend/routes/settings.js
  </read_first>
  <action>
    Per ADA-01 + ADA-02: Die Dashboards rendern Sektionen dynamisch basierend auf section_order aus der dashboard_config.

    **KonfiDashboardPage.tsx:**
    - dashboardConfig wird bereits aus dashboardData.dashboard_config gelesen
    - section_order Feld hinzufuegen: `const sectionOrder: string[] = dashboardData.dashboard_config?.section_order || ['konfirmation', 'events', 'losung', 'badges', 'ranking'];`
    - An DashboardView als Prop weiterreichen: `sectionOrder={sectionOrder}`

    **DashboardView.tsx:**
    1. Props-Interface erweitern: `sectionOrder?: string[];`

    2. Default-Reihenfolge als Konstante:
    ```
    const DEFAULT_ORDER = ['konfirmation', 'events', 'losung', 'badges', 'ranking'];
    ```

    3. Die aktuell hardcoded Sektionen (Konfirmation Zeile ~316, Events Zeile ~368, Losung Zeile ~413, Badges Zeile ~443, Ranking Zeile ~567) in ein Render-Mapping umwandeln:

    ```tsx
    const order = sectionOrder || DEFAULT_ORDER;

    const sectionRenderers: Record<string, () => React.ReactNode> = {
      konfirmation: () => (
        dashboardConfig?.show_konfirmation !== false && (...) ? (
          <div className="app-dashboard-section app-dashboard-section--konfirmation" key="konfirmation">
            {/* ... bisheriger Konfirmation-Block ... */}
          </div>
        ) : null
      ),
      events: () => (
        dashboardConfig?.show_events !== false ? (
          <div className="app-dashboard-section app-dashboard-section--events" key="events">
            {/* ... bisheriger Events-Block ... */}
          </div>
        ) : null
      ),
      losung: () => ( /* ... bisheriger Losung-Block ... */ ),
      badges: () => ( /* ... bisheriger Badges-Block ... */ ),
      ranking: () => ( /* ... bisheriger Ranking-Block ... */ ),
    };
    ```

    4. Im JSX die hardcoded Sektionen durch dynamisches Rendering ersetzen:
    ```tsx
    {order.map(key => sectionRenderers[key]?.())}
    ```

    WICHTIG: Der Header-Block (Level, Stats, Progress) bleibt IMMER oben und wird NICHT sortiert. Nur die Sektionen unterhalb (Konfirmation, Events, Losung, Badges, Ranking) werden dynamisch geordnet.

    **TeamerDashboardPage.tsx:**
    - Analog: section_order aus Settings laden (falls ueber API verfuegbar) oder aus einem separaten Fetch
    - Da das Teamer-Dashboard die Settings anders laedt, pruefen wie teamer_dashboard_section_order an das Frontend kommt
    - Falls die Teamer-Dashboard-Route die section_order noch nicht mitliefert: In der teamer.js Route die Settings laden und teamer_dashboard_section_order im Response mitgeben
    - Sektions-Rendering analog zu DashboardView dynamisch machen mit:
      Default-Order: ["zertifikate", "events", "badges", "losung"]

    Per ADA-01: Die Default-Reihenfolge im AdminDashboardSettingsPage muss mit der Render-Reihenfolge im Konfi-Dashboard uebereinstimmen. Beide verwenden: konfirmation, events, losung, badges, ranking.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "section_order\|sectionOrder\|DEFAULT_ORDER" frontend/src/components/konfi/views/DashboardView.tsx && grep "section_order\|sectionOrder" frontend/src/components/konfi/pages/KonfiDashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DashboardView.tsx hat sectionOrder Prop
    - DashboardView.tsx rendert Sektionen dynamisch basierend auf Order-Array
    - Default-Order ist ["konfirmation", "events", "losung", "badges", "ranking"]
    - Header-Block (Level, Stats) bleibt immer oben
    - KonfiDashboardPage.tsx liest section_order aus dashboard_config
    - TeamerDashboardPage.tsx unterstuetzt teamer_dashboard_section_order
  </acceptance_criteria>
  <done>Konfi- und Teamer-Dashboard rendern Sektionen in der vom Admin festgelegten Reihenfolge</done>
</task>

</tasks>

<verification>
- IonReorderGroup in AdminDashboardSettingsPage funktional
- section_order wird in Settings-Tabelle gespeichert und gelesen
- Konfi-Dashboard rendert Sektionen dynamisch
- Teamer-Dashboard rendert Sektionen dynamisch
- Default-Reihenfolgen stimmen zwischen Admin-Settings und Dashboard ueberein
- Frontend kompiliert: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Dashboard-Reihenfolge bei Admin und Konfi gleich (ADA-01)
- Dashboard-Sektionen sortierbar durch Admin (ADA-02)
- Aenderungen werden persistiert und beim naechsten Laden angewendet
</success_criteria>

<output>
After completion, create `.planning/phases/100-admin-zertifikate-material-dashboard/100-03-SUMMARY.md`
</output>
