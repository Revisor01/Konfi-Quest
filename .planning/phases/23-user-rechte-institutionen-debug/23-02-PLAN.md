---
phase: 23-user-rechte-institutionen-debug
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/activities.js
  - backend/routes/events.js
  - backend/routes/organizations.js
autonomous: true
requirements:
  - USR-02
  - USR-03

must_haves:
  truths:
    - "Teamer sehen in der Konfis-Liste und bei Aktivitaets-Zuweisungen nur Konfis ihrer zugewiesenen Jahrgaenge"
    - "Events-Liste fuer Admins/Teamer wird nach Organisations-Zugehoerigkeit gefiltert"
    - "Loeschen einer Organisation entfernt alle abhaengigen Daten sauber und vollstaendig"
  artifacts:
    - path: "backend/routes/activities.js"
      provides: "Jahrgang-gefilterte Konfis-Liste fuer Aktivitaets-Zuweisungen"
      contains: "assigned_jahrgaenge"
    - path: "backend/routes/events.js"
      provides: "Jahrgang-gefilterte Teilnehmer-Ansicht"
      contains: "jahrgang"
    - path: "backend/routes/organizations.js"
      provides: "Vollstaendige CASCADE-Loeschung aller abhaengigen Daten"
      contains: "DELETE FROM"
  key_links:
    - from: "backend/routes/activities.js"
      to: "req.user.assigned_jahrgaenge"
      via: "Jahrgang-Filter bei Konfi-Listen"
      pattern: "assigned_jahrgaenge"
    - from: "backend/routes/organizations.js"
      to: "Alle abhaengigen Tabellen"
      via: "Explizite DELETE-Kette in Transaktion"
      pattern: "client.query.*DELETE"
---

<objective>
Jahrgang-basierte Filterung in Activities/Events absichern und Org-Loeschung mit vollstaendiger CASCADE-Kette implementieren.

Purpose: USR-02 stellt sicher dass Teamer nur Konfis sehen die ihren zugewiesenen Jahrgaengen angehoeren. USR-03 verhindert verwaiste Daten nach Org-Loeschung.
Output: Konsistente Jahrgang-Filterung in allen relevanten Routes, sichere Org-Loeschkette.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/middleware/rbac.js
@backend/routes/activities.js
@backend/routes/events.js
@backend/routes/organizations.js
@backend/routes/konfi-managment.js

<interfaces>
<!-- Aus backend/middleware/rbac.js - Verfuegbare Jahrgang-Filter-Utility -->
```javascript
// filterByJahrgangAccess(req) - Gibt {where, params} zurueck
// Fuer Teamer: filtert nach assigned_jahrgaenge mit can_view
// Fuer org_admin/admin: filtert nur nach organization_id

// req.user.assigned_jahrgaenge enthält:
// [{ id: number, name: string, can_view: boolean, can_edit: boolean }]
```

<!-- Referenz-Implementation aus konfi-managment.js Zeile 48-66 -->
```javascript
// Korrekte Jahrgang-Filterung fuer Teamer:
if (!req.user.is_super_admin && req.user.role_name !== 'org_admin') {
    const viewableJahrgaenge = req.user.assigned_jahrgaenge
        .filter(j => j.can_view)
        .map(j => j.id);
    if (viewableJahrgaenge.length === 0) {
        return res.json([]);
    }
    const placeholders = viewableJahrgaenge.map(() => `$${placeholderIndex++}`).join(',');
    jahrgangFilter = `AND j.id IN (${placeholders})`;
    params.push(...viewableJahrgaenge);
}
```

<!-- Aus RBAC-SCHEMA.md - FK Constraints OHNE CASCADE auf organizations -->
```sql
-- roles: FOREIGN KEY (organization_id) REFERENCES organizations (id)  -- KEIN CASCADE!
-- users: FOREIGN KEY (organization_id) REFERENCES organizations (id)  -- KEIN CASCADE!
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jahrgang-Filterung in Activities und Events absichern</name>
  <files>backend/routes/activities.js, backend/routes/events.js</files>
  <action>
**activities.js** - Alle Endpunkte die Konfis auflisten oder Aktivitaeten zuweisen pruefen:

1. Suche nach Endpunkten die Konfis laden (z.B. fuer Aktivitaets-Zuweisung). Falls ein GET-Endpunkt Konfis auflistet, muss er nach Jahrgang filtern analog zu konfi-managment.js:
   - Pruefe ob `req.user.role_name` ein Teamer ist
   - Falls ja: filtere nach `req.user.assigned_jahrgaenge` mit `can_view`
   - Falls `viewableJahrgaenge.length === 0`: leeres Array zurueckgeben

2. Bei POST /assign (Aktivitaet einem Konfi zuweisen): Pruefe ob der Ziel-Konfi in einem Jahrgang ist den der Teamer sehen darf. Dazu VOR dem INSERT pruefen:
   ```javascript
   // Jahrgang-Zugriff pruefen fuer Teamer
   if (req.user.role_name === 'teamer') {
     const { rows: [konfiProfile] } = await db.query(
       'SELECT jahrgang_id FROM konfi_profiles WHERE user_id = $1', [konfiId]
     );
     if (!konfiProfile) {
       return res.status(404).json({ error: 'Konfi nicht gefunden' });
     }
     const hasAccess = req.user.assigned_jahrgaenge.some(
       j => j.id === konfiProfile.jahrgang_id && j.can_view
     );
     if (!hasAccess) {
       return res.status(403).json({ error: 'Kein Zugriff auf diesen Konfi' });
     }
   }
   ```

3. Bei Activity-Requests (GET /requests): Pruefe ob Requests-Endpunkte bereits nach Organization gefiltert sind. Falls Teamer Requests sehen koennen, muessen diese nach Jahrgang gefiltert sein.

**events.js** - Teilnehmer-Ansicht und Event-Listen pruefen:

1. GET /:id/participants (Teilnehmer-Liste eines Events): Falls Teamer diese sehen koennen, ist die Filterung nach Jahrgang hier weniger kritisch (Events sind fuer alle sichtbar). Aber die Teilnehmer-Anzeige sollte zeigen welchem Jahrgang sie angehoeren (ist bereits der Fall - jahrgang_name wird geladen).

2. Events-Liste (GET /): Pruefe ob `organization_id` Filter vorhanden ist (sollte durch Middleware sichergestellt sein). Falls Events jahrgang-spezifisch sind (event_jahrgang_assignments), pruefen ob Teamer nur Events ihrer Jahrgaenge sehen. Suche nach der GET / Route und pruefe ob fuer Teamer ein Filter eingebaut ist:
   ```javascript
   // Fuer Teamer: nur Events anzeigen die mindestens einem zugewiesenen Jahrgang zugeordnet sind
   // ODER die keinem Jahrgang zugeordnet sind (allgemeine Events)
   if (req.user.role_name === 'teamer' && req.user.assigned_jahrgaenge.length > 0) {
     const viewableJahrgaenge = req.user.assigned_jahrgaenge
       .filter(j => j.can_view)
       .map(j => j.id);
     // Fuege HAVING-Clause oder Subquery hinzu die Events filtert
   }
   ```

WICHTIG: org_admin und admin Rollen sollen ALLE Konfis/Events ihrer Organisation sehen -- nur Teamer werden nach Jahrgang gefiltert. Bestehende Queries die bereits `organization_id` filtern sind korrekt fuer org_admin/admin.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "assigned_jahrgaenge\|role_name.*teamer\|viewableJahrgaenge" backend/routes/activities.js | xargs test 0 -lt && echo "PASS: Jahrgang-Filter in activities.js vorhanden"</automated>
  </verify>
  <done>Teamer sehen bei Aktivitaets-Zuweisungen und Requests nur Konfis/Daten ihrer zugewiesenen Jahrgaenge. org_admin und admin sehen weiterhin alles in ihrer Organisation.</done>
</task>

<task type="auto">
  <name>Task 2: Org-Loeschung mit vollstaendiger CASCADE-Kette absichern</name>
  <files>backend/routes/organizations.js</files>
  <action>
Die aktuelle DELETE /:id Route (Zeile 318-346) prueft nur ob Konfis existieren und loescht dann die Organisation mit der Annahme dass CASCADE greift. Laut RBAC-SCHEMA.md haben roles und users aber KEIN ON DELETE CASCADE auf organizations.

Ersetze die DELETE-Route durch eine transaktionssichere Loeschkette die ALLE abhaengigen Daten explizit entfernt:

```javascript
router.delete('/:id', rbacVerifier, requireSuperAdmin, validateOrgId, async (req, res) => {
  const client = await db.connect();
  try {
    const { id } = req.params;
    await client.query('BEGIN');

    // Pruefen ob Organisation existiert
    const { rows: [org] } = await client.query('SELECT id FROM organizations WHERE id = $1', [id]);
    if (!org) {
      await client.query('ROLLBACK');
      return res.status(404).json({ error: 'Organisation nicht gefunden' });
    }

    // Alle abhaengigen Daten in korrekter Reihenfolge loeschen
    // 1. Chat-System (haengt von users ab)
    await client.query('DELETE FROM chat_poll_votes WHERE poll_id IN (SELECT id FROM chat_polls WHERE room_id IN (SELECT id FROM chat_rooms WHERE organization_id = $1))', [id]);
    await client.query('DELETE FROM chat_polls WHERE room_id IN (SELECT id FROM chat_rooms WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM chat_read_status WHERE room_id IN (SELECT id FROM chat_rooms WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM chat_messages WHERE room_id IN (SELECT id FROM chat_rooms WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM chat_participants WHERE room_id IN (SELECT id FROM chat_rooms WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM chat_rooms WHERE organization_id = $1', [id]);

    // 2. Event-Daten
    await client.query('DELETE FROM event_points WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM event_bookings WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM event_timeslots WHERE event_id IN (SELECT id FROM events WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM event_category_assignments WHERE event_id IN (SELECT id FROM events WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM event_jahrgang_assignments WHERE event_id IN (SELECT id FROM events WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM events WHERE organization_id = $1', [id]);

    // 3. Konfi-Daten
    await client.query('DELETE FROM konfi_activities WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM bonus_points WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM konfi_badges WHERE konfi_id IN (SELECT user_id FROM konfi_profiles WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM activity_requests WHERE organization_id = $1', [id]);

    // 4. Notifications und Push-Tokens
    await client.query('DELETE FROM notifications WHERE user_id IN (SELECT id FROM users WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM push_tokens WHERE user_id IN (SELECT id FROM users WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM password_resets WHERE user_id IN (SELECT id FROM users WHERE organization_id = $1)', [id]);

    // 5. User-Zuweisungen
    await client.query('DELETE FROM user_jahrgang_assignments WHERE user_id IN (SELECT id FROM users WHERE organization_id = $1)', [id]);

    // 6. Konfi-Profile
    await client.query('DELETE FROM konfi_profiles WHERE organization_id = $1', [id]);

    // 7. Users
    await client.query('DELETE FROM users WHERE organization_id = $1', [id]);

    // 8. Aktivitaeten und Kategorien
    await client.query('DELETE FROM activity_category_assignments WHERE activity_id IN (SELECT id FROM activities WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM activities WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM categories WHERE organization_id = $1', [id]);

    // 9. Badges, Levels, Settings
    await client.query('DELETE FROM custom_badges WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM levels WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM settings WHERE organization_id = $1', [id]);

    // 10. Invite-Codes und Jahrgaenge
    await client.query('DELETE FROM invite_codes WHERE organization_id = $1', [id]);
    await client.query('DELETE FROM jahrgaenge WHERE organization_id = $1', [id]);

    // 11. Rollen (role_permissions werden per CASCADE geloescht)
    await client.query('DELETE FROM role_permissions WHERE role_id IN (SELECT id FROM roles WHERE organization_id = $1)', [id]);
    await client.query('DELETE FROM roles WHERE organization_id = $1', [id]);

    // 12. Organisation selbst
    const { rowCount } = await client.query('DELETE FROM organizations WHERE id = $1', [id]);
    if (rowCount === 0) {
      await client.query('ROLLBACK');
      return res.status(404).json({ error: 'Organisation nicht gefunden' });
    }

    await client.query('COMMIT');
    res.json({ message: 'Organisation und alle zugehoerigen Daten erfolgreich geloescht' });

  } catch (err) {
    await client.query('ROLLBACK').catch(rbErr => console.error('Rollback failed:', rbErr));
    console.error('Error deleting organization:', err);
    res.status(500).json({ error: 'Datenbankfehler beim Loeschen der Organisation' });
  } finally {
    client.release();
  }
});
```

Die bisherige Pruefung ob Konfis existieren wird ENTFERNT -- wenn ein Super-Admin loeschen will, wird ALLES geloescht.

WICHTIG: client.connect() Pattern verwenden (nicht db.query('BEGIN')), wie in Phase 22 entschieden. Alle DELETE-Queries ueber client ausfuehren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "client.query.*DELETE" backend/routes/organizations.js | xargs test 5 -le && grep -q "client.query.*BEGIN" backend/routes/organizations.js && grep -q "client.query.*COMMIT" backend/routes/organizations.js && echo "PASS: Transaktionssichere Org-Loeschung implementiert"</automated>
  </verify>
  <done>Org-Loeschung entfernt alle abhaengigen Daten (Chat, Events, Konfis, Badges, Settings, Rollen, Users) in einer Transaktion. Keine verwaisten Daten moeglich.</done>
</task>

</tasks>

<verification>
- activities.js hat Jahrgang-Filterung fuer Teamer-Rolle
- events.js hat Organisation-Filter bei allen relevanten Queries
- organizations.js DELETE-Route verwendet client.connect() + BEGIN/COMMIT Pattern
- organizations.js DELETE-Route loescht mindestens 15 verschiedene Tabellen
- Backend startet ohne Fehler
</verification>

<success_criteria>
- Teamer-Rolle sieht bei Aktivitaets-Zuweisungen nur Konfis ihrer zugewiesenen Jahrgaenge
- Org-Loeschung entfernt alle abhaengigen Daten in korrekter Reihenfolge transaktionssicher
- Keine verwaisten Datensaetze nach Org-Loeschung moeglich
</success_criteria>

<output>
After completion, create `.planning/phases/23-user-rechte-institutionen-debug/23-02-SUMMARY.md`
</output>
