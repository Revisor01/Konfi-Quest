---
phase: 89-architektur-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/server.js
  - backend/utils/liveUpdate.js
  - backend/routes/chat.js
  - backend/routes/users.js
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "global.io existiert nicht mehr im laufenden Prozess"
    - "Socket.io Echtzeit-Events (neue Nachricht, Reaktion, Live-Update) funktionieren weiterhin"
    - "liveUpdate.js hat keinen Zugriff mehr auf global.io"
  artifacts:
    - path: "backend/utils/liveUpdate.js"
      provides: "io-Parameter als DI statt global.io"
      contains: "module.exports = (io) =>"
    - path: "backend/server.js"
      provides: "io-Injektion in chat, users, liveUpdate"
      contains: "liveUpdate.init(io)"
    - path: "backend/routes/chat.js"
      provides: "io als injizierter Parameter statt global.io"
  key_links:
    - from: "backend/server.js"
      to: "backend/utils/liveUpdate.js"
      via: "liveUpdate.init(io) Aufruf nach io-Erstellung"
    - from: "backend/server.js"
      to: "backend/routes/chat.js"
      via: "chatRoutes(db, { verifyTokenRBAC: rbacVerifier }, uploadsDir, chatUpload, io)"
    - from: "backend/server.js"
      to: "backend/routes/users.js"
      via: "usersRoutes(db, rbacVerifier, roleHelpers, io)"
---

<objective>
global.io aus dem Node.js-Prozess entfernen. Socket.io-Instanz wird als Parameter injiziert, analog zum bestehenden `db`-DI-Pattern.

Purpose: Implizite globale Abhaengigkeit eliminieren. Testbarkeit und Explizitheit verbessern.
Output: liveUpdate.js als initiierbares Modul, chat.js und users.js mit io-Parameter, kein global.io mehr.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehendes DI-Pattern in server.js (Referenz fuer io-Injektion): -->

```javascript
// server.js Zeile 455-498 (Auszug) - so laeuft DI bereits fuer db:
const badgesRouter = adminBadgesRoutes(db, rbacVerifier, roleHelpers);
app.use('/api/chat', chatRoutes(db, { verifyTokenRBAC: rbacVerifier }, uploadsDir, chatUpload));
app.use('/api/admin/users', usersRoutes(db, rbacVerifier, roleHelpers));
app.use('/api/users', usersRoutes(db, rbacVerifier, roleHelpers));

// server.js Zeile 139 - ZU ENTFERNEN:
global.io = io;
```

```javascript
// chat.js Zeile 11 - Modul-Signatur (aktuell):
module.exports = (db, rbacMiddleware, uploadsDir, chatUpload) => { ... }
// global.io Zugriffe in chat.js (Zeilen 755, 756, 770, 1427, 1428, 1719, 1720, 1741, 1742):
if (global.io) { global.io.to(`room_${roomId}`).emit(...) }
```

```javascript
// users.js Zeile 12 - Modul-Signatur (aktuell):
module.exports = (db, rbacVerifier, { requireOrgAdmin }) => { ... }
// global.io Zugriff in users.js (Zeilen 260, 267):
if (role_id !== undefined && global.io) {
  const sockets = await global.io.in(roomName).fetchSockets();
}
```

```javascript
// liveUpdate.js - aktuell alle 4 Funktionen mit global.io:
function sendToUser(userType, userId, ...) {
  if (!global.io) { return; }
  global.io.to(userRoom).emit('liveUpdate', event);
}
// Weitere: sendToOrgAdmins, sendToOrgKonfis, sendToJahrgang - alle gleich
module.exports = { sendToUser, sendToKonfi, sendToAdmin, sendToOrgAdmins, sendToOrgKonfis, sendToOrg, sendToJahrgang };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: liveUpdate.js auf init(io)-Pattern umstellen</name>
  <files>backend/utils/liveUpdate.js</files>
  <action>
liveUpdate.js in ein initialisierbares Modul umwandeln:

1. Modul-lokale Variable `let _io = null;` am Anfang hinzufuegen.
2. Neue `init(io)`-Funktion: `function init(io) { _io = io; }` hinzufuegen.
3. Alle 7 Vorkommen von `global.io` durch `_io` ersetzen (Zeilen 13, 27, 38, 61, 77, 100, 120, 141).
4. `init` zu `module.exports` hinzufuegen: `module.exports = { init, sendToUser, sendToKonfi, sendToAdmin, sendToOrgAdmins, sendToOrgKonfis, sendToOrg, sendToJahrgang };`

Alle anderen Funktionen bleiben unveraendert. Keine Logik-Aenderungen.
  </action>
  <verify>
    <automated>grep -n "global\.io" /Users/simonluthe/Documents/Konfipoints/backend/utils/liveUpdate.js | wc -l</automated>
  </verify>
  <done>Ausgabe ist 0 — kein global.io mehr in liveUpdate.js. Datei exportiert init-Funktion.</done>
</task>

<task type="auto">
  <name>Task 2: chat.js und users.js io als Parameter empfangen</name>
  <files>backend/routes/chat.js, backend/routes/users.js</files>
  <action>
**chat.js:**
1. Modul-Signatur erweitern: `module.exports = (db, rbacMiddleware, uploadsDir, chatUpload, io) => {`
2. Alle `global.io` in chat.js durch `io` ersetzen (Zeilen 755, 756, 770, 1427, 1428, 1719, 1720, 1741, 1742).
3. Die `if (global.io)` Guards werden zu `if (io)` — gleiche Logik, nur anderer Bezeichner.

**users.js:**
1. Modul-Signatur erweitern: `module.exports = (db, rbacVerifier, { requireOrgAdmin }, io) => {`
2. Alle `global.io` in users.js durch `io` ersetzen (Zeilen 260, 267).
3. `if (role_id !== undefined && global.io)` wird zu `if (role_id !== undefined && io)`.

Keine Logik-Aenderungen ausser dem Bezeichner.
  </action>
  <verify>
    <automated>grep -n "global\.io" /Users/simonluthe/Documents/Konfipoints/backend/routes/chat.js /Users/simonluthe/Documents/Konfipoints/backend/routes/users.js | wc -l</automated>
  </verify>
  <done>Ausgabe ist 0 — kein global.io mehr in chat.js oder users.js.</done>
</task>

<task type="auto">
  <name>Task 3: server.js global.io entfernen und io injizieren</name>
  <files>backend/server.js</files>
  <action>
1. `global.io = io;` (Zeile 139) entfernen.
2. `const liveUpdate = require('./utils/liveUpdate');` am Anfang der Route Imports Sektion hinzufuegen (nach Zeile 435 im Bereich "ROUTE IMPORTS").
3. Nach dem `io`-Setup (nach dem socket.on disconnect-Handler, ca. Zeile 136) liveUpdate initialisieren:
   ```javascript
   liveUpdate.init(io);
   ```
4. chat.js-Mount erweitern — `io` als fuenften Parameter uebergeben:
   ```javascript
   app.use('/api/chat', chatRoutes(db, { verifyTokenRBAC: rbacVerifier }, uploadsDir, chatUpload, io));
   ```
5. users.js-Mounts erweitern — `io` als vierten Parameter uebergeben (BEIDE Stellen):
   ```javascript
   app.use('/api/admin/users', usersRoutes(db, rbacVerifier, roleHelpers, io));
   app.use('/api/users', usersRoutes(db, rbacVerifier, roleHelpers, io));
   ```

Reihenfolge beachten: liveUpdate.init(io) MUSS vor dem ersten Route-Mount stehen, da liveUpdate von anderen Routes (badges, activities etc.) beim ersten Request verwendet wird.
  </action>
  <verify>
    <automated>grep -rn "global\.io" /Users/simonluthe/Documents/Konfipoints/backend/ --include="*.js" | wc -l</automated>
  </verify>
  <done>Ausgabe ist 0 — global.io existiert nicht mehr im gesamten Backend. Server startet ohne Fehler: `cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "require('./server')" 2>&1 | head -5` zeigt keinen SyntaxError.</done>
</task>

</tasks>

<verification>
```bash
# Kein global.io mehr im gesamten Backend
grep -rn "global\.io" /Users/simonluthe/Documents/Konfipoints/backend/ --include="*.js"
# Erwartete Ausgabe: (leer)

# liveUpdate.js exportiert init
grep "module.exports" /Users/simonluthe/Documents/Konfipoints/backend/utils/liveUpdate.js
# Erwartet: { init, sendToUser, ... }

# chat.js Signatur hat io-Parameter
grep "module.exports = " /Users/simonluthe/Documents/Konfipoints/backend/routes/chat.js
# Erwartet: module.exports = (db, rbacMiddleware, uploadsDir, chatUpload, io) =>

# Syntax-Check
node --check /Users/simonluthe/Documents/Konfipoints/backend/server.js
node --check /Users/simonluthe/Documents/Konfipoints/backend/routes/chat.js
node --check /Users/simonluthe/Documents/Konfipoints/backend/routes/users.js
node --check /Users/simonluthe/Documents/Konfipoints/backend/utils/liveUpdate.js
```
</verification>

<success_criteria>
- global.io existiert nicht mehr (grep liefert 0 Treffer)
- liveUpdate.js initialisierbar via init(io)
- chat.js und users.js empfangen io als Parameter
- server.js uebergibt io an alle betroffenen Module
- Alle 4 Dateien bestehen node --check ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/89-architektur-cleanup/89-01-SUMMARY.md` erstellen.
</output>
