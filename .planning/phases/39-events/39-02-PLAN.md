---
phase: 39-events
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
autonomous: false
requirements: [EVT-01, EVT-02, EVT-03, EVT-04, EVT-05, EVT-06]

must_haves:
  truths:
    - "Admin sieht im Event-Formular einen Teamer-Zugang-Select mit 3 Optionen (Nur Konfis, Teamer:innen gesucht, Nur Teamer:innen)"
    - "Admin sieht in der Event-Teilnehmerliste getrennte Sektionen: Konfis (X) und Teamer:innen (X)"
    - "Teamer sieht Events-Tab mit 3 Segmenten (Meine, Alle, Team)"
    - "Teamer kann sich ueber 'Ich bin dabei'-Button fuer Events einbuchen"
    - "Teamer kann sich ueber 'Nicht mehr dabei'-Button wieder austragen"
    - "Teamer sieht TEAM-Corner-Badge bei Events die fuer Teamer:innen offen sind"
    - "Teamer kann QR-Code scannen und einchecken"
  artifacts:
    - path: "frontend/src/components/admin/modals/EventModal.tsx"
      provides: "Teamer-Zugang IonSelect im Event-Formular"
      contains: "teamer_needed"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "Getrennte Teilnehmer-Sektionen nach Rolle"
      contains: "role_name"
    - path: "frontend/src/components/teamer/pages/TeamerEventsPage.tsx"
      provides: "Vollstaendige Events-Seite mit 3 Segmenten und Event-Detail"
      min_lines: 200
  key_links:
    - from: "EventModal.tsx"
      to: "POST /api/events"
      via: "api.post mit teamer_needed/teamer_only"
      pattern: "teamer_needed|teamer_only"
    - from: "TeamerEventsPage.tsx"
      to: "GET /api/events"
      via: "api.get und clientseitige Segment-Filterung"
      pattern: "teamer_needed.*teamer_only"
    - from: "TeamerEventsPage.tsx"
      to: "POST /api/events/:id/book"
      via: "Teamer-Booking (vereinfacht)"
      pattern: "api.post.*book"
---

<objective>
Frontend-Implementierung: Admin Event-Formular mit Teamer-Zugang, getrennte Teilnehmerliste, und vollstaendige Teamer-Events-Seite mit 3 Segmenten.

Purpose: Admin kann Events fuer Teamer:innen konfigurieren, Teamer:innen sehen und buchen Events in einer strukturierten Ansicht mit Meine/Alle/Team-Segmenten.
Output: Erweiterte Admin-Komponenten und vollstaendige TeamerEventsPage
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-events/39-CONTEXT.md
@.planning/phases/39-events/39-RESEARCH.md
@.planning/phases/39-events/39-01-SUMMARY.md

<interfaces>
<!-- Vorlagen und Patterns aus bestehendem Code -->

From frontend/src/components/konfi/views/EventsView.tsx (Zeile 75-81):
```typescript
interface EventsViewProps {
  events: Event[];
  activeTab: 'upcoming' | 'registered' | 'konfirmation';
  onTabChange: (tab: 'upcoming' | 'registered' | 'konfirmation') => void;
  onSelectEvent: (event: Event) => void;
  onUpdate: () => void;
}
```

From frontend/src/components/konfi/views/EventsView.tsx - Event Interface (Zeile 40-73):
```typescript
interface Event {
  id: number;
  name: string;
  description?: string;
  event_date: string;
  event_end_time?: string;
  location?: string;
  location_maps_url?: string;
  points: number;
  categories?: Category[];
  category_names?: string;
  type: string;
  max_participants: number;
  registration_opens_at?: string;
  registration_closes_at?: string;
  registered_count: number;
  registration_status: 'upcoming' | 'open' | 'closed' | 'cancelled';
  is_registered?: boolean;
  can_register?: boolean;
  attendance_status?: 'present' | 'absent' | null;
  booking_status?: 'confirmed' | 'waitlist' | 'pending' | 'opted_out' | null;
  mandatory?: boolean;
  bring_items?: string;
  // NEU aus Plan 39-01:
  // teamer_needed?: boolean;
  // teamer_only?: boolean;
  // teamer_count?: number;
}
```

From frontend/src/components/admin/modals/EventModal.tsx (Zeile 32-52):
```typescript
interface Event {
  id: number;
  name: string;
  // ... bestehende Felder ...
  mandatory?: boolean;
  bring_items?: string;
  checkin_window?: number;
  // NEU: teamer_needed?: boolean; teamer_only?: boolean;
}
```

From frontend/src/components/admin/modals/EventModal.tsx (Zeile 73-78):
```typescript
interface EventModalProps {
  event?: Event | null;
  onClose: () => void;
  onSuccess: () => void;
}
```

From frontend/src/theme/variables.css - Corner Badge Pattern:
```css
.app-corner-badge { /* bestehendes Pattern */ }
.app-corner-badge--purple { /* Teamer-Farbe */ }
```

From frontend/src/components/shared/EmptyState.tsx:
```typescript
// EmptyState Komponente mit icon, title, message Props
```

From frontend/src/components/teamer/pages/TeamerEventsPage.tsx:
```typescript
// Aktuell EmptyState-Platzhalter, wird komplett ersetzt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Event-Formular und Teilnehmerliste erweitern</name>
  <files>frontend/src/components/admin/modals/EventModal.tsx, frontend/src/components/admin/views/EventDetailView.tsx</files>
  <action>
1. **EventModal.tsx** - Teamer-Zugang IonSelect hinzufuegen:
   - Event-Interface erweitern: `teamer_needed?: boolean; teamer_only?: boolean;`
   - Neuen State: `const [teamerAccess, setTeamerAccess] = useState<'normal' | 'teamer_needed' | 'teamer_only'>('normal');`
   - Bei Edit-Mode: teamerAccess aus event.teamer_needed/teamer_only ableiten
   - IonSelect im Formular NACH dem mandatory-Toggle einfuegen:
     ```tsx
     <IonItem>
       <IonSelect
         label="Teamer-Zugang"
         value={teamerAccess}
         onIonChange={(e) => {
           const v = e.detail.value;
           setTeamerAccess(v);
         }}
       >
         <IonSelectOption value="normal">Nur Konfis</IonSelectOption>
         <IonSelectOption value="teamer_needed">Teamer:innen gesucht</IonSelectOption>
         <IonSelectOption value="teamer_only">Nur Teamer:innen</IonSelectOption>
       </IonSelect>
     </IonItem>
     ```
   - Im Submit-Handler: `teamer_needed: teamerAccess === 'teamer_needed'` und `teamer_only: teamerAccess === 'teamer_only'` zum API-Call hinzufuegen

2. **EventDetailView.tsx** - Getrennte Teilnehmer-Sektionen:
   - Participants nach role_name aufteilen (role_name kommt jetzt aus dem Backend per Plan 01):
     ```typescript
     const konfiParticipants = participants.filter(p => p.role_name !== 'teamer');
     const teamerParticipants = participants.filter(p => p.role_name === 'teamer');
     ```
   - Zwei getrennte Sektionen rendern mit SectionHeader:
     - "Konfis ({konfiParticipants.length})" -- bestehende Teilnehmer-Liste
     - "Teamer:innen ({teamerParticipants.length})" -- nur anzeigen wenn teamerParticipants.length > 0
   - Teamer:innen-Sektion: Gleiche Item-Darstellung wie Konfis (Name, Status, Attendance)
   - Teamer-Zustand des Events anzeigen: Ein Info-Badge unter dem Event-Titel ("Teamer:innen gesucht" oder "Nur Teamer:innen")
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "teamer_needed\|teamer_only\|teamerAccess" frontend/src/components/admin/modals/EventModal.tsx && grep -c "role_name\|teamerParticipants" frontend/src/components/admin/views/EventDetailView.tsx</automated>
  </verify>
  <done>Admin sieht Teamer-Zugang-Select im Event-Formular und getrennte Konfis/Teamer:innen-Sektionen in der Teilnehmerliste.</done>
</task>

<task type="auto">
  <name>Task 2: Vollstaendige TeamerEventsPage mit 3 Segmenten und Event-Detail</name>
  <files>frontend/src/components/teamer/pages/TeamerEventsPage.tsx</files>
  <action>
Die bestehende EmptyState-Platzhalter-Datei komplett ersetzen durch eine vollstaendige Events-Seite. Vorlage ist `src/components/konfi/views/EventsView.tsx` -- gleiches Layout, gleiche CSS-Klassen, angepasste Segmente und Buchungslogik.

**Aufbau der Komponente (alles in einer Datei, wie KonfiEventsPage-Pattern):**

1. **Event Interface** erweitern um `teamer_needed?: boolean; teamer_only?: boolean; teamer_count?: number;`

2. **State:**
   - `events: Event[]` -- alle Events vom Backend
   - `activeTab: 'meine' | 'alle' | 'team'` -- Default: 'meine'
   - `selectedEvent: Event | null` -- fuer Detail-Ansicht
   - `loading: boolean`

3. **Daten laden:**
   - `api.get('/events')` -- Backend liefert bereits gefilterte Events (Jahrgang + teamer_only)
   - `api.get('/events/user/bookings')` -- eigene Buchungen laden fuer is_registered Mapping

4. **Kompakter Header** (wie Konfi-EventsView):
   - Icon + "Events" Titel inline
   - Stats-Row darunter: Gesamt | Anstehend | Gebucht (Zaehler aus gefilterten Events)

5. **3 Segmente** mit IonSegment:
   - **Meine:** `events.filter(e => e.is_registered)` -- Alle Events wo Teamer:in dabei ist
   - **Alle:** `events.filter(e => !e.teamer_only)` -- Alle Jahrgangs-Events (auch reine Konfi-Events sichtbar aber nicht buchbar)
   - **Team:** `events.filter(e => e.teamer_needed || e.teamer_only)` -- Alle fuer Teamer:innen buchbare Events

6. **Event-Karten** (gleiches Layout wie Konfi-EventsView):
   - Name, Datum (formatiert DD.MM.YYYY), Uhrzeit (HH:MM), Ort
   - Status-Info: Angemeldete Konfis + Teamer:innen Anzahl
   - **TEAM Corner Badge** oben rechts bei Events mit teamer_needed oder teamer_only:
     ```tsx
     {(event.teamer_needed || event.teamer_only) && (
       <div className="app-corner-badge app-corner-badge--purple"
            style={{ top: 'auto', bottom: 0, borderRadius: '10px 0 10px 0' }}>
         TEAM
       </div>
     )}
     ```
   - Vergangene Events: Anwesenheitsstatus anzeigen (checkmarkCircle/closeCircle)
   - Sortierung: Chronologisch (naechstes Event zuerst), vergangene am Ende

7. **Event-Detail-Ansicht** (inline in der Seite, nicht als separate Route):
   - Event-Info (Name, Beschreibung, Datum, Uhrzeit, Ort mit Maps-Link, Kategorie, Mitbringen)
   - Angemeldete Teamer:innen-Liste (Namen)
   - **Vereinfachter Buchungsbereich:**
     - Wenn nicht gebucht und Event buchbar (teamer_needed oder teamer_only):
       `<IonButton expand="block" onClick={handleBook}>Ich bin dabei</IonButton>`
     - Wenn gebucht:
       `<IonButton expand="block" color="danger" onClick={handleUnbook}>Nicht mehr dabei</IonButton>`
     - Wenn Event nicht fuer Teamer:innen (im "Alle"-Segment, reines Konfi-Event): Keine Buchungs-Buttons, nur Info-Text
   - API-Calls: `api.post('/events/{id}/book')` und `api.delete('/events/{id}/book')`

8. **QR-Code Check-in:**
   - QRScannerModal 1:1 wiederverwenden (Import aus konfi/modals/QRScannerModal)
   - FAB-Button unten rechts mit qrCodeOutline Icon (wie bei Konfi-EventsView)
   - `useIonModal(QRScannerModal, { onClose, onSuccess })` -- onSuccess laedt Events neu

9. **Sprache:** Echte Umlaute ueberall, Gendern (Teamer:innen), "Konfis" nicht gendern.

10. **Leere Segmente:** EmptyState-Komponente mit passendem Text pro Segment:
    - Meine: "Du bist noch bei keinem Event dabei"
    - Alle: "Keine Events vorhanden"
    - Team: "Keine Events fuer Teamer:innen verfuegbar"
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && wc -l frontend/src/components/teamer/pages/TeamerEventsPage.tsx && grep -c "IonSegment\|meine\|alle\|team\|teamer_needed\|QRScannerModal" frontend/src/components/teamer/pages/TeamerEventsPage.tsx</automated>
  </verify>
  <done>Teamer sieht vollstaendige Events-Seite mit 3 Segmenten (Meine/Alle/Team), kann Events buchen/stornieren, sieht TEAM-Badge und kann QR-Code einchecken.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-End Verifikation Teamer-Event-System</name>
  <files>n/a</files>
  <action>
Vollstaendiges Teamer-Event-System verifizieren: Admin kann Events fuer Teamer:innen oeffnen (3 Zustaende), Teamer:innen sehen Events in 3 Segmenten und koennen buchen/stornieren/einchecken.

Pruefschritte:
1. Als Admin einloggen, Events, neues Event erstellen -- "Teamer-Zugang" Select muss sichtbar sein mit 3 Optionen
2. Event mit "Teamer:innen gesucht" erstellen
3. Als Teamer einloggen, Events-Tab, "Team"-Segment -- Event muss sichtbar sein mit TEAM-Badge
4. Event antippen, "Ich bin dabei" klicken -- Buchung erfolgreich
5. "Meine"-Segment -- gebuchtes Event muss erscheinen
6. Event antippen, "Nicht mehr dabei" klicken -- Austragung erfolgreich
7. Als Admin, Event-Detail -- Teilnehmerliste muss Konfis und Teamer:innen getrennt zeigen
8. "Alle"-Segment des Teamers -- auch reine Konfi-Events sichtbar (ohne Buchungs-Button)
  </action>
  <verify>Manueller Test durch User</verify>
  <done>User bestaetigt mit "approved" dass alle 8 Pruefschritte funktionieren.</done>
</task>

</tasks>

<verification>
- Admin Event-Formular zeigt Teamer-Zugang-Select mit 3 Optionen
- Admin Event-Detail zeigt getrennte Konfis/Teamer:innen-Sektionen
- Teamer Events-Tab hat 3 Segmente (Meine, Alle, Team)
- Teamer kann Events buchen und stornieren
- TEAM-Corner-Badge erscheint bei relevanten Events
- QR-Check-in FAB-Button ist vorhanden
- Vergangene Events zeigen Anwesenheitsstatus
</verification>

<success_criteria>
- Admin kann Events mit Teamer-Zugang konfigurieren (3 Zustaende)
- Teamer sieht strukturierten Events-Tab mit 3 Segmenten
- Buchung/Storno funktioniert fuer Teamer (vereinfacht)
- QR-Code Check-in ist verfuegbar
- Teilnehmerliste zeigt Konfis und Teamer:innen getrennt
- Layout und CSS-Klassen identisch zu Konfi-Events (Konsistenz)
</success_criteria>

<output>
After completion, create `.planning/phases/39-events/39-02-SUMMARY.md`
</output>
