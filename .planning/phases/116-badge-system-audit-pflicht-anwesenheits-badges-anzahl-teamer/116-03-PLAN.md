---
phase: 116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/modals/BadgeManagementModal.tsx
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "Die Leitung kann im Badge-Modal bei target_role=konfi den Typ Pflicht-Anwesenheit (mandatory_event_count) auswaehlen"
    - "Der neue Typ wird bei target_role=teamer NICHT angezeigt"
    - "Der Wert-Label und die Default-Farbe sind fuer den neuen Typ gesetzt"
  artifacts:
    - path: "frontend/src/components/admin/modals/BadgeManagementModal.tsx"
      provides: "Freischaltung mandatory_event_count in Typ-Auswahl + getValueLabel + Default-Farbe"
      contains: "mandatory_event_count"
  key_links:
    - from: "Typ-Auswahl-Filter (BadgeManagementModal :1009)"
      to: "criteriaTypes vom Backend GET /criteria-types"
      via: "mandatory_event_count erscheint bei target_role=konfi"
      pattern: "mandatory_event_count"
---

<objective>
Frontend-Freischaltung des neuen Pflicht-Anwesenheits-Badge-Typs (`mandatory_event_count`) im Badge-Erstellungs-Modal. Der Typ kommt automatisch ueber GET /admin/badges/criteria-types vom Backend (Plan 01), muss aber im UI korrekt fuer Konfis sichtbar sein, einen passenden Wert-Label und eine Default-Farbe bekommen.

Purpose: Die Leitung kann das neue Badge ueber die bestehende Badge-Verwaltung anlegen und den Schwellwert spaeter editieren (bestehender Edit-Pfad, D-05).
Output: Aktualisiertes BadgeManagementModal.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-CONTEXT.md
@.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-AUDIT.md

<interfaces>
<!-- Zentrale Stellen in BadgeManagementModal.tsx (bereits in context geladen) -->

CATEGORY_COLORS-Map :210-219 (Default-Farbe pro criteria_type, z.B. event_count: '#e63946', teamer_year: '#5b21b6'). getCategoryColor :221 faellt sonst auf '#667eea'.
TEAMER_HIDDEN_TYPES :255 (Typen, die bei Teamer ausgeblendet werden — Punkte + time_based + streak + event_count).
getValueLabel() :713-739 (switch ueber criteria_type -> Wert-Label-Text).
Typ-Auswahl-Filter :1008-1015 (Object.entries(criteriaTypes).filter — blendet TEAMER_HIDDEN_TYPES bei Teamer aus, teamer_year nur bei Teamer).
Typen werden vom Backend geladen :332 (GET /admin/badges/criteria-types -> liefert CRITERIA_TYPES inkl. neuem mandatory_event_count nach Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mandatory_event_count im Badge-Modal freischalten</name>
  <files>frontend/src/components/admin/modals/BadgeManagementModal.tsx</files>
  <read_first>
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx:210-221 (CATEGORY_COLORS + getCategoryColor)
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx:253-269 (TEAMER_HIDDEN_TYPES, defaultCriteriaType)
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx:713-739 (getValueLabel)
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx:1008-1016 (Typ-Auswahl-Filter)
    - .planning/phases/116-.../116-CONTEXT.md D-01, D-03 + Claude's Discretion (Modal-Details)
  </read_first>
  <action>
    Den neuen Typ `mandatory_event_count` im Modal vollstaendig integrieren (er kommt bereits ueber GET /admin/badges/criteria-types vom Backend, daher KEINE eigene Label-/Beschreibungsdefinition noetig — die liefert das Backend):

    1. Default-Farbe: in CATEGORY_COLORS (:210-219) einen Eintrag `mandatory_event_count: '#...'` ergaenzen (eine klar von event_count '#e63946' unterscheidbare Farbe waehlen, z.B. ein dunkleres Rot/Orange-Ton). Keine Emojis.

    2. getValueLabel (:713-739): einen `case 'mandatory_event_count':` mit Rueckgabe 'Anzahl (Pflicht-Events)' ergaenzen. Echte Umlaute.

    3. Sichtbarkeit (D-03, Konfi-only): Der Typ soll NUR bei target_role='konfi' erscheinen, nicht bei Teamer. Im Typ-Auswahl-Filter (:1008-1015) sicherstellen, dass mandatory_event_count bei Teamer ausgeblendet wird — am sichersten, indem 'mandatory_event_count' zu TEAMER_HIDDEN_TYPES (:255) hinzugefuegt wird (analog zu event_count, das dort schon steht). Bei Konfi bleibt er sichtbar.

    Pruefe, ob durch die Backend-Lieferung der Typ-Reihenfolge sonst nichts angepasst werden muss (der filter/map laeuft generisch ueber criteriaTypes). KEINE IonModal isOpen-Aenderungen, Modal-Struktur unangetastet lassen.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "BadgeManagementModal" || echo "no TS errors in BadgeManagementModal"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "mandatory_event_count" frontend/src/components/admin/modals/BadgeManagementModal.tsx liefert >= 3 Treffer (CATEGORY_COLORS, getValueLabel-case, TEAMER_HIDDEN_TYPES).
    - 'mandatory_event_count' steht in TEAMER_HIDDEN_TYPES (:255) -> bei Teamer ausgeblendet.
    - getValueLabel gibt fuer mandatory_event_count 'Anzahl (Pflicht-Events)' zurueck.
    - npx tsc --noEmit meldet keine neuen Fehler in BadgeManagementModal.tsx.
    - Keine Unicode-Emojis im geaenderten Code; echte Umlaute verwendet.
  </acceptance_criteria>
  <done>Pflicht-Anwesenheits-Typ ist im Modal fuer Konfis waehlbar, bei Teamer ausgeblendet, mit Wert-Label und Default-Farbe; TS kompiliert fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-UI -> Backend POST /admin/badges | criteria_type wird serverseitig gespeichert; UI ist reine Auswahl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-06 | Spoofing | target_role-Sichtbarkeit im Modal | accept | UI-Filter ist Komfort; die echte Rollen-Trennung erfolgt serverseitig (Wertungs-Branch in badges.js). Ein manuell gesendeter mandatory_event_count fuer einen Teamer-Badge wuerde nie gewertet (Konfi-Branch) |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete — reine Modal-Aenderung, kein Install |
</threat_model>

<verification>
- grep -c "mandatory_event_count" frontend/src/components/admin/modals/BadgeManagementModal.tsx >= 3
- npx tsc --noEmit -> keine neuen Fehler in BadgeManagementModal.tsx
- Manuell/Code: mandatory_event_count in TEAMER_HIDDEN_TYPES vorhanden
</verification>

<success_criteria>
- mandatory_event_count im Modal fuer Konfis waehlbar, bei Teamer ausgeblendet
- Wert-Label 'Anzahl (Pflicht-Events)' + Default-Farbe gesetzt
- TypeScript kompiliert fehlerfrei, keine Emojis, echte Umlaute
</success_criteria>

<output>
Create `.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-03-SUMMARY.md` when done.
</output>
