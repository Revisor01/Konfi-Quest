---
phase: 116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/badges.js
  - backend/tests/routes/badges.test.js
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]
must_haves:
  truths:
    - "Die Leitung kann ein Badge vom Typ Pflicht-Anwesenheit (mandatory_event_count) anlegen"
    - "Ein Konfi erhaelt das Pflicht-Anwesenheits-Badge, wenn die Anzahl besuchter Pflicht-Events (mandatory=true, attendance_status=present) den Schwellwert erreicht"
    - "Nicht-Pflicht-Events und nicht besuchte Pflicht-Events zaehlen NICHT mit"
    - "Ein bereits vergebenes Pflicht-Badge bleibt bestehen, wenn der Schwellwert spaeter angehoben wird (kein Entzug)"
    - "Das teamer_year-Badge nutzt users.teamer_since als Startjahr statt der nicht-existenten user_role_history"
    - "Bei teamer_since = NULL faellt teamer_year auf die aelteste Teamer-Aktivitaet zurueck"
  artifacts:
    - path: "backend/routes/badges.js"
      provides: "CRITERIA_TYPES.mandatory_event_count + Wertungs-Case im Konfi-Branch + teamer_since-Startjahr im teamer_year-Case"
      contains: "mandatory_event_count"
    - path: "backend/tests/routes/badges.test.js"
      provides: "Integrationstests fuer mandatory_event_count-Wertung und teamer_since-Startjahr"
  key_links:
    - from: "checkAndAwardBadges Konfi-switch"
      to: "event_bookings JOIN events WHERE e.mandatory=true AND eb.attendance_status='present'"
      via: "neuer case 'mandatory_event_count'"
      pattern: "mandatory_event_count"
    - from: "teamer_year-case"
      to: "users.teamer_since"
      via: "Startjahr-Query"
      pattern: "teamer_since"
---

<objective>
Backend-Wertungslogik fuer das Badge-System erweitern und korrigieren:
1. Neuer criteria_type `mandatory_event_count` (Pflicht-Anwesenheits-Badge fuer Konfis): zaehlt die absolute Anzahl besuchter Pflicht-Events.
2. Fix des bestehenden `teamer_year`-Badges: Startjahr aus `users.teamer_since` statt der nicht-existenten Tabelle `user_role_history`.

Purpose: Die Leitung kann Pflicht-Anwesenheit als Badge auszeichnen (z.B. "alle 12 Pflicht-Termine besucht"), und Teamer-Jahre zaehlen korrekt ab dem echten Promotions-Datum.
Output: Erweiterte Wertungslogik in backend/routes/badges.js plus Integrationstests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-CONTEXT.md
@.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-AUDIT.md

<interfaces>
<!-- Zentrale Stellen in backend/routes/badges.js (bereits in context geladen, hier zur Orientierung) -->

CRITERIA_TYPES-Objekt: badges.js:9-91 (jeder Typ hat label, description, help).
Konfi-Wertungs-switch: badges.js:182-289 (z.B. case 'event_count' :271-274 nutzt preloaded.eventCount).
Vorab-Query-Block: badges.js:152-172 (preloaded enthaelt eventCount aus event_bookings WHERE attendance_status='present').
teamer_year-case: badges.js:405-466 (Startjahr-Fallback-Kette: try user_role_history :412-422, dann MIN(completed_date) :424-435).
POST-Validierung validateCreateBadge: badges.js:648-652 (verlangt nur notEmpty fuer criteria_type, KEINE Whitelist — neuer Typ ist ohne Validierungs-Aenderung erlaubt).

Identische Zaehl-Query, die Plan 02 (konfi.js Progress) ebenfalls verwenden MUSS (Konsistenz-Vertrag):
  SELECT COUNT(*) FROM event_bookings eb JOIN events e ON eb.event_id = e.id
  WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND e.mandatory = true AND eb.organization_id = $2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: criteria_type mandatory_event_count definieren + Wertungs-Case (Konfi)</name>
  <files>backend/routes/badges.js, backend/tests/routes/badges.test.js</files>
  <read_first>
    - backend/routes/badges.js:9-91 (CRITERIA_TYPES — neuen Eintrag ergaenzen)
    - backend/routes/badges.js:152-172 (Vorab-Query-Block preloaded)
    - backend/routes/badges.js:182-289 (Konfi-Wertungs-switch, Vorbild case 'event_count' :271)
    - backend/routes/badges.js:732-755 (POST — bestaetigt: keine criteria_type-Whitelist)
    - .planning/phases/116-.../116-AUDIT.md Abschnitt 4 (Pflicht-Events, exakte Zaehl-Query :114-118)
    - .planning/phases/116-.../116-CONTEXT.md D-01 bis D-06
  </read_first>
  <behavior>
    - Test: Konfi mit 12 besuchten Pflicht-Events (events.mandatory=true, attendance_status='present') + Badge criteria_value=12 -> Badge wird vergeben (in user_badges).
    - Test: Konfi mit 11 besuchten Pflicht-Events + Badge criteria_value=12 -> Badge NICHT vergeben.
    - Test: Nicht-Pflicht-Events (mandatory=false) und Pflicht-Events ohne attendance_status='present' zaehlen NICHT mit (Konfi mit 5 Nicht-Pflicht + 3 nicht-besuchten Pflicht-Events -> current=0, kein Badge bei value=1).
    - Test: Bereits vergebenes Badge bleibt bei spaeterer Schwellwert-Anhebung erhalten (kein Entzug — alreadyEarned-Pfad, INSERT-only).
  </behavior>
  <action>
    In CRITERIA_TYPES (badges.js:9-91) einen neuen Eintrag `mandatory_event_count` ergaenzen (Position sinnvoll bei den aktivitaets-/event-basierten Kriterien, nahe event_count). Felder: label "Pflicht-Anwesenheit", description "Anzahl besuchter Pflicht-Events", help-Text der erklaert, dass nur Events mit Pflicht-Markierung und bestaetigter Anwesenheit zaehlen, Beispiel Wert 12. Echte Umlaute, keine Emojis.

    Im Konfi-Wertungs-switch (badges.js:182-289) einen neuen `case 'mandatory_event_count':` ergaenzen. Die Zaehlung per dedizierter Query (nicht ueber preloaded.eventCount, da dieser KEINEN mandatory-Filter hat): SELECT COUNT(*) FROM event_bookings eb JOIN events e ON eb.event_id = e.id WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND e.mandatory = true AND eb.organization_id = $2 (Parameter userId, konfi.organization_id). `earned = parseInt(count) >= badge.criteria_value`. Diese Query ist der Konsistenz-Vertrag mit Plan 02 (konfi.js Progress muss IDENTISCH zaehlen — sonst neuer Wertung/Progress-Mismatch wie beim bestehenden Bug). Per D-04 zaehlen Pflicht-Events fuer Badges, obwohl sie punktneutral sind. Per D-03 ist das ein Konfi-Badge (target_role='konfi', laeuft im Konfi-Branch). KEINE Entzugs-Logik bauen (D-06): die bestehende INSERT-only-Vergabe und der alreadyEarned-Check (:175) bleiben unveraendert.

    Tests in backend/tests/routes/badges.test.js nach dem Muster bestehender Wertungstests ergaenzen (Test-Konfi anlegen, Pflicht-Events + Buchungen mit attendance_status setzen, checkAndAwardBadges bzw. zustaendigen Endpoint triggern, user_badges pruefen).
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/routes/badges.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "mandatory_event_count" backend/routes/badges.js liefert mindestens 2 Treffer (CRITERIA_TYPES-Eintrag + switch-case).
    - grep zeigt im case eine Query mit "e.mandatory = true" UND "attendance_status = 'present'".
    - vitest badges.test.js: neue Tests gruen (12 Events -> Badge, 11 -> kein Badge, Nicht-Pflicht zaehlt nicht).
    - Bestehende badges.test.js-Tests weiterhin gruen (keine Regression).
  </acceptance_criteria>
  <done>mandatory_event_count ist definiert und wird korrekt gewertet; nur besuchte Pflicht-Events zaehlen; kein Entzug; Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: teamer_year-Startjahr auf users.teamer_since umstellen</name>
  <files>backend/routes/badges.js, backend/tests/routes/badges.test.js</files>
  <read_first>
    - backend/routes/badges.js:405-466 (teamer_year-case, Startjahr-Fallback-Kette)
    - backend/routes/badges.js:412-422 (toter try/catch auf user_role_history)
    - .planning/phases/116-.../116-AUDIT.md Abschnitt 6 (Teamer-Jahre, teamer_since Migration 064:108)
    - .planning/phases/116-.../116-CONTEXT.md D-07, D-08, D-09
  </read_first>
  <behavior>
    - Test: Teamer mit teamer_since='2024-01-01', Aktivitaeten in 2024 und 2026 (nicht 2025) + Badge criteria_value=2 -> Badge vergeben (relevantYears = {2024,2026} ab startYear 2024).
    - Test: Teamer mit teamer_since='2026-01-01' aber Aktivitaet in 2024 -> 2024 zaehlt NICHT (vor Startjahr), nur Jahre >= 2026.
    - Test: Teamer mit teamer_since=NULL, aelteste Teamer-Aktivitaet 2023 -> Fallback greift, startYear=2023.
  </behavior>
  <action>
    Im teamer_year-case (badges.js:405-466) die Startjahr-Ermittlung umbauen. Den ersten Versuch (try/catch-Block :412-422, der die nicht-existente Tabelle user_role_history liest) ersetzen durch eine Query auf users.teamer_since: SELECT teamer_since FROM users WHERE id = $1. Wenn teamer_since gesetzt ist: `startYear = new Date(teamer_since).getFullYear()`. Wenn teamer_since NULL ist (Altdaten): den bestehenden Fallback auf die aelteste Teamer-Aktivitaet (MIN(ua.completed_date) JOIN activities target_role='teamer', :424-435) UNVERAENDERT beibehalten. Die restliche Logik (allDates sammeln :443-453, activeYears-Set :456-461, relevantYears-Filter y >= startYear :463, Vergleich gegen criteria_value :464) bleibt unveraendert. Den toten user_role_history-Query entfernen. Per D-09 ist target_role='teamer' (laeuft im Teamer-Branch).

    Tests in badges.test.js ergaenzen: Teamer mit gesetztem teamer_since (Startjahr wirksam), Teamer mit teamer_since vor/nach Aktivitaetsjahren, Teamer mit teamer_since=NULL (Fallback).
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/routes/badges.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "user_role_history" backend/routes/badges.js liefert 0 Treffer (toter Pfad entfernt).
    - grep -n "teamer_since" backend/routes/badges.js zeigt die neue Startjahr-Query im teamer_year-case.
    - vitest: teamer_year-Tests gruen (Startjahr ab teamer_since, NULL-Fallback auf aelteste Aktivitaet).
    - Bestehende teamer_year-Tests weiterhin gruen.
  </acceptance_criteria>
  <done>teamer_year nutzt users.teamer_since als Startjahr mit NULL-Fallback; user_role_history-Referenz entfernt; Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Konfi/Teamer -> GET-Endpoints | Badge-Daten werden serverseitig pro user_id + organization_id berechnet |
| Cron/BackgroundService -> checkAndAwardBadges | automatische Wertung alle 5 Min, keine User-Eingabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | mandatory_event_count-Query | mitigate | Zaehlung serverseitig mit eb.organization_id-Filter (Mandanten-Trennung), userId aus Token; keine Client-Werte uebernommen |
| T-116-02 | Elevation | target_role-Trennung | accept | Konfi-Branch wertet nur target_role='konfi', Teamer-Branch nur 'teamer' — bestehende, getestete Trennung; neuer Typ reiht sich ein |
| T-116-03 | Information Disclosure | teamer_since-Query | accept | teamer_since ist nicht-sensibles Datum, nur fuer eigenen User gelesen |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete in diesem Plan — reiner Code-Plan, kein Install |
</threat_model>

<verification>
- npx vitest run --config tests/vitest.config.ts tests/routes/badges.test.js -> alle gruen
- grep -c "mandatory_event_count" backend/routes/badges.js >= 2
- grep "user_role_history" backend/routes/badges.js -> kein Treffer
- grep "teamer_since" backend/routes/badges.js -> Treffer im teamer_year-case
</verification>

<success_criteria>
- mandatory_event_count gewertet: nur besuchte Pflicht-Events, absolute Anzahl, kein Entzug
- teamer_year-Startjahr aus teamer_since mit NULL-Fallback
- Konsistenz-Vertrag-Query dokumentiert und identisch zur Plan-02-Progress-Query
- Tests mitgeschrieben und gruen, keine Regression
</success_criteria>

<output>
Create `.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-01-SUMMARY.md` when done. Dokumentiere die exakte mandatory_event_count-Zaehl-Query (Konsistenz-Vertrag fuer Plan 02) und die teamer_since-Startjahr-Logik.
</output>
