---
phase: 116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi.js
  - backend/routes/teamer.js
  - backend/tests/routes/konfi.test.js
  - backend/tests/routes/teamer.test.js
autonomous: true
requirements: [D-10, D-11, D-12, D-01]
must_haves:
  truths:
    - "Ein Konfi sieht fuer event_count-Badges einen Fortschritt > 0%, wenn er Events besucht hat"
    - "Ein Konfi sieht fuer mandatory_event_count-Badges einen Fortschritt, der mit der Badge-Wertung uebereinstimmt"
    - "specific_activity- und activity_combination-Badges zeigen korrekten Fortschritt (Extra-Feld-Mismatch behoben)"
    - "Ein Teamer sieht fuer teamer_year-Badges einen Fortschritt, der das Startjahr aus teamer_since beruecksichtigt"
  artifacts:
    - path: "backend/routes/konfi.js"
      provides: "Progress-Cases event_count, teamer_year, mandatory_event_count + Extra-Feld-Fix specific_activity/activity_combination"
      contains: "mandatory_event_count"
    - path: "backend/routes/teamer.js"
      provides: "teamer_year-Progress mit Startjahr-Filter aus teamer_since (Konsistenz zur Wertung)"
  key_links:
    - from: "GET /konfi/badges Progress-switch"
      to: "event_bookings JOIN events WHERE e.mandatory=true (mandatory_event_count)"
      via: "neuer case identisch zur Wertung in badges.js"
      pattern: "mandatory_event_count"
    - from: "konfi.js specific_activity-Progress"
      to: "criteria_extra.required_activity_name (statt activity_id)"
      via: "Extra-Feld-Fix"
      pattern: "required_activity_name"
---

<objective>
Prozent-Bug der Fortschrittsanzeige beheben (volle D-10/D-11/D-12-Abdeckung):
1. Fehlende Progress-Cases in GET /konfi/badges ergaenzen: `event_count`, `teamer_year`, `mandatory_event_count`.
2. Extra-Feld-Mismatch beheben: specific_activity (nutzt aktuell activity_id) und activity_combination (nutzt activity_ids) auf required_activity_name / required_activities umstellen — dieselben Schluessel wie die Wertung.
3. Teamer-Progress (teamer.js): teamer_year-Fortschritt mit demselben Startjahr-Filter (teamer_since) wie die Wertung berechnen.

Purpose: Badges zeigen real existierenden Fortschritt statt dauerhaft "0% - In Arbeit". Neue und bestehende Typen werden konsistent dargestellt.
Output: Korrigierte Progress-Berechnung in konfi.js und teamer.js plus Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-CONTEXT.md
@.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-AUDIT.md

<interfaces>
<!-- Zentrale Stellen (bereits in context geladen, hier zur Orientierung) -->

konfi.js: GET /konfi/badges Progress-switch :921-1136. Init progress = { current: 0, target: criteria_value||1, percentage: 0 } :918. Abschluss percentage = Math.min((current/target)*100, 100) :1138.
  Vorhandene Cases: total_points, gottesdienst_points, gemeinde_points, both_categories, activity_count, unique_activities, specific_activity (:987, nutzt FALSCH extraData.activity_id :992), category_activities, activity_combination (:1034, nutzt FALSCH extraData.activity_ids :1039), bonus_points, streak, time_based.
  FEHLEND: event_count, teamer_year, mandatory_event_count.

badges.js Wertung (Quelle der Wahrheit fuer die Schluessel):
  specific_activity nutzt criteria.required_activity_name + JOIN activities a ON ... a.name = $2 (badges.js:205-210).
  activity_combination nutzt criteria.required_activities (Namen) gegen completedActivityNames (badges.js:215-219).
  event_count nutzt COUNT event_bookings WHERE attendance_status='present' (badges.js:160,271).

teamer.js: GET /badges Progress :257-373. activeYears-Berechnung :302-313 (zaehlt AKTUELL ALLE aktiven Jahre OHNE Startjahr-Filter). teamer_year-case :346 setzt progressPoints = activeYears.

KONSISTENZ-VERTRAG (identische Query wie Plan 01 badges.js mandatory_event_count-Wertung):
  SELECT COUNT(*) FROM event_bookings eb JOIN events e ON eb.event_id = e.id
  WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND e.mandatory = true AND eb.organization_id = $2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fehlende Progress-Cases + Extra-Feld-Fix in GET /konfi/badges</name>
  <files>backend/routes/konfi.js, backend/tests/routes/konfi.test.js</files>
  <read_first>
    - backend/routes/konfi.js:882-1144 (GET /konfi/badges komplette Progress-Berechnung)
    - backend/routes/konfi.js:987-1006 (specific_activity-Progress, FALSCHER Schluessel activity_id)
    - backend/routes/konfi.js:1034-1056 (activity_combination-Progress, FALSCHER Schluessel activity_ids)
    - backend/routes/badges.js:204-219 (Wertung — korrekte Schluessel required_activity_name / required_activities)
    - backend/routes/badges.js:160,271 (event_count-Wertung als Vorbild)
    - .planning/phases/116-.../116-AUDIT.md Abschnitt 5 (Prozent-Bug, Hypothesen A+B)
    - .planning/phases/116-.../116-CONTEXT.md D-10, D-11
  </read_first>
  <behavior>
    - Test: Konfi mit 4 besuchten Events + event_count-Badge value=6 -> progress.current=4, percentage ca. 66 (vorher 0).
    - Test: Konfi mit 3 besuchten Pflicht-Events + mandatory_event_count-Badge value=12 -> progress.current=3, percentage=25; identisch zur Wertungs-Zaehlung (Nicht-Pflicht zaehlt nicht).
    - Test: Konfi mit 2x Aktivitaet "Sonntagsgottesdienst" + specific_activity-Badge (criteria_extra.required_activity_name='Sonntagsgottesdienst', value=5) -> progress.current=2 (vorher 0 wegen activity_id-Mismatch).
    - Test: activity_combination-Badge mit required_activities=['A','B'], Konfi hat A erledigt -> progress.current=1 (vorher 0).
  </behavior>
  <action>
    Im Progress-switch (konfi.js:921-1136) drei neue Cases ergaenzen:
    - `case 'event_count':` COUNT aus event_bookings WHERE user_id=$1 AND attendance_status='present' AND organization_id=$2 -> progress.current. (Vorbild badges.js:160.)
    - `case 'mandatory_event_count':` die KONSISTENZ-VERTRAG-Query verwenden (IDENTISCH zur Wertung in Plan 01 badges.js): SELECT COUNT(*) FROM event_bookings eb JOIN events e ON eb.event_id = e.id WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND e.mandatory = true AND eb.organization_id = $2 -> progress.current. Diese MUSS exakt dasselbe Ergebnis liefern wie der Wertungs-case, sonst entsteht ein neuer Wertung/Progress-Mismatch.
    - `case 'teamer_year':` GET /konfi/badges liefert nur target_role='konfi' (konfi.js:906), daher kommen hier praktisch keine teamer_year-Badges an. Trotzdem defensiv progress.current=0 setzen ODER den Case weglassen; der echte teamer_year-Progress liegt im Teamer-Endpoint (Task 2, teamer.js). Im Kommentar vermerken, dass teamer_year fuer Konfis nicht zutrifft (Verweis Task 2).

    Extra-Feld-Mismatch beheben (D-11):
    - specific_activity (konfi.js:987-1006): statt extraData.activity_id den Schluessel extraData.required_activity_name verwenden und die Query auf JOIN activities a ON ua.activity_id = a.id WHERE a.name = $2 umstellen (analog Wertung badges.js:209). Damit zaehlt der Progress nach Aktivitaets-Name wie die Wertung.
    - activity_combination (konfi.js:1034-1056): statt extraData.activity_ids den Schluessel extraData.required_activities (Namen) verwenden; COUNT DISTINCT der erledigten Aktivitaeten, deren Name in required_activities liegt (analog Wertung badges.js:216-217, ueber JOIN activities a ON ... a.name IN (...)).

    Tests in konfi.test.js nach bestehendem Muster: Konfi + Events/Aktivitaeten anlegen, GET /konfi/badges aufrufen, badge.progress.current und percentage pruefen. Insbesondere ein mandatory_event_count-Progress-Test, der GEGEN die Wertung gegenprueft (gleicher Konfi -> Progress.current == Wertungs-COUNT).
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/routes/konfi.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "case 'event_count'" backend/routes/konfi.js liefert Treffer im Progress-switch.
    - grep -n "case 'mandatory_event_count'" backend/routes/konfi.js liefert Treffer; zugehoerige Query enthaelt "e.mandatory = true" UND "attendance_status = 'present'".
    - grep -n "required_activity_name" backend/routes/konfi.js zeigt Verwendung im specific_activity-Progress; "activity_id" als Extra-Feld dort entfernt.
    - grep -n "required_activities" backend/routes/konfi.js zeigt Verwendung im activity_combination-Progress.
    - vitest konfi.test.js: event_count/mandatory_event_count/specific_activity/activity_combination-Progress-Tests gruen, percentage != 0 fuer Test-Konfi mit Fortschritt.
  </acceptance_criteria>
  <done>Progress-Cases event_count + mandatory_event_count ergaenzt (mandatory identisch zur Wertung), Extra-Feld-Mismatch behoben, Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: teamer_year-Progress mit Startjahr-Filter (teamer.js)</name>
  <files>backend/routes/teamer.js, backend/tests/routes/teamer.test.js</files>
  <read_first>
    - backend/routes/teamer.js:257-373 (GET /badges Teamer-Progress-Endpoint)
    - backend/routes/teamer.js:302-313 (activeYears-Query OHNE Startjahr-Filter)
    - backend/routes/teamer.js:346-348 (teamer_year-case nutzt activeYears)
    - backend/routes/badges.js:405-466 (Wertung — Startjahr-Logik teamer_since, nach Plan 01 Task 2)
    - .planning/phases/116-.../116-CONTEXT.md D-12
  </read_first>
  <behavior>
    - Test: Teamer mit teamer_since='2026-01-01', Aktivitaet in 2024 + 2026 -> teamer_year-Progress current=1 (nur 2026 zaehlt, 2024 vor Startjahr), NICHT 2.
    - Test: Teamer mit teamer_since='2024-01-01', Aktivitaeten 2024+2026 -> current=2.
    - Test: Teamer mit teamer_since=NULL -> Fallback (aelteste Teamer-Aktivitaet als Startjahr), alle aktiven Jahre ab dann.
  </behavior>
  <action>
    Den teamer_year-Progress (teamer.js:346) konsistent zur Wertung machen (D-12): Aktuell zaehlt activeYears (teamer.js:302-313) ALLE aktiven Jahre ohne Startjahr-Begrenzung. Ergaenze die gleiche Startjahr-Logik wie die Wertung (badges.js teamer_year nach Plan 01 Task 2): Startjahr = YEAR(users.teamer_since); bei NULL Fallback auf aelteste Teamer-Aktivitaet. Filtere die aktiven Jahre auf year >= startYear, bevor gezaehlt wird. Setze progressPoints im teamer_year-case auf die Anzahl der so gefilterten Jahre. Die activeYears-Quell-Query (UNION user_activities target_role='teamer' + event_bookings present) bleibt; nur der Startjahr-Filter kommt hinzu. So ist der Progress identisch zur Wertung (kein neuer Mismatch).

    Tests in teamer.test.js: Teamer mit teamer_since vor/nach Aktivitaetsjahren -> Progress beruecksichtigt Startjahr; NULL-Fallback.
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/routes/teamer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "teamer_since" backend/routes/teamer.js zeigt Startjahr-Nutzung im Progress-Pfad.
    - teamer_year-Progress zaehlt nur Jahre >= Startjahr (Test mit teamer_since='2026' + Aktivitaet 2024 -> current=1).
    - vitest teamer.test.js: teamer_year-Progress-Tests gruen, keine Regression bei activity_count/event_count-Progress.
  </acceptance_criteria>
  <done>teamer_year-Progress beruecksichtigt das Startjahr aus teamer_since (mit NULL-Fallback), konsistent zur Wertung; Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Konfi -> GET /konfi/badges | Progress serverseitig pro user_id + organization_id |
| Teamer -> GET /badges (teamer.js) | Progress serverseitig pro user_id + organization_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-04 | Tampering | mandatory_event_count-Progress-Query | mitigate | organization_id-Filter + userId aus Token; identisch zur Wertung, keine Client-Werte |
| T-116-05 | Information Disclosure | Progress nur fuer eigenen User | accept | konfi.js filtert auf konfiId aus Token, teamer.js auf userId aus Token |
| T-116-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete — reiner Code-Plan, kein Install |
</threat_model>

<verification>
- npx vitest run --config tests/vitest.config.ts tests/routes/konfi.test.js -> gruen
- npx vitest run --config tests/vitest.config.ts tests/routes/teamer.test.js -> gruen
- grep "case 'event_count'" + "case 'mandatory_event_count'" in konfi.js -> Treffer
- grep "required_activity_name" + "required_activities" in konfi.js Progress -> Treffer
- mandatory_event_count-Progress.current == Wertungs-COUNT fuer denselben Test-Konfi (Konsistenz)
</verification>

<success_criteria>
- Progress-Cases event_count + mandatory_event_count ergaenzt; mandatory identisch zur Wertung (Plan 01)
- Extra-Feld-Mismatch specific_activity/activity_combination behoben
- teamer_year-Progress mit Startjahr-Filter (teamer_since), konsistent zur Wertung
- Tests mitgeschrieben und gruen, keine Regression
</success_criteria>

<output>
Create `.planning/phases/116-badge-system-audit-pflicht-anwesenheits-badges-anzahl-teamer/116-02-SUMMARY.md` when done. Bestaetige explizit, dass die mandatory_event_count-Progress-Query Byte-fuer-Byte der Wertungs-Query aus Plan 01 entspricht.
</output>
