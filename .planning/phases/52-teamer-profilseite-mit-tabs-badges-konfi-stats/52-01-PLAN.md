---
phase: 52-teamer-profilseite-mit-tabs-badges-konfi-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/teamer.js
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
autonomous: true
requirements:
  - PRF-02
  - PRF-03
  - TMR-01
must_haves:
  truths:
    - "Teamer sieht zentrierten Avatar mit Initialen, Name, role_title und Info-Chips (E-Mail, Teamer seit) im Rose/Rot-Gradient-Header"
    - "Teamer kann Funktionsbeschreibung, E-Mail und Passwort ueber klickbare Einstellungs-Items aendern"
    - "Teamer sieht eigene Teamer-Badges im 3-Spalten-Grid mit Fortschritt und Popover-Details"
    - "Ehemaliger Konfi sieht eingefrorene Konfi-Badges im gleichen Grid-Look"
    - "Ehemaliger Konfi sieht Konfi-Punkte-Zusammenfassung und kann PointsHistoryModal oeffnen"
    - "Logout-Button steht ganz unten auf der Seite"
  artifacts:
    - path: "backend/routes/teamer.js"
      provides: "Erweiterter GET /teamer/profile mit email, role_title, teamer_since"
      contains: "role_title"
    - path: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      provides: "Vollstaendige Teamer-Profilseite mit Header, Settings, Badges, Stats, Logout"
      min_lines: 200
  key_links:
    - from: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      to: "/teamer/profile"
      via: "api.get in useEffect/useCallback"
      pattern: "api\\.get.*teamer/profile"
    - from: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      to: "/teamer/badges"
      via: "api.get fuer Teamer-Badges"
      pattern: "api\\.get.*teamer/badges"
    - from: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      to: "ChangeEmailModal, ChangePasswordModal, ChangeRoleTitleModal"
      via: "useIonModal Hook"
      pattern: "useIonModal"
---

<objective>
Teamer-Profilseite vollstaendig ueberarbeiten: AdminProfilePage-Layout mit Rose/Rot-Gradient, vertikal gestapelte Sektionen (Header, Einstellungen, Teamer-Badges, Konfi-Badges, Konfi-Stats, Logout). Backend erweitern um fehlende Felder (email, role_title, teamer_since).

Purpose: Teamer bekommt eine professionelle, vollstaendige Profilseite analog zur Admin-Profilseite
Output: Ueberarbeitete TeamerProfilePage.tsx + erweitertes Backend-Endpoint
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-teamer-profilseite-mit-tabs-badges-konfi-stats/52-CONTEXT.md

@frontend/src/components/admin/pages/AdminProfilePage.tsx
@frontend/src/components/konfi/views/BadgesView.tsx
@frontend/src/components/konfi/modals/PointsHistoryModal.tsx
@frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx
@frontend/src/components/konfi/modals/ChangeEmailModal.tsx
@frontend/src/components/konfi/modals/ChangePasswordModal.tsx
@frontend/src/theme/variables.css
@backend/routes/teamer.js

<interfaces>
<!-- Backend GET /teamer/profile aktuell: -->
Response: {
  user: { display_name, username, organization_name },
  konfi_data: { gottesdienst_points, gemeinde_points, jahrgang_name, badges: [{badge_id, name, icon, color, awarded_date}] }
}

<!-- Backend GET /teamer/badges: -->
Response: Array<{ id, name, description, icon, criteria_type, criteria_value, criteria_extra, is_hidden, is_active, color, earned (boolean), awarded_date, progress_points, progress_percentage }>

<!-- GET /auth/me: -->
Response: { id, username, display_name, email, role_title, role_name, role_display_name }

<!-- ChangeRoleTitleModal Props: -->
{ onClose: () => void, onSuccess: () => void, initialRoleTitle?: string }

<!-- ChangeEmailModal Props (konfi version): -->
{ onClose: () => void, onSuccess: () => void }

<!-- ChangePasswordModal Props (konfi version): -->
{ onClose: () => void, onSuccess: () => void }

<!-- PointsHistoryModal Props: -->
{ onClose: () => void, pointConfig?: { gottesdienst_enabled: boolean, gemeinde_enabled: boolean } }

<!-- BadgesView Props (Konfi): -->
{ badges: Badge[], badgeStats: { totalVisible, totalSecret }, selectedFilter: string, onFilterChange: (filter: string) => void }

<!-- CSS-Klassen (variables.css): -->
app-detail-header, app-detail-header__content, app-detail-header__title, app-detail-header__subtitle
app-detail-header__info-row, app-detail-header__info-chip
app-header-banner--teamer (Rose/Rot Gradient: #e11d48 -> #be123c)
app-icon-circle, app-icon-circle--users
app-list-item, app-list-item__row, app-list-item__main, app-list-item__content, app-list-item__title, app-list-item__meta
app-card, app-card-content, app-section-inset, app-empty-state
app-gradient-background, app-condense-toolbar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend GET /teamer/profile erweitern</name>
  <files>backend/routes/teamer.js</files>
  <read_first>
    - backend/routes/teamer.js (aktueller Endpoint, Zeilen 57-103)
    - backend/routes/auth.js (GET /auth/me als Referenz fuer Felder)
  </read_first>
  <action>
Im GET /teamer/profile Endpoint (Zeile 57-103 in teamer.js) die SQL-Query und Response erweitern:

1. In der Response `user` folgende Felder ergaenzen: `email`, `role_title`, `teamer_since`
2. Die existierende Query nutzt `req.user` direkt fuer display_name/username/organization_name. Stattdessen eine DB-Query ausfuehren die diese Felder aus der users-Tabelle laedt:

```sql
SELECT u.display_name, u.username, u.email, u.role_title, u.teamer_since,
       o.name as organization_name
FROM users u
LEFT JOIN organizations o ON u.organization_id = o.id
WHERE u.id = $1
```

3. Response-Objekt erweitern:
```javascript
user: {
  display_name: userData.display_name,
  username: userData.username,
  email: userData.email || '',
  role_title: userData.role_title || '',
  teamer_since: userData.teamer_since || null,
  organization_name: userData.organization_name || ''
}
```

4. Der Rest (konfi_data mit gottesdienst_points, gemeinde_points, jahrgang_name, badges) bleibt unveraendert.

WICHTIG: `teamer_since` Spalte existiert bereits (Migration in Zeile 40-42 der gleichen Datei). `role_title` existiert bereits auf users-Tabelle (GET /auth/me nutzt es).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "role_title\|teamer_since\|email" backend/routes/teamer.js | head -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/routes/teamer.js GET /profile Query enthaelt `u.email, u.role_title, u.teamer_since`
    - backend/routes/teamer.js Response-Objekt user enthaelt Schluessel `email`, `role_title`, `teamer_since`
    - Bestehende konfi_data Response bleibt unveraendert (gottesdienst_points, gemeinde_points, jahrgang_name, badges)
  </acceptance_criteria>
  <done>GET /teamer/profile liefert email, role_title und teamer_since im user-Objekt zusaetzlich zu den bestehenden Feldern</done>
</task>

<task type="auto">
  <name>Task 2: TeamerProfilePage komplett ueberarbeiten</name>
  <files>frontend/src/components/teamer/pages/TeamerProfilePage.tsx</files>
  <read_first>
    - frontend/src/components/teamer/pages/TeamerProfilePage.tsx (aktuelle Implementierung)
    - frontend/src/components/admin/pages/AdminProfilePage.tsx (Referenz fuer Header + Settings Pattern)
    - frontend/src/components/konfi/views/BadgesView.tsx (Referenz fuer Badge-Grid-Look, BADGE_ICONS Map, getIconFromString, getBadgeColor, Popover)
    - frontend/src/components/konfi/modals/PointsHistoryModal.tsx (Props-Interface)
    - frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx (Props-Interface)
    - frontend/src/theme/variables.css (CSS-Klassen)
  </read_first>
  <action>
TeamerProfilePage.tsx komplett neu schreiben. Folgende Sektionen von oben nach unten:

**1. Imports und Interfaces:**
- Ionic: IonPage, IonHeader, IonToolbar, IonTitle, IonContent, IonRefresher, IonRefresherContent, IonList, IonListHeader, IonItem, IonLabel, IonIcon, IonCard, IonCardContent, IonButton, IonButtons, useIonModal, useIonAlert, useIonPopover
- ionicons: personOutline, mailOutline, keyOutline, briefcaseOutline, calendarOutline, settingsOutline, trophy, trophyOutline, ribbon, star, logOutOutline, flashOutline, informationCircleOutline + ALLE Badge-Icons aus BadgesView (checkmarkCircle, diamond, shield, flame, flash, rocket, sparkles, etc.)
- Contexts: useApp, useModalPage
- Services: api, logout (from '../../../services/auth')
- Modals: ChangeEmailModal aus '../../konfi/modals/ChangeEmailModal', ChangePasswordModal aus '../../konfi/modals/ChangePasswordModal', ChangeRoleTitleModal aus '../../admin/modals/ChangeRoleTitleModal', PointsHistoryModal aus '../../konfi/modals/PointsHistoryModal'
- LoadingSpinner aus '../../common/LoadingSpinner'

- Interface TeamerProfile erweitern:
```typescript
interface TeamerProfile {
  user: {
    display_name: string;
    username: string;
    email: string;
    role_title: string;
    teamer_since: string | null;
    organization_name: string;
  };
  konfi_data: {
    gottesdienst_points: number;
    gemeinde_points: number;
    jahrgang_name: string;
    badges: Array<{ badge_id: number; name: string; icon: string; color: string; awarded_date: string }>;
  };
}
```

- Interface TeamerBadge (fuer /teamer/badges):
```typescript
interface TeamerBadge {
  id: number; name: string; description?: string; icon: string;
  criteria_type: string; criteria_value: number; criteria_extra?: string;
  is_hidden: boolean; is_active: boolean; color?: string;
  earned: boolean; awarded_date?: string;
  progress_points?: number; progress_percentage?: number;
}
```

**2. BADGE_ICONS Map:** Kopiere die komplette BADGE_ICONS Map und getIconFromString Funktion aus BadgesView.tsx (Zeilen 78-136). Diese wird fuer die Badge-Grid-Darstellung benoetigt.

**3. BadgePopoverContent Komponente:** Kopiere aus BadgesView.tsx (Zeilen 156-276) und passe an: Gleiche Popover-Darstellung mit Badge-Details, Earned/Progress/Locked Status.

**4. Component Body:**
- `useModalPage('profile')` fuer pageRef und presentingElement
- `useApp()` fuer user, setUser, setSuccess, setError
- `useIonAlert()` fuer Logout-Bestaetigung
- State: profile (TeamerProfile | null), teamerBadges (TeamerBadge[]), loading, selectedBadgeFilter ('alle' | 'nicht_erhalten' | 'in_arbeit')
- `loadProfile` via useCallback: `api.get('/teamer/profile')` + `api.get('/teamer/badges')` parallel mit Promise.all
- useEffect ruft loadProfile auf
- Alle 3 Modals via useIonModal Hook (wie in AdminProfilePage):
  - ChangeEmailModal: onClose/onSuccess (bei Success: loadProfile aufrufen)
  - ChangePasswordModal: onClose/onSuccess
  - ChangeRoleTitleModal: onClose/onSuccess (bei Success: loadProfile aufrufen), initialRoleTitle: profile?.user.role_title || ''
- PointsHistoryModal via useIonModal: onClose, pointConfig: { gottesdienst_enabled: true, gemeinde_enabled: true }
- useIonPopover fuer BadgePopoverContent (wie in BadgesView)
- Logout-Handler mit useIonAlert (wie aktuell, Header: 'Abmelden', destructive button)

**5. JSX Struktur:**

```
<IonPage ref={pageRef}>
  <IonHeader translucent>
    <IonToolbar><IonTitle>Profil</IonTitle></IonToolbar>
  </IonHeader>
  <IonContent className="app-gradient-background" fullscreen>
    <IonHeader collapse="condense">
      <IonToolbar className="app-condense-toolbar">
        <IonTitle size="large">Profil</IonTitle>
      </IonToolbar>
    </IonHeader>
    <IonRefresher ... onIonRefresh={loadProfile + complete}>

    {/* 1. Detail-Header - wie AdminProfilePage */}
    <div className="app-detail-header" style={{
      background: 'linear-gradient(135deg, #e11d48 0%, #be123c 100%)',
      boxShadow: '0 20px 40px rgba(225, 29, 72, 0.3)'
    }}>
      <div className="app-detail-header__content" style={{ padding: '70px 24px 24px 24px', alignItems: 'center', textAlign: 'center' }}>
        <div className="app-icon-circle" style={{
          width: '80px', height: '80px',
          background: 'rgba(255,255,255,0.2)',
          marginBottom: '16px', color: 'white', fontSize: '2rem', fontWeight: '600',
          border: '3px solid rgba(255,255,255,0.3)'
        }}>
          {profile.user.display_name?.charAt(0)?.toUpperCase() || 'T'}
        </div>
        <h1 className="app-detail-header__title">{profile.user.display_name}</h1>
        <p className="app-detail-header__subtitle">
          {profile.user.role_title || 'Teamer:in'}
        </p>
        <div className="app-detail-header__info-row" style={{ justifyContent: 'center' }}>
          {profile.user.email && (
            <div className="app-detail-header__info-chip">
              <IonIcon icon={mailOutline} style={{ fontSize: '0.85rem' }} />
              {profile.user.email}
            </div>
          )}
          {profile.user.teamer_since && (
            <div className="app-detail-header__info-chip">
              <IonIcon icon={calendarOutline} style={{ fontSize: '0.85rem' }} />
              Teamer:in seit {new Date(profile.user.teamer_since).toLocaleDateString('de-DE', { day: 'numeric', month: 'short', year: 'numeric' })}
            </div>
          )}
        </div>
      </div>
    </div>

    {/* 2. Konto-Einstellungen - exakt wie AdminProfilePage IonList Pattern */}
    IonList inset mit IonListHeader (settingsOutline Icon, "Konto-Einstellungen")
    IonCard app-card mit 3 IonItems:
      - Funktionsbeschreibung (briefcaseOutline): onClick -> presentRoleTitleModal, Meta: role_title oder 'z.B. Jugendleiter:in'
      - E-Mail aendern (mailOutline): onClick -> presentEmailModal, Meta: aktuelle E-Mail
      - Passwort aendern (keyOutline): onClick -> presentPasswordModal, Meta: 'Sicherheitseinstellungen'
    Jedes Item nutzt das app-list-item Pattern aus AdminProfilePage (gleiche Struktur mit app-list-item__row, __main, app-icon-circle, __content, __title, __meta)

    {/* 3. Teamer-Badges - Konfi-BadgesView-Look */}
    IonList inset mit IonListHeader (ribbon Icon, violetter Hintergrund #5b21b6, "Teamer-Badges ({earned count})")

    Filter-Segment (Alle / Offen / In Arbeit) -- genau wie in BadgesView

    Badge-Grid: Badges nach criteria_type gruppieren (gleiche Kategorien wie BadgesView aber fuer Teamer-Badge-Typen: activity_count, event_count, streak, collection, yearly, category_activities).
    Fuer jede Kategorie: IonCard mit Category-Header (Icon + Titel + earned/total + Progress-Circle SVG) und darunter 3-Spalten-Grid mit Badge-Icons (earned = farbig mit Checkmark, nicht earned = grau mit Lock, in_progress = grau mit Progress-Ring). Badge-Klick oeffnet Popover.

    Wenn keine Teamer-Badges vorhanden: EmptyState "Noch keine Teamer-Badges" mit trophyOutline Icon.

    {/* 4. Konfi-Badges - nur wenn vorhanden */}
    Nur rendern wenn profile.konfi_data.badges.length > 0:
    IonList inset mit IonListHeader (trophy Icon, gold #f59e0b, "Konfi-Badges ({count})")
    Einfache Badge-Liste (wie aktuell in TeamerProfilePage): Jeder Badge als app-list-item mit farbigem app-icon-circle, Badge-Name, Datum.
    (Konfi-Badges sind bereits erreicht und haben keine Fortschritts-Logik, daher einfache Liste statt Grid)

    {/* 5. Konfi-Stats - nur wenn Daten vorhanden */}
    Nur rendern wenn profile.konfi_data.jahrgang_name nicht leer:
    IonList inset mit IonListHeader (flashOutline Icon, "Konfi-Punkte")
    IonCard app-card mit:
      - Jahrgang-Zeile: Label + Wert rechts
      - Gottesdienst-Punkte-Zeile: Label + Wert rechts (fontWeight 600)
      - Gemeinde-Punkte-Zeile: Label + Wert rechts (fontWeight 600)
      - "Details anzeigen"-Button (IonButton fill="clear"): onClick -> presentPointsHistoryModal

    {/* 6. Logout-Button */}
    div padding 0 16px, marginTop 16px:
    IonButton expand="block" fill="outline" color="danger" onClick={handleLogout}
    style height 48px, borderRadius 12px, fontWeight 600
    IonIcon logOutOutline slot="start", Text "Abmelden"

    div height 32px (Spacer)
  </IonContent>
</IonPage>
```

**Badge-Grid Helper-Funktionen (im Component):**
- `getBadgeColor(badge)`: Wenn badge.color vorhanden, nutze es. Sonst Standard #e11d48 (Teamer-Farbe).
- `getTeamerBadgeCategories()`: Gruppiert teamerBadges nach criteria_type. Mapping:
  - activity_count -> "Aktivitaeten" (checkmarkCircle, #3880ff)
  - event_count -> "Event-Champion" (calendarOutline, #e63946)
  - streak -> "Serien-Champion" (flame, #eb445a)
  - collection -> "Sammler" (trophy, #ffd700)
  - yearly -> "Jahres-Badges" (calendarOutline, #8e8e93)
  - category_activities -> "Kategorie-Meister" (cubeOutline, #0cd1e8)
  - Fallback fuer unbekannte Typen: "Weitere" (star, #667eea)
- Filter-Logik: 'alle' zeigt alle, 'nicht_erhalten' zeigt !earned, 'in_arbeit' zeigt !earned && progress_percentage > 0

WICHTIG:
- KEINE Unicode Emojis!
- useIonModal fuer ALLE Modals (NIEMALS IonModal isOpen)
- Echte Umlaute in allen Strings (ae->ae ist ok in Variablennamen, aber UI-Texte muessen aeoeue haben)
- app-list-item CSS Pattern verwenden, NICHT eigene Styles
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "useIonModal" frontend/src/components/teamer/pages/TeamerProfilePage.tsx && grep -c "app-detail-header" frontend/src/components/teamer/pages/TeamerProfilePage.tsx && grep "ChangeRoleTitleModal\|ChangeEmailModal\|ChangePasswordModal\|PointsHistoryModal" frontend/src/components/teamer/pages/TeamerProfilePage.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - TeamerProfilePage.tsx enthaelt mindestens 4x useIonModal (Email, Password, RoleTitle, PointsHistory)
    - TeamerProfilePage.tsx enthaelt "app-detail-header" (AdminProfilePage Header-Pattern)
    - TeamerProfilePage.tsx enthaelt "linear-gradient(135deg, #e11d48" (Teamer Rose/Rot Gradient)
    - TeamerProfilePage.tsx enthaelt "ChangeRoleTitleModal" Import
    - TeamerProfilePage.tsx enthaelt "ChangeEmailModal" Import aus konfi/modals
    - TeamerProfilePage.tsx enthaelt "ChangePasswordModal" Import aus konfi/modals
    - TeamerProfilePage.tsx enthaelt "PointsHistoryModal" Import
    - TeamerProfilePage.tsx enthaelt "api.get('/teamer/badges')" fuer Teamer-Badge-Laden
    - TeamerProfilePage.tsx enthaelt "api.get('/teamer/profile')" fuer Profil-Laden
    - TeamerProfilePage.tsx enthaelt KEIN "TeamerBadgesView" Import (NICHT den alten Grid-View nutzen)
    - TeamerProfilePage.tsx enthaelt "gridTemplateColumns: 'repeat(3, 1fr)'" fuer 3-Spalten Badge-Grid
    - TeamerProfilePage.tsx enthaelt Logout-Button mit "logOutOutline" Icon
    - TeamerProfilePage.tsx enthaelt KEINE Unicode Emojis
    - TeamerProfilePage.tsx hat mindestens 200 Zeilen
  </acceptance_criteria>
  <done>TeamerProfilePage zeigt vollstaendige Profilseite mit Detail-Header (Rose/Rot Gradient, Avatar, Name, role_title, Info-Chips), Konto-Einstellungen (3 klickbare Items mit Modals), Teamer-Badges im 3-Spalten-Grid mit Popover, Konfi-Badges (conditional), Konfi-Stats mit PointsHistory-Modal (conditional), Logout-Button</done>
</task>

</tasks>

<verification>
1. Backend: GET /teamer/profile liefert email, role_title, teamer_since im user-Objekt
2. Frontend: TeamerProfilePage rendert alle 6 Sektionen (Header, Settings, Teamer-Badges, Konfi-Badges, Konfi-Stats, Logout)
3. Alle Modals nutzen useIonModal Hook (kein IonModal isOpen)
4. Badge-Grid nutzt Konfi-BadgesView-Look (3 Spalten, Popover), NICHT TeamerBadgesView
5. Konfi-Badges und Konfi-Stats werden nur conditional gerendert (wenn Daten vorhanden)
6. Kein TeamerBadgesView Import mehr in TeamerProfilePage
7. Keine Unicode Emojis in der gesamten Datei
</verification>

<success_criteria>
- Teamer-Profilseite sieht aus wie AdminProfilePage (gleicher Header-Style, gleiche Settings-Liste)
- Teamer-Badges werden im Konfi-Badge-Grid-Look dargestellt (3 Spalten, Progress, Popover)
- Ehemalige Konfi-Daten werden conditional angezeigt
- Alle 4 Modals (RoleTitle, Email, Password, PointsHistory) funktionieren via useIonModal
- Pull-to-refresh aktualisiert alle Daten
</success_criteria>

<output>
After completion, create `.planning/phases/52-teamer-profilseite-mit-tabs-badges-konfi-stats/52-01-SUMMARY.md`
</output>
