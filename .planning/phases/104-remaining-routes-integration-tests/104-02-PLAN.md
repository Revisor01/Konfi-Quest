---
phase: 104-remaining-routes-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/routes/notifications.test.js
  - backend/tests/routes/organizations.test.js
  - backend/tests/routes/roles.test.js
  - backend/tests/routes/settings.test.js
  - backend/tests/routes/users.test.js
autonomous: true
requirements: [BIT-08]

must_haves:
  truths:
    - "Notifications Tests laufen gruen (Device-Token CRUD)"
    - "Organizations Tests laufen gruen (SuperAdmin CRUD + Stats)"
    - "Roles Tests laufen gruen (GET List + Assignable)"
    - "Settings Tests laufen gruen (GET + PUT)"
    - "Users Tests laufen gruen (CRUD + Jahrgang-Assignments + Password-Reset)"
  artifacts:
    - path: "backend/tests/routes/notifications.test.js"
      provides: "Notifications Route Integration-Tests"
    - path: "backend/tests/routes/organizations.test.js"
      provides: "Organizations Route Integration-Tests"
    - path: "backend/tests/routes/roles.test.js"
      provides: "Roles Route Integration-Tests"
    - path: "backend/tests/routes/settings.test.js"
      provides: "Settings Route Integration-Tests"
    - path: "backend/tests/routes/users.test.js"
      provides: "Users Route Integration-Tests"
  key_links:
    - from: "backend/tests/routes/organizations.test.js"
      to: "/api/organizations"
      via: "supertest"
    - from: "backend/tests/routes/users.test.js"
      to: "/api/admin/users"
      via: "supertest"
---

<objective>
Integration-Tests fuer Admin-Verwaltungs- und System-Routes: Notifications, Organizations, Roles, Settings, Users.

Purpose: Per D-06/D-07 — Admin-Verwaltung und System-Verwaltung. Organizations ist die komplexeste Route (622 Zeilen, SuperAdmin-only), Users die zweitkomplexeste (626 Zeilen, OrgAdmin-only mit Hierarchie-Checks).
Output: 5 Test-Dateien mit Happy-Path + Fehlerfall-Tests (per D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-remaining-routes-integration-tests/104-CONTEXT.md

@backend/tests/routes/activities.test.js (Referenz-Pattern)
@backend/tests/routes/rbac.test.js (Referenz fuer Rollen-Tests)
@backend/tests/helpers/testApp.js
@backend/tests/helpers/db.js
@backend/tests/helpers/seed.js
@backend/tests/helpers/auth.js

@backend/routes/notifications.js
@backend/routes/organizations.js
@backend/routes/roles.js
@backend/routes/settings.js
@backend/routes/users.js
@backend/createApp.js (Route-Mounting: /api/notifications, /api/organizations, /api/roles, /api/settings, /api/admin/users + /api/users)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notifications + Roles + Settings Integration-Tests</name>
  <files>backend/tests/routes/notifications.test.js, backend/tests/routes/roles.test.js, backend/tests/routes/settings.test.js</files>
  <read_first>backend/routes/notifications.js, backend/routes/roles.js, backend/routes/settings.js, backend/tests/routes/activities.test.js</read_first>
  <action>
Erstelle 3 Test-Dateien nach dem etablierten Pattern (per D-01, D-02, D-03, D-04):

**notifications.test.js** — Route: `/api/notifications`
- POST /device-token — Authentifizierter User speichert Device-Token (200), ohne Token 401, Validierungsfehler (fehlender token/platform)
- DELETE /device-token — User loescht Device-Token (200), Validierungsfehler (fehlende device_id)
- POST /test-push — Admin sendet Test-Push (200 oder graceful error bei fehlendem Firebase), Konfi bekommt 403
  HINWEIS: Firebase ist im Test nicht konfiguriert. Der Test-Push wird wahrscheinlich einen Fehler werfen — teste dass der Endpoint erreichbar ist und korrekt antwortet (kein 500 crash, sondern kontrollierter Fehler oder 200).

**roles.test.js** — Route: `/api/roles`
- GET / — OrgAdmin/SuperAdmin bekommt 200 + Rollen-Array mit user_count, Admin/Teamer/Konfi bekommt 403
- GET /:id — OrgAdmin bekommt 200 + einzelne Rolle, nicht-existierende ID (404)
- GET /list/assignable — OrgAdmin bekommt 200 + zuweisbare Rollen, Admin bekommt 403

**settings.test.js** — Route: `/api/settings`
- GET / — Jeder authentifizierte User bekommt 200 + Settings-Objekt, ohne Token 401
- PUT / — OrgAdmin aktualisiert Settings (200), Admin/Teamer/Konfi bekommt 403
- PUT / mit ungueltigen Werten (Validierungsfehler)
- Org-Isolation: Settings sind pro Organisation getrennt

Jeder Test nutzt beforeAll(getTestPool+getTestApp), beforeEach(truncateAll+seed), afterAll(closePool).
Tokens: generateToken fuer alle relevanten Rollen.
Per D-02: Echte JWT-Tokens, KEIN Mocking.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/notifications.test.js backend/tests/routes/roles.test.js backend/tests/routes/settings.test.js</automated>
  </verify>
  <acceptance_criteria>Alle 3 Test-Dateien existieren, jede hat Happy-Path + Fehlerfall Tests, alle gruen.</acceptance_criteria>
  <done>Notifications, Roles, Settings Tests laufen gruen mit mindestens je 3 Test-Cases.</done>
</task>

<task type="auto">
  <name>Task 2: Organizations + Users Integration-Tests</name>
  <files>backend/tests/routes/organizations.test.js, backend/tests/routes/users.test.js</files>
  <read_first>backend/routes/organizations.js, backend/routes/users.js, backend/tests/routes/activities.test.js, backend/utils/roleHierarchy.js</read_first>
  <action>
Erstelle 2 Test-Dateien fuer die komplexesten Admin-Routes (per D-01, D-02, D-03, D-04):

**organizations.test.js** — Route: `/api/organizations`
- GET / — SuperAdmin bekommt 200 + alle Orgs, OrgAdmin/Admin/Teamer/Konfi bekommt 403
- GET /:id — SuperAdmin sieht jede Org (200), OrgAdmin sieht nur eigene Org (200), fremde Org (403)
- GET /current — Jeder authentifizierte User bekommt eigene Org-Details (200)
- POST / — SuperAdmin erstellt neue Org mit Admin-User (201/200), OrgAdmin bekommt 403, Validierungsfehler (fehlender name/slug)
- PUT /:id — SuperAdmin/OrgAdmin aktualisiert Org (200), OrgAdmin nur eigene Org
- DELETE /:id — SuperAdmin loescht Org (200), OrgAdmin bekommt 403
- GET /:id/users — SuperAdmin/OrgAdmin sieht User-Liste (200)
- GET /:id/admins — SuperAdmin/OrgAdmin sieht Admin-Liste (200)
- POST /:id/admins — SuperAdmin erstellt Admin fuer Org (200), Validierungsfehler
- GET /:id/stats — SuperAdmin/OrgAdmin bekommt Org-Statistiken (200)

**users.test.js** — Route: `/api/admin/users` (auch `/api/users` — doppelt gemountet)
- GET / — OrgAdmin bekommt 200 + User-Liste der eigenen Org, Admin/Teamer/Konfi bekommt 403
- GET /:id — OrgAdmin bekommt 200 + User-Details, Hierarchie-Check (OrgAdmin kann keinen anderen OrgAdmin sehen falls Hierarchie das verhindert)
- POST / — OrgAdmin erstellt User (200/201), Validierungsfehler (fehlender username/password), Duplikat-Username
- PUT /:id — OrgAdmin aktualisiert User (200), Hierarchie-Check
- DELETE /:id — OrgAdmin loescht User (200), Hierarchie-Check, nicht-existierende ID (404)
- POST /:id/jahrgaenge — OrgAdmin weist User Jahrgaenge zu (200)
- GET /me/jahrgaenge — Authentifizierter User sieht eigene Jahrgaenge (200)
- GET /:id/jahrgaenge — OrgAdmin sieht User-Jahrgaenge (200)
- PUT /:id/reset-password — OrgAdmin/SuperAdmin setzt Passwort zurueck (200)
- Org-Isolation: OrgAdmin2 kann keinen User aus Org 1 sehen/bearbeiten

Tokens: generateToken fuer alle 5 Rollen + admin2/orgAdmin2 fuer Org-Isolation.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/organizations.test.js backend/tests/routes/users.test.js</automated>
  </verify>
  <acceptance_criteria>Beide Test-Dateien existieren, Organizations hat mindestens 8 Test-Cases, Users hat mindestens 10 Test-Cases, alle gruen.</acceptance_criteria>
  <done>Organizations + Users Tests laufen gruen, SuperAdmin-Flows und OrgAdmin-Hierarchie getestet, Org-Isolation verifiziert.</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/notifications.test.js backend/tests/routes/organizations.test.js backend/tests/routes/roles.test.js backend/tests/routes/settings.test.js backend/tests/routes/users.test.js
</verification>

<success_criteria>
- 5 neue Test-Dateien existieren und alle Tests gruen
- Jede Test-Datei hat mindestens Happy-Path (200) + Fehlerfall (401/403/404)
- Organizations: SuperAdmin-CRUD + OrgAdmin-Einschraenkungen getestet
- Users: Hierarchie-Checks und Jahrgang-Assignments getestet
- Org-Isolation durchgaengig geprueft
</success_criteria>

<output>
After completion, create `.planning/phases/104-remaining-routes-integration-tests/104-02-SUMMARY.md`
</output>
