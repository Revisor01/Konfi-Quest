---
phase: 104-remaining-routes-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/routes/categories.test.js
  - backend/tests/routes/jahrgaenge.test.js
  - backend/tests/routes/levels.test.js
  - backend/tests/routes/konfi-management.test.js
autonomous: true
requirements: [BIT-08]

must_haves:
  truths:
    - "Categories CRUD Tests laufen gruen (GET/POST/PUT/DELETE)"
    - "Jahrgaenge CRUD Tests laufen gruen (GET/POST/PUT/DELETE)"
    - "Levels CRUD + Konfi-Level Tests laufen gruen"
    - "Konfi-Management Tests laufen gruen (CRUD, Bonus, Activities, Promote)"
  artifacts:
    - path: "backend/tests/routes/categories.test.js"
      provides: "Categories Route Integration-Tests"
    - path: "backend/tests/routes/jahrgaenge.test.js"
      provides: "Jahrgaenge Route Integration-Tests"
    - path: "backend/tests/routes/levels.test.js"
      provides: "Levels Route Integration-Tests"
    - path: "backend/tests/routes/konfi-management.test.js"
      provides: "Konfi-Management Route Integration-Tests"
  key_links:
    - from: "backend/tests/routes/categories.test.js"
      to: "/api/admin/categories"
      via: "supertest"
    - from: "backend/tests/routes/konfi-management.test.js"
      to: "/api/admin/konfis"
      via: "supertest"
---

<objective>
Integration-Tests fuer CRUD-artige Admin-Routes: Categories, Jahrgaenge, Levels und Konfi-Management (inkl. Bonus-Punkte).

Purpose: Per D-05 — diese Routes folgen alle einem aehnlichen CRUD-Pattern mit requireTeamer/requireAdmin Guards.
Output: 4 Test-Dateien mit Happy-Path + Fehlerfall-Tests (per D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-remaining-routes-integration-tests/104-CONTEXT.md

@backend/tests/routes/activities.test.js (Referenz-Pattern fuer CRUD-Tests)
@backend/tests/helpers/testApp.js
@backend/tests/helpers/db.js
@backend/tests/helpers/seed.js
@backend/tests/helpers/auth.js

@backend/routes/categories.js
@backend/routes/jahrgaenge.js
@backend/routes/levels.js
@backend/routes/konfi-management.js
@backend/createApp.js (Route-Mounting: /api/admin/categories, /api/admin/jahrgaenge, /api/levels, /api/admin/konfis)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Categories + Jahrgaenge + Levels Integration-Tests</name>
  <files>backend/tests/routes/categories.test.js, backend/tests/routes/jahrgaenge.test.js, backend/tests/routes/levels.test.js</files>
  <read_first>backend/routes/categories.js, backend/routes/jahrgaenge.js, backend/routes/levels.js, backend/tests/routes/activities.test.js</read_first>
  <action>
Erstelle 3 Test-Dateien nach dem etablierten Pattern aus activities.test.js (per D-01, D-02, D-03):

**categories.test.js** — Route: `/api/admin/categories`
- GET / — Admin/Teamer bekommt 200 + Array, Konfi bekommt 403, ohne Token 401
- POST / — Admin erstellt Kategorie (200), Teamer bekommt 403, Validierungsfehler (leerer Name)
- PUT /:id — Admin aktualisiert Kategorie (200), nicht-existierende ID (404)
- DELETE /:id — Admin loescht Kategorie (200), nicht-existierende ID (404)
- Org-Isolation: Admin2 (Org 2) sieht keine Kategorien von Org 1

**jahrgaenge.test.js** — Route: `/api/admin/jahrgaenge`
- GET / — Admin/Teamer bekommt 200 + Array, Konfi bekommt 403
- POST / — Admin erstellt Jahrgang mit name (200), Validierungsfehler
- PUT /:id — Admin aktualisiert Jahrgang (200), nicht-existierende ID (404)
- DELETE /:id — Admin loescht Jahrgang (200), nicht-existierende ID (404)
- Org-Isolation: Admin2 sieht nur eigene Jahrgaenge

**levels.test.js** — Route: `/api/levels`
- GET / — Jeder authentifizierte User bekommt 200 + Array (levels hat eigenen Rollen-Check), ohne Token 401
- POST / — Admin/OrgAdmin erstellt Level (200/201), Konfi bekommt 403
- PUT /:id — Admin aktualisiert Level (200), Validierungsfehler
- DELETE /:id — Admin loescht Level (200), nicht-existierende ID (404)
- GET /konfi/:userId — Konfi-Level abfragen (200)
- Org-Isolation: Levels nur fuer eigene Org sichtbar

Jeder Test nutzt beforeAll(getTestPool+getTestApp), beforeEach(truncateAll+seed), afterAll(closePool).
Tokens via generateToken('admin1'), generateToken('teamer1'), generateToken('konfi1'), generateToken('admin2').
Per D-02: Echte JWT-Tokens, KEIN Mocking.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/categories.test.js backend/tests/routes/jahrgaenge.test.js backend/tests/routes/levels.test.js</automated>
  </verify>
  <acceptance_criteria>Alle 3 Test-Dateien existieren, jede hat mindestens Happy-Path (200) und Fehlerfall (401/403/404) Tests, alle Tests gruen.</acceptance_criteria>
  <done>Categories, Jahrgaenge, Levels Tests laufen gruen mit mindestens je 5 Test-Cases.</done>
</task>

<task type="auto">
  <name>Task 2: Konfi-Management Integration-Tests</name>
  <files>backend/tests/routes/konfi-management.test.js</files>
  <read_first>backend/routes/konfi-management.js, backend/tests/routes/activities.test.js, backend/createApp.js</read_first>
  <action>
Erstelle konfi-management.test.js fuer Route `/api/admin/konfis` (per D-01, D-02, D-03, D-04):

**CRUD Tests:**
- GET / — Admin/Teamer bekommt 200 + Konfi-Liste der eigenen Org, Konfi bekommt 403
- GET /teamer — Teamer-spezifische Konfi-Liste (200)
- GET /:id — Admin bekommt 200 + Konfi-Details mit activities/bonusPoints/eventPoints
- POST / — Admin erstellt neuen Konfi (200), Validierungsfehler bei fehlendem username
- PUT /:id — Admin aktualisiert Konfi (200), nicht-existierende ID (404)
- DELETE /:id — Admin loescht Konfi (200)

**Bonus-Punkte Tests:**
- POST /:id/bonus-points — Teamer vergibt Bonus (200), Konfi bekommt 403, Validierungsfehler (fehlende points)
- DELETE /:id/bonus-points/:bonusId — Admin loescht Bonus (200), nicht-existierende ID (404)

**Activity-Assignment Tests:**
- POST /:id/activities — Admin weist Activity zu (200), bereits zugewiesene Activity (Duplikat-Handling)
- DELETE /:id/activities/:activityId — Admin entfernt Activity (200)

**Weitere Endpoints:**
- POST /:id/regenerate-password — Admin regeneriert Passwort (200)
- GET /:id/event-points — Teamer bekommt Event-Punkte (200)
- GET /:id/attendance-stats — Admin bekommt Anwesenheitsstatistik (200)
- POST /:id/promote-teamer — Admin befoeördert Konfi zum Teamer (200)

**Org-Isolation:**
- Admin2 (Org 2) kann keinen Konfi aus Org 1 sehen/bearbeiten

Tokens: generateToken('admin1'), generateToken('teamer1'), generateToken('konfi1'), generateToken('admin2').
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/konfi-management.test.js</automated>
  </verify>
  <acceptance_criteria>konfi-management.test.js existiert, testet CRUD + Bonus + Activities + Promote mit Happy-Path und Fehlerfaellen, alle Tests gruen.</acceptance_criteria>
  <done>Konfi-Management Tests laufen gruen mit mindestens 12 Test-Cases (CRUD, Bonus, Activities, Promote, Org-Isolation).</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/categories.test.js backend/tests/routes/jahrgaenge.test.js backend/tests/routes/levels.test.js backend/tests/routes/konfi-management.test.js
</verification>

<success_criteria>
- 4 neue Test-Dateien existieren und alle Tests gruen
- Jede Test-Datei hat mindestens Happy-Path (200) + Fehlerfall (401/403/404)
- Org-Isolation in allen CRUD-Tests geprueft
</success_criteria>

<output>
After completion, create `.planning/phases/104-remaining-routes-integration-tests/104-01-SUMMARY.md`
</output>
