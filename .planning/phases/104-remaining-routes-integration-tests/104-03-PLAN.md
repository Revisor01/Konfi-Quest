---
phase: 104-remaining-routes-integration-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/routes/material.test.js
  - backend/tests/routes/teamer.test.js
  - backend/tests/routes/wrapped.test.js
autonomous: true
requirements: [BIT-08]

must_haves:
  truths:
    - "Material Tests laufen gruen (Tags CRUD, Material CRUD, File-Upload)"
    - "Teamer Tests laufen gruen (Dashboard, Konfis, Badges, Certificates)"
    - "Wrapped Tests laufen gruen (Generate, History, Delete)"
  artifacts:
    - path: "backend/tests/routes/material.test.js"
      provides: "Material Route Integration-Tests"
    - path: "backend/tests/routes/teamer.test.js"
      provides: "Teamer Route Integration-Tests"
    - path: "backend/tests/routes/wrapped.test.js"
      provides: "Wrapped Route Integration-Tests"
  key_links:
    - from: "backend/tests/routes/teamer.test.js"
      to: "/api/teamer"
      via: "supertest"
    - from: "backend/tests/routes/wrapped.test.js"
      to: "/api/wrapped"
      via: "supertest"
---

<objective>
Integration-Tests fuer Feature-Routes: Material (Upload-System), Teamer (Dashboard + Zertifikate) und Wrapped (Jahresrueckblick).

Purpose: Per D-08 — diese Routes sind Feature-spezifisch und komplexer als einfaches CRUD (Material hat File-Upload, Teamer hat Dashboard-Aggregation, Wrapped hat Snapshot-Generierung).
Output: 3 Test-Dateien mit Happy-Path + Fehlerfall-Tests (per D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-remaining-routes-integration-tests/104-CONTEXT.md

@backend/tests/routes/activities.test.js (Referenz-Pattern)
@backend/tests/routes/events.test.js (Referenz fuer komplexe Routes)
@backend/tests/helpers/testApp.js
@backend/tests/helpers/db.js
@backend/tests/helpers/seed.js
@backend/tests/helpers/auth.js

@backend/routes/material.js
@backend/routes/teamer.js
@backend/routes/wrapped.js
@backend/createApp.js (Route-Mounting: /api/material, /api/teamer, /api/wrapped)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Material Integration-Tests</name>
  <files>backend/tests/routes/material.test.js</files>
  <read_first>backend/routes/material.js, backend/tests/routes/activities.test.js, backend/createApp.js</read_first>
  <action>
Erstelle material.test.js fuer Route `/api/material` (per D-01, D-02, D-03, D-04):

**Tag-CRUD Tests:**
- GET /tags — Teamer/Admin bekommt 200 + Tag-Array, Konfi bekommt 403
- POST /tags — OrgAdmin erstellt Tag (200/201), Teamer bekommt 403, Validierungsfehler
- PUT /tags/:id — OrgAdmin aktualisiert Tag (200), nicht-existierende ID (404)
- DELETE /tags/:id — OrgAdmin loescht Tag (200)

**Material-CRUD Tests:**
- GET / — Teamer/Admin bekommt 200 + Material-Liste, Konfi bekommt 403
- GET /:id — Teamer bekommt 200 + Material-Details, nicht-existierende ID (404)
- POST / — OrgAdmin erstellt Material (200/201), Teamer bekommt 403, Validierungsfehler (fehlender title)
- PUT /:id — OrgAdmin aktualisiert Material (200)
- DELETE /:id — OrgAdmin loescht Material (200)
- GET /by-event/:eventId — Teamer bekommt Material fuer Event (200)

**File-Tests (ohne echten Upload, nur Endpoint-Erreichbarkeit):**
- POST /:id/files — Ohne Datei ergibt Fehler (kein crash), OrgAdmin-only
- DELETE /files/:fileId — OrgAdmin loescht Datei (200 oder 404)
- GET /files/:filename — Teamer ruft Datei ab (404 da Datei nicht existiert — kein crash)
  HINWEIS: File-Upload via supertest mit .attach() ist moeglich, aber materialUpload Middleware braucht ggf. spezielle Config. Teste zumindest dass die Endpoints erreichbar sind und korrekte Auth-Checks haben. Falls .attach() funktioniert (kleines Textfile), teste auch den Upload-Flow.

**Org-Isolation:**
- Admin2/Teamer2 sieht kein Material aus Org 1

Tokens: generateToken fuer relevante Rollen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/material.test.js</automated>
  </verify>
  <acceptance_criteria>material.test.js existiert, Tag-CRUD + Material-CRUD + File-Endpoints getestet, alle gruen.</acceptance_criteria>
  <done>Material Tests laufen gruen mit mindestens 10 Test-Cases.</done>
</task>

<task type="auto">
  <name>Task 2: Teamer + Wrapped Integration-Tests</name>
  <files>backend/tests/routes/teamer.test.js, backend/tests/routes/wrapped.test.js</files>
  <read_first>backend/routes/teamer.js, backend/routes/wrapped.js, backend/tests/routes/activities.test.js</read_first>
  <action>
Erstelle 2 Test-Dateien fuer die Feature-Routes (per D-01, D-02, D-03, D-04):

**teamer.test.js** — Route: `/api/teamer`
- GET /profile — Teamer bekommt 200 + Profil-Daten, Konfi bekommt 403 (requireTeamer Check: role_name in teamer/admin/org_admin)
- GET /konfis — Teamer bekommt 200 + Konfi-Liste des eigenen Jahrgangs, Konfi bekommt 403
- GET /konfi-history — Teamer bekommt 200 + History, Konfi bekommt 403
- GET /badges — Teamer bekommt 200 + Badge-Liste, Konfi bekommt 403
- GET /badges/unseen — Teamer bekommt 200 + unseen Badges
- PUT /badges/mark-seen — Teamer markiert Badges als gesehen (200)
- GET /certificate-types — Admin bekommt 200 + Zertifikat-Typen, Teamer bekommt 403 (requireAdmin)
- POST /certificate-types — OrgAdmin erstellt Zertifikat-Typ (200/201), Admin bekommt 403
- PUT /certificate-types/:id — OrgAdmin aktualisiert Typ (200)
- DELETE /certificate-types/:id — OrgAdmin loescht Typ (200)
- GET /:userId/certificates — Admin bekommt 200 + Zertifikate eines Users
- POST /:userId/certificates — OrgAdmin vergibt Zertifikat (200/201), Validierungsfehler
- DELETE /:userId/certificates/:certId — OrgAdmin loescht Zertifikat (200)
- GET /dashboard — Teamer bekommt 200 + Dashboard-Daten (Aggregation), Konfi bekommt 403
- GET /tageslosung — Teamer bekommt 200 (oder graceful error wenn losungService offline)
- Org-Isolation: Teamer2 sieht keine Konfis/Badges/Zertifikate aus Org 1

HINWEIS: Fuer Certificate-Tests muss ggf. zuerst ein certificate_type in der DB existieren. Erstelle ihn per direktem DB-Insert im Test oder per POST /certificate-types.

**wrapped.test.js** — Route: `/api/wrapped`
- GET /me — Authentifizierter User bekommt 200 + eigene Wrapped-Daten (leer wenn nicht generiert)
- POST /generate/:jahrgangId — Admin generiert Wrapped fuer Jahrgang (200), Konfi bekommt 403
- POST /generate-teamer — Admin generiert Teamer-Wrapped (200), Konfi/Teamer bekommt 403
- DELETE /:jahrgangId — Admin loescht Wrapped (200), Konfi bekommt 403
- GET /history/:userId — Admin bekommt 200 + Wrapped-History eines Users
- Ohne Token: 401

HINWEIS: Wrapped-Generierung braucht echte Daten (Konfi-Profiles, Activities, Events). Die Seed-Fixtures haben genug Daten fuer eine Basic-Generierung. Falls die Generierung fehlschlaegt weil bestimmte Daten fehlen, teste zumindest Auth-Checks und dass der Endpoint nicht crasht (kein 500).

Tokens: generateToken fuer alle relevanten Rollen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/teamer.test.js backend/tests/routes/wrapped.test.js</automated>
  </verify>
  <acceptance_criteria>Beide Test-Dateien existieren, Teamer hat mindestens 12 Test-Cases, Wrapped hat mindestens 5 Test-Cases, alle gruen.</acceptance_criteria>
  <done>Teamer + Wrapped Tests laufen gruen, Dashboard-Aggregation, Zertifikat-CRUD und Wrapped-Generierung getestet.</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/Konfipoints && npx vitest run --config vitest.config.backend.ts backend/tests/routes/material.test.js backend/tests/routes/teamer.test.js backend/tests/routes/wrapped.test.js
</verification>

<success_criteria>
- 3 neue Test-Dateien existieren und alle Tests gruen
- Material: Tag-CRUD + Material-CRUD + File-Endpoints getestet
- Teamer: Dashboard, Konfis, Badges, Certificates getestet
- Wrapped: Generate, History, Delete getestet
- Org-Isolation durchgaengig geprueft
</success_criteria>

<output>
After completion, create `.planning/phases/104-remaining-routes-integration-tests/104-03-SUMMARY.md`
</output>
