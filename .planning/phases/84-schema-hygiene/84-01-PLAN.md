---
phase: 84-schema-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database.js
  - backend/migrations/064_consolidate_inline_schemas.sql
  - backend/migrations/076_badges_rename_migrations.sql
autonomous: true
requirements:
  - MIG-01
  - MIG-02

must_haves:
  truths:
    - "database.js fuehrt alle SQL-Dateien aus migrations/ beim Server-Start aus"
    - "badges.js runMigrations-Logik ist vollstaendig als idempotentes SQL in backend/migrations/ abgebildet"
    - "jahrgaenge.js ensurePointConfigColumns ist vollstaendig in 064_consolidate_inline_schemas.sql abgebildet"
    - "wrapped.js ensureWrappedSchema ist vollstaendig in 075_wrapped.sql abgebildet"
  artifacts:
    - path: "backend/migrations/076_badges_rename_migrations.sql"
      provides: "Idempotente badges/activities Tabellen-Renames und Spalten-Renames als SQL"
      contains: "ALTER TABLE konfi_badges RENAME TO user_badges"
    - path: "backend/database.js"
      provides: "Migration-Runner der alle .sql-Dateien aus backend/migrations/ beim Start ausfuehrt"
      exports: ["query", "getClient", "end"]
  key_links:
    - from: "backend/database.js"
      to: "backend/migrations/*.sql"
      via: "fs.readdir + pg.query beim Pool-Start"
      pattern: "migrations/"
    - from: "backend/migrations/064_consolidate_inline_schemas.sql"
      to: "jahrgaenge-Tabelle"
      via: "ALTER TABLE jahrgaenge ADD COLUMN IF NOT EXISTS gottesdienst_enabled"
      pattern: "gottesdienst_enabled"
---

<objective>
Migration-Runner in database.js einbauen und alle Inline-Migrationen als vollstaendige SQL-Dateien in backend/migrations/ abbilden.

Purpose: Derzeit werden Migrationen nur durch den Inline-Code in den Route-Dateien ausgefuehrt. Die SQL-Dateien unter backend/migrations/ sind reine Dokumentation, kein ausgefuehrter Code. Dieser Plan schafft den Runner, der sie ausfuehrt, und ergaenzt die fehlenden Migrationen (badges.js runMigrations) als SQL.

Output:
- backend/database.js mit Migration-Runner
- backend/migrations/076_badges_rename_migrations.sql (neu — vollstaendige badges/activities rename logic)
- backend/migrations/064_consolidate_inline_schemas.sql (Bereinigung des HINWEIS-Kommentars)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/database.js
@backend/migrations/064_consolidate_inline_schemas.sql
@backend/migrations/075_wrapped.sql
@backend/routes/badges.js
@backend/routes/jahrgaenge.js
@backend/routes/wrapped.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration-Runner in database.js einbauen</name>
  <files>backend/database.js</files>
  <action>
Nach dem erfolgreichen Pool-Verbindungstest (`pool.query('SELECT NOW()')`) eine `runMigrations()`-Funktion hinzufuegen und aufrufen.

Die Funktion:
1. Liest alle .sql-Dateien aus `path.join(__dirname, 'migrations')` (mit `fs.readdirSync`, alphabetisch sortiert — das garantiert richtige Ausfuehrungsreihenfolge durch Nummerierung)
2. Fuehrt jede SQL-Datei als einzelne Query aus: `pool.query(sqlContent)`
3. Fehler einer einzelnen Datei werden geloggt (`console.error('Migration failed:', file, err.message)`) und rethrown — der Server soll nicht starten wenn Migrationen fehlschlagen
4. Erfolgreiche Ausfuehrung: `console.log('Migrations applied:', count, 'files')`

Wichtig: Die SQL-Dateien sind idempotent (IF NOT EXISTS, DO-Bloecke, RENAME nur wenn Spalte noch existiert). Sie koennen bei jedem Start ausgefuehrt werden. Kein Migration-Tracking-State noetig.

Struktur nach der Aenderung:
```js
const path = require('path');
const fs = require('fs');
const { Pool } = require('pg');
// ...bestehender Pool-Code...

async function runMigrations(pool) {
  const migrationsDir = path.join(__dirname, 'migrations');
  const files = fs.readdirSync(migrationsDir)
    .filter(f => f.endsWith('.sql'))
    .sort(); // alphabetisch = numerisch durch Dateinamen-Praefixe
  for (const file of files) {
    const sql = fs.readFileSync(path.join(migrationsDir, file), 'utf8');
    try {
      await pool.query(sql);
    } catch (err) {
      console.error(`Migration failed: ${file}`, err.message);
      throw err;
    }
  }
  console.log(`Migrations applied: ${files.length} files`);
}

pool.query('SELECT NOW()')
  .then(() => runMigrations(pool))
  .catch(err => {
    console.error('Database startup failed:', err);
    process.exit(1);
  });
```

Das `module.exports` bleibt unveraendert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "const db = require('./database.js')" 2>&1 | head -5</automated>
  </verify>
  <done>backend/database.js importierbar ohne Syntaxfehler; enthaelt runMigrations-Funktion; grep -n "runMigrations\|migrations" backend/database.js zeigt die neue Funktion</done>
</task>

<task type="auto">
  <name>Task 2: badges.js runMigrations als neue SQL-Datei 076 anlegen</name>
  <files>backend/migrations/076_badges_rename_migrations.sql</files>
  <action>
Neue Datei `backend/migrations/076_badges_rename_migrations.sql` erstellen.

Inhalt: Die gesamte Logik aus `badges.js` `runMigrations()` (Zeilen 632-710) als idempotente SQL-Statements.

Die Inline-JS-Funktion prueft per information_schema und fuehrt conditionale ALTERs durch. In SQL sind die aequivalenten idempotenten Statements DO-Bloecke:

```sql
-- ====================================================================
-- Migration 076: Badges/Activities Tabellen- und Spalten-Renames
-- aus backend/routes/badges.js runMigrations() ausgelagert
--
-- Alle Statements sind idempotent (DO-Bloecke mit Existenz-Checks).
-- ====================================================================

-- 1. konfi_badges -> user_badges (nur wenn alte Tabelle noch existiert und neue nicht)
DO $$
BEGIN
  IF EXISTS (
    SELECT FROM information_schema.tables WHERE table_name = 'konfi_badges'
  ) AND NOT EXISTS (
    SELECT FROM information_schema.tables WHERE table_name = 'user_badges'
  ) THEN
    ALTER TABLE konfi_badges RENAME TO user_badges;
    RAISE NOTICE 'Migration: konfi_badges -> user_badges';
  END IF;
END $$;

-- 2. konfi_activities -> user_activities (nur wenn alte Tabelle noch existiert und neue nicht)
DO $$
BEGIN
  IF EXISTS (
    SELECT FROM information_schema.tables WHERE table_name = 'konfi_activities'
  ) AND NOT EXISTS (
    SELECT FROM information_schema.tables WHERE table_name = 'user_activities'
  ) THEN
    ALTER TABLE konfi_activities RENAME TO user_activities;
    RAISE NOTICE 'Migration: konfi_activities -> user_activities';
  END IF;
END $$;

-- 3. user_badges: konfi_id -> user_id (nur wenn Spalte noch konfi_id heisst)
DO $$
BEGIN
  IF EXISTS (
    SELECT FROM information_schema.columns
    WHERE table_name = 'user_badges' AND column_name = 'konfi_id'
  ) THEN
    ALTER TABLE user_badges RENAME COLUMN konfi_id TO user_id;
    RAISE NOTICE 'Migration: user_badges.konfi_id -> user_id';
  END IF;
END $$;

-- 4. user_activities: konfi_id -> user_id (nur wenn Spalte noch konfi_id heisst)
DO $$
BEGIN
  IF EXISTS (
    SELECT FROM information_schema.columns
    WHERE table_name = 'user_activities' AND column_name = 'konfi_id'
  ) THEN
    ALTER TABLE user_activities RENAME COLUMN konfi_id TO user_id;
    RAISE NOTICE 'Migration: user_activities.konfi_id -> user_id';
  END IF;
END $$;

-- 5. user_badges: earned_at -> awarded_date (nur wenn Spalte noch earned_at heisst)
DO $$
BEGIN
  IF EXISTS (
    SELECT FROM information_schema.columns
    WHERE table_name = 'user_badges' AND column_name = 'earned_at'
  ) THEN
    ALTER TABLE user_badges RENAME COLUMN earned_at TO awarded_date;
    RAISE NOTICE 'Migration: user_badges.earned_at -> awarded_date';
  END IF;
END $$;

-- 6. activities: target_role Spalte hinzufuegen (idempotent)
ALTER TABLE activities ADD COLUMN IF NOT EXISTS target_role VARCHAR(10) DEFAULT 'konfi';

-- 7. custom_badges: target_role Spalte hinzufuegen (idempotent)
ALTER TABLE custom_badges ADD COLUMN IF NOT EXISTS target_role VARCHAR(10) DEFAULT 'konfi';

-- 8. UNIQUE Constraint auf user_activities entfernen (idempotent via DO-Block)
DO $$
DECLARE
  c RECORD;
BEGIN
  FOR c IN (
    SELECT constraint_name FROM information_schema.table_constraints
    WHERE table_name = 'user_activities' AND constraint_type = 'UNIQUE'
  ) LOOP
    EXECUTE 'ALTER TABLE user_activities DROP CONSTRAINT IF EXISTS "' || c.constraint_name || '"';
    RAISE NOTICE 'Migration: Dropped UNIQUE constraint % from user_activities', c.constraint_name;
  END LOOP;
END $$;
```

Gleichzeitig in `backend/migrations/064_consolidate_inline_schemas.sql` den HINWEIS-Kommentar (Zeilen 148-155) ersetzen durch:
```sql
-- Tabellen-Renames und Spalten-Renames aus badges.js:
-- Siehe 076_badges_rename_migrations.sql
```
  </action>
  <verify>
    <automated>grep -c "DO \$\$" /Users/simonluthe/Documents/Konfipoints/backend/migrations/076_badges_rename_migrations.sql</automated>
  </verify>
  <done>Datei backend/migrations/076_badges_rename_migrations.sql existiert mit mindestens 5 DO-Bloecken; grep -l "konfi_badges RENAME TO user_badges" backend/migrations/076_badges_rename_migrations.sql gibt die Datei zurueck</done>
</task>

</tasks>

<verification>
Nach Ausfuehrung beider Tasks:
1. `grep -n "runMigrations\|migrations/" backend/database.js` zeigt die neue Funktion
2. `ls backend/migrations/*.sql | wc -l` zeigt 7 Dateien (war 6, neu: 076)
3. `grep -c "DO \$\$" backend/migrations/076_badges_rename_migrations.sql` gibt >= 5
4. `node -e "require('./backend/database.js')" 2>&1 | head -3` — kein Syntaxfehler
</verification>

<success_criteria>
- database.js fuehrt alle SQL-Dateien aus backend/migrations/ beim Start aus
- 076_badges_rename_migrations.sql existiert mit vollstaendiger idempotenter Logik aus badges.js runMigrations()
- 064_consolidate_inline_schemas.sql HINWEIS-Block verweist auf 076
</success_criteria>

<output>
Nach Abschluss `.planning/phases/84-schema-hygiene/84-01-SUMMARY.md` erstellen.
</output>
