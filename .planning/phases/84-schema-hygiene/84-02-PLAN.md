---
phase: 84-schema-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 84-01
files_modified:
  - backend/routes/badges.js
  - backend/routes/jahrgaenge.js
  - backend/routes/wrapped.js
autonomous: true
requirements:
  - MIG-01
  - MIG-03

must_haves:
  truths:
    - "badges.js enthaelt keine runMigrations-Funktion und keinen runMigrations()-Aufruf mehr"
    - "jahrgaenge.js enthaelt keine ensurePointConfigColumns-Funktion und keinen Aufruf mehr"
    - "wrapped.js enthaelt keine ensureWrappedSchema-Funktion und keinen Aufruf mehr"
    - "Server startet ohne Fehler und alle Routen sind erreichbar"
  artifacts:
    - path: "backend/routes/badges.js"
      provides: "Badges-Route ohne Inline-Migration"
      contains: "// Schema: siehe backend/migrations/076_badges_rename_migrations.sql"
    - path: "backend/routes/jahrgaenge.js"
      provides: "Jahrgaenge-Route ohne Inline-Migration"
      contains: "// Schema: siehe backend/migrations/064_consolidate_inline_schemas.sql"
    - path: "backend/routes/wrapped.js"
      provides: "Wrapped-Route ohne Inline-Migration"
      contains: "// Schema: siehe backend/migrations/075_wrapped.sql"
  key_links:
    - from: "backend/routes/badges.js"
      to: "backend/migrations/076_badges_rename_migrations.sql"
      via: "Kommentar-Verweis (kein Code mehr)"
      pattern: "076_badges_rename_migrations"
---

<objective>
Inline-Migrationen aus badges.js, jahrgaenge.js und wrapped.js entfernen, nachdem der Migration-Runner aus Plan 84-01 aktiv ist.

Purpose: Der Migration-Runner aus 84-01 fuehrt alle SQL-Dateien beim Start aus — die Inline-Varianten in den Route-Dateien sind damit vollstaendig redundant. Entfernen reduziert Server-Startzeit und trennt Migrations-Logik von Request-Handler-Logik.

Output:
- badges.js ohne runMigrations()-Funktion (Zeilen 631-718 entfernt)
- jahrgaenge.js ohne ensurePointConfigColumns()-Funktion (Zeilen 10-44 entfernt)
- wrapped.js ohne ensureWrappedSchema()-Funktion (Zeilen 10-33 entfernt)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-schema-hygiene/84-01-SUMMARY.md
@backend/routes/badges.js
@backend/routes/jahrgaenge.js
@backend/routes/wrapped.js
</context>

<interfaces>
<!-- Aus 84-01-PLAN: database.js fuehrt jetzt alle migrations/*.sql beim Start aus.
     Die SQL-Aequivalente der Inline-Migrationen sind in folgenden Dateien:
     - badges.js runMigrations() -> backend/migrations/076_badges_rename_migrations.sql
     - jahrgaenge.js ensurePointConfigColumns() -> 064_consolidate_inline_schemas.sql (Zeilen 113-118)
     - wrapped.js ensureWrappedSchema() -> 075_wrapped.sql -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: runMigrations aus badges.js entfernen</name>
  <files>backend/routes/badges.js</files>
  <action>
In `backend/routes/badges.js` die folgenden Bereiche entfernen:

1. **Zeilen 631-710**: Die gesamte `runMigrations`-Funktion loeschen (Kommentarzeile "// Migration: Tabellen umbenennen..." bis Ende der schliessenden Klammer `}`)

2. **Zeilen 715-718** (im module.exports-Block): Den Block
   ```js
   // Migrations beim Server-Start ausfuehren
   runMigrations(db).catch(err => {
     console.error('Failed to run badge/activity migrations:', err);
   });
   ```
   loeschen.

3. Direkt nach der Kommentarzeile "// Badges: Teamer darf ansehen, Admin darf bearbeiten" (Zeile 712) einen Verweis einfuegen:
   ```js
   // Schema-Migrationen: siehe backend/migrations/076_badges_rename_migrations.sql
   ```

Das Ergebnis: badges.js beginnt ab module.exports direkt mit den Validierungsregeln, kein Migrations-Code mehr.
  </action>
  <verify>
    <automated>grep -c "runMigrations" /Users/simonluthe/Documents/Konfipoints/backend/routes/badges.js</automated>
  </verify>
  <done>grep -c "runMigrations" backend/routes/badges.js gibt 0 zurueck; Datei syntaktisch korrekt (node --check backend/routes/badges.js meldet keinen Fehler)</done>
</task>

<task type="auto">
  <name>Task 2: ensurePointConfigColumns aus jahrgaenge.js und ensureWrappedSchema aus wrapped.js entfernen</name>
  <files>backend/routes/jahrgaenge.js, backend/routes/wrapped.js</files>
  <action>
**jahrgaenge.js** — Zeilen 10-44 entfernen:
Den gesamten Block von `// Idempotente Migration: Punkte-Typ-Spalten auf jahrgaenge-Tabelle` bis einschliesslich `ensurePointConfigColumns();` und der Leerzeile danach.

Ersetzen durch einen einzeiligen Kommentar an derselben Position:
```js
// Schema-Migrationen: siehe backend/migrations/064_consolidate_inline_schemas.sql
```

Anschliessend folgt direkt `// Validierungsregeln` und `const validateCreateJahrgang`.

---

**wrapped.js** — Zeilen 10-33 entfernen:
Den Block von `// Idempotente Inline-Migration` bis einschliesslich `ensureWrappedSchema();` (ohne die danach folgende Leerzeile, die bleibt).

Ersetzen durch:
```js
// Schema-Migrationen: siehe backend/migrations/075_wrapped.sql
```

---

Beide Dateien nach dem Entfernen auf Syntaxfehler pruefen:
```bash
node --check backend/routes/jahrgaenge.js
node --check backend/routes/wrapped.js
```
  </action>
  <verify>
    <automated>grep -c "ensurePointConfigColumns\|ensureWrappedSchema" /Users/simonluthe/Documents/Konfipoints/backend/routes/jahrgaenge.js /Users/simonluthe/Documents/Konfipoints/backend/routes/wrapped.js</automated>
  </verify>
  <done>Beide grep-Counts geben 0 zurueck; node --check meldet keine Fehler fuer beide Dateien; grep "Schema-Migrationen" zeigt Kommentar-Verweis in beiden Dateien</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Migration-Runner in database.js aktiv, alle Inline-Migrationen aus badges.js, jahrgaenge.js und wrapped.js entfernt. Die SQL-Dateien unter backend/migrations/ sind jetzt die einzige Quelle der Schema-Migrationen.
  </what-built>
  <how-to-verify>
    Auf dem Server nach dem naechsten git push pruefen (oder lokal wenn DB verfuegbar):

    1. Server-Start-Log: Zeile "Migrations applied: 7 files" muss erscheinen
    2. Kein "Migration error" oder "Migration failed" im Log
    3. Alle API-Endpunkte erreichbar: GET /api/badges, GET /api/jahrgaenge, GET /api/wrapped (kein 500)

    Schnellpruefung ohne Server-Start:
    ```bash
    node --check backend/database.js
    node --check backend/routes/badges.js
    node --check backend/routes/jahrgaenge.js
    node --check backend/routes/wrapped.js
    ```
    Alle vier Befehle muessen ohne Ausgabe durchlaufen.
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle Checks bestanden, oder beschreibe Fehler</resume-signal>
</task>

</tasks>

<verification>
Finale Pruefung vor Abschluss:
1. `grep -c "runMigrations" backend/routes/badges.js` = 0
2. `grep -c "ensurePointConfigColumns" backend/routes/jahrgaenge.js` = 0
3. `grep -c "ensureWrappedSchema" backend/routes/wrapped.js` = 0
4. `node --check backend/routes/badges.js backend/routes/jahrgaenge.js backend/routes/wrapped.js backend/database.js` — kein Fehler
5. `ls backend/migrations/*.sql | wc -l` >= 7
</verification>

<success_criteria>
- Keine Inline-Migrations-Funktionen mehr in badges.js, jahrgaenge.js, wrapped.js
- Alle drei Dateien enthalten Kommentar-Verweis auf die entsprechende SQL-Datei
- Alle vier modifizierten Dateien syntaktisch korrekt (node --check)
- Server startet und loggt "Migrations applied"
</success_criteria>

<output>
Nach Abschluss `.planning/phases/84-schema-hygiene/84-02-SUMMARY.md` erstellen.
</output>
