---
phase: 44-push-debug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/backgroundService.js
  - backend/services/pushService.js
  - backend/push/firebase.js
autonomous: true
requirements:
  - PUSH-01

must_haves:
  truths:
    - "Admin erhaelt keine leeren Ghost-Push-Benachrichtigungen mehr"
    - "Badge-Sync laeuft weiterhin alle 5 Minuten fuer Konfis und Teamer"
    - "Konfi/Teamer erhaelt sichtbare Push bei neuem Badge"
    - "Konfi/Teamer erhaelt KEINE sichtbare Push wenn Badge-Count unveraendert bleibt"
  artifacts:
    - path: "backend/services/backgroundService.js"
      provides: "Badge-Sync ohne Admin-User, Change-Detection fuer neue Badges"
      contains: "role_name !== 'admin'"
    - path: "backend/services/pushService.js"
      provides: "sendNewBadgeNotification Methode mit title/body"
      contains: "sendNewBadgeNotification"
    - path: "backend/push/firebase.js"
      provides: "Silent badge-count update ohne sichtbare Notification"
      contains: "content-available"
  key_links:
    - from: "backend/services/backgroundService.js"
      to: "backend/services/pushService.js"
      via: "sendNewBadgeNotification bei neuem Badge, sendBadgeUpdate nur als silent update"
      pattern: "sendNewBadgeNotification"
    - from: "backend/push/firebase.js"
      to: "APNs"
      via: "Silent push mit content-available statt alert fuer badge-count-only updates"
      pattern: "content-available"
---

<objective>
Ghost-Push-Bug fixen: Admin erhaelt alle 5 Minuten leere Push-Benachrichtigungen weil `sendBadgeUpdate()` ohne title/body sendet.

Purpose: App-Stoerung beseitigen — Admins werden durch leere Pushes alle 5 Min geplaegt.
Output: Bereinigte Push-Logik: Admins werden aus Badge-Sync ausgeschlossen, Konfis/Teamer bekommen nur bei NEUEN Badges eine sichtbare Push.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-push-debug/44-CONTEXT.md

<interfaces>
<!-- Bestehende Push-Infrastruktur -->

From backend/push/firebase.js:
```javascript
const sendFirebasePushNotification = async (deviceToken, notificationData) => {
  // notificationData: { title?, body?, alert?, badge?, sound?, data? }
  // Baut message mit notification.title, notification.body, apns.payload.aps.badge
  // Problem: apns-push-type ist IMMER 'alert' — auch fuer badge-only updates
};
```

From backend/services/pushService.js:
```javascript
static async sendBadgeUpdate(db, userId, badgeCount) {
  // Sendet Push OHNE title/body — nur badge count + data.type='badge_update'
  // ROOT CAUSE: Firebase setzt body: undefined → leere sichtbare Push
}
static async getTokensForUser(db, userId) { /* returns [{id, token, error_count}] */ }
```

From backend/services/backgroundService.js:
```javascript
static async updateAllUserBadges(db) {
  // Laedt ALLE User mit Push-Tokens (inkl. Admins)
  // Berechnet badge count: chat_unread + pending_requests + pending_events
  // Bei badgeCount > 0: sendBadgeUpdate() → Ghost Push
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firebase silent push + sendBadgeUpdate fixen + sendNewBadgeNotification erstellen</name>
  <files>backend/push/firebase.js, backend/services/pushService.js</files>
  <read_first>
    backend/push/firebase.js
    backend/services/pushService.js
  </read_first>
  <action>
**firebase.js — Silent Push Support:**

Neue Funktion `sendFirebaseSilentPush` erstellen die NUR den Badge-Count am App-Icon aktualisiert, OHNE sichtbare Notification:

```javascript
const sendFirebaseSilentPush = async (deviceToken, badgeCount) => {
  const app = initializeFirebase();
  if (!app) throw new Error('Firebase not initialized');

  const message = {
    token: deviceToken,
    apns: {
      payload: {
        aps: {
          badge: badgeCount,
          'content-available': 1,
        },
      },
      headers: {
        'apns-push-type': 'background',
        'apns-priority': '5',
      },
    },
    data: { type: 'badge_update', count: badgeCount.toString() },
  };

  const response = await admin.messaging().send(message);
  return { success: true, messageId: response };
};
```

Export `sendFirebaseSilentPush` zusaetzlich zu den bestehenden Exports.

**pushService.js — sendBadgeUpdate fixen:**

`sendBadgeUpdate()` aendern: statt `sendFirebasePushNotification` jetzt `sendFirebaseSilentPush` verwenden. Import oben anpassen:
```javascript
const { sendFirebasePushNotification, sendFirebaseSilentPush } = require('../push/firebase');
```

In `sendBadgeUpdate()` den Loop-Body aendern:
```javascript
const result = await sendFirebaseSilentPush(token.token, badgeCount);
```
(statt `sendFirebasePushNotification(token.token, { badge: badgeCount, data: {...} })`)

**pushService.js — sendNewBadgeNotification erstellen:**

Neue Methode in der PushService Klasse, NACH `sendBadgeUpdate`:
```javascript
static async sendNewBadgeNotification(db, userId, badgeName) {
  try {
    const tokens = await this.getTokensForUser(db, userId);
    if (tokens.length === 0) return { success: false, message: 'No tokens found' };

    let successCount = 0;
    let errorCount = 0;

    for (const token of tokens) {
      const result = await sendFirebasePushNotification(token.token, {
        title: 'Neues Badge erreicht!',
        body: badgeName,
        data: { type: 'new_badge', badge_name: badgeName },
      });

      if (result.success) {
        successCount++;
        if (token.error_count > 0) {
          await db.query('UPDATE push_tokens SET error_count = 0, last_error_at = NULL WHERE id = $1', [token.id]);
        }
      } else {
        const fatalCodes = ['messaging/registration-token-not-registered', 'messaging/invalid-registration-token'];
        if (fatalCodes.includes(result.errorCode)) {
          await db.query('DELETE FROM push_tokens WHERE id = $1', [token.id]);
        } else {
          await db.query('UPDATE push_tokens SET error_count = error_count + 1, last_error_at = NOW() WHERE id = $1', [token.id]);
        }
        errorCount++;
      }
    }
    return { success: true, sent: successCount, errors: errorCount, total: tokens.length };
  } catch (error) {
    console.error('PushService.sendNewBadgeNotification error:', error);
    throw error;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "sendFirebaseSilentPush" backend/push/firebase.js && grep -n "sendNewBadgeNotification" backend/services/pushService.js && grep -n "content-available" backend/push/firebase.js && grep -n "sendFirebaseSilentPush" backend/services/pushService.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "sendFirebaseSilentPush" backend/push/firebase.js findet Funktionsdefinition UND Export
    - grep "content-available" backend/push/firebase.js findet silent push payload
    - grep "apns-push-type.*background" backend/push/firebase.js findet background push type
    - grep "sendNewBadgeNotification" backend/services/pushService.js findet neue Methode
    - grep "Neues Badge erreicht" backend/services/pushService.js findet Push-Text
    - grep "sendFirebaseSilentPush" backend/services/pushService.js findet Import UND Verwendung in sendBadgeUpdate
  </acceptance_criteria>
  <done>
    - firebase.js hat sendFirebaseSilentPush Funktion die nur badge count setzt ohne sichtbare Notification
    - pushService.sendBadgeUpdate nutzt silent push (kein title/body mehr)
    - pushService.sendNewBadgeNotification existiert mit "Neues Badge erreicht!" title und Badge-Name als body
  </done>
</task>

<task type="auto">
  <name>Task 2: backgroundService — Admins ausschliessen + Change-Detection fuer neue Badges</name>
  <files>backend/services/backgroundService.js</files>
  <read_first>
    backend/services/backgroundService.js
    backend/services/pushService.js
    backend/routes/badges.js
  </read_first>
  <action>
**backgroundService.js — updateAllUserBadges komplett ueberarbeiten:**

1. **SQL-Query aendern** um Admins auszuschliessen UND role_name mitzuladen:
```sql
SELECT DISTINCT pt.user_id, pt.user_type,
  r.name as role_name
FROM push_tokens pt
JOIN users u ON pt.user_id = u.id
JOIN roles r ON u.role_id = r.id
WHERE pt.token IS NOT NULL
  AND r.name != 'admin'
```
Damit werden Admins komplett aus dem Badge-Sync-Loop ausgeschlossen.

2. **Badge-Query anpassen**: Die `CASE WHEN $2 = 'admin'` Bloecke fuer `pending_requests` und `pending_events` koennen auf `0` bleiben (oder entfernt werden), da Admins jetzt gar nicht mehr im Loop sind. Die Abfrage bleibt fuer Konfis/Teamer bestehen — sie zaehlen nur `chat_unread`.

3. **Change-Detection implementieren** — Vor dem Badge-Sync die aktuellen Badge-Counts aus der DB holen und nach dem Sync vergleichen:

Vor dem User-Loop:
```javascript
// Badge-Counts VOR dem Sync laden (fuer Change-Detection)
const { rows: badgeCounts } = await db.query(`
  SELECT user_id, COUNT(*) as badge_count
  FROM user_badges
  GROUP BY user_id
`);
const previousBadgeCounts = {};
for (const row of badgeCounts) {
  previousBadgeCounts[row.user_id] = parseInt(row.badge_count);
}
```

Im User-Loop, NACH dem bestehenden badge-count-silent-push, Badge-Check und Vergleich hinzufuegen:
```javascript
// Badge-Check durchfuehren (Streak, zeitbasiert etc.)
const checkAndAwardBadges = require('../routes/badges').checkAndAwardBadges;
const awardResult = await checkAndAwardBadges(db, user.user_id);

// Neue Badge-Counts nach Check
const { rows: [newCount] } = await db.query(
  'SELECT COUNT(*)::int as cnt FROM user_badges WHERE user_id = $1',
  [user.user_id]
);
const prevCount = previousBadgeCounts[user.user_id] || 0;
const currentCount = newCount?.cnt || 0;

// Nur bei NEUEN Badges sichtbare Push senden
if (currentCount > prevCount && awardResult?.badges?.length > 0) {
  for (const badge of awardResult.badges) {
    await PushService.sendNewBadgeNotification(db, user.user_id, badge.name || 'Neues Badge');
  }
}
```

4. **Import anpassen**: `checkAndAwardBadges` muss aus badges.js importiert werden. Pruefen ob badges.js diese Funktion exportiert — falls nicht, den Export in badges.js hinzufuegen:
```javascript
module.exports.checkAndAwardBadges = checkAndAwardBadges;
```
(Am Ende der Datei pruefen und ggf. ergaenzen.)

5. **Silent badge count update beibehalten**: Der bestehende `sendBadgeUpdate` Call fuer `badgeCount > 0` (Chat-Unread etc.) bleibt erhalten — er ist jetzt silent dank Task 1.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "role_name.*admin\|admin.*role_name\|!= 'admin'" backend/services/backgroundService.js && grep -n "sendNewBadgeNotification" backend/services/backgroundService.js && grep -n "previousBadgeCounts\|badge_count" backend/services/backgroundService.js && grep -n "checkAndAwardBadges" backend/services/backgroundService.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "!= 'admin'" backend/services/backgroundService.js findet Admin-Ausschluss in SQL
    - grep "sendNewBadgeNotification" backend/services/backgroundService.js findet Aufruf bei neuen Badges
    - grep "previousBadgeCounts" backend/services/backgroundService.js findet Change-Detection Logik
    - grep "checkAndAwardBadges" backend/services/backgroundService.js findet Badge-Check Import/Aufruf
    - grep "r.name" backend/services/backgroundService.js findet JOIN auf roles Tabelle
    - Die SQL-Query im Badge-Sync hat JOIN users + JOIN roles
  </acceptance_criteria>
  <done>
    - Admins sind aus dem Badge-Sync-Loop ausgeschlossen (SQL WHERE r.name != 'admin')
    - Change-Detection vergleicht Badge-Count vor/nach checkAndAwardBadges
    - Nur bei NEUEN Badges wird sendNewBadgeNotification aufgerufen
    - Silent badge-count update (Chat-Unread) laeuft weiterhin fuer Konfis/Teamer
    - checkAndAwardBadges wird im 5-Min-Intervall fuer Konfis/Teamer ausgefuehrt
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "sendFirebaseSilentPush" backend/` — muss in firebase.js (Definition + Export) und pushService.js (Import + Verwendung) auftauchen
2. `grep -rn "sendNewBadgeNotification" backend/` — muss in pushService.js (Definition) und backgroundService.js (Aufruf) auftauchen
3. `grep "!= 'admin'" backend/services/backgroundService.js` — Admin-Ausschluss vorhanden
4. `grep "content-available" backend/push/firebase.js` — Silent Push korrekt konfiguriert
5. `grep "previousBadgeCounts" backend/services/backgroundService.js` — Change-Detection vorhanden
6. Backend startet ohne Fehler: `cd backend && node -e "require('./services/pushService'); require('./services/backgroundService'); require('./push/firebase'); console.log('OK')"`
</verification>

<success_criteria>
- Admin erhaelt keine Ghost-Push-Benachrichtigungen mehr (aus Sync-Loop ausgeschlossen)
- Badge-Sync laeuft weiterhin alle 5 Minuten fuer Konfis und Teamer
- Silent badge-count updates aktualisieren App-Icon-Badge ohne sichtbare Push
- Bei neuem Badge: sichtbare Push "Neues Badge erreicht! [Badge-Name]"
- Bei gleichbleibendem Badge-Count: keine sichtbare Push
</success_criteria>

<output>
After completion, create `.planning/phases/44-push-debug/44-01-SUMMARY.md`
</output>
