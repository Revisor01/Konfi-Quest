---
phase: 111-konfi-badges-popovers
plan: 02
type: execute
wave: 1
depends_on: [111-01]
files_modified:
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/theme/variables.css
autonomous: true
requirements: [BPO-01, BPO-02]
must_haves:
  truths:
    - "Popover-Titel ist einzeilig mit Ellipsis bei Overflow"
    - "Popover-Beschreibung darf mehrzeilig umbrechen"
    - "Popovers erscheinen centered und laufen nicht aus dem Bildschirm"
    - "Popover hat max-width damit es auf schmalen Screens nicht zu breit wird"
  artifacts:
    - path: "frontend/src/components/konfi/views/BadgesView.tsx"
      provides: "BadgePopoverContent mit einzeiligem Titel, centered Positionierung"
      contains: "whiteSpace.*nowrap"
    - path: "frontend/src/theme/variables.css"
      provides: "Popover CSS mit max-width und centered Positionierung"
      contains: "badge-detail-popover"
  key_links:
    - from: "handleBadgeClick"
      to: "presentBadgePopover"
      via: "side/alignment Parameter"
      pattern: "alignment.*center"
---

<objective>
Badge-Popovers an Design anpassen: Titel einzeilig, Beschreibung mehrzeilig, centered Positionierung, max-width begrenzt.

Purpose: Popovers laufen nicht mehr aus dem Bildschirm und haben konsistentes Text-Layout.
Output: Aktualisiertes Popover-Styling in BadgesView.tsx und variables.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/111-konfi-badges-popovers/111-CONTEXT.md
@.planning/phases/111-konfi-badges-popovers/111-01-SUMMARY.md

@frontend/src/components/konfi/views/BadgesView.tsx
@frontend/src/theme/variables.css

<interfaces>
<!-- Aktueller Popover-Code in BadgesView.tsx -->

BadgePopoverContent (Zeile 157-277):
- Titel h3: `whiteSpace: 'nowrap'` ist BEREITS gesetzt (Zeile 192)
- Beschreibung p: kein whiteSpace gesetzt — darf umbrechen (korrekt)
- Popover-Container: padding 12px, background white

handleBadgeClick (Zeile 347-355):
```tsx
presentBadgePopover({
  event: e.nativeEvent,
  side: 'top',
  alignment: 'center',
  cssClass: 'badge-detail-popover badge-popover-auto-width'
});
```

Bestehende CSS (variables.css Zeile 1565-1571):
```css
.badge-popover-auto-width,
.badge-detail-popover {
  --width: auto;
  --max-width: 80vw;
  --min-width: 200px;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Popover-Styling und Positionierung anpassen</name>
  <files>frontend/src/components/konfi/views/BadgesView.tsx, frontend/src/theme/variables.css</files>
  <read_first>
    - frontend/src/components/konfi/views/BadgesView.tsx (Zeile 157-277: BadgePopoverContent, Zeile 343-355: handleBadgeClick)
    - frontend/src/theme/variables.css (Zeile 1565-1571: badge-popover CSS)
  </read_first>
  <action>
**1. Popover-Titel einzeilig mit Ellipsis (per D-07, BPO-01):**
- In BadgePopoverContent, die h3 (Titel, ca. Zeile 192) hat bereits `whiteSpace: 'nowrap'`
- Ergaenze `overflow: 'hidden'` und `textOverflow: 'ellipsis'` falls nicht vorhanden
- Die Beschreibung (p-Tag, ca. Zeile 196-200) soll KEINE whiteSpace-Einschraenkung haben — pruefen und sicherstellen dass sie mehrzeilig umbrechen darf (kein nowrap)

**2. Popovers centered positionieren (per D-08, BPO-02):**
- In `handleBadgeClick` (ca. Zeile 347-355): Aendere die Popover-Positionierung:
  - Entferne `event: e.nativeEvent` — stattdessen Popover OHNE Event-Referenz praesentieren, damit es centered erscheint
  - Setze `side: 'top'` und `alignment: 'center'`
  - ALTERNATIV (falls ohne event der Popover nicht erscheint): Behalte `event` aber setze `side: undefined` — das laesst Ionic die beste Position waehlen
  - Teste welche Variante funktioniert. Die sicherste Loesung: `event` behalten, `side: 'bottom'`, `alignment: 'center'` — Ionic positioniert dann relativ zum geklickten Element aber centered

**3. Max-Width auf Popover (per D-09):**
- In variables.css, aktualisiere die bestehende Regel `.badge-popover-auto-width, .badge-detail-popover`:
  - `--max-width: min(320px, 85vw);` — begrenzt auf 320px oder 85% Viewport, je nachdem was kleiner ist
  - Behalte `--width: auto;` und `--min-width: 200px;`

**4. Popover-Container Styling:**
- In BadgePopoverContent: Setze auf dem aeusseren div `maxWidth: '100%'` und `overflow: 'hidden'` damit der Inhalt nicht aus dem Popover laeuft
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
- Popover-Titel hat whiteSpace: nowrap + overflow: hidden + textOverflow: ellipsis
- Popover-Beschreibung darf umbrechen (kein nowrap)
- Popover wird centered positioniert (alignment: center)
- CSS max-width auf min(320px, 85vw) gesetzt
- TypeScript kompiliert fehlerfrei
  </done>
  <acceptance_criteria>
- h3 in BadgePopoverContent hat overflow: hidden und textOverflow: ellipsis
- p in BadgePopoverContent hat KEIN whiteSpace: nowrap
- presentBadgePopover nutzt alignment: 'center'
- CSS-Regel .badge-detail-popover hat --max-width mit min() oder aehnlicher Begrenzung
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` kompiliert fehlerfrei
- BadgesView.tsx BadgePopoverContent h3 hat overflow/textOverflow/whiteSpace Kombination
- variables.css .badge-detail-popover hat --max-width begrenzt
- handleBadgeClick nutzt alignment: 'center'
</verification>

<success_criteria>
Badge-Popovers haben einzeiligen Titel mit Ellipsis, mehrzeilige Beschreibung, centered Positionierung und begrenzten max-width. TypeScript kompiliert fehlerfrei.
</success_criteria>

<output>
After completion, create `.planning/phases/111-konfi-badges-popovers/111-02-SUMMARY.md`
</output>
