---
phase: 111-konfi-badges-popovers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/theme/variables.css
autonomous: true
requirements: [KBV-01, KBV-02, KBV-03, KBV-04, KBV-05]
must_haves:
  truths:
    - "Tab-Leiste (IonSegment) ist ausserhalb der Card in app-segment-wrapper"
    - "Suchleiste ist vorhanden (IonSearchbar mit ios26-searchbar-classic)"
    - "Badge-Grid zeigt immer 3 Spalten mit gleicher Breite (CSS Grid 1fr)"
    - "Erreichte-Badges CardContent hat 12px Padding"
    - "Kategorie-Cards folgen dem app-list-item Pattern mit 8px margin-bottom"
  artifacts:
    - path: "frontend/src/components/konfi/views/BadgesView.tsx"
      provides: "Badges-View mit Tab-Leiste ausserhalb Card, Suchleiste, Grid 1/3"
      contains: "app-segment-wrapper"
    - path: "frontend/src/theme/variables.css"
      provides: "Badge-Popover CSS (unveraendert in diesem Plan)"
  key_links:
    - from: "BadgesView IonSegment"
      to: "app-segment-wrapper div"
      via: "className"
      pattern: "app-segment-wrapper"
    - from: "BadgesView IonSearchbar"
      to: "ios26-searchbar-classic"
      via: "className"
      pattern: "ios26-searchbar-classic"
---

<objective>
BadgesView Layout an Design-Patterns anpassen: Tab-Leiste aus Card heraus in app-segment-wrapper, Suchleiste hinzufuegen, Badge-Grid auf CSS Grid 1/3, Abstaende korrigieren.

Purpose: Konsistentes Design wie EventsView und ChatOverview.
Output: Aktualisierte BadgesView.tsx mit korrektem Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/111-konfi-badges-popovers/111-CONTEXT.md

@frontend/src/components/konfi/views/BadgesView.tsx
@frontend/src/components/konfi/views/EventsView.tsx
@frontend/src/components/chat/ChatOverview.tsx
@frontend/src/theme/variables.css

<interfaces>
<!-- Referenz-Patterns aus EventsView und ChatOverview -->

EventsView Tab-Leiste Pattern (Zeile 215-230):
```tsx
{/* Tab Navigation - wie Admin */}
<div className="app-segment-wrapper">
  <IonSegment value={activeTab} onIonChange={(e) => onTabChange(e.detail.value as any)}>
    <IonSegmentButton value="meine"><IonLabel>Meine</IonLabel></IonSegmentButton>
    ...
  </IonSegment>
</div>
```

ChatOverview Suchleiste Pattern (Zeile 348-371):
```tsx
<IonList inset={true} style={{ margin: '16px' }}>
  <IonListHeader>
    <div className="app-section-icon app-section-icon--chat">
      <IonIcon icon={filterOutline} />
    </div>
    <IonLabel>Suche & Filter</IonLabel>
  </IonListHeader>
  <IonItemGroup>
    <IonItem>
      <IonIcon icon={search} slot="start" style={{ color: '#8e8e93', fontSize: '1rem' }} />
      <IonInput value={searchText} onIonInput={(e) => setSearchText(e.detail.value!)} placeholder="..." />
    </IonItem>
  </IonItemGroup>
</IonList>
```

CSS app-segment-wrapper (variables.css Zeile 870):
```css
.app-segment-wrapper {
  margin: 16px;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BadgesView Layout umbauen — Tab-Leiste, Suchleiste, Grid, Abstaende</name>
  <files>frontend/src/components/konfi/views/BadgesView.tsx</files>
  <read_first>
    - frontend/src/components/konfi/views/BadgesView.tsx (komplett)
    - frontend/src/components/konfi/views/EventsView.tsx (Zeile 215-230: app-segment-wrapper Pattern)
    - frontend/src/components/chat/ChatOverview.tsx (Zeile 348-371: Suchleiste Pattern)
  </read_first>
  <action>
Umbau der BadgesView.tsx — folgende Aenderungen:

**1. Tab-Leiste aus Card heraus (per D-01, KBV-01):**
- Entferne die IonList > IonListHeader > IonCard > IonCardContent Wrapper um das IonSegment (Zeile 372-388)
- Ersetze durch `<div className="app-segment-wrapper">` direkt um das IonSegment — exakt wie EventsView Zeile 215-230
- Der IonListHeader mit "Suche & Filter" Label bleibt NICHT erhalten — die Tab-Leiste steht allein in app-segment-wrapper

**2. Suchleiste hinzufuegen (per D-02, KBV-01):**
- Fuege einen neuen `searchText` State hinzu: `const [searchText, setSearchText] = useState('');`
- Importiere `IonSearchbar` aus `@ionic/react` und `search` aus `ionicons/icons` (falls nicht vorhanden)
- Platziere UNTER dem app-segment-wrapper eine IonSearchbar mit className="ios26-searchbar-classic":
  ```tsx
  <div style={{ margin: '0 16px' }}>
    <IonSearchbar
      className="ios26-searchbar-classic"
      value={searchText}
      onIonInput={(e) => setSearchText(e.detail.value || '')}
      placeholder="Badges durchsuchen..."
      showClearButton="focus"
    />
  </div>
  ```
- Filtere die Badges in `getBadgeCategories()` zusaetzlich nach searchText: Badge-Name oder Badge-Description muss den Suchtext enthalten (case-insensitive)

**3. Badge-Grid CSS (per D-06, KBV-05):**
- Das Grid nutzt bereits `gridTemplateColumns: 'repeat(3, 1fr)'` (Zeile 471-475) — das ist korrekt
- Stelle sicher, dass jede Grid-Zelle `minWidth: 0` hat, damit Text nicht die Spalte aufblaest
- Setze auf jeder Badge-Zelle (dem aeusseren div, Zeile 487) zusaetzlich `minWidth: 0, overflow: 'hidden'`

**4. Erreichte-Badges Abstaende (per D-05, KBV-04):**
- Aendere in den Kategorie-Cards das `<IonCardContent style={{ padding: '16px' }}>` (Zeile 436) zu `padding: '12px'`

**5. Kategorie-Listen Pattern (KBV-02, KBV-03):**
- Die BadgesView hat keine Teilnehmer:innen-Liste oder Anmelde-Buttons — diese Requirements sind fuer den Konfi-BadgesView nicht anwendbar, da nur Badge-Grid-Karten existieren
- KEINE Aenderungen noetig, aber dokumentiere dies im Summary

**Imports:** Stelle sicher dass `IonSearchbar` importiert wird. `searchOutline` kann entfernt werden wenn nicht mehr benoetigt nach dem Umbau. `IonItemGroup` kann entfernt werden wenn nicht mehr verwendet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
- Tab-Leiste (IonSegment) steht in app-segment-wrapper ausserhalb jeder Card
- IonSearchbar mit ios26-searchbar-classic ist sichtbar und filtert Badges nach Name/Description
- Badge-Grid hat repeat(3, 1fr) mit minWidth: 0 auf Zellen
- Kategorie-CardContent hat 12px Padding
- TypeScript kompiliert fehlerfrei
  </done>
  <acceptance_criteria>
- IonSegment wrapped in div.app-segment-wrapper, NICHT in IonCard
- IonSearchbar mit className="ios26-searchbar-classic" vorhanden
- searchText State existiert und filtert getBadgeCategories
- gridTemplateColumns bleibt repeat(3, 1fr), Grid-Zellen haben minWidth: 0
- CardContent padding ist 12px
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` kompiliert fehlerfrei
- BadgesView.tsx enthaelt `app-segment-wrapper` Klasse
- BadgesView.tsx enthaelt `ios26-searchbar-classic` Klasse
- BadgesView.tsx enthaelt `repeat(3, 1fr)` Grid
- Kein IonCard-Wrapper mehr um das IonSegment
</verification>

<success_criteria>
BadgesView hat Tab-Leiste in app-segment-wrapper, Suchleiste mit ios26-searchbar-classic, Badge-Grid mit CSS Grid 1/3, 12px Padding in Kategorie-Cards. TypeScript kompiliert fehlerfrei.
</success_criteria>

<output>
After completion, create `.planning/phases/111-konfi-badges-popovers/111-01-SUMMARY.md`
</output>
