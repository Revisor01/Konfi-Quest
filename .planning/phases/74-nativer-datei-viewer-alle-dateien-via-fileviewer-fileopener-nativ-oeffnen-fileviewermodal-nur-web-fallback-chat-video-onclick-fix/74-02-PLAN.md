---
phase: 74-nativer-datei-viewer
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - frontend/src/components/chat/ChatRoom.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
autonomous: true
requirements: [NATIVE-02, NATIVE-03]

must_haves:
  truths:
    - "Datei-Tap im Chat oeffnet nativ auf iOS/Android, zeigt FileViewerModal auf Web"
    - "Datei-Tap in TeamerMaterialDetailPage oeffnet nativ auf iOS/Android, zeigt FileViewerModal auf Web"
    - "Datei-Tap in TeamerMaterialPage oeffnet nativ auf iOS/Android, zeigt FileViewerModal auf Web"
    - "FileViewerModal bleibt als Web-Fallback und bei nativem Fehler erhalten"
  artifacts:
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "handleFileClick mit openFileNatively-Vorrang"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx"
      provides: "openFile mit openFileNatively-Vorrang"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialPage.tsx"
      provides: "openFile mit openFileNatively-Vorrang"
  key_links:
    - from: "frontend/src/components/chat/ChatRoom.tsx"
      to: "frontend/src/utils/nativeFileViewer.ts"
      via: "import openFileNatively"
      pattern: "import.*openFileNatively.*nativeFileViewer"
    - from: "frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx"
      to: "frontend/src/utils/nativeFileViewer.ts"
      via: "import openFileNatively"
      pattern: "import.*openFileNatively.*nativeFileViewer"
    - from: "frontend/src/components/teamer/pages/TeamerMaterialPage.tsx"
      to: "frontend/src/utils/nativeFileViewer.ts"
      via: "import openFileNatively"
      pattern: "import.*openFileNatively.*nativeFileViewer"
---

<objective>
Alle FileViewerModal-Consumer auf openFileNatively umstellen: nativ oeffnen wenn moeglich, FileViewerModal nur als Web-Fallback.

Purpose: Einheitliches natives Datei-Oeffnen in Chat und Material (per D-05/D-06/D-07/D-12/D-13/D-15).
Output: 3 Consumer-Dateien nutzen openFileNatively mit FileViewerModal-Fallback
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-nativer-datei-viewer-alle-dateien-via-fileviewer-fileopener-nativ-oeffnen-fileviewermodal-nur-web-fallback-chat-video-onclick-fix/74-CONTEXT.md
@.planning/phases/74-nativer-datei-viewer-alle-dateien-via-fileviewer-fileopener-nativ-oeffnen-fileviewermodal-nur-web-fallback-chat-video-onclick-fix/74-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 74-01 erstellt -->
From frontend/src/utils/nativeFileViewer.ts:
```typescript
export async function openFileNatively(blobOrUrl: Blob | string, fileName: string, mimeType: string): Promise<boolean>;
// Gibt true zurueck wenn nativ geoeffnet, false wenn Web oder Fehler
```

<!-- Bestehende Consumer-Patterns -->

ChatRoom.tsx handleFileClick (Zeile 900-930):
- Laedt Blob per api.get('/chat/files/...')
- Baut FileItem-Array fuer Swipe-Kontext
- Ruft presentFileViewer() auf

TeamerMaterialDetailPage.tsx openFile (Zeile 119-147):
- Laedt Blob per api.get('/material/files/...')
- Baut FileItem-Array fuer Swipe-Kontext
- Ruft presentFileViewer() auf

TeamerMaterialPage.tsx openFile (Zeile 193-239):
- Hat BEREITS natives Pattern (identisch zu MaterialFormModal)
- Fallback: openInAppViewer → presentFileViewer
- Muss auf openFileNatively Utility umgestellt werden
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatRoom + TeamerMaterialDetailPage auf openFileNatively umstellen</name>
  <files>
    frontend/src/components/chat/ChatRoom.tsx
    frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  </files>
  <read_first>
    - frontend/src/utils/nativeFileViewer.ts (aus Plan 01)
    - frontend/src/components/chat/ChatRoom.tsx (Zeile 1-40 fuer Imports, Zeile 895-935 fuer handleFileClick)
    - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx (Zeile 1-50 fuer Imports, Zeile 119-147 fuer openFile)
  </read_first>
  <action>
    Beide Dateien nach dem gleichen Muster umstellen:

    **ChatRoom.tsx:**
    1. Import hinzufuegen: `import { openFileNatively } from '../../utils/nativeFileViewer';`
    2. Kommentar-Zeile 35 aktualisieren: `// Native FileViewer ueber openFileNatively, FileViewerModal als Web-Fallback`
    3. handleFileClick (Zeile 900) umbauen:
       ```
       const handleFileClick = async (filePath: string, fileName: string, mimeType: string) => {
         try {
           await Haptics.impact({ style: ImpactStyle.Light });

           // Angeklickte Datei als Blob laden
           const response = await api.get('/chat/files/' + filePath, { responseType: 'blob' });
           const blob = response.data;
           const mime = response.headers?.['content-type'] || mimeType;

           // Nativ oeffnen versuchen (per D-12)
           const openedNatively = await openFileNatively(blob, fileName, mime);
           if (openedNatively) return;

           // Web-Fallback: FileViewerModal mit Swipe-Kontext
           const blobUrl = URL.createObjectURL(new Blob([blob], { type: mime }));
           const allFileMessages = messages.filter(m => m.file_path);
           const files: FileItem[] = allFileMessages.map(m => {
             if (m.file_path === filePath) {
               return { url: blobUrl, fileName: m.file_name || fileName, mimeType: mime };
             }
             return {
               url: '/api/chat/files/' + m.file_path,
               fileName: m.file_name || 'Datei',
               mimeType: m.file_name ? getMimeFromFileName(m.file_name) : 'application/octet-stream'
             };
           });
           const clickedIndex = allFileMessages.findIndex(m => m.file_path === filePath);
           viewerRef.current = { files, initialIndex: Math.max(0, clickedIndex) };
           presentFileViewer({ cssClass: 'file-viewer-modal' });
         } catch {
           setError('Fehler beim Oeffnen der Datei');
         }
       };
       ```

    **TeamerMaterialDetailPage.tsx:**
    1. Import hinzufuegen: `import { openFileNatively } from '../../../utils/nativeFileViewer';`
    2. Kommentar-Zeile 36 aktualisieren: `// Native FileViewer ueber openFileNatively, FileViewerModal als Web-Fallback`
    3. openFile (Zeile 119) umbauen:
       ```
       const openFile = async (file: MaterialFile) => {
         try {
           await Haptics.impact({ style: ImpactStyle.Medium });
           const response = await api.get('/material/files/' + file.stored_name, { responseType: 'blob' });
           const blob = response.data;
           const mime = response.headers?.['content-type'] || file.mime_type;

           // Nativ oeffnen versuchen (per D-13)
           const openedNatively = await openFileNatively(blob, file.original_name, mime);
           if (openedNatively) return;

           // Web-Fallback: FileViewerModal mit Swipe-Kontext
           const blobUrl = URL.createObjectURL(new Blob([blob], { type: mime }));
           const files: FileItem[] = (material?.files || []).map(f => ({
             url: '/api/material/files/' + f.stored_name,
             fileName: f.original_name,
             mimeType: f.mime_type
           }));
           const clickedIdx = (material?.files || []).findIndex(f => f.id === file.id);
           if (clickedIdx >= 0) {
             files[clickedIdx] = { url: blobUrl, fileName: file.original_name, mimeType: mime };
           }
           viewerRef.current = { files, initialIndex: Math.max(0, clickedIdx) };
           presentFileViewer({ cssClass: 'file-viewer-modal' });
         } catch {
           setError('Fehler beim Oeffnen der Datei');
         }
       };
       ```

    WICHTIG: FileViewerModal Import + useIonModal Setup BEIBEHALTEN — wird weiterhin als Web-Fallback gebraucht.
    KEINE Emojis. Umlaute in UI-Strings verwenden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | grep -E "ChatRoom|TeamerMaterialDetail" | head -5 || echo "Keine Fehler"</automated>
  </verify>
  <acceptance_criteria>
    - grep "openFileNatively" frontend/src/components/chat/ChatRoom.tsx findet mindestens 2 Treffer (Import + Aufruf)
    - grep "openFileNatively" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx findet mindestens 2 Treffer (Import + Aufruf)
    - grep "if (openedNatively) return" frontend/src/components/chat/ChatRoom.tsx findet 1 Treffer
    - grep "if (openedNatively) return" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx findet 1 Treffer
    - grep "presentFileViewer" frontend/src/components/chat/ChatRoom.tsx findet mindestens 1 Treffer (Web-Fallback bleibt)
    - grep "presentFileViewer" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx findet mindestens 1 Treffer (Web-Fallback bleibt)
  </acceptance_criteria>
  <done>ChatRoom und TeamerMaterialDetailPage oeffnen Dateien nativ auf iOS/Android, FileViewerModal nur auf Web</done>
</task>

<task type="auto">
  <name>Task 2: TeamerMaterialPage auf openFileNatively Utility umstellen</name>
  <files>frontend/src/components/teamer/pages/TeamerMaterialPage.tsx</files>
  <read_first>
    - frontend/src/components/teamer/pages/TeamerMaterialPage.tsx (Zeile 1-60 fuer Imports, Zeile 120-240 fuer openFile + openInAppViewer)
  </read_first>
  <action>
    TeamerMaterialPage hat BEREITS das native Pattern inline (identisch zu MaterialFormModal). Auf Utility umstellen (per D-15):

    1. Import hinzufuegen: `import { openFileNatively } from '../../../utils/nativeFileViewer';`
    2. Alte Imports entfernen die nur vom inline-Native-Pattern gebraucht wurden:
       - `Filesystem, Directory` aus @capacitor/filesystem (pruefen ob anderswo benutzt — wenn nicht, entfernen)
       - `FileOpener` aus @capacitor-community/file-opener (pruefen ob anderswo benutzt)
       - `FileViewer` aus @capacitor/file-viewer (pruefen ob anderswo benutzt)
    3. openFile umbauen (Zeile 193-239):
       ```
       const openFile = async (file: MaterialFile) => {
         try {
           await Haptics.impact({ style: ImpactStyle.Medium });
           const response = await api.get('/material/files/' + file.stored_name, { responseType: 'blob' });
           const blob = response.data;
           const mime = response.headers?.['content-type'] || file.mime_type;

           // Nativ oeffnen versuchen (per D-15)
           const openedNatively = await openFileNatively(blob, file.original_name, mime);
           if (openedNatively) return;

           // Web-Fallback: In-App Viewer
           openInAppViewer(blob, file.original_name, mime);
         } catch {
           setError('Fehler beim Oeffnen der Datei');
         }
       };
       ```
    4. openInAppViewer-Funktion BEIBEHALTEN (wird als Web-Fallback gebraucht).

    Dadurch wird die ~45 Zeilen inline-Duplizierung auf ~15 Zeilen reduziert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | grep "TeamerMaterialPage" | head -5 || echo "Keine Fehler"</automated>
  </verify>
  <acceptance_criteria>
    - grep "openFileNatively" frontend/src/components/teamer/pages/TeamerMaterialPage.tsx findet mindestens 2 Treffer (Import + Aufruf)
    - grep "if (openedNatively) return" frontend/src/components/teamer/pages/TeamerMaterialPage.tsx findet 1 Treffer
    - grep "Filesystem.writeFile" frontend/src/components/teamer/pages/TeamerMaterialPage.tsx findet 0 Treffer (inline-Pattern entfernt)
    - grep "openInAppViewer" frontend/src/components/teamer/pages/TeamerMaterialPage.tsx findet mindestens 1 Treffer (Fallback bleibt)
  </acceptance_criteria>
  <done>TeamerMaterialPage nutzt openFileNatively statt inline-Duplizierung, Fallback bleibt erhalten</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler
- Alle 3 Consumer importieren und nutzen openFileNatively
- FileViewerModal bleibt ueberall als Web-Fallback erhalten
- Kein dupliziertes Filesystem/FileOpener/FileViewer Pattern mehr in Consumer-Dateien (ausser MaterialFormModal das bereits korrekt ist per D-14)
</verification>

<success_criteria>
- ChatRoom, TeamerMaterialDetailPage, TeamerMaterialPage nutzen openFileNatively
- Natives Oeffnen hat Vorrang, FileViewerModal ist Web-Fallback
- Keine TypeScript-Fehler
- Kein dupliziertes native-Pattern in Consumer-Dateien
</success_criteria>

<output>
After completion, create `.planning/phases/74-nativer-datei-viewer-alle-dateien-via-fileviewer-fileopener-nativ-oeffnen-fileviewermodal-nur-web-fallback-chat-video-onclick-fix/74-02-SUMMARY.md`
</output>
