---
phase: 41-zertifikate-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - frontend/src/components/admin/pages/AdminSettingsPage.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements: [ZRT-01, ZRT-02, ZRT-03, DSH-01, DSH-02, DSH-03, DSH-04]

must_haves:
  truths:
    - "Teamer sieht tageszeitabhaengige Begruessing im Dashboard"
    - "Teamer sieht alle verfuegbaren Zertifikat-Typen als horizontale Karten (gueltig, abgelaufen, nicht erhalten)"
    - "Teamer sieht naechste 3 Events im Dashboard"
    - "Teamer sieht letzte 3 Badges und Gesamtanzahl im Dashboard"
    - "Admin kann Zertifikat-Typen in Settings verwalten"
    - "Admin kann im Teamer-Detail Zertifikate zuweisen"
    - "Admin kann Teamer-Dashboard-Sektionen konfigurieren"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "Vollstaendiges Teamer-Dashboard mit 5 Sektionen"
    - path: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      provides: "Zertifikat-Typen-CRUD + Teamer-Dashboard-Toggles"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "Zertifikat-Zuweisung im Teamer-Detail"
  key_links:
    - from: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      to: "/api/teamer/dashboard"
      via: "api.get"
      pattern: "api\\.get.*teamer/dashboard"
    - from: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      to: "/api/teamer/certificate-types"
      via: "api.get/post/put/delete"
      pattern: "api\\..*(teamer/certificate-types|settings)"
    - from: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      to: "/api/teamer/:userId/certificates"
      via: "api.post/delete"
      pattern: "api\\..*(teamer.*certificates)"
---

<objective>
Frontend fuer das komplette Teamer-Dashboard und die Admin-Zertifikatverwaltung bauen: TeamerDashboardPage mit Begruessing, Zertifikat-Karten, Events, Badges und Losung. Admin-Settings um Zertifikat-Typen-CRUD und Teamer-Dashboard-Konfiguration erweitern. KonfiDetailView um Zertifikat-Zuweisung fuer Teamer ergaenzen.

Purpose: Teamer hat ein vollwertiges Dashboard, Admin kann Zertifikate verwalten und zuweisen.
Output: Funktionierendes Teamer-Dashboard und Admin-Zertifikatverwaltung.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-zertifikate-dashboard/41-CONTEXT.md
@.planning/phases/41-zertifikate-dashboard/41-01-SUMMARY.md

@frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
@frontend/src/components/konfi/views/DashboardView.tsx (Referenz: Dashboard-Pattern mit Events, Badges, Losung)
@frontend/src/components/admin/pages/AdminSettingsPage.tsx
@frontend/src/components/admin/views/KonfiDetailView.tsx
@frontend/src/types/dashboard.ts

<interfaces>
<!-- API-Endpoints aus Plan 01 -->

GET /api/teamer/dashboard Response:
```json
{
  "greeting": { "display_name": "...", "hour": 14 },
  "certificates": [
    { "id": 1, "name": "JuLeiCa", "icon": "ribbon", "issued_date": "2026-01-15", "expiry_date": "2029-01-15", "status": "valid" },
    { "id": 2, "name": "Erste Hilfe", "icon": "medkit", "issued_date": null, "expiry_date": null, "status": "not_earned" }
  ],
  "events": [
    { "id": 1, "title": "Konfi-Tag", "event_date": "2026-03-20", "location": "Gemeindehaus", "is_registered": true }
  ],
  "badges": { "recent": [...], "earned_count": 5, "total_count": 12 },
  "config": { "show_zertifikate": true, "show_events": true, "show_badges": true, "show_losung": true }
}
```

GET /api/teamer/certificate-types Response:
```json
[{ "id": 1, "name": "JuLeiCa", "icon": "ribbon", "is_active": true }]
```

POST /api/teamer/:userId/certificates Body:
```json
{ "certificate_type_id": 1, "issued_date": "2026-01-15", "expiry_date": "2029-01-15" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeamerDashboardPage komplett aufbauen</name>
  <files>frontend/src/components/teamer/pages/TeamerDashboardPage.tsx</files>
  <action>
TeamerDashboardPage.tsx komplett umbauen (aktuell nur EmptyState-Platzhalter). Orientierung an DashboardView.tsx (Konfi-Dashboard):

1. State und Daten-Laden:
   - useState fuer dashboardData (greeting, certificates, events, badges, config)
   - useEffect mit api.get('/teamer/dashboard') beim Mount
   - IonRefresher fuer Pull-to-Refresh
   - Loading-State mit IonProgressBar

2. Begruessing-Sektion (immer sichtbar):
   - Tageszeitabhaengig: 5-11 "Guten Morgen", 11-14 "Guten Mittag", 14-18 "Guten Tag", 18-22 "Guten Abend", 22-5 "Gute Nacht"
   - "Moin" als norddeutsche Variante: Bei jedem 5. Laden (Math.random() < 0.2) "Moin" statt der regulaeren Begruessing
   - Format: "{Begruessing}, {display_name}" im app-header-banner--teamer Style

3. Zertifikate-Sektion (wenn config.show_zertifikate):
   - IonList mit IonListHeader "Zertifikate"
   - Horizontale Karten in einem scrollbaren Flex-Container (display: flex, gap: 12px, overflow-x: auto, padding-bottom fuer Scrollbar)
   - Jede Karte: Feste Breite (160px), gleiche Hoehe
   - 3 Zustaende:
     a) Gueltig (status='valid'): Volle Farbe, IonIcon des Zertifikat-Icons, Name, Datum "Seit {issued_date}", gruener Akzent
     b) Abgelaufen (status='expired'): Orange/roter Akzent, "Abgelaufen" Label, Icon gedimmt
     c) Nicht erhalten (status='not_earned'): Ausgegraut (opacity: 0.4), Name, kein Datum
   - Klick auf Karte: useIonPopover mit Details (Name, Ausstellungsdatum, Ablaufdatum falls vorhanden, Status)
   - BADGE_ICONS Mapping aus DashboardView.tsx wiederverwenden fuer Icon-Aufloesung

4. Events-Sektion (wenn config.show_events):
   - IonListHeader "Naechste Events"
   - Bis zu 3 Event-Karten im bestehenden app-card Style
   - Jede Karte: Titel, Datum (formatiert), Ort, is_registered Badge
   - "Alle anzeigen" Link der zum Events-Tab navigiert (history.push('/teamer/events'))
   - Leerer Zustand: "Keine anstehenden Events" Text

5. Badges-Sektion (wenn config.show_badges):
   - IonListHeader "Badges"
   - Kompakte Anzeige: Letzte 3 Badge-Icons nebeneinander + "{earned_count} von {total_count} Badges"
   - Klick navigiert zu TeamerBadgesView (history.push('/teamer/profil') mit Badge-Anchor oder separater Route)
   - Leerer Zustand: "Noch keine Badges erhalten"

6. Losung-Sektion (wenn config.show_losung):
   - Gleiche Implementierung wie in DashboardView.tsx: api.get('https://konfi-points.de/losung/api/today')
   - Losungstext + Lehrtext in einer IonCard
   - Separater useEffect fuer Losungs-Fetch (unabhaengig vom Dashboard-Endpoint)

KEINE Emojis verwenden! Nur IonIcon mit ionicons/icons.
Teamer-Akzentfarbe: app-header-banner--teamer (Lila/Rose wie in Phase 38 definiert).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TeamerDashboardPage zeigt alle 5 Sektionen (Begruessing, Zertifikate, Events, Badges, Losung), respektiert Dashboard-Config, tageszeitabhaengige Begruessing mit "Moin"-Variante funktioniert</done>
</task>

<task type="auto">
  <name>Task 2: Admin-Settings Zertifikat-Typen + Dashboard-Config + KonfiDetailView Zertifikat-Zuweisung</name>
  <files>frontend/src/components/admin/pages/AdminSettingsPage.tsx, frontend/src/components/admin/views/KonfiDetailView.tsx</files>
  <action>
1. AdminSettingsPage.tsx erweitern:

   a) Neuer "Zertifikate"-Abschnitt im "Inhalt"-Block (nach Kategorien, vor Level-System):
      - Klick navigiert zu '/admin/settings/certificates' (neue Unterseite, ABER: als inline-Abschnitt realisieren wie die bestehenden Settings-Items mit history.push)
      - IonIcon: ribbonOutline oder documentOutline
      - Subtitle: "Zertifikat-Typen fuer Teamer:innen verwalten"
      - ALTERNATIV: Direkt in AdminSettingsPage als eigene IonList-Sektion mit CRUD (einfacher, weniger Navigation)
      - ENTSCHEIDUNG: Inline CRUD in AdminSettingsPage (wie Dashboard-Toggles, nicht separate Seite):
        - "Zertifikat-Typen" IonListHeader
        - Liste der bestehenden Typen mit Name + Icon + Bearbeiten/Loeschen
        - "Neuen Typ erstellen" Button am Ende
        - useIonAlert fuer Name-Eingabe beim Erstellen/Bearbeiten
        - API: GET/POST/PUT/DELETE /teamer/certificate-types

   b) Dashboard-Konfiguration erweitern:
      - Bestehende "Dashboard-Konfiguration" Sektion umbenennen oder Segment-Toggle hinzufuegen: "Konfi" | "Teamer"
      - Konfi-Segment: Bestehende 5 Toggles (show_konfirmation, show_events, etc.)
      - Teamer-Segment: 4 neue Toggles:
        - "Zertifikate anzeigen" (teamer_dashboard_show_zertifikate)
        - "Events anzeigen" (teamer_dashboard_show_events)
        - "Badges anzeigen" (teamer_dashboard_show_badges)
        - "Tageslosung anzeigen" (teamer_dashboard_show_losung)
      - TeamerDashboardConfig Interface: { show_zertifikate, show_events, show_badges, show_losung }
      - Gleiche Toggle-Handler-Logik wie bestehend, Keys mit teamer_dashboard_ Prefix

2. KonfiDetailView.tsx erweitern (nur wenn isTeamer=true):

   a) Neue "Zertifikate" Sektion nach den bestehenden Badges/Aktivitaeten:
      - Nur sichtbar wenn isTeamer (targetRole === 'teamer')
      - IonListHeader mit documentOutline Icon + "Zertifikate"
      - Zertifikate kommen aus dem Detail-Response (certificates Array, in Plan 01 ergaenzt)
      - Vorhandene Zertifikate: Liste mit Name, Icon, Ausstellungsdatum, Ablaufdatum, Loeschen-Button (IonItemSliding mit Delete-Option)
      - "Zertifikat zuweisen" Button: useIonAlert mit:
        - Select/Picker fuer Zertifikat-Typ (aus GET /teamer/certificate-types)
        - Date-Input fuer Ausstellungsdatum (Pflicht)
        - Date-Input fuer Ablaufdatum (optional)
      - POST /teamer/{konfiId}/certificates zum Zuweisen
      - DELETE /teamer/{konfiId}/certificates/{certId} zum Entfernen
      - Nach Aktion: loadKonfiData() aufrufen (bestehender Reload-Mechanismus)

KEINE Emojis! Nur IonIcon. Deutsche UI-Texte mit echten Umlauten.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Admin kann Zertifikat-Typen erstellen/bearbeiten/loeschen in Settings, Admin kann Teamer-Dashboard-Sektionen konfigurieren mit Segment-Toggle, Admin kann im Teamer-Detail Zertifikate zuweisen und entfernen</done>
</task>

</tasks>

<verification>
- Frontend kompiliert ohne TypeScript-Fehler
- TeamerDashboardPage rendert alle Sektionen basierend auf Config
- AdminSettingsPage hat Zertifikat-CRUD und Teamer-Dashboard-Toggles
- KonfiDetailView zeigt Zertifikat-Sektion fuer Teamer
- Alle API-Calls verwenden korrekte Endpoints aus Plan 01
</verification>

<success_criteria>
- Teamer sieht tageszeitabhaengige Begruessing mit gelegentlichem "Moin"
- Zertifikate als horizontale Karten mit 3 Zustaenden (gueltig, abgelaufen, nicht erhalten)
- Naechste 3 Events und letzte 3 Badges im Dashboard
- Tageslosung wie bei Konfis
- Admin kann Zertifikat-Typen verwalten und Teamern zuweisen
- Dashboard-Sektionen admin-konfigurierbar
</success_criteria>

<output>
After completion, create `.planning/phases/41-zertifikate-dashboard/41-02-SUMMARY.md`
</output>
