---
phase: 61-admin-teamer-queue
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminCategoriesPage.tsx
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/admin/pages/AdminCertificatesPage.tsx
  - frontend/src/components/admin/modals/ActivityRequestModal.tsx
  - frontend/src/components/admin/modals/BonusModal.tsx
  - frontend/src/components/admin/modals/ActivityModal.tsx
  - frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx
autonomous: true
requirements: [QUE-A08, QUE-A09, QUE-A10, QUE-A11, QUE-A14, QUE-A15, QUE-A18, QUE-A19, QUE-A20, QUE-A21]

must_haves:
  truths:
    - "Admin kann offline Kategorien erstellen und bearbeiten"
    - "Admin kann offline Jahrgaenge erstellen und bearbeiten"
    - "Admin kann offline Zertifikat-Typen erstellen und bearbeiten"
    - "Admin kann offline Antraege genehmigen/ablehnen und zuruecksetzen"
    - "Admin kann offline Bonus-Punkte vergeben und Aktivitaeten zuweisen"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminCategoriesPage.tsx"
      provides: "Queue-Fallback fuer Kategorie erstellen/bearbeiten"
      contains: "writeQueue.enqueue"
    - path: "frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx"
      provides: "Queue-Fallback fuer Jahrgang erstellen/bearbeiten"
      contains: "writeQueue.enqueue"
    - path: "frontend/src/components/admin/pages/AdminCertificatesPage.tsx"
      provides: "Queue-Fallback fuer Zertifikat-Typ erstellen/bearbeiten"
      contains: "writeQueue.enqueue"
    - path: "frontend/src/components/admin/modals/ActivityRequestModal.tsx"
      provides: "Queue-Fallback fuer Antrag genehmigen/ablehnen"
      contains: "writeQueue.enqueue"
    - path: "frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx"
      provides: "Queue-Fallback fuer Antrag zuruecksetzen"
      contains: "writeQueue.enqueue"
    - path: "frontend/src/components/admin/modals/BonusModal.tsx"
      provides: "Queue-Fallback fuer Bonus-Punkte vergeben"
      contains: "writeQueue.enqueue"
    - path: "frontend/src/components/admin/modals/ActivityModal.tsx"
      provides: "Queue-Fallback fuer Aktivitaet zuweisen"
      contains: "writeQueue.enqueue"
  key_links:
    - from: "AdminCategoriesPage.tsx"
      to: "writeQueue"
      via: "import { writeQueue }"
      pattern: "writeQueue\\.enqueue"
    - from: "AdminActivityRequestsPage.tsx"
      to: "writeQueue"
      via: "import { writeQueue }"
      pattern: "writeQueue\\.enqueue"
---

<objective>
7 Admin-Dateien queue-faehig machen: AdminCategoriesPage (inline Modal), AdminJahrgaengeePage (inline Modal), AdminCertificatesPage (inline Modal), Admin ActivityRequestModal (genehmigen/ablehnen), AdminActivityRequestsPage (zuruecksetzen), BonusModal, ActivityModal.

Purpose: Restliche Admin-CRUD-Aktionen funktionieren offline via WriteQueue.
Output: 7 Dateien mit Online/Offline-Branching in den Submit-Handlern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-admin-teamer-queue/61-CONTEXT.md
@.planning/phases/60-queue-kern-konfi-aktionen/60-03-SUMMARY.md

<interfaces>
<!-- writeQueue API — identisch wie in Plan 01 -->
```typescript
export const writeQueue = {
  enqueue(item: Omit<QueueItem, 'id' | 'retryCount' | 'createdAt'>): Promise<QueueItem>;
  // ...
};
// metadata.type: 'admin' fuer alle Admin-Aktionen
```

<!-- networkMonitor -->
```typescript
import { networkMonitor } from '../../../services/networkMonitor';
// networkMonitor.isOnline: boolean
```

<!-- Aktuelle API-Endpunkte in den betroffenen Dateien: -->
```
AdminCategoriesPage handleSubmit:
  POST /admin/categories { name, description }
  PUT  /admin/categories/{id} { name, description }

AdminJahrgaengeePage handleSubmit:
  POST /admin/jahrgaenge { name, confirmation_date, gottesdienst_enabled, gemeinde_enabled, target_gottesdienst, target_gemeinde }
  PUT  /admin/jahrgaenge/{id} { ...same }

AdminCertificatesPage handleSubmit:
  POST /teamer/certificate-types { name, icon }
  PUT  /teamer/certificate-types/{id} { name, icon }

Admin ActivityRequestModal handleSubmit:
  PUT /admin/activities/requests/{id} { status: 'approved'|'rejected', admin_comment }

AdminActivityRequestsPage handleResetRequest:
  PUT /admin/activities/requests/{id}/reset  (kein Body)

BonusModal handleSave:
  POST /admin/konfis/{konfiId}/bonus-points { points, type, description, completed_date }

ActivityModal handleSave:
  POST /admin/konfis/{konfiId}/activities { activity_id, completed_date, comment }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminCategoriesPage + AdminJahrgaengeePage + AdminCertificatesPage queue-faehig</name>
  <files>
    frontend/src/components/admin/pages/AdminCategoriesPage.tsx
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
    frontend/src/components/admin/pages/AdminCertificatesPage.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminCategoriesPage.tsx
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
    frontend/src/components/admin/pages/AdminCertificatesPage.tsx
    frontend/src/services/writeQueue.ts (Zeilen 197-210, enqueue Signatur)
  </read_first>
  <action>
    Alle 3 Pages haben ein INLINE-Modal-Pattern (CategoryModal/JahrgangModal/CertificateTypeModal als interne Komponente). Die handleSubmit-Funktion liegt INNERHALB dieser internen Komponente.

    In jeder der 3 Dateien:

    1. Import hinzufuegen (am Dateianfang): `import { writeQueue } from '../../../services/writeQueue';` und `import { networkMonitor } from '../../../services/networkMonitor';`

    2. handleSubmit umbauen: Den bestehenden try/catch-Block mit `if (networkMonitor.isOnline) { ... } else { ... }` wrappen.
       - Online-Pfad: Bestehender Code bleibt 1:1 erhalten (api.post/put + setSuccess + onSuccess + handleClose).
       - Offline-Pfad:
         - Payload zusammenbauen (identisch wie Online)
         - writeQueue.enqueue aufrufen
         - setSuccess('...wird gespeichert sobald du wieder online bist')
         - onSuccess(); handleClose();

    **AdminCategoriesPage.tsx** (QUE-A08, QUE-A09):
    - handleSubmit Zeile ~96: `if (category) { PUT } else { POST }`
    - Offline: `writeQueue.enqueue({ method: category ? 'PUT' : 'POST', url: category ? \`/admin/categories/${category.id}\` : '/admin/categories', body: payload, maxRetries: 5, hasFileUpload: false, metadata: { type: 'admin', clientId: crypto.randomUUID(), label: category ? 'Kategorie bearbeiten' : 'Kategorie erstellen' } })`
    - KEIN setLoading im Offline-Pfad (ist synchron genug)

    **AdminJahrgaengeePage.tsx** (QUE-A10, QUE-A11):
    - handleSubmit Zeile ~119: `if (jahrgang) { PUT } else { POST }`
    - Offline: Gleiches Pattern, url: `/admin/jahrgaenge/...`, label: 'Jahrgang erstellen/bearbeiten'

    **AdminCertificatesPage.tsx** (QUE-A14, QUE-A15):
    - handleSubmit Zeile ~201: `if (certificateType) { PUT } else { POST }`
    - Offline: url: `/teamer/certificate-types/...`, label: 'Zertifikat erstellen/bearbeiten'

    3. Submit-Button in allen 3 Dateien: Diese Buttons haben aktuell KEIN `!isOnline` disabled (die Pages haben isOnline nur fuer delete-Guards). Das ist gut — Buttons bleiben wie sie sind.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/pages/AdminCategoriesPage.tsx liefert mindestens 1
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx liefert mindestens 1
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/pages/AdminCertificatesPage.tsx liefert mindestens 1
    - grep -c "networkMonitor.isOnline" in allen 3 Dateien liefert jeweils mindestens 1
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>AdminCategoriesPage, AdminJahrgaengeePage und AdminCertificatesPage haben Online/Offline-Branching fuer erstellen/bearbeiten.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityRequestModal + AdminActivityRequestsPage + BonusModal + ActivityModal queue-faehig</name>
  <files>
    frontend/src/components/admin/modals/ActivityRequestModal.tsx
    frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx
    frontend/src/components/admin/modals/BonusModal.tsx
    frontend/src/components/admin/modals/ActivityModal.tsx
  </files>
  <read_first>
    frontend/src/components/admin/modals/ActivityRequestModal.tsx
    frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx (Zeilen 85-120)
    frontend/src/components/admin/modals/BonusModal.tsx
    frontend/src/components/admin/modals/ActivityModal.tsx
  </read_first>
  <action>
    In jeder der 4 Dateien: Import writeQueue + networkMonitor, Online/Offline-Branching, Submit-Button "Du bist offline" entfernen.

    **Admin ActivityRequestModal.tsx** (QUE-A18 — genehmigen/ablehnen):
    - handleSubmit Zeile ~116: `api.put(\`/admin/activities/requests/${request.id}\`, { status, admin_comment })`
    - Offline-Pfad: `writeQueue.enqueue({ method: 'PUT', url: \`/admin/activities/requests/${request.id}\`, body: { status: selectedAction === 'approve' ? 'approved' : 'rejected', admin_comment: adminComment }, maxRetries: 5, hasFileUpload: false, metadata: { type: 'admin', clientId: crypto.randomUUID(), label: \`Antrag ${selectedAction === 'approve' ? 'genehmigen' : 'ablehnen'}\` } })`
    - Nach enqueue: Gleiche Success-Meldung + window.dispatchEvent(new CustomEvent('requestStatusChanged')) + onSuccess() + onClose()
    - Submit-Button: `!isOnline` aus disabled ENTFERNEN, "Du bist offline" Text ENTFERNEN

    **AdminActivityRequestsPage.tsx** (QUE-A19 — zuruecksetzen):
    - handleResetRequest Zeile ~90: Hat `if (!isOnline) return;` Guard — diesen ENTFERNEN
    - Im Alert-Handler: Online/Offline-Branching einfuegen
    - Online: bestehender Code (api.put + refreshRequests)
    - Offline: `writeQueue.enqueue({ method: 'PUT', url: \`/admin/activities/requests/${request.id}/reset\`, body: {}, maxRetries: 5, hasFileUpload: false, metadata: { type: 'admin', clientId: crypto.randomUUID(), label: 'Antrag zuruecksetzen' } })`
    - Import writeQueue + networkMonitor hinzufuegen

    **BonusModal.tsx** (QUE-A20 — Bonus-Punkte vergeben):
    - handleSave Zeile ~48: `api.post(\`/admin/konfis/${konfiId}/bonus-points\`, { points, type, description, completed_date })`
    - Offline-Pfad: writeQueue.enqueue mit method POST, gleicher url + body
    - metadata: `{ type: 'admin', clientId: crypto.randomUUID(), label: 'Bonus-Punkte vergeben' }`
    - Nach enqueue: onSave() + handleClose()
    - Submit-Button: `!isOnline` aus disabled ENTFERNEN, "Du bist offline" Text ENTFERNEN

    **ActivityModal.tsx** (QUE-A21 — Aktivitaet zuweisen):
    - handleSave Zeile ~101: `api.post(\`/admin/konfis/${konfiId}/activities\`, { activity_id, completed_date, comment })`
    - Offline-Pfad: writeQueue.enqueue mit method POST, gleicher url + body
    - metadata: `{ type: 'admin', clientId: crypto.randomUUID(), label: 'Aktivitaet zuweisen' }`
    - Nach enqueue: setIsDirty(false); onSave(); doClose();
    - Submit-Button: `!isOnline` aus disabled ENTFERNEN, "Du bist offline" Text ENTFERNEN
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/modals/ActivityRequestModal.tsx liefert mindestens 1
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/pages/AdminActivityRequestsPage.tsx liefert mindestens 1
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/modals/BonusModal.tsx liefert mindestens 1
    - grep -c "writeQueue.enqueue" frontend/src/components/admin/modals/ActivityModal.tsx liefert mindestens 1
    - grep "Du bist offline" in BonusModal.tsx, ActivityModal.tsx, ActivityRequestModal.tsx liefert jeweils 0 Treffer
    - grep "if (!isOnline) return" in AdminActivityRequestsPage.tsx handleResetRequest liefert 0 Treffer (Guard entfernt)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>ActivityRequestModal (genehmigen/ablehnen), AdminActivityRequestsPage (zuruecksetzen), BonusModal und ActivityModal haben Online/Offline-Branching.</done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
- Alle 7 Dateien enthalten `writeQueue.enqueue`
- Alle 7 Dateien enthalten `networkMonitor.isOnline`
- Kein "Du bist offline" Text mehr in den Submit-Buttons der Modals (BonusModal, ActivityModal, ActivityRequestModal)
- grep -r "writeQueue.enqueue" auf alle 7 Dateien liefert mindestens 8 Treffer
</verification>

<success_criteria>
10 Requirements abgedeckt: QUE-A08..A11, QUE-A14..A15, QUE-A18..A21.
Alle 7 Admin-Dateien funktionieren online wie bisher UND offline via WriteQueue.
</success_criteria>

<output>
After completion, create `.planning/phases/61-admin-teamer-queue/61-02-SUMMARY.md`
</output>
