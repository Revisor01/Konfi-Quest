---
phase: 119-konfispruch-jahrgang-steuerzentrale
plan: 05
type: execute
wave: 2
depends_on: [119-01]
files_modified:
  - backend/routes/jahrgaenge.js
  - backend/services/emailService.js
  - backend/tests/routes/jahrgaenge.test.js
  - frontend/src/components/admin/modals/AttendanceMatrixModal.tsx
autonomous: false
requirements: [SPRUCH-09, SPRUCH-10]
must_haves:
  truths:
    - "Anwesenheitsmatrix hat eine Umschaltung (IonSegment) Anwesenheit / Spruch"
    - "Die Spruch-Ansicht ist eine Liste Konfi -> gewaehlter Spruch (oder noch keiner)"
    - "Admin kann sich die Anwesenheitsmatrix ODER die Sprueche-Liste per E-Mail an die eigene Adresse schicken"
    - "Die Sprueche-Liste-E-Mail enthaelt pro Konfi Name + Konfirmationstermin (aus is_konfirmation-Event) + Spruch"
  artifacts:
    - path: "backend/routes/jahrgaenge.js"
      provides: "GET sprueche-Liste je Jahrgang + POST E-Mail-Versand (Anwesenheit/Sprueche)"
      contains: "sprueche"
    - path: "backend/services/emailService.js"
      provides: "neue Versand-Funktion fuer Matrix/Sprueche-E-Mail an Admin selbst"
      contains: "Anwesenheit"
    - path: "frontend/src/components/admin/modals/AttendanceMatrixModal.tsx"
      provides: "IonSegment Anwesenheit/Spruch + E-Mail-Versand-Button"
      contains: "IonSegment"
  key_links:
    - from: "backend jahrgaenge.js Sprueche-Endpoint"
      to: "konfsprueche / konfspruch_uebersetzungen + is_konfirmation-Event"
      via: "Builder + Konfirmationstermin-Query"
      pattern: "konfspruch|is_konfirmation"
    - from: "backend E-Mail-Endpoint"
      to: "emailService + users.email (req.user.id)"
      via: "Versand an Admin-eigene Adresse"
      pattern: "email"
    - from: "AttendanceMatrixModal IonSegment"
      to: "Spruch-Ansicht + Mail-Button"
      via: "State-Umschaltung + api-Post"
      pattern: "IonSegment"
---

<objective>
Anwesenheitsmatrix-Umschaltung + E-Mail-Versand (SPRUCH-09/10, D-07..D-10). Die Matrix bekommt eine IonSegment-Umschaltung Anwesenheit/Spruch. Die Spruch-Ansicht ist eine LISTE Konfi -> gewaehlter Spruch (D-07, kein Zellen-Layout). Der Admin kann sich aus der Ansicht heraus per E-Mail an die EIGENE Adresse (D-08) entweder die Anwesenheitsmatrix oder die Sprueche-Liste schicken (fuers Buero). Die Sprueche-Liste-E-Mail enthaelt pro Konfi Name + Konfirmationstermin (aus is_konfirmation-Event, D-05/D-09) + Spruch.

Purpose: Admin kann Anwesenheit + Sprueche aus der Matrix ans Buero weitergeben, ohne Chat.
Output: Backend Sprueche-Daten + E-Mail-Endpoint (emailService), Frontend Segment + Mail-Button.

Abhaengigkeit: 119-01 (jahrgaenge.js wird dort ebenfalls angefasst — diese Datei gehoert sequenziell nach 119-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-CONTEXT.md
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-01-SUMMARY.md
@.planning/phases/118-konfispruch-datenmodell/118-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- konfspruch-Builder (konfi.js:486-522) — fuer die Sprueche-Liste pro Konfi -->
Listen-Wahl: Referenz + Text der gewaehlten Uebersetzung; Freitext: Text + Referenz; sonst null. Org-gescopt (organization_id IS NULL OR = org).
WICHTIG (W1): translation = konfi.konfspruch_translation || 'luther2017'. Quelle ist die DEDIZIERTE Spalte kp.konfspruch_translation (wie konfi.js:358/491), NICHT kp.bible_translation — letzteres ist die separate Tageslosungs-Praeferenz (konfi.js:1539/2076) und darf hier NICHT verwendet werden. Beim Laden der Konfis das Feld kp.konfspruch_translation mitselektieren.

<!-- is_konfirmation-Event als Konfirmationstermin (D-05/D-09) -->
SELECT MIN(e.event_date) FROM events e JOIN event_jahrgang_assignments eja ON e.id=eja.event_id WHERE eja.jahrgang_id=$1 AND e.is_konfirmation=true AND e.organization_id=$2 AND (e.cancelled IS NULL OR e.cancelled=false). Termin pro Jahrgang (alle Konfis eines Jahrgangs teilen den Termin).

<!-- jahrgaenge.js (verifiziert) -->
- attendance-matrix GET /:id/attendance-matrix Z265-313 (requireAdmin): liefert jahrgang/konfis/events/bookings. Muster fuer Org-Scope + Konfi-Query (Z274-283).
- NEU GET /:id/sprueche (requireAdmin, org-gescopt): Konfis des Jahrgangs (wie Z274-283, inkl. kp.konfspruch_id/konfspruch_freitext/konfspruch_freitext_referenz/konfspruch_translation) + je Konfi den Builder-Spruch (konfsprueche/uebersetzungen) -> Array { user_id, display_name, konfspruch }.
- NEU POST /:id/matrix-email (requireAdmin): Body { type: 'anwesenheit' | 'sprueche' }. Laedt users.email des Admins (SELECT email FROM users WHERE id=$reqUserId) — req.user hat KEINE email (rbac.js liefert sie nicht). Fehlt die Adresse -> 400 ("Keine E-Mail-Adresse hinterlegt"). Baut den Inhalt (Anwesenheitsmatrix bzw. Sprueche-Liste mit Name+Konfirmationstermin+Spruch) und ruft die neue emailService-Funktion an die Admin-Adresse. Antwort { success:true }.

<!-- emailService.js (verifiziert) -->
- Muster sendLicenseExpiryReminderEmail Z228-255: baut subject/text/html (wrapHtml) und ruft sendEmail({to,subject,text,html}). module.exports Z276ff.
- NEU sendKonfiMatrixEmail(email, adminName, jahrgangName, type, rows): type='anwesenheit' -> Tabelle/Text Konfi x Anwesenheits-Zusammenfassung; type='sprueche' -> pro Konfi Name + Konfirmationstermin + Spruch (Listen: Referenz+Text, Freitext: Text+Referenz, sonst "noch keiner"). Format/Layout Planner-Discretion (D-10): einfache HTML-Tabelle + Text-Fallback. In module.exports aufnehmen. Deutsche Texte, echte Umlaute, KEINE Emojis.

<!-- AttendanceMatrixModal.tsx (verifiziert) -->
- Z83-154 State/Loader; getCellStatus Z122-128; konfiStats Z131-143; filteredKonfis Z146-154.
- NEU viewMode-State 'anwesenheit'|'sprueche' + IonSegment (Muster RequestsView.tsx:135-151) im Header/oberhalb der Matrix. Bei 'sprueche': GET /admin/jahrgaenge/:id/sprueche laden + als Liste Konfi -> Spruch rendern (kein Zellen-Layout). Bei 'anwesenheit': bestehende Matrix.
- NEU Mail-Button (IonButton im Toolbar slot=end oder unter der Ansicht): ruft POST /admin/jahrgaenge/:id/matrix-email mit { type: viewMode }. Erfolg -> setSuccess("E-Mail an deine Adresse gesendet"); Fehler -> setError. useApp liefert setError (Z88) — setSuccess ggf. ergaenzen.
- IonSegment/IonSegmentButton + ggf. IonLabel zu den @ionic/react-Imports hinzufuegen. IonIcon mail/sendOutline aus ionicons/icons. KEINE Emojis, echte Umlaute.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — Sprueche-Liste + E-Mail-Versand-Endpoints</name>
  <files>backend/routes/jahrgaenge.js, backend/services/emailService.js, backend/tests/routes/jahrgaenge.test.js</files>
  <behavior>
    - GET /admin/jahrgaenge/:id/sprueche -> Array { user_id, display_name, konfspruch } je Konfi (org-gescopt).
    - POST /admin/jahrgaenge/:id/matrix-email { type:'anwesenheit' } -> 200 { success:true } (E-Mail an Admin-Adresse).
    - POST mit { type:'sprueche' } -> 200; E-Mail enthaelt pro Konfi Name + Konfirmationstermin + Spruch.
    - Admin ohne hinterlegte E-Mail -> 400.
    - Fremder Jahrgang (andere Org) -> 404.
  </behavior>
  <action>jahrgaenge.js: GET /:id/sprueche (requireAdmin, Org-Scope wie attendance-matrix) — Konfis laden (Muster Z274-283) inkl. kp.konfspruch_id/konfspruch_freitext/konfspruch_freitext_referenz/kp.konfspruch_translation + je Konfi konfspruch-Builder (konfi.js:486-522) anwenden; Array zurueck. WICHTIG (W1): Uebersetzung aus kp.konfspruch_translation (NICHT kp.bible_translation). POST /:id/matrix-email (requireAdmin): Jahrgang org-validieren (404 sonst); Admin-Email per SELECT email FROM users WHERE id=$reqUserId laden (400 wenn leer); Konfirmationstermin via is_konfirmation-Event-Query (D-09) ermitteln; bei type='sprueche' die Sprueche-Liste (Name+Termin+Spruch) bauen, bei type='anwesenheit' die Matrix-Zusammenfassung; emailService.sendKonfiMatrixEmail aufrufen. emailService.js: sendKonfiMatrixEmail nach Muster sendLicenseExpiryReminderEmail ergaenzen + exportieren. Tests in jahrgaenge.test.js: GET sprueche (mit/ohne Spruch), POST matrix-email beide Typen (emailService ggf. mocken), 400 ohne Email, 404 fremder Jahrgang. Deutsche Texte, echte Umlaute, keine Emojis.</action>
  <verify>
    <automated>cd backend && node --check routes/jahrgaenge.js && node --check services/emailService.js && node --check tests/routes/jahrgaenge.test.js && grep -q "sprueche" routes/jahrgaenge.js && grep -q "matrix-email" routes/jahrgaenge.js && grep -q "konfspruch_translation" routes/jahrgaenge.js && grep -q "sendKonfiMatrixEmail" services/emailService.js && echo ok</automated>
  </verify>
  <done>Sprueche-Endpoint + matrix-email-Endpoint vorhanden (Uebersetzung aus konfspruch_translation); emailService.sendKonfiMatrixEmail exportiert. Tests syntaktisch valide (CI fuehrt aus).</done>
</task>

<task type="auto">
  <name>Task 2: AttendanceMatrixModal — Segment Anwesenheit/Spruch + Mail-Button</name>
  <files>frontend/src/components/admin/modals/AttendanceMatrixModal.tsx</files>
  <action>viewMode-State ('anwesenheit'|'sprueche') + IonSegment/IonSegmentButton (Muster RequestsView.tsx:135-151) oberhalb der Matrix. Bei 'sprueche': GET /admin/jahrgaenge/${jahrgangId}/sprueche laden und als Liste Konfi -> Spruch rendern (Listen: Referenz+Text, Freitext: Text+Referenz, sonst "noch keiner"); KEINE Zellen-Matrix (D-07). Mail-Button: POST /admin/jahrgaenge/${jahrgangId}/matrix-email mit { type: viewMode }; Erfolg -> setSuccess, Fehler -> setError. IonSegment/IonSegmentButton + Mail-IonIcon importieren. useApp ggf. um setSuccess erweitern. Deutsche UI-Texte, echte Umlaute, gendern wo passend, KEINE Emojis, useIonModal/Modal-Muster unangetastet.</action>
  <verify>
    <automated>cd frontend && grep -q "IonSegment" src/components/admin/modals/AttendanceMatrixModal.tsx && grep -q "matrix-email" src/components/admin/modals/AttendanceMatrixModal.tsx && grep -q "sprueche" src/components/admin/modals/AttendanceMatrixModal.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AttendanceMatrixModal" | head; echo done</automated>
  </verify>
  <done>Modal hat Anwesenheit/Spruch-Umschaltung (Spruch=Liste) + Mail-Button (Versand an eigene Adresse); tsc ohne neue Fehler in der Datei.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Anwesenheitsmatrix mit Umschaltung Anwesenheit/Spruch und E-Mail-Versand (Anwesenheit ODER Sprueche-Liste) an die eigene Admin-Adresse. Frontend-Aenderung erfordert Xcode-Build.</what-built>
  <how-to-verify>
    1. App bauen + starten, als Admin mit hinterlegter E-Mail einloggen.
    2. Anwesenheitsmatrix oeffnen -> IonSegment Anwesenheit/Spruch. Auf "Spruch" -> Liste Konfi -> gewaehlter Spruch (oder "noch keiner"), KEINE Zellen.
    3. Mail-Button bei "Anwesenheit" -> E-Mail mit Anwesenheitsmatrix kommt an die eigene Adresse.
    4. Mail-Button bei "Spruch" -> E-Mail mit Sprueche-Liste (pro Konfi Name + Konfirmationstermin + Spruch) kommt an.
    5. Admin ohne E-Mail-Adresse -> klare Fehlermeldung.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe Abweichungen</resume-signal>
</task>

</tasks>

<verification>
- Backend: /sprueche + /matrix-email (Uebersetzung aus konfspruch_translation); emailService.sendKonfiMatrixEmail.
- Frontend: IonSegment + Spruch-Liste + Mail-Button.
- node --check + tsc gruen; Integrationstests in CI.
</verification>

<success_criteria>
- Matrix umschaltbar Anwesenheit/Spruch (Spruch=Liste, SPRUCH-09).
- Admin schickt sich Anwesenheit ODER Sprueche-Liste (Name+Termin+Spruch) per E-Mail (SPRUCH-10, D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-05-SUMMARY.md` when done
</output>
