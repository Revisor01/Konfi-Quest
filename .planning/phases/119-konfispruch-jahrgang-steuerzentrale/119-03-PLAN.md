---
phase: 119-konfispruch-jahrgang-steuerzentrale
plan: 03
type: execute
wave: 3
depends_on: [119-01, 119-02]
files_modified:
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
autonomous: false
requirements: [SPRUCH-07]
must_haves:
  truths:
    - "Jahrgang-Modal hat einen Toggle Konfispruch-Auswahl (konfspruch_enabled)"
    - "Jahrgang-Modal hat einen Wrapped-Freigabe-Toggle (An=generieren mit Warnhinweis, Aus=loeschen)"
    - "Jahrgang-Modal enthaelt KEIN Konfirmationsdatum-Feld mehr"
    - "Jahrgang-Liste zeigt pro Jahrgang Punkteziele, Wrapped-Status (ob/wann), Spruch-Freigabe"
    - "Konfi-Dashboard-Card Konfispruch erscheint nur bei konfspruch_visible=true, sonst gar nicht"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx"
      provides: "konfspruch_enabled-Toggle, Wrapped-Toggle, confirmation_date entfernt, erweiterte Liste"
      contains: "konfspruch_enabled"
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Card-Gate fuer konfispruch-Sektion"
      contains: "konfspruch_visible"
  key_links:
    - from: "AdminJahrgaengeePage Wrapped-Toggle"
      to: "POST /admin/wrapped/generate/:id und DELETE /admin/wrapped/:id"
      via: "api-Aufruf beim Umlegen"
      pattern: "wrapped/generate|wrapped/"
    - from: "DashboardView konfispruch-Renderer"
      to: "dashboardData.konfspruch_visible"
      via: "Gate vor Sektion-Render"
      pattern: "konfspruch_visible"
---

<objective>
Frontend der Jahrgang-Steuerzentrale + Konfispruch-Card-Gate. Das Jahrgang-Modal (AdminJahrgaengeePage.tsx) wird zur zentralen Steuerstelle: Punkteziele (bestehend), Konfispruch-Freischaltung (konfspruch_enabled, D-01), Wrapped-Freigabe (Toggle, D-Wrapped-1). Das Konfirmationsdatum-Feld faellt komplett raus (D-04). Die Jahrgang-Liste zeigt die Zustaende auf einen Blick. Im Konfi-Dashboard erscheint die Konfispruch-Card nur noch bei konfspruch_visible=true (SPRUCH-07, D-02).

Purpose: Pro Jahrgang gebuendelte Steuerung; gefahrlos testbarer Wrapped-Toggle; Card-Gate haengt jetzt am Flag (statt 118-Stand "immer sichtbar").
Output: erweitertes Jahrgang-Modal + Liste, gegateter Konfispruch-Renderer.

Abhaengigkeit: 119-01 (Backend akzeptiert konfspruch_enabled, kein confirmation_date), 119-02 (konfspruch_visible im Dashboard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-CONTEXT.md
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-01-SUMMARY.md
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- AdminJahrgaengeePage.tsx (verifiziert) -->
- Jahrgang-Interface Z56-68: confirmation_date entfernen; konfspruch_enabled?: boolean + wrapped_released_at?: string | null ergaenzen.
- formData Z93-100 + useEffect-Init Z102-122 + Reset: confirmation_date raus; konfspruch_enabled (Default true) rein.
- handleSubmit Z124-183: confirmation_date-Pflichtcheck (Z130-133) + Payload-Feld (Z137) entfernen; konfspruch_enabled in Payload (Z135-142). Submit-Disabled (Z200) confirmation_date-Bedingung entfernen.
- Modal-UI: Jahrgang-Details-Card (Z221-241) Konfirmationsdatum-Item (Z234-237) + der IonDatetime-Block (Z323-338) ENTFERNEN. Punkte-Konfiguration-Card (Z244-321): Muster der bestehenden Toggle-Reihe (IonToggle className="app-toggle--jahrgang", Z254-262/Z286-294) fuer konfspruch_enabled-Toggle wiederverwenden (eigene Card-Section "Konfispruch & Wrapped" oder unter Punkte). Erklaertext erweitern (was hier gesteuert wird: Punkteziele, Konfispruch-Freischaltung, Wrapped-Freigabe).
- Liste Z532-556: app-list-item__meta zeigt aktuell confirmation_date (Z545-552). Ersetzen durch: Punkteziele (target_gottesdienst/target_gemeinde, nur aktive Typen), Spruch-Freigabe (konfspruch_enabled ja/nein), Wrapped-Status (wrapped_released_at: "gestartet am <Datum>" sonst "nicht freigegeben"). IonIcons aus ionicons/icons (z.B. checkmarkCircle/closeCircle/sparkles-Aequivalent vorhandener Icons), KEINE Emojis.
- Modal-Hook Z362-369 + useApp/api bereits vorhanden.

<!-- Wrapped-Endpoints (wrapped.js, gemountet unter /admin/wrapped) -->
- POST /admin/wrapped/generate/:jahrgangId -> generiert Snapshots + setzt wrapped_released_at=NOW + Push. requireAdmin.
- DELETE /admin/wrapped/:jahrgangId -> loescht Snapshots + setzt wrapped_released_at NULL. requireOrgAdmin.
Wrapped-Toggle-Logik (D-Wrapped-1): Toggle checked = (wrapped_released_at != null). Einschalten -> presentAlert Warnhinweis ("Wrapped wird fuer alle Konfis dieses Jahrgangs generiert und sie erhalten eine Push-Benachrichtigung. Fortfahren?") -> bei Bestaetigung POST generate -> refresh. Ausschalten -> presentAlert ("Wrapped-Rueckblick fuer diesen Jahrgang loeschen?") -> DELETE -> refresh. Erneutes Einschalten generiert frisch (Endpoint ueberschreibt). useIonAlert ist bereits importiert (presentAlert Z359).

<!-- DashboardView.tsx + KonfiDashboardPage.tsx (verifiziert) -->
- DashboardView DashboardData-Interface Z44-66: konfspruch_visible?: boolean ergaenzen.
- Section-Render-Loop Z330 (map ueber sectionOrder), Z656 return sectionRenderers[key]?.(). Gate: wenn key==='konfispruch' && dashboardData.konfspruch_visible !== true -> return null (Card komplett weg, D-02). Sauberster Ort: vor dem return in der map-Callback bzw. im konfispruch-Renderer-Eintrag.
- KonfiDashboardPage.tsx DashboardData-Interface (Z48-66 Bereich) + das an DashboardView durchgereichte dashboardData-Objekt (Z305-311 sectionOrder/merge): konfspruch_visible aus der /konfi/dashboard-Antwort durchreichen (Feld kommt aus 119-02). Pruefen wo dashboardData zusammengebaut wird und konfspruch_visible mitnehmen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jahrgang-Modal — konfspruch_enabled + Wrapped-Toggle, confirmation_date raus</name>
  <files>frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx</files>
  <action>Jahrgang-Interface, formData, useEffect-Init/Reset, handleSubmit, Submit-Disabled: confirmation_date vollstaendig entfernen (D-04), konfspruch_enabled (Default true) + wrapped_released_at ergaenzen. Im Modal: Konfirmationsdatum-IonItem (Z234-237) + IonDatetime-Block (Z323-338) entfernen. Neue Toggle-Reihe konfspruch_enabled nach Muster app-toggle--jahrgang (Z254-262). Wrapped-Freigabe-Toggle: checked = !!formData.wrapped_released_at; onIonChange ruft bei true presentAlert-Warnhinweis -> POST /admin/wrapped/generate/${jahrgang.id} -> onSuccess/refresh; bei false presentAlert -> DELETE /admin/wrapped/${jahrgang.id} -> refresh (D-Wrapped-1; Push beim Toggle ist gewuenscht, KEINE Unterdrueckung noetig — D-Wrapped-4: Standard belassen, im Warnhinweis erwaehnen). Wrapped-Toggle nur sinnvoll bei bestehendem Jahrgang (jahrgang != null) — bei Neuanlage ausblenden. Erklaertext im Modal erweitern. Deutsche UI-Texte, echte Umlaute (ü/ö/ä/ß), gendern wo passend, KEINE Emojis (nur IonIcons), useIonModal-Muster bleibt unangetastet.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AdminJahrgaengeePage" | head; grep -q "konfspruch_enabled" src/components/admin/pages/AdminJahrgaengeePage.tsx && grep -q "wrapped/generate" src/components/admin/pages/AdminJahrgaengeePage.tsx && ! grep -q "confirmation_date" src/components/admin/pages/AdminJahrgaengeePage.tsx && echo ok</automated>
  </verify>
  <done>Modal hat konfspruch_enabled-Toggle + Wrapped-Toggle (generate/delete mit Warnhinweis), kein confirmation_date mehr; tsc ohne neue Fehler in dieser Datei.</done>
</task>

<task type="auto">
  <name>Task 2: Jahrgang-Liste erweitern + Konfispruch-Card-Gate</name>
  <files>frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx, frontend/src/components/konfi/views/DashboardView.tsx, frontend/src/components/konfi/pages/KonfiDashboardPage.tsx</files>
  <action>Jahrgang-Liste (AdminJahrgaengeePage Z532-556): confirmation_date-Meta ersetzen durch Punkteziele (aktive Typen), Spruch-Freigabe (konfspruch_enabled) und Wrapped-Status (wrapped_released_at -> "Wrapped gestartet am <toLocaleDateString de-DE>" bzw. "nicht freigegeben"). Vorhandene IonIcons nutzen, keine Emojis. DashboardView: konfspruch_visible?: boolean ins DashboardData-Interface; im Section-Render (Z330/Z656) Gate setzen -> bei key==='konfispruch' und konfspruch_visible !== true return null. KonfiDashboardPage: konfspruch_visible aus der Dashboard-Antwort ins durchgereichte dashboardData uebernehmen (Interface + Merge-Stelle Z305-311). Deutsche Texte, echte Umlaute.</action>
  <verify>
    <automated>cd frontend && grep -q "konfspruch_visible" src/components/konfi/views/DashboardView.tsx && grep -q "konfspruch_visible" src/components/konfi/pages/KonfiDashboardPage.tsx && grep -q "wrapped_released_at" src/components/admin/pages/AdminJahrgaengeePage.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "DashboardView|KonfiDashboardPage|AdminJahrgaengeePage" | head; echo done</automated>
  </verify>
  <done>Liste zeigt Punkteziele/Wrapped-Status/Spruch-Freigabe; Konfispruch-Card erscheint nur bei konfspruch_visible=true; tsc ohne neue Fehler in den drei Dateien.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Jahrgang-Steuerzentrale (Modal mit konfspruch_enabled-Toggle + Wrapped-Freigabe-Toggle, kein Konfirmationsdatum mehr, erweiterte Liste) und Konfispruch-Card-Gate im Konfi-Dashboard. Frontend-Aenderungen erfordern einen Xcode-Build (auf diesem Mac nicht automatisierbar — kein cap-sync/Xcode non-interaktiv).</what-built>
  <how-to-verify>
    1. App bauen + auf Geraet/Simulator starten (npm run build + cap sync + Xcode), als Admin einloggen.
    2. Jahrgaenge -> Jahrgang oeffnen: Es gibt KEIN Konfirmationsdatum-Feld mehr. Toggle "Konfispruch-Auswahl" und (bei bestehendem Jahrgang) "Wrapped freigeben" sind sichtbar.
    3. Wrapped-Toggle einschalten -> Warnhinweis erscheint -> bestaetigen -> Erfolg. Liste zeigt "Wrapped gestartet am ...". Toggle ausschalten -> loescht -> Liste zeigt "nicht freigegeben". Erneut einschalten funktioniert.
    4. konfspruch_enabled fuer einen Test-Jahrgang AUSschalten -> als Konfi dieses Jahrgangs einloggen -> Dashboard zeigt KEINE Konfispruch-Card. Wieder einschalten -> Card erscheint.
    5. Liste zeigt pro Jahrgang Punkteziele + Wrapped-Status + Spruch-Freigabe.
    6. (W3 — Sicht-Check Konfirmationstermin) Als Konfi mit gesetztem is_konfirmation-Event (Phase 117) das Profil/Dashboard oeffnen: Der angezeigte Konfirmationstermin (ProfileView.tsx / Dashboard-Termin) stimmt weiterhin — die Backend-Quelle ist von jahrgaenge.confirmation_date auf das is_konfirmation-Event gewechselt (119-02). Kein Konfirmations-Event -> kein Termin/keine Tage-Anzeige (statt veraltetem Datum). Bei Abweichung melden.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe Abweichungen</resume-signal>
</task>

</tasks>

<verification>
- Modal: konfspruch_enabled + Wrapped-Toggle, kein confirmation_date.
- Liste: Punkteziele + Wrapped-Status + Spruch-Freigabe.
- Card-Gate: konfspruch_visible steuert Sichtbarkeit.
- tsc ohne neue Fehler in betroffenen Dateien; Human-Verify nach Xcode-Build (inkl. Konfirmationstermin-Sicht-Check W3).
</verification>

<success_criteria>
- Jahrgang-Modal ist zentrale Steuerstelle (Punkte + Konfispruch + Wrapped), ohne Konfirmationsdatum.
- Wrapped gefahrlos per Toggle ein-/ausschaltbar (generate/delete) mit Warnhinweis.
- Konfispruch-Card haengt am Flag (komplett weg bei false).
</success_criteria>

<output>
Create `.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-03-SUMMARY.md` when done
</output>
