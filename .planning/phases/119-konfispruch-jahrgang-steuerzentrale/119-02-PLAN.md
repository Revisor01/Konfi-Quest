---
phase: 119-konfispruch-jahrgang-steuerzentrale
plan: 02
type: execute
wave: 2
depends_on: [119-01]
files_modified:
  - backend/services/backgroundService.js
  - backend/routes/konfi.js
  - backend/tests/routes/konfi.test.js
autonomous: true
requirements: [SPRUCH-07]
must_haves:
  truths:
    - "Teamer-Wrapped-Cron triggert jaehrlich am 6.1. (nicht mehr 1. Dezember)"
    - "Es gibt KEINEN Konfi-Wrapped-Cron mehr, der auf confirmation_date keyt"
    - "Auto-Loesch-Service leitet den Konfirmations-Stichtag pro Jahrgang aus dem is_konfirmation-Event ab, NICHT mehr aus jahrgaenge.confirmation_date"
    - "Konfi-Dashboard berechnet keinen days_to_confirmation/confirmation_date mehr aus jahrgaenge.confirmation_date"
    - "GET /dashboard liefert ein Flag konfspruch_visible (aus jahrgaenge.konfspruch_enabled)"
  artifacts:
    - path: "backend/services/backgroundService.js"
      provides: "Teamer-Cron 6.1., Konfi-Cron-Block entfernt, Auto-Deletion auf is_konfirmation-Event"
      contains: "is_konfirmation"
    - path: "backend/routes/konfi.js"
      provides: "Dashboard/Profile ohne jahrgaenge.confirmation_date, Termin aus is_konfirmation-Event, konfspruch_visible-Flag"
      contains: "konfspruch_visible"
  key_links:
    - from: "backend/services/backgroundService.js runAutoDeletion"
      to: "events.is_konfirmation"
      via: "Stichtag-Query je Jahrgang aus is_konfirmation-Event"
      pattern: "is_konfirmation"
    - from: "backend/routes/konfi.js GET /dashboard"
      to: "jahrgaenge.konfspruch_enabled"
      via: "konfspruch_visible-Flag im Response"
      pattern: "konfspruch_enabled"
---

<objective>
Konfirmations-Termin-Umzug im Backend (D-04/D-05) + Backend-Seite des Konfispruch-Gates (SPRUCH-07, D-02): jahrgaenge.confirmation_date wird im Backend-Code nirgends mehr ausgewertet; der Termin lebt ausschliesslich als Konfi-Event mit is_konfirmation=true (Phase 117). Zusaetzlich liefert GET /dashboard ein Flag konfspruch_visible (analog has_wrapped), das aus jahrgaenge.konfspruch_enabled (Migration 094, 119-01) stammt — das Frontend rendert die Konfispruch-Card nur bei true (Card-Gate in 119-03).

Betroffen: Wrapped-Cron (Konfi-Trigger raus, Teamer auf 6.1.), Auto-Loesch-Service (DSG-EKD-Stichtag aus is_konfirmation-Event), Konfi-Dashboard/Profile (Termin + Location aus is_konfirmation-Event, alte String-Kategorie 'Konfirmation' raus, konfspruch_visible-Flag).

Purpose: Eine einzige, robuste Termin-Quelle (is_konfirmation-Flag) statt fragiler confirmation_date-Spalte + String-Kategorie-Erkennung. Backend-Gate fuer SPRUCH-07.
Output: bereinigter backgroundService.js + konfi.js, angepasste Tests.

WICHTIG (Sicherheitsnetz Auto-Loeschung): runAutoDeletion (Phase 114, DSG-EKD) keyt heute hart auf jahrgaenge.confirmation_date. Der Stichtag darf NICHT verloren gehen — er wird je Jahrgang aus dem fruehesten/relevanten is_konfirmation-Event abgeleitet. Hat ein Jahrgang KEIN is_konfirmation-Event, gilt: kein Stichtag -> keine Auto-Loeschung (sicherer Default, keine versehentliche Loeschung). Diese Semantik im Code-Kommentar festhalten.

Abhaengigkeit: 119-01 (Migration 094 liefert jahrgaenge.konfspruch_enabled), das Task 3 liest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-CONTEXT.md
@.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- is_konfirmation (Phase 117) -->
events.is_konfirmation BOOLEAN DEFAULT false (Migration 091). Events sind Jahrgaengen ueber event_jahrgang_assignments (eja.event_id, eja.jahrgang_id) zugeordnet. Org-Filter ueber e.organization_id.

<!-- jahrgaenge.konfspruch_enabled (119-01, Migration 094) -->
BOOLEAN NOT NULL DEFAULT true. Pro Konfi ueber kp.jahrgang_id JOIN jahrgaenge j erreichbar.

<!-- backgroundService.js (verifiziert) -->
- startWrappedCron Z400-419: cron '0 6 1 * *' (1. des Monats). Auf jaehrlich 6.1. -> '0 6 6 1 *' (6. Januar, 06:00), Kommentar/Log anpassen. timezone Europe/Berlin bleibt.
- checkWrappedTriggers Z435-491: Block 1 (Z442-460, Konfi-Wrapped aus confirmation_date) KOMPLETT ENTFERNEN (D-Wrapped-3). Block 2 (Z462-482, Teamer): today.getMonth()===11-Bedingung entfaellt (Cron feuert nur am 6.1.) -> Teamer fuer alle Orgs generieren (Idempotenz-Check bleibt). konfiJahrgaengeGenerated-Zaehler entfernen.
- runAutoDeletion Z687-768: laedt jahrgaenge inkl. confirmation_date (Z693-695), nutzt $2=jg.confirmation_date in Hard- (Z714-716) + Soft-Query (Z751-755). UMBAUEN auf is_konfirmation-Event-Stichtag (siehe Task 2).

<!-- konfi.js (verifiziert) -->
- GET /dashboard konfiQuery Z46-67: SELECT enthaelt j.confirmation_date (Z48) + LEFT JOIN-Subquery 'ce' (Z58-65) ueber c.name='Konfirmation'. Auf is_konfirmation umstellen, j.confirmation_date raus. j.konfspruch_enabled mitselektieren fuer das Flag.
- has_wrapped-Muster Z284-295 (EXISTS-Query) + Response Z315 — Blaupause fuer konfspruch_visible.
- daysToConfirmation Z179-186: aus konfi.confirmation_date -> aus is_konfirmation-Event-Datum.
- Response Z298-315: days_to_confirmation, confirmation_date, has_wrapped — konfspruch_visible ergaenzen.
- GET /profile Z360 (j.confirmation_date) + Z416-441 (confirmationQuery via LIKE '%konfirmation%') + Z534 (Fallback konfi.confirmation_date): auf is_konfirmation umstellen, Fallback entfernen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrapped-Cron auf 6.1. + Konfi-Cron-Block entfernen</name>
  <files>backend/services/backgroundService.js</files>
  <behavior>
    - startWrappedCron registriert Schedule '0 6 6 1 *' (6. Januar jaehrlich).
    - checkWrappedTriggers generiert NUR Teamer-Wrapped (alle Orgs, Idempotenz-Check), KEIN Konfi-Wrapped.
    - Kein Verweis auf jahrgaenge.confirmation_date in der Wrapped-Logik.
  </behavior>
  <action>startWrappedCron (Z400-419): cron '0 6 1 * *' -> '0 6 6 1 *', Log-Text + Kommentare auf "jaehrlich am 6.1. um 06:00" (D-Wrapped-2). checkWrappedTriggers (Z435-491): Block 1 (Konfi-Wrapped via confirmation_date, Z442-460) loeschen inkl. konfiJahrgaengeGenerated-Variable; Block 2 (Teamer) die today.getMonth()===11-Bedingung entfernen, Teamer-Generierung fuer alle Orgs ausfuehren (Idempotenz-Check via wrapped_snapshots bleibt). Abschluss-Log auf teamerOrgsGenerated reduzieren. Deutsche Kommentare, echte Umlaute.</action>
  <verify>
    <automated>cd backend && node --check services/backgroundService.js && grep -q "0 6 6 1 \*" services/backgroundService.js && ! grep -n "EXTRACT(MONTH FROM j.confirmation_date)" services/backgroundService.js && echo ok</automated>
  </verify>
  <done>Teamer-Cron feuert 6.1.; Konfi-Wrapped-Trigger via confirmation_date entfernt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-Loesch-Service auf is_konfirmation-Event-Stichtag</name>
  <files>backend/services/backgroundService.js</files>
  <behavior>
    - runAutoDeletion ermittelt pro Jahrgang den Stichtag aus dem is_konfirmation-Event (MIN event_date, nicht-cancelled, org-gescopt).
    - Jahrgang ohne is_konfirmation-Event wird uebersprungen (kein Soft/Hard-Delete).
    - 60/120-Tage-Schwellen unveraendert; Fehler-Isolation pro Jahrgang bleibt.
  </behavior>
  <action>runAutoDeletion (Z687-768): jahrgaenge-Query (Z693-695) ohne confirmation_date laden (id, organization_id). Pro Jahrgang zuerst Stichtag-Query auf is_konfirmation-Event (SELECT MIN(e.event_date) AS stichtag FROM events e JOIN event_jahrgang_assignments eja ON e.id=eja.event_id WHERE eja.jahrgang_id=$1 AND e.is_konfirmation=true AND e.organization_id=$2 AND (e.cancelled IS NULL OR e.cancelled=false)). stichtag NULL -> continue (Kommentar: kein Konfirmationstermin -> keine Aufbewahrungsfrist -> keine Loeschung, sicherer Default). Hard-Query (Z707-716) + Soft-Query (Z742-755): $2=jg.confirmation_date durch den ermittelten Stichtag ersetzen. Kommentare Z678/Z499 aktualisieren. Deutsche Kommentare, echte Umlaute.</action>
  <verify>
    <automated>cd backend && node --check services/backgroundService.js && grep -q "is_konfirmation" services/backgroundService.js && ! grep -n "confirmation_date" services/backgroundService.js && echo ok</automated>
  </verify>
  <done>Auto-Loeschung keyt auf is_konfirmation-Event; jahrgaenge.confirmation_date kommt in backgroundService.js nicht mehr vor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Konfi-Dashboard + Profile auf is_konfirmation-Event + konfspruch_visible-Flag</name>
  <files>backend/routes/konfi.js, backend/tests/routes/konfi.test.js</files>
  <behavior>
    - GET /dashboard: days_to_confirmation/confirmation_date/confirmation_location stammen aus dem is_konfirmation-Event (oder null), nicht aus jahrgaenge.confirmation_date oder categories 'Konfirmation'.
    - GET /dashboard liefert konfspruch_visible: true bei jahrgaenge.konfspruch_enabled=true, sonst false.
    - GET /profile: confirmation_date stammt aus is_konfirmation-Event (oder null), kein jahrgaenge-Fallback.
    - Kein Verweis auf j.confirmation_date in konfi.js.
  </behavior>
  <action>GET /dashboard: aus konfiQuery (Z46-67) j.confirmation_date entfernen, j.konfspruch_enabled mitselektieren; ce-Subquery (Z58-65) auf e.is_konfirmation=true (statt categories-Join) umstellen und Event-Datum mitselektieren (ce.location + ce.event_date). daysToConfirmation (Z179-186) aus diesem Event-Datum berechnen. konfspruch_visible = boolean von j.konfspruch_enabled (analog has_wrapped-Muster Z284-295/Z315). Response (Z298-315): confirmation_date = Event-Datum oder null; konfspruch_visible neben has_wrapped ergaenzen. GET /profile: Z360 j.confirmation_date entfernen; confirmationQuery (Z420-432) von c.name LIKE '%konfirmation%' auf e.is_konfirmation=true; Z534 confirmation_date: confirmationEventDate || konfi.confirmation_date -> confirmationEventDate (kein Fallback). Tests in konfi.test.js: Dashboard-/Profile-Faelle die confirmation_date aus jahrgaenge erwarten auf is_konfirmation-Event-Setup umstellen (Test-Event is_konfirmation=true + event_jahrgang_assignment + Buchung); je ein Fall konfspruch_enabled=true -> konfspruch_visible true und =false -> false. Deutsche Texte, echte Umlaute, keine Emojis.</action>
  <verify>
    <automated>cd backend && node --check routes/konfi.js && node --check tests/routes/konfi.test.js && grep -q "is_konfirmation" routes/konfi.js && grep -q "konfspruch_visible" routes/konfi.js && ! grep -n "j.confirmation_date" routes/konfi.js && echo ok</automated>
  </verify>
  <done>konfi.js leitet Konfirmationstermin/-ort aus is_konfirmation-Event ab, liefert konfspruch_visible; j.confirmation_date kommt nicht mehr vor. Tests syntaktisch valide (CI fuehrt aus).</done>
</task>

</tasks>

<verification>
- backgroundService.js: Cron 6.1., kein Konfi-Trigger, Auto-Deletion auf is_konfirmation-Event, kein confirmation_date.
- konfi.js: Dashboard + Profile auf is_konfirmation-Event, kein j.confirmation_date, konfspruch_visible-Flag vorhanden.
- node --check gruen; Integrationstests laufen in CI (kein lokales Docker/Test-Postgres).
</verification>

<success_criteria>
- jahrgaenge.confirmation_date wird im Backend-Code (Cron, Auto-Loeschung, Konfi-Dashboard/Profile) nicht mehr ausgewertet.
- DSG-EKD-Auto-Loeschung bleibt funktional, keyt auf is_konfirmation-Event, sicherer Default (kein Event -> keine Loeschung).
- Teamer-Wrapped jaehrlich 6.1.; Konfi-Wrapped nicht mehr per Cron.
- GET /dashboard liefert konfspruch_visible (SPRUCH-07 Backend-Gate).
</success_criteria>

<output>
Create `.planning/phases/119-konfispruch-jahrgang-steuerzentrale/119-02-SUMMARY.md` when done
</output>
