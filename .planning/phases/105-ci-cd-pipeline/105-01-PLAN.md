---
phase: 105-ci-cd-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/backend.yml
autonomous: true
requirements: [CIC-01, CIC-02, CIC-03, CIC-04]

must_haves:
  truths:
    - "Push auf main mit backend-Aenderungen startet CI Workflow mit Test-Jobs"
    - "Backend-Tests laufen gegen PostgreSQL Service Container und muessen gruen sein"
    - "Frontend-Tests laufen als eigener Job parallel zu Backend-Tests"
    - "npm audit bricht bei critical Vulnerabilities ab"
    - "Build+Deploy passiert NUR wenn beide Test-Jobs gruen sind"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI/CD Pipeline mit 3 Jobs"
      contains: "backend-test"
    - path: ".github/workflows/ci.yml"
      provides: "Frontend-Test Job"
      contains: "frontend-test"
  key_links:
    - from: "build-and-push job"
      to: "backend-test + frontend-test jobs"
      via: "needs: [backend-test, frontend-test]"
      pattern: "needs.*backend-test.*frontend-test"
    - from: "backend-test job"
      to: "PostgreSQL Service Container"
      via: "services.postgres"
      pattern: "postgres:16-alpine"
---

<objective>
CI/CD Pipeline: backend.yml wird zu ci.yml mit Test-Jobs als Deploy-Gate

Purpose: Tests muessen bei jedem Push automatisch laufen und Deployments bei Fehlern blockieren
Output: .github/workflows/ci.yml mit backend-test, frontend-test und build-and-push Jobs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-ci-cd-pipeline/105-CONTEXT.md
@.github/workflows/backend.yml
@backend/tests/vitest.config.ts
@backend/package.json
</context>

<interfaces>
<!-- Bestehender Workflow: backend.yml wird komplett ersetzt -->

From backend/package.json:
```json
"test:ci": "npx vitest run --config tests/vitest.config.ts"
```

From backend/tests/vitest.config.ts:
```typescript
env: {
  JWT_SECRET: 'test-secret-key-for-vitest',
  QR_SECRET: 'test-qr-secret-for-vitest',
  NODE_ENV: 'test',
  TEST_DATABASE_URL: 'postgresql://postgres:postgres@localhost:5432/postgres',
}
```

From frontend/package.json:
```json
"test.unit": "vitest"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ci.yml erstellen und backend.yml loeschen</name>
  <files>.github/workflows/ci.yml, .github/workflows/backend.yml</files>
  <action>
1. Loesche `.github/workflows/backend.yml` (per D-01: wird zu ci.yml umbenannt).

2. Erstelle `.github/workflows/ci.yml` mit folgendem KOMPLETTEM Inhalt:

```yaml
name: CI/CD Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
      - '.github/workflows/ci.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/konfi-quest-backend

jobs:
  backend-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Run npm audit
        working-directory: backend
        run: npm audit --audit-level=critical

      - name: Run backend tests
        working-directory: backend
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres
          JWT_SECRET: test-secret-ci
          NODE_ENV: test
        run: npm run test:ci

  frontend-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Run npm audit
        working-directory: frontend
        run: npm audit --audit-level=critical

      - name: Run frontend tests
        working-directory: frontend
        run: npx vitest run

  build-and-push:
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-test]
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trigger Portainer Webhook
        run: |
          curl -X POST "${{ secrets.PORTAINER_WEBHOOK }}" || true
```

Wichtige Design-Entscheidungen:
- Per D-01: backend.yml wird geloescht, ci.yml ersetzt es
- Per D-03: Drei Jobs — backend-test und frontend-test parallel, build-and-push danach
- Per D-04: build-and-push Job ist identisch zum bisherigen backend.yml
- Per D-07/D-08: PostgreSQL 16-alpine als Service Container auf Port 5432
- Per D-10: TEST_DATABASE_URL, JWT_SECRET, NODE_ENV als env im backend-test Job
- Per D-11: npm audit --audit-level=critical (nur Critical blockiert)
- Per D-13/D-14: Frontend-Test als eigener Job mit npx vitest run
- Claudes Discretion: Node modules Cache via setup-node cache, 10min Timeout, paths inkl. frontend/**
- npm audit laeuft in BEIDEN Jobs (backend + frontend) per D-11/CIC-03
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml | head -5 && echo "---" && ! test -f .github/workflows/backend.yml && echo "backend.yml geloescht" || echo "FEHLER: backend.yml existiert noch"</automated>
  </verify>
  <done>ci.yml existiert mit allen 3 Jobs (backend-test, frontend-test, build-and-push). backend.yml ist geloescht. build-and-push hat needs: [backend-test, frontend-test]. PostgreSQL Service Container konfiguriert. npm audit in beiden Test-Jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Workflow YAML validieren und Push-Test</name>
  <files>.github/workflows/ci.yml</files>
  <action>
1. Validiere die YAML-Syntax von ci.yml:
   - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` oder `npx yaml-lint .github/workflows/ci.yml`
   - Falls kein YAML-Linter vorhanden: `node -e "const fs=require('fs'); const y=require('yaml'); y.parse(fs.readFileSync('.github/workflows/ci.yml','utf8')); console.log('YAML valid')"`

2. Pruefe strukturelle Korrektheit:
   - ci.yml hat genau 3 Jobs: backend-test, frontend-test, build-and-push
   - build-and-push hat `needs: [backend-test, frontend-test]`
   - backend-test hat `services.postgres` mit postgres:16-alpine
   - npm audit --audit-level=critical ist in beiden Test-Jobs vorhanden
   - Trigger ist push auf main mit korrekten paths

3. Pruefe dass backend.yml wirklich geloescht ist (nicht nur umbenannt, sondern weg).

4. Wenn alles validiert: git add und commit beider Dateiaenderungen.
  </action>
  <verify>
    <automated>python3 -c "import yaml; data=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=list(data['jobs'].keys()); assert sorted(jobs)==['backend-test','build-and-push','frontend-test'], f'Jobs falsch: {jobs}'; assert 'backend-test' in data['jobs']['build-and-push']['needs']; assert 'frontend-test' in data['jobs']['build-and-push']['needs']; print('Workflow VALID: 3 jobs, needs-Dependency korrekt')"</automated>
  </verify>
  <done>ci.yml ist syntaktisch und strukturell korrekt. 3 Jobs vorhanden, needs-Dependency gesetzt, PostgreSQL Service Container konfiguriert, npm audit in beiden Test-Jobs, backend.yml geloescht.</done>
</task>

</tasks>

<verification>
1. `cat .github/workflows/ci.yml` — Workflow vollstaendig mit 3 Jobs
2. `! test -f .github/workflows/backend.yml` — Alte Datei geloescht
3. `grep -c "needs:" .github/workflows/ci.yml` — Mindestens 1 needs-Dependency
4. `grep "postgres:16-alpine" .github/workflows/ci.yml` — Service Container konfiguriert
5. `grep "audit-level=critical" .github/workflows/ci.yml` — npm audit korrekt
6. Nach Push auf main: GitHub Actions Tab zeigt 3 Jobs (backend-test, frontend-test, build-and-push)
</verification>

<success_criteria>
- ci.yml existiert mit backend-test, frontend-test und build-and-push Jobs
- backend.yml ist geloescht
- build-and-push laeuft NUR wenn beide Test-Jobs gruen sind (needs-Dependency)
- Backend-Tests laufen gegen PostgreSQL Service Container
- npm audit bricht bei critical Vulnerabilities ab
- Frontend-Tests laufen als separater paralleler Job
</success_criteria>

<output>
After completion, create `.planning/phases/105-ci-cd-pipeline/105-01-SUMMARY.md`
</output>
