---
phase: 37-dashboard-widget-anwesenheitsstatistik
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements: [EUI-02, EUI-03, ANW-01, ANW-02]

must_haves:
  truths:
    - "KonfiDetailView zeigt Anwesenheits-Sektion mit Quote 'X/Y anwesend (Z%)' und IonProgressBar"
    - "Verpasste Pflicht-Events werden als Liste angezeigt mit Unterscheidung Opt-out vs Nicht erschienen"
    - "Dashboard Events-Widget zeigt bring_items bereits korrekt an (Verifikation, keine Aenderung noetig)"
    - "show_events Toggle steuert Events-Widget im Dashboard (Verifikation, keine Aenderung noetig)"
  artifacts:
    - frontend/src/components/admin/views/KonfiDetailView.tsx
  key_links:
    - "Nutzt GET /admin/konfis/:id/attendance-stats aus Plan 37-01"
    - "Folgt bestehendem IonList/IonListHeader/IonCard Pattern der KonfiDetailView"
---

<objective>
Anwesenheitsstatistik-Sektion in der Admin KonfiDetailView und Verifikation des Dashboard Events-Widgets.

Purpose: Admin kann pro Konfi die Pflicht-Event-Anwesenheit einsehen. EUI-02/EUI-03 werden verifiziert (bereits implementiert).
Output: Neue Anwesenheits-Sektion in KonfiDetailView.tsx
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dashboard-widget-anwesenheitsstatistik/37-RESEARCH.md
@.planning/phases/37-dashboard-widget-anwesenheitsstatistik/37-01-SUMMARY.md
@frontend/src/components/admin/views/KonfiDetailView.tsx
@frontend/src/components/konfi/views/DashboardView.tsx
</context>

<interfaces>
<!-- Attendance Stats API Response (aus Plan 37-01): -->
GET /admin/konfis/:id/attendance-stats
Response: {
  total_mandatory: number,
  attended: number,
  percentage: number,
  missed_events: [{
    event_id: number,
    event_name: string,
    event_date: string,
    location: string,
    status: 'opted_out' | 'absent',
    opt_out_reason: string | null
  }]
}

<!-- Bestehende KonfiDetailView Patterns: -->
- Sektionen: IonList className="app-section-inset" inset={true} > IonListHeader > IonCard className="app-card" > IonCardContent
- Icons: IonIcon aus ionicons/icons (KEINE Emojis!)
- Meta: app-list-item__meta, app-list-item__meta-item
- Farben: Blau #3b82f6, Gruen #059669, Rot #ef4444, Orange #f59e0b
- Leerer Zustand: app-empty-state > app-empty-state__text

<!-- Dashboard Events-Widget (bereits implementiert, Zeile 828-943 in DashboardView.tsx): -->
- show_events Toggle: dashboardConfig?.show_events !== false
- bring_items: event.bring_items && (...) mit bagHandle Icon
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Anwesenheitsstatistik-Sektion in KonfiDetailView</name>
  <files>frontend/src/components/admin/views/KonfiDetailView.tsx</files>
  <action>
KonfiDetailView.tsx erweitern um eine Anwesenheits-Sektion. Platzierung: **nach der Events-Sektion und vor der Aktivitaeten-Sektion**.

**1. Neue State-Variable und Lade-Logik:**

```typescript
const [attendanceStats, setAttendanceStats] = useState<{
  total_mandatory: number;
  attended: number;
  percentage: number;
  missed_events: Array<{
    event_id: number;
    event_name: string;
    event_date: string;
    location: string;
    status: 'opted_out' | 'absent';
    opt_out_reason: string | null;
  }>;
} | null>(null);
```

In `loadKonfiData()` nach dem bestehenden event-points API-Call hinzufuegen:
```typescript
try {
  const attendanceRes = await api.get(`/admin/konfis/${konfiId}/attendance-stats`);
  setAttendanceStats(attendanceRes.data);
} catch (attendanceError) {
  setAttendanceStats(null);
}
```

**2. Imports hinzufuegen:**
- `IonProgressBar` aus '@ionic/react' (falls noch nicht importiert)
- `checkmarkCircle` und `closeCircle` aus 'ionicons/icons' (fuer Anwesenheits-Icons)
- `eyeOff` aus 'ionicons/icons' (fuer Opt-out-Icon, falls noch nicht importiert)

**3. Anwesenheits-Sektion (JSX):**

Nach der Events-Sektion (`{/* Event Points */}` Block) und vor der Aktivitaeten-Sektion einfuegen:

```jsx
{/* Anwesenheit - Pflicht-Events */}
{attendanceStats && attendanceStats.total_mandatory > 0 && (
  <IonList className="app-section-inset" inset={true}>
    <IonListHeader>
      <div className="app-section-icon" style={{ backgroundColor: '#6366f1' }}>
        <IonIcon icon={calendar} />
      </div>
      <IonLabel>Anwesenheit</IonLabel>
    </IonListHeader>
    <IonCard className="app-card">
      <IonCardContent className="app-card-content">
        {/* Quote Summary */}
        <div style={{ marginBottom: '16px' }}>
          <div style={{
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
            marginBottom: '8px'
          }}>
            <span style={{ fontWeight: '600', fontSize: '0.95rem' }}>
              {attendanceStats.attended}/{attendanceStats.total_mandatory} anwesend
            </span>
            <span style={{
              fontWeight: '700',
              fontSize: '1.1rem',
              color: attendanceStats.percentage >= 80 ? '#059669' :
                     attendanceStats.percentage >= 50 ? '#f59e0b' : '#ef4444'
            }}>
              {attendanceStats.percentage}%
            </span>
          </div>
          <IonProgressBar
            value={attendanceStats.percentage / 100}
            color={attendanceStats.percentage >= 80 ? 'success' :
                   attendanceStats.percentage >= 50 ? 'warning' : 'danger'}
            style={{ borderRadius: '4px', height: '8px' }}
          />
        </div>

        {/* Verpasste Events Liste */}
        {attendanceStats.missed_events.length > 0 && (
          <>
            <div style={{
              fontSize: '0.8rem',
              fontWeight: '600',
              color: '#999',
              textTransform: 'uppercase',
              letterSpacing: '0.5px',
              marginBottom: '8px'
            }}>
              Verpasste Pflicht-Events
            </div>
            <IonList className="app-list-inner" lines="none">
              {attendanceStats.missed_events.map((event, index) => (
                <div
                  key={event.event_id}
                  className="app-list-item"
                  style={{
                    marginBottom: index < attendanceStats.missed_events.length - 1 ? '8px' : '0',
                    borderLeftColor: event.status === 'opted_out' ? '#f59e0b' : '#ef4444'
                  }}
                >
                  <div className="app-list-item__row">
                    <div className="app-list-item__main">
                      <div
                        className="app-icon-circle"
                        style={{
                          backgroundColor: event.status === 'opted_out' ? '#f59e0b' : '#ef4444'
                        }}
                      >
                        <IonIcon icon={event.status === 'opted_out' ? eyeOff : closeCircle} />
                      </div>
                      <div className="app-list-item__content">
                        <div className="app-list-item__title">
                          {event.event_name}
                        </div>
                        <div className="app-list-item__meta">
                          <span className="app-list-item__meta-item">
                            <IonIcon icon={calendar} className="app-icon-color--events" />
                            {new Date(event.event_date).toLocaleDateString('de-DE', {
                              day: '2-digit',
                              month: '2-digit',
                              year: 'numeric'
                            })}
                          </span>
                          <span className="app-list-item__meta-item" style={{
                            color: event.status === 'opted_out' ? '#f59e0b' : '#ef4444'
                          }}>
                            {event.status === 'opted_out'
                              ? `Opt-out: ${event.opt_out_reason || 'Ohne Begruendung'}`
                              : 'Nicht erschienen'}
                          </span>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              ))}
            </IonList>
          </>
        )}
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```

**Wichtig:**
- KEINE Emojis verwenden! Nur IonIcons.
- `closeCircle` aus ionicons/icons importieren (fuer "Nicht erschienen")
- Die Sektion wird NUR angezeigt wenn es vergangene Pflicht-Events gibt (total_mandatory > 0)
- Farbschema: Gruen (>=80%), Gelb (50-79%), Rot (<50%) fuer die Quote
- Opt-out: Gelb/Orange (#f59e0b) mit eyeOff-Icon
- Absent: Rot (#ef4444) mit closeCircle-Icon
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - KonfiDetailView zeigt Anwesenheits-Sektion mit "X/Y anwesend (Z%)" und farbiger IonProgressBar
    - Verpasste Pflicht-Events werden als Liste mit Unterscheidung "Opt-out: [Grund]" vs "Nicht erschienen" angezeigt
    - Sektion erscheint nur wenn es vergangene Pflicht-Events gibt
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: Verifikation Dashboard Events-Widget (EUI-02, EUI-03)</name>
  <files>frontend/src/components/konfi/views/DashboardView.tsx</files>
  <action>
**Reine Verifikation - keine Code-Aenderungen erwartet.**

Verifiziere dass folgende Requirements bereits erfuellt sind:

1. **EUI-02**: Dashboard zeigt Events mit Titel, Datum, Ort und Was-mitbringen-Info
   - DashboardView.tsx Zeile 828: `dashboardConfig?.show_events !== false && regularEvents && regularEvents.length > 0`
   - Zeile 909: `event.title || event.name` (Titel)
   - Zeile 913: `formatEventDate(event.event_date || event.date)` (Datum)
   - Zeile 926: `event.location` (Ort)
   - Zeile 930-935: `event.bring_items && (...)` (Was mitbringen)

2. **EUI-03**: show_events Toggle steuert Widget
   - Zeile 828: `dashboardConfig?.show_events !== false` kontrolliert die gesamte Events-Sektion
   - Kein neuer show_next_event Toggle noetig (per User-Decision)

3. Backend liefert bring_items:
   - `/konfi/events` nutzt `SELECT e.*` was bring_items einschliesst
   - DashboardEvent Interface in types/dashboard.ts hat `bring_items?: string`

Falls alles korrekt: Keine Aenderungen noetig, nur im Summary dokumentieren.
Falls etwas fehlt: Entsprechend nachbessern.
  </action>
  <verify>
    <automated>cd frontend && grep -n "bring_items" src/components/konfi/views/DashboardView.tsx src/types/dashboard.ts</automated>
  </verify>
  <done>
    - bring_items wird im Dashboard Events-Widget angezeigt (wenn vorhanden)
    - show_events Toggle steuert Events-Widget Sichtbarkeit
    - DashboardEvent Interface enthaelt bring_items und mandatory Felder
  </done>
</task>

</tasks>

<verification>
- KonfiDetailView zeigt Anwesenheitsquote mit farbiger Progress-Bar (gruen/gelb/rot)
- Verpasste Events unterscheiden klar zwischen "Opt-out: [Grund]" und "Nicht erschienen"
- Dashboard Events-Widget zeigt bring_items mit bagHandle-Icon
- show_events Toggle blendet Events-Widget ein/aus
- TypeScript kompiliert fehlerfrei
</verification>

<success_criteria>
Admin sieht in der KonfiDetailView eine Anwesenheitsstatistik fuer Pflicht-Events mit Quote und verpassten Events. Dashboard zeigt Events mit bring_items-Info und ist per Toggle steuerbar.
</success_criteria>

<output>
After completion, create `.planning/phases/37-dashboard-widget-anwesenheitsstatistik/37-02-SUMMARY.md`
</output>
