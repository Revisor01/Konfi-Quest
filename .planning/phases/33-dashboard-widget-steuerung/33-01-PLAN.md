---
phase: 33-dashboard-widget-steuerung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
autonomous: true
requirements: [DSH-02, DSH-03]

must_haves:
  truths:
    - "Deaktivierte Dashboard-Sektionen werden nicht gerendert"
    - "Tageslosung-API-Call wird bei show_losung=false uebersprungen"
    - "Aenderungen wirken beim naechsten Dashboard-Laden (Pull-to-Refresh oder App-Oeffnen)"
    - "Header-Card (Begruessing, ActivityRings, Level) bleibt immer sichtbar"
    - "Wenn alle 5 Widgets deaktiviert sind, bleibt nur die Header-Card"
  artifacts:
    - path: "frontend/src/components/konfi/pages/KonfiDashboardPage.tsx"
      provides: "dashboard_config Extraktion und Prop-Weitergabe"
      contains: "dashboard_config"
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Bedingte Widget-Renders basierend auf dashboard_config"
      contains: "dashboardConfig"
  key_links:
    - from: "frontend/src/components/konfi/pages/KonfiDashboardPage.tsx"
      to: "frontend/src/components/konfi/views/DashboardView.tsx"
      via: "dashboardConfig prop"
      pattern: "dashboardConfig.*show_"
    - from: "frontend/src/components/konfi/views/DashboardView.tsx"
      to: "/api/konfi/tageslosung"
      via: "Bedingter API-Call nur wenn show_losung !== false"
      pattern: "dashboardConfig.*show_losung"
---

<objective>
Dashboard rendert nur die vom Org-Admin aktivierten Sektionen. Die 5 Toggle-Keys (show_konfirmation, show_events, show_losung, show_badges, show_ranking) werden aus dem bereits vorhandenen dashboard_config im Dashboard-API-Response ausgelesen und steuern die Sichtbarkeit der jeweiligen Sektionen in der DashboardView.

Purpose: Letzte Phase von v1.6 -- Org-Admins koennen das Konfi-Dashboard individuell konfigurieren
Output: DashboardView zeigt nur aktivierte Widget-Sektionen, Tageslosung-API wird bei Deaktivierung uebersprungen
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-dashboard-widget-steuerung/33-CONTEXT.md

@frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
@frontend/src/components/konfi/views/DashboardView.tsx

<interfaces>
<!-- Backend liefert dashboard_config bereits im GET /konfi/dashboard Response (Phase 30) -->

From backend/routes/konfi.js (L252-258):
```javascript
const dashboard_config = {
  show_konfirmation: dashboardMap.dashboard_show_konfirmation !== undefined ? dashboardMap.dashboard_show_konfirmation : true,
  show_events: dashboardMap.dashboard_show_events !== undefined ? dashboardMap.dashboard_show_events : true,
  show_losung: dashboardMap.dashboard_show_losung !== undefined ? dashboardMap.dashboard_show_losung : true,
  show_badges: dashboardMap.dashboard_show_badges !== undefined ? dashboardMap.dashboard_show_badges : true,
  show_ranking: dashboardMap.dashboard_show_ranking !== undefined ? dashboardMap.dashboard_show_ranking : true
};
```

From KonfiDashboardPage.tsx -- DashboardData Interface (L26-45):
```typescript
interface DashboardData {
  konfi: { ... };
  total_points: number;
  recent_badges: any[];
  badge_count: number;
  recent_events: any[];
  event_count: number;
  ranking: any[];
  days_to_confirmation?: number;
  confirmation_date?: string;
  point_config?: PointConfig;
  // dashboard_config fehlt noch im Interface!
}
```

From DashboardView.tsx -- Props Interface (L393-404):
```typescript
interface DashboardViewProps {
  dashboardData: DashboardData;
  dailyVerse: DailyVerse | null;
  badgeStats: BadgeStats;
  allBadges: AllBadgesData;
  upcomingEvents: DashboardEvent[];
  targetGottesdienst: number;
  targetGemeinde: number;
  gottesdienstEnabled?: boolean;
  gemeindeEnabled?: boolean;
  onOpenPointsHistory?: () => void;
  // dashboardConfig fehlt noch!
}
```

Sektions-Positionen in DashboardView.tsx:
- Konfirmation Card: L752 `{/* Konfirmation Card */}`
- Events Section: L810 `{/* Events Section */}`
- Tageslosung: L922 `{/* Tageslosung */}`
- Badges Section: L969 `{/* Badges Section */}`
- Ranking Section: L1243 `{/* Ranking Section */}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KonfiDashboardPage -- dashboard_config extrahieren und weitergeben</name>
  <files>frontend/src/components/konfi/pages/KonfiDashboardPage.tsx</files>
  <action>
1. DashboardConfig Interface definieren (oberhalb DashboardData):
```typescript
interface DashboardConfig {
  show_konfirmation: boolean;
  show_events: boolean;
  show_losung: boolean;
  show_badges: boolean;
  show_ranking: boolean;
}
```

2. DashboardData Interface erweitern um `dashboard_config?: DashboardConfig;`

3. In der return-Section: dashboardConfig aus dashboardData extrahieren (nach pointConfig, ca. L259):
```typescript
const dashboardConfig: DashboardConfig = {
  show_konfirmation: (dashboardData as any).dashboard_config?.show_konfirmation !== false,
  show_events: (dashboardData as any).dashboard_config?.show_events !== false,
  show_losung: (dashboardData as any).dashboard_config?.show_losung !== false,
  show_badges: (dashboardData as any).dashboard_config?.show_badges !== false,
  show_ranking: (dashboardData as any).dashboard_config?.show_ranking !== false,
};
```
Hinweis: `!== false` Pattern als Default true (bestehendes Pattern aus Phase 32 ActivityRings).

4. dashboardConfig als Prop an DashboardView weitergeben:
```typescript
<DashboardView
  ...
  dashboardConfig={dashboardConfig}
/>
```

5. Tageslosung-Load bedingt machen: `loadDailyVerse()` im useEffect (L117) und in handleRefresh (L234) nur aufrufen wenn `dashboardConfig.show_losung` true ist. Da dashboardData beim ersten Render noch null ist, loadDailyVerse() im initialen useEffect (L115-120) beibehalten aber in handleRefresh bedingt machen. Stattdessen: Die Logik in DashboardView verlagern (Task 2 kuemmert sich um den bedingten API-Call dort).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>DashboardData Interface enthaelt dashboard_config, DashboardConfig Interface existiert, dashboardConfig Prop wird an DashboardView uebergeben</done>
</task>

<task type="auto">
  <name>Task 2: DashboardView -- Sektionen bedingt rendern + Tageslosung-Call bedingt</name>
  <files>frontend/src/components/konfi/views/DashboardView.tsx</files>
  <action>
1. DashboardConfig Interface importieren oder lokal definieren (gleiche Struktur wie in KonfiDashboardPage):
```typescript
interface DashboardConfig {
  show_konfirmation: boolean;
  show_events: boolean;
  show_losung: boolean;
  show_badges: boolean;
  show_ranking: boolean;
}
```

2. DashboardViewProps erweitern um `dashboardConfig?: DashboardConfig;`

3. In der Komponente destructuren mit Default:
```typescript
const DashboardView: React.FC<DashboardViewProps> = ({
  ...bestehende props,
  dashboardConfig
}) => {
```

4. Tageslosung useEffect (L451-481) bedingt machen: Den API-Call `loadTageslosung()` nur ausfuehren wenn `dashboardConfig?.show_losung !== false`. Wenn false, setLoadingVerse(false) und setActualDailyVerse(null) aufrufen:
```typescript
useEffect(() => {
  if (dashboardConfig?.show_losung === false) {
    setLoadingVerse(false);
    setActualDailyVerse(null);
    return;
  }
  // ... bestehender Tageslosung-Load-Code ...
}, [dailyVerse, dashboardConfig?.show_losung]);
```

5. Jede der 5 Sektionen mit Config-Check umwickeln. Der Config-Check kommt VOR die bestehende Datenverfuegbarkeits-Pruefung:

- **Konfirmation Card** (L752): Bestehende Bedingung `{(dashboardData.days_to_confirmation !== null ...` erweitern:
  `{dashboardConfig?.show_konfirmation !== false && ((dashboardData.days_to_confirmation !== null && dashboardData.days_to_confirmation !== undefined) || nextConfirmationEvent) ? (`

- **Events Section** (L810): Bestehende Bedingung `{regularEvents && regularEvents.length > 0 && (` erweitern:
  `{dashboardConfig?.show_events !== false && regularEvents && regularEvents.length > 0 && (`

- **Tageslosung** (L922): Bestehende Bedingung `{!loadingVerse && actualDailyVerse && ...` erweitern:
  `{dashboardConfig?.show_losung !== false && !loadingVerse && actualDailyVerse && ...`

- **Badges Section** (L969): Bestehende Bedingung `{(allBadges.available.length > 0 || allBadges.earned.length > 0) && (` erweitern:
  `{dashboardConfig?.show_badges !== false && (allBadges.available.length > 0 || allBadges.earned.length > 0) && (`

- **Ranking Section** (L1243): Bestehende Bedingung `{dashboardData.ranking && dashboardData.ranking.length > 0 && (` erweitern:
  `{dashboardConfig?.show_ranking !== false && dashboardData.ranking && dashboardData.ranking.length > 0 && (`

WICHTIG: `!== false` als Pattern damit ohne Config (undefined) alles sichtbar bleibt (Backward-Compatibility).
WICHTIG: Header-Card (Begruessing, ActivityRings, Level-Info) wird NICHT umwickelt -- bleibt immer sichtbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Alle 5 Widget-Sektionen werden bedingt gerendert basierend auf dashboardConfig. Tageslosung-API-Call wird bei show_losung=false uebersprungen. Header-Card bleibt immer sichtbar. Ohne Config (Default) bleibt alles sichtbar.</done>
</task>

</tasks>

<verification>
1. TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
2. Frontend startet: `cd frontend && npm run dev` (kurz pruefen ob keine Runtime-Fehler)
3. Manuell pruefen: Dashboard zeigt alle Sektionen wenn alle Toggles an (Default-Verhalten unveraendert)
</verification>

<success_criteria>
- DashboardView rendert nur aktivierte Sektionen basierend auf dashboard_config aus dem API-Response
- Tageslosung-API-Call wird bei show_losung=false uebersprungen (spart Netzwerk-Request)
- Header-Card (Begruessing, ActivityRings, Level) bleibt immer sichtbar
- Default-Verhalten (ohne Config) zeigt alle Sektionen -- Backward-Compatibility
- Aenderungen an Admin-Settings-Toggles wirken beim naechsten Dashboard-Laden
</success_criteria>

<output>
After completion, create `.planning/phases/33-dashboard-widget-steuerung/33-01-SUMMARY.md`
</output>
