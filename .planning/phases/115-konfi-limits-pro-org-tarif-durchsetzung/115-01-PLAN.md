---
phase: 115-konfi-limits-pro-org-tarif-durchsetzung
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/migrations/083_org_konfi_limit.sql
  - backend/utils/konfiLimit.js
  - backend/tests/utils/konfiLimit.test.js
  - backend/routes/organizations.js
  - backend/tests/routes/organizations.test.js
  - backend/tests/helpers/seed.js
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-10]

must_haves:
  truths:
    - "organizations.max_konfis existiert als nullbare Spalte (NULL = unbegrenzt)"
    - "Bestehende Orgs behalten nach der Migration max_konfis NULL (kein Backfill)"
    - "checkKonfiLimit gibt fuer eine Org count, limit und stufe (under_limit / grace / hard_block) zurueck"
    - "Bei max_konfis NULL liefert checkKonfiLimit immer stufe under_limit"
    - "Soft-geloeschte Konfis (deleted_at IS NOT NULL) zaehlen NICHT in den COUNT"
    - "super_admin kann max_konfis ueber einen eigenen Endpunkt setzen"
    - "org_admin kann max_konfis NICHT setzen (403 am Limit-Endpunkt, Feld in PUT ignoriert)"
  artifacts:
    - path: "backend/migrations/083_org_konfi_limit.sql"
      provides: "organizations.max_konfis INTEGER NULL"
      contains: "max_konfis"
    - path: "backend/utils/konfiLimit.js"
      provides: "checkKonfiLimit(db, organizationId) Single Source of Truth"
      exports: ["checkKonfiLimit"]
      min_lines: 20
    - path: "backend/routes/organizations.js"
      provides: "super_admin-only PATCH /:id/limit"
      contains: "max_konfis"
  key_links:
    - from: "backend/routes/organizations.js"
      to: "organizations.max_konfis"
      via: "requireSuperAdmin PATCH-Endpunkt"
      pattern: "max_konfis"
    - from: "backend/utils/konfiLimit.js"
      to: "users / roles / konfi role"
      via: "COUNT konfis WHERE deleted_at IS NULL"
      pattern: "deleted_at IS NULL"
---

<objective>
Fundament der Konfi-Limit-Durchsetzung: Schema-Spalte, gemeinsame Pruef-Util als Single Source of
Truth und der super_admin-only Endpunkt zum Setzen des Limits.

Purpose: Beide Anlage-Wege (POST-Konfi-Route und Invite-Registrierung, Wave 2) brauchen dieselbe
Limit-Logik. Wie deleteKonfiCascade in Phase 114 wird die Pruefung EINMAL gebaut und von beiden
Aufrufern genutzt. Das Limit darf nur super_admin setzen (D-03/D-04) — niemals ueber die normale
Org-PUT-Route, die org_admin fuer die eigene Org erlaubt.
Output: Migration 083, backend/utils/konfiLimit.js, super_admin PATCH-Endpunkt, Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md
@.planning/phases/114-self-loeschung-auto-loeschung/114-01-SUMMARY.md

<interfaces>
<!-- Muster aus Phase 114, direkt uebernehmen. Keine Codebase-Exploration noetig. -->

Migration-Muster (backend/migrations/082_self_auto_deletion.sql):
- Datei wird vom Runner (backend/database.js) alphabetisch sortiert in EINEM pool.query ausgefuehrt.
- Idempotent: ALTER TABLE ... ADD COLUMN IF NOT EXISTS.
- KEIN Backfill bei dieser Migration (D-01: bestehende UND neue Orgs starten auf NULL).

Util-Muster (backend/utils/konfiDeletion.js):
  async function deleteKonfiCascade(client, userId, organizationId) { ... }
  module.exports = { deleteKonfiCascade };

organizations.js Modul-Signatur:
  module.exports = (db, rbacVerifier, { requireSuperAdmin }) => { ... }
  Vorhandene Endpunkte: POST '/' (requireSuperAdmin), PUT '/:id' (org_admin via isOwnOrg erlaubt!),
  DELETE '/:id' (requireSuperAdmin), GET '/:id/stats' (eigene Org).

Konfi-Zaehlung — WICHTIG: NICHT konfi_profiles zaehlen (filtert deleted_at nicht). Stattdessen:
  SELECT COUNT(*)::int FROM users u
  JOIN roles r ON u.role_id = r.id
  WHERE r.name = 'konfi' AND u.organization_id = $1 AND u.deleted_at IS NULL

Test-Helper (backend/tests/helpers): getTestPool, truncateAll, seed.
Test-Kommando: npx vitest run --config tests/vitest.config.ts
Test-DB-Container backend-test-db-1 auf Port 5433.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 083 — organizations.max_konfis</name>
  <files>backend/migrations/083_org_konfi_limit.sql</files>
  <read_first>
    - backend/migrations/082_self_auto_deletion.sql (Idempotenz- und Kommentar-Muster, naechste Nummer ist 083)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    Lege Migration 083 an: ALTER TABLE organizations ADD COLUMN IF NOT EXISTS max_konfis INTEGER NULL.
    KEIN Backfill, KEIN DEFAULT-Wert (D-01: NULL = unbegrenzt, bestehende und neue Orgs starten NULL).
    Kopf-Kommentar analog Migration 082: erklaere NULL-Semantik (Durchsetzung uebersprungen) und dass
    super_admin Limits manuell pro Tarif setzt. Datei muss idempotent sein (zweimal anwendbar).
    Deutsche Kommentare, echte Umlaute, keine Emojis.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS max_konfis" backend/migrations/083_org_konfi_limit.sql</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/migrations/083_org_konfi_limit.sql existiert.
    - Enthaelt exakt `ADD COLUMN IF NOT EXISTS max_konfis INTEGER` (nullbar, ohne NOT NULL, ohne DEFAULT).
    - Enthaelt KEIN UPDATE/Backfill-Statement auf max_konfis.
    - `grep -v '^--' backend/migrations/083_org_konfi_limit.sql | grep -ci "backfill\|default" ` ergibt 0 (kein Default/Backfill in Statements).
  </acceptance_criteria>
  <done>Migration 083 fuegt nullbare max_konfis-Spalte ohne Backfill hinzu, idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: checkKonfiLimit Util (Single Source of Truth)</name>
  <files>backend/utils/konfiLimit.js, backend/tests/utils/konfiLimit.test.js, backend/tests/helpers/seed.js</files>
  <read_first>
    - backend/utils/konfiDeletion.js (Util-Muster: async-Funktion, module.exports-Objekt)
    - backend/tests/utils/konfiDeletion.test.js (Test-Muster: getTestPool, truncateAll, seed)
    - backend/tests/helpers/seed.js (welche Orgs/Rollen/Konfis geseedet werden, ob max_konfis setzbar)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <behavior>
    - max_konfis IS NULL -> { stufe: 'under_limit', count, limit: null } (Check uebersprungen, D-06).
    - count < max_konfis -> { stufe: 'under_limit', count, limit }.
    - max_konfis <= count < max_konfis + 5 -> { stufe: 'grace', count, limit }.
    - count >= max_konfis + 5 -> { stufe: 'hard_block', count, limit }.
    - COUNT zaehlt nur Rolle 'konfi' der Org mit deleted_at IS NULL (D-07); soft-geloeschter Konfi
      wird NICHT mitgezaehlt (Test mit deleted_at-gesetztem User beweist das).
    - COUNT ist org-isoliert: Konfis einer anderen Org beeinflussen das Ergebnis nicht.
  </behavior>
  <action>
    Erstelle backend/utils/konfiLimit.js mit `async function checkKonfiLimit(db, organizationId)`.
    Liest max_konfis aus organizations; zaehlt aktive Konfis via JOIN users/roles WHERE
    r.name = 'konfi' AND organization_id = $1 AND u.deleted_at IS NULL. Gibt ein Objekt mit den
    Feldern count (int), limit (int|null) und stufe ('under_limit'|'grace'|'hard_block') zurueck.
    Grace-Puffer ist genau 5 (max_konfis+5 als harte Grenze). module.exports = { checkKonfiLimit }.
    Funktion bekommt db (Pool oder Client) als ersten Parameter, damit sie in laufende Transaktionen
    der Aufrufer einbettbar ist (analog Util-Muster Phase 114).
    Ergaenze seed.js falls noetig um die Moeglichkeit, einer Org ein max_konfis zu setzen, ohne
    bestehende Seeds zu brechen (vorhandene Felder beibehalten).
    Schreibe die Tests ZUERST (RED), dann implementiere (GREEN).
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/utils/konfiLimit.test.js</automated>
  </verify>
  <acceptance_criteria>
    - backend/utils/konfiLimit.js exportiert checkKonfiLimit.
    - Test deckt alle vier Stufen ab: NULL, under_limit, grace, hard_block.
    - Mindestens ein Test setzt deleted_at auf einem Konfi und beweist, dass count ihn ausschliesst.
    - Mindestens ein Test beweist Org-Isolation (Fremd-Konfi zaehlt nicht).
    - `grep -c "deleted_at IS NULL" backend/utils/konfiLimit.js` >= 1.
    - vitest-Lauf der Datei ist gruen.
  </acceptance_criteria>
  <done>checkKonfiLimit liefert korrekte Stufe inkl. NULL-Fall, deleted_at-Filter und Org-Isolation; Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: super_admin-only Limit-Setzen (PATCH /:id/limit)</name>
  <files>backend/routes/organizations.js, backend/tests/routes/organizations.test.js</files>
  <read_first>
    - backend/routes/organizations.js:165-330 (POST-Create, PUT-Update mit isOwnOrg, Modul-Signatur, requireSuperAdmin)
    - backend/tests/routes/organizations.test.js (Test-Muster: Auth-Header, super_admin- vs org_admin-Token)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md (D-03, D-04, D-10)
  </read_first>
  <behavior>
    - PATCH /api/organizations/:id/limit mit { max_konfis: <int|null> } als super_admin -> 200, Spalte gesetzt.
    - PATCH /:id/limit als org_admin (eigene Org) -> 403 (requireSuperAdmin blockt).
    - max_konfis = null im PATCH-Body -> setzt Spalte auf NULL (unbegrenzt).
    - Negative oder nicht-ganzzahlige max_konfis -> 400 (Validierung).
    - Die bestehende PUT /:id-Route ignoriert ein mitgesendetes max_konfis (Spalte bleibt unveraendert,
      kein Hochsetzen durch org_admin moeglich, D-03).
  </behavior>
  <action>
    Fuege in organizations.js einen neuen Endpunkt `router.patch('/:id/limit', rbacVerifier,
    requireSuperAdmin, ...)` hinzu, der ausschliesslich organizations.max_konfis setzt
    (UPDATE organizations SET max_konfis = $1, updated_at = NOW() WHERE id = $2). Validiere max_konfis
    als nullbaren nicht-negativen Integer (400 bei ungueltig). 404 wenn rowCount 0.
    Die bestehende PUT /:id-Route (Zeile 291) NICHT um max_konfis erweitern — ihr UPDATE-Statement
    bleibt unveraendert, sodass ein mitgesendetes Feld verworfen wird (D-03).
    Optional (Discretion, D-04): max_konfis im POST-Create-Body zulassen — falls umgesetzt, im
    INSERT mit aufnehmen; nur super_admin erreicht POST ohnehin. Wird das nicht gemacht, bleibt PATCH
    der einzige Weg. Dokumentiere die Wahl im SUMMARY.
    Schreibe die Tests ZUERST (RED): super_admin-Erfolg, org_admin-403, NULL-Setzen, 400-Validierung,
    PUT-ignoriert-max_konfis. Deutsche Fehlertexte, echte Umlaute, keine Emojis.
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/routes/organizations.test.js</automated>
  </verify>
  <acceptance_criteria>
    - PATCH /:id/limit existiert und ist mit requireSuperAdmin geschuetzt.
    - Test: super_admin setzt max_konfis -> 200 und DB-Wert stimmt.
    - Test: org_admin auf PATCH /:id/limit -> Status 403.
    - Test: max_konfis = null -> Spalte NULL.
    - Test: ungueltiger Wert (z.B. -1) -> 400.
    - Test: PUT /:id mit max_konfis im Body laesst die Spalte unveraendert.
    - Test: GET /organizations/:id enthaelt das Feld `max_konfis` im Response-Body (noetig fuer die
      read-only "X von Y"-Anzeige in Plan 03 — das SUMMARY MUSS dokumentieren, ueber welchen
      GET-Endpunkt Frontend `max_konfis` + aktuellen Konfi-count bezieht, damit Plan 03 keinen
      neuen Backend-Endpunkt bauen muss).
    - vitest-Lauf der Datei ist gruen, bestehende organizations-Tests bleiben gruen.
  </acceptance_criteria>
  <done>super_admin setzt das Limit ueber dedizierten Endpunkt; org_admin kann es nicht (403); PUT verwirft das Feld. GET liefert max_konfis fuer die Anzeige.</done>
</task>

</tasks>

<verification>
- Migration 083 idempotent gegen Test-DB anwendbar (Spalte vorhanden, NULL bei allen Orgs).
- checkKonfiLimit ist Single Source of Truth fuer Wave 2 (beide Aufrufer importieren daraus).
- Limit-Setzen ausschliesslich super_admin; org_admin-Pfade abgesichert.
- Alle neuen Tests gruen: `cd backend && npx vitest run --config tests/vitest.config.ts tests/utils/konfiLimit.test.js tests/routes/organizations.test.js`
</verification>

<success_criteria>
- organizations.max_konfis existiert (nullbar, kein Backfill).
- backend/utils/konfiLimit.js exportiert checkKonfiLimit mit korrekter 4-Stufen-Logik + deleted_at-Filter.
- super_admin-only PATCH /:id/limit gesetzt und getestet.
- requirements D-01, D-02, D-03, D-04, D-06, D-07, D-10 (Fundament) sowie D-05 (Stufen-Definition) abgedeckt.
</success_criteria>

<output>
Create `.planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-01-SUMMARY.md` when done.
Dokumentiere darin: gewaehlte Migration-Nummer (083), ob max_konfis auch im POST-Create-Body landet,
und das genaue Rueckgabe-Objekt von checkKonfiLimit (Feldnamen count/limit/stufe), damit Wave 2 es
ohne Codebase-Exploration nutzen kann.
</output>
