---
phase: 115-konfi-limits-pro-org-tarif-durchsetzung
plan: 03
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - frontend/src/components/admin/modals/OrganizationManagementModal.tsx
  - frontend/src/components/admin/modals/KonfiModal.tsx
  - frontend/src/components/admin/pages/AdminKonfisPage.tsx
  - frontend/src/components/admin/views/KonfisView.tsx
autonomous: false
requirements: [D-05, D-09, D-10]

must_haves:
  truths:
    - "super_admin sieht und setzt max_konfis in der Org-Verwaltung"
    - "Org-Admin sieht den aktuellen Stand 'X von Y Konfis' read-only"
    - "Bei 409 beim Konfi-Anlegen erscheint ein Grace-Bestaetigungsdialog"
    - "Der Grace-Dialog weist auf den naechsthoeheren Tarif hin und erlaubt 'Trotzdem anlegen'"
    - "Bei Bestaetigung wird der Konfi mit confirm-Flag erneut angelegt"
    - "Bei 403 (Limit+5) erscheint ein Hinweis auf noetiges Upgrade ohne Override"
  artifacts:
    - path: "frontend/src/components/admin/modals/OrganizationManagementModal.tsx"
      provides: "max_konfis-Feld (nur super_admin)"
      contains: "max_konfis"
    - path: "frontend/src/components/admin/pages/AdminKonfisPage.tsx"
      provides: "Grace-Dialog-Handling bei 409 + confirm-Retry"
      contains: "confirm"
  key_links:
    - from: "frontend/src/components/admin/pages/AdminKonfisPage.tsx"
      to: "POST /admin/konfis mit confirm:true"
      via: "Retry nach Grace-Bestaetigung"
      pattern: "confirm"
    - from: "frontend/src/components/admin/modals/OrganizationManagementModal.tsx"
      to: "PATCH /organizations/:id/limit"
      via: "super_admin Limit-Speichern"
      pattern: "limit"
---

<objective>
Frontend-Anbindung der Konfi-Limit-Durchsetzung: super_admin setzt das Limit, Org-Leitung sieht den
Stand read-only, und beim Anlegen erscheint der Grace-Bestaetigungsdialog bzw. Hard-Block-Hinweis.

Purpose: Der Server ist Single Source of Truth (Wave 2 Backend), aber die Nutzerfuehrung braucht den
"Trotzdem anlegen"-Dialog mit Tarif-Hinweis (D-05), das read-only "X von Y" (D-09) und das
super_admin-Feld (D-10).
Output: max_konfis-Feld in Org-Modal, Grace-Dialog + Hard-Block-Hinweis beim Anlegen, "X von Y"-Anzeige.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md
@.planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-01-SUMMARY.md
@.planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-02-SUMMARY.md

<interfaces>
<!-- Projekt-Regeln (CLAUDE.md/MEMORY): keine Emojis, echte Umlaute, Modals NUR via useIonModal,
     IonIcon aus ionicons/icons erlaubt. "Konfis" ist Plural, NICHT gendern. -->

OrganizationManagementModal.tsx:
  - formData-State (useState) mit Feldern display_name, description, contact_email, contact_phone,
    address, website_url, is_active (+ admin_* im Create-Modus). max_konfis hier ergaenzen.
  - isEditMode unterscheidet Create (POST /organizations) und Edit (PUT /organizations/:id).
  - super_admin-Erkennung: useApp() -> user?.role_name === 'super_admin' (vgl. AdminKonfisPage user).
  - Limit-Speichern via api.patch('/organizations/:id/limit', { max_konfis }). Endpunkt-Pfad aus
    115-01-SUMMARY bestaetigen.

AdminKonfisPage.tsx:
  - handleAddKonfi(konfiData) ruft api.post('/admin/konfis', konfiData); catch behandelt aktuell nur
    err.response?.status === 409 als Namens-Kollision -> muss erweitert werden:
    err.response.data.error_code === 'limit_grace' -> Grace-Dialog; 'limit_exceeded' (403) -> Hard-Block.
  - useIonModal-Pattern bereits vorhanden (presentKonfiModalHook). user aus useApp().
  - Konfi-Liste kommt aus useOfflineQuery /admin/konfis; settings aus /settings.
  - Response-Felder bei 409/403 (aus 115-02-SUMMARY): error_code, count, limit, next_tier.

KonfisView.tsx: rendert Header/Liste der Konfis (onAddKonfiClick-Prop). Geeignet fuer "X von Y"-Anzeige.

Stand "count + limit" fuer die Anzeige: aus 115-01-SUMMARY pruefen, ob ein GET-Endpunkt existiert
oder die Werte aus bestehenden Konfi-Liste/Stats kommen. Falls noetig: vorhandene konfi_count- bzw.
/organizations/:id/stats-Daten nutzen, KEINEN neuen Backend-Endpunkt bauen (Backend ist Wave 1 fertig).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: max_konfis-Feld in Org-Verwaltung (nur super_admin) + read-only "X von Y"</name>
  <files>frontend/src/components/admin/modals/OrganizationManagementModal.tsx, frontend/src/components/admin/views/KonfisView.tsx</files>
  <read_first>
    - frontend/src/components/admin/modals/OrganizationManagementModal.tsx (formData-State, isEditMode, handleSave api.put/api.post, super_admin via useApp)
    - frontend/src/components/admin/views/KonfisView.tsx (Header-Bereich, vorhandene Konfi-Anzahl/Stats)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-01-SUMMARY.md (PATCH-Pfad, ob max_konfis im Create-Body)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    OrganizationManagementModal: Fuege ein numerisches max_konfis-Feld (IonInput type="number", leer =
    unbegrenzt/NULL) hinzu, das NUR fuer super_admin (user?.role_name === 'super_admin') sichtbar ist.
    Im Edit-Modus speichere es via api.patch('/organizations/:id/limit', { max_konfis: <int|null> })
    (Pfad aus 115-01-SUMMARY); leeres Feld sendet null. Im Create-Modus: falls 115-01-SUMMARY
    max_konfis im POST-Body vorsieht, mit in orgData aufnehmen, sonst nach dem Create per PATCH setzen.
    Lade den bestehenden Wert beim Oeffnen (api.get(`/organizations/${id}`) liefert ihn nach Migration).
    KonfisView: Zeige fuer die Org-Leitung read-only "X von Y Konfis" (D-09) im vorhandenen
    Header-/Stats-Bereich. Bei limit NULL: nur "X Konfis" ohne Limit. Nutze bereits vorhandene
    Konfi-Anzahl + Limit-Wert (kein neuer Backend-Endpunkt). Deutsche Texte, echte Umlaute, keine Emojis.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max_konfis" frontend/src/components/admin/modals/OrganizationManagementModal.tsx` >= 1.
    - max_konfis-Feld ist an `role_name === 'super_admin'` gebunden (org_admin sieht es nicht).
    - Edit-Speichern ruft PATCH /organizations/:id/limit auf.
    - KonfisView zeigt "X von Y Konfis" bei gesetztem Limit, "X Konfis" bei NULL.
    - `npx tsc --noEmit` ohne neue Fehler.
    - Keine Unicode-Emojis, echte Umlaute (ueberpruefbar via grep auf ue/oe/ae in neuen Strings -> keine Ersatzschreibweise).
  </acceptance_criteria>
  <done>super_admin setzt max_konfis; org_admin sieht read-only "X von Y Konfis".</done>
</task>

<task type="auto">
  <name>Task 2: Grace-Bestaetigungsdialog + Hard-Block-Hinweis beim Konfi-Anlegen</name>
  <files>frontend/src/components/admin/pages/AdminKonfisPage.tsx, frontend/src/components/admin/modals/KonfiModal.tsx</files>
  <read_first>
    - frontend/src/components/admin/pages/AdminKonfisPage.tsx:146-211 (handleAddKonfi, 409-catch, presentAlert, useIonModal)
    - frontend/src/components/admin/modals/KonfiModal.tsx (handleSave, onSave-Prop)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-02-SUMMARY.md (Response-Struktur error_code/count/limit/next_tier, confirm-Flag-Feld)
    - .planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-CONTEXT.md (D-05)
  </read_first>
  <action>
    Erweitere handleAddKonfi in AdminKonfisPage: unterscheide im catch nach err.response.data.error_code.
    Bei 'limit_grace' (Status 409): zeige einen Bestaetigungsdialog (useIonAlert/presentAlert ist hier
    schon im Einsatz; falls ein eigenes Modal noetig ist, via useIonModal — niemals <IonModal isOpen>).
    Der Dialog erklaert die Grace-Periode, nennt "X von Y Konfis" (count/limit), weist aktiv auf den
    naechsthoeheren Tarif hin (next_tier aus dem Response, z.B. "Der naechste Tarif gibt dir bis zu Z
    Konfis") und bietet zwei Aktionen: "Abbrechen" und "Trotzdem anlegen". "Trotzdem anlegen" sendet
    denselben POST /admin/konfis erneut mit dem confirm-Flag (Feldname aus 115-02-SUMMARY, z.B.
    confirm: true) und behandelt die 201-Antwort wie den Normalfall (Einmalpasswort-Alert, refreshKonfis).
    Bei 'limit_exceeded' (Status 403): zeige einen reinen Hinweis-Alert "Tarif-Upgrade noetig" mit
    Verweis auf next_tier, KEINE "Trotzdem anlegen"-Option. Bestehendes Username-409-Handling
    (error_code !== 'limit_grace') unveraendert lassen.
    Deutsche Texte, echte Umlaute, keine Emojis. "Konfis" nicht gendern.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "limit_grace" frontend/src/components/admin/pages/AdminKonfisPage.tsx` >= 1.
    - `grep -c "limit_exceeded" frontend/src/components/admin/pages/AdminKonfisPage.tsx` >= 1.
    - Grace-Pfad sendet einen zweiten POST /admin/konfis mit confirm-Flag.
    - Grace-Dialog-Text referenziert count, limit und next_tier.
    - 403-Pfad bietet KEINE "Trotzdem anlegen"-Option (nur Bestaetigung/Schliessen).
    - Username-Kollisions-409 (ohne error_code 'limit_grace') zeigt weiterhin die Namens-Fehlermeldung.
    - `npx tsc --noEmit` ohne neue Fehler.
  </acceptance_criteria>
  <done>Grace-409 oeffnet "Trotzdem anlegen"-Dialog mit Tarif-Hinweis + confirm-Retry; 403 zeigt Hard-Block ohne Override.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    max_konfis-Feld in der Org-Verwaltung (nur super_admin), read-only "X von Y Konfis" fuer die
    Org-Leitung, Grace-Bestaetigungsdialog ("Trotzdem anlegen" + Tarif-Hinweis) bei 409 und
    Hard-Block-Hinweis bei 403 beim Konfi-Anlegen.
  </what-built>
  <how-to-verify>
    1. Als super_admin eine Org bearbeiten: max_konfis z.B. auf die aktuelle Konfi-Zahl setzen, speichern.
    2. Als org_admin pruefen, dass das max_konfis-Feld NICHT erscheint, aber "X von Y Konfis" sichtbar ist.
    3. Einen Konfi anlegen, waehrend count == limit: Grace-Dialog muss erscheinen, "X von Y" und
       naechsten Tarif nennen, "Trotzdem anlegen" legt den Konfi an.
    4. max_konfis testweise so setzen, dass count >= limit+5: Anlegen muss mit Upgrade-Hinweis ohne
       "Trotzdem anlegen" abgewiesen werden.
    5. Texte pruefen: echte Umlaute (ue/oe/ae nicht als Ersatz), keine Emojis.
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe Abweichungen.</resume-signal>
</task>

</tasks>

<verification>
- super_admin-only max_konfis-Feld + read-only "X von Y" (D-09/D-10).
- Grace-Dialog mit Tarif-Hinweis und confirm-Retry, Hard-Block-Hinweis ohne Override (D-05).
- `cd frontend && npx tsc --noEmit -p tsconfig.json` ohne neue Fehler.
- Keine Emojis, echte Umlaute in allen neuen Strings.
</verification>

<success_criteria>
- Org-Verwaltung: max_konfis nur fuer super_admin, gespeichert via PATCH /:id/limit.
- Konfi-Liste: read-only "X von Y Konfis" (bzw. "X Konfis" bei NULL).
- Anlegen: 409 -> Grace-Dialog + confirm-Retry; 403 -> Hard-Block-Hinweis.
- requirements D-05, D-09, D-10 abgedeckt.
</success_criteria>

<output>
Create `.planning/phases/115-konfi-limits-pro-org-tarif-durchsetzung/115-03-SUMMARY.md` when done.
</output>
