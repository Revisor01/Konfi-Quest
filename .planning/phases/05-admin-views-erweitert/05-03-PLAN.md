---
phase: 05-admin-views-erweitert
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements: [DET-01, DET-02, DET-03]

must_haves:
  truths:
    - "Admin-EventDetailView nutzt app-icon-color-- Klassen statt Inline-Farben auf Icons in Info-Rows"
    - "Konfi-EventDetailView nutzt app-icon-color-- Klassen statt Inline-Farben auf Icons in Info-Rows"
    - "KonfiDetailView Info-Rows und Listen nutzen CSS-Klassen, ActivityRings-Header bleibt unberuehrt"
    - "Alle drei Views haben deutlich weniger Inline-Styles als vorher"
  artifacts:
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "Bereinigte Admin-EventDetailView mit Icon-Farb-CSS-Klassen"
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "Bereinigte Konfi-EventDetailView mit Icon-Farb-CSS-Klassen"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "Bereinigte KonfiDetailView mit CSS-Klassen"
  key_links:
    - from: "frontend/src/components/admin/views/EventDetailView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS class usage"
      pattern: "app-icon-color--events|app-icon-color--participants|app-icon-color--badges"
    - from: "frontend/src/components/konfi/views/EventDetailView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS class usage"
      pattern: "app-icon-color--events|app-icon-color--participants"
    - from: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS class usage"
      pattern: "app-icon-color--badges|app-icon-color--activities"
---

<objective>
Admin-EventDetailView, Konfi-EventDetailView und KonfiDetailView nachtraeglich auf Icon-Farb-CSS-Klassen umstellen und verbleibende Inline-Styles bereinigen. Diese Views wurden in Phase 4 bearbeitet aber die Icon-Farb-Klassen existierten damals noch nicht.

Purpose: Icon-Farb-Konsistenz ueber alle Detail-Views herstellen und Inline-Style-Count signifikant reduzieren.
Output: 3 bereinigte Detail-Views mit konsistenten Icon-Farben.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-views-erweitert/05-CONTEXT.md
@.planning/phases/05-admin-views-erweitert/05-01-SUMMARY.md
@.planning/phases/04-admin-views-core/04-02-SUMMARY.md

<interfaces>
<!-- Icon-Farb-CSS-Klassen aus Plan 05-01 (variables.css) -->
<!-- .app-icon-color--events { color: #dc2626; }    fuer calendar, time, location -->
<!-- .app-icon-color--participants { color: #34c759; } fuer people (Teilnehmer) -->
<!-- .app-icon-color--users { color: #667eea; }      fuer person -->
<!-- .app-icon-color--warning { color: #fd7e14; }    fuer listOutline, hourglass -->
<!-- .app-icon-color--badges { color: #ff9500; }     fuer trophy, ribbon -->
<!-- .app-icon-color--category { color: #5b21b6; }   fuer pricetag -->
<!-- .app-icon-color--gottesdienst { color: #3b82f6; } fuer school (Gottesdienst-Kontext) -->
<!-- .app-icon-color--gemeinde { color: #22c55e; }   fuer people (Gemeinde-Kontext) -->
<!-- .app-icon-color--activities { color: #059669; }  fuer flash -->
<!-- .app-icon-color--success { color: #34c759; }    fuer checkmarkCircle -->
<!-- .app-icon-color--danger { color: #dc3545; }     fuer closeCircle -->

<!-- Bestehende CSS-Klassen in den Views -->
<!-- app-info-row, app-info-row__icon, app-info-row__content, app-info-row__sublabel -->
<!-- app-list-item, app-list-item__row, app-list-item__main, app-list-item__content -->
<!-- app-detail-header, app-detail-header__content, app-detail-header__title, app-detail-header__subtitle -->
<!-- app-status-chip mit Varianten -->
<!-- SectionHeader mit dynamischen getStatusColors() in EventDetailViews -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin-EventDetailView und Konfi-EventDetailView Icon-Farben durch CSS-Klassen ersetzen</name>
  <files>frontend/src/components/admin/views/EventDetailView.tsx, frontend/src/components/konfi/views/EventDetailView.tsx</files>
  <action>
**Admin-EventDetailView.tsx (63 Inline-Styles -> unter 25):**

Systematisch alle Icon-Farb-Inline-Styles durch CSS-Klassen ersetzen:

1. Info-Row Icons: Die View hat viele `<IonIcon icon={...} className="app-info-row__icon" style={{ color: '#...' }} />` Muster. Ersetze `style={{ color: '#dc2626' }}` durch Hinzufuegen von `app-icon-color--events` zur className. Ergebnis: `className="app-info-row__icon app-icon-color--events"`.

   Mapping fuer die vorhandenen Icons:
   - `calendar` Icon mit `color: '#dc2626'` -> `app-icon-color--events`
   - `time` Icon mit `color: '#dc2626'` -> `app-icon-color--events`
   - `people` Icon mit `color: '#34c759'` -> `app-icon-color--participants`
   - `listOutline` Icon mit `color: '#fd7e14'` -> `app-icon-color--warning`
   - `trophy` Icon mit `color: '#ff9500'` -> `app-icon-color--badges`
   - `pricetag` Icon mit `color: '#5b21b6'` -> `app-icon-color--category`
   - `location` Icon mit `color: '#dc2626'` -> `app-icon-color--events`
   - `people` Icon mit `color: '#007aff'` (Jahrgaenge-Kontext) -> `app-icon-color--jahrgang`
   - `checkmarkCircle` / `closeCircle` Icons -> `app-icon-color--success` / `app-icon-color--danger`

2. Punkt-Typ Farbe: `style={{ color: eventData?.point_type === 'gottesdienst' ? '#007aff' : '#2dd36f' }}` ist DYNAMISCH und darf inline bleiben (abhaengig von Daten).

3. Layout-Inline-Styles: Ersetze wiederkehrende Patterns:
   - `style={{ fontSize: '0.9rem', color: '#666', marginBottom: '4px' }}` fuer Timeslot-Text -> nutze `app-info-row__sublabel` oder belasse inline (spezifisch fuer Timeslot-Auflistung)
   - `style={{ margin: '0 0 8px 0', fontSize: '1rem', fontWeight: '600', color: '#333' }}` fuer Ueberschriften -> nutze `app-list-item__title` Klasse
   - `style={{ margin: '0', fontSize: '0.95rem', color: '#666', lineHeight: '1.5' }}` fuer Beschreibungstexte -> nutze `app-settings-item__subtitle` oder belasse inline

4. `style={{ color: 'black' }}` auf IonTitle -> entfernen (Standard-Farbe).

5. Teilnehmer-Liste Items: Ersetze Inline-Styles auf den Teilnehmer-Eintraegen durch app-list-item Klassen.

6. Links: `style={{ color: '#007aff', textDecoration: 'underline', cursor: 'pointer' }}` kann bleiben (einmalig fuer Maps-Link).

**Konfi-EventDetailView.tsx (34 Inline-Styles -> unter 12):**

Gleiche Vorgehensweise wie Admin-EventDetailView:

1. Info-Row Icons: Alle `style={{ color: '#...' }}` auf Icons durch `app-icon-color--*` Klassen ersetzen (gleiches Mapping wie oben).

2. Timeslot-Texte: `style={{ fontSize: '0.9rem', color: '#666' }}` patterns ersetzen oder belassen.

3. Punkt-Typ Farbe: Dynamisch, bleibt inline.

4. `style={{ color: 'black' }}` auf IonTitle -> entfernen.

5. Beschreibungstext und Teilnehmer-Info: Ersetze durch app-list-item__subtitle oder app-settings-item__subtitle wo passend.

WICHTIG:
- Keine Emojis.
- Icons IMMER solid. Einzige Ausnahme: `listOutline` fuer Warteliste (spezifisches Wartelisten-Icon).
- Dynamische Farben (basierend auf Event-Status/Punkt-Typ) bleiben inline.
- SectionHeader mit getStatusColors() bleibt unberuehrt.
- `marginTop: '4px'` oder `marginTop: '2px'` auf einzelnen Icons darf inline bleiben (einmalige Ausrichtungs-Korrektur).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && echo "Admin-EventDetail:" && grep -c 'style={{' frontend/src/components/admin/views/EventDetailView.tsx && echo "Konfi-EventDetail:" && grep -c 'style={{' frontend/src/components/konfi/views/EventDetailView.tsx && grep -c "app-icon-color--" frontend/src/components/admin/views/EventDetailView.tsx && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Admin-EventDetailView hat unter 25 Inline-Styles (von 63), Konfi-EventDetailView unter 12 (von 34). Beide nutzen app-icon-color-- Klassen fuer konsistente Icon-Farben. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: KonfiDetailView Info-Rows und Listen bereinigen</name>
  <files>frontend/src/components/admin/views/KonfiDetailView.tsx</files>
  <action>
**KonfiDetailView.tsx (51 Inline-Styles -> unter 20):**

WICHTIG: Der ActivityRings-Header-Bereich (ActivityRings-Komponente und der umgebende Header mit Gradient) bleibt UNBERUEHRT. Nur die Bereiche unterhalb des Headers werden bereinigt.

1. Icon-Farben: Ersetze alle `style={{ color: '#...' }}` auf IonIcon-Elementen durch app-icon-color-- Klassen:
   - `trophy` mit `color: '#fbbf24'` -> `app-icon-color--badges`
   - `calendar` mit `color: '#666'` -> entscheide: Kontext-abhaengig. In Aktivitaets-Listen nutze `app-icon-color--events`, in neutralem Kontext belasse inline oder nutze eine neutrale Farbe.
   - `image` mit `color: '#5b21b6'` -> `app-icon-color--category`
   - `flash` -> `app-icon-color--activities`

2. Leere-Listen-Text: Ersetze `style={{ textAlign: 'center', padding: '16px', color: '#666' }}` fuer "Keine Aktivitaeten/Bonus/Events" durch die bestehende `app-empty-state` Klasse oder ein einfaches `className` mit Utility-Klassen.

3. Aktivitaets-/Bonus-/Event-Listen: Ersetze Inline-Styles auf Listen-Items durch app-list-item CSS-Klassen. Die Items haben bereits IonItemSliding.

4. Badge-Anzeige im Header: `style={{ color: 'white', fontWeight: '600' }}` auf dem Badge-Count im Header darf inline bleiben (spezifisch fuer den Gradient-Header-Bereich, der nicht angepasst wird).

5. Wiederkehrende Layout-Patterns:
   - `style={{ display: 'flex', ... }}` Container -> prüfe ob app-list-item__row oder app-info-row passt
   - `style={{ fontWeight: '600', fontSize: '0.95rem', color: '#333' }}` -> app-list-item__title
   - `style={{ fontSize: '0.8rem', color: '#666' }}` -> app-list-item__subtitle oder app-info-row__sublabel

6. Nicht anfassen:
   - ActivityRings-Komponente und deren Props
   - Header-Gradient und der Container drum herum
   - Dynamische Werte (Farben basierend auf Daten)

WICHTIG:
- Keine Emojis.
- ActivityRings-Header ist eine separate Komponente und bleibt wie sie ist.
- Icons solid verwenden. `calendar` in neutralem Kontext: verwende die Events-Farbe (gleicher Kalender = gleiche Farbe).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && echo "KonfiDetailView:" && grep -c 'style={{' frontend/src/components/admin/views/KonfiDetailView.tsx && grep -c "app-icon-color--" frontend/src/components/admin/views/KonfiDetailView.tsx && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>KonfiDetailView hat unter 20 Inline-Styles (von 51). Icon-Farben nutzen app-icon-color-- Klassen. ActivityRings-Header ist unberuehrt. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `grep -c 'style={{' frontend/src/components/admin/views/EventDetailView.tsx` zeigt unter 25
2. `grep -c 'style={{' frontend/src/components/konfi/views/EventDetailView.tsx` zeigt unter 12
3. `grep -c 'style={{' frontend/src/components/admin/views/KonfiDetailView.tsx` zeigt unter 20
4. `grep -c "app-icon-color--" frontend/src/components/admin/views/EventDetailView.tsx` zeigt mindestens 8
5. `grep -c "app-icon-color--" frontend/src/components/konfi/views/EventDetailView.tsx` zeigt mindestens 6
6. `grep -c "app-icon-color--" frontend/src/components/admin/views/KonfiDetailView.tsx` zeigt mindestens 3
7. `grep "ActivityRings" frontend/src/components/admin/views/KonfiDetailView.tsx` zeigt unveraenderte Nutzung
8. TypeScript kompiliert fehlerfrei
</verification>

<success_criteria>
- Alle drei Detail-Views nutzen app-icon-color-- Klassen statt Inline-Farben fuer Icons
- Gleiches Konzept = gleiches Icon = gleiche Farbe ueberall
- Inline-Style-Reduktion: Admin-EventDetailView ~60%, Konfi-EventDetailView ~65%, KonfiDetailView ~60%
- ActivityRings-Header in KonfiDetailView bleibt unberuehrt
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-views-erweitert/05-03-SUMMARY.md`
</output>
