---
phase: 29-token-cleanup-end-to-end-verifikation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/backgroundService.js
  - backend/services/pushService.js
  - frontend/src/contexts/AppContext.tsx
autonomous: true
requirements:
  - CLN-02
  - CMP-01

must_haves:
  truths:
    - "Verwaiste Tokens (User geloescht, error_count >= 10, aelter als 30 Tage) werden automatisch alle 6 Stunden bereinigt"
    - "sendBadgeUpdate wertet Firebase result.success korrekt aus und tracked error_count / loescht fatale Tokens"
    - "Alle 18 Push-Types haben funktionierende Frontend-Tap-Navigation (ausser badge_update als silent push)"
  artifacts:
    - path: "backend/services/backgroundService.js"
      provides: "Token-Cleanup Service mit 6h setInterval"
      contains: "cleanupStaleTokens"
    - path: "backend/services/pushService.js"
      provides: "sendBadgeUpdate mit Result-Pattern (error_count Tracking + fatale Token-Loeschung)"
      contains: "result.success"
    - path: "frontend/src/contexts/AppContext.tsx"
      provides: "Navigation-Cases fuer event_unregistration, events_pending_approval, new_konfi_registration"
      contains: "event_unregistration"
  key_links:
    - from: "backend/services/backgroundService.js"
      to: "push_tokens Tabelle"
      via: "DELETE FROM push_tokens WHERE error_count >= 10 / updated_at < 30 Tage / user NOT EXISTS"
      pattern: "DELETE FROM push_tokens"
    - from: "backend/services/pushService.js sendBadgeUpdate"
      to: "backend/push/firebase.js"
      via: "result = await sendFirebasePushNotification und result.success Auswertung"
      pattern: "result\\.success"
---

<objective>
Token-Cleanup Service implementieren, sendBadgeUpdate Result-Pattern fixen und fehlende Frontend-Navigation-Cases ergaenzen.

Purpose: Das Push-System wird selbstreinigend (verwaiste Tokens automatisch bereinigt) und alle 18 Push-Flows sind End-to-End konsistent (korrektes Error-Handling, korrekte Tap-Navigation).
Output: Drei Dateien geaendert — backgroundService.js (Cleanup), pushService.js (sendBadgeUpdate Fix), AppContext.tsx (Navigation Cases).
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-token-cleanup-end-to-end-verifikation/29-CONTEXT.md
@.planning/phases/29-token-cleanup-end-to-end-verifikation/29-RESEARCH.md

<interfaces>
<!-- Bestehendes backgroundService Pattern (setInterval + static property) -->

From backend/services/backgroundService.js:
```javascript
class BackgroundService {
  static badgeUpdateInterval = null;
  static eventReminderInterval = null;
  static pendingEventsInterval = null;

  // Pattern: start + stop + worker Methode
  static startBadgeUpdateService(db) { ... }
  static stopBadgeUpdateService() { ... }
  static async updateAllUserBadges(db) { ... }

  static startAllServices(db) {
    this.startBadgeUpdateService(db);
    this.startEventReminderService(db);
    this.startPendingEventsService(db);
  }
  static stopAllServices() { ... }
}
```

From backend/services/pushService.js (sendToUser Result-Pattern Z.79-106):
```javascript
if (result.success) {
  successCount++;
  if (token.error_count > 0) {
    await db.query(
      'UPDATE push_tokens SET error_count = 0, last_error_at = NULL WHERE id = $1',
      [token.id]
    );
  }
} else {
  const fatalCodes = [
    'messaging/registration-token-not-registered',
    'messaging/invalid-registration-token'
  ];
  if (fatalCodes.includes(result.errorCode)) {
    await db.query('DELETE FROM push_tokens WHERE id = $1', [token.id]);
  } else {
    await db.query(
      'UPDATE push_tokens SET error_count = error_count + 1, last_error_at = NOW() WHERE id = $1',
      [token.id]
    );
  }
  errorCount++;
}
```

From backend/services/pushService.js (sendBadgeUpdate AKTUELL Z.231-262 — FEHLERHAFT):
```javascript
static async sendBadgeUpdate(db, userId, badgeCount) {
  // ... KEIN result.success Check, try/catch faengt nie Firebase-Errors
  for (const token of tokens) {
    try {
      await sendFirebasePushNotification(token.token, { ... });
      successCount++;
    } catch (error) {
      console.error('Badge update failed:', error);
    }
  }
}
```

From push_tokens Schema:
```sql
CREATE TABLE IF NOT EXISTS push_tokens (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL,  -- KEIN FK auf users(id)!
    token TEXT NOT NULL,
    platform TEXT NOT NULL,
    device_id TEXT NOT NULL,
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    error_count INTEGER DEFAULT 0,
    last_error_at TIMESTAMPTZ
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token-Cleanup Service + sendBadgeUpdate Result-Pattern Fix</name>
  <files>backend/services/backgroundService.js, backend/services/pushService.js</files>
  <action>
**1. sendBadgeUpdate in pushService.js fixen (Z.231-262):**

Die sendBadgeUpdate Methode auf das gleiche Result-Pattern wie sendToUser umstellen. Das Problem: `sendFirebasePushNotification` gibt `{ success, errorCode }` zurueck, wirft aber NICHT. Der aktuelle try/catch faengt daher nie Firebase-Fehler.

Fix: Den for-Loop ersetzen durch:
```javascript
for (const token of tokens) {
  const result = await sendFirebasePushNotification(token.token, {
    badge: badgeCount,
    data: {
      type: 'badge_update',
      count: badgeCount.toString()
    }
  });

  if (result.success) {
    successCount++;
    if (token.error_count > 0) {
      await db.query(
        'UPDATE push_tokens SET error_count = 0, last_error_at = NULL WHERE id = $1',
        [token.id]
      );
    }
  } else {
    const fatalCodes = [
      'messaging/registration-token-not-registered',
      'messaging/invalid-registration-token'
    ];
    if (fatalCodes.includes(result.errorCode)) {
      await db.query('DELETE FROM push_tokens WHERE id = $1', [token.id]);
      console.warn(`Token ${token.id} geloescht (${result.errorCode})`);
    } else {
      await db.query(
        'UPDATE push_tokens SET error_count = error_count + 1, last_error_at = NOW() WHERE id = $1',
        [token.id]
      );
    }
    errorCount++;
  }
}
```

Auch `errorCount` Variable deklarieren (fehlt aktuell) und im Return-Objekt zurueckgeben: `return { success: true, sent: successCount, errors: errorCount, total: tokens.length };`

**2. Token-Cleanup Service in backgroundService.js:**

a) Neue static Property: `static tokenCleanupInterval = null;`

b) Neue Methode `startTokenCleanupService(db)`:
- Guard: `if (this.tokenCleanupInterval) return;`
- Intervall: `const SIX_HOURS = 6 * 60 * 60 * 1000;`
- Sofort einmal ausfuehren: `this.cleanupStaleTokens(db);`
- Dann setInterval mit SIX_HOURS, try/catch im Callback

c) Neue Methode `stopTokenCleanupService()`:
- Standard-Pattern: clearInterval + null setzen

d) Neue Methode `async cleanupStaleTokens(db)`:
- Drei DELETE-Queries ausfuehren:
  1. `DELETE FROM push_tokens WHERE error_count >= 10` (fehlerhafte Tokens)
  2. `DELETE FROM push_tokens WHERE updated_at < NOW() - INTERVAL '30 days'` (inaktive Tokens)
  3. `DELETE FROM push_tokens WHERE user_id NOT IN (SELECT id FROM users)` (verwaiste Tokens)
- Jede Query mit RETURNING id um Count zu bekommen
- Eine Zusammenfassungszeile loggen: `console.log('Token cleanup: X error tokens, Y inactive tokens, Z orphaned tokens deleted');`
- Nur loggen wenn mindestens 1 Token geloescht wurde

e) `startAllServices(db)` erweitern: `this.startTokenCleanupService(db);` hinzufuegen

f) `stopAllServices()` erweitern: `this.stopTokenCleanupService();` hinzufuegen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && node -e "const BS = require('./backend/services/backgroundService'); console.log('tokenCleanupInterval' in BS ? 'OK' : 'FAIL'); console.log(typeof BS.startTokenCleanupService === 'function' ? 'OK' : 'FAIL'); console.log(typeof BS.cleanupStaleTokens === 'function' ? 'OK' : 'FAIL');" && node -e "const fs = require('fs'); const code = fs.readFileSync('./backend/services/pushService.js','utf8'); const hasResultPattern = code.includes('result.success') && code.includes('sendBadgeUpdate'); console.log(hasResultPattern ? 'sendBadgeUpdate Result-Pattern: OK' : 'FAIL');"</automated>
  </verify>
  <done>
- backgroundService.js hat startTokenCleanupService/stopTokenCleanupService/cleanupStaleTokens Methoden
- cleanupStaleTokens loescht Tokens mit error_count >= 10, updated_at > 30 Tage, und verwaiste User-Tokens
- startAllServices ruft startTokenCleanupService auf
- sendBadgeUpdate in pushService.js verwendet result.success Pattern identisch zu sendToUser
- sendBadgeUpdate tracked error_count und loescht fatale Tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Fehlende Frontend Push-Navigation-Cases ergaenzen</name>
  <files>frontend/src/contexts/AppContext.tsx</files>
  <action>
Im `pushNotificationActionPerformed` Handler (ca. Z.320-377) den switch-Block um die 3 fehlenden Admin-Navigation-Cases ergaenzen.

Die fehlenden Cases VOR dem default-Case einfuegen:

```typescript
case 'event_unregistration':
case 'events_pending_approval':
  targetUrl = userType === 'admin'
    ? '/admin/events'
    : '/konfi/events';
  break;

case 'new_konfi_registration':
  targetUrl = userType === 'admin'
    ? '/admin/konfis'
    : '/konfi/dashboard';
  break;
```

WICHTIG: Den bestehenden Code genau lesen um die richtige Variable (`targetUrl` oder aehnlich) und das URL-Pattern zu verwenden. Die Research zeigt dass die bestehenden Cases Pfade wie `/requests`, `/badges`, `/events` verwenden — pruefen ob mit oder ohne `/admin/` bzw `/konfi/` Prefix.

`badge_update` braucht KEINEN Case — ist eine silent notification nur fuer Badge-Count-Update, kein User-Tap erwartet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "event_unregistration\|events_pending_approval\|new_konfi_registration" frontend/src/contexts/AppContext.tsx</automated>
  </verify>
  <done>
- AppContext.tsx hat Navigation-Cases fuer event_unregistration, events_pending_approval, new_konfi_registration
- Admin-Push-Taps navigieren zu den korrekten Admin-Views (/admin/events bzw /admin/konfis)
- badge_update hat keinen Case (silent notification)
  </done>
</task>

</tasks>

<verification>
1. backgroundService.js: cleanupStaleTokens Methode vorhanden mit 3 DELETE-Queries
2. backgroundService.js: startAllServices ruft startTokenCleanupService auf
3. pushService.js: sendBadgeUpdate nutzt result.success Pattern mit error_count Tracking und fataler Token-Loeschung
4. AppContext.tsx: Alle 18 Push-Types haben entweder einen Navigation-Case oder sind explizit silent (badge_update)
5. Backend startet ohne Fehler: `cd backend && node -e "require('./services/backgroundService'); require('./services/pushService'); console.log('OK');"`
</verification>

<success_criteria>
- Token-Cleanup Service laeuft alle 6 Stunden und bereinigt fehlerhafte (error_count >= 10), inaktive (30 Tage) und verwaiste (User geloescht) Tokens
- sendBadgeUpdate hat identisches Error-Handling wie sendToUser (result.success, error_count, fatale Loeschung)
- Alle Admin-Push-Types (event_unregistration, events_pending_approval, new_konfi_registration) haben funktionierende Tap-Navigation
- Kein Node.js Startup-Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/29-token-cleanup-end-to-end-verifikation/29-01-SUMMARY.md`
</output>
