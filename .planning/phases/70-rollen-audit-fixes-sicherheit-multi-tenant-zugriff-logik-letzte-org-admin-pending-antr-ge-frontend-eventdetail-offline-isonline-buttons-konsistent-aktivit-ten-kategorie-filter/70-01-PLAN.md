---
phase: 70-rollen-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi-managment.js
  - backend/routes/users.js
  - backend/routes/organizations.js
  - backend/routes/activities.js
autonomous: true
requirements: [AUDIT-S1, AUDIT-S2, AUDIT-L1, AUDIT-L2]

must_haves:
  truths:
    - "Bonus-Points DELETE prueft organization_id via JOIN"
    - "Org-Users GET prueft role_name === org_admin zusaetzlich zu organization_id"
    - "Letzter org_admin einer Organisation kann nicht geloescht werden"
    - "Aktivitaet mit pending activity_requests kann nicht geloescht werden"
  artifacts:
    - path: "backend/routes/konfi-managment.js"
      provides: "Multi-Tenant bonus-points DELETE mit org_id JOIN"
      contains: "u.organization_id"
    - path: "backend/routes/organizations.js"
      provides: "Org-Users Zugriffspruefung mit Rollen-Check"
      contains: "org_admin"
    - path: "backend/routes/users.js"
      provides: "Letzte-org_admin-Schutz beim Loeschen"
      contains: "Letzter Org-Admin"
    - path: "backend/routes/activities.js"
      provides: "Pending-Requests-Check vor Aktivitaet-Loeschen"
      contains: "activity_requests"
  key_links:
    - from: "backend/routes/konfi-managment.js"
      to: "users table"
      via: "JOIN users u ON bp.konfi_id = u.id"
      pattern: "JOIN users.*organization_id"
    - from: "backend/routes/activities.js"
      to: "activity_requests table"
      via: "COUNT query mit status pending"
      pattern: "activity_requests.*pending"
---

<objective>
Backend-Sicherheits- und Logik-Fixes aus dem Rollen-Audit: Multi-Tenant bonus-points, Org-Users Zugriff, letzte org_admin Schutz, pending Antraege Check.

Purpose: Sicherheitsluecken und Logikfehler im Backend schliessen.
Output: 4 korrigierte Route-Dateien.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-rollen-audit-fixes-sicherheit-multi-tenant-zugriff-logik-letzte-org-admin-pending-antr-ge-frontend-eventdetail-offline-isonline-buttons-konsistent-aktivit-ten-kategorie-filter/70-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unstaged Changes verifizieren + S2 + L2 Backend-Fixes</name>
  <read_first>
    backend/routes/konfi-managment.js (unstaged S1 Fix pruefen)
    backend/routes/users.js (unstaged L1 Fix pruefen)
    backend/routes/organizations.js (S2 Fix - Zeile ~421-429)
    backend/routes/activities.js (L2 Fix - Zeile ~189-217)
  </read_first>
  <files>
    backend/routes/konfi-managment.js
    backend/routes/users.js
    backend/routes/organizations.js
    backend/routes/activities.js
  </files>
  <action>
WICHTIG: Zwei Dateien haben UNSTAGED Changes von abgebrochenen Agents. Zuerst verifizieren!

**S1 — konfi-managment.js (UNSTAGED, verifizieren per D-01, D-02):**
Die bestehende unstaged Aenderung ersetzt in der DELETE bonus-points Route (~Zeile 724):
- Alt: `SELECT * FROM bonus_points WHERE id = $1 AND konfi_id = $2`
- Neu: `SELECT bp.* FROM bonus_points bp JOIN users u ON bp.konfi_id = u.id WHERE bp.id = $1 AND bp.konfi_id = $2 AND u.organization_id = $3`
- Dritter Parameter: `req.user.organization_id`
Verifizieren dass der JOIN korrekt ist und der dritte Parameter stimmt. Wenn korrekt: stagen. Wenn nicht: korrigieren dann stagen.

**L1 — users.js (UNSTAGED, verifizieren per D-05, D-06, D-07):**
Die bestehende unstaged Aenderung fuegt vor dem DELETE /:id einen Check ein:
- Prueft ob der zu loeschende User role_name === 'org_admin' hat
- Zaehlt andere org_admins in derselben Organisation (WHERE u.id != $3)
- Wenn count === 0: return 400 mit "Letzter Org-Admin kann nicht geloescht werden"
Verifizieren dass der Check korrekt ist. Achten auf: Fehlermeldung muss "Letzter Org-Admin kann nicht gelöscht werden" sein (mit echtem oe/ue). Wenn korrekt: stagen. Wenn nicht: korrigieren dann stagen.

**S2 — organizations.js (NEU, per D-03, D-04):**
In GET /:id/users Route (~Zeile 421-429) ist die Zugriffspruefung bereits korrekt implementiert:
```javascript
const isOrgAdmin = req.user.role_name === 'org_admin';
if (!isSuperAdmin && !(isOwnOrg && isOrgAdmin)) {
```
Dies prueft BEREITS role_name === 'org_admin'. VERIFIZIEREN ob das reicht. Falls die Route auch fuer andere Rollen (teamer, konfi) erreichbar sein sollte: NEIN, nur org_admin und super_admin duerfen User-Listen sehen. Die bestehende Implementierung ist korrekt — NUR verifizieren, kein Code-Change noetig falls korrekt.

**L2 — activities.js (NEU, per D-08, D-09):**
In DELETE /:id Route (~Zeile 191-217) nach dem bestehenden `user_activities` Check (Zeile 194-199) und VOR dem eigentlichen DELETE (Zeile 201): Zusaetzlichen Check einfuegen:
```javascript
// Pending Antraege pruefen
const { rows: [pendingCheck] } = await db.query(
  `SELECT COUNT(*)::int as count FROM activity_requests ar
   WHERE ar.activity_id = $1 AND ar.status = 'pending'`,
  [activityId]
);
if (pendingCheck.count > 0) {
  return res.status(409).json({
    error: `Aktivität kann nicht gelöscht werden: ${pendingCheck.count} offene Anträge vorhanden.`
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "u.organization_id" backend/routes/konfi-managment.js | head -5 && grep -n "Letzter Org-Admin" backend/routes/users.js | head -5 && grep -n "activity_requests" backend/routes/activities.js | grep -c "pending" && grep -n "org_admin" backend/routes/organizations.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "JOIN users u ON bp.konfi_id = u.id" backend/routes/konfi-managment.js findet genau 1 Treffer
    - grep "Letzter Org-Admin" backend/routes/users.js findet genau 1 Treffer
    - grep "activity_requests.*pending" backend/routes/activities.js findet mindestens 1 Treffer
    - grep "org_admin" backend/routes/organizations.js findet den bestehenden Rollen-Check
    - Alle 4 Dateien sind staged oder committed
  </acceptance_criteria>
  <done>Alle 4 Backend-Fixes verifiziert und korrekt. S1 (org_id JOIN) und L1 (letzte org_admin) aus unstaged Changes uebernommen, S2 (org-users Zugriff) verifiziert, L2 (pending requests Check) neu implementiert.</done>
</task>

</tasks>

<verification>
- konfi-managment.js: bonus-points DELETE hat organization_id JOIN
- users.js: DELETE /:id prueft auf letzten org_admin
- organizations.js: GET /:id/users hat korrekten Rollen-Check (org_admin + super_admin)
- activities.js: DELETE /:id prueft pending activity_requests vor Loeschung
</verification>

<success_criteria>
- Alle 4 Backend-Sicherheits-/Logik-Fixes implementiert und verifiziert
- Keine Regression in bestehenden Routes
- Fehlermeldungen mit echten Umlauten
</success_criteria>

<output>
After completion, create `.planning/phases/70-rollen-audit-fixes-sicherheit-multi-tenant-zugriff-logik-letzte-org-admin-pending-antr-ge-frontend-eventdetail-offline-isonline-buttons-konsistent-aktivit-ten-kategorie-filter/70-01-SUMMARY.md`
</output>
