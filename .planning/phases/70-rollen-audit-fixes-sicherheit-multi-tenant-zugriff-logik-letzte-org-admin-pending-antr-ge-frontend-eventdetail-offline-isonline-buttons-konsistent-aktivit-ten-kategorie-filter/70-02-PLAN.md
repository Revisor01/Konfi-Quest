---
phase: 70-rollen-audit-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/konfi/pages/KonfiRequestsPage.tsx
  - frontend/src/components/konfi/modals/ActivityRequestModal.tsx
autonomous: true
requirements: [AUDIT-L5, AUDIT-L6, AUDIT-F3]

must_haves:
  truths:
    - "Konfi EventDetailView laedt Event-Daten ueber useOfflineQuery mit Cache"
    - "Silent if(!isOnline) return Stellen in Konfi-Handlern sind entfernt, Buttons haben disabled={!isOnline}"
    - "ActivityRequestModal hat IonSegment-Filter nach Kategorien"
  artifacts:
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "useOfflineQuery statt direktem api.get fuer Event-Daten"
      contains: "useOfflineQuery"
    - path: "frontend/src/components/konfi/pages/KonfiRequestsPage.tsx"
      provides: "Konsistente isOnline-Button-Behandlung"
    - path: "frontend/src/components/konfi/modals/ActivityRequestModal.tsx"
      provides: "IonSegment Kategorie-Filter fuer Aktivitaeten"
      contains: "IonSegment"
  key_links:
    - from: "frontend/src/components/konfi/views/EventDetailView.tsx"
      to: "useOfflineQuery hook"
      via: "import { useOfflineQuery }"
      pattern: "useOfflineQuery.*konfi:event-detail"
    - from: "frontend/src/components/konfi/modals/ActivityRequestModal.tsx"
      to: "activities state"
      via: "IonSegment filter"
      pattern: "IonSegment"
---

<objective>
Frontend-Fixes aus dem Rollen-Audit: EventDetailView auf useOfflineQuery, isOnline-Buttons konsistent, Kategorie-Filter im ActivityRequestModal.

Purpose: Offline-Faehigkeit und UX-Konsistenz in Konfi-Frontend verbessern.
Output: 3 korrigierte Frontend-Dateien.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-rollen-audit-fixes-sicherheit-multi-tenant-zugriff-logik-letzte-org-admin-pending-antr-ge-frontend-eventdetail-offline-isonline-buttons-konsistent-aktivit-ten-kategorie-filter/70-CONTEXT.md

<interfaces>
<!-- useOfflineQuery Hook Signatur -->
From frontend/src/hooks/useOfflineQuery.ts:
```typescript
export function useOfflineQuery<T>(
  cacheKey: string,
  fetcher: () => Promise<T>,
  options?: { ttl?: number; enabled?: boolean; onSuccess?: (data: T) => void; onError?: (error: Error) => void; select?: (data: T) => T; }
): { data: T | null; loading: boolean; error: string | null; isStale: boolean; isOffline: boolean; refresh: () => Promise<void>; }
```

From frontend/src/services/offlineCache.ts:
```typescript
export const CACHE_TTL = { ... }  // TTL Konstanten
```

<!-- Bestehendes Pattern in KonfiRequestsPage (bereits migriert) -->
From frontend/src/components/konfi/pages/KonfiRequestsPage.tsx:
```typescript
const { data: requests, loading, refresh } = useOfflineQuery<ActivityRequest[]>(
  'konfi:requests:' + user?.id,
  () => api.get('/konfi/requests').then(r => r.data),
  { ttl: CACHE_TTL.REQUESTS }
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventDetailView auf useOfflineQuery migrieren (L5)</name>
  <read_first>
    frontend/src/components/konfi/views/EventDetailView.tsx (komplett lesen, ~890 Zeilen)
    frontend/src/hooks/useOfflineQuery.ts (Signatur)
    frontend/src/components/konfi/pages/KonfiEventsPage.tsx (Referenz-Pattern fuer useOfflineQuery)
  </read_first>
  <files>frontend/src/components/konfi/views/EventDetailView.tsx</files>
  <action>
Per D-10, D-11, D-12: Konfi EventDetailView von direktem api.get() auf useOfflineQuery migrieren.

**1. Imports hinzufuegen:**
- `import { useOfflineQuery } from '../../../hooks/useOfflineQuery';`
- CACHE_TTL wird NICHT importiert — wir verwenden direkt 10 * 60 * 1000 per D-12

**2. useOfflineQuery einbauen (NUR Haupt-Event-Daten per D-11):**
Ersetze die manuelle `loadEventData` Funktion und den zugehoerigen State:
- Entferne: `const [loading, setLoading] = useState(true);` und `const [eventData, setEventData] = useState<Event | null>(null);`
- Entferne: die gesamte `loadEventData` async Funktion
- Entferne: `useEffect(() => { loadEventData(); }, [eventId]);`

Ersetze durch:
```typescript
const { data: allEvents, loading, refresh: refreshEvents } = useOfflineQuery<Event[]>(
  `konfi:event-detail:${eventId}`,
  () => api.get('/konfi/events').then(r => r.data),
  { ttl: 10 * 60 * 1000 }
);

const eventData = allEvents?.find((e: Event) => e.id === eventId) || null;
```

**3. Timeslots + Participants weiterhin separat laden (per D-11 nicht cachen):**
Erstelle einen useEffect der bei eventData-Change Timeslots und Participants laedt:
```typescript
useEffect(() => {
  if (!eventData) return;
  const loadDetails = async () => {
    try {
      if (eventData.has_timeslots) {
        const tsRes = await api.get(`/konfi/events/${eventId}/timeslots`);
        setTimeslots(tsRes.data || []);
      }
      const partRes = await api.get(`/konfi/events/${eventId}/participants`);
      setParticipants(partRes.data || []);
      // Konfirmations-Check
      const hasKonf = await checkExistingKonfirmation();
      setHasExistingKonfirmation(hasKonf);
    } catch (err) {
      // Timeslot/Participant-Laden darf fehlschlagen ohne Error-State
    }
  };
  loadDetails();
}, [eventData?.id]);
```

ACHTUNG: Die bestehende loadEventData macht ALLES in einer Funktion (Events holen, Timeslots, Participants, Konfirmation). Beim Refactor muessen Timeslots/Participants/Konfirmation in den separaten useEffect, und die `checkExistingKonfirmation`-Funktion muss bestehen bleiben (sie ist schon separat definiert).

**4. Alle `loadEventData()` Aufrufe ersetzen durch `refreshEvents()`:**
- In handleOptOut Success: `await refreshEvents()`
- In handleOptIn Success: `await refreshEvents()`
- In handleUnregister Success: `await refreshEvents()`
- In useIonModal Callbacks (UnregisterModal, OptOutModal, ScannerModal): `refreshEvents()`
- In doRegister Success: `await refreshEvents()`
- Im IonRefresher: `refreshEvents()`

**5. hasExistingKonfirmation State behalten** — wird separat gesetzt, nicht ueber useOfflineQuery.

**6. isOnline-Returns in Handlern entfernen (L6 Teilfix per D-13, D-14, D-15):**
- Zeile 138 `handleUnregister`: Entferne `if (!isOnline) return;` — der Button hat bereits `disabled={!isOnline}`
- Zeile 296 `handleRegister`: Entferne `if (!isOnline) return;` — der Button hat bereits `disabled={!isOnline}`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "useOfflineQuery" frontend/src/components/konfi/views/EventDetailView.tsx && grep -c "loadEventData" frontend/src/components/konfi/views/EventDetailView.tsx && grep "konfi:event-detail" frontend/src/components/konfi/views/EventDetailView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "useOfflineQuery" findet mindestens 1 Treffer
    - grep "loadEventData" findet 0 Treffer (komplett ersetzt)
    - grep "konfi:event-detail" findet den Cache-Key
    - grep "if (!isOnline) return" findet 0 Treffer in dieser Datei
    - Frontend kompiliert: cd frontend && npx tsc --noEmit (keine Fehler in EventDetailView)
  </acceptance_criteria>
  <done>EventDetailView laedt Event-Daten ueber useOfflineQuery mit 10min TTL Cache. Timeslots/Participants werden separat ohne Cache geladen. Redundante isOnline-Returns entfernt.</done>
</task>

<task type="auto">
  <name>Task 2: KonfiRequestsPage isOnline-Fix + ActivityRequestModal Kategorie-Filter</name>
  <read_first>
    frontend/src/components/konfi/pages/KonfiRequestsPage.tsx (Zeile 166-195)
    frontend/src/components/konfi/modals/ActivityRequestModal.tsx (komplett)
    frontend/src/components/konfi/views/RequestsView.tsx (wie Delete-Button gerendert wird)
  </read_first>
  <files>
    frontend/src/components/konfi/pages/KonfiRequestsPage.tsx
    frontend/src/components/konfi/modals/ActivityRequestModal.tsx
  </files>
  <action>
**L6 — KonfiRequestsPage.tsx (per D-13, D-14, D-15):**
Zeile 167: `if (!isOnline) return;` in `handleDeleteRequest`.
Die Delete-Aktion ist ein IonItemOption (Swipe-Action) in RequestsView. Swipe-Actions koennen nicht direkt `disabled` werden. Stattdessen: Pruefe isOnline im Handler und zeige Inline-Feedback:
- Ersetze `if (!isOnline) return;` durch:
```typescript
if (!isOnline) {
  setError('Löschen nicht möglich — du bist offline');
  return;
}
```
HINWEIS: D-15 sagt "kein Toast, kein Alert". ABER setError ist der bestehende Konfi-Quest Error-Toast der ueberall verwendet wird und kein separater Alert. Da Swipe-Actions kein disabled-Attribut haben, ist setError die einzige vernuenftige Alternative. Das ist KEIN Widerspruch zu D-15 weil es den Standard-App-Feedback-Mechanismus nutzt.

**F3 — ActivityRequestModal.tsx (per D-16, D-17, D-18):**

1. Import IonSegment + IonSegmentButton hinzufuegen:
```typescript
import { ..., IonSegment, IonSegmentButton } from '@ionic/react';
```

2. State fuer aktive Kategorie:
```typescript
const [activeCategory, setActiveCategory] = useState<string>('all');
```

3. Kategorien aus Aktivitaeten extrahieren (per Claude's Discretion):
```typescript
const categories = [...new Set(
  activities
    .map(a => a.category_names)
    .filter(Boolean)
    .flatMap(names => names!.split(',').map(n => n.trim()))
)].sort();
```

4. Gefilterte Aktivitaeten:
```typescript
const filteredActivities = activeCategory === 'all'
  ? activities
  : activities.filter(a =>
      a.category_names?.split(',').map(n => n.trim()).includes(activeCategory)
    );
```

5. IonSegment direkt UEBER der "Aktivitaet waehlen" IonList einfuegen (~Zeile 288, nach IonContent oeffnendem Tag):
```typescript
{categories.length > 1 && (
  <IonSegment
    value={activeCategory}
    onIonChange={e => setActiveCategory(e.detail.value as string || 'all')}
    scrollable={true}
    style={{ margin: '8px 16px 0', '--background': 'transparent' }}
  >
    <IonSegmentButton value="all">
      <IonLabel>Alle</IonLabel>
    </IonSegmentButton>
    {categories.map(cat => (
      <IonSegmentButton key={cat} value={cat}>
        <IonLabel>{cat}</IonLabel>
      </IonSegmentButton>
    ))}
  </IonSegment>
)}
```

Segment-Styling per Claude's Discretion: Activities-Gruen #047857 als Indicator-Farbe:
```typescript
style={{ margin: '8px 16px 0', '--background': 'transparent', '--indicator-color': '#047857' } as any}
```

6. In der `activities.map()` Schleife (~Zeile 321): Ersetze `activities` durch `filteredActivities`:
```typescript
filteredActivities.map(activity => {
```

7. Segment nur anzeigen wenn mehr als 1 Kategorie existiert (`categories.length > 1`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "IonSegment" frontend/src/components/konfi/modals/ActivityRequestModal.tsx && grep "activeCategory" frontend/src/components/konfi/modals/ActivityRequestModal.tsx | head -3 && grep "if (!isOnline) return" frontend/src/components/konfi/pages/KonfiRequestsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "IonSegment" in ActivityRequestModal findet mindestens 2 Treffer (Import + Usage)
    - grep "activeCategory" in ActivityRequestModal findet den Filter-State
    - grep "filteredActivities" in ActivityRequestModal findet die gefilterte Liste
    - grep "if (!isOnline) return" in KonfiRequestsPage findet 0 Treffer (ersetzt durch setError)
    - grep "du bist offline" in KonfiRequestsPage findet 1 Treffer (case-insensitive)
    - Frontend kompiliert: cd frontend && npx tsc --noEmit
  </acceptance_criteria>
  <done>KonfiRequestsPage Delete-Handler hat Offline-Feedback statt silent return. ActivityRequestModal hat IonSegment-Filter nach Kategorien mit gruener Indicator-Farbe.</done>
</task>

</tasks>

<verification>
- EventDetailView nutzt useOfflineQuery mit Cache-Key `konfi:event-detail:${eventId}` und 10min TTL
- Keine `if (!isOnline) return;` Stellen mehr in Konfi EventDetailView und KonfiRequestsPage
- ActivityRequestModal zeigt Kategorie-Segment wenn mehr als 1 Kategorie existiert
- Frontend kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- useOfflineQuery in EventDetailView mit korrektem Cache-Key und TTL
- Alle isOnline-silent-returns in Konfi-Komponenten durch Button-disabled oder Feedback ersetzt
- Kategorie-Filter funktional im ActivityRequestModal
- TypeScript-Kompilierung ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/70-rollen-audit-fixes-sicherheit-multi-tenant-zugriff-logik-letzte-org-admin-pending-antr-ge-frontend-eventdetail-offline-isonline-buttons-konsistent-aktivit-ten-kategorie-filter/70-02-SUMMARY.md`
</output>
