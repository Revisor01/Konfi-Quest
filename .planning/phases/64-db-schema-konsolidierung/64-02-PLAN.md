---
phase: 64-db-schema-konsolidierung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/064_consolidate_inline_schemas.sql
  - backend/routes/material.js
  - backend/routes/teamer.js
autonomous: true
requirements: [SCHEMA-CONSOLIDATE, SCHEMA-CLEANUP]

must_haves:
  truths:
    - "Inline CREATE TABLE Statements in Routes werden nicht mehr bei jedem Server-Start ausgefuehrt"
    - "Tabellen-Erstellung ist in einer zentralen Migration-Datei dokumentiert"
    - "Material- und Teamer-Routes funktionieren weiterhin korrekt"
  artifacts:
    - path: "backend/migrations/064_consolidate_inline_schemas.sql"
      provides: "Zentrale Migration fuer material_*, certificate_*, teamer-Spalten"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "backend/routes/material.js"
      provides: "Material-Route ohne inline Schema-Erstellung"
    - path: "backend/routes/teamer.js"
      provides: "Teamer-Route ohne inline Schema-Erstellung"
  key_links:
    - from: "backend/migrations/064_consolidate_inline_schemas.sql"
      to: "backend/routes/material.js"
      via: "Migration erstellt Tabellen die material.js nutzt"
      pattern: "material_tags|materials|material_files"
    - from: "backend/migrations/064_consolidate_inline_schemas.sql"
      to: "backend/routes/teamer.js"
      via: "Migration erstellt Tabellen die teamer.js nutzt"
      pattern: "certificate_types|user_certificates"
---

<objective>
Inline CREATE TABLE Statements aus Route-Dateien in zentrale Migrations extrahieren.

Purpose: material.js und teamer.js erstellen bei jedem Server-Start Tabellen via CREATE TABLE IF NOT EXISTS. Das ist ein Anti-Pattern: Schema-Definition gehoert in Migrations, nicht in Route-Handler. Die Inline-Statements verlangsamen den Server-Start und erschweren Schema-Uebersicht.

Output: 1 zentrale Migrations-Datei + bereinigte Routes ohne inline Schema-Code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-db-schema-konsolidierung/64-CONTEXT.md

@backend/routes/material.js
@backend/routes/teamer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zentrale Migration aus Inline-Schemas erstellen</name>
  <files>backend/migrations/064_consolidate_inline_schemas.sql</files>
  <read_first>
    - backend/routes/material.js (Zeilen 14-100: inline CREATE TABLE fuer material_tags, materials, material_file_tags, material_files, material_events, material_jahrgaenge)
    - backend/routes/teamer.js (Zeilen 12-42: inline CREATE TABLE fuer certificate_types, user_certificates, ALTER TABLE users ADD teamer_since)
    - backend/routes/badges.js (Zeilen 640-700: inline ALTER TABLE fuer user_badges, user_activities Renames — pruefen ob noch aktiv)
    - backend/routes/jahrgaenge.js (Zeilen 19-22: inline ALTER TABLE fuer gottesdienst_enabled etc.)
    - backend/routes/settings.js (Zeilen 37-54: inline ALTER TABLE fuer organization_id)
  </read_first>
  <action>
Erstelle `backend/migrations/064_consolidate_inline_schemas.sql` die ALLE inline Schema-Statements aus den Routes in einer Datei zusammenfasst.

Extrahiere exakt die SQL-Statements aus:
1. **material.js**: CREATE TABLE material_tags, materials, material_file_tags, material_files, material_events, material_jahrgaenge
2. **teamer.js**: CREATE TABLE certificate_types, user_certificates, ALTER TABLE users ADD COLUMN teamer_since
3. **badges.js**: ALTER TABLE konfi_badges RENAME TO user_badges, konfi_activities RENAME TO user_activities (etc.) — NUR falls diese noch aktiv ausgefuehrt werden
4. **jahrgaenge.js**: ALTER TABLE jahrgaenge ADD COLUMN gottesdienst_enabled, gemeinde_enabled, target_gottesdienst, target_gemeinde
5. **settings.js**: ALTER TABLE settings ADD COLUMN organization_id, Constraint-Aenderungen

Alle Statements mit IF NOT EXISTS bzw. IF NOT EXISTS Check (DO-Block fuer ALTER). Kommentiere jeden Block mit der Herkunft (Route-Datei).

WICHTIG: Die SQL muss IDENTISCH sein mit den Inline-Statements — keine "Verbesserungen" oder Aenderungen an Datentypen/Constraints. Die Migration ist ein 1:1-Extrakt.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS\|ADD COLUMN IF NOT EXISTS\|ALTER TABLE" backend/migrations/064_consolidate_inline_schemas.sql</automated>
  </verify>
  <done>Migration enthaelt alle inline Schema-Statements aus material.js, teamer.js, badges.js, jahrgaenge.js, settings.js — exakt 1:1 kopiert</done>
  <acceptance_criteria>
    - Datei backend/migrations/064_consolidate_inline_schemas.sql existiert
    - Enthaelt CREATE TABLE fuer: material_tags, materials, material_file_tags, material_files, material_events, material_jahrgaenge, certificate_types, user_certificates
    - Enthaelt ALTER TABLE fuer: users.teamer_since, jahrgaenge.(gottesdienst_enabled, gemeinde_enabled, target_gottesdienst, target_gemeinde)
    - Jeder Block mit Kommentar zur Herkunft (z.B. "-- Aus backend/routes/material.js")
    - Alle IF NOT EXISTS
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Inline Schema-Erstellung aus Routes entfernen</name>
  <files>backend/routes/material.js, backend/routes/teamer.js</files>
  <read_first>
    - backend/routes/material.js (vollstaendig — Schema-Init-Block identifizieren und Route-Logik verstehen)
    - backend/routes/teamer.js (vollstaendig — Schema-Init-Block identifizieren und Route-Logik verstehen)
  </read_first>
  <action>
Entferne die inline CREATE TABLE / ALTER TABLE Bloecke aus material.js und teamer.js.

**material.js:**
- Entferne den gesamten Schema-Init-Block (ca. Zeile 14-100) der die 6 Tabellen erstellt und die Migration-Inserts macht
- BEHALTE alle Route-Handler, Queries, Exports unveraendert
- Ersetze den Init-Block durch einen kurzen Kommentar: `// Schema: siehe backend/migrations/064_consolidate_inline_schemas.sql`

**teamer.js:**
- Entferne den Schema-Init-Block (ca. Zeile 12-42) der certificate_types, user_certificates erstellt und users.teamer_since hinzufuegt
- BEHALTE alle Route-Handler, Queries, Exports unveraendert
- Ersetze den Init-Block durch einen kurzen Kommentar: `// Schema: siehe backend/migrations/064_consolidate_inline_schemas.sql`

WICHTIG:
- NUR Schema-Erstellung entfernen, NICHT die Route-Logik
- badges.js, jahrgaenge.js, settings.js vorerst NICHT aendern — deren Inline-Migrations sind defensiver (ADD COLUMN IF NOT EXISTS) und weniger problematisch
- Testen: `cd backend && node -e "require('./routes/material')" 2>&1` und analog fuer teamer.js darf keinen Syntax-Error werfen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "CREATE TABLE IF NOT EXISTS" backend/routes/material.js && grep -c "CREATE TABLE IF NOT EXISTS" backend/routes/teamer.js</automated>
  </verify>
  <done>material.js und teamer.js enthalten keine CREATE TABLE Statements mehr, Route-Logik ist unveraendert, kein Syntax-Error bei require()</done>
  <acceptance_criteria>
    - grep "CREATE TABLE" backend/routes/material.js gibt 0 Treffer
    - grep "CREATE TABLE" backend/routes/teamer.js gibt 0 Treffer
    - Route-Handler (GET, POST, PUT, DELETE) sind alle noch vorhanden
    - module.exports am Ende der Datei ist unveraendert
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Migration 064_consolidate_inline_schemas.sql existiert mit allen extrahierten Statements
2. material.js hat keine inline Schema-Erstellung mehr
3. teamer.js hat keine inline Schema-Erstellung mehr
4. Route-Exporte und Handler sind unveraendert
5. Migration ist idempotent (IF NOT EXISTS ueberall)
</verification>

<success_criteria>
- 1 zentrale Migrations-Datei mit allen inline Schema-Statements
- material.js und teamer.js bereinigt (kein CREATE TABLE mehr)
- Keine Breaking Changes — Routes funktionieren wenn Tabellen bereits existieren
- Migration kann vor oder nach den Routes ausgefuehrt werden (IF NOT EXISTS)
</success_criteria>

<output>
After completion, create `.planning/phases/64-db-schema-konsolidierung/64-02-SUMMARY.md`
</output>
