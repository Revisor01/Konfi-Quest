---
phase: 80-wrapped-persistenz-individualisierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/wrapped.js
  - frontend/src/types/wrapped.ts
autonomous: true
requirements:
  - D-04
  - D-05
  - D-12
  - D-13
  - D-14

must_haves:
  truths:
    - "GET /api/wrapped/history/:userId liefert Array aller Wrapped-Snapshots eines Users"
    - "Konfi-Snapshots enthalten highlight_type, formulierung_seed, gottesdienst_count und kategorie_verteilung"
    - "highlight_type wird korrekt aus staerkstem Bereich berechnet, ueber_das_ziel hat hoechste Prioritaet"
  artifacts:
    - path: "backend/routes/wrapped.js"
      provides: "Erweiterte Aggregation + History-Endpoint"
      contains: "history/:userId"
    - path: "frontend/src/types/wrapped.ts"
      provides: "Erweiterte TypeScript-Interfaces mit highlight_type etc."
      contains: "highlight_type"
  key_links:
    - from: "backend/routes/wrapped.js generateKonfiSnapshot"
      to: "wrapped_snapshots.data JSONB"
      via: "highlight_type + gottesdienst_count + kategorie_verteilung Felder"
      pattern: "highlight_type"
    - from: "backend/routes/wrapped.js /history/:userId"
      to: "wrapped_snapshots Tabelle"
      via: "SELECT alle Snapshots eines Users"
      pattern: "history"
---

<objective>
Backend-Erweiterung: Aggregation um highlight_type, gottesdienst_count, kategorie_verteilung und formulierung_seed erweitern. Neuer History-Endpoint fuer Wiederansicht. TypeScript-Interfaces aktualisieren.

Purpose: Liefert die Daten-Grundlage fuer individualisierte Slides (Plan 02) und Wiederansicht (Plan 03).
Output: Erweiterter wrapped.js + aktualisierte wrapped.ts Types
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/wrapped.js
@frontend/src/types/wrapped.ts
</context>

<interfaces>
<!-- Bestehende Struktur, die erweitert wird -->

From backend/routes/wrapped.js:
```javascript
async function generateKonfiSnapshot(client, userId, orgId, jahrgangId, year) {
  // Gibt JSONB-Snapshot zurueck mit: version, slides: { punkte, events, badges, aktivster_monat, chat, endspurt, zeitraum }
}
```

From frontend/src/types/wrapped.ts:
```typescript
export interface KonfiWrappedData {
  version: number;
  slides: {
    punkte: KonfiPunkteSlide;
    events: KonfiEventsSlide;
    badges: KonfiBadgesSlide;
    aktivster_monat: KonfiAktivsterMonatSlide;
    chat: KonfiChatSlide;
    endspurt: KonfiEndspurtSlide;
    zeitraum: KonfiZeitraumSlide;
  };
}

export interface WrappedResponse {
  data: KonfiWrappedData | TeamerWrappedData;
  computed_at: string;
  year: number;
  wrapped_type: 'konfi' | 'teamer';
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend-Aggregation erweitern + History-Endpoint</name>
  <files>backend/routes/wrapped.js</files>
  <read_first>backend/routes/wrapped.js</read_first>
  <action>
**A) generateKonfiSnapshot erweitern** (per D-12, D-13):

1. **Gottesdienst-Count** (per D-13): Neue Query nach generateKonfiSnapshot Zeile ~72 (nach totalAttended):
```sql
SELECT COUNT(*) as count FROM event_bookings eb
JOIN events e ON eb.event_id = e.id
WHERE eb.user_id = $1 AND eb.status = 'confirmed' AND eb.attendance_status = 'present'
  AND e.organization_id = $2 AND e.point_type = 'gottesdienst'
```
Variable: `gottesdienstCount`

2. **Kategorie-Verteilung** (per D-13): Neue Query fuer Aktivitaeten nach Kategorie:
```sql
SELECT c.name as kategorie, COUNT(*) as count
FROM user_activities ua
JOIN activities a ON ua.activity_id = a.id
JOIN categories c ON a.category_id = c.id
WHERE ua.user_id = $1 AND ua.organization_id = $2
GROUP BY c.name
ORDER BY count DESC
```
Variable: `kategorieVerteilung` — Array von `{ kategorie: string, count: number }`

3. **highlight_type berechnen** (per D-05): NACH allen Queries, VOR dem return. Logik:
   - Zuerst pruefen: wenn `aktuellTotal >= zielTotal && zielTotal > 0` → `ueber_das_ziel` (hoechste Prio!)
   - Sonst den staerksten Bereich bestimmen aus:
     - `events_held`: totalAttended (Events besucht)
     - `badge_collector`: badgeRows.length
     - `chat_champion`: nachrichtenGesendet
     - `gottesdienst_treue`: gottesdienstCount
     - `gemeinde_aktiv`: gemeinde (Gemeinde-Punkte)
   - Vergleiche die Werte, nimm den mit dem hoechsten Wert. Bei Gleichstand: Reihenfolge wie oben (events_held gewinnt).
   Variable: `highlightType`

4. **formulierung_seed** (per D-07): Berechne deterministischen Seed:
```javascript
const formulierungSeed = (userId * 31 + year * 17) % 97;
```

5. **Return-Objekt erweitern**: Im return-Objekt der Funktion (ca. Zeile 165) folgende Felder auf Top-Level (neben `version` und `slides`) hinzufuegen:
```javascript
highlight_type: highlightType,
formulierung_seed: formulierungSeed,
```

Und in `slides` zwei neue Eintraege:
```javascript
gottesdienst: {
  count: gottesdienstCount
},
kategorie: {
  verteilung: kategorieVerteilung,
  top_kategorie: kategorieVerteilung.length > 0 ? kategorieVerteilung[0].kategorie : null
}
```

**B) History-Endpoint** (per D-04, D-14):

NACH dem DELETE-Endpoint (Zeile ~579), VOR den Batch-Generierungs-Funktionen, neuen Endpoint hinzufuegen:

```javascript
// GET /history/:userId - Alle Wrapped-Snapshots eines Users
router.get('/history/:userId',
  rbacVerifier,
  param('userId').isInt({ min: 1 }),
  handleValidationErrors,
  async (req, res) => {
    try {
      const targetUserId = parseInt(req.params.userId, 10);
      // Sicherheitspruefung: Nur eigene Daten ODER Admin der gleichen Org
      const roleName = req.user.role_name;
      if (req.user.id !== targetUserId) {
        if (roleName !== 'admin' && roleName !== 'org_admin') {
          return res.status(403).json({ error: 'Keine Berechtigung' });
        }
        // Admin: Pruefen ob User zur gleichen Org gehoert
        const { rows: [targetUser] } = await db.query(
          'SELECT organization_id FROM users WHERE id = $1', [targetUserId]
        );
        if (!targetUser || targetUser.organization_id !== req.user.organization_id) {
          return res.status(403).json({ error: 'Keine Berechtigung' });
        }
      }

      const { rows } = await db.query(
        `SELECT id, wrapped_type, year, data, computed_at
         FROM wrapped_snapshots
         WHERE user_id = $1
         ORDER BY year DESC, wrapped_type`,
        [targetUserId]
      );

      res.json(rows);
    } catch (err) {
      console.error('Error loading wrapped history:', err);
      res.status(500).json({ error: 'Fehler beim Laden der Wrapped-Historie' });
    }
  }
);
```

WICHTIG: Der History-Endpoint MUSS vor den Batch-Funktionen (`router.generateAllKonfiWrapped`) stehen, da diese keine Express-Routen sind sondern direkte Funktionen am router-Objekt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && node -e "
      const src = require('fs').readFileSync('backend/routes/wrapped.js', 'utf8');
      const checks = [
        src.includes('highlight_type'),
        src.includes('formulierung_seed'),
        src.includes('gottesdienst'),
        src.includes('kategorie'),
        src.includes('history/:userId'),
        src.includes('point_type'),
        src.includes('kategorieVerteilung') || src.includes('kategorie_verteilung'),
      ];
      const pass = checks.every(Boolean);
      console.log(pass ? 'PASS' : 'FAIL: ' + checks.map((c,i) => c ? 'ok' : 'MISSING-' + i).join(', '));
      process.exit(pass ? 0 : 1);
    "</automated>
  </verify>
  <acceptance_criteria>
    - generateKonfiSnapshot gibt highlight_type, formulierung_seed, slides.gottesdienst und slides.kategorie zurueck
    - highlight_type-Logik: ueber_das_ziel hat hoechste Prio, dann staerkster Bereich
    - formulierung_seed ist deterministisch (userId * 31 + year * 17) % 97
    - GET /history/:userId liefert Array aller Snapshots eines Users (eigene oder gleiche Org fuer Admins)
  </acceptance_criteria>
  <done>Backend liefert erweiterte Konfi-Snapshots mit Individualisierungs-Daten und History-Endpoint antwortet korrekt</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript-Interfaces erweitern</name>
  <files>frontend/src/types/wrapped.ts</files>
  <read_first>frontend/src/types/wrapped.ts</read_first>
  <action>
Per D-12: Die TypeScript-Interfaces in `wrapped.ts` erweitern, damit sie die neuen Backend-Felder abbilden:

1. **Neue Slide-Interfaces** hinzufuegen (nach KonfiZeitraumSlide):
```typescript
export interface KonfiGottesdienstSlide {
  count: number;
}

export interface KonfiKategorieSlide {
  verteilung: Array<{ kategorie: string; count: number }>;
  top_kategorie: string | null;
}
```

2. **HighlightType als Union-Type**:
```typescript
export type HighlightType =
  | 'events_held'
  | 'badge_collector'
  | 'chat_champion'
  | 'gottesdienst_treue'
  | 'gemeinde_aktiv'
  | 'ueber_das_ziel';
```

3. **KonfiWrappedData erweitern** — Top-Level Felder + neue Slides:
```typescript
export interface KonfiWrappedData {
  version: number;
  highlight_type: HighlightType;
  formulierung_seed: number;
  slides: {
    punkte: KonfiPunkteSlide;
    events: KonfiEventsSlide;
    badges: KonfiBadgesSlide;
    aktivster_monat: KonfiAktivsterMonatSlide;
    chat: KonfiChatSlide;
    endspurt: KonfiEndspurtSlide;
    zeitraum: KonfiZeitraumSlide;
    gottesdienst: KonfiGottesdienstSlide;
    kategorie: KonfiKategorieSlide;
  };
}
```

4. **WrappedHistoryEntry Interface** (fuer History-Endpoint):
```typescript
export interface WrappedHistoryEntry {
  id: number;
  wrapped_type: 'konfi' | 'teamer';
  year: number;
  data: KonfiWrappedData | TeamerWrappedData;
  computed_at: string;
}
```

WICHTIG: Keine bestehenden Interfaces loeschen oder umbenennen — nur erweitern! Alte Snapshots haben die neuen Felder nicht, daher muessen highlight_type und formulierung_seed im Frontend optional behandelt werden. Setze sie im Interface aber als required, da neue Snapshots sie immer haben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - KonfiWrappedData hat highlight_type: HighlightType und formulierung_seed: number
    - KonfiGottesdienstSlide und KonfiKategorieSlide Interfaces existieren
    - WrappedHistoryEntry Interface existiert
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Alle neuen Backend-Felder sind als TypeScript-Interfaces abgebildet, tsc kompiliert fehlerfrei</done>
</task>

</tasks>

<verification>
- Backend: `node -e "require('./backend/routes/wrapped.js')"` laeuft ohne Syntaxfehler
- Frontend: `cd frontend && npx tsc --noEmit` kompiliert fehlerfrei
- wrapped.js enthaelt highlight_type Berechnung, gottesdienst Query, kategorie Query, history Endpoint
- wrapped.ts enthaelt HighlightType, KonfiGottesdienstSlide, KonfiKategorieSlide, WrappedHistoryEntry
</verification>

<success_criteria>
Backend generiert Konfi-Snapshots mit highlight_type, formulierung_seed, gottesdienst.count und kategorie.verteilung. History-Endpoint liefert alle Snapshots eines Users. TypeScript-Types matchen die JSONB-Struktur.
</success_criteria>

<output>
After completion, create `.planning/phases/80-wrapped-persistenz-individualisierung-wiederansicht-meine-wrappeds-konfi-history-wrapped-card-individualisierte-slides-farben-reihenfolge-highlights-formulierungen-pro-user/80-01-SUMMARY.md`
</output>
