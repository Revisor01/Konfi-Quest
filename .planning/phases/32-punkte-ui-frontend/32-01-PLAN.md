---
phase: 32-punkte-ui-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/views/ActivityRings.tsx
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/modals/PointsHistoryModal.tsx
autonomous: true
requirements:
  - PUI-01
  - PUI-03
  - PUI-05

must_haves:
  truths:
    - "ActivityRings zeigt dynamisch 1 oder 3 Ringe basierend auf aktiven Punkte-Typen"
    - "Bei 1 aktivem Typ kein Total-Ring und kein Gesamt-Chip"
    - "Ranking im Dashboard zeigt Backend-gelieferte total_points (nur aktive Typen)"
    - "Punkte-Historie blendet Eintraege deaktivierter Typen komplett aus"
    - "KonfiDashboardPage nutzt point_config aus Dashboard-Response statt /settings"
  artifacts:
    - path: "frontend/src/components/admin/views/ActivityRings.tsx"
      provides: "Dynamische Ring-Anzahl basierend auf enabled-Flags"
      contains: "gottesdienstEnabled"
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Bedingte Stats-Chips und Ranking mit point_config"
      contains: "gottesdienstEnabled"
    - path: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      provides: "Filter nach aktiven Punkte-Typen"
      contains: "pointConfig"
  key_links:
    - from: "frontend/src/components/konfi/pages/KonfiDashboardPage.tsx"
      to: "DashboardView"
      via: "point_config aus dashboardResponse.data.point_config als Props"
      pattern: "point_config"
    - from: "frontend/src/components/konfi/views/DashboardView.tsx"
      to: "ActivityRings"
      via: "gottesdienstEnabled/gemeindeEnabled Props"
      pattern: "gottesdienstEnabled"
    - from: "frontend/src/components/konfi/pages/KonfiDashboardPage.tsx"
      to: "PointsHistoryModal"
      via: "pointConfig Prop im useIonModal-Aufruf"
      pattern: "pointConfig"
---

<objective>
Konfi-Dashboard und zugehoerige Komponenten reagieren auf deaktivierte Punkte-Typen. ActivityRings zeigt dynamische Ring-Anzahl, Stats/Chips blenden deaktivierte Typen aus, Ranking nutzt Backend-Summen, und Punkte-Historie filtert deaktivierte Typen.

Purpose: Konfis sehen nur aktive Punkte-Typen -- kein verwirrendes UI fuer deaktivierte Kategorien.
Output: 4 Frontend-Dateien angepasst, point_config durchgereicht und genutzt.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-punkte-ui-frontend/32-CONTEXT.md
@.planning/phases/32-punkte-ui-frontend/32-RESEARCH.md

<interfaces>
<!-- Backend liefert bereits point_config im Dashboard-Endpoint -->

From backend/routes/konfi.js (Zeile 236-266):
```javascript
const point_config = {
  gottesdienst_enabled: konfi.gottesdienst_enabled !== false,
  gemeinde_enabled: konfi.gemeinde_enabled !== false,
  target_gottesdienst: konfi.target_gottesdienst || 10,
  target_gemeinde: konfi.target_gemeinde || 10
};
// Wird in Response als point_config mitgeliefert
```

From frontend/src/components/admin/views/ActivityRings.tsx:
```typescript
interface ActivityRingsProps {
  totalPoints: number;
  gottesdienstPoints: number;
  gemeindePoints: number;
  gottesdienstGoal: number;
  gemeindeGoal: number;
  size?: number;
}
```

From frontend/src/components/konfi/views/DashboardView.tsx:
```typescript
interface DashboardViewProps {
  dashboardData: DashboardData;
  dailyVerse: DailyVerse | null;
  badgeStats: BadgeStats;
  allBadges: AllBadgesData;
  upcomingEvents: DashboardEvent[];
  targetGottesdienst: number;
  targetGemeinde: number;
  onOpenPointsHistory?: () => void;
}
```

From frontend/src/components/konfi/modals/PointsHistoryModal.tsx:
```typescript
interface PointsHistoryModalProps {
  onClose: () => void;
}
```

From frontend/src/components/konfi/pages/KonfiDashboardPage.tsx:
```typescript
// Aktuell: laedt settings separat via api.get('/settings')
// settings.target_gottesdienst / settings.target_gemeinde
// MUSS auf dashboardResponse.data.point_config umgestellt werden
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityRings dynamische Ring-Anzahl + KonfiDashboardPage point_config</name>
  <files>frontend/src/components/admin/views/ActivityRings.tsx, frontend/src/components/konfi/pages/KonfiDashboardPage.tsx</files>
  <action>
**ActivityRings.tsx:**
1. Props-Interface erweitern um `gottesdienstEnabled?: boolean` und `gemeindeEnabled?: boolean` (Default: true via `!== false` Check, damit bestehende Aufrufe ohne diese Props weiterhin 3 Ringe zeigen)
2. Aktive Typen bestimmen: `activeTypes` Array basierend auf enabled-Flags
3. `showTotal = activeTypes.length === 2` -- Total-Ring nur bei 2 aktiven Typen
4. Ring-Rendering dynamisch:
   - Bei 2 aktiven Typen: 3 Ringe wie bisher (Total aussen, Godi mitte, Gemeinde innen)
   - Bei 1 aktivem Typ: 1 Ring auf aeusserem Radius (ringRadii[0]), kein Total
   - Bei 0 aktiven: Fallback-Text "Keine Punkte-Typen aktiv"
5. Zielwerte-Berechnung anpassen: effectiveTotalGoal nur wenn showTotal, sonst Goal des aktiven Typs
6. Zentrale Anzeige im Ring: Bei 1 Typ die Punkte des aktiven Typs, bei 2 Typen Gesamtpunkte (wie bisher)
7. Legende/Stats unter den Ringen: Nur aktive Typen anzeigen, kein Gesamt-Chip bei 1 Typ
8. size bleibt konstant (kein Layout-Sprung per User-Entscheidung)
9. Animation: animatedValues nur fuer aktive Typen berechnen

**KonfiDashboardPage.tsx:**
1. `Settings`-Interface und `settings`-State entfernen
2. `api.get('/settings')` Aufruf aus `loadDashboardData` entfernen -- nur noch `api.get('/konfi/dashboard')`
3. `DashboardData`-Interface erweitern um `point_config: { gottesdienst_enabled: boolean, gemeinde_enabled: boolean, target_gottesdienst: number, target_gemeinde: number }`
4. Targets aus `dashboardData.point_config` statt aus `settings` lesen:
   - `targetGottesdienst = dashboardData.point_config.target_gottesdienst`
   - `targetGemeinde = dashboardData.point_config.target_gemeinde`
5. Neue Props an DashboardView durchreichen: `gottesdienstEnabled` und `gemeindeEnabled` aus `dashboardData.point_config`
6. `pointConfig` an PointsHistoryModal im useIonModal-Aufruf durchreichen:
   ```typescript
   const [presentPointsHistoryModal, dismissPointsHistoryModal] = useIonModal(PointsHistoryModal, {
     onClose: () => dismissPointsHistoryModal(),
     pointConfig: dashboardData?.point_config
   });
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ActivityRings akzeptiert enabled-Props und rendert dynamisch 1 oder 3 Ringe. KonfiDashboardPage nutzt point_config statt /settings. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardView bedingte Anzeige + PointsHistoryModal Filter</name>
  <files>frontend/src/components/konfi/views/DashboardView.tsx, frontend/src/components/konfi/modals/PointsHistoryModal.tsx</files>
  <action>
**DashboardView.tsx:**
1. DashboardViewProps erweitern um `gottesdienstEnabled?: boolean` und `gemeindeEnabled?: boolean`
2. Props destrukturieren mit Defaults: `gottesdienstEnabled = true, gemeindeEnabled = true`
3. `gottesdienstEnabled` und `gemeindeEnabled` an ActivityRings durchreichen
4. Stats-Chips (Glass-Chips) unter den Ringen: Nur aktive Typen zeigen
   - Godi-Chip nur wenn `gottesdienstEnabled`
   - Gemeinde-Chip nur wenn `gemeindeEnabled`
   - Gesamt-Chip nur wenn beide aktiv (kein redundanter Chip bei 1 Typ)
5. `totalCurrentPoints` Berechnung anpassen: nur aktive Typen summieren (oder `dashboardData.total_points` nutzen, das vom Backend schon korrekt ist)
6. `totalTarget` nur berechnen wenn beide Typen aktiv
7. Ranking-Sektion: `total_points` wird bereits korrekt vom Backend geliefert -- keine Aenderung noetig (Label bleibt generisch "Punkte" per User-Entscheidung)

**PointsHistoryModal.tsx:**
1. Props-Interface erweitern: `pointConfig?: { gottesdienst_enabled: boolean, gemeinde_enabled: boolean }`
2. Historie-Eintraege filtern: `useMemo` das history-Array nach aktiven Typen filtert
   - Wenn `entry.category === 'gottesdienst'` und `!pointConfig.gottesdienst_enabled` -> ausfiltern
   - Wenn `entry.category === 'gemeinde'` und `!pointConfig.gemeinde_enabled` -> ausfiltern
   - Bonus-Punkte und andere Kategorien immer anzeigen
   - Fallback: wenn pointConfig undefined, alles anzeigen (Abwaertskompatibilitaet)
3. Zusammenfassungs-Totals ebenfalls anpassen: nur aktive Typen in Header-Stats zeigen
4. Wenn nur 1 Typ aktiv: Typ-Chips/Filter-Optionen fuer den deaktivierten Typ ausblenden
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>DashboardView zeigt nur aktive Punkte-Typen in Chips und Stats. PointsHistoryModal filtert deaktivierte Typen aus. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- keine TypeScript-Fehler
2. `cd frontend && npm run build` -- Build erfolgreich
3. Manuell: Dashboard oeffnen -- ActivityRings, Stats-Chips und Ranking korrekt
</verification>

<success_criteria>
- ActivityRings rendert dynamisch 1 oder 3 Ringe basierend auf gottesdienstEnabled/gemeindeEnabled
- Bei 1 aktivem Typ: kein Total-Ring, kein Gesamt-Chip, 1 Ring auf aeusserem Radius
- KonfiDashboardPage nutzt point_config aus Dashboard-Response (kein /settings Aufruf mehr)
- DashboardView-Chips zeigen nur aktive Typen
- PointsHistoryModal filtert Eintraege deaktivierter Typen aus
- TypeScript und Build fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/32-punkte-ui-frontend/32-01-SUMMARY.md`
</output>
