---
phase: 32-punkte-ui-frontend
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - backend/routes/konfi-managment.js
  - frontend/src/components/admin/KonfisView.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements:
  - PUI-02
  - PUI-05

must_haves:
  truths:
    - "KonfisView Progress-Bars blenden deaktivierte Punkte-Typen komplett aus"
    - "KonfisView nutzt pro-Jahrgang Targets statt globaler Settings"
    - "KonfiDetailView zeigt deaktivierte Typen ausgegraut mit (deaktiviert) Label"
    - "Admin sieht historische Punktedaten auch fuer deaktivierte Typen"
    - "Backend Konfi-Detail liefert Jahrgang-Config-Spalten mit"
  artifacts:
    - path: "backend/routes/konfi-managment.js"
      provides: "Jahrgang-Config in Konfi-Detail-Response"
      contains: "gottesdienst_enabled"
    - path: "frontend/src/components/admin/KonfisView.tsx"
      provides: "Bedingte Progress-Bars pro Jahrgang-Config"
      contains: "gottesdienst_enabled"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "Admin-ausgegraut-Pattern fuer deaktivierte Typen"
      contains: "deaktiviert"
  key_links:
    - from: "backend/routes/konfi-managment.js"
      to: "KonfiDetailView"
      via: "j.gottesdienst_enabled, j.gemeinde_enabled im SQL JOIN"
      pattern: "j\\.gottesdienst_enabled"
    - from: "frontend/src/components/admin/KonfisView.tsx"
      to: "jahrgaenge Array"
      via: "Pro-Konfi Jahrgang matchen fuer Config"
      pattern: "jahrgang_name"
    - from: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      to: "ActivityRings"
      via: "gottesdienstEnabled/gemeindeEnabled Props aus Konfi-Detail-Response"
      pattern: "gottesdienstEnabled"
---

<objective>
Admin-Views (KonfisView und KonfiDetailView) reagieren auf deaktivierte Punkte-Typen. KonfisView blendet Progress-Bars aus, KonfiDetailView zeigt deaktivierte Typen ausgegraut. Backend liefert Jahrgang-Config in Konfi-Detail-Response.

Purpose: Admins sehen vollstaendigen Ueberblick mit visueller Unterscheidung zwischen aktiven und deaktivierten Typen.
Output: 1 Backend-Route + 2 Frontend-Dateien angepasst.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-punkte-ui-frontend/32-CONTEXT.md
@.planning/phases/32-punkte-ui-frontend/32-RESEARCH.md
@.planning/phases/32-punkte-ui-frontend/32-01-SUMMARY.md

<interfaces>
<!-- ActivityRings nach Plan 01 hat neue Props -->
From frontend/src/components/admin/views/ActivityRings.tsx (nach Plan 01):
```typescript
interface ActivityRingsProps {
  totalPoints: number;
  gottesdienstPoints: number;
  gemeindePoints: number;
  gottesdienstGoal: number;
  gemeindeGoal: number;
  size?: number;
  gottesdienstEnabled?: boolean;  // NEU aus Plan 01
  gemeindeEnabled?: boolean;      // NEU aus Plan 01
}
```

From backend/routes/konfi-managment.js (Zeile 91-104, aktuell OHNE Config-Spalten):
```javascript
// GET /:id -- Konfi-Detail
const konfiQuery = `
  SELECT u.id, u.display_name as name, u.username,
         kp.gottesdienst_points, kp.gemeinde_points,
         j.name as jahrgang_name, j.id as jahrgang_id,
         (SELECT COUNT(*) FROM konfi_badges WHERE konfi_id = u.id) as "badgeCount"
  FROM users u
  JOIN roles r ON u.role_id = r.id
  LEFT JOIN konfi_profiles kp ON u.id = kp.user_id
  LEFT JOIN jahrgaenge j ON kp.jahrgang_id = j.id
  WHERE r.name = 'konfi' AND u.id = $1 AND u.organization_id = $2
`;
```

From backend/routes/konfi-managment.js (Zeile 67-80, Konfis-Liste OHNE Config-Spalten):
```javascript
// GET / -- Konfis-Liste
const query = `
  SELECT u.id, u.display_name as name, u.username,
         kp.gottesdienst_points, kp.gemeinde_points,
         j.name as jahrgang_name, j.id as jahrgang_id,
         (SELECT COUNT(*) FROM konfi_badges WHERE konfi_id = u.id) as "badgeCount"
  FROM users u
  JOIN roles r ON u.role_id = r.id
  LEFT JOIN konfi_profiles kp ON u.id = kp.user_id
  LEFT JOIN jahrgaenge j ON kp.jahrgang_id = j.id
  WHERE r.name = 'konfi' AND u.organization_id = $1
  ORDER BY j.name DESC, u.display_name
`;
```

From frontend/src/components/admin/KonfisView.tsx:
```typescript
// Erhaelt jahrgaenge als Prop, nutzt aktuell globale settings fuer Targets
// Muss pro Konfi den Jahrgang matchen fuer Config
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Konfi-Detail + Liste mit Jahrgang-Config erweitern</name>
  <files>backend/routes/konfi-managment.js</files>
  <action>
1. GET /:id (Konfi-Detail, Zeile ~93) Query erweitern: `j.gottesdienst_enabled, j.gemeinde_enabled, j.target_gottesdienst, j.target_gemeinde` zum SELECT hinzufuegen. Der JOIN auf `jahrgaenge j` existiert bereits.
2. GET / (Konfis-Liste, Zeile ~69) Query erweitern: gleiche 4 Spalten hinzufuegen (`j.gottesdienst_enabled, j.gemeinde_enabled, j.target_gottesdienst, j.target_gemeinde`). So hat KonfisView die Config pro Konfi direkt dabei, ohne Jahrgang-Matching.
3. Keine weiteren Aenderungen -- die Response-Struktur bleibt identisch, nur 4 Felder mehr pro Konfi-Objekt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "require('./routes/konfi-managment.js')" 2>&1 | head -5 || echo "Syntax check: Module loads require db param, expected error is OK"</automated>
  </verify>
  <done>Beide Konfi-Queries (Liste + Detail) liefern gottesdienst_enabled, gemeinde_enabled, target_gottesdienst, target_gemeinde aus der jahrgaenge-Tabelle mit.</done>
</task>

<task type="auto">
  <name>Task 2: KonfisView bedingte Progress-Bars + KonfiDetailView ausgegraut-Pattern</name>
  <files>frontend/src/components/admin/KonfisView.tsx, frontend/src/components/admin/views/KonfiDetailView.tsx</files>
  <action>
**KonfisView.tsx:**
1. Pro Konfi die Config direkt aus den Konfi-Daten nutzen (Backend liefert jetzt `gottesdienst_enabled`, `gemeinde_enabled`, `target_gottesdienst`, `target_gemeinde` pro Konfi-Zeile)
2. Progress-Bars bedingt rendern:
   - Godi-Bar nur wenn `konfi.gottesdienst_enabled !== false`
   - Gemeinde-Bar nur wenn `konfi.gemeinde_enabled !== false`
   - Gesamt-Bar nur wenn beide Typen aktiv
3. Bei 1 aktivem Typ: nur 1 Progress-Bar anzeigen, kein Gesamt
4. Targets pro Konfi aus den neuen Feldern statt aus globalen Settings:
   - `konfi.target_gottesdienst || 10` statt `settings.target_gottesdienst`
5. Falls KonfisView aktuell einen globalen Settings-Abruf hat: entfernen oder ignorieren fuer Punkte-Targets

**KonfiDetailView.tsx:**
1. `loadSettings()` Aufruf fuer Punkte-Targets entfernen/ersetzen -- stattdessen die Jahrgang-Config aus der Konfi-Detail-Response nutzen (`konfi.gottesdienst_enabled`, `konfi.gemeinde_enabled`, `konfi.target_gottesdienst`, `konfi.target_gemeinde`)
2. ActivityRings: `gottesdienstEnabled` und `gemeindeEnabled` aus Konfi-Daten als Props durchreichen, Targets ebenfalls aus Konfi-Daten
3. Punkte-Aufschluesselung (Stats-Bereich):
   - Admin sieht ALLE Typen (auch deaktivierte) -- aber deaktivierte ausgegraut
   - Style fuer deaktivierte: `opacity: 0.4, filter: grayscale(100%)`
   - Label "(deaktiviert)" als kleiner grauer Text neben dem Typ-Namen
4. Punkte-Historie im Admin:
   - Alle Eintraege sichtbar (auch deaktivierte Typen) -- Admin bekommt vollstaendigen Ueberblick
   - Deaktivierte Eintraege visuell ausgegraut mit dem gleichen Pattern
5. Bonus-Punkte-Vergabe UI: Kein Guard noetig (Backend prueft bereits in Phase 31)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>KonfisView zeigt nur aktive Typen in Progress-Bars mit pro-Jahrgang Targets. KonfiDetailView zeigt deaktivierte Typen ausgegraut mit "(deaktiviert)" Label. Admin sieht historische Daten auch fuer deaktivierte Typen. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- keine TypeScript-Fehler
2. `cd frontend && npm run build` -- Build erfolgreich
3. Backend: Route-Datei syntaktisch korrekt (node -e require-Check)
</verification>

<success_criteria>
- Backend Konfi-Detail und Konfis-Liste liefern Jahrgang-Config-Spalten
- KonfisView blendet deaktivierte Progress-Bars komplett aus
- KonfisView nutzt pro-Jahrgang Targets statt globaler Settings
- KonfiDetailView zeigt deaktivierte Typen ausgegraut mit "(deaktiviert)" Label
- Admin sieht historische Punktedaten auch fuer deaktivierte Typen
- ActivityRings in Admin-Detail nutzt enabled-Flags korrekt
- TypeScript und Build fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/32-punkte-ui-frontend/32-02-SUMMARY.md`
</output>
