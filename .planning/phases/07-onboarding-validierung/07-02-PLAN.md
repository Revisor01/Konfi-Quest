---
phase: 07-onboarding-validierung
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/components/auth/KonfiRegisterPage.tsx
  - frontend/src/components/auth/LoginView.tsx
  - frontend/src/theme/variables.css
autonomous: true
requirements:
  - ONB-01
  - ONB-02

must_haves:
  truths:
    - "Username-Feld zeigt Echtzeit-Verfuegbarkeit nach Blur (Debounced)"
    - "Registrierung fuehrt zu Auto-Login und Weiterleitung zum Dashboard statt Login-Seite"
    - "Differenzierte Fehlermeldungen werden fuer abgelaufene und nicht-existierende Codes angezeigt"
    - "Netzwerkfehler zeigen Retry-Button statt generischer Meldung"
    - "Auth-Seiten (Login + Register) nutzen CSS-Klassen statt Inline-Styles"
    - "Login-Seite hat sichtbaren Passwort-vergessen-Link"
  artifacts:
    - path: "frontend/src/components/auth/KonfiRegisterPage.tsx"
      provides: "Username-Check, Auto-Login, differenzierte Fehler, CSS-Klassen"
      contains: "check-username"
    - path: "frontend/src/components/auth/LoginView.tsx"
      provides: "CSS-Klassen, Passwort-vergessen-Link"
    - path: "frontend/src/theme/variables.css"
      provides: "app-auth-* CSS-Klassen fuer Auth-Seiten"
      contains: "app-auth"
  key_links:
    - from: "KonfiRegisterPage.tsx"
      to: "GET /api/auth/check-username/:username"
      via: "Debounced API-Call bei Blur"
      pattern: "check-username"
    - from: "KonfiRegisterPage.tsx"
      to: "POST /api/auth/register-konfi"
      via: "Token aus Response -> localStorage -> setUser -> redirect"
      pattern: "localStorage.*konfi_token"
---

<objective>
KonfiRegisterPage und LoginView ueberarbeiten: Username-Echtzeit-Check, Auto-Login nach Registrierung, differenzierte Fehlermeldungen fuer Invite-Codes, Netzwerkfehler mit Retry-Button, und CSS-Klassen statt Inline-Styles.

Purpose: Die beiden meistgenutzten Auth-Seiten (Login + Register) funktional und visuell verbessern.
Output: Polierte Login- und Register-Seiten mit allen Onboarding-Verbesserungen.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-onboarding-validierung/07-CONTEXT.md
@.planning/phases/07-onboarding-validierung/07-01-SUMMARY.md

@frontend/src/components/auth/KonfiRegisterPage.tsx
@frontend/src/components/auth/LoginView.tsx
@frontend/src/services/auth.ts
@frontend/src/services/api.ts
@frontend/src/theme/variables.css

<interfaces>
<!-- Backend-Endpoints aus Plan 01 die hier konsumiert werden -->

Neuer Endpoint (aus Plan 01):
```
GET /api/auth/check-username/:username
Response: { available: boolean, message: string }
```

Geaenderter register-konfi Response (aus Plan 01):
```json
{
  "message": "Registrierung erfolgreich",
  "token": "jwt-token-hier",
  "user": {
    "id": 123,
    "display_name": "Max Mustermann",
    "username": "max123",
    "type": "konfi",
    "jahrgang": "Konfis 2025/26",
    "role_name": "konfi"
  }
}
```

Differenzierte validate-invite Fehler (aus Plan 01):
```
404: { error: "Dieser Einladungscode existiert nicht", error_code: "not_found" }
410: { error: "Dieser Einladungscode ist abgelaufen...", error_code: "expired" }
```

Bestehende Auth-Service-Funktion:
```typescript
// frontend/src/services/auth.ts
export const loginWithAutoDetection = async (username: string, password: string): Promise<User>
// Speichert token in localStorage unter 'konfi_token' und user unter 'konfi_user'
```

Bestehende AppContext-Hooks:
```typescript
const { setUser, setSuccess, setError } = useApp();
// setUser(user) setzt den eingeloggten User im Context
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth-CSS-Klassen in variables.css erstellen</name>
  <files>frontend/src/theme/variables.css</files>
  <action>
Am Ende von variables.css (vor dem letzten Kommentar-Block oder nach den bestehenden app-* Klassen) folgende CSS-Klassen hinzufuegen:

```css
/* ===== Auth Pages (Login, Register, ForgotPassword, ResetPassword) ===== */

.app-auth-background {
  --background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.app-auth-container {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  padding: 20px;
}

.app-auth-hero {
  text-align: center;
  margin-bottom: 40px;
  color: white;
}

.app-auth-hero__icons {
  font-size: 4rem;
  margin-bottom: 16px;
  display: flex;
  gap: 8px;
  justify-content: center;
}

.app-auth-hero__icons--small {
  font-size: 3.5rem;
  margin-bottom: 12px;
}

.app-auth-hero__title {
  font-size: 3.5rem;
  font-family: 'Bebas Neue', sans-serif;
  font-weight: 400;
  margin: 0 0 8px 0;
  text-shadow: 0 2px 4px rgba(0,0,0,0.3);
  letter-spacing: 2px;
}

.app-auth-hero__title--small {
  font-size: 3rem;
}

.app-auth-hero__subtitle {
  font-size: 1.1rem;
  opacity: 0.9;
  margin: 0;
  text-shadow: 0 1px 2px rgba(0,0,0,0.3);
}

.app-auth-hero__subtitle--small {
  font-size: 1rem;
}

.app-auth-hero__circle {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 20px auto;
}

.app-auth-hero__circle-icon {
  font-size: 2.5rem;
  color: white;
}

.app-auth-card {
  width: 100%;
  max-width: 420px;
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(0,0,0,0.1);
  overflow: hidden;
}

.app-auth-card--error {
  box-shadow: 0 20px 40px rgba(220,53,69,0.2);
  border: 2px solid rgba(220,53,69,0.3);
}

.app-auth-card--narrow {
  max-width: 400px;
}

.app-auth-card__content {
  padding: 32px;
}

.app-auth-card__content--compact {
  padding: 24px;
}

.app-auth-card__heading {
  text-align: center;
  margin-bottom: 32px;
}

.app-auth-card__heading h2 {
  font-size: 1.5rem;
  font-weight: 600;
  margin: 0;
  color: #2c3e50;
}

.app-auth-card__heading p {
  color: #7f8c8d;
  margin: 8px 0 0 0;
  font-size: 0.9rem;
}

.app-auth-input {
  --background: #f8f9fa;
  --border-radius: 12px;
  margin-bottom: 16px;
}

.app-auth-input--compact {
  margin-bottom: 12px;
}

.app-auth-input__label {
  color: #667eea;
  font-weight: 500;
}

.app-auth-input__value {
  --color: #2c3e50;
}

.app-auth-input__toggle {
  cursor: pointer;
  font-size: 1.2rem;
  color: #667eea;
  margin-top: 20px;
}

.app-auth-button {
  --background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --color: white;
  --border-radius: 12px;
  height: 48px;
  font-size: 1rem;
  font-weight: 600;
  margin: 0;
}

.app-auth-error {
  margin-bottom: 16px;
  padding: 12px 14px;
  border-radius: 12px;
  background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
  border: 1px solid #f87171;
  display: flex;
  align-items: center;
  gap: 10px;
}

.app-auth-error__icon {
  font-size: 1.2rem;
  color: #dc2626;
  flex-shrink: 0;
}

.app-auth-error__text {
  font-size: 0.85rem;
  color: #991b1b;
  flex: 1;
}

.app-auth-error__close {
  font-size: 1.1rem;
  color: #b91c1c;
  cursor: pointer;
}

.app-auth-error--with-badge {
  margin-top: 16px;
}

.app-auth-error__badge {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  box-shadow: 0 2px 8px rgba(220, 38, 38, 0.3);
}

.app-auth-error__badge-icon {
  font-size: 1.3rem;
  color: white;
}

.app-auth-error__title {
  font-size: 0.85rem;
  font-weight: 600;
  color: #991b1b;
  margin-bottom: 2px;
}

.app-auth-error__detail {
  font-size: 0.8rem;
  color: #b91c1c;
}

.app-auth-link {
  color: #667eea;
  cursor: pointer;
  font-size: 0.9rem;
}

.app-auth-link--muted {
  color: #7f8c8d;
  font-size: 0.8rem;
}

.app-auth-link--strong {
  font-weight: 500;
}

.app-auth-footer {
  text-align: center;
  margin-top: 16px;
}

.app-auth-footer--separator {
  padding-top: 16px;
  border-top: 1px solid #e5e7eb;
}

.app-auth-invite-info {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 20px;
  color: white;
  text-align: center;
}

.app-auth-invite-info__icon {
  font-size: 2rem;
  margin-bottom: 8px;
}

.app-auth-invite-info__title {
  font-size: 1.1rem;
  font-weight: 600;
}

.app-auth-invite-info__subtitle {
  font-size: 0.85rem;
  opacity: 0.9;
}

.app-auth-password-checks {
  background: #f8f9fa;
  border-radius: 10px;
  padding: 12px;
  margin-bottom: 12px;
  font-size: 0.8rem;
}

.app-auth-password-checks__grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 6px;
}

.app-auth-password-match-error {
  margin-bottom: 12px;
  padding: 10px 12px;
  border-radius: 10px;
  background: #fee2e2;
  border: 1px solid #f87171;
  font-size: 0.85rem;
  color: #991b1b;
  display: flex;
  align-items: center;
  gap: 8px;
}

.app-auth-username-status {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 10px;
  border-radius: 8px;
  font-size: 0.8rem;
  margin-bottom: 12px;
}

.app-auth-username-status--available {
  background: #d1fae5;
  color: #065f46;
}

.app-auth-username-status--taken {
  background: #fee2e2;
  color: #991b1b;
}

.app-auth-username-status--checking {
  background: #f0f9ff;
  color: #0369a1;
}

.app-auth-retry-button {
  --background: transparent;
  --color: #667eea;
  --border-color: #667eea;
  --border-radius: 8px;
  --border-width: 1px;
  --border-style: solid;
  height: 36px;
  font-size: 0.85rem;
  margin-top: 8px;
}

.app-auth-success-circle {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  background: linear-gradient(135deg, #34c759 0%, #30d158 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 24px auto;
  box-shadow: 0 8px 24px rgba(52, 199, 89, 0.4);
}

.app-auth-success-circle--small {
  width: 64px;
  height: 64px;
  background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  margin-bottom: 20px;
  box-shadow: 0 8px 24px rgba(16, 185, 129, 0.3);
}

.app-auth-success-circle__icon {
  font-size: 3.5rem;
  color: white;
}

.app-auth-success-circle__icon--small {
  font-size: 2rem;
}

/* Shake Animation fuer Auth-Seiten */
@keyframes app-auth-shake {
  0%, 100% { transform: translateX(0); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
  20%, 40%, 60%, 80% { transform: translateX(5px); }
}

.app-auth-card--shaking {
  animation: app-auth-shake 0.6s ease-in-out;
}
```

Die @keyframes shake in den einzelnen Komponenten (via style-Tag) werden dadurch ersetzt.
  </action>
  <verify>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "app-auth" src/theme/variables.css</verify>
  <done>Mindestens 30 app-auth-* CSS-Klassen in variables.css definiert, inklusive Shake-Animation</done>
</task>

<task type="auto">
  <name>Task 2: KonfiRegisterPage komplett ueberarbeiten</name>
  <files>frontend/src/components/auth/KonfiRegisterPage.tsx</files>
  <action>
KonfiRegisterPage.tsx komplett ueberarbeiten mit folgenden Aenderungen:

1. **CSS-Klassen statt Inline-Styles:**
   - Alle `style={{...}}` durch die neuen app-auth-* CSS-Klassen ersetzen
   - IonContent: className="app-auth-background" statt style
   - Container-div: className="app-auth-container"
   - Hero-Section: className="app-auth-hero", app-auth-hero__icons--small, app-auth-hero__title--small
   - Card: className={`app-auth-card ${error ? 'app-auth-card--error' : ''} ${shakeError ? 'app-auth-card--shaking' : ''}`}
   - CardContent: className="app-auth-card__content--compact"
   - Input Items: className="app-auth-input app-auth-input--compact" mit lines="none"
   - Labels: className="app-auth-input__label"
   - Button: className="app-auth-button"
   - Error-Box: className="app-auth-error"
   - Die `<style>` Tags mit @keyframes shake am Ende der Komponente ENTFERNEN (Shake ist jetzt in variables.css)

2. **Username-Echtzeit-Verfuegbarkeitspruefung:**
   - Neuen State: `const [usernameStatus, setUsernameStatus] = useState<'idle' | 'checking' | 'available' | 'taken'>('idle');`
   - Debounce-Timer-Ref: `const usernameCheckTimer = useRef<ReturnType<typeof setTimeout> | null>(null);`
   - Beim onIonInput des Username-Felds: Timer clearen und neuen setzen (300ms Debounce)
   - Beim onIonBlur des Username-Felds: Sofort pruefen falls noch nicht geprueft
   - Check-Funktion:
     ```typescript
     const checkUsername = async (name: string) => {
       if (name.length < 3) { setUsernameStatus('idle'); return; }
       setUsernameStatus('checking');
       try {
         const res = await api.get(`/auth/check-username/${encodeURIComponent(name.toLowerCase().trim())}`);
         setUsernameStatus(res.data.available ? 'available' : 'taken');
       } catch { setUsernameStatus('idle'); }
     };
     ```
   - Unter dem Username-Input: Status-Anzeige mit app-auth-username-status Klassen
     - 'checking': IonSpinner + "Wird geprueft..." (app-auth-username-status--checking)
     - 'available': Checkmark-Icon + "Benutzername verfuegbar" (app-auth-username-status--available)
     - 'taken': Alert-Icon + "Benutzername bereits vergeben" (app-auth-username-status--taken)
   - Submit-Button deaktivieren wenn usernameStatus === 'taken'

3. **Differenzierte Fehlermeldungen fuer Invite-Codes:**
   - In validateCode catch-Block: error_code auswerten:
     ```typescript
     const errorCode = err.response?.data?.error_code;
     if (errorCode === 'not_found') {
       setError('Dieser Einladungscode existiert nicht. Bitte pruefe deine Eingabe.');
     } else if (errorCode === 'expired') {
       setError('Dieser Einladungscode ist abgelaufen. Bitte frage deinen Konfi-Leiter nach einem neuen Code.');
     } else {
       setError(err.response?.data?.error || 'Fehler bei der Code-Validierung');
     }
     ```

4. **Netzwerkfehler mit Retry-Button:**
   - Neuen State: `const [isNetworkError, setIsNetworkError] = useState(false);`
   - In validateCode catch: Netzwerkfehler erkennen (`!err.response` oder `err.code === 'ERR_NETWORK'`)
   - Bei Netzwerkfehler: setIsNetworkError(true), Error-Text: "Verbindung fehlgeschlagen. Bitte pruefe deine Internetverbindung."
   - IonButton mit className="app-auth-retry-button" und onClick: `() => validateCode(inviteCode)`, Text: "Erneut versuchen" mit refreshOutline Icon
   - Gleiches Pattern in handleSubmit fuer Registrierungs-Netzwerkfehler

5. **Auto-Login nach Registrierung:**
   - useApp() Hook importieren: `const { setUser, setSuccess } = useApp();`
   - In handleSubmit nach erfolgreicher api.post:
     ```typescript
     const { token, user } = response.data;
     if (token && user) {
       localStorage.setItem('konfi_token', token);
       localStorage.setItem('konfi_user', JSON.stringify(user));
       setUser(user);
       setSuccess('Willkommen bei Konfi Quest!');
       // Kurz warten fuer visuelles Feedback, dann zum Dashboard
       setTimeout(() => {
         history.replace('/konfi/dashboard');
       }, 1500);
     }
     ```
   - Success-Screen anpassen: "Du wirst gleich zur Anmeldung weitergeleitet..." -> "Du wirst zum Dashboard weitergeleitet..."
   - Timeout aendern: 3000ms -> 1500ms
   - Den `setSuccess(true)` State beibehalten fuer den Success-Screen

6. **Imports bereinigen:**
   - `refreshOutline` hinzufuegen (fuer Retry-Button)
   - `useRef` hinzufuegen
   - `useApp` importieren
   - Ungenutzte Imports entfernen falls vorhanden
  </action>
  <verify>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "KonfiRegisterPage" | head -10</verify>
  <done>KonfiRegisterPage nutzt CSS-Klassen, hat Username-Live-Check, differenzierte Code-Fehler, Netzwerk-Retry und Auto-Login</done>
</task>

<task type="auto">
  <name>Task 3: LoginView CSS-Migration und Verbesserungen</name>
  <files>frontend/src/components/auth/LoginView.tsx</files>
  <action>
LoginView.tsx ueberarbeiten:

1. **CSS-Klassen statt Inline-Styles:**
   - IonContent: className="app-auth-background"
   - Container: className="app-auth-container"
   - Hero: className="app-auth-hero" mit Unter-Elementen
   - Card: className={`app-auth-card app-auth-card--narrow ${loginError ? 'app-auth-card--error' : ''} ${shakeError ? 'app-auth-card--shaking' : ''}`}
   - CardContent: className="app-auth-card__content"
   - Heading-Block: className="app-auth-card__heading"
   - Input Items: className="app-auth-input" mit lines="none"
   - Labels: className="app-auth-input__label"
   - Password Toggle: className="app-auth-input__toggle"
   - Login-Button: className="app-auth-button"
   - Fehler-Box: Nutzt app-auth-error--with-badge mit app-auth-error__badge Pattern
   - Passwort-vergessen-Link: className="app-auth-link--muted" im app-auth-footer
   - Register-Link: className="app-auth-link" im app-auth-footer--separator
   - Die `<style>` Tags am Ende ENTFERNEN

2. **Netzwerkfehler mit Retry-Button:**
   - Wie bei KonfiRegisterPage: Bei Netzwerkfehler einen Retry-Button zeigen
   - In handleLogin: Netzwerkfehler erkennen und `isNetworkError` State setzen
   - Error-Box bekommt optional einen IonButton "Erneut versuchen"

3. **Sicherstellen dass Passwort-vergessen-Link prominent ist:**
   - Der Link existiert bereits, aber sicherstellen dass er nach der Fehlermeldung immer sichtbar bleibt
   - Position: Direkt unter dem Login-Button (vor der Trennlinie), NICHT unter der Fehlermeldung
   - Struktur: Button -> Passwort-vergessen-Link -> Fehlermeldung (falls vorhanden) -> Trennlinie -> Register-Link
   - So ist der Link immer an der gleichen Stelle, egal ob Fehler angezeigt wird oder nicht

4. **Imports aufraeuemen:**
   - Ungenutzte Imports entfernen (IonCardHeader, IonCardTitle, IonImg falls unbenutzt)
  </action>
  <verify>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "LoginView" | head -10</verify>
  <done>LoginView nutzt app-auth-* CSS-Klassen, hat Netzwerk-Retry, Passwort-vergessen-Link immer an fester Position</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` laeuft ohne Fehler in KonfiRegisterPage.tsx und LoginView.tsx
- KonfiRegisterPage hat keinen einzigen `<style>` Tag mehr (Shake-Animation kommt aus CSS)
- LoginView hat keinen einzigen `<style>` Tag mehr
- grep nach "app-auth" in beiden Dateien findet mindestens 10 Treffer pro Datei
- grep nach "style={{" in beiden Dateien findet maximal 5 verbleibende dynamische Styles (z.B. transition)
</verification>

<success_criteria>
Login und Register nutzen CSS-Klassen statt Inline-Styles. Username-Check funktioniert mit Debounce. Auto-Login leitet zum Dashboard weiter. Differenzierte Fehlermeldungen fuer Codes werden angezeigt. Netzwerkfehler haben Retry-Button.
</success_criteria>

<output>
After completion, create `.planning/phases/07-onboarding-validierung/07-02-SUMMARY.md`
</output>
