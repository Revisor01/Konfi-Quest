---
phase: 07-onboarding-validierung
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/components/auth/ForgotPasswordPage.tsx
  - frontend/src/components/auth/ResetPasswordPage.tsx
  - frontend/src/components/admin/pages/AdminInvitePage.tsx
  - backend/services/emailService.js
autonomous: true
requirements:
  - ONB-01
  - ONB-02

must_haves:
  truths:
    - "ForgotPasswordPage zeigt ehrlichen Fehler bei E-Mail-Versand-Problemen statt immer Erfolg"
    - "ForgotPasswordPage zeigt Hinweis fuer Konfis ohne E-Mail: Frag deinen Konfi-Leiter"
    - "ResetPasswordPage nutzt CSS-Klassen statt Inline-Styles"
    - "AdminInvitePage zeigt keine abgelaufenen Codes und erlaubt keine Verlaengerung abgelaufener Codes"
    - "AdminInvitePage zeigt used_count pro Code an"
  artifacts:
    - path: "frontend/src/components/auth/ForgotPasswordPage.tsx"
      provides: "Ehrliche Fehlerbehandlung, Konfi-Hinweis, CSS-Klassen"
    - path: "frontend/src/components/auth/ResetPasswordPage.tsx"
      provides: "CSS-Klassen statt Inline-Styles"
    - path: "frontend/src/components/admin/pages/AdminInvitePage.tsx"
      provides: "Nur aktive Codes, used_count Anzeige"
  key_links:
    - from: "ForgotPasswordPage.tsx"
      to: "POST /api/auth/request-password-reset"
      via: "Ehrliche Fehlerbehandlung im catch-Block"
      pattern: "catch.*err.*setSent"
    - from: "AdminInvitePage.tsx"
      to: "GET /api/auth/invite-codes"
      via: "Backend filtert bereits abgelaufene Codes"
      pattern: "existingInvites"
---

<objective>
ForgotPasswordPage Bug-Fix und Konfi-Hinweis, ResetPasswordPage CSS-Migration, AdminInvitePage Verbesserungen: Die restlichen Auth-Seiten und die Admin-Einladungsverwaltung polieren.

Purpose: Alle Randfaelle im Onboarding-Flow abdecken: Passwort-Recovery, Admin-Code-Management.
Output: Alle Auth-Seiten und AdminInvitePage entsprechen den Onboarding-Anforderungen.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-onboarding-validierung/07-CONTEXT.md
@.planning/phases/07-onboarding-validierung/07-01-SUMMARY.md

@frontend/src/components/auth/ForgotPasswordPage.tsx
@frontend/src/components/auth/ResetPasswordPage.tsx
@frontend/src/components/admin/pages/AdminInvitePage.tsx
@frontend/src/theme/variables.css

<interfaces>
<!-- CSS-Klassen aus Plan 02 Task 1 (variables.css) -->

Verfuegbare CSS-Klassen fuer Auth-Seiten:
```css
.app-auth-background          /* IonContent Gradient */
.app-auth-container            /* Flex-Container */
.app-auth-hero                 /* Hero-Section */
.app-auth-hero__circle         /* 80px Kreis fuer Icon */
.app-auth-hero__circle-icon    /* Icon im Kreis */
.app-auth-card                 /* Karten-Container */
.app-auth-card--narrow         /* max-width: 400px */
.app-auth-card__content        /* padding: 32px */
.app-auth-input                /* Input-Item Styling */
.app-auth-input__label         /* Label Farbe */
.app-auth-input__toggle        /* Passwort-Toggle Icon */
.app-auth-button               /* Gradient-Button */
.app-auth-error                /* Fehler-Box */
.app-auth-error__icon          /* Fehler-Icon */
.app-auth-error__text          /* Fehler-Text */
.app-auth-link                 /* Link-Styling */
.app-auth-footer               /* Footer-Bereich */
.app-auth-success-circle       /* Erfolgs-Kreis (gross) */
.app-auth-success-circle--small /* Erfolgs-Kreis (klein) */
.app-auth-password-checks      /* Passwort-Anforderungen Box */
.app-auth-password-checks__grid /* 2-Spalten Grid */
.app-auth-password-match-error /* Passwort-Mismatch Fehler */
```

Backend request-password-reset (aus Plan 01):
- Gibt bei E-Mail-Versand-Fehler jetzt 500 mit { error: "..." } zurueck
- Gibt bei nicht-gefundenem User weiterhin 200 zurueck (Privacy)

AdminInvitePage bestehende Design-Klassen (bereits vorhanden):
```
app-segment-wrapper, app-card, app-section-icon, app-section-icon--jahrgang,
app-list-inner, app-item-transparent, app-list-item, app-corner-badge,
app-icon-circle, app-list-item__row, app-list-item__main, app-list-item__content,
app-list-item__title, app-list-item__meta, app-list-item__meta-item,
app-button-row, app-info-box, app-info-box--blue, app-settings-item__subtitle
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ForgotPasswordPage Bug-Fix und Konfi-Hinweis + CSS-Migration</name>
  <files>frontend/src/components/auth/ForgotPasswordPage.tsx</files>
  <action>
ForgotPasswordPage.tsx ueberarbeiten:

1. **KRITISCHER BUG-FIX: catch-Block setzt nicht mehr setSent(true):**
   - Aktueller Code (Zeile 43-45):
     ```typescript
     } catch (err: any) {
       // Auch bei Fehlern zeigen wir Erfolg an (Sicherheit)
       setSent(true);
     }
     ```
   - Neuer Code:
     ```typescript
     } catch (err: any) {
       const status = err.response?.status;
       if (status === 500) {
         // E-Mail-Versand fehlgeschlagen - ehrlich kommunizieren
         setError('E-Mail konnte nicht gesendet werden. Bitte versuche es spaeter erneut.');
       } else if (!err.response) {
         // Netzwerkfehler
         setError('Verbindung fehlgeschlagen. Bitte pruefe deine Internetverbindung.');
       } else {
         // Fuer alle anderen Faelle (inkl. 200 bei unbekannter E-Mail)
         // zeigen wir weiterhin "gesendet" an (Privacy)
         setSent(true);
       }
     }
     ```

2. **Konfi-Hinweis auf der Seite:**
   - Unter dem E-Mail-Eingabefeld (vor dem Submit-Button) einen Info-Hinweis hinzufuegen:
     ```html
     <div className="app-auth-konfi-hint">
       <IonIcon icon={informationCircleOutline} />
       <span>Keine E-Mail-Adresse hinterlegt? Frag deinen Konfi-Leiter -- er kann dein Passwort direkt zuruecksetzen.</span>
     </div>
     ```
   - CSS-Klasse (entweder inline oder als neuer Block in diesem File -- da es nur 1x vorkommt, inline ist ok):
     ```css
     background: #f0f9ff; border: 1px solid #bae6fd; border-radius: 10px;
     padding: 10px 12px; font-size: 0.8rem; color: #0369a1;
     display: flex; align-items: flex-start; gap: 8px; margin-bottom: 16px;
     ```
   - Import: `informationCircleOutline` aus ionicons/icons

3. **CSS-Klassen statt Inline-Styles:**
   - IonContent: className="app-auth-background"
   - Container: className="app-auth-container"
   - Hero: className="app-auth-hero" mit app-auth-hero__circle und app-auth-hero__circle-icon
   - Titel/Untertitel: die h1/p Styles durch className Varianten ersetzen (font-size/margin bleiben als minimale inline-Styles wenn CSS-Klasse nicht exakt passt)
   - Card: className="app-auth-card app-auth-card--narrow"
   - CardContent: className="app-auth-card__content"
   - Input Items: className="app-auth-input" lines="none"
   - Error-Box: className="app-auth-error"
   - Button: className="app-auth-button" (style-Prop nur fuer marginBottom)
   - Erfolgs-Screen: app-auth-success-circle--small und CSS-Klassen

4. **Netzwerkfehler-Retry:**
   - Neuer State: `isNetworkError`
   - Bei Netzwerkfehler: Retry-Button anzeigen mit `onClick={() => handleSubmit()}`
  </action>
  <verify>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "ForgotPassword" | head -10</verify>
  <done>ForgotPasswordPage zeigt ehrliche Fehler, hat Konfi-Leiter-Hinweis, nutzt CSS-Klassen, catch-Block setzt nicht mehr blind setSent(true)</done>
</task>

<task type="auto">
  <name>Task 2: ResetPasswordPage CSS-Migration</name>
  <files>frontend/src/components/auth/ResetPasswordPage.tsx</files>
  <action>
ResetPasswordPage.tsx auf CSS-Klassen umstellen:

1. **CSS-Klassen statt Inline-Styles:**
   - IonContent: className="app-auth-background"
   - Container: className="app-auth-container"
   - Hero: className="app-auth-hero" mit app-auth-hero__circle
   - Card: className="app-auth-card app-auth-card--narrow"
   - CardContent: className="app-auth-card__content"
   - Input Items: className="app-auth-input" (margin-bottom: 12px als --compact Variante)
   - Labels: className="app-auth-input__label"
   - Password Toggle Icons: className="app-auth-input__toggle"
   - Passwort-Checks: className="app-auth-password-checks" und app-auth-password-checks__grid
   - Passwort-Mismatch: className="app-auth-password-match-error"
   - Error-Box: className="app-auth-error"
   - Submit-Button: className="app-auth-button"
   - Erfolgs-Screen: app-auth-success-circle--small
   - Kein-Token-Screen: Error-Kreis als Inline-Style beibehalten (einmalig, spezielles Rot-Design)

2. **PasswordCheckItem Komponente:**
   - Aus der Render-Funktion extrahieren (aktuell als lokale FC innerhalb der Komponente definiert)
   - Inline-Styles beibehalten da es eine Mini-Komponente ist (color dynamisch basierend auf checked)

3. **Sicherstellen dass alle Fehlermeldungen deutsch und mit echten Umlauten sind:**
   - "Passwörter stimmen nicht überein" -- korrekt (bereits vorhanden)
   - "Ungültiger Reset-Link" -- korrekt
   - "Das Passwort erfüllt nicht alle Anforderungen" -- korrekt
  </action>
  <verify>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "ResetPassword" | head -10</verify>
  <done>ResetPasswordPage nutzt app-auth-* CSS-Klassen statt Inline-Styles, Fehlerbehandlung mit echten Umlauten</done>
</task>

<task type="auto">
  <name>Task 3: AdminInvitePage Verbesserungen</name>
  <files>frontend/src/components/admin/pages/AdminInvitePage.tsx</files>
  <action>
AdminInvitePage.tsx anpassen:

1. **Abgelaufene Codes werden bereits vom Backend gefiltert** (Query: `WHERE ic.expires_at > NOW()`). Pruefe ob das Frontend zusaetzlich filtert -- aktuell wird `isExpired` im Frontend berechnet und trotzdem angezeigt. Da das Backend nur aktive zurueckgibt, kann die `isExpired` Berechnung entfernt werden.

   - Die `isExpired` Variable und alle darauf basierenden bedingten Styles entfernen
   - Alle Codes in der Liste sind aktiv, also immer gruene Farbgebung (#22c55e)
   - Die Verlaengerungs-Buttons fuer alle angezeigten Codes anzeigen (sind per Definition nicht abgelaufen)

2. **used_count prominent anzeigen:**
   - Der Corner-Badge zeigt bereits `{invite.used_count || 0}x` an -- das ist korrekt
   - Zusaetzlich in den Meta-Items unter dem Code: "X Registrierungen" als eigenes Meta-Item hinzufuegen
     ```tsx
     <span className="app-list-item__meta-item">
       <IonIcon icon={peopleOutline} />
       {invite.used_count || 0} {(invite.used_count || 0) === 1 ? 'Registrierung' : 'Registrierungen'}
     </span>
     ```
   - Import: `peopleOutline` aus ionicons/icons

3. **Verlaengerungs-Button Fehlerbehandlung:**
   - Da Backend jetzt abgelaufene Codes mit 400 ablehnt, reicht die aktuelle catch-Logik
   - Keine Aenderung noetig hier

4. **Info-Box Text anpassen:**
   - Aktueller Text erwaehnt nicht explizit, dass Codes mehrfach nutzbar sind
   - Neuer Text: "Einladungscodes sind 7 Tage gueltig und koennen von beliebig vielen Konfis verwendet werden. Konfis werden automatisch dem gewaehlten Jahrgang zugeordnet."
   - WICHTIG: Echte Umlaute verwenden: "gültig", "können", "Schüler" etc.

5. **Registrierungs-URL in Share-Text anpassen:**
   - Der Share-Text "Registriere dich fuer..." muss echte Umlaute haben: "Registriere dich für..."
   - Pruefe alle Strings auf korrekte Umlaute
  </action>
  <verify>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "AdminInvite" | head -10</verify>
  <done>AdminInvitePage zeigt nur aktive Codes, used_count als Meta-Item, keine isExpired-Logik mehr, Info-Text mit Multi-Use-Hinweis</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` laeuft ohne Fehler fuer alle drei Dateien
- ForgotPasswordPage catch-Block enthaelt NICHT mehr `setSent(true)` als einzige catch-Aktion
- ForgotPasswordPage enthaelt "Konfi-Leiter" Hinweis-Text
- ResetPasswordPage hat keine `<style>` Tags mehr
- AdminInvitePage hat keine `isExpired` Berechnung mehr (grep "isExpired" findet 0 Treffer)
- AdminInvitePage enthaelt "peopleOutline" Import
</verification>

<success_criteria>
ForgotPasswordPage zeigt ehrliche Fehler und Konfi-Leiter-Hinweis. ResetPasswordPage nutzt CSS-Klassen. AdminInvitePage zeigt nur aktive Codes mit Registrierungsanzahl. Alle Texte haben echte Umlaute.
</success_criteria>

<output>
After completion, create `.planning/phases/07-onboarding-validierung/07-03-SUMMARY.md`
</output>
