---
phase: 72-ui-testing-fixes
plan: 03
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - frontend/src/components/chat/modals/DirectMessageModal.tsx
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "Teamer koennen im DirectMessageModal Konfis sehen und anschreiben"
    - "Admin/OrgAdmin nutzen weiterhin /admin/konfis"
    - "Teamer nutzen den neuen /teamer/konfis Endpoint"
  artifacts:
    - path: "frontend/src/components/chat/modals/DirectMessageModal.tsx"
      provides: "Rollen-basierter API-Aufruf"
      contains: "/teamer/konfis"
  key_links:
    - from: "frontend/src/components/chat/modals/DirectMessageModal.tsx"
      to: "backend/routes/teamer.js GET /konfis"
      via: "api.get('/teamer/konfis')"
      pattern: "teamer/konfis"
---

<objective>
DirectMessageModal auf rollen-basierte Konfis-API umstellen — Teamer nutzt /teamer/konfis statt /admin/konfis.

Purpose: Teamer bekommt 403 bei /admin/konfis. Nach Plan 01 existiert der neue Endpoint /teamer/konfis.
Output: DirectMessageModal erkennt Rolle und ruft korrekten Endpoint auf.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-ui-testing-fixes-material-suchleiste-groesse-event-suche-filter-teamer-chat-konfi-zugriff-directmessagemodal-403-teamer-cache-luecken-offline-button-icon/72-01-SUMMARY.md
@frontend/src/components/chat/modals/DirectMessageModal.tsx

<interfaces>
From AppContext (user object):
```typescript
// user.type = 'konfi' | 'teamer' | 'admin'
// user.role_name = 'konfi' | 'teamer' | 'admin' | 'org_admin' | 'super_admin'
const { user } = useApp();
```

Neuer Endpoint (aus Plan 01):
```
GET /teamer/konfis
Response: Array<{ id, name, username, jahrgang_name, jahrgang_id }>
```

Bestehender Endpoint:
```
GET /admin/konfis
Response: Array<{ id, name, username, gottesdienst_points, gemeinde_points, jahrgang_name, jahrgang_id, ... }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DirectMessageModal rollen-basierter Konfis-Endpoint</name>
  <files>frontend/src/components/chat/modals/DirectMessageModal.tsx</files>
  <read_first>
    - frontend/src/components/chat/modals/DirectMessageModal.tsx (vollstaendig)
  </read_first>
  <action>
Per D-06, D-10: DirectMessageModal soll Teamer-Rolle erkennen und anderen Endpoint aufrufen.

Aenderung in der `loadUsers` Funktion (Zeile 46-71):

1. Bestimme den Konfis-Endpoint basierend auf Rolle:
```typescript
const konfisEndpoint = user?.type === 'teamer' ? '/teamer/konfis' : '/admin/konfis';
```

2. Ersetze Zeile 50:
```typescript
// VORHER:
api.get('/admin/konfis'),
// NACHHER:
api.get(konfisEndpoint),
```

3. Der Rest der Logik bleibt gleich — die Response-Struktur ist kompatibel (beide liefern `id`, `name`, `jahrgang_name`).

4. Fuer Konfis: Die Konfis selbst sehen den DirectMessageModal eh nicht (nur admin/teamer erstellen DMs), aber falls doch: der Endpoint waere /admin/konfis oder /teamer/konfis — beides funktioniert nicht fuer Konfis. Da DirectMessageModal nur fuer Admin/Teamer gedacht ist, ist `user?.type === 'teamer'` ausreichend.

5. Auch den `/users` Endpunkt pruefen (Zeile 51): Dieser laedt Admin-User. Fuer Teamer: `/users` gibt alle User der Org zurueck — das sollte funktionieren (requireTeamer). Falls nicht, mit `.catch(() => ({ data: [] }))` abfangen (bereits vorhanden).

Keine weiteren Aenderungen noetig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "teamer/konfis" frontend/src/components/chat/modals/DirectMessageModal.tsx && grep -n "konfisEndpoint\|user.*type.*teamer" frontend/src/components/chat/modals/DirectMessageModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "/teamer/konfis" in DirectMessageModal.tsx findet mindestens 1 Treffer
    - grep "user.*type.*teamer" in DirectMessageModal.tsx findet die Rollen-Pruefung
    - grep "/admin/konfis" in DirectMessageModal.tsx findet noch die Fallback-Route (fuer Admin)
    - cd frontend && npx tsc --noEmit 2>&1 | head -5 — keine Fehler
  </acceptance_criteria>
  <done>DirectMessageModal ruft /teamer/konfis fuer Teamer und /admin/konfis fuer Admin/OrgAdmin auf — kein 403 mehr</done>
</task>

</tasks>

<verification>
- `grep -c "teamer/konfis" frontend/src/components/chat/modals/DirectMessageModal.tsx` >= 1
- `grep -c "admin/konfis" frontend/src/components/chat/modals/DirectMessageModal.tsx` >= 1
- `cd frontend && npx tsc --noEmit` — keine Fehler
</verification>

<success_criteria>
- Teamer kann DirectMessageModal oeffnen ohne 403
- Admin/OrgAdmin verwendet weiterhin /admin/konfis
</success_criteria>

<output>
After completion, create `.planning/phases/72-ui-testing-fixes-material-suchleiste-groesse-event-suche-filter-teamer-chat-konfi-zugriff-directmessagemodal-403-teamer-cache-luecken-offline-button-icon/72-03-SUMMARY.md`
</output>
