---
phase: 72-ui-testing-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/teamer.js
  - frontend/src/components/chat/ChatOverview.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  - frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
autonomous: true
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - "GET /teamer/konfis gibt Konfis der zugewiesenen Jahrgaenge zurueck"
    - "Teamer mit zugewiesenen Jahrgaengen erhaelt nur deren Konfis"
    - "Cache-Nutzung auf Teamer-Pages ist korrekt (useOfflineQuery vorhanden)"
  artifacts:
    - path: "backend/routes/teamer.js"
      provides: "GET /teamer/konfis Endpoint"
      contains: "router.get('/konfis'"
    - path: "frontend/src/components/chat/ChatOverview.tsx"
      provides: "Cache-Pruefung"
      contains: "useOfflineQuery"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx"
      provides: "Cache-Pruefung"
      contains: "useOfflineQuery"
    - path: "frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx"
      provides: "Cache-Pruefung"
      contains: "useOfflineQuery"
  key_links:
    - from: "backend/routes/teamer.js"
      to: "konfi_profiles + jahrgaenge"
      via: "SQL JOIN ueber assigned_jahrgaenge"
      pattern: "assigned_jahrgaenge.*can_view"
---

<objective>
Backend-Endpoint GET /teamer/konfis fuer DirectMessageModal + Cache-Verifizierung auf Teamer-Pages.

Purpose: Teamer brauchen einen eigenen Konfis-Endpoint (per D-10 sicherer als /admin/konfis zu oeffnen). Ausserdem werden 3 Teamer-Pages auf korrekte useOfflineQuery-Nutzung geprueft.
Output: Neuer Backend-Endpoint + verifizierte Cache-Nutzung.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/teamer.js
@backend/routes/konfi-managment.js
@backend/middleware/rbac.js
@frontend/src/components/chat/ChatOverview.tsx
@frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
@frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx

<interfaces>
From backend/middleware/rbac.js:
```javascript
// req.user.assigned_jahrgaenge ist ein Array mit { id, can_view, can_edit }
const requireTeamer = requireRole('org_admin', 'admin', 'teamer');
```

From backend/routes/konfi-managment.js (Referenz-Pattern fuer Jahrgang-Filter):
```javascript
router.get('/', rbacVerifier, requireTeamer, async (req, res) => {
    const viewableJahrgaenge = req.user.assigned_jahrgaenge
        .filter(j => j.can_view)
        .map(j => j.id);
    // ... SQL mit jahrgangFilter
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /teamer/konfis Endpoint in backend/routes/teamer.js</name>
  <files>backend/routes/teamer.js</files>
  <read_first>
    - backend/routes/teamer.js (vollstaendig)
    - backend/routes/konfi-managment.js (Zeilen 48-90 als Referenz fuer SQL-Query)
    - backend/middleware/rbac.js (assigned_jahrgaenge Struktur)
  </read_first>
  <action>
Neuen Endpoint GET /teamer/konfis in backend/routes/teamer.js einfuegen (nach dem /profile Block, vor /konfi-history).

Per D-10: Eigener Teamer-Endpoint — Teamer soll nicht die volle Admin-Konfi-Liste bekommen, sondern NUR Konfis der zugewiesenen Jahrgaenge.

Implementierung:
1. Route: `router.get('/konfis', rbacVerifier, requireTeamer, async (req, res) => { ... })`
2. Fuer Teamer: `req.user.assigned_jahrgaenge.filter(j => j.can_view).map(j => j.id)` als viewableJahrgaenge
3. Fuer org_admin/admin (die auch requireTeamer passieren): Alle Konfis der Organisation zurueckgeben (kein Jahrgang-Filter)
4. SQL-Query (gleiche Struktur wie konfi-managment.js GET /):
```sql
SELECT u.id, u.display_name as name, u.username,
       j.name as jahrgang_name, j.id as jahrgang_id
FROM users u
JOIN roles r ON u.role_id = r.id
LEFT JOIN konfi_profiles kp ON u.id = kp.user_id
LEFT JOIN jahrgaenge j ON kp.jahrgang_id = j.id
WHERE r.name = 'konfi' AND u.organization_id = $1
```
5. Fuer Teamer: `AND j.id IN ($2, $3, ...)` mit viewableJahrgaenge
6. Wenn viewableJahrgaenge leer: leeres Array zurueckgeben `res.json([])`
7. Response: Array von `{ id, name, username, jahrgang_name, jahrgang_id }` — KEINE Punkte-Daten (nur fuer Chat-Auswahl benoetigt)
8. ORDER BY `j.name DESC, u.display_name`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "router.get.*konfis.*rbacVerifier.*requireTeamer" backend/routes/teamer.js && grep -c "viewableJahrgaenge" backend/routes/teamer.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "router.get('/konfis'" backend/routes/teamer.js findet genau 1 Treffer
    - grep "assigned_jahrgaenge" backend/routes/teamer.js findet mindestens 1 Treffer im neuen Endpoint
    - grep "r.name = 'konfi'" backend/routes/teamer.js findet den SQL-Filter
    - Endpoint gibt NUR id, name, username, jahrgang_name, jahrgang_id zurueck (keine Punkte)
  </acceptance_criteria>
  <done>GET /teamer/konfis Endpoint existiert, filtert nach zugewiesenen Jahrgaengen fuer Teamer, gibt vollstaendige Liste fuer Admin/OrgAdmin</done>
</task>

<task type="auto">
  <name>Task 2: Cache-Verifizierung auf 3 Teamer-Pages</name>
  <files>frontend/src/components/chat/ChatOverview.tsx, frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx, frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatOverview.tsx
    - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
    - frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
  </read_first>
  <action>
Per D-11, D-12, D-13, D-14: Alle drei Pages auf useOfflineQuery pruefen.

Fuer jede Page:
1. LESEN und pruefen ob useOfflineQuery bereits verwendet wird
2. Falls JA: Notieren als "Cache korrekt — useOfflineQuery vorhanden, Cache wird beim ersten Besuch befuellt"
3. Falls NEIN: Migration auf useOfflineQuery nach Phase-56-Pattern:
   - Import: `import { useOfflineQuery } from '../../../hooks/useOfflineQuery';`
   - Import: `import { CACHE_TTL } from '../../../services/offlineCache';`
   - useState + useEffect ersetzen durch `useOfflineQuery(cacheKey, fetchFn, { ttl })`
   - Cache-Key Pattern: `'teamer:pagename:' + user?.id` oder `+ user?.organization_id`

WICHTIG per D-14: "Moeglicherweise fehlt der erste Online-Aufruf (Cache leer weil Page noch nie besucht)" — das ist KEIN Bug, sondern erwartetes Verhalten. useOfflineQuery befuellt den Cache erst beim ersten Page-Besuch. Falls alle 3 Pages bereits useOfflineQuery nutzen, ist Issue 4 kein Code-Bug sondern User-Erwartung. In dem Fall: Keine Code-Aenderung noetig, nur Dokumentation im SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -l "useOfflineQuery" frontend/src/components/chat/ChatOverview.tsx frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "useOfflineQuery" findet Treffer in allen 3 Dateien
    - Keine useState/useEffect-Pattern fuer Datenladung mehr vorhanden (oder bereits korrekt migriert)
  </acceptance_criteria>
  <done>Alle 3 Teamer-Pages nutzen useOfflineQuery — Cache wird beim ersten Besuch befuellt</done>
</task>

</tasks>

<verification>
- `grep -n "router.get.*konfis" backend/routes/teamer.js` zeigt neuen Endpoint
- `grep -c "useOfflineQuery" frontend/src/components/chat/ChatOverview.tsx` >= 1
- `cd frontend && npx tsc --noEmit 2>&1 | head -20` — kein TypeScript-Fehler
</verification>

<success_criteria>
- GET /teamer/konfis Endpoint liefert Konfis der zugewiesenen Jahrgaenge
- Cache-Nutzung auf allen 3 Pages verifiziert/implementiert
</success_criteria>

<output>
After completion, create `.planning/phases/72-ui-testing-fixes-material-suchleiste-groesse-event-suche-filter-teamer-chat-konfi-zugriff-directmessagemodal-403-teamer-cache-luecken-offline-button-icon/72-01-SUMMARY.md`
</output>
