---
phase: 88-backend-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/wrapped.js
  - backend/database.js
autonomous: true
requirements:
  - PERF-03
  - PERF-04
must_haves:
  truths:
    - "Wrapped-Snapshot-Generierung parallelisiert generateKonfiSnapshot-Aufrufe mit Promise.allSettled"
    - "Der DB Pool hat explizite max/idleTimeoutMillis/connectionTimeoutMillis Werte"
    - "PG_POOL_MAX ist als ENV-Variable konfigurierbar"
  artifacts:
    - path: "backend/routes/wrapped.js"
      provides: "Parallele Snapshot-Generierung"
      contains: "Promise.allSettled"
    - path: "backend/database.js"
      provides: "Explizite Pool-Konfiguration"
      contains: "PG_POOL_MAX"
  key_links:
    - from: "backend/routes/wrapped.js generateAllKonfiWrapped"
      to: "generateKonfiSnapshot"
      via: "Promise.allSettled statt for..of"
      pattern: "Promise\\.allSettled"
    - from: "backend/database.js"
      to: "pg.Pool"
      via: "max: parseInt(process.env.PG_POOL_MAX"
      pattern: "PG_POOL_MAX"
---

<objective>
Wrapped-Snapshot-Generierung parallelisieren und DB Pool explizit konfigurieren.

Purpose: Die sequenzielle for..of-Schleife in der Wrapped-Generierung blockiert die DB-Verbindung fuer 30 × 10+ Queries nacheinander. Promise.allSettled ermoeglicht parallele Ausfuehrung pro Konfi. Der DB Pool hat aktuell kein explizites max-Limit (pg-Default: 10) -- bei EKD-Skalierung ein Risiko.

Output: wrapped.js mit Promise.allSettled, database.js mit expliziter Pool-Konfiguration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aus backend/routes/wrapped.js -->

generateKonfiSnapshot Signatur (Zeile 22):
```javascript
async function generateKonfiSnapshot(client, userId, orgId, jahrgangId, year)
// gibt snapshot-Objekt zurueck (plain object)
```

Sequenzielle Schleife in POST /generate (Zeilen 406-422, admin-Endpoint):
```javascript
for (const konfi of konfis) {
  try {
    const snapshot = await generateKonfiSnapshot(client, konfi.user_id, req.user.organization_id, jahrgangId, currentYear);
    await client.query(`INSERT INTO wrapped_snapshots ... ON CONFLICT DO UPDATE SET ...`, [...]);
    generated++;
  } catch (err) {
    console.error(`Wrapped generation error for konfi ${konfi.user_id}:`, err.message);
    errors++;
  }
}
```

Gleiche Schleife in generateAllKonfiWrapped (Zeilen 639-654, Cron-Pfad):
```javascript
for (const konfi of konfis) {
  try {
    const snapshot = await generateKonfiSnapshot(client, konfi.user_id, orgId, jahrgangId, year);
    await client.query(`INSERT INTO wrapped_snapshots ...`, [...]);
    generated++;
  } catch (err) {
    errors++;
  }
}
```

WICHTIG: Beide Schleifen laufen innerhalb einer DB-Transaktion (BEGIN/COMMIT) mit einem geteilten `client`.
Bei Promise.allSettled muss jeder Konfi seinen eigenen Client aus dem Pool holen, da parallele Queries
auf demselben client Race Conditions erzeugen. Die Transaktion wird dann pro Konfi atomar statt
fuer alle gemeinsam -- das ist akzeptabel, da jeder Snapshot ein eigenes INSERT ON CONFLICT ist.

Strategie:
1. `client.query('BEGIN')` / `client.query('COMMIT')` fuer den aeusseren Jahrgang-Block behalten (wrapped_released_at)
2. Pro Konfi einen separaten db.getClient() holen, Snapshot generieren + inserieren, dann freigeben
3. Promise.allSettled ueber alle Konfis

<!-- Aus backend/database.js (Zeilen 14-16) -->
```javascript
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrapped-Generierung parallelisieren mit Promise.allSettled (PERF-03)</name>
  <files>backend/routes/wrapped.js</files>
  <action>
Beide for..of-Schleifen (Zeilen ~406-422 im Admin-Endpoint und ~639-654 in generateAllKonfiWrapped) durch Promise.allSettled ersetzen.

Da beide Schleifen innerhalb einer Transaktion auf `client` laufen, ist folgender Ansatz sicher:

**Parallele Hilfsfunktion erstellen** (einmalig, nahe generateKonfiSnapshot):
```javascript
async function generateAndSaveKonfiSnapshot(dbRef, userId, orgId, jahrgangId, year) {
  const konfiClient = await dbRef.getClient();
  try {
    const snapshot = await generateKonfiSnapshot(konfiClient, userId, orgId, jahrgangId, year);
    await konfiClient.query(
      `INSERT INTO wrapped_snapshots (user_id, organization_id, wrapped_type, jahrgang_id, year, data, computed_at)
       VALUES ($1, $2, 'konfi', $3, $4, $5, NOW())
       ON CONFLICT (user_id, wrapped_type, year)
       DO UPDATE SET data = EXCLUDED.data, computed_at = NOW(), jahrgang_id = EXCLUDED.jahrgang_id, organization_id = EXCLUDED.organization_id`,
      [userId, orgId, jahrgangId, year, JSON.stringify(snapshot)]
    );
    return { userId, ok: true };
  } catch (err) {
    console.error(`Wrapped generation error for konfi ${userId}:`, err.message);
    return { userId, ok: false, err };
  } finally {
    konfiClient.release();
  }
}
```

**Im Admin-Endpoint** (POST /generate, nach dem Laden von `konfis`):
```javascript
// Aeussere Transaktion nur noch fuer wrapped_released_at
await client.query('BEGIN');

const results = await Promise.allSettled(
  konfis.map(konfi => generateAndSaveKonfiSnapshot(db, konfi.user_id, req.user.organization_id, jahrgangId, currentYear))
);

const generated = results.filter(r => r.status === 'fulfilled' && r.value.ok).length;
const errors = results.length - generated;

await client.query(
  `UPDATE jahrgaenge SET wrapped_released_at = NOW() WHERE id = $1`,
  [jahrgangId]
);
await client.query('COMMIT');
```

Den alten for..of-Block inkl. INSERT vollstaendig entfernen.

**In generateAllKonfiWrapped** (Cron-Pfad, Zeilen ~639-654) analog:
```javascript
const results = await Promise.allSettled(
  konfis.map(konfi => generateAndSaveKonfiSnapshot(dbRef, konfi.user_id, orgId, jahrgangId, year))
);
const generated = results.filter(r => r.status === 'fulfilled' && r.value.ok).length;
const errors = results.length - generated;
```

Den alten for..of-Block inkl. INSERT entfernen. `wrapped_released_at`-Update und COMMIT bleiben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "const s = require('fs').readFileSync('./routes/wrapped.js','utf8'); const hasSettled = s.includes('Promise.allSettled'); const hasHelper = s.includes('generateAndSaveKonfiSnapshot'); console.log('allSettled:', hasSettled, 'helper:', hasHelper); process.exit(hasSettled && hasHelper ? 0 : 1);" 2>/dev/null; echo "exit: $?"</automated>
  </verify>
  <done>
- wrapped.js enthaelt `Promise.allSettled`
- Hilfsfunktion `generateAndSaveKonfiSnapshot` ist definiert
- Beide for..of-Schleifen (Admin-Endpoint + Cron-Pfad) sind entfernt
- Node.js laedt die Datei ohne Syntaxfehler
  </done>
</task>

<task type="auto">
  <name>Task 2: DB Pool explizit konfigurieren (PERF-04)</name>
  <files>backend/database.js</files>
  <action>
Den `new Pool({...})` Block in backend/database.js (Zeilen 14-16) um explizite Werte erweitern:

```javascript
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  max: parseInt(process.env.PG_POOL_MAX || '20', 10),
  idleTimeoutMillis: parseInt(process.env.PG_IDLE_TIMEOUT || '30000', 10),
  connectionTimeoutMillis: parseInt(process.env.PG_CONN_TIMEOUT || '5000', 10),
});
```

Erklaerenden Kommentar darueber ersetzen/aktualisieren:
```javascript
// Pool-Konfiguration: PG_POOL_MAX (Standard 20), PG_IDLE_TIMEOUT (Standard 30s), PG_CONN_TIMEOUT (Standard 5s)
// Bei EKD-Skalierung PG_POOL_MAX im Docker-Compose setzen (z.B. 50).
```

Kein weiterer Code-Aenderungsbedarf in database.js.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "const s = require('fs').readFileSync('./database.js','utf8'); const hasPgMax = s.includes('PG_POOL_MAX'); const hasIdle = s.includes('idleTimeoutMillis'); const hasConn = s.includes('connectionTimeoutMillis'); console.log('PG_POOL_MAX:', hasPgMax, 'idle:', hasIdle, 'conn:', hasConn); process.exit(hasPgMax && hasIdle && hasConn ? 0 : 1);" 2>/dev/null; echo "exit: $?"</automated>
  </verify>
  <done>
- database.js enthaelt `PG_POOL_MAX`, `idleTimeoutMillis`, `connectionTimeoutMillis`
- parseInt mit Fallback-Werten (max=20, idle=30000, conn=5000)
- Node.js laedt database.js ohne Fehler
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/Konfipoints/backend
node -e "
const wrapped = require('fs').readFileSync('./routes/wrapped.js', 'utf8');
const db = require('fs').readFileSync('./database.js', 'utf8');
const checks = [
  ['Promise.allSettled in wrapped.js', wrapped.includes('Promise.allSettled')],
  ['generateAndSaveKonfiSnapshot Hilfsfunktion', wrapped.includes('generateAndSaveKonfiSnapshot')],
  ['Kein for..of ueber konfis mehr (admin)', !wrapped.includes('for (const konfi of konfis)')],
  ['PG_POOL_MAX in database.js', db.includes('PG_POOL_MAX')],
  ['idleTimeoutMillis konfiguriert', db.includes('idleTimeoutMillis')],
  ['connectionTimeoutMillis konfiguriert', db.includes('connectionTimeoutMillis')],
];
checks.forEach(([name, ok]) => console.log((ok ? 'PASS' : 'FAIL') + ': ' + name));
"
```
</verification>

<success_criteria>
- Wrapped-Snapshot-Generierung (Admin-Endpoint + Cron) parallelisiert Konfis mit Promise.allSettled
- DB Pool hat explizite max/idleTimeoutMillis/connectionTimeoutMillis (keine pg-Defaults)
- PG_POOL_MAX ENV-Variable konfigurierbar mit Fallback 20
- Beide Dateien laden ohne Syntaxfehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/88-backend-performance/88-02-SUMMARY.md` erstellen.
</output>
