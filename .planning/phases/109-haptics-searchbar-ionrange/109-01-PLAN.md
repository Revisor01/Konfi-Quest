---
phase: 109-haptics-searchbar-ionrange
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/admin/modals/ActivityManagementModal.tsx
  - frontend/src/components/admin/modals/BonusModal.tsx
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/admin/modals/EventFormSections.tsx
  - frontend/src/components/admin/modals/BadgeManagementModal.tsx
  - frontend/src/components/admin/modals/LevelManagementModal.tsx
autonomous: true
requirements:
  - UXH-01
  - SBS-01
  - IRV-01
  - IRV-02

must_haves:
  truths:
    - "IonRange-Slider zeigt den aktuellen Wert permanent als Badge/Label neben dem Slider an"
    - "Der angezeigte Wert aktualisiert sich live beim Ziehen des Sliders"
    - "Der Wert bleibt auch nach dem Loslassen des Sliders sichtbar"
    - "Pull-to-Refresh Haptics funktionieren in allen Views (bereits implementiert, verifizieren)"
    - "Alle IonSearchbar haben ios26-searchbar-classic Klasse (bereits implementiert, verifizieren)"
  artifacts:
    - path: "frontend/src/components/admin/modals/EventFormSections.tsx"
      provides: "IonRange mit permanentem Wert-Badge (6 Instanzen)"
      contains: "style.*fontWeight.*600"
    - path: "frontend/src/components/admin/modals/BadgeManagementModal.tsx"
      provides: "IonRange mit permanentem Wert-Badge (2 Instanzen)"
      contains: "style.*fontWeight.*600"
    - path: "frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx"
      provides: "IonRange mit permanentem Wert-Badge (2 Instanzen)"
      contains: "style.*fontWeight.*600"
  key_links:
    - from: "IonRange value prop"
      to: "Wert-Badge span"
      via: "Gleiche State-Variable wird in beiden gerendert"
      pattern: "value=.*formData\\."
---

<objective>
IonRange permanente Wert-Anzeige + Verifizierung bestehender Haptics/Searchbar

Purpose: IRV-01 verlangt, dass der aktuell eingestellte Wert eines IonRange-Sliders permanent sichtbar ist (nicht nur beim Ziehen via pin). Derzeit zeigt die span neben jedem Slider nur den statischen Maximalwert. UXH-01, SBS-01 und IRV-02 sind bereits vollstaendig implementiert und werden nur verifiziert.
Output: Alle 12 IonRange-Instanzen in 7 Dateien zeigen den aktuellen Wert als permanentes Label, waehrend die Maximalwert-Anzeige rechts bestehen bleibt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-haptics-searchbar-ionrange/109-CONTEXT.md
@frontend/src/utils/haptics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: IonRange permanente Wert-Anzeige in allen Admin-Modalen und Pages</name>
  <files>
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx,
    frontend/src/components/admin/modals/ActivityManagementModal.tsx,
    frontend/src/components/admin/modals/BonusModal.tsx,
    frontend/src/components/admin/modals/EventModal.tsx,
    frontend/src/components/admin/modals/EventFormSections.tsx,
    frontend/src/components/admin/modals/BadgeManagementModal.tsx,
    frontend/src/components/admin/modals/LevelManagementModal.tsx
  </files>
  <read_first>
    frontend/src/components/admin/modals/EventFormSections.tsx (6 IonRange-Instanzen, Hauptdatei),
    frontend/src/components/admin/modals/BadgeManagementModal.tsx (2 IonRange-Instanzen),
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx (2 IonRange-Instanzen),
    frontend/src/components/admin/modals/ActivityManagementModal.tsx (1 IonRange),
    frontend/src/components/admin/modals/BonusModal.tsx (1 IonRange),
    frontend/src/components/admin/modals/EventModal.tsx (1 IonRange),
    frontend/src/components/admin/modals/LevelManagementModal.tsx (1 IonRange)
  </read_first>
  <action>
Per D-05: In ALLEN 12 IonRange-Instanzen den aktuellen Wert permanent sichtbar machen.

Aktuelles Pattern (bei jeder IonRange identisch):
```tsx
<IonRange min={1} max={20} step={1} pin={true} pinFormatter={(value: number) => `${value}`}
  value={formData.xxx} onIonChange={...} style={{ flex: 1 }} />
<span style={{ fontSize: '0.75rem', color: '#8e8e93', minWidth: '24px', textAlign: 'center' }}>20</span>
```

Neues Pattern -- die bestehende statische Maximalwert-span wird durch eine dynamische Wert-Anzeige ersetzt:
```tsx
<IonRange min={1} max={20} step={1} pin={true} pinFormatter={(value: number) => `${value}`}
  value={formData.xxx} onIonChange={...} style={{ flex: 1 }} />
<span style={{ fontSize: '0.85rem', fontWeight: 600, color: 'var(--ion-color-primary)', minWidth: '28px', textAlign: 'center' }}>{formData.xxx}</span>
```

Aenderungen pro Datei:
1. **EventFormSections.tsx** (6 Stellen):
   - Zeile ~191: checkin_window -- Wert in Minuten anzeigen: `{formData.checkin_window} min`
   - Zeile ~250: max_participants -- `{formData.max_participants}`
   - Zeile ~270: points -- `{formData.points}`
   - Zeile ~459: max_waitlist_size -- `{formData.max_waitlist_size}`
   - Zeile ~513: series_count -- `{formData.series_count}`

2. **BadgeManagementModal.tsx** (2 Stellen):
   - Zeile ~616: weeks (extraCriteria) -- `{extraCriteria.weeks || 4}`
   - Zeile ~1069: criteria_value -- `{formData.criteria_value}`

3. **AdminJahrgaengeePage.tsx** (2 Stellen):
   - Zeile ~267: target_gottesdienst -- `{formData.target_gottesdienst}`
   - Zeile ~298: target_gemeinde -- `{formData.target_gemeinde}`

4. **ActivityManagementModal.tsx** (1 Stelle):
   - Zeile ~382: points -- `{formData.points}`

5. **BonusModal.tsx** (1 Stelle):
   - Zeile ~147: points -- `{points}`

6. **EventModal.tsx** (1 Stelle):
   - Zeile ~400: timeslot.max_participants -- `{timeslot.max_participants}`

7. **LevelManagementModal.tsx** (1 Stelle):
   - Zeile ~334: points_required -- `{formData.points_required}`

Wichtig:
- Das `pin={true}` BEIBEHALTEN fuer zusaetzliches Feedback beim Ziehen
- Der `pinFormatter` bleibt unveraendert
- Die span-Farbe wird `var(--ion-color-primary)` statt `#8e8e93` damit der aktuelle Wert visuell hervorsticht gegenueber einem statischen Label
- Font-Groesse auf 0.85rem erhoehen und fontWeight 600 fuer bessere Lesbarkeit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Alle 12 IonRange-Instanzen zeigen den aktuellen dynamischen Wert in der span rechts neben dem Slider
    - Wert-Badge hat primaere Farbe (--ion-color-primary), fontWeight 600, fontSize 0.85rem
    - pin={true} bleibt bestehen fuer Feedback waehrend des Ziehens
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Jeder IonRange-Slider in der App zeigt seinen aktuellen Wert permanent als farbiges Label rechts neben dem Slider an. Der Wert aktualisiert sich live beim Ziehen und bleibt nach dem Loslassen sichtbar.</done>
</task>

<task type="auto">
  <name>Task 2: Verifizierung UXH-01, SBS-01, IRV-02 (bereits implementiert)</name>
  <files></files>
  <read_first></read_first>
  <action>
Verifiziere per Codebase-Grep dass die drei bereits implementierten Requirements weiterhin vollstaendig sind:

1. **UXH-01 (Haptics):** Per D-01/D-02 -- Alle IonRefresher muessen `onIonPull={triggerPullHaptic}` haben.
   Grep: Alle Dateien mit `IonRefresher` muessen auch `triggerPullHaptic` enthalten.

2. **SBS-01 (Searchbar Classic):** Per D-03/D-04 -- Alle IonSearchbar muessen `ios26-searchbar-classic` haben.
   Grep: Alle Dateien mit `IonSearchbar` muessen auch `ios26-searchbar-classic` enthalten.

3. **IRV-02 (Maximalwerte):** Per D-06 -- Alle IonRange haben sinnvolle Maximalwerte.
   Grep: Alle IonRange haben ein `max=` Attribut (keine unbegrenzten Slider).

Falls ein Requirement NICHT erfuellt ist, die fehlende Stelle fixen. Basierend auf der Codebase-Analyse sind alle drei Requirements bereits vollstaendig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && echo "=== UXH-01 CHECK ===" && diff <(grep -rl "IonRefresher" frontend/src --include="*.tsx" | sort) <(grep -rl "triggerPullHaptic" frontend/src --include="*.tsx" | sort) && echo "UXH-01: OK (alle IonRefresher haben Haptics)" && echo "=== SBS-01 CHECK ===" && diff <(grep -rl "IonSearchbar" frontend/src --include="*.tsx" | sort) <(grep -rl "ios26-searchbar-classic" frontend/src --include="*.tsx" | sort) && echo "SBS-01: OK (alle Searchbar haben Classic Style)" && echo "=== IRV-02 CHECK ===" && grep -c "max=" frontend/src/components/admin/modals/EventFormSections.tsx frontend/src/components/admin/modals/BadgeManagementModal.tsx frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx && echo "IRV-02: OK (alle IonRange haben max-Werte)"</automated>
  </verify>
  <acceptance_criteria>
    - Alle IonRefresher-Dateien enthalten triggerPullHaptic (UXH-01)
    - Alle IonSearchbar-Dateien enthalten ios26-searchbar-classic (SBS-01)
    - Alle IonRange haben explizite max-Werte (IRV-02)
    - Diff-Output ist leer (keine Abweichungen)
  </acceptance_criteria>
  <done>UXH-01, SBS-01 und IRV-02 sind als vollstaendig verifiziert. Alle IonRefresher haben Haptics, alle IonSearchbar haben Classic Style, alle IonRange haben sinnvolle Maximalwerte.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript kompiliert ohne Fehler
2. Grep-Verifizierung: Alle 12 IonRange-spans zeigen dynamische Werte (kein statischer Maximalwert-Text mehr)
3. Grep: `fontWeight.*600` existiert neben jedem IonRange
</verification>

<success_criteria>
- Alle 12 IonRange-Instanzen zeigen den aktuellen Wert permanent als farbiges Label
- UXH-01, SBS-01, IRV-02 sind verifiziert als vollstaendig
- TypeScript Build fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/109-haptics-searchbar-ionrange/109-01-SUMMARY.md`
</output>
