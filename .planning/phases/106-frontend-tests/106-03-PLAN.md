---
phase: 106-frontend-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 106-01
files_modified:
  - frontend/src/__tests__/components/EmptyState.test.tsx
  - frontend/src/__tests__/components/LoadingSpinner.test.tsx
  - frontend/src/__tests__/components/SectionHeader.test.tsx
  - frontend/src/__tests__/components/ListSection.test.tsx
  - frontend/src/__tests__/components/ErrorBoundary.test.tsx
autonomous: true
requirements:
  - FRT-03
must_haves:
  truths:
    - "EmptyState rendert Icon, Titel und Nachricht"
    - "LoadingSpinner rendert inline und fullScreen Variante"
    - "SectionHeader rendert Titel, Subtitle, Stats und Preset-Farben"
    - "ListSection zeigt EmptyState bei count=0 und children bei count>0"
    - "ErrorBoundary faengt Fehler und zeigt Fallback-UI"
  artifacts:
    - path: "frontend/src/__tests__/components/EmptyState.test.tsx"
      provides: "EmptyState Render-Tests"
      min_lines: 20
    - path: "frontend/src/__tests__/components/LoadingSpinner.test.tsx"
      provides: "LoadingSpinner Varianten-Tests"
      min_lines: 25
    - path: "frontend/src/__tests__/components/SectionHeader.test.tsx"
      provides: "SectionHeader Render-Tests"
      min_lines: 30
    - path: "frontend/src/__tests__/components/ListSection.test.tsx"
      provides: "ListSection Empty/Children-Tests"
      min_lines: 30
    - path: "frontend/src/__tests__/components/ErrorBoundary.test.tsx"
      provides: "ErrorBoundary Catch-Tests"
      min_lines: 30
  key_links:
    - from: "frontend/src/__tests__/components/ListSection.test.tsx"
      to: "frontend/src/components/shared/ListSection.tsx"
      via: "import + render"
      pattern: "import.*ListSection"
    - from: "frontend/src/__tests__/components/ErrorBoundary.test.tsx"
      to: "frontend/src/components/common/ErrorBoundary.tsx"
      via: "import + render"
      pattern: "import.*ErrorBoundary"
---

<objective>
5 Component-Tests fuer die wichtigsten shared/common Komponenten erstellen.

Purpose: FRT-03 erfordert mindestens 5 Component-Tests. Die gewahlten Komponenten (EmptyState, LoadingSpinner, SectionHeader, ListSection, ErrorBoundary) sind die meistverwendeten shared Components und haben klares, testbares Render-Verhalten ohne Ionic Shadow-DOM Komplexitaet (per D-04 und D-12).
Output: 5 Test-Dateien die pruefen ob Komponenten korrekt rendern und auf Props reagieren.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-frontend-tests/106-01-SUMMARY.md

<interfaces>
<!-- Exakte Props-Interfaces der zu testenden Komponenten -->

From frontend/src/components/shared/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: string;
  title: string;
  message: string;
  iconColor?: string;
}
const EmptyState: React.FC<EmptyStateProps>;
export default EmptyState;
```

From frontend/src/components/common/LoadingSpinner.tsx:
```typescript
interface LoadingSpinnerProps {
  fullScreen?: boolean;
  message?: string;
}
const LoadingSpinner: React.FC<LoadingSpinnerProps>;
export default LoadingSpinner;
// Default message: 'Quest wird geladen...'
```

From frontend/src/components/shared/SectionHeader.tsx:
```typescript
interface SectionHeaderProps {
  title: string;
  subtitle: string;
  icon: string;
  preset?: 'events' | 'activities' | 'konfis' | 'users' | 'organizations' | 'badges' | 'requests' | 'jahrgang' | 'konfi-requests' | 'categories' | 'level';
  colors?: { primary: string; secondary: string };
  stats: Array<{ value: number; label: string }>;
}
const SectionHeader: React.FC<SectionHeaderProps>;
export default SectionHeader;
```

From frontend/src/components/shared/ListSection.tsx:
```typescript
interface ListSectionProps {
  icon: string;
  title: string;
  count: number;
  iconColorClass?: string;
  children: React.ReactNode;
  emptyIcon?: string;
  emptyTitle?: string;
  emptyMessage?: string;
  emptyIconColor?: string;
  isEmpty?: boolean;
}
const ListSection: React.FC<ListSectionProps>;
export default ListSection;
```

From frontend/src/components/common/ErrorBoundary.tsx:
```typescript
interface ErrorBoundaryProps {
  children: React.ReactNode;
  fallback?: React.ReactNode;
}
class ErrorBoundary extends React.Component<ErrorBoundaryProps, ErrorBoundaryState>;
export default ErrorBoundary;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EmptyState + LoadingSpinner + SectionHeader Tests</name>
  <files>
    frontend/src/__tests__/components/EmptyState.test.tsx,
    frontend/src/__tests__/components/LoadingSpinner.test.tsx,
    frontend/src/__tests__/components/SectionHeader.test.tsx
  </files>
  <read_first>
    frontend/src/components/shared/EmptyState.tsx,
    frontend/src/components/common/LoadingSpinner.tsx,
    frontend/src/components/shared/SectionHeader.tsx
  </read_first>
  <behavior>
    EmptyState.test.tsx:
    - Rendert Titel-Text korrekt
    - Rendert Nachricht-Text korrekt
    - Wendet custom iconColor an wenn uebergeben

    LoadingSpinner.test.tsx:
    - Inline-Variante (default) rendert message-Text
    - Default-Message ist 'Quest wird geladen...'
    - Custom message wird angezeigt
    - fullScreen=true rendert IonPage Wrapper

    SectionHeader.test.tsx:
    - Rendert title und subtitle
    - Rendert alle Stats (value + label)
    - Nutzt Preset-Farben fuer 'events' (rot)
    - Nutzt custom colors wenn uebergeben
  </behavior>
  <action>
    1. Erstelle frontend/src/__tests__/components/EmptyState.test.tsx:
       - Importiere render, screen aus @testing-library/react
       - WICHTIG: Ionic Custom Elements (IonIcon) rendern in jsdom als leere Custom-Elements. Teste daher Text-Inhalte (title, message), nicht Icon-Rendering
       - 3 Test-Cases: title text, message text, iconColor prop

    2. Erstelle frontend/src/__tests__/components/LoadingSpinner.test.tsx:
       - Ionic Components (IonPage, IonContent, IonSpinner) rendern als Custom-Elements in jsdom
       - Teste Text-Inhalte: Default-Message, Custom-Message
       - Teste fullScreen via Container-Struktur (querySelector fuer ion-page)
       - 3-4 Test-Cases

    3. Erstelle frontend/src/__tests__/components/SectionHeader.test.tsx:
       - Teste text content: title, subtitle
       - Teste stats rendering: Anzahl stat-Items, Werte und Labels
       - Teste preset: background-style enthaelt korrekte Farben
       - 4 Test-Cases

    Per D-11 und D-12: Testen ob Komponenten rendern und Props korrekt verarbeiten, NICHT ob Ionic korrekt styled.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/components/EmptyState src/__tests__/components/LoadingSpinner src/__tests__/components/SectionHeader --reporter=verbose</automated>
  </verify>
  <done>3 Test-Dateien mit je 3-4 Tests, alle gruen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ListSection + ErrorBoundary Tests</name>
  <files>
    frontend/src/__tests__/components/ListSection.test.tsx,
    frontend/src/__tests__/components/ErrorBoundary.test.tsx
  </files>
  <read_first>
    frontend/src/components/shared/ListSection.tsx,
    frontend/src/components/common/ErrorBoundary.tsx
  </read_first>
  <behavior>
    ListSection.test.tsx:
    - Rendert title mit count in Klammern: "Titel (3)"
    - Rendert children wenn count > 0
    - Rendert EmptyState wenn count=0 und emptyIcon/emptyTitle/emptyMessage gesetzt
    - Rendert EmptyState wenn isEmpty=true

    ErrorBoundary.test.tsx:
    - Rendert children wenn kein Fehler
    - Zeigt Fallback-UI wenn Kind-Komponente wirft
    - Zeigt custom fallback wenn fallback-Prop gesetzt
    - Zeigt "Etwas ist schiefgelaufen" als Default-Fehlertext
  </behavior>
  <action>
    1. Erstelle frontend/src/__tests__/components/ListSection.test.tsx:
       - ListSection nutzt IonList, IonListHeader, IonCard intern — diese rendern als Custom-Elements
       - Teste title+count via textContent: "Titel (3)" im IonLabel
       - Teste children-Rendering: Uebergebe <div data-testid="child">test</div>, pruefe ob sichtbar
       - Teste Empty-State: count=0 + emptyIcon/Title/Message, pruefe ob EmptyState-Text erscheint
       - 4 Test-Cases

    2. Erstelle frontend/src/__tests__/components/ErrorBoundary.test.tsx:
       - Erstelle eine ThrowingComponent die einen Error wirft:
         ```tsx
         const ThrowingComponent = () => { throw new Error('Test Error'); };
         ```
       - Unterdruecke console.error in beforeEach (vi.spyOn(console, 'error').mockImplementation())
       - Teste: children rendern ohne Fehler, Fehler-UI bei Throw, custom fallback-Prop
       - WICHTIG: React Error Boundaries loggen den Fehler via console.error — Mock verhindert Test-Output-Verschmutzung
       - 4 Test-Cases

    Per D-11: 5 Component-Tests (EmptyState, LoadingSpinner, SectionHeader, ListSection, ErrorBoundary) erfuellt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/components/ListSection src/__tests__/components/ErrorBoundary --reporter=verbose</automated>
  </verify>
  <done>2 Test-Dateien mit je 3-4 Tests, alle gruen. Insgesamt 5 Component-Test-Dateien in diesem Plan.</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/components/ --reporter=verbose
Alle 5 Component-Test-Dateien laufen durch, mindestens 15 Tests insgesamt.
</verification>

<success_criteria>
- 5 Component-Test-Dateien existieren (EmptyState, LoadingSpinner, SectionHeader, ListSection, ErrorBoundary)
- Jede Datei hat 3-4 Tests die Render-Verhalten und Props-Verarbeitung pruefen
- Keine Ionic Shadow-DOM Tests (per D-04)
- npx vitest run laeuft gruen fuer alle Component-Tests
- Gesamte Test-Suite (Services + Hooks + Components) laeuft gruen
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-tests/106-03-SUMMARY.md`
</output>
