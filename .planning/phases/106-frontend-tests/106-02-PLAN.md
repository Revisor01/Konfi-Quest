---
phase: 106-frontend-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 106-01
files_modified:
  - frontend/src/__tests__/hooks/useActionGuard.test.ts
  - frontend/src/__tests__/hooks/useOfflineQuery.test.ts
  - frontend/src/__tests__/contexts/AppContext.test.tsx
autonomous: true
requirements:
  - FRT-01
must_haves:
  truths:
    - "useActionGuard blockiert parallele Aufrufe und gibt Fehler bei Doppel-Submit"
    - "useActionGuard setzt isSubmitting korrekt auf true/false"
    - "useOfflineQuery gibt gecachte Daten zurueck wenn offline"
    - "useOfflineQuery revalidiert im Hintergrund wenn online (SWR)"
    - "useOfflineQuery ruft onError bei fehlgeschlagenem Fetch auf"
    - "AppContext liefert user, isOnline, error, success und Setter-Funktionen"
  artifacts:
    - path: "frontend/src/__tests__/hooks/useActionGuard.test.ts"
      provides: "useActionGuard Tests"
      min_lines: 40
    - path: "frontend/src/__tests__/hooks/useOfflineQuery.test.ts"
      provides: "useOfflineQuery SWR Tests"
      min_lines: 80
    - path: "frontend/src/__tests__/contexts/AppContext.test.tsx"
      provides: "AppContext Provider Tests"
      min_lines: 50
  key_links:
    - from: "frontend/src/__tests__/hooks/useOfflineQuery.test.ts"
      to: "frontend/src/hooks/useOfflineQuery.ts"
      via: "import + renderHook"
      pattern: "renderHook.*useOfflineQuery"
    - from: "frontend/src/__tests__/contexts/AppContext.test.tsx"
      to: "frontend/src/contexts/AppContext.tsx"
      via: "import AppProvider + useApp"
      pattern: "AppProvider"
---

<objective>
Kritische Hooks (useActionGuard, useOfflineQuery) und AppContext mit Tests absichern.

Purpose: FRT-01 erfordert Tests fuer die drei wichtigsten State-Management-Bausteine der App. useOfflineQuery wird auf jeder Seite benutzt (SWR-Pattern), useActionGuard schuetzt alle Mutations, AppContext liefert den globalen App-State.
Output: 3 Test-Dateien die Kern-Verhalten der Hooks und des Contexts verifizieren.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-frontend-tests/106-01-SUMMARY.md

<interfaces>
<!-- Exakte Interfaces der zu testenden Module -->

From frontend/src/hooks/useActionGuard.ts:
```typescript
interface ActionGuardResult {
  isSubmitting: boolean;
  guard: <T>(action: () => Promise<T>) => Promise<T>;
}
export function useActionGuard(): ActionGuardResult;
```

From frontend/src/hooks/useOfflineQuery.ts:
```typescript
interface UseOfflineQueryOptions<T> {
  ttl?: number;
  enabled?: boolean;
  onSuccess?: (data: T) => void;
  onError?: (error: Error) => void;
  select?: (data: T) => T;
}
interface UseOfflineQueryResult<T> {
  data: T | null;
  loading: boolean;
  error: string | null;
  isStale: boolean;
  isOffline: boolean;
  refresh: () => Promise<void>;
}
export function useOfflineQuery<T>(
  cacheKey: string,
  fetcher: () => Promise<T>,
  options?: UseOfflineQueryOptions<T>
): UseOfflineQueryResult<T>;
```

From frontend/src/contexts/AppContext.tsx:
```typescript
interface AppContextType {
  user: BaseUser | null;
  loading: boolean;
  error: string;
  success: string;
  isOnline: boolean;
  pushNotificationsPermission: string;
  setUser: (user: BaseUser | null) => void;
  setError: (error: string) => void;
  setSuccess: (success: string) => void;
  clearMessages: () => void;
  requestPushPermissions: () => Promise<void>;
}
export const AppProvider: React.FC<{ children: React.ReactNode }>;
export function useApp(): AppContextType; // Custom hook
```

From Plan 01 Mocks (verfuegbar via __mocks__):
- @capacitor/preferences: In-Memory Store mit clearMockStore()
- @capacitor/network: Network.getStatus, Network.addListener
- @capacitor/core: Capacitor.isNativePlatform() = false
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useActionGuard + useOfflineQuery Hook-Tests</name>
  <files>
    frontend/src/__tests__/hooks/useActionGuard.test.ts,
    frontend/src/__tests__/hooks/useOfflineQuery.test.ts
  </files>
  <read_first>
    frontend/src/hooks/useActionGuard.ts,
    frontend/src/hooks/useOfflineQuery.ts,
    frontend/src/services/offlineCache.ts,
    frontend/src/services/networkMonitor.ts
  </read_first>
  <behavior>
    useActionGuard.test.ts:
    - isSubmitting ist initial false
    - guard() setzt isSubmitting auf true waehrend Ausfuehrung, danach false
    - guard() gibt Ergebnis der Action zurueck
    - guard() wirft Fehler wenn bereits eine Action laeuft ("Aktion laeuft bereits")
    - guard() setzt isSubmitting auf false auch wenn Action fehlschlaegt (finally)

    useOfflineQuery.test.ts:
    - loading ist initial true, wird false nach Fetch
    - data enthaelt Fetch-Ergebnis nach erfolgreichem Fetch
    - error wird gesetzt wenn Fetch fehlschlaegt und kein Cache vorhanden
    - onSuccess Callback wird nach erfolgreichem Fetch aufgerufen
    - onError Callback wird nach fehlgeschlagenem Fetch aufgerufen
    - enabled=false ueberspringt Fetch, loading wird sofort false
    - select-Funktion transformiert Daten vor Rueckgabe
    - isOffline reflektiert networkMonitor.isOnline
  </behavior>
  <action>
    1. Erstelle frontend/src/__tests__/hooks/useActionGuard.test.ts:
       - Nutze renderHook aus @testing-library/react
       - Nutze act() fuer State-Updates
       - Test-Cases: initial state, guard execution, double-guard rejection, error handling
       - Fuer den double-guard Test: Erstelle eine Promise die nicht sofort resolved, rufe guard() zweimal auf, pruefe dass zweiter Aufruf wirft

    2. Erstelle frontend/src/__tests__/hooks/useOfflineQuery.test.ts:
       - Mocke offlineCache und networkMonitor mit vi.mock()
       - offlineCache.get: mocke als vi.fn() das null zurueckgibt (kein Cache)
       - offlineCache.set: mocke als vi.fn()
       - offlineCache.isStale: mocke als vi.fn()
       - networkMonitor: mocke isOnline als true, subscribe als vi.fn()
       - Nutze renderHook mit waitFor fuer async Updates
       - WICHTIG: useOfflineQuery nutzt useEffect + async, daher sind waitFor() oder act() + flush noetig
       - Test-Cases: successful fetch, failed fetch, disabled, select transform, onSuccess/onError callbacks

    Per D-05: useOfflineQuery SWR-Verhalten (Cache + Revalidate) wird getestet.
    Per D-06: useActionGuard Double-Submit-Schutz wird getestet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/hooks/ --reporter=verbose</automated>
  </verify>
  <done>useActionGuard hat 5+ Tests, useOfflineQuery hat 6+ Tests, alle gruen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppContext Provider-Tests</name>
  <files>
    frontend/src/__tests__/contexts/AppContext.test.tsx
  </files>
  <read_first>
    frontend/src/contexts/AppContext.tsx,
    frontend/src/services/tokenStore.ts,
    frontend/src/services/networkMonitor.ts,
    frontend/src/services/api.ts
  </read_first>
  <behavior>
    - AppProvider rendert children korrekt
    - useApp() wirft Fehler ausserhalb von AppProvider
    - user ist initial getUser() (aus tokenStore)
    - setUser() aktualisiert user State
    - setError() aktualisiert error State
    - setSuccess() aktualisiert success State
    - clearMessages() setzt error und success auf ''
    - isOnline ist initial true
  </behavior>
  <action>
    1. Erstelle frontend/src/__tests__/contexts/AppContext.test.tsx:
       - Mocke ALLE Capacitor-Plugins die AppContext importiert:
         a) @capacitor/core (Capacitor, registerPlugin)
         b) @capacitor/device (Device.getId)
         c) @capacitor/app (App.addListener)
         d) @capacitor/push-notifications (PushNotifications)
         e) @capawesome/capacitor-background-task (BackgroundTask)
       - Mocke die Services: tokenStore (getUser gibt null), networkMonitor (isOnline=true, subscribe=vi.fn()), writeQueue, offlineCache, api
       - WICHTIG: AppContext.tsx hat viele Capacitor-Imports — alle muessen gemockt sein, sonst crashed der Import
       - Teste Provider mit einem Test-Consumer-Component:
         ```tsx
         const TestConsumer = () => {
           const ctx = useApp();
           return <div data-testid="user">{ctx.user?.display_name || 'none'}</div>;
         };
         ```
       - Rendere <AppProvider><TestConsumer /></AppProvider>
       - Nutze act() + fireEvent oder direkte State-Setter

    Per D-07: AppContext Rollen-Kontext, User-Daten, Login/Logout-State werden getestet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/contexts/ --reporter=verbose</automated>
  </verify>
  <done>AppContext hat 6+ Tests, alle gruen. Provider rendert, useApp liefert korrekten State, Setter funktionieren.</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/hooks/ src/__tests__/contexts/ --reporter=verbose
Alle Hook- und Context-Tests laufen durch.
</verification>

<success_criteria>
- useActionGuard: Double-Submit-Schutz, isSubmitting-State, Error-Handling getestet
- useOfflineQuery: SWR-Verhalten, Caching, Offline-Modus, Callbacks getestet
- AppContext: Provider-Rendering, State-Setter, User-Management getestet
- npx vitest run laeuft gruen fuer alle Tests
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-tests/106-02-SUMMARY.md`
</output>
