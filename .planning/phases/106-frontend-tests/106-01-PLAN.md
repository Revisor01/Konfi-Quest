---
phase: 106-frontend-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/setupTests.ts
  - frontend/src/__mocks__/@capacitor/preferences.ts
  - frontend/src/__mocks__/@capacitor/network.ts
  - frontend/src/__mocks__/@capacitor/core.ts
  - frontend/src/__tests__/services/tokenStore.test.ts
  - frontend/src/__tests__/services/networkMonitor.test.ts
  - frontend/src/__tests__/services/api.test.ts
autonomous: true
requirements:
  - FRT-02
must_haves:
  truths:
    - "tokenStore sync-Getter geben korrekte Werte zurueck nach async-Settern"
    - "tokenStore clearAuth loescht Token, User und RefreshToken aber behaelt DeviceId"
    - "networkMonitor subscribe benachrichtigt Listener bei Status-Wechsel"
    - "networkMonitor unsubscribe entfernt Listener korrekt"
    - "api-Service setzt Authorization-Header wenn Token vorhanden"
    - "api-Service handelt 401 mit Token-Refresh"
    - "api-Service handelt 429 Rate-Limit mit Custom-Event"
  artifacts:
    - path: "frontend/src/__tests__/services/tokenStore.test.ts"
      provides: "tokenStore Unit-Tests"
      min_lines: 60
    - path: "frontend/src/__tests__/services/networkMonitor.test.ts"
      provides: "networkMonitor Unit-Tests"
      min_lines: 50
    - path: "frontend/src/__tests__/services/api.test.ts"
      provides: "api-Service Unit-Tests"
      min_lines: 80
  key_links:
    - from: "frontend/src/__tests__/services/tokenStore.test.ts"
      to: "frontend/src/services/tokenStore.ts"
      via: "import aller exports"
      pattern: "import.*tokenStore"
    - from: "frontend/src/__tests__/services/api.test.ts"
      to: "frontend/src/services/api.ts"
      via: "import default + API_URL"
      pattern: "import.*api"
---

<objective>
Test-Infrastruktur aufsetzen und alle drei Service-Module (tokenStore, networkMonitor, api) mit Unit-Tests absichern.

Purpose: FRT-02 erfordert Unit-Tests fuer alle oeffentlichen Methoden der Utility-Module. Zuerst muessen Capacitor-Mocks erstellt und setupTests.ts aktualisiert werden, da alle drei Services Capacitor-Plugins nutzen.
Output: 3 Test-Dateien mit vollstaendiger Abdeckung aller oeffentlichen Methoden, plus Capacitor-Mock-Infrastruktur die auch Plan 02 und 03 nutzen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/vite.config.ts
@frontend/package.json
@frontend/src/setupTests.ts

<interfaces>
<!-- Exakte Interfaces der zu testenden Module -->

From frontend/src/services/tokenStore.ts:
```typescript
import { Preferences } from '@capacitor/preferences';
import { BaseUser } from '../types/user';

export const getToken = (): string | null => _token;
export const getUser = (): BaseUser | null => _user;
export const getDeviceId = (): string | null => _deviceId;
export const getPushTokenTimestamp = (): number => _pushTokenTimestamp;
export const getRefreshToken = (): string | null => _refreshToken;
export const setToken = async (token: string): Promise<void>;
export const setUser = async (user: BaseUser): Promise<void>;
export const setDeviceId = async (id: string): Promise<void>;
export const setPushTokenTimestamp = async (ts: number): Promise<void>;
export const setRefreshToken = async (token: string): Promise<void>;
export const clearAuth = async (): Promise<void>;
export const initTokenStore = async (): Promise<void>;
```

From frontend/src/services/networkMonitor.ts:
```typescript
import { Network } from '@capacitor/network';
import { Capacitor } from '@capacitor/core';

type NetworkListener = (isOnline: boolean) => void;
export const networkMonitor = {
  get isOnline(): boolean,
  subscribe(fn: NetworkListener): () => void,
  init: initNetworkMonitor(): Promise<void>,
};
```

From frontend/src/services/api.ts:
```typescript
import axios from 'axios';
import axiosRetry from 'axios-retry';
import { getToken, getRefreshToken, setToken, setRefreshToken, clearAuth } from './tokenStore';
import { networkMonitor } from './networkMonitor';

const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://konfi-quest.de/api';
export const API_URL = API_BASE_URL;
export default api; // axios instance mit interceptors
```

From frontend/src/types/user.ts:
```typescript
export interface BaseUser {
  id: number;
  type: 'admin' | 'konfi' | 'teamer' | 'user';
  display_name: string;
  username?: string;
  email?: string;
  organization?: string;
  organization_id?: number;
  roles?: string[];
  role_name?: string;
  jahrgang?: string;
  is_super_admin?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capacitor-Mocks + setupTests aktualisieren</name>
  <files>
    frontend/src/setupTests.ts,
    frontend/src/__mocks__/@capacitor/preferences.ts,
    frontend/src/__mocks__/@capacitor/network.ts,
    frontend/src/__mocks__/@capacitor/core.ts
  </files>
  <behavior>
    - Mock Preferences: get/set/remove/keys als vi.fn() mit In-Memory-Store
    - Mock Network: getStatus gibt {connected: true}, addListener ist vi.fn()
    - Mock Capacitor: isNativePlatform() gibt false, getPlatform() gibt 'web'
    - setupTests.ts: Import von @testing-library/jest-dom (ohne /extend-expect, da Vitest 4.x), matchMedia Mock beibehalten, vi.stubGlobal fuer import.meta.env.VITE_API_URL
  </behavior>
  <action>
    1. Erstelle frontend/src/__mocks__/@capacitor/preferences.ts:
       - In-Memory Map als Store
       - Preferences.get: liest aus Map, gibt {value: ...}
       - Preferences.set: schreibt in Map
       - Preferences.remove: loescht aus Map
       - Preferences.keys: gibt alle Keys
       - Export einer clearMockStore() Hilfsfunktion zum Zuruecksetzen zwischen Tests

    2. Erstelle frontend/src/__mocks__/@capacitor/network.ts:
       - Network.getStatus: gibt Promise.resolve({connected: true})
       - Network.addListener: vi.fn() der unsubscribe-Funktion zurueckgibt

    3. Erstelle frontend/src/__mocks__/@capacitor/core.ts:
       - Capacitor.isNativePlatform: gibt false
       - Capacitor.getPlatform: gibt 'web'
       - registerPlugin: gibt leeres Mock-Objekt zurueck

    4. Aktualisiere frontend/src/setupTests.ts:
       - Import aendern von '@testing-library/jest-dom/extend-expect' zu '@testing-library/jest-dom'
       - matchMedia Mock beibehalten
       - Kein import.meta.env Stub noetig (Vite/Vitest handhabt das automatisch)

    Per D-01 und D-02: Vitest mit jsdom ist bereits konfiguriert in vite.config.ts.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <done>Alle drei Capacitor-Mock-Dateien existieren, setupTests.ts aktualisiert, npx vitest run laeuft ohne Fehler</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tokenStore + networkMonitor + api Unit-Tests</name>
  <files>
    frontend/src/__tests__/services/tokenStore.test.ts,
    frontend/src/__tests__/services/networkMonitor.test.ts,
    frontend/src/__tests__/services/api.test.ts
  </files>
  <read_first>
    frontend/src/services/tokenStore.ts,
    frontend/src/services/networkMonitor.ts,
    frontend/src/services/api.ts
  </read_first>
  <behavior>
    tokenStore.test.ts:
    - getToken() gibt null vor setToken()
    - Nach setToken('abc') gibt getToken() 'abc'
    - setUser() speichert BaseUser, getUser() gibt ihn zurueck
    - clearAuth() loescht Token+User+RefreshToken, behaelt DeviceId
    - initTokenStore() laedt Werte aus Preferences in Memory
    - initTokenStore() handhabt korruptes JSON in Preferences graceful

    networkMonitor.test.ts:
    - isOnline ist initial true (optimistisch)
    - subscribe() registriert Listener, gibt unsubscribe zurueck
    - unsubscribe entfernt Listener (wird nicht mehr aufgerufen)
    - init() auf Web-Platform registriert online/offline Window-Events

    api.test.ts:
    - api setzt Authorization-Header wenn Token vorhanden
    - api setzt keinen Header wenn kein Token
    - 401-Response mit RefreshToken triggert Token-Refresh
    - 401-Response ohne RefreshToken dispatcht auth:relogin-required Event
    - 429-Response setzt rateLimitMessage und dispatcht rate-limit Event
    - API_URL export hat korrekten Default-Wert
  </behavior>
  <action>
    1. Erstelle frontend/src/__tests__/services/tokenStore.test.ts:
       - Importiere alle exports aus tokenStore
       - Importiere clearMockStore aus dem Preferences-Mock
       - beforeEach: clearMockStore() aufrufen + Modul neu laden (vi.resetModules + dynamischer Import), da tokenStore Modul-Level-State hat
       - WICHTIG: tokenStore nutzt Modul-Level-Variablen (_token, _user etc.) — Tests muessen das Modul per vi.resetModules() zwischen Tests isolieren, ODER die Reihenfolge beachten (zuerst set, dann get, dann clear)
       - Teste alle 7 Getter, 5 Setter, clearAuth, initTokenStore

    2. Erstelle frontend/src/__tests__/services/networkMonitor.test.ts:
       - Mock @capacitor/network und @capacitor/core (bereits via __mocks__)
       - Teste isOnline Property, subscribe/unsubscribe, Web-Fallback
       - WICHTIG: networkMonitor ist Singleton mit _initialized Flag — entweder vi.resetModules() oder nur einmal init() aufrufen

    3. Erstelle frontend/src/__tests__/services/api.test.ts:
       - Mock tokenStore-Funktionen mit vi.mock('../services/tokenStore')
       - Mock networkMonitor mit vi.mock('../services/networkMonitor')
       - Nutze vi.importActual('axios') fuer echtes axios, aber mocke HTTP-Calls mit vi.spyOn oder axios-mock-adapter
       - ALTERNATIV: Nutze msw (Mock Service Worker) NICHT — zu viel Overhead. Stattdessen: vi.mock('axios') und mockiere create() um eine Mock-Instance zurueckzugeben, ODER nutze axios-mock-adapter (muss ggf. installiert werden)
       - PRAGMATISCHER ANSATZ: Da api.ts interceptors auf der axios-Instance registriert, teste die Interceptor-Logik indirekt:
         a) Request-Interceptor: Importiere api, mocke getToken, mache einen Request mit nock/msw/mock-adapter
         b) Response-Interceptor 401: Simuliere 401 Response, pruefe ob refresh aufgerufen wird
         c) Response-Interceptor 429: Simuliere 429 Response, pruefe rateLimitMessage

       Per D-08, D-09, D-10: Alle oeffentlichen Methoden werden getestet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/services/ --reporter=verbose</automated>
  </verify>
  <done>Alle drei Test-Dateien existieren und alle Tests sind gruen. tokenStore: 8+ Tests, networkMonitor: 4+ Tests, api: 5+ Tests.</done>
</task>

</tasks>

<verification>
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run --reporter=verbose
Alle Service-Tests laufen durch, keine Fehler, keine Warnungen.
</verification>

<success_criteria>
- tokenStore hat Tests fuer alle 12 oeffentlichen Funktionen (5 Getter, 5 Setter, clearAuth, initTokenStore)
- networkMonitor hat Tests fuer isOnline, subscribe, unsubscribe
- api-Service hat Tests fuer Auth-Header, Token-Refresh bei 401, Rate-Limit bei 429
- Capacitor-Mocks sind wiederverwendbar fuer Plan 02 und 03
- npx vitest run laeuft gruen
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-tests/106-01-SUMMARY.md`
</output>
