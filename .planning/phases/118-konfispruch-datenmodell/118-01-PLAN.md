---
phase: 118-konfispruch-datenmodell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/093_konfspruch.sql
  - backend/routes/konfi.js
  - backend/tests/helpers/db.js
  - backend/tests/routes/konfi.test.js
autonomous: true
requirements: [SPRUCH-01, SPRUCH-02, SPRUCH-03, SPRUCH-04]
must_haves:
  truths:
    - "Es gibt die Tabelle konfsprueche (kuratierte Vers-Referenzen) und konfspruch_uebersetzungen (4 Uebersetzungen pro Vers) nach Migration 093"
    - "Migration 093 seedet ~32 Vers-REFERENZEN mit LEEREN Uebersetzungstexten als Platzhalter (kein erfundener Bibeltext)"
    - "konfi_profiles hat die Spalten konfspruch_id (FK auf konfsprueche), konfspruch_freitext und konfspruch_freitext_referenz; bible_translation wird als gewaehlte Uebersetzung wiederverwendet"
    - "GET /api/konfi/konfsprueche liefert die kuratierte Liste samt den hinterlegten Uebersetzungen fuer das Auswahl-Modal"
    - "GET /api/konfi/profile liefert den gewaehlten Spruch des Konfis mit (Listen-Wahl ODER Freitext + gewaehlte Uebersetzung)"
    - "PATCH /api/konfi/profile setzt eine Listen-Wahl (konfspruch_id + bible_translation) und loescht dabei die Freitext-Felder"
    - "PATCH /api/konfi/profile setzt einen Freitext-Spruch (Text + PFLICHT-Referenz) und loescht dabei konfspruch_id"
    - "PATCH /api/konfi/profile lehnt Freitext ohne Stellenangabe mit 400 ab (Pflicht-Referenz)"
    - "Ein Konfi kann nur sein EIGENES Profil setzen (RBAC: type==='konfi', user_id-Scope, kein Fremdzugriff)"
  artifacts:
    - path: "backend/migrations/093_konfspruch.sql"
      provides: "Tabellen konfsprueche + konfspruch_uebersetzungen, konfi_profiles-Spalten, Referenz-Seed mit leeren Texten"
      contains: "konfsprueche"
    - path: "backend/routes/konfi.js"
      provides: "GET /konfsprueche, GET /profile-Erweiterung um Spruch, PATCH /profile (Listen-Wahl + Freitext + Validierung)"
      contains: "konfsprueche"
    - path: "backend/tests/helpers/db.js"
      provides: "konfsprueche + konfspruch_uebersetzungen in der truncateAll-Liste"
      contains: "konfspruch_uebersetzungen"
    - path: "backend/tests/routes/konfi.test.js"
      provides: "Integrationstests GET /konfsprueche, PATCH /profile (Listen-Wahl, Freitext, Pflicht-Referenz, RBAC, Exklusivitaet)"
      contains: "konfsprueche"
  key_links:
    - from: "PATCH /api/konfi/profile"
      to: "konfi_profiles.konfspruch_id / konfspruch_freitext / bible_translation"
      via: "UPDATE konfi_profiles SET ... WHERE user_id=$1"
      pattern: "UPDATE konfi_profiles"
    - from: "GET /api/konfi/konfsprueche"
      to: "konfsprueche JOIN konfspruch_uebersetzungen"
      via: "SELECT mit aggregierten Uebersetzungen pro Spruch"
      pattern: "konfspruch_uebersetzungen"
    - from: "Migration 093 Seed"
      to: ".planning/phases/118-spruch-referenzen.md (Referenzliste)"
      via: "INSERT INTO konfsprueche + konfspruch_uebersetzungen mit text='' Platzhalter"
      pattern: "INSERT INTO konfsprueche"
---

<objective>
Datenmodell und Backend fuer die Konfispruch-Auswahl legen:
1. Migration 093: Tabellen `konfsprueche` (kuratierte Vers-Referenzen, organization_id NULL=global) und `konfspruch_uebersetzungen` (4 Uebersetzungen pro Vers, UNIQUE(spruch_id, translation)). konfi_profiles um `konfspruch_id` (FK Listen-Wahl) + `konfspruch_freitext` + `konfspruch_freitext_referenz` erweitern. Die bereits existierende Spalte `bible_translation` wird als gewaehlte Uebersetzung WIEDERVERWENDET (NICHT neu anlegen). Seed: ~32 Vers-REFERENZEN aus 118-spruch-referenzen.md mit LEEREN Uebersetzungstexten als Platzhalter (kein erfundener Bibeltext).
2. Backend `konfi.js`: GET /konfsprueche (Liste + Uebersetzungen fuer das Modal), GET /profile um den gewaehlten Spruch erweitern, neuer PATCH /profile (Listen-Wahl ODER Freitext mit Pflicht-Referenz; genau EINE Quelle aktiv; RBAC Konfi-eigenes-Profil).
3. Tests: konfsprueche + konfspruch_uebersetzungen in truncateAll ergaenzen, Integrationstests gegen echte PostgreSQL.

Purpose: Robustes, lizenz-konformes Datenmodell (Referenzen frei, Texte traegt der User per UPDATE nach) plus Backend-Vertrag fuer das Frontend-Modal aus Plan 02.
Output: Migration 093, erweiterte konfi.js, erweiterte db.js, neue Tests in konfi.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-CONTEXT.md
@.planning/phases/118-spruch-referenzen.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Verifizierte Code-Stellen. Datei:Zeile. Executor nutzt diese direkt, keine Codebase-Suche noetig. -->

bible_translation EXISTIERT BEREITS:
- konfi_profiles.bible_translation VARCHAR(100), KEIN Default. Wird in GET /profile selektiert (backend/routes/konfi.js:346).
- Bestehende Update-Route PUT /bible-translation (backend/routes/konfi.js:2006-2040) ist die VORLAGE fuer RBAC + UPDATE-Muster: type-Check :2007-2009, `UPDATE konfi_profiles SET bible_translation=$1 WHERE user_id=$2` :2025-2028.
- WICHTIG (Translation-Keys): Die bestehende validateBibleTranslation-Route nutzt Kuerzel ['LUT','ELB','GNB','BIGS','NIV','LSG','RVR60'] (:2016). Das Konfispruch-Feature nutzt jedoch die 4 DESKRIPTIVEN Keys aus 118-spruch-referenzen.md: 'luther2017','bigs','gute_nachricht','elberfelder'. Diese 4 Keys sind die EINZIGEN gueltigen Werte fuer den Konfispruch. Da bible_translation ein freies VARCHAR(100) OHNE DB-Constraint ist, koennen beide Stil-Welten koexistieren. Beim Setzen des Konfispruchs wird bible_translation mit einem der 4 deskriptiven Keys belegt. KEINE Migration/Aenderung an PUT /bible-translation noetig.

konfi_profiles-Schema:
- init-scripts/01-create-schema.sql:124-133 (Basis-Spalten, KEINE Spruch-Spalten). UNIQUE(user_id) :136.
- bible_translation wird in Tests via globalSetup ergaenzt: backend/tests/globalSetup.js:156 (ALTER ... ADD COLUMN IF NOT EXISTS bible_translation VARCHAR(100)).

GET /profile (zu erweitern):
- backend/routes/konfi.js:336-494. konfiId=req.user.id :342. type-Check :337-339. Haupt-SELECT :344-353 (hier kp.bible_translation bereits dabei). Response-Objekt :471-488 (hier die neuen Spruch-Felder ergaenzen).

Migration-Runner:
- backend/database.js trackt by filename in schema_migrations. Tests: backend/tests/globalSetup.js:308-334 wendet ALLE migrations/*.sql sortiert auf die Test-DB an (Migration 093 laeuft dort automatisch). Migration 093 muss daher auf der via globalSetup aufgebauten Test-Schema-Variante lauffaehig sein (bible_translation existiert dort schon).
- Stil-Vorbild Migration: backend/migrations/090_drop_legacy_badges.sql (Kommentarkopf, echte Umlaute, keine Emojis). Letzte Migration ist 092 -> naechste ist 093.

Seed-Quelle:
- .planning/phases/118-spruch-referenzen.md — ~32 Referenzen im Format `reference | book | chapter | verse` plus die 4 Translation-Keys. NUR diese Referenzen seeden. Uebersetzungstexte = LEERER String '' (Platzhalter), KEIN erfundener/aus Training reproduzierter Bibeltext.

truncateAll:
- backend/tests/helpers/db.js:44-61 (TRUNCATE-Liste). konfsprueche + konfspruch_uebersetzungen ergaenzen. konfi_profiles wird dort bereits truncated (:54). konfsprueche ist eine Lookup-Tabelle (wie categories) -> bei den Lookup-Tabellen einordnen; konfspruch_uebersetzungen haengt per FK an konfsprueche -> davor truncaten oder per CASCADE abdecken (TRUNCATE ... CASCADE deckt es ab, aber explizit auflisten = Falle-vermeiden, analog NICHT-die-badges-Falle).

Test-Muster konfi.test.js:
- backend/tests/routes/konfi.test.js:1-5 (Imports: request, getTestApp, getTestPool/truncateAll/closePool, seed/USERS, generateToken).
- beforeEach :19-24 (truncateAll + seed + konfiToken=generateToken('konfi1'), adminToken=generateToken('admin1'), konfi3Token=generateToken('konfi3')).
- GET /profile-Test :109-130 (Muster fuer 200 + 403 Admin).
- USERS.konfi1 = org 1 (seed.js:37), USERS.konfi3 = org 2 (seed.js:43) -> fuer Cross-Org/RBAC-Tests nutzbar.
- seed.js:161-170 legt konfi_profiles ohne Spruch-Felder an (Default NULL nach Migration).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 093 — Tabellen konfsprueche + konfspruch_uebersetzungen, konfi_profiles-Spalten, Referenz-Seed</name>
  <files>backend/migrations/093_konfspruch.sql</files>
  <read_first>
    - backend/migrations/090_drop_legacy_badges.sql (Stil/Kommentar-Muster einer Migration)
    - init-scripts/01-create-schema.sql:124-142 (konfi_profiles + Indizes)
    - .planning/phases/118-spruch-referenzen.md (vollstaendige Referenzliste + Translation-Keys)
    - backend/tests/globalSetup.js:155-158 (bible_translation existiert in Test-DB), :308-334 (Migrations-Runner Tests)
  </read_first>
  <action>
    Neue Datei backend/migrations/093_konfspruch.sql anlegen. Kommentarkopf analog 090 (echte Umlaute, keine Emojis), der Zweck, Idempotenz und den LIZENZ-Hinweis dokumentiert: Es werden NUR Vers-Referenzen geseedet; die Uebersetzungstexte sind leere Platzhalter und werden vom Betreiber (Kirche/ACK-Mitglied) per UPDATE aus den lizenzierten Quellen nachgetragen. KEINE Bibeltexte erfinden oder aus dem Training reproduzieren.

    Schritt 1 (Tabelle konfsprueche): `CREATE TABLE IF NOT EXISTS konfsprueche` mit Spalten: id SERIAL PRIMARY KEY, reference VARCHAR(100) NOT NULL (z.B. 'Josua 1,9'), book VARCHAR(50) NOT NULL, chapter INTEGER NOT NULL, verse INTEGER NOT NULL, organization_id INTEGER NULL REFERENCES organizations(id) ON DELETE CASCADE (NULL = global/systemweit), is_active BOOLEAN DEFAULT true, sort_order INTEGER DEFAULT 0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP. Index auf organization_id und auf is_active.

    Schritt 2 (Tabelle konfspruch_uebersetzungen): `CREATE TABLE IF NOT EXISTS konfspruch_uebersetzungen` mit: id SERIAL PRIMARY KEY, spruch_id INTEGER NOT NULL REFERENCES konfsprueche(id) ON DELETE CASCADE, translation VARCHAR(30) NOT NULL (Werte 'luther2017'|'bigs'|'gute_nachricht'|'elberfelder'), text TEXT NOT NULL DEFAULT '', UNIQUE(spruch_id, translation). Index auf spruch_id.

    Schritt 3 (konfi_profiles erweitern): per `ALTER TABLE konfi_profiles ADD COLUMN IF NOT EXISTS` (idempotent) drei Spalten:
    - konfspruch_id INTEGER NULL REFERENCES konfsprueche(id) ON DELETE SET NULL (Listen-Wahl)
    - konfspruch_freitext TEXT NULL (Freitext-Spruch)
    - konfspruch_freitext_referenz VARCHAR(100) NULL (PFLICHT-Stellenangabe bei Freitext)
    bible_translation NICHT anlegen (existiert bereits, wird wiederverwendet). KEINE DB-CHECK-Constraint fuer die Exklusivitaet (Listen vs. Freitext) — die Exklusivitaet wird in der PATCH-Route erzwungen (Task 2), damit das Setzen einer Quelle die andere sauber loescht.

    Schritt 4 (Seed der Referenzen): Fuer JEDE der ~32 Referenzen aus 118-spruch-referenzen.md ein `INSERT INTO konfsprueche (reference, book, chapter, verse, organization_id, sort_order)` mit organization_id NULL (global) und fortlaufendem sort_order. Idempotenz: vorab `DELETE FROM konfsprueche WHERE organization_id IS NULL;` (CASCADE loescht auch die zugehoerigen Uebersetzungs-Platzhalter) ODER per `INSERT ... ON CONFLICT` — da konfsprueche keinen natuerlichen Unique-Key auf reference hat, ist die saubere idempotente Variante: am Anfang von Schritt 4 `DELETE FROM konfsprueche WHERE organization_id IS NULL;` und danach frisch einfuegen. So bleibt ein erneuter Migration-Lauf (Tests laufen die Migration ggf. erneut) konsistent. Anschliessend fuer JEDEN geseedeten Spruch GENAU 4 Zeilen in konfspruch_uebersetzungen (translation = die 4 Keys, text = ''). Empfehlung fuer den Executor: die 4 Uebersetzungs-Zeilen per `INSERT INTO konfspruch_uebersetzungen (spruch_id, translation, text) SELECT id, t.translation, '' FROM konfsprueche, (VALUES ('luther2017'),('bigs'),('gute_nachricht'),('elberfelder')) AS t(translation) WHERE konfsprueche.organization_id IS NULL;` erzeugen — so ist die Texte-Tabelle vollstaendig und der User muss nur noch die text-Spalte per UPDATE befuellen.

    ALLE Schritte idempotent/wiederholungssicher. Reihenfolge: Tabellen vor Spalten vor Seed. KEINE Aenderung an schema_migrations-Logik.
  </action>
  <verify>
    <automated>cd backend && node -e "const fs=require('fs');const s=fs.readFileSync('migrations/093_konfspruch.sql','utf8');const need=['CREATE TABLE IF NOT EXISTS konfsprueche','CREATE TABLE IF NOT EXISTS konfspruch_uebersetzungen','UNIQUE','konfspruch_id','konfspruch_freitext','konfspruch_freitext_referenz','INSERT INTO konfsprueche','luther2017','elberfelder'];const miss=need.filter(n=>!s.includes(n));if(miss.length){console.error('FEHLT:',miss);process.exit(1)};if(!/ADD COLUMN IF NOT EXISTS/.test(s)){console.error('idempotente ALTER fehlt');process.exit(1)};if(/bible_translation/.test(s)){console.error('bible_translation darf NICHT angelegt werden');process.exit(1)};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/migrations/093_konfspruch.sql existiert.
    - Enthaelt CREATE TABLE IF NOT EXISTS konfsprueche und konfspruch_uebersetzungen mit UNIQUE(spruch_id, translation).
    - Enthaelt ADD COLUMN IF NOT EXISTS fuer konfspruch_id, konfspruch_freitext, konfspruch_freitext_referenz.
    - Enthaelt KEIN bible_translation (wird wiederverwendet, nicht neu angelegt).
    - Seedet alle ~32 Referenzen (organization_id NULL) + je 4 Uebersetzungs-Zeilen mit text='' (Platzhalter), idempotent (DELETE WHERE organization_id IS NULL vor Re-Insert).
    - Keine Emojis, echte Umlaute, Lizenz-Hinweis im Kommentarkopf.
  </acceptance_criteria>
  <done>Migration 093 legt beide Tabellen + die drei konfi_profiles-Spalten an und seedet die Referenzen mit leeren Uebersetzungs-Platzhaltern; idempotent und Test-DB-sicher; bible_translation bleibt unberuehrt.</done>
</task>

<task type="auto">
  <name>Task 2: konfi.js — GET /konfsprueche, GET /profile-Erweiterung, PATCH /profile (Listen-Wahl + Freitext + Validierung + RBAC)</name>
  <files>backend/routes/konfi.js</files>
  <read_first>
    - backend/routes/konfi.js:336-494 (GET /profile: SELECT :344-353, Response :471-488)
    - backend/routes/konfi.js:2006-2040 (PUT /bible-translation als RBAC/UPDATE-Vorlage)
    - backend/routes/konfi.js:20 (module.exports-Signatur), :2042 (return router)
    - .planning/phases/118-spruch-referenzen.md (gueltige Translation-Keys)
  </read_first>
  <action>
    Drei Erweiterungen in backend/routes/konfi.js, alle mit verifyTokenRBAC und type==='konfi'-Guard analog der bestehenden Routen. KEINE Emojis, echte Umlaute, deutsche Fehlertexte.

    1. GET /konfsprueche (neue Route, vor `return router`): Liefert die kuratierte Liste fuer das Modal. SELECT aus konfsprueche WHERE is_active = true AND (organization_id IS NULL OR organization_id = $1) [$1 = req.user.organization_id], sortiert nach sort_order, id. Pro Spruch die 4 Uebersetzungen mitliefern — entweder per zweitem Query (alle konfspruch_uebersetzungen fuer die gefundenen spruch_ids) und im Code zu einem Objekt { 'luther2017': text, 'bigs': ..., 'gute_nachricht': ..., 'elberfelder': ... } gruppieren, ODER per JSON-Aggregation (json_object_agg(translation, text)) direkt im SQL. Antwort: Array von { id, reference, book, chapter, verse, uebersetzungen: { luther2017, bigs, gute_nachricht, elberfelder } }. Leere text-Werte ('') bleiben leer (Platzhalter) — das Frontend zeigt dann einen Hinweis. type-Check + 500-Fehlerbehandlung wie in den Nachbarrouten.

    2. GET /profile erweitern (:336-494): In den Haupt-SELECT (:344-353) die neuen konfi_profiles-Felder aufnehmen (kp.konfspruch_id, kp.konfspruch_freitext, kp.konfspruch_freitext_referenz; kp.bible_translation ist bereits dabei). Falls kp.konfspruch_id gesetzt ist, den gewaehlten Listen-Spruch samt der gewaehlten Uebersetzung nachladen (JOIN bzw. Folge-Query auf konfsprueche + konfspruch_uebersetzungen mit translation = kp.bible_translation). Im Response-Objekt (:471-488) ein Feld `konfspruch` ergaenzen, das EINE der beiden Quellen widerspiegelt:
       - Listen-Wahl: { source: 'liste', id, reference, text (Text der gewaehlten Uebersetzung), translation: bible_translation }
       - Freitext: { source: 'freitext', text: konfspruch_freitext, reference: konfspruch_freitext_referenz }
       - keine Wahl: null.
       So bekommt das Dashboard alles fuer die Card ohne Zusatz-Call.

    3. PATCH /profile (neue Route, RBAC wie PUT /bible-translation): Body unterscheidet zwei Modi (genau EINER aktiv):
       - Listen-Wahl: { konfspruch_id: <int>, translation: <einer der 4 Keys> }. Validieren: konfspruch_id existiert in konfsprueche und ist sichtbar fuer die Org (organization_id IS NULL OR = req.user.organization_id), sonst 400/404. translation muss in ['luther2017','bigs','gute_nachricht','elberfelder'] liegen, sonst 400. UPDATE: SET konfspruch_id=$id, bible_translation=$translation, konfspruch_freitext=NULL, konfspruch_freitext_referenz=NULL WHERE user_id=req.user.id. (Setzen der Listen-Wahl loescht den Freitext -> Exklusivitaet.)
       - Freitext: { konfspruch_freitext: <string>, konfspruch_freitext_referenz: <string> }. konfspruch_freitext_referenz ist PFLICHT: fehlt/leer -> 400 mit deutschem Fehlertext (z.B. 'Bei einem eigenen Spruch ist die Stellenangabe (Referenz) verpflichtend'). konfspruch_freitext darf nicht leer sein -> 400. UPDATE: SET konfspruch_freitext=$1, konfspruch_freitext_referenz=$2, konfspruch_id=NULL WHERE user_id=req.user.id. bible_translation bleibt unveraendert (Freitext hat keine Uebersetzungs-Tabs). (Setzen des Freitexts loescht die Listen-Wahl -> Exklusivitaet.)
       Wenn weder eine gueltige Listen-Wahl noch ein gueltiger Freitext im Body steht -> 400 mit erklaerendem Fehlertext. RBAC: type==='konfi' (sonst 403), WHERE user_id=req.user.id erzwingt eigenes Profil (kein Scope-Param, kein Fremdzugriff moeglich). Antwort 200 mit { success: true } + dem gesetzten Zustand. 500-Fehlerbehandlung wie Nachbarrouten.

    Die Route(n) VOR `return router;` (:2042) einfuegen.
  </action>
  <verify>
    <automated>cd backend && node --check routes/konfi.js && grep -q "router.get('/konfsprueche'" routes/konfi.js && grep -q "router.patch('/profile'" routes/konfi.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - GET /konfsprueche existiert, filtert org-sichtbar (NULL oder eigene Org), liefert Uebersetzungen pro Spruch.
    - GET /profile liefert ein Feld konfspruch (liste|freitext|null) inkl. Text der gewaehlten Uebersetzung bei Listen-Wahl.
    - PATCH /profile: Listen-Wahl setzt konfspruch_id+bible_translation und NULLt Freitext; Freitext setzt Text+Referenz und NULLt konfspruch_id.
    - Freitext ohne Referenz -> 400. Ungueltige translation -> 400. Nicht-Konfi -> 403.
    - node --check gruen, beide Routen per grep gefunden.
  </acceptance_criteria>
  <done>GET /konfsprueche, GET /profile-Erweiterung und PATCH /profile sind verdrahtet; Exklusivitaet Listen/Freitext wird beim Setzen erzwungen; Pflicht-Referenz und RBAC greifen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: truncateAll ergaenzen + Integrationstests gegen echte PostgreSQL</name>
  <files>backend/tests/helpers/db.js, backend/tests/routes/konfi.test.js</files>
  <read_first>
    - backend/tests/helpers/db.js:44-61 (TRUNCATE-Liste)
    - backend/tests/routes/konfi.test.js:1-32 (Imports + beforeEach + Token-Setup), :109-130 (GET /profile-Test-Muster)
    - backend/tests/helpers/seed.js:161-170 (konfi_profiles-Seed ohne Spruch)
    - .planning/phases/118-spruch-referenzen.md (Translation-Keys)
  </read_first>
  <behavior>
    - GET /api/konfi/konfsprueche liefert 200 + ein nicht-leeres Array (Seed-Referenzen) mit Feld reference und einem uebersetzungen-Objekt, das die 4 Keys enthaelt.
    - GET /api/konfi/konfsprueche fuer Nicht-Konfi (Admin) -> 403.
    - PATCH /api/konfi/profile mit { konfspruch_id, translation:'luther2017' } -> 200; nachfolgendes GET /profile liefert konfspruch.source='liste' mit passender id und translation.
    - PATCH /api/konfi/profile mit Freitext { konfspruch_freitext:'Mein Spruch', konfspruch_freitext_referenz:'Joh 3,16' } -> 200; GET /profile liefert konfspruch.source='freitext' mit reference 'Joh 3,16'.
    - PATCH /api/konfi/profile Freitext OHNE konfspruch_freitext_referenz -> 400 (Pflicht-Referenz).
    - PATCH Listen-Wahl nach vorherigem Freitext loescht den Freitext (Exklusivitaet): GET /profile danach hat konfspruch.source='liste', konfspruch_freitext ist nicht mehr aktiv.
    - PATCH /api/konfi/profile mit ungueltiger translation -> 400.
    - PATCH /api/konfi/profile als Admin -> 403 (RBAC).
  </behavior>
  <action>
    1. backend/tests/helpers/db.js:44-61 — konfsprueche und konfspruch_uebersetzungen in die TRUNCATE-Liste aufnehmen (konfspruch_uebersetzungen VOR konfsprueche oder per CASCADE; explizit auflisten, um die badges-Falle nicht zu wiederholen). konfsprueche bei den Lookup-Tabellen (nahe categories) einordnen.
    2. WICHTIG zum Seed-Verhalten in Tests: truncateAll loescht konfsprueche nach jedem Test, aber die Migration 093 seedet nur EINMAL beim globalSetup. Damit die GET /konfsprueche-Tests deterministisch sind, im beforeEach (oder einem Test-Helper) nach dem seed() mindestens einen konfspruch + dessen 4 Uebersetzungen mit echtem Text in die Test-DB einfuegen (z.B. reference 'Josua 1,9', organization_id NULL, dann 4 Zeilen konfspruch_uebersetzungen). NICHT auf den Migration-Seed verlassen (der wird durch truncateAll entfernt). Die eingefuegte spruch_id fuer die PATCH-Listen-Wahl-Tests wiederverwenden.
    3. backend/tests/routes/konfi.test.js — neuen describe-Block 'Konfispruch' mit den oben genannten Tests ergaenzen. konfiToken (generateToken('konfi1')), adminToken fuer 403. Deutsche Testbeschreibungen, echte Umlaute, keine Emojis.
  </action>
  <verify>
    <automated>cd backend && npx vitest run --config tests/vitest.config.ts tests/routes/konfi.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "konfspruch" backend/tests/helpers/db.js >= 2 (beide Tabellen in truncateAll).
    - Neue Konfispruch-Tests (GET-Liste, PATCH Listen-Wahl, PATCH Freitext, Pflicht-Referenz 400, Exklusivitaet, ungueltige translation 400, RBAC 403) vorhanden und gruen.
    - Bestehende konfi.test.js-Tests weiterhin gruen (keine Regression).
  </acceptance_criteria>
  <done>truncateAll deckt beide neuen Tabellen ab; Integrationstests decken Liste, beide PATCH-Modi, Pflicht-Referenz, Exklusivitaet und RBAC gegen echte PostgreSQL ab; alle gruen, keine Regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Konfi -> PATCH /api/konfi/profile | konfspruch_id/Freitext/translation kommen aus dem Request-Body; nur eigenes Profil (user_id=req.user.id) darf gesetzt werden |
| Konfi -> GET /api/konfi/konfsprueche | Liste wird org-gefiltert ausgeliefert (NULL=global oder eigene Org), keine Fremd-Org-Sprueche |
| Migration-Runner -> Prod/Test-DB | Migration 093 legt Tabellen an + seedet Referenzen serverseitig, keine User-Eingabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Elevation of Privilege | PATCH /profile | mitigate | type==='konfi'-Guard + WHERE user_id=req.user.id; kein Scope-Param, ein Konfi kann ausschliesslich sein eigenes Profil setzen |
| T-118-02 | Tampering | konfspruch_id im Body | mitigate | Server validiert konfspruch_id gegen konfsprueche (org-sichtbar) vor dem UPDATE; ungueltige IDs -> 400/404; translation gegen Whitelist der 4 Keys |
| T-118-03 | Tampering | Freitext-Felder | mitigate | Pflicht-Referenz serverseitig erzwungen (400 bei fehlender Stellenangabe); Texte werden als reine Strings gespeichert, Frontend rendert sie nicht als HTML |
| T-118-04 | Information Disclosure | GET /konfsprueche | accept | Sprueche sind nicht sensibel (kuratierte Bibelverse), org-gefiltert wie alle Org-Ressourcen |
| T-118-05 | Repudiation/Lizenz | Uebersetzungstexte | mitigate | Seed enthaelt NUR Referenzen + LEERE Text-Platzhalter; keine Bibeltexte aus dem Training reproduziert; Betreiber traegt lizenzierte Texte selbst nach (ACK-konform) |
| T-118-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete — reiner Code-/Migrations-/Test-Plan, kein Install |
</threat_model>

<verification>
- node backend/migrations-Check (Task 1 verify) gruen: Tabellen, Spalten, Seed, kein bible_translation.
- node --check backend/routes/konfi.js gruen; GET /konfsprueche + PATCH /profile per grep vorhanden.
- npx vitest run --config tests/vitest.config.ts tests/routes/konfi.test.js -> alle gruen (inkl. neue Konfispruch-Tests).
- grep -c "konfspruch" backend/tests/helpers/db.js >= 2.
</verification>

<success_criteria>
- Tabellen konfsprueche + konfspruch_uebersetzungen existieren (Migration 093), UNIQUE(spruch_id, translation).
- konfi_profiles um konfspruch_id + Freitext-Felder erweitert; bible_translation wiederverwendet, NICHT neu angelegt.
- ~32 Referenzen geseedet mit LEEREN Uebersetzungstexten (kein erfundener Bibeltext).
- GET /konfsprueche, GET /profile (mit Spruch), PATCH /profile (Listen-Wahl + Freitext + Pflicht-Referenz + Exklusivitaet + RBAC) funktionieren.
- truncateAll deckt beide Tabellen ab; Integrationstests mitgeschrieben und gruen, keine Regression.
</success_criteria>

<output>
Create `.planning/phases/118-konfispruch-datenmodell/118-01-SUMMARY.md` when done. Dokumentiere als Vertrag fuer Plan 02 (Frontend):
- Exakter GET /konfsprueche-Antwort-Shape (Array von { id, reference, book, chapter, verse, uebersetzungen: {luther2017,bigs,gute_nachricht,elberfelder} }).
- Exaktes GET /profile-Feld `konfspruch` (source 'liste'|'freitext'|null + Felder je Variante).
- Exakter PATCH /api/konfi/profile-Body-Vertrag (Listen-Wahl: {konfspruch_id, translation}; Freitext: {konfspruch_freitext, konfspruch_freitext_referenz}).
- Die 4 gueltigen Translation-Keys (luther2017, bigs, gute_nachricht, elberfelder) und der Hinweis, dass Uebersetzungstexte initial leer sind (Frontend muss Platzhalter-Hinweis anzeigen).
</output>
