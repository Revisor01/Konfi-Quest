---
phase: 118-konfispruch-datenmodell
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - frontend/src/components/konfi/modals/KonfispruchSelectModal.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
  - frontend/src/theme/variables.css
autonomous: false
requirements: [SPRUCH-04, SPRUCH-05, SPRUCH-06]
must_haves:
  truths:
    - "Auf dem Konfi-Dashboard erscheint eine Card 'Dein Konfispruch' (neue Sektion 'konfispruch')"
    - "Ein Klick auf die Card oeffnet das Auswahl-Modal via useIonModal"
    - "Im Modal wechselt eine IonSegment-Tableiste zwischen den 4 Uebersetzungen (Luther 2017, Bibel in gerechter Sprache, Gute Nachricht, Elberfelder)"
    - "Der Konfi kann einen Spruch aus der kuratierten Liste waehlen ODER einen eigenen Freitext mit Pflicht-Stellenangabe eintragen"
    - "Beim Freitext gibt es KEINE Uebersetzungs-Tabs (nur Listen-Sprueche haben die 4 Uebersetzungen)"
    - "Speichern ruft PATCH /api/konfi/profile auf und die Card zeigt danach den gewaehlten Spruch"
    - "Die Card ist in Phase 118 ohne pro-Jahrgang-Gate sichtbar"
  artifacts:
    - path: "frontend/src/components/konfi/modals/KonfispruchSelectModal.tsx"
      provides: "useIonModal-Auswahl-Modal mit Uebersetzungs-Tabs, Listen-Auswahl und Freitext-Tab mit Pflicht-Referenz"
      min_lines: 80
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Neue sectionRenderer 'konfispruch' (Card analog konfirmation) + onOpenKonfispruch-Callback"
      contains: "konfispruch"
    - path: "frontend/src/components/konfi/pages/KonfiDashboardPage.tsx"
      provides: "useIonModal-Wiring fuer KonfispruchSelectModal + Uebergabe an DashboardView"
      contains: "KonfispruchSelectModal"
    - path: "frontend/src/theme/variables.css"
      provides: "CSS-Klasse .app-dashboard-section--konfispruch (Gradient analog Nachbarsektionen)"
      contains: "app-dashboard-section--konfispruch"
  key_links:
    - from: "DashboardView Card 'konfispruch'"
      to: "KonfispruchSelectModal (useIonModal)"
      via: "onOpenKonfispruch-Callback aus KonfiDashboardPage"
      pattern: "onOpenKonfispruch"
    - from: "KonfispruchSelectModal Speichern"
      to: "PATCH /api/konfi/profile"
      via: "api.patch('/konfi/profile', body)"
      pattern: "konfi/profile"
    - from: "KonfispruchSelectModal Liste"
      to: "GET /api/konfi/konfsprueche"
      via: "api.get beim Modal-Oeffnen"
      pattern: "konfi/konfsprueche"
---

<objective>
Frontend fuer die Konfispruch-Auswahl bauen:
1. Neue Dashboard-Sektion 'konfispruch' in DashboardView (Card analog der konfirmation-Sektion, neue CSS-Klasse .app-dashboard-section--konfispruch). Card zeigt den gewaehlten Spruch oder eine Aufforderung; Klick oeffnet das Modal.
2. KonfispruchSelectModal via useIonModal: IonSegment-Tableiste fuer die 4 Uebersetzungen (nur fuer Listen-Sprueche), Listen-Auswahl aus GET /konfsprueche, separater Freitext-Tab mit Text + PFLICHT-Stellenangabe. Speichern -> PATCH /api/konfi/profile.
3. Wiring in KonfiDashboardPage (useIonModal, presentingElement, Callback an DashboardView).

Card in Phase 118 OHNE pro-Jahrgang-Gate sichtbar (Gate folgt in Phase 119).

Purpose: Konfis koennen ihren Konfirmationsspruch selbst hinterlegen — direkt vom Dashboard, mit Umschaltung der Uebersetzung.
Output: neues Modal, erweiterte DashboardView, erweiterte KonfiDashboardPage, neue CSS-Klasse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-CONTEXT.md
@.planning/phases/118-konfispruch-datenmodell/118-01-SUMMARY.md

<interfaces>
<!-- Verifizierte Code-Stellen. Datei:Zeile. Backend-Vertrag steht im 118-01-SUMMARY.md. -->

Backend-Vertrag (aus Plan 01 — siehe 118-01-SUMMARY.md fuer exakte Shapes):
- GET /api/konfi/konfsprueche -> Array von { id, reference, book, chapter, verse, uebersetzungen: { luther2017, bigs, gute_nachricht, elberfelder } } (Texte koennen LEER '' sein -> Platzhalter-Hinweis anzeigen).
- GET /api/konfi/profile -> Feld `konfspruch`: { source:'liste', id, reference, text, translation } | { source:'freitext', text, reference } | null.
- PATCH /api/konfi/profile -> Listen-Wahl Body { konfspruch_id, translation }; Freitext Body { konfspruch_freitext, konfspruch_freitext_referenz }. translation in ['luther2017','bigs','gute_nachricht','elberfelder'].

DashboardView (Card einbauen):
- frontend/src/components/konfi/views/DashboardView.tsx:115-138 (DashboardConfig-Interface + DashboardViewProps; hier onOpenKonfispruch?: () => void ergaenzen).
- :123 DEFAULT_KONFI_ORDER = ['konfirmation','events','losung','badges','ranking'] -> 'konfispruch' einreihen (z.B. nach 'konfirmation').
- :140-153 Destructuring der Props (onOpenKonfispruch ergaenzen).
- :321-373 sectionRenderers — die konfirmation-Sektion (:323-373) ist die VORLAGE fuer Markup/Klassen. Neue Renderer-Key 'konfispruch' analog einfuegen.
- dashboardData.konfi enthaelt den Spruch NICHT automatisch — der gewaehlte Spruch kommt aus GET /profile (Feld konfspruch). Die Card im Dashboard nutzt die Daten, die die Page liefert (siehe KonfiDashboardPage unten); falls dashboardData den Spruch nicht traegt, die Card zeigt generisch 'Dein Konfispruch' + Aufforderung und der genaue gewaehlte Spruch wird nach Modal-Speichern via Refresh nachgeladen. Einfachste robuste Variante: Card zeigt immer 'Dein Konfispruch' als Titel; wenn ein Spruch-Wert vorliegt (aus profile), Referenz+Text anzeigen, sonst 'Tippe, um deinen Spruch zu waehlen'.

Card-Markup/CSS-Vorlage:
- konfirmation-Sektion DashboardView.tsx:325-371 (app-dashboard-section + __bg-text/__bg-label + __content).
- CSS frontend/src/theme/variables.css: .app-dashboard-section :2219, __bg-label :2236, __content :2247, Sektion-Modifier-Block :2258-2288 (neue .app-dashboard-section--konfispruch analog einfuegen, eigener Gradient — z.B. Violett/Indigo, NICHT identisch mit konfirmation #1e3a8a). .app-dashboard-glass-card :2337, .app-dashboard-quote :2345, .app-dashboard-cite :2357 (fuer Zitat-Optik des Spruchs nutzbar).

Modal-Wiring (KonfiDashboardPage):
- frontend/src/components/konfi/pages/KonfiDashboardPage.tsx:189-199 (useIonModal PointsHistoryModal: onClose+dismiss, presentingElement: pageRef.current). :202-211 (WrappedModal-Muster). Neues useIonModal fuer KonfispruchSelectModal analog, mit onSuccess: () => { dismiss(); refreshDashboard(); } (Card aktualisiert sich).
- pageRef existiert bereits (:266 <IonPage ref={pageRef}>).
- DashboardView-Aufruf :320-333 (onOpenKonfispruch={openKonfispruch} ergaenzen).
- refreshDashboard ist vorhanden (:214-218 refreshAllData nutzt refreshDashboard).

useIonModal-Modal-Vorlage:
- frontend/src/components/konfi/modals/PointsHistoryModal.tsx:1-70 (Imports IonPage/IonHeader/IonToolbar/IonContent, Props { onClose }, app-modal-close-btn).
- frontend/src/components/konfi/modals/ChangeEmailModal.tsx (onClose+onSuccess-Muster, Header + Speichern-Button).

IonSegment-Tabs-Vorlage:
- frontend/src/components/konfi/views/RequestsView.tsx:135-151 (app-segment-wrapper + IonSegment value/onIonChange + IonSegmentButton/IonLabel). .app-segment-wrapper existiert in variables.css:979.

api-Service:
- frontend/src/services/api.ts (default export `api`, Methoden api.get/api.patch). Import-Muster: `import api from '../../../services/api';` (siehe DashboardView.tsx:36).

Projektregeln: KEINE Emojis (nur IonIcons aus ionicons/icons), echte Umlaute, useIonModal (NIE IonModal isOpen), deutsche Texte. document-Icon falls noetig als documentIcon importieren.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KonfispruchSelectModal — useIonModal mit Uebersetzungs-Tabs, Listen-Auswahl und Freitext (Pflicht-Referenz)</name>
  <files>frontend/src/components/konfi/modals/KonfispruchSelectModal.tsx</files>
  <read_first>
    - frontend/src/components/konfi/modals/PointsHistoryModal.tsx:1-70 (Modal-Grundgeruest, Props, Header)
    - frontend/src/components/konfi/modals/ChangeEmailModal.tsx (onClose+onSuccess + Speichern-Button-Muster)
    - frontend/src/components/konfi/views/RequestsView.tsx:135-151 (IonSegment-Tabs)
    - 118-01-SUMMARY.md (exakte API-Shapes GET /konfsprueche, PATCH /profile)
  </read_first>
  <action>
    Neue Datei KonfispruchSelectModal.tsx. Props: { onClose: () => void; onSuccess: () => void; current?: { source: 'liste'|'freitext'; id?: number; reference?: string; text?: string; translation?: string } | null }. Grundgeruest wie PointsHistoryModal (IonPage > IonHeader/IonToolbar mit Titel 'Dein Konfispruch' + Schliessen-Button app-modal-close-btn > IonContent).

    Zwei Modi ueber eine OBERE IonSegment-Umschaltung 'Aus Liste' | 'Eigener Spruch' (zwei Buttons). State `mode: 'liste'|'freitext'`.

    Modus 'Aus Liste':
    - Beim Modal-Mount GET /api/konfi/konfsprueche laden (api.get), in State halten. Ladezustand mit IonSpinner.
    - Eine ZWEITE IonSegment-Tableiste fuer die 4 Uebersetzungen: Werte 'luther2017'|'bigs'|'gute_nachricht'|'elberfelder', Labels 'Luther 2017' | 'Bibel in gerechter Sprache' | 'Gute Nachricht' | 'Elberfelder'. State `translation` (Default 'luther2017' oder current.translation). Diese Tableiste NUR im Listen-Modus rendern.
    - Liste der Sprueche (IonList/IonRadioGroup oder anklickbare Items): pro Spruch reference + der Text der aktuell gewaehlten Uebersetzung (spruch.uebersetzungen[translation]). Ist der Text leer (''), einen dezenten Platzhalter-Hinweis anzeigen (z.B. 'Text wird noch ergaenzt') statt leer. Auswahl setzt State `selectedSpruchId`.
    - Speichern (nur aktiv wenn selectedSpruchId gesetzt): api.patch('/konfi/profile', { konfspruch_id: selectedSpruchId, translation }). Bei Erfolg onSuccess().

    Modus 'Eigener Spruch' (Freitext, KEINE Uebersetzungs-Tabs):
    - IonTextarea fuer den Spruchtext (State `freitext`) + IonInput fuer die Stellenangabe (State `freitextReferenz`, Label 'Stelle (z.B. Joh 3,16)').
    - Speichern: Validierung clientseitig — freitextReferenz darf nicht leer sein (sonst Hinweis/Toast 'Bitte gib die Stellenangabe an' und kein Submit; der Server validiert zusaetzlich). api.patch('/konfi/profile', { konfspruch_freitext: freitext, konfspruch_freitext_referenz: freitextReferenz }). Bei Erfolg onSuccess().

    Fehler aus der API (z.B. 400) abfangen und als deutschen Hinweis anzeigen (useIonToast oder IonText). Vorbelegung: wenn current vorhanden, den passenden Modus + Felder vorbefuellen. KEINE Emojis (IonIcons aus ionicons/icons), echte Umlaute, deutsche Texte, useIonModal-kompatibel (Komponente bekommt Props via useIonModal).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "KonfispruchSelectModal" || echo "OK keine TS-Fehler in KonfispruchSelectModal"</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert, Props { onClose, onSuccess, current }.
    - IonSegment fuer Modus (Liste/Freitext) + zweite IonSegment fuer 4 Uebersetzungen NUR im Listen-Modus.
    - Listen-Modus laedt GET /konfsprueche; leere Uebersetzungstexte zeigen Platzhalter-Hinweis.
    - Freitext-Modus: Pflicht-Referenz clientseitig validiert; KEINE Uebersetzungs-Tabs.
    - Speichern ruft PATCH /api/konfi/profile mit korrektem Body je Modus.
    - Keine Emojis, echte Umlaute. tsc ohne Fehler in dieser Datei.
  </acceptance_criteria>
  <done>KonfispruchSelectModal bietet Listen-Auswahl mit 4 Uebersetzungs-Tabs und einen Freitext-Modus mit Pflicht-Stellenangabe; Speichern persistiert via PATCH /profile.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard-Card 'konfispruch' + CSS + Modal-Wiring</name>
  <files>frontend/src/components/konfi/views/DashboardView.tsx, frontend/src/components/konfi/pages/KonfiDashboardPage.tsx, frontend/src/theme/variables.css</files>
  <read_first>
    - frontend/src/components/konfi/views/DashboardView.tsx:115-153 (Props/Destructuring/DEFAULT_KONFI_ORDER), :323-373 (konfirmation-Sektion als Vorlage)
    - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx:189-211 (useIonModal-Muster), :320-333 (DashboardView-Aufruf)
    - frontend/src/theme/variables.css:2258-2288 (Sektion-Modifier-Block)
  </read_first>
  <action>
    1. DashboardView.tsx:
       - Props-Interface (:125-138): `onOpenKonfispruch?: () => void;` ergaenzen. Im Destructuring (:140-153) aufnehmen.
       - DEFAULT_KONFI_ORDER (:123): 'konfispruch' einreihen, z.B. direkt nach 'konfirmation': ['konfirmation','konfispruch','events','losung','badges','ranking'].
       - In sectionRenderers (:322) neuen Key `konfispruch` ergaenzen, Markup analog der konfirmation-Sektion (app-dashboard-section app-dashboard-section--konfispruch + __bg-text mit __bg-label 'DEIN'/'KONFISPRUCH' + __content). Inhalt: Titel-Bereich; wenn ein gewaehlter Spruch vorliegt (aus dashboardData — falls dashboardData den Spruch nicht traegt, generisch bleiben) Referenz + Textauszug als Zitat (app-dashboard-quote/app-dashboard-cite); sonst Aufforderung 'Tippe, um deinen Konfirmationsspruch zu waehlen'. Die ganze Card per onClick={onOpenKonfispruch} klickbar (cursor: pointer). In Phase 118 OHNE pro-Jahrgang-Gate — die Card immer rendern (kein show_*-Gate). Keine Emojis, echte Umlaute.
    2. variables.css: neue Klasse `.app-dashboard-section--konfispruch` im Modifier-Block (:2258-2288) analog den anderen, eigener Gradient (z.B. Indigo/Violett-Variante, klar unterscheidbar von --konfirmation) + passender box-shadow.
    3. KonfiDashboardPage.tsx:
       - Import KonfispruchSelectModal.
       - useIonModal analog PointsHistoryModal (:190-199): const [presentKonfispruchModal, dismissKonfispruchModal] = useIonModal(KonfispruchSelectModal, { onClose: () => dismissKonfispruchModal(), onSuccess: () => { dismissKonfispruchModal(); refreshDashboard(); }, current: dashboardData?.konfspruch ?? null }).
       - openKonfispruch = () => presentKonfispruchModal({ presentingElement: pageRef.current || undefined }).
       - Im DashboardView-Aufruf (:320-333) onOpenKonfispruch={openKonfispruch} ergaenzen.
       Falls dashboardData den gewaehlten Spruch nicht enthaelt, current optional null lassen (das Modal funktioniert auch ohne Vorbelegung; nach Speichern sorgt refreshDashboard fuer Aktualitaet).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "DashboardView|KonfiDashboardPage" || echo "OK keine TS-Fehler in Dashboard-Dateien"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "konfispruch" auf DashboardView.tsx (Renderer-Key + Klasse) und auf variables.css (.app-dashboard-section--konfispruch).
    - grep -q "KonfispruchSelectModal" und "onOpenKonfispruch" in KonfiDashboardPage.tsx.
    - Card ist klickbar (onClick={onOpenKonfispruch}), in DEFAULT_KONFI_ORDER enthalten, ohne show_*-Gate (immer sichtbar).
    - tsc ohne Fehler in den geaenderten Dateien.
  </acceptance_criteria>
  <done>Die Dashboard-Card 'Dein Konfispruch' wird gerendert, ist klickbar und oeffnet das KonfispruchSelectModal; nach Speichern aktualisiert sich das Dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Dashboard-Card 'Dein Konfispruch' + KonfispruchSelectModal (Listen-Auswahl mit 4 Uebersetzungs-Tabs + Freitext mit Pflicht-Stellenangabe), verdrahtet mit GET /konfsprueche, GET /profile und PATCH /profile. Frontend-Build + Capacitor-Sync fuer iOS sind erforderlich (Projektregel: nach Frontend-Aenderungen Xcode-Build).
  </what-built>
  <how-to-verify>
    Vorbereitung (Claude fuehrt aus, soweit moeglich): `cd frontend && npm run build && npx cap sync ios`. Danach App im iOS-Simulator als Konfi (z.B. simon123 / Test-Konfi) starten.

    1. Dashboard oeffnen: Die Card 'Dein Konfispruch' ist sichtbar (eigene Farbe, unterscheidbar von der Konfirmation-Card), ohne Emojis, mit echten Umlauten.
    2. Card antippen: Das Auswahl-Modal oeffnet sich (als Sheet ueber dem Dashboard, presentingElement-Optik).
    3. Modus 'Aus Liste': Die kuratierte Spruchliste erscheint. Die obere Uebersetzungs-Tableiste (Luther 2017 / Bibel in gerechter Sprache / Gute Nachricht / Elberfelder) ist sichtbar und umschaltbar. Solange noch keine Texte hinterlegt sind, zeigt jeder Eintrag den Platzhalter-Hinweis (kein erfundener Bibeltext). Einen Spruch auswaehlen + speichern -> Modal schliesst, Card zeigt den gewaehlten Spruch (Referenz).
    4. Card erneut oeffnen, Modus 'Eigener Spruch': KEINE Uebersetzungs-Tabs. Nur Spruchtext + Pflicht-Stellenangabe speichern OHNE Stellenangabe versuchen -> es erscheint ein deutscher Hinweis und es wird NICHT gespeichert. Mit Stellenangabe speichern -> Card zeigt den Freitext + Referenz.
    5. Pruefen: keine Emojis, korrekte Umlaute, deutsche Texte ueberall.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe gefundene Abweichungen (Layout, fehlende Tabs, Validierung, Umlaute).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KonfispruchSelectModal -> PATCH /api/konfi/profile | Frontend sendet konfspruch_id/Freitext; Server validiert (siehe Plan 01), Frontend-Validierung der Pflicht-Referenz ist nur UX, nicht sicherheitskritisch |
| Modal -> GET /api/konfi/konfsprueche | Liste kommt org-gefiltert vom Server; Frontend rendert Referenzen/Texte als Plain-Text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118F-01 | Tampering | Freitext-Felder im Modal | mitigate | Server erzwingt Pflicht-Referenz (Plan 01); Frontend-Check ist nur UX. Texte werden als IonText/Plain-Text gerendert (kein dangerouslySetInnerHTML) |
| T-118F-02 | Information Disclosure | GET /konfsprueche im Modal | accept | Daten sind nicht sensibel (kuratierte Verse), org-gefiltert serverseitig |
| T-118F-SC | Tampering | npm installs | mitigate | Keine neuen Pakete — reines React/Ionic-Frontend mit bestehenden Abhaengigkeiten; kein Install |
</threat_model>

<verification>
- npx tsc --noEmit -p tsconfig.json -> keine Fehler in KonfispruchSelectModal.tsx, DashboardView.tsx, KonfiDashboardPage.tsx.
- grep -q "app-dashboard-section--konfispruch" frontend/src/theme/variables.css.
- grep -q "KonfispruchSelectModal" frontend/src/components/konfi/pages/KonfiDashboardPage.tsx.
- Human-Verify-Checkpoint: Card sichtbar+klickbar, Modal mit 4 Uebersetzungs-Tabs (Liste), Freitext ohne Tabs mit Pflicht-Referenz, Speichern aktualisiert Card, keine Emojis, korrekte Umlaute.
</verification>

<success_criteria>
- Card 'Dein Konfispruch' auf dem Konfi-Dashboard sichtbar (neue Sektion, eigene CSS-Klasse), in Phase 118 ohne Jahrgang-Gate.
- Klick oeffnet KonfispruchSelectModal via useIonModal.
- Listen-Modus mit 4 umschaltbaren Uebersetzungs-Tabs; Freitext-Modus ohne Tabs, mit Pflicht-Stellenangabe.
- Speichern via PATCH /api/konfi/profile; Card aktualisiert sich danach.
- Keine Emojis, echte Umlaute, deutsche Texte. Human-Verify bestanden.
</success_criteria>

<output>
Create `.planning/phases/118-konfispruch-datenmodell/118-02-SUMMARY.md` when done. Dokumentiere: die neue Sektion-Reihenfolge (DEFAULT_KONFI_ORDER mit 'konfispruch'), den CSS-Gradient der neuen Klasse, das useIonModal-Wiring und ob dashboardData den gewaehlten Spruch traegt oder ueber GET /profile nachgeladen wird (offener Punkt fuer Phase 119: pro-Jahrgang-Gate + Admin-Einsicht).
</output>
