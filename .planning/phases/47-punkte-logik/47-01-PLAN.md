---
phase: 47-punkte-logik
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/admin/KonfisView.tsx
autonomous: true
requirements: [PKT-v19-01, PKT-v19-02, PKT-v19-03]

must_haves:
  truths:
    - "Beim Deaktivieren eines Punkt-Typs wird der Toggle des anderen ausgegraut"
    - "Inline-Hinweis unter dem disabled Toggle zeigt Konfi-Anzahl und Punkte-Info"
    - "Admin-Konfi-Liste zeigt korrekte Gesamtpunkte basierend auf aktiven Typen"
    - "Bei nur einem aktiven Punkt-Typ wird ein breiter Statusbalken angezeigt"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx"
      provides: "Toggle-Sperre mit disabled Attribut und Inline-Hinweis"
      contains: "disabled="
    - path: "frontend/src/components/admin/KonfisView.tsx"
      provides: "Korrigierte getTotalPoints und Ein-Typ-Statusbalken"
      contains: "godiEnabled"
  key_links:
    - from: "AdminJahrgaengeePage.tsx"
      to: "formData.gottesdienst_enabled / formData.gemeinde_enabled"
      via: "IonToggle disabled Attribut"
      pattern: "disabled=.*!formData"
    - from: "KonfisView.tsx getTotalPoints"
      to: "konfi.gottesdienst_enabled / konfi.gemeinde_enabled"
      via: "enabled-Flag Pruefung in getTotalPoints"
      pattern: "godiEnabled.*gottesdienst_points"
---

<objective>
Toggle-Sperre verhindert Deaktivierung beider Punkt-Typen, Admin-Konfi-Liste zeigt korrekte Gesamtpunkte, und Ein-Typ-Statusbalken wird breit angezeigt.

Purpose: Drei zusammenhaengende Punkte-Logik-Bugs fixen die das Admin-Erlebnis bei der Punktekonfiguration betreffen.
Output: Korrekte Toggle-Logik in JahrgangModal, korrekte Punkte-Summe in Konfi-Liste, breiter Einzelbalken bei einem Typ.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-punkte-logik/47-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx (Zeile 210-274):
Toggle-UI fuer Gottesdienst/Gemeinde-Punkte im JahrgangModal. Aktuell:
- IonToggle checked={formData.gottesdienst_enabled} ohne disabled
- IonToggle checked={formData.gemeinde_enabled} ohne disabled

From frontend/src/components/admin/KonfisView.tsx (Zeile 113-117):
```typescript
const getTotalPoints = (konfi: Konfi) => {
  const gottesdienst = konfi.gottesdienst_points ?? konfi.points?.gottesdienst ?? 0;
  const gemeinde = konfi.gemeinde_points ?? konfi.points?.gemeinde ?? 0;
  return gottesdienst + gemeinde;  // BUG: ignoriert enabled-Flags
};
```

From KonfisView.tsx (Zeile 156-163):
```typescript
const getKonfiTargets = (konfi: Konfi) => {
  const godiEnabled = konfi.gottesdienst_enabled !== false;
  const gemEnabled = konfi.gemeinde_enabled !== false;
  // ... bereits korrekt enabled-basiert
};
```

Progress Bars (Zeile 439-491): Zwei schmale Balken nebeneinander, Gesamtbalken nur bei beiden Typen aktiv.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle-Sperre in JahrgangModal + getTotalPoints Fix</name>
  <files>
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx,
    frontend/src/components/admin/KonfisView.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx,
    frontend/src/components/admin/KonfisView.tsx,
    backend/routes/jahrgaenge.js
  </read_first>
  <action>
**AdminJahrgaengeePage.tsx - Toggle-Sperre (PKT-v19-01):**

1. Gottesdienst-Toggle (Zeile 223-227): Erweitere `disabled` um die Sperre:
   ```
   disabled={loading || (!formData.gemeinde_enabled)}
   ```
   Wenn `gemeinde_enabled` false ist, darf Gottesdienst nicht auch deaktiviert werden.

2. Gemeinde-Toggle (Zeile 248-252): Analog:
   ```
   disabled={loading || (!formData.gottesdienst_enabled)}
   ```

3. Unter jedem Toggle einen Inline-Hinweis anzeigen wenn der Toggle disabled ist (weil der andere Typ deaktiviert wurde). Hinweis-Text:
   ```
   "Mindestens ein Punkt-Typ muss aktiv bleiben."
   ```
   Style: fontSize 0.75rem, color #f59e0b (Warnung-Orange), marginTop 4px, paddingLeft 16px.
   Bedingung fuer Gottesdienst-Hinweis: `!formData.gemeinde_enabled && formData.gottesdienst_enabled`
   Bedingung fuer Gemeinde-Hinweis: `!formData.gottesdienst_enabled && formData.gemeinde_enabled`

**KonfisView.tsx - getTotalPoints Fix (PKT-v19-02):**

4. Funktion `getTotalPoints` (Zeile 113-117) aendern zu:
   ```typescript
   const getTotalPoints = (konfi: Konfi) => {
     const godiEnabled = konfi.gottesdienst_enabled !== false;
     const gemEnabled = konfi.gemeinde_enabled !== false;
     const gottesdienst = godiEnabled ? (konfi.gottesdienst_points ?? konfi.points?.gottesdienst ?? 0) : 0;
     const gemeinde = gemEnabled ? (konfi.gemeinde_points ?? konfi.points?.gemeinde ?? 0) : 0;
     return gottesdienst + gemeinde;
   };
   ```

**KonfisView.tsx - Ein-Typ-Statusbalken (PKT-v19-03):**

5. Progress Bars Bereich (Zeile 439-491) anpassen: Wenn nur EIN Typ aktiv ist und der andere nicht, soll statt der zwei schmalen Balken + Gesamtbalken NUR EIN breiter Balken gezeigt werden (wie der Gesamtbalken). Konkret:

   - Wenn `godiEnabled && !gemEnabled`: Einen breiten Balken mit Farbe #3b82f6, Label "Gottesdienst", Wert `godiPoints/targetGodi`, CSS-Klasse `app-progress-bar--thick`.
   - Wenn `gemEnabled && !godiEnabled`: Einen breiten Balken mit Farbe #059669, Label "Gemeinde", Wert `gemPoints/targetGem`, CSS-Klasse `app-progress-bar--thick`.
   - Wenn beide aktiv: Bestehendes Layout beibehalten (zwei schmale + Gesamtbalken).

   Implementierung: Den gesamten Progress-Bars Block (Zeile 439-491) durch eine Bedingung ersetzen:
   ```tsx
   {godiEnabled && gemEnabled ? (
     // Bestehender Code: 2 schmale Balken + Gesamtbalken (unveraendert)
   ) : (
     // Ein breiter Balken fuer den aktiven Typ
     <div>
       <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '2px' }}>
         <span style={{ fontSize: '0.7rem', color: godiEnabled ? '#3b82f6' : '#059669', fontWeight: '700' }}>
           {godiEnabled ? 'Gottesdienst' : 'Gemeinde'}
         </span>
         <span style={{ fontSize: '0.7rem', color: '#666', fontWeight: '600' }}>
           {godiEnabled ? godiPoints : gemPoints}/{godiEnabled ? targetGodi : targetGem}
         </span>
       </div>
       <div className="app-progress-bar app-progress-bar--thick">
         <div className="app-progress-bar__track" style={{ backgroundColor: godiEnabled ? 'rgba(59, 130, 246, 0.15)' : 'rgba(34, 197, 94, 0.15)' }}>
           <div className="app-progress-bar__fill" style={{
             width: `${Math.min(100, godiEnabled ? percentGodi : percentGem)}%`,
             backgroundColor: godiEnabled ? '#3b82f6' : '#059669'
           }} />
         </div>
       </div>
     </div>
   )}
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - AdminJahrgaengeePage.tsx enthaelt `disabled={loading || (!formData.gemeinde_enabled)}` beim Gottesdienst-Toggle
    - AdminJahrgaengeePage.tsx enthaelt `disabled={loading || (!formData.gottesdienst_enabled)}` beim Gemeinde-Toggle
    - AdminJahrgaengeePage.tsx enthaelt "Mindestens ein Punkt-Typ muss aktiv bleiben"
    - KonfisView.tsx getTotalPoints prueft godiEnabled/gemEnabled vor Punkte-Addition
    - KonfisView.tsx zeigt app-progress-bar--thick bei nur einem aktiven Typ
  </acceptance_criteria>
  <done>Toggle-Sperre verhindert Deaktivierung beider Typen mit Hinweis, Gesamtpunkte respektieren enabled-Flags, Ein-Typ zeigt breiten Balken</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` kompiliert ohne Fehler
- AdminJahrgaengeePage: Toggle disabled wenn anderer Typ deaktiviert
- KonfisView: getTotalPoints nutzt enabled-Flags
- KonfisView: Ein-Typ-Statusbalken mit app-progress-bar--thick
</verification>

<success_criteria>
1. Beim Deaktivieren eines Punkt-Typs wird der Toggle des anderen ausgegraut mit Inline-Hinweis
2. Admin-Konfi-Liste zeigt korrekte Gesamtpunkte (nur aktive Typen)
3. Bei nur einem aktiven Punkt-Typ: Breiter Einzelbalken in Typ-Farbe statt 2 schmale + Gesamt
</success_criteria>

<output>
After completion, create `.planning/phases/47-punkte-logik/47-01-SUMMARY.md`
</output>
