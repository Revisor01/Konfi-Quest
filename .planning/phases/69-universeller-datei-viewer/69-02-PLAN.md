---
phase: 69-universeller-datei-viewer
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - frontend/src/components/chat/ChatRoom.tsx
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  - frontend/src/components/chat/modals/FileViewerModal.tsx
autonomous: true
requirements: [FV-08, FV-09]

must_haves:
  truths:
    - "Tap auf Bild in Chat oeffnet neuen FileViewerModal mit Zoom/Pan"
    - "Tap auf Video in Chat oeffnet neuen FileViewerModal mit Video-Player"
    - "Tap auf Datei in Chat oeffnet neuen FileViewerModal"
    - "Swipe zwischen allen Dateien im Chat-Kontext funktioniert"
    - "Tap auf Datei in Material oeffnet neuen FileViewerModal"
    - "Swipe zwischen allen Material-Dateien funktioniert"
    - "Altes FileViewerModal aus chat/modals/ wird nicht mehr genutzt"
  artifacts:
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "FileViewer-Integration mit files-Array aus Chat-Messages"
      contains: "shared/FileViewerModal"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx"
      provides: "FileViewer-Integration mit Material-Dateien"
      contains: "shared/FileViewerModal"
  key_links:
    - from: "ChatRoom.tsx"
      to: "shared/FileViewerModal"
      via: "useIonModal mit files-Array aus messages"
      pattern: "useIonModal.*FileViewerModal"
    - from: "TeamerMaterialDetailPage.tsx"
      to: "shared/FileViewerModal"
      via: "useIonModal mit files-Array aus material.files"
      pattern: "useIonModal.*FileViewerModal"
    - from: "MessageBubble.tsx"
      to: "ChatRoom.tsx"
      via: "onFileClick callback mit messageId + fileIndex"
      pattern: "onFileClick"
---

<objective>
Neues universelles FileViewerModal in Chat und Material integrieren. Chat: Tap auf Bild/Video/Datei oeffnet Viewer mit Swipe durch alle Dateien im Raum. Material: Tap auf Datei oeffnet Viewer mit Swipe durch alle Material-Dateien.

Purpose: Alle Dateien in der App werden ueber den einheitlichen Fullscreen-Viewer geoeffnet statt ueber native Viewer oder den alten minimalen Fallback.
Output: Aktualisierte ChatRoom, MessageBubble, TeamerMaterialDetailPage
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-universeller-datei-viewer/69-CONTEXT.md
@.planning/phases/69-universeller-datei-viewer/69-01-SUMMARY.md

@frontend/src/components/chat/ChatRoom.tsx
@frontend/src/components/chat/MessageBubble.tsx
@frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
@frontend/src/components/chat/modals/FileViewerModal.tsx

<interfaces>
<!-- Neues FileViewerModal aus Plan 01: -->
From frontend/src/components/shared/FileViewerModal.tsx:
```typescript
interface FileItem {
  url: string;
  fileName: string;
  mimeType: string;
}

interface FileViewerModalProps {
  files: FileItem[];
  initialIndex: number;
  onClose: () => void;
}
```

<!-- MessageBubble bestehendes onImageClick: -->
```typescript
onImageClick: (filePath: string) => void;
```
Wird zu:
```typescript
onFileClick: (filePath: string, fileName: string, mimeType: string) => void;
```

<!-- ChatRoom bestehender handleImageOrFileClick: -->
```typescript
const handleImageOrFileClick = async (filePath: string) => { ... }
```
Wird ersetzt durch files-Array-basierte Logik.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat-Integration — ChatRoom + MessageBubble auf neues FileViewerModal umstellen</name>
  <files>frontend/src/components/chat/ChatRoom.tsx, frontend/src/components/chat/MessageBubble.tsx</files>
  <read_first>
    - frontend/src/components/shared/FileViewerModal.tsx (neues Modal aus Plan 01)
    - frontend/src/components/chat/ChatRoom.tsx (Zeilen 60-80: viewerDataRef, Zeilen 266-283: useIonModal+openInAppViewer, Zeilen 972-1012: handleImageOrFileClick)
    - frontend/src/components/chat/MessageBubble.tsx (Zeilen 36-56: Props, Zeilen 429-495: file_path Rendering mit onImageClick)
  </read_first>
  <action>
    **MessageBubble.tsx aendern (per D-11):**
    1. Prop `onImageClick` umbenennen zu `onFileClick` mit Signatur: `(filePath: string, fileName: string, mimeType: string) => void`
    2. Bei Bild-Klick (Zeile ~442): `onFileClick(message.file_path, message.file_name || 'Bild', 'image/jpeg')` — MIME aus Extension ableiten (jpg→image/jpeg, png→image/png, gif→image/gif, webp→image/webp)
    3. Bei Video: Neuen onClick-Handler auf VideoPreview-Wrapper: `onFileClick(message.file_path, message.file_name || 'Video', 'video/mp4')` — MIME aus Extension ableiten
    4. Bei Datei-Klick (Zeile ~467-470): `onFileClick(message.file_path, message.file_name || 'Datei', getMimeFromExtension(message.file_name))` — helper Funktion fuer MIME-Ableitung
    5. Helper-Funktion `getMimeFromFileName(fileName: string): string` oben in der Datei:
       - .jpg/.jpeg → image/jpeg, .png → image/png, .gif → image/gif, .webp → image/webp
       - .mp4 → video/mp4, .mov → video/quicktime, .avi → video/x-msvideo, .webm → video/webm
       - .pdf → application/pdf
       - .docx → application/vnd.openxmlformats-officedocument.wordprocessingml.document
       - .xlsx → application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
       - default → application/octet-stream

    **ChatRoom.tsx aendern (per D-11):**
    1. Import aendern: `FileViewerModal` von `'../../shared/FileViewerModal'` statt `'./modals/FileViewerModal'`
    2. Import `FileItem` Type aus dem neuen FileViewerModal
    3. `viewerDataRef` ersetzen durch: `const viewerRef = useRef<{ files: FileItem[]; initialIndex: number }>({ files: [], initialIndex: 0 })`
    4. useIonModal aktualisieren:
       ```typescript
       const [presentFileViewer, dismissFileViewer] = useIonModal(FileViewerModal, {
         get files() { return viewerRef.current.files; },
         get initialIndex() { return viewerRef.current.initialIndex; },
         onClose: () => {
           dismissFileViewer();
           // Blob-URLs revoken
           viewerRef.current.files.forEach(f => {
             if (f.url.startsWith('blob:')) URL.revokeObjectURL(f.url);
           });
           viewerRef.current = { files: [], initialIndex: 0 };
         }
       });
       ```
    5. `presentFileViewer` Aufruf mit cssClass: `presentFileViewer({ cssClass: 'file-viewer-modal' })`
    6. Neuer `handleFileClick` ersetzt `handleImageOrFileClick`:
       ```typescript
       const handleFileClick = async (filePath: string, fileName: string, mimeType: string) => {
         try {
           await Haptics.impact({ style: ImpactStyle.Light });
           // Alle Dateien im Chat sammeln fuer Swipe-Kontext
           const fileMessages = messages.filter(m => m.file_path);
           const allFiles: FileItem[] = [];
           let clickedIndex = 0;
           for (const msg of fileMessages) {
             const blobResp = await api.get(`/chat/files/${msg.file_path}`, { responseType: 'blob' });
             // Nur die angeklickte Datei sofort laden, Rest lazy
             // VEREINFACHUNG: Nur angeklickte Datei als Blob, Rest als Placeholder
           }
           // PRAGMATISCHER ANSATZ: Nur die angeklickte Datei laden
           const response = await api.get(`/chat/files/${filePath}`, { responseType: 'blob' });
           const blob = response.data;
           const mime = response.headers?.['content-type'] || mimeType;
           const blobUrl = URL.createObjectURL(new Blob([blob], { type: mime }));
           // Alle file-Messages als FileItem-Array aufbauen, aber nur aktuelle mit Blob-URL
           // Die anderen Dateien mit API-URL (Viewer laedt sie lazy beim Swipe)
           const allFileMessages = messages.filter(m => m.file_path);
           const files: FileItem[] = allFileMessages.map(m => {
             if (m.file_path === filePath) {
               return { url: blobUrl, fileName: m.file_name || fileName, mimeType: mime };
             }
             // Fuer andere Dateien: API-URL direkt (Viewer muss fetch unterstuetzen)
             // EINFACHER: Blob-URL erst beim Swipe laden → FileViewerModal muss das handlen
             // NOCH EINFACHER: Nur aktuelle Datei, kein Swipe im Chat (zu viele API-Calls)
             return { url: `/api/chat/files/${m.file_path}`, fileName: m.file_name || 'Datei', mimeType: getMimeFromFileName(m.file_name || '') };
           });
           clickedIndex = allFileMessages.findIndex(m => m.file_path === filePath);
           if (clickedIndex < 0) clickedIndex = 0;
           viewerRef.current = { files, initialIndex: clickedIndex };
           presentFileViewer({ cssClass: 'file-viewer-modal' });
         } catch {
           setError('Fehler beim Oeffnen der Datei');
         }
       };
       ```
       WICHTIG: Da API-URLs fuer nicht-geladene Dateien verwendet werden, muss der FileViewerModal (aus Plan 01) damit umgehen. Wenn die URL nicht mit `blob:` startet, muss der Viewer die Datei per fetch laden. Das muss in Plan 01 bereits beruecksichtigt sein — falls nicht, hier eine Hilfsfunktion hinzufuegen die im Viewer lazy laedt.

       ALTERNATIVE (einfacher, empfohlen): Alle Dateien als Blob vorher laden ist zu teuer. Stattdessen: FileViewerModal bekommt eine `loadFile` Callback-Funktion die eine URL -> Blob-URL konvertiert. Oder: Nur die aktuelle Datei anzeigen, Swipe laedt naechste on-demand.

       DER EXECUTOR soll den pragmatischsten Ansatz waehlen:
       - Chat-Dateien: files-Array mit API-Pfaden aufbauen, FileViewerModal laedt per fetch on-demand
       - Die `url` in FileItem kann entweder blob: oder ein relativer API-Pfad sein
       - FileViewerModal prüft: wenn url nicht blob: startet → `api.get(url, {responseType:'blob'})` und dann `URL.createObjectURL`

    7. `openImageWithFileOpener` Funktion und den nativen FileOpener/FileViewer Code ENTFERNEN — alles geht jetzt ueber den In-App FileViewerModal
    8. `openInAppViewer` Funktion ENTFERNEN (durch handleFileClick ersetzt)
    9. MessageBubble Prop von `onImageClick={handleImageOrFileClick}` zu `onFileClick={handleFileClick}` aendern

    KEINE Unicode Emojis. Echte Umlaute in Fehlermeldungen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "shared/FileViewerModal" frontend/src/components/chat/ChatRoom.tsx
    - grep -q "onFileClick" frontend/src/components/chat/MessageBubble.tsx
    - grep -rL "chat/modals/FileViewerModal" frontend/src/components/chat/ChatRoom.tsx (keine Referenz mehr zum alten Modal)
    - grep -q "handleFileClick\|handleFileOpen" frontend/src/components/chat/ChatRoom.tsx
    - grep -q "getMimeFromFileName\|getMimeFromExt" frontend/src/components/chat/MessageBubble.tsx
  </acceptance_criteria>
  <done>
    Chat nutzt neues FileViewerModal aus shared/. MessageBubble ruft onFileClick mit filePath, fileName, mimeType auf. ChatRoom baut files-Array und oeffnet FileViewerModal. Kein nativer FileOpener mehr fuer Chat-Dateien. TypeScript kompiliert fehlerfrei.
  </done>
</task>

<task type="auto">
  <name>Task 2: Material-Integration — TeamerMaterialDetailPage auf neues FileViewerModal umstellen</name>
  <files>frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx, frontend/src/components/chat/modals/FileViewerModal.tsx</files>
  <read_first>
    - frontend/src/components/shared/FileViewerModal.tsx (neues Modal aus Plan 01)
    - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx (komplett — openFile Funktion, useIonModal Pattern)
    - frontend/src/components/chat/modals/FileViewerModal.tsx (altes Modal — pruefen ob noch irgendwo importiert)
  </read_first>
  <action>
    **TeamerMaterialDetailPage.tsx aendern (per D-12):**
    1. Import aendern: `FileViewerModal` von `'../../shared/FileViewerModal'` statt `'../../chat/modals/FileViewerModal'`
    2. Import `FileItem` Type aus dem neuen FileViewerModal
    3. `viewerDataRef` ersetzen durch: `const viewerRef = useRef<{ files: FileItem[]; initialIndex: number }>({ files: [], initialIndex: 0 })`
    4. useIonModal aktualisieren (gleiche Struktur wie Chat):
       ```typescript
       const [presentFileViewer, dismissFileViewer] = useIonModal(FileViewerModal, {
         get files() { return viewerRef.current.files; },
         get initialIndex() { return viewerRef.current.initialIndex; },
         onClose: () => {
           dismissFileViewer();
           viewerRef.current.files.forEach(f => {
             if (f.url.startsWith('blob:')) URL.revokeObjectURL(f.url);
           });
           viewerRef.current = { files: [], initialIndex: 0 };
         }
       });
       ```
    5. `openFile` Funktion vereinfachen:
       - Nativen FileOpener/FileViewer Code ENTFERNEN
       - Stattdessen: Datei per `api.get(/material/files/${file.stored_name}, {responseType:'blob'})` laden
       - Blob-URL erstellen
       - Alle Material-Dateien als FileItem-Array aufbauen (per D-07: Swipe durch alle Material-Dateien):
         ```typescript
         const files: FileItem[] = (material?.files || []).map(f => ({
           url: `/api/material/files/${f.stored_name}`,  // API-Pfad, Viewer laedt lazy
           fileName: f.original_name,
           mimeType: f.mime_type
         }));
         // Angeklickte Datei: Blob-URL setzen (schon geladen)
         const clickedIdx = (material?.files || []).findIndex(f => f.id === file.id);
         if (clickedIdx >= 0) {
           files[clickedIdx] = { url: blobUrl, fileName: file.original_name, mimeType: file.mime_type };
         }
         viewerRef.current = { files, initialIndex: Math.max(0, clickedIdx) };
         presentFileViewer({ cssClass: 'file-viewer-modal' });
         ```
    6. `openInAppViewer` Hilfsfunktion ENTFERNEN (nicht mehr noetig)
    7. `@capacitor/file-viewer` und `@capacitor-community/file-opener` Imports ENTFERNEN (nicht mehr genutzt)
    8. Haptics beibehalten beim Datei-Oeffnen

    **Altes FileViewerModal (chat/modals/FileViewerModal.tsx):**
    - Pruefen ob es noch von anderen Dateien importiert wird (grep nach "chat/modals/FileViewerModal")
    - Wenn KEINE anderen Imports mehr: Datei NICHT loeschen (kein Breaking Change), aber einen Kommentar hinzufuegen: `// DEPRECATED: Ersetzt durch shared/FileViewerModal.tsx`
    - Wenn noch andere Imports: Diese auch umstellen (unwahrscheinlich)

    KEINE Unicode Emojis. Echte Umlaute.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "shared/FileViewerModal" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
    - grep -rL "chat/modals/FileViewerModal" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx (keine Referenz mehr zum alten Modal)
    - grep -q "files:" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
    - grep -q "DEPRECATED\|deprecated" frontend/src/components/chat/modals/FileViewerModal.tsx
  </acceptance_criteria>
  <done>
    Material nutzt neues FileViewerModal aus shared/. Tap auf Datei oeffnet Fullscreen-Viewer mit Swipe durch alle Material-Dateien. Kein nativer FileOpener/FileViewer mehr. Altes FileViewerModal als deprecated markiert. TypeScript kompiliert fehlerfrei.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` kompiliert fehlerfrei
- Chat: MessageBubble ruft onFileClick statt onImageClick
- Chat: ChatRoom importiert shared/FileViewerModal statt chat/modals/FileViewerModal
- Material: TeamerMaterialDetailPage importiert shared/FileViewerModal
- Altes FileViewerModal als deprecated markiert
- Kein nativer FileOpener/FileViewer Code mehr in Chat oder Material
</verification>

<success_criteria>
- Beide Einsatzorte (Chat + Material) nutzen das neue universelle FileViewerModal
- Swipe-Kontext: Chat = alle Dateien im Chat, Material = alle Dateien des Materials
- Altes Modal nicht mehr in Benutzung
- TypeScript fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/69-universeller-datei-viewer/69-02-SUMMARY.md`
</output>
