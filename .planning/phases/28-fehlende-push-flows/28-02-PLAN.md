---
phase: 28-fehlende-push-flows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/pushService.js
  - backend/routes/activities.js
  - backend/routes/konfi-managment.js
  - backend/routes/events.js
autonomous: true
requirements:
  - FLW-03
  - FLW-04

must_haves:
  truths:
    - "Konfi erhaelt Push-Benachrichtigung wenn ein neues Level erreicht wird"
    - "Level-Check passiert NACH COMMIT bei jeder Punkte-Vergabe"
    - "Kein Level-Down Push bei Punkte-Abzug"
    - "FLW-04 ist als covered markiert (Badge-System deckt Meilensteine ab)"
  artifacts:
    - path: "backend/services/pushService.js"
      provides: "checkAndSendLevelUp Helper-Methode"
      contains: "checkAndSendLevelUp"
    - path: "backend/routes/activities.js"
      provides: "Level-Check bei Antrag-Genehmigung und Aktivitaets-Zuweisung"
      contains: "checkAndSendLevelUp"
    - path: "backend/routes/konfi-managment.js"
      provides: "Level-Check bei Bonus-Punkte-Vergabe"
      contains: "checkAndSendLevelUp"
    - path: "backend/routes/events.js"
      provides: "Level-Check bei Event-Attendance"
      contains: "checkAndSendLevelUp"
  key_links:
    - from: "backend/routes/activities.js"
      to: "backend/services/pushService.js"
      via: "PushService.checkAndSendLevelUp nach Badge-Check"
      pattern: "checkAndSendLevelUp"
    - from: "backend/routes/konfi-managment.js"
      to: "backend/services/pushService.js"
      via: "PushService.checkAndSendLevelUp nach Badge-Check"
      pattern: "checkAndSendLevelUp"
    - from: "backend/routes/events.js"
      to: "backend/services/pushService.js"
      via: "PushService.checkAndSendLevelUp nach Badge-Check"
      pattern: "checkAndSendLevelUp"
---

<objective>
Level-Up Push implementieren (FLW-03) und FLW-04 als covered markieren.

Purpose: Konfis werden motiviert wenn sie ein neues Level erreichen — sofortige Push-Benachrichtigung nach Punkte-Vergabe.
Output: Wiederverwendbare checkAndSendLevelUp Helper-Methode, integriert an allen 4 Punkte-Vergabe-Stellen.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-fehlende-push-flows/28-CONTEXT.md
@.planning/phases/28-fehlende-push-flows/28-RESEARCH.md

@backend/services/pushService.js
@backend/routes/activities.js
@backend/routes/konfi-managment.js
@backend/routes/events.js

<interfaces>
<!-- Bestehende Methode die nie aufgerufen wird -->

From backend/services/pushService.js (Zeile 527-546):
```javascript
static async sendLevelUpToKonfi(db, konfiId, levelName, levelTitle, levelIcon, levelId = null) {
  // Sendet Push: "Level Up! {icon}" / "Herzlichen Glueckwunsch! Du hast Level "{title}" erreicht!"
  // data.type = 'level_up'
}
```

<!-- Punkte-Vergabe-Stellen wo Level-Check eingebaut werden muss -->

activities.js Zeile 338-339 (Antrag genehmigt):
```javascript
// Nach COMMIT, nach Badge-Check
if (status === 'approved') {
  newBadges = await checkAndAwardBadges(db, request.konfi_id);
  // HIER: Level-Check einbauen
}
```

activities.js Zeile 459-461 (Aktivitaet zugewiesen):
```javascript
// Nach COMMIT, nach Badge-Check
const badgeResult = await checkAndAwardBadges(db, konfiId);
// HIER: Level-Check einbauen (konfiId, req.user.organization_id)
```

konfi-managment.js Zeile 501-509 (Bonus-Punkte):
```javascript
// Nach COMMIT, nach Badge-Check
const newBadges = await checkAndAwardBadges(db, req.params.id);
// HIER: Level-Check einbauen (req.params.id, req.user.organization_id)
```

events.js Zeile 1334-1341 (Event-Attendance):
```javascript
// Nach COMMIT, nach Badge-Check
const newBadges = await checkAndAwardBadges(db, eventData.user_id);
// HIER: Level-Check einbauen (eventData.user_id, req.user.organization_id)
```

<!-- Level-Daten Struktur -->
```sql
-- levels Tabelle: id, name, title, icon, points_required, organization_id, is_active
-- konfi_profiles: current_level_id (FK zu levels.id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: checkAndSendLevelUp Helper in pushService.js erstellen</name>
  <files>backend/services/pushService.js</files>
  <action>
In pushService.js eine neue statische Helper-Methode checkAndSendLevelUp VOR sendLevelUpToKonfi einbauen (ca. Zeile 525):

```javascript
/**
 * Level-Check nach Punkte-Vergabe: Vergleicht aktuelles Level mit gespeichertem Level
 * und sendet Level-Up Push falls aufgestiegen.
 * Wird an allen Punkte-Vergabe-Stellen aufgerufen (activities, konfi-managment, events).
 */
static async checkAndSendLevelUp(db, konfiId, organizationId) {
  try {
    // 1. Aktuelle Punkte und gespeichertes Level holen
    const { rows: [profile] } = await db.query(
      'SELECT gottesdienst_points, gemeinde_points, current_level_id FROM konfi_profiles WHERE user_id = $1',
      [konfiId]
    );
    if (!profile) return;

    const totalPoints = (profile.gottesdienst_points || 0) + (profile.gemeinde_points || 0);

    // 2. Alle aktiven Levels der Organisation holen (aufsteigend nach Punkten)
    const { rows: levels } = await db.query(
      'SELECT * FROM levels WHERE organization_id = $1 AND is_active = true ORDER BY points_required ASC',
      [organizationId]
    );
    if (levels.length === 0) return;

    // 3. Hoechstes erreichtes Level berechnen
    let newLevel = null;
    for (const level of levels) {
      if (totalPoints >= level.points_required) {
        newLevel = level;
      }
    }

    // 4. Vergleich mit gespeichertem Level — nur wenn Level AUFGESTIEGEN
    if (newLevel && newLevel.id !== profile.current_level_id) {
      // Pruefen ob neues Level HOEHER ist (nicht Level-Down bei Punkte-Abzug)
      const oldLevel = levels.find(l => l.id === profile.current_level_id);
      if (oldLevel && newLevel.points_required <= oldLevel.points_required) {
        // Level-Down oder gleiches Level — kein Push, aber Level-ID updaten
        await db.query(
          'UPDATE konfi_profiles SET current_level_id = $1 WHERE user_id = $2',
          [newLevel.id, konfiId]
        );
        return;
      }

      // Level-ID updaten
      await db.query(
        'UPDATE konfi_profiles SET current_level_id = $1 WHERE user_id = $2',
        [newLevel.id, konfiId]
      );

      // Level-Up Push senden
      await this.sendLevelUpToKonfi(
        db, konfiId, newLevel.name, newLevel.title, newLevel.icon, newLevel.id
      );
    }
  } catch (error) {
    console.error('checkAndSendLevelUp error:', error);
    // Fehler nicht weiterwerfen — Level-Check darf Punkte-Vergabe nicht blockieren
  }
}
```

WICHTIG:
- Fehler werden geloggt aber NICHT geworfen (darf Punkte-Vergabe nicht blockieren)
- Level-Down wird erkannt und NICHT gepusht
- current_level_id wird in BEIDEN Faellen (up und down) aktualisiert
- Push-Type 'level_up' ist bereits in der Registry eingetragen (pruefen, ggf. ergaenzen)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "checkAndSendLevelUp\|sendLevelUpToKonfi" backend/services/pushService.js</automated>
  </verify>
  <done>checkAndSendLevelUp existiert als statische Methode in pushService.js, prueft Level-Aufstieg, aktualisiert current_level_id, und sendet Level-Up Push nur bei Aufstieg.</done>
</task>

<task type="auto">
  <name>Task 2: Level-Check an allen 4 Punkte-Vergabe-Stellen integrieren + FLW-04 markieren</name>
  <files>backend/routes/activities.js, backend/routes/konfi-managment.js, backend/routes/events.js</files>
  <action>
An allen 4 Punkte-Vergabe-Stellen den Level-Check NACH dem Badge-Check einbauen. Pattern ueberall gleich:

```javascript
// Level-Check NACH Badge-Check
try {
  await PushService.checkAndSendLevelUp(db, konfiId, req.user.organization_id);
} catch (levelErr) {
  console.error('Level-up check failed:', levelErr);
}
```

Konkrete Stellen:

1. **activities.js (Antrag genehmigt, ca. Zeile 339):**
   Nach `newBadges = await checkAndAwardBadges(db, request.konfi_id);` einfuegen:
   ```javascript
   try {
     await PushService.checkAndSendLevelUp(db, request.konfi_id, req.user.organization_id);
   } catch (levelErr) {
     console.error('Level-up check failed:', levelErr);
   }
   ```

2. **activities.js (Aktivitaet zugewiesen, ca. Zeile 460):**
   Nach `const badgeResult = await checkAndAwardBadges(db, konfiId);` einfuegen:
   ```javascript
   try {
     await PushService.checkAndSendLevelUp(db, konfiId, req.user.organization_id);
   } catch (levelErr) {
     console.error('Level-up check failed:', levelErr);
   }
   ```

3. **konfi-managment.js (Bonus-Punkte, ca. Zeile 503):**
   Nach `const newBadges = await checkAndAwardBadges(db, req.params.id);` (innerhalb des try-Blocks) einfuegen:
   ```javascript
   try {
     await PushService.checkAndSendLevelUp(db, parseInt(req.params.id), req.user.organization_id);
   } catch (levelErr) {
     console.error('Level-up check failed:', levelErr);
   }
   ```
   Beachte: req.params.id ist ein String, muss ggf. als parseInt uebergeben werden falls checkAndSendLevelUp eine Number erwartet. Pruefe ob die bestehenden Aufrufe (sendBonusPointsToKonfi) auch req.params.id als String uebergeben — falls ja, konsistent bleiben.

4. **events.js (Event-Attendance, ca. Zeile 1335):**
   Nach `const newBadges = await checkAndAwardBadges(db, eventData.user_id);` einfuegen:
   ```javascript
   try {
     await PushService.checkAndSendLevelUp(db, eventData.user_id, req.user.organization_id);
   } catch (levelErr) {
     console.error('Level-up check failed:', levelErr);
   }
   ```

PushService ist in allen drei Route-Dateien bereits importiert (activities.js:5, konfi-managment.js:6, events.js:5).

FLW-04: Keine Code-Aenderung noetig. Badge-System (sendBadgeEarnedToKonfi) deckt Punkte-Meilensteine bereits ab. Im SUMMARY dokumentieren dass FLW-04 als "covered by existing badge system" gilt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "checkAndSendLevelUp" backend/routes/activities.js backend/routes/konfi-managment.js backend/routes/events.js</automated>
  </verify>
  <done>checkAndSendLevelUp wird an allen 4 Punkte-Vergabe-Stellen aufgerufen (2x activities.js, 1x konfi-managment.js, 1x events.js). FLW-04 als "covered by existing badge system" dokumentiert.</done>
</task>

</tasks>

<verification>
- checkAndSendLevelUp Methode existiert in pushService.js
- Methode wird an 4 Stellen aufgerufen: activities.js (2x), konfi-managment.js (1x), events.js (1x)
- Kein Level-Down Push (nur Aufstieg loest Push aus)
- current_level_id wird bei Level-Aenderung aktualisiert
- FLW-04 als covered dokumentiert
</verification>

<success_criteria>
- FLW-03: Bei jeder Punkte-Vergabe (Antrag, Zuweisung, Bonus, Event-Attendance) wird geprueft ob ein Level-Up stattfand und ggf. Push gesendet
- FLW-04: Als "covered by existing badge system" markiert — kein separater Punkte-Meilenstein Push noetig
</success_criteria>

<output>
After completion, create `.planning/phases/28-fehlende-push-flows/28-02-SUMMARY.md`
</output>
