---
phase: 112-konfi-aktivitaeten-profil-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/RequestsView.tsx
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/konfi/modals/ActivityRequestModal.tsx
autonomous: true
requirements: [KAV-01, KPR-01, KPR-02, MAB-01]
must_haves:
  truths:
    - "Aktivitaeten-Liste (RequestsView) nutzt flex-div statt IonItem-Wrapper mit 12px Card-Padding"
    - "Profil-Listen (Meine Wrappeds, Letzte Aktivitaeten, Konto-Einstellungen) nutzen 12px Card-Padding"
    - "Meine-Wrappeds hat kein Padding unten wenn nur ein Wrapped vorhanden"
    - "ActivityRequestModal Cards haben 12px Padding"
  artifacts:
    - path: "frontend/src/components/konfi/views/RequestsView.tsx"
      provides: "Aktivitaeten-Liste im neuen Listen-Stil"
      contains: "padding.*12"
    - path: "frontend/src/components/konfi/views/ProfileView.tsx"
      provides: "Profil-Listen im neuen Stil, Wrapped-Padding conditional"
      contains: "wrappedHistory.length === 1"
    - path: "frontend/src/components/konfi/modals/ActivityRequestModal.tsx"
      provides: "Modal mit 12px Card-Abstaenden"
      contains: "padding.*12"
  key_links:
    - from: "frontend/src/components/konfi/views/RequestsView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS-Klassen app-list-item"
      pattern: "app-list-item"
    - from: "frontend/src/components/konfi/views/ProfileView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS-Klassen app-list-item"
      pattern: "app-list-item"
---

<objective>
Listen-Pattern und Card-Padding in den verbleibenden Konfi-Views und Modalen an das Design-System anpassen.

Purpose: Konsistentes Listen-Pattern (flex-div statt IonItem-Wrapper, 12px Card-Padding) in allen Konfi-Views.
Output: RequestsView, ProfileView und ActivityRequestModal mit einheitlichem Listen-Stil.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-konfi-aktivitaeten-profil-modal/112-CONTEXT.md

@frontend/src/components/konfi/modals/PointsHistoryModal.tsx (Referenz-Pattern)
@frontend/src/theme/variables.css

<interfaces>
<!-- Referenz-Pattern aus PointsHistoryModal.tsx: Listen als flex-div, 12px CardContent padding -->

Listen-Container:
```tsx
<IonCardContent style={{ padding: '12px' }}>
  <div style={{ display: 'flex', flexDirection: 'column' }}>
    {items.map((item) => (
      <div key={item.id} className="app-list-item app-list-item--variant">
        <div className="app-list-item__row">
          <div className="app-list-item__main">
            <div className="app-icon-circle app-icon-circle--variant">
              <IonIcon icon={...} />
            </div>
            <div className="app-list-item__content">
              <div className="app-list-item__title">...</div>
              <div className="app-list-item__meta">...</div>
            </div>
          </div>
        </div>
      </div>
    ))}
  </div>
</IonCardContent>
```

Wichtig: KEIN IonItem-Wrapper um app-list-item divs, KEIN IonList innerhalb CardContent.
CSS-Klasse app-list-item hat bereits 8px margin-bottom via variables.css (letztes Kind 0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequestsView und ProfileView Listen-Stil anpassen</name>
  <files>
    frontend/src/components/konfi/views/RequestsView.tsx
    frontend/src/components/konfi/views/ProfileView.tsx
  </files>
  <action>
**RequestsView.tsx (KAV-01, per D-01):**
Die RequestsView nutzt IonItemSliding/IonItem als Wrapper um die app-list-item divs. Das ist ein bekanntes Pattern das in anderen Views bereits auf flex-div umgestellt wurde.

Aenderungen:
1. Im `ListSection` die Items von IonItemSliding+IonItem auf pure div umstellen (wie PointsHistoryModal)
2. ABER: Die Swipe-to-Delete Funktion (IonItemSliding) muss erhalten bleiben -- das ist ein Ionic-Feature das IonItemSliding braucht. Daher NUR den inneren IonItem-Wrapper entfernen und durch einen klickbaren div ersetzen, aber IonItemSliding behalten fuer die Swipe-Actions.
3. Falls IonItemSliding ohne IonItem nicht funktioniert: IonItem-Wrapper beibehalten, aber sicherstellen dass --padding-start 0 und --inner-padding-end 0 gesetzt sind (ist bereits der Fall). In dem Fall nur den CardContent-Padding auf 12px setzen.
4. WICHTIGER: Pruefen ob die ListSection-Komponente intern bereits 12px Padding nutzt. Falls nicht, die ListSection mit entsprechendem Padding konfigurieren.

**ProfileView.tsx (KPR-01, per D-02 + KPR-02, per D-03):**

1. **Meine Wrappeds** (Zeile ~510): IonCardContent padding von 16px auf 12px aendern. Conditional: wenn `wrappedHistory.length === 1`, dann paddingBottom auf 0 setzen (per D-03).
   ```tsx
   <IonCardContent style={{ padding: wrappedHistory.length === 1 ? '12px 12px 0 12px' : '12px' }}>
   ```

2. **Letzte Aktivitaeten** (Zeile ~573-614): Die IonList+IonItem Struktur auf flex-div umstellen:
   - IonList entfernen, durch `<div style={{ display: 'flex', flexDirection: 'column' }}>` ersetzen
   - Jeden IonItem durch einen div mit `className="app-list-item"` ersetzen
   - Die inline-gestylten Kreis-Icons auf `app-icon-circle` CSS-Klassen umstellen
   - IonLabel durch `app-list-item__content` / `app-list-item__title` / `app-list-item__meta` ersetzen
   - Farbvarianten: badge=`app-list-item--warning`, event=`app-list-item--info`, activity=`app-list-item--success`, request=`app-list-item--warning`
   - IonCardContent padding auf 12px setzen

3. **Konto-Einstellungen** (Zeile ~617-782): IonCardContent padding von 16px auf 12px. Die innere IonList+IonItem Wrapper entfernen:
   - Die aeussere `<IonList lines="none">` durch `<div style={{ display: 'flex', flexDirection: 'column' }}>` ersetzen
   - Jeden IonItem-Wrapper (mit den ganzen --background, --padding-start etc. CSS-Vars) entfernen
   - Die app-list-item divs direkt in den flex-Container setzen (sie haben bereits die korrekte Struktur)
   - Die marginBottom: '8px' auf den IonItems wird nicht mehr gebraucht -- app-list-item hat bereits 8px margin-bottom via CSS (letztes Kind 0)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- RequestsView Listen haben 12px Card-Padding (KAV-01)
- ProfileView: Meine Wrappeds hat 12px Padding, kein Padding unten bei 1 Wrapped (KPR-02)
- ProfileView: Letzte Aktivitaeten nutzt flex-div statt IonList/IonItem (KPR-01)
- ProfileView: Konto-Einstellungen nutzt flex-div statt IonList/IonItem Wrapper (KPR-01)
- TypeScript kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: ActivityRequestModal Card-Abstaende anpassen</name>
  <files>
    frontend/src/components/konfi/modals/ActivityRequestModal.tsx
  </files>
  <action>
**ActivityRequestModal.tsx (MAB-01, per D-04):**

Alle IonCardContent-Elemente im Modal auf konsistente 12px Padding umstellen:

1. **Aktivitaet-waehlen Card** (Zeile ~300): `padding: '0'` -- hier ist 0 korrekt weil das Accordion eigenes Padding hat. NICHT aendern.

2. **Datum-Card** (Zeile ~397): IonCardContent hat default-Padding. Explizit `style={{ padding: '12px' }}` setzen.

3. **Anmerkungen-Card** (Zeile ~424): IonCardContent hat default-Padding. Explizit `style={{ padding: '12px' }}` setzen.

4. **Foto-Card** (Zeile ~448): IonCardContent hat default-Padding. Explizit `style={{ padding: '12px' }}` setzen.

5. Innerhalb der Aktivitaeten-Liste im Accordion (Zeile ~315, `padding: '0 16px 16px'`): Auf `padding: '0 12px 12px'` aendern fuer Konsistenz.

6. IonItem-Wrapper innerhalb der Cards (Datum, Anmerkungen): Diese haben `--background: transparent`. Beibehalten, da sie IonDatetimeButton/IonTextarea wrappen und Ionic-Bindings benoetigen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Alle Cards im ActivityRequestModal haben konsistente 12px Padding (MAB-01)
- Accordion-Content hat 12px horizontales Padding
- TypeScript kompiliert fehlerfrei
  </done>
</task>

</tasks>

<verification>
1. TypeScript Build: `cd frontend && npx tsc --noEmit` -- keine Fehler
2. Vite Build: `cd frontend && npm run build` -- erfolgreicher Build
3. Visuell: RequestsView, ProfileView und ActivityRequestModal haben einheitlich 12px Card-Padding
4. Profil Meine-Wrappeds: Bei nur 1 Wrapped kein Padding unten
</verification>

<success_criteria>
- Alle 4 Requirements (KAV-01, KPR-01, KPR-02, MAB-01) sind umgesetzt
- Listen nutzen flex-div statt IonList/IonItem Wrapper (wo moeglich)
- Card-Padding konsistent 12px
- Meine-Wrappeds conditional Padding bei 1 Wrapped
- Frontend baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/112-konfi-aktivitaeten-profil-modal/112-01-SUMMARY.md`
</output>
