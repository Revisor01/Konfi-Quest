---
phase: 08-super-admin-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/components/admin/pages/AdminSettingsPage.tsx
autonomous: true
requirements:
  - SUI-01
  - SUI-02

must_haves:
  truths:
    - "Super-Admin sieht nach Login nur 2 Tabs: Organisationen und Profil/Settings"
    - "Super-Admin sieht NICHT die Tabs Konfis, Chat, Events oder Antraege"
    - "Super-Admin kann sein eigenes Profil aufrufen und App-Einstellungen aendern"
    - "Super-Admin kann Organisationen verwalten (bestehende Funktionalitaet)"
    - "Normaler org_admin sieht weiterhin alle 5 Tabs wie bisher"
  artifacts:
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Separate TabBar und Routing fuer super_admin mit 2 Tabs"
      contains: "isSuperAdmin"
    - path: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      provides: "Reduzierte Settings-Seite fuer super_admin ohne Inhalt-Block"
      contains: "super_admin"
  key_links:
    - from: "frontend/src/components/layout/MainTabs.tsx"
      to: "user.role_name"
      via: "isSuperAdmin Variable aus useApp()"
      pattern: "role_name.*super_admin"
    - from: "frontend/src/components/layout/MainTabs.tsx"
      to: "AdminOrganizationsPage, AdminSettingsPage, AdminProfilePage"
      via: "Route Definitionen innerhalb super_admin IonTabs Block"
      pattern: "isSuperAdmin.*IonTab"
    - from: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      to: "user.role_name"
      via: "Bedingte Anzeige der Inhalt-Sektion"
      pattern: "role_name.*super_admin"
---

<objective>
Super-Admin TabBar und Routing auf Organisations-Verwaltung und Profil/Settings einschraenken.

Purpose: Super-Admins verwalten nur Organisationen und ihr eigenes Konto. Sie brauchen keinen Zugriff auf Konfis, Chat, Events oder Antraege -- diese Tabs sollen nicht sichtbar sein.
Output: MainTabs.tsx rendert fuer super_admin eine eigene IonTabs-Struktur mit nur 2 Tabs (Organisationen + Profil). AdminSettingsPage blendet den Inhalt-Block (Aktivitaeten, Kategorien, etc.) fuer super_admin aus.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@frontend/src/components/layout/MainTabs.tsx
@frontend/src/components/admin/pages/AdminSettingsPage.tsx
@frontend/src/contexts/AppContext.tsx

<interfaces>
<!-- Bestehende Interfaces aus dem Codebase -->

From frontend/src/contexts/AppContext.tsx:
```typescript
interface User {
  id: number;
  type: 'admin' | 'konfi' | 'user';
  display_name: string;
  username?: string;
  email?: string;
  organization?: string;
  organization_id?: number;
  roles?: string[];
  role_name?: string;  // 'super_admin' | 'org_admin' | 'teamer'
  jahrgang?: string;
  is_super_admin?: boolean;
}
```

From frontend/src/components/layout/MainTabs.tsx (Zeile 166):
```typescript
const isSuperAdmin = user.role_name === 'super_admin';
```
Diese Variable existiert bereits und wird fuer die Redirect-Logik genutzt (Zeile 174, 205).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Super-Admin TabBar und Routing in MainTabs.tsx einschraenken</name>
  <files>frontend/src/components/layout/MainTabs.tsx</files>
  <action>
In MainTabs.tsx einen dritten Rendering-Pfad fuer super_admin einfuegen, VOR dem bestehenden admin-Block. Die Logik am Ende der Komponente (ab Zeile 168) aendern von:

```
return user.type === 'admin' ? ( ... admin tabs ... ) : ( ... konfi tabs ... )
```

zu:

```
return isSuperAdmin ? ( ... super_admin tabs ... ) : user.type === 'admin' ? ( ... admin tabs ... ) : ( ... konfi tabs ... )
```

Der super_admin-Block bekommt:
1. **IonRouterOutlet** mit nur diesen Routes:
   - `/admin` -> Redirect zu `/admin/organizations`
   - `/admin/organizations` -> AdminOrganizationsPage
   - `/admin/settings` -> AdminSettingsPage
   - `/admin/profile` -> AdminProfilePage
   - `/admin/users` -> AdminUsersPage (erreichbar ueber Settings > Verwaltung > Benutzer:innen)
   - `/` -> Redirect zu `/admin/organizations`

2. **IonTabBar** mit genau 2 Tabs:
   - Tab "Organisationen" mit `business` Icon, href="/admin/organizations"
   - Tab "Profil" mit `person` Icon, href="/admin/settings"

Wichtig:
- Den `business` Icon-Import oben ergaenzen (aus ionicons/icons)
- Der super_admin-Block braucht KEINEN ModalProvider (kein Chat, keine Konfis)
  -> Doch: AdminOrganizationsPage nutzt useIonModal, also ModalProvider BEIBEHALTEN
- Die useEffect-Hooks fuer pendingRequestsCount und pendingEventsCount haben bereits einen `user?.type === 'admin'` Check, aber laden trotzdem unnoetig Daten fuer super_admin. Fuer den super_admin-Fall in beiden useEffects den `isSuperAdmin`-Check ergaenzen: `if (user?.type === 'admin' && !isSuperAdmin)` -- damit werden keine Konfis/Events-Daten geladen.
- Das `isSuperAdmin` ist bereits auf Zeile 166 definiert, aber innerhalb der Komponente nach den useEffects. Es muss VOR die useEffects verschoben werden (direkt nach `const { badgeCount } = useBadge();`), damit die useEffects darauf zugreifen koennen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Super-Admin sieht nur 2 Tabs (Organisationen + Profil). Normaler org_admin sieht weiterhin alle 5 Tabs. Super-Admin wird beim Login zu /admin/organizations weitergeleitet. Nur die relevanten Routes (organizations, settings, profile, users) sind erreichbar.</done>
</task>

<task type="auto">
  <name>Task 2: AdminSettingsPage Inhalt-Block fuer Super-Admin ausblenden</name>
  <files>frontend/src/components/admin/pages/AdminSettingsPage.tsx</files>
  <action>
In AdminSettingsPage.tsx den "BLOCK 2: Inhalt" (Zeilen 206-282) mit einer Rollenpruefung umschliessen, sodass er NUR fuer org_admin (und teamer, falls relevant) angezeigt wird, NICHT fuer super_admin.

Konkret: Den gesamten IonList-Block "Inhalt" (Aktivitaeten, Kategorien, Jahrgaenge, Level-System, Badges) in eine Bedingung wrappen:

```tsx
{user?.role_name !== 'super_admin' && (
  <IonList inset={true} className="app-segment-wrapper">
    {/* BLOCK 2: Inhalt -- bestehender Code bleibt unveraendert */}
  </IonList>
)}
```

Folgende Bloecke BLEIBEN fuer super_admin sichtbar (keine Aenderung noetig):
- Profil-Block (Zeile 98-122) -- zeigt bereits korrekt "Super-Admin" an
- Verwaltung-Block (Zeile 124-176) -- hat bereits `role_name === 'org_admin' || role_name === 'super_admin'` Check
- System-Administration-Block (Zeile 178-204) -- hat bereits `role_name === 'super_admin'` Check
- Konto-Block (Zeile 284-335) -- Profil + Push-Benachrichtigungen
- Abmelden-Button (Zeile 337-348)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Super-Admin sieht auf der Settings-Seite: Profil-Info, Verwaltung (Benutzer, Punkte-Ziele, Konfis einladen), System-Administration (Organisationen), Konto (Profil, Push), Abmelden. Der Inhalt-Block (Aktivitaeten, Kategorien, Jahrgaenge, Level, Badges) ist NICHT sichtbar.</done>
</task>

</tasks>

<verification>
1. TypeScript-Kompilierung ohne Fehler: `cd frontend && npx tsc --noEmit`
2. Vite Build ohne Fehler: `cd frontend && npm run build`
3. Manuelle Pruefung (optional): Als super_admin einloggen -> nur 2 Tabs sichtbar, Settings zeigt keine Inhalt-Sektion
</verification>

<success_criteria>
- Super-Admin sieht nach Login genau 2 Tabs: "Organisationen" und "Profil"
- Super-Admin sieht NICHT: Konfis, Chat, Events, Antraege Tabs
- Super-Admin kann Organizations-Seite oeffnen und verwalten
- Super-Admin kann Settings oeffnen mit Verwaltung, System-Administration, Konto, Abmelden (kein Inhalt-Block)
- Super-Admin kann Profil-Seite aufrufen
- org_admin sieht weiterhin alle 5 Tabs und alle Settings-Sektionen wie bisher
- TypeScript kompiliert ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/08-super-admin-ui/08-01-SUMMARY.md`
</output>
