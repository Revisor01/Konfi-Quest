---
phase: 90-backend-cleanup-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api.ts
  - frontend/src/services/websocket.ts
  - backend/server.js
  - backend/services/emailService.js
  - backend/routes/events.js
  - backend/package.json
  - backend/migrations/add_idempotency_keys.sql
  - backend/migrations/add_invite_codes.sql
  - backend/migrations/add_push_foundation.sql
  - backend/services/losungService.js
  - backend/routes/konfi.js
  - backend/routes/teamer.js
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CLN-03, CLN-04, CLN-05, CLN-06]

must_haves:
  truths:
    - "Frontend API-URL und WebSocket-URL sind ueber VITE_API_URL konfigurierbar"
    - "Backend SMTP-Config hat keinen hardcodierten IP-Fallback mehr"
    - "QR_SECRET ist eigene Pflicht-ENV ohne Fallback auf JWT_SECRET"
    - "Keine SQLite-Skripte mehr in package.json"
    - "Alle Migrationen haben numerisches Praefix"
    - "Losung-Abruf liegt in eigenem Service statt dupliziert in konfi.js und teamer.js"
    - "Server faehrt bei SIGTERM sauber herunter (DB-Pool + HTTP-Server)"
  artifacts:
    - path: "frontend/src/services/api.ts"
      provides: "VITE_API_URL Konfiguration"
      contains: "VITE_API_URL"
    - path: "frontend/src/services/websocket.ts"
      provides: "VITE_API_URL fuer WebSocket"
      contains: "VITE_API_URL"
    - path: "backend/server.js"
      provides: "SIGTERM-Handler, SMTP ohne IP-Fallback"
      contains: "SIGTERM"
    - path: "backend/services/losungService.js"
      provides: "Extrahierter Losung-Service"
      contains: "fetchTageslosung"
    - path: "backend/routes/events.js"
      provides: "QR_SECRET als Pflicht-ENV"
      contains: "QR_SECRET"
  key_links:
    - from: "backend/routes/konfi.js"
      to: "backend/services/losungService.js"
      via: "require('./services/losungService') statt inline Losung-Code"
      pattern: "losungService"
    - from: "backend/routes/teamer.js"
      to: "backend/services/losungService.js"
      via: "require('./services/losungService') statt inline Losung-Code"
      pattern: "losungService"
---

<objective>
ENV-Konfiguration, Cleanup und Graceful Shutdown: Frontend-URLs konfigurierbar machen, hardcodierte IP-Fallbacks entfernen, SQLite-Reste loeschen, Migrationen umbenennen, Losung-Service extrahieren, SIGTERM-Handler vervollstaendigen.

Purpose: Backend ist staging-faehig konfiguriert und enthaelt keine veralteten Artefakte mehr.
Output: Konfigurierbare URLs, saubere Migrations-Namen, extrahierter losungService, vollstaendiger Shutdown.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENV-Konfiguration + Fallbacks entfernen</name>
  <files>frontend/src/services/api.ts, frontend/src/services/websocket.ts, backend/server.js, backend/services/emailService.js, backend/routes/events.js</files>
  <read_first>
    - frontend/src/services/api.ts (komplett -- API_BASE_URL Zeile 6)
    - frontend/src/services/websocket.ts (komplett -- WS_URL Zeile 6)
    - backend/server.js (Zeilen 162-190 -- SMTP_CONFIG)
    - backend/services/emailService.js (Zeilen 25-40 -- SMTP host)
    - backend/routes/events.js (Zeilen 1-15 -- QR_SECRET)
  </read_first>
  <action>
    **CONF-01 -- Frontend VITE_API_URL:**

    In frontend/src/services/api.ts Zeile 6:
    ```typescript
    const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://konfi-quest.de/api';
    ```
    Die bestehende Zeile `const API_BASE_URL = 'https://konfi-quest.de/api';` ersetzen.

    In frontend/src/services/websocket.ts Zeile 6:
    ```typescript
    const WS_URL = import.meta.env.VITE_API_URL
      ? import.meta.env.VITE_API_URL.replace(/\/api$/, '')
      : 'https://konfi-quest.de';
    ```
    Die bestehende Zeile `const WS_URL = 'https://konfi-quest.de';` ersetzen.
    Den Kommentar "Hardcoded URL wie in api.ts" entfernen oder anpassen.

    **CONF-02 -- SMTP ohne hardcodierte IP:**

    In backend/server.js Zeile 167:
    ```javascript
    host: process.env.SMTP_HOST || 'server.godsapp.de',
    ```
    Statt `'213.109.162.132'` den Hostnamen verwenden.

    In backend/services/emailService.js Zeile 31:
    ```javascript
    host: process.env.SMTP_HOST || 'server.godsapp.de',
    ```
    Gleiche Aenderung.

    **CONF-03 -- QR_SECRET als Pflicht-ENV:**

    In backend/routes/events.js Zeile 10:
    ```javascript
    const QR_SECRET = process.env.QR_SECRET;
    if (!QR_SECRET) {
      console.error('FATAL: QR_SECRET Umgebungsvariable fehlt!');
      process.exit(1);
    }
    ```
    Den Fallback `|| process.env.JWT_SECRET` entfernen. Beim Server-Start sofort abbrechen wenn nicht gesetzt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "VITE_API_URL" frontend/src/services/api.ts frontend/src/services/websocket.ts && grep "213.109" backend/server.js backend/services/emailService.js ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VITE_API_URL" frontend/src/services/api.ts` liefert mindestens 1
    - `grep -c "VITE_API_URL" frontend/src/services/websocket.ts` liefert mindestens 1
    - `grep -c "213.109" backend/server.js` liefert 0
    - `grep -c "213.109" backend/services/emailService.js` liefert 0
    - `grep "QR_SECRET.*JWT_SECRET" backend/routes/events.js` liefert 0 Treffer (kein Fallback mehr)
    - `grep -c "process.exit(1)" backend/routes/events.js` liefert mindestens 1 (Pflicht-Check)
  </acceptance_criteria>
  <done>Frontend-URLs konfigurierbar via VITE_API_URL, SMTP ohne IP-Fallback, QR_SECRET ohne JWT_SECRET-Fallback</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup + losungService + SIGTERM</name>
  <files>backend/package.json, backend/migrations/add_idempotency_keys.sql, backend/migrations/add_invite_codes.sql, backend/migrations/add_push_foundation.sql, backend/services/losungService.js, backend/routes/konfi.js, backend/routes/teamer.js, backend/server.js</files>
  <read_first>
    - backend/package.json (Zeilen 1-15 -- scripts)
    - backend/migrations/ (Dateiliste -- ls)
    - backend/routes/konfi.js (Zeilen 1404-1555 -- tageslosung Endpoint)
    - backend/routes/teamer.js (Zeilen 740-810 -- tageslosung Endpoint)
    - backend/server.js (Zeilen 577-592 -- SIGINT Handler)
  </read_first>
  <action>
    **CLN-03 -- SQLite-Skripte entfernen:**

    In backend/package.json die Scripts entfernen:
    - `"start:sqlite": "node backend/backup_sqlite/server.js"` -- loeschen
    - `"dev:sqlite": "nodemon backend/backup_sqlite/server.js"` -- loeschen
    - `"migrate": "node ../migrate-sqlite-to-postgres.js"` -- loeschen

    **CLN-04 -- Migrationen umbenennen:**

    Die drei Dateien in backend/migrations/ umbenennen:
    - `add_idempotency_keys.sql` -> `078_add_idempotency_keys.sql`
    - `add_invite_codes.sql` -> `079_add_invite_codes.sql`
    - `add_push_foundation.sql` -> `080_add_push_foundation.sql`

    Nummerierung: Die hoechste existierende Nummer ist 077. Daher 078, 079, 080.

    **CLN-05 -- losungService extrahieren:**

    Neue Datei backend/services/losungService.js erstellen:
    ```javascript
    /**
     * Losung-Service: Tageslosung abrufen mit DB-Cache
     */
    async function fetchTageslosung(db, translation) {
      const today = new Date().toISOString().split('T')[0];

      // Cache pruefen
      const { rows: [cachedVerse] } = await db.query(
        'SELECT verse_data FROM daily_verses WHERE date = $1 AND translation = $2',
        [today, translation]
      );

      if (cachedVerse) {
        return { data: cachedVerse.verse_data, translation, cached: true };
      }

      // Von API abrufen
      const fetch = (await import('node-fetch')).default;
      const losungApiKey = process.env.LOSUNG_API_KEY;
      if (!losungApiKey) {
        throw new Error('LOSUNG_API_KEY Umgebungsvariable fehlt');
      }

      const apiUrl = `https://losung.konfi-quest.de/api/?api_key=${losungApiKey}&translation=${translation}`;
      const response = await fetch(apiUrl, { timeout: 5000 });

      if (!response.ok) {
        throw new Error(`Losungen API error: ${response.status}`);
      }

      const losungData = await response.json();
      if (!losungData.success) {
        throw new Error('Losungen API returned error');
      }

      // In Cache speichern
      await db.query(
        'INSERT INTO daily_verses (date, translation, verse_data) VALUES ($1, $2, $3) ON CONFLICT (date, translation) DO UPDATE SET verse_data = $3',
        [today, translation, losungData.data]
      );

      return { data: losungData.data, translation, cached: false };
    }

    module.exports = { fetchTageslosung };
    ```

    Dann in backend/routes/konfi.js den tageslosung-Endpoint (Zeilen 1404-1555) vereinfachen:
    - Am Anfang der Datei: `const { fetchTageslosung } = require('../services/losungService');`
    - Den Endpoint-Body ersetzen durch Aufruf von `fetchTageslosung(db, translation)` mit try/catch und Fallback-Logik (DB-Cache Fallback bei API-Fehler beibehalten).
    - Den inline fetch-Code und die duplizierte Logik entfernen.

    Gleich in backend/routes/teamer.js den tageslosung-Endpoint (Zeilen 744-808) vereinfachen:
    - Am Anfang der Datei: `const { fetchTageslosung } = require('../services/losungService');`
    - Endpoint-Body durch `fetchTageslosung(db, 'LUT')` ersetzen mit try/catch.

    **CLN-06 -- SIGTERM-Handler:**

    In backend/server.js nach dem bestehenden SIGINT-Handler (Zeile 582-592) einen SIGTERM-Handler hinzufuegen:
    ```javascript
    process.on('SIGTERM', async () => {
      console.warn('SIGTERM empfangen - Graceful Shutdown...');
      server.close(async () => {
        console.warn('HTTP-Server geschlossen.');
        try {
          await db.end();
          console.warn('Datenbankverbindung geschlossen.');
        } catch (err) {
          console.error('Fehler beim Schliessen der Datenbankverbindung:', err.message);
        }
        process.exit(0);
      });

      // Timeout nach 10 Sekunden erzwingen
      setTimeout(() => {
        console.error('Shutdown-Timeout erreicht - erzwinge Beendigung');
        process.exit(1);
      }, 10000);
    });
    ```

    Auch den bestehenden SIGINT-Handler erweitern: `server.close()` vor `db.end()` aufrufen (gleiche Struktur wie SIGTERM).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "sqlite" backend/package.json ; ls backend/migrations/078* backend/migrations/079* backend/migrations/080* 2>/dev/null ; test -f backend/services/losungService.js && echo "losungService exists" ; grep "SIGTERM" backend/server.js ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sqlite" backend/package.json` liefert 0
    - `grep -c "migrate" backend/package.json` liefert 0 (das alte migrate Script)
    - `ls backend/migrations/078_add_idempotency_keys.sql` existiert
    - `ls backend/migrations/079_add_invite_codes.sql` existiert
    - `ls backend/migrations/080_add_push_foundation.sql` existiert
    - `ls backend/migrations/add_idempotency_keys.sql 2>/dev/null` existiert NICHT mehr
    - `test -f backend/services/losungService.js` ist true
    - `grep -c "fetchTageslosung" backend/services/losungService.js` liefert mindestens 1
    - `grep -c "fetchTageslosung" backend/routes/konfi.js` liefert mindestens 1
    - `grep -c "fetchTageslosung" backend/routes/teamer.js` liefert mindestens 1
    - `grep -c "SIGTERM" backend/server.js` liefert mindestens 1
    - `grep -c "server.close" backend/server.js` liefert mindestens 1
  </acceptance_criteria>
  <done>SQLite-Skripte entfernt, Migrationen mit numerischem Praefix, losungService extrahiert und in konfi.js/teamer.js eingebunden, SIGTERM-Handler mit server.close + db.end implementiert</done>
</task>

</tasks>

<verification>
- `grep -c "sqlite\|migrate" backend/package.json` = 0
- `ls backend/migrations/ | grep -v "^[0-9]"` liefert 0 Treffer (alle haben numerisches Praefix)
- `test -f backend/services/losungService.js` = true
- `grep -c "SIGTERM" backend/server.js` >= 1
- `grep -c "213.109" backend/` = 0 (kein IP-Fallback mehr)
- `grep "QR_SECRET.*JWT_SECRET" backend/routes/events.js` = 0 Treffer
- `grep "VITE_API_URL" frontend/src/services/api.ts` hat Treffer
</verification>

<success_criteria>
1. Frontend API-URL und WS-URL sind via VITE_API_URL konfigurierbar (Fallback bleibt Produktion)
2. Kein hardcodierter IP-Fallback (213.109.162.132) im Backend
3. QR_SECRET ist Pflicht-ENV ohne JWT_SECRET-Fallback
4. Keine SQLite-Skripte in package.json
5. Alle Migrationen haben numerisches Praefix (078, 079, 080)
6. losungService.js existiert und wird von konfi.js + teamer.js verwendet
7. SIGTERM-Handler schliesst HTTP-Server und DB-Pool sauber
</success_criteria>

<output>
After completion, create `.planning/phases/90-backend-cleanup-performance/90-02-SUMMARY.md`
</output>
