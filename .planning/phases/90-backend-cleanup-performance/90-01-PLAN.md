---
phase: 90-backend-cleanup-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi-management.js
  - backend/routes/users.js
  - backend/routes/konfi.js
  - backend/routes/badges.js
autonomous: true
requirements: [PERF-05, PERF-06, PERF-07]

must_haves:
  truths:
    - "bcrypt.hashSync und bcrypt.compareSync kommen nirgends mehr im Backend vor"
    - "Notification-Insert fuer Aktivitaetsantraege nutzt einen einzelnen Bulk-INSERT statt Schleife"
    - "Badge-Pruefung in checkAndAwardBadges laedt activity_count und event_count vorab statt pro Badge"
  artifacts:
    - path: "backend/routes/konfi-management.js"
      provides: "async bcrypt.hash statt hashSync"
      contains: "await bcrypt.hash"
    - path: "backend/routes/users.js"
      provides: "async bcrypt.hash statt hashSync"
      contains: "await bcrypt.hash"
    - path: "backend/routes/konfi.js"
      provides: "Bulk-INSERT fuer Notifications"
      contains: "unnest"
    - path: "backend/routes/badges.js"
      provides: "Vorab-geladene Counts fuer Badge-Pruefung"
      contains: "activityCounts"
  key_links:
    - from: "backend/routes/konfi-management.js"
      to: "bcrypt"
      via: "await bcrypt.hash(password, 10)"
      pattern: "await bcrypt\\.hash\\("
    - from: "backend/routes/konfi.js"
      to: "notifications table"
      via: "single INSERT with unnest"
      pattern: "unnest"
---

<objective>
Performance-kritische Backend-Stellen fixen: bcrypt von sync auf async umstellen (Event-Loop-Blockade), Notification-Insert von Schleife auf Bulk-INSERT, Badge-Pruefung N+1 Queries reduzieren.

Purpose: Event-Loop wird nicht mehr durch bcrypt blockiert, DB-Roundtrips bei Badge-Checks und Notifications sinken deutlich.
Output: Drei geaenderte Route-Dateien ohne sync-bcrypt, mit Bulk-Insert und vorgeladenen Counts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/auth.js (Vorbild fuer async bcrypt)
</context>

<tasks>

<task type="auto">
  <name>Task 1: bcrypt sync durch async ersetzen + Notification Bulk-INSERT</name>
  <files>backend/routes/konfi-management.js, backend/routes/users.js, backend/routes/konfi.js</files>
  <read_first>
    - backend/routes/konfi-management.js (Zeilen 130-145 und 385-400 -- hashSync Stellen)
    - backend/routes/users.js (Zeilen 165-175 und 230-240 -- hashSync Stellen)
    - backend/routes/auth.js (Zeilen 1-50 -- Vorbild fuer async bcrypt)
    - backend/routes/konfi.js (Zeilen 670-700 -- Notification-Schleife fuer Admins)
  </read_first>
  <action>
    **bcrypt async (PERF-05):**

    In backend/routes/konfi-management.js:
    - Zeile 140: `bcrypt.hashSync(password, 10)` ersetzen durch `await bcrypt.hash(password, 10)`
    - Zeile 392: `bcrypt.hashSync(newPassword, 10)` ersetzen durch `await bcrypt.hash(newPassword, 10)`
    - Die umgebenden Funktionen sind bereits async, kein weiterer Umbau noetig.

    In backend/routes/users.js:
    - Zeile 169: `bcrypt.hashSync(password, 10)` ersetzen durch `await bcrypt.hash(password, 10)`
    - Zeile 236: `bcrypt.hashSync(password, 10)` ersetzen durch `await bcrypt.hash(password, 10)`
    - Die umgebenden Funktionen sind bereits async.

    **Notification Bulk-INSERT (PERF-07):**

    In backend/routes/konfi.js, ab Zeile 681 die for-Schleife:
    ```javascript
    for (const admin of admins) {
      await db.query("INSERT INTO notifications ...", [...]);
    }
    ```
    Ersetzen durch einen einzelnen Bulk-INSERT mit unnest:
    ```javascript
    if (admins.length > 0) {
      const adminIds = admins.map(a => a.id);
      await db.query(
        `INSERT INTO notifications (user_id, title, message, type, data, organization_id)
         SELECT unnest($1::int[]), $2, $3, $4, $5, $6`,
        [
          adminIds,
          'Neuer Antrag eingegangen',
          `${konfiData.display_name} hat einen Antrag fuer "${activity.name}" (${activity.points} ${activity.points === 1 ? 'Punkt' : 'Punkte'}) eingereicht.`,
          'new_activity_request',
          JSON.stringify({
            request_id: newRequest.id,
            konfi_id: konfiId,
            konfi_name: konfiData.display_name,
            activity_name: activity.name,
            points: activity.points
          }),
          req.user.organization_id
        ]
      );
    }
    ```
    WICHTIG: Echte Umlaute in Strings verwenden (fuer, nicht fuer).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -rn "hashSync\|compareSync" backend/routes/ | grep -v node_modules | grep -v ".bak" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "hashSync" backend/routes/konfi-management.js` liefert 0 Treffer
    - `grep -rn "hashSync" backend/routes/users.js` liefert 0 Treffer
    - `grep -c "await bcrypt.hash" backend/routes/konfi-management.js` liefert mindestens 2
    - `grep -c "await bcrypt.hash" backend/routes/users.js` liefert mindestens 2
    - `grep -c "unnest" backend/routes/konfi.js` liefert mindestens 1
    - `grep -c "for (const admin" backend/routes/konfi.js` liefert 0 (Schleife entfernt)
  </acceptance_criteria>
  <done>Kein hashSync/compareSync mehr in Routes, Notification-Insert nutzt Bulk-Query statt Schleife</done>
</task>

<task type="auto">
  <name>Task 2: Badge-Pruefung N+1 Queries reduzieren</name>
  <files>backend/routes/badges.js</files>
  <read_first>
    - backend/routes/badges.js (komplett -- 813 Zeilen, besonders checkAndAwardBadges ab Zeile 99 und checkAndAwardTeamerBadges ab Zeile 303)
  </read_first>
  <action>
    **Badge N+1 Query Fix (PERF-06):**

    In checkAndAwardBadges (Konfi-Branch, ab Zeile 117):
    Vor der Badge-Schleife (Zeile 151) die haeufig abgefragten Counts einmalig vorladen:

    ```javascript
    // Vorab-Queries: activity_count, event_count, bonus_count, completed_activities
    const [
      { rows: [activityCountResult] },
      { rows: [eventCountResult] },
      { rows: [bonusCountResult] },
      { rows: completedActivitiesResult }
    ] = await Promise.all([
      db.query("SELECT COUNT(*) as count FROM user_activities WHERE user_id = $1 AND organization_id = $2", [userId, konfi.organization_id]),
      db.query("SELECT COUNT(*) as count FROM event_bookings WHERE user_id = $1 AND attendance_status = 'present' AND organization_id = $2", [userId, konfi.organization_id]),
      db.query("SELECT COUNT(*) as count FROM bonus_points WHERE konfi_id = $1", [userId]),
      db.query("SELECT DISTINCT a.name FROM user_activities ua JOIN activities a ON ua.activity_id = a.id WHERE ua.user_id = $1 AND a.organization_id = $2", [userId, konfi.organization_id])
    ]);

    const preloaded = {
      activityCount: parseInt(activityCountResult.count),
      eventCount: parseInt(eventCountResult.count),
      bonusCount: parseInt(bonusCountResult.count),
      completedActivityNames: completedActivitiesResult.map(r => r.name)
    };
    ```

    Dann im switch-Block die Cases umstellen:
    - `activity_count` (ca. Zeile 200-210): statt DB-Query `preloaded.activityCount + preloaded.eventCount >= badge.criteria_value` verwenden
    - `event_count`: statt DB-Query `preloaded.eventCount >= badge.criteria_value`
    - `bonus_points`: statt DB-Query `preloaded.bonusCount >= badge.criteria_value`
    - `specific_activity`: statt DB-Query `preloaded.completedActivityNames.includes(criteria.required_activity_name)` und bei criteria_value > 1 eine separate Count-Query fuer diese spezifische Activity belassen
    - `activity_combination`: statt DB-Query `preloaded.completedActivityNames` verwenden

    In checkAndAwardTeamerBadges (ab Zeile 303):
    Gleiche Strategie -- activity_count und event_count vorab laden:

    ```javascript
    const [
      { rows: [teamerActCount] },
      { rows: [teamerEvCount] },
      { rows: teamerCompletedActs }
    ] = await Promise.all([
      db.query(`SELECT COUNT(*) as count FROM user_activities ua JOIN activities a ON ua.activity_id = a.id WHERE ua.user_id = $1 AND ua.organization_id = $2 AND a.target_role = 'teamer'`, [userId, organizationId]),
      db.query("SELECT COUNT(*) as count FROM event_bookings WHERE user_id = $1 AND attendance_status = 'present' AND organization_id = $2", [userId, organizationId]),
      db.query(`SELECT DISTINCT a.name FROM user_activities ua JOIN activities a ON ua.activity_id = a.id WHERE ua.user_id = $1 AND a.organization_id = $2 AND a.target_role = 'teamer'`, [userId, organizationId])
    ]);
    ```

    Dann in der Teamer-Badge-Schleife preloaded Werte verwenden statt pro-Badge DB-Queries.

    WICHTIG: `streak` und `time_based` Kriterien behalten ihre eigenen Queries -- diese sind individuell und nicht sinnvoll vorab ladbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && node -e "require('./backend/routes/badges.js')" 2>&1 | head -5 ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "preloaded" backend/routes/badges.js` liefert mindestens 5
    - `grep -c "Promise.all" backend/routes/badges.js` liefert mindestens 2 (Konfi + Teamer Vorab-Queries)
    - Die Badge-Schleife enthaelt keine DB-Queries mehr fuer activity_count, event_count, bonus_points, activity_combination
    - `node -e "require('./backend/routes/badges.js')"` wirft keinen Syntax-Error
  </acceptance_criteria>
  <done>Badge-Pruefung laedt Counts einmalig vorab, N+1 Queries fuer activity/event/bonus/combination eliminiert</done>
</task>

</tasks>

<verification>
- `grep -rn "hashSync\|compareSync" backend/routes/` liefert 0 Treffer
- `grep -c "unnest" backend/routes/konfi.js` >= 1
- `grep -c "preloaded" backend/routes/badges.js` >= 5
- Backend startet ohne Fehler: `cd backend && node -e "require('./routes/badges.js'); require('./routes/konfi.js'); require('./routes/users.js'); require('./routes/konfi-management.js'); console.log('OK')"`
</verification>

<success_criteria>
1. Kein bcrypt.hashSync oder bcrypt.compareSync in backend/routes/
2. Notification-Insert nutzt Bulk-Query (unnest) statt for-Schleife
3. Badge-Pruefung laedt activity/event/bonus Counts vorab statt pro Badge
4. Alle geaenderten Dateien sind syntaktisch korrekt (require wirft keinen Fehler)
</success_criteria>

<output>
After completion, create `.planning/phases/90-backend-cleanup-performance/90-01-SUMMARY.md`
</output>
