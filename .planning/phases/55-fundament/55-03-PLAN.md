---
phase: 55-fundament
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/networkMonitor.ts
  - frontend/src/contexts/AppContext.tsx
  - frontend/src/services/api.ts
autonomous: true
requirements: [NET-01, NET-02, NET-04]

must_haves:
  truths:
    - "App erkennt Online/Offline-Status ueber @capacitor/network"
    - "isOnline ist im AppContext fuer alle Komponenten verfuegbar"
    - "Bei Netzwerkausfall wird der User NICHT ausgeloggt (401-Handler prueft Netzwerkstatus)"
  artifacts:
    - path: "frontend/src/services/networkMonitor.ts"
      provides: "Singleton-Service fuer Netzwerkstatus-Erkennung"
      exports: ["networkMonitor"]
    - path: "frontend/src/contexts/AppContext.tsx"
      provides: "isOnline State fuer alle Komponenten"
      contains: "isOnline"
    - path: "frontend/src/services/api.ts"
      provides: "401-Handler mit Offline-Pruefung"
      contains: "networkMonitor"
  key_links:
    - from: "frontend/src/services/api.ts"
      to: "frontend/src/services/networkMonitor.ts"
      via: "networkMonitor.isOnline Check vor Token-Loeschung"
      pattern: "networkMonitor\\.isOnline"
    - from: "frontend/src/contexts/AppContext.tsx"
      to: "frontend/src/services/networkMonitor.ts"
      via: "subscribe() fuer isOnline State-Updates"
      pattern: "networkMonitor\\.subscribe"
---

<objective>
NetworkMonitor-Singleton erstellen, isOnline in AppContext integrieren, und 401-Handler in api.ts fixen damit Offline nicht zum Logout fuehrt.

Purpose: Ohne Netzwerk-Erkennung loescht die App bei jedem 401 (auch offline-bedingtem) den Token und erzwingt einen erneuten Login. Das ist das Hauptproblem das Phase 55 loest.
Output: networkMonitor.ts, aktualisierter AppContext mit isOnline, gefixter 401-Handler
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-fundament/55-CONTEXT.md
@frontend/src/services/websocket.ts
@frontend/src/services/api.ts
@frontend/src/contexts/AppContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkMonitor Singleton + isOnline in AppContext</name>
  <files>
    frontend/src/services/networkMonitor.ts,
    frontend/src/contexts/AppContext.tsx
  </files>
  <read_first>
    frontend/src/services/websocket.ts,
    frontend/src/contexts/AppContext.tsx
  </read_first>
  <action>
1. `frontend/src/services/networkMonitor.ts` erstellen (per D-11, D-12, D-13):
   - Singleton-Pattern wie websocket.ts (Modul-Level Variable, kein Class noetig)
   - Import: `import { Network } from '@capacitor/network';` und `import { Capacitor } from '@capacitor/core';`
   - Modul-Level State: `let _isOnline: boolean = true;` (optimistisch starten)
   - `type NetworkListener = (isOnline: boolean) => void;`
   - `const _listeners: Set<NetworkListener> = new Set();`
   - `let _initialized = false;`
   - Debounce-Variable: `let _debounceTimer: ReturnType<typeof setTimeout> | null = null;` (Claude's Discretion: Debouncing)
   - `initNetworkMonitor(): Promise<void>`:
     - Wenn _initialized, return
     - Versuche `Network.getStatus()` — setze `_isOnline = status.connected`
     - `Network.addListener('networkStatusChange', (status) => { ... })` — im Callback:
       - Debounce: clearTimeout(_debounceTimer), dann `_debounceTimer = setTimeout(() => { ... }, 300)` (300ms Debounce fuer schnelle Wechsel)
       - Im Timeout: `_isOnline = status.connected; _listeners.forEach(fn => fn(_isOnline));`
     - Fallback fuer Web: `window.addEventListener('online', ...)` und `window.addEventListener('offline', ...)` mit gleichem Debounce-Pattern
     - `_initialized = true;`
   - `subscribe(fn: NetworkListener): () => void`: Fuegt fn zu _listeners hinzu, gibt Unsubscribe-Funktion zurueck
   - Getter: `get isOnline(): boolean` — NEIN, kein Getter auf Modul-Level. Stattdessen: Objekt exportieren.
   - Exportiere als Objekt:
     ```typescript
     export const networkMonitor = {
       get isOnline() { return _isOnline; },
       subscribe,
       init: initNetworkMonitor,
     };
     ```

2. `frontend/src/contexts/AppContext.tsx` aktualisieren (per D-11):
   - Import: `import { networkMonitor } from '../services/networkMonitor';`
   - Neuer State: `const [isOnline, setIsOnline] = useState<boolean>(true);`
   - Neuer useEffect (nach user-unabhaengigen Effects):
     ```typescript
     useEffect(() => {
       networkMonitor.init();
       const unsubscribe = networkMonitor.subscribe((online) => {
         setIsOnline(online);
       });
       // Initialen Status setzen
       setIsOnline(networkMonitor.isOnline);
       return () => { unsubscribe(); };
     }, []);
     ```
   - `isOnline` zum AppContextType Interface hinzufuegen: `isOnline: boolean;`
   - `isOnline` zum value Objekt hinzufuegen
   - KEIN eigener NetworkContext (per D-11 — zu wenig State)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && test -f src/services/networkMonitor.ts && grep -q "isOnline" src/contexts/AppContext.tsx && grep -q "networkMonitor" src/contexts/AppContext.tsx && grep -q "subscribe" src/services/networkMonitor.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - networkMonitor.ts existiert und exportiert `networkMonitor` Objekt mit `isOnline` Getter, `subscribe` Methode und `init` Methode
    - networkMonitor.ts importiert `Network` aus `@capacitor/network`
    - networkMonitor.ts hat Debounce-Logik (setTimeout mit mindestens 200ms)
    - networkMonitor.ts hat Web-Fallback mit `window.addEventListener('online'/'offline')`
    - AppContext.tsx hat `isOnline: boolean` im AppContextType Interface
    - AppContext.tsx hat `const [isOnline, setIsOnline] = useState<boolean>(true)`
    - AppContext.tsx ruft `networkMonitor.init()` und `networkMonitor.subscribe()` in einem useEffect auf
    - AppContext.tsx gibt `isOnline` im value Objekt zurueck
  </acceptance_criteria>
  <done>NetworkMonitor-Singleton erstellt. isOnline State in AppContext verfuegbar fuer alle Komponenten.</done>
</task>

<task type="auto">
  <name>Task 2: 401-Handler Offline-Fix in api.ts</name>
  <files>
    frontend/src/services/api.ts
  </files>
  <read_first>
    frontend/src/services/api.ts,
    frontend/src/services/networkMonitor.ts
  </read_first>
  <action>
1. `frontend/src/services/api.ts` aktualisieren (per D-14, D-15, D-16):
   - Import hinzufuegen: `import { networkMonitor } from './networkMonitor';`
   - Response-Interceptor 401-Block (aktuell Zeilen 25-33) komplett ersetzen:
     ```typescript
     if (error.response?.status === 401) {
       const isLoginRequest = error.config?.url?.includes('/login');
       if (!isLoginRequest) {
         // D-14: Pruefe Netzwerkstatus bevor Token geloescht wird
         if (networkMonitor.isOnline) {
           // D-16: Online und 401 = Token wirklich ungueltig
           clearAuth().then(() => {
             window.location.href = '/';
           });
         } else {
           // D-15: Offline und 401 = Netzwerkproblem, Token behalten
           // Error NICHT weiterreichen — gecachte Daten werden in Phase 56 angezeigt
           console.warn('401 waehrend Offline — Token wird behalten');
           return Promise.reject(error);
         }
       }
     }
     ```
   - Import von `clearAuth` aus tokenStore: `import { getToken, clearAuth } from './tokenStore';`
   - HINWEIS: `getToken` Import sollte schon von Plan 01 Task 2 vorhanden sein. Nur `clearAuth` ergaenzen falls noch nicht importiert. Und `networkMonitor` Import ist neu.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -q "networkMonitor.isOnline" src/services/api.ts && grep -q "networkMonitor" src/services/api.ts && grep -q "clearAuth" src/services/api.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - api.ts importiert `networkMonitor` aus `./networkMonitor`
    - api.ts 401-Handler enthaelt `if (networkMonitor.isOnline)` Check
    - api.ts loescht Token NUR wenn `networkMonitor.isOnline === true` (per D-16)
    - api.ts behaelt Token wenn offline (per D-15) — `console.warn` Meldung vorhanden
    - api.ts importiert `clearAuth` aus `./tokenStore`
    - api.ts enthaelt KEINEN `localStorage` Zugriff mehr
  </acceptance_criteria>
  <done>401-Handler prueft Netzwerkstatus. Offline = Token behalten, Online = Token loeschen und Redirect. Kein falsches Logout mehr bei Netzwerkausfall.</done>
</task>

</tasks>

<verification>
- `grep -q "networkMonitor.isOnline" frontend/src/services/api.ts` findet Offline-Check
- `grep -q "isOnline" frontend/src/contexts/AppContext.tsx` findet State
- `test -f frontend/src/services/networkMonitor.ts` findet Singleton
</verification>

<success_criteria>
- NetworkMonitor erkennt Online/Offline via @capacitor/network mit Web-Fallback
- isOnline State in AppContext fuer alle Komponenten
- 401-Handler loescht Token nur bei echtem Auth-Fehler (online), nicht bei Offline
</success_criteria>

<output>
After completion, create `.planning/phases/55-fundament/55-03-SUMMARY.md`
</output>
