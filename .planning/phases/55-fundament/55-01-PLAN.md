---
phase: 55-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/tokenStore.ts
  - frontend/src/services/migrateStorage.ts
  - frontend/src/main.tsx
  - frontend/src/services/auth.ts
  - frontend/src/services/api.ts
  - frontend/src/contexts/AppContext.tsx
  - frontend/package.json
autonomous: true
requirements: [STR-01, STR-02, STR-03, STR-04]

must_haves:
  truths:
    - "JWT-Token und User-Daten ueberleben einen App-Neustart auf iOS (gespeichert in Capacitor Preferences)"
    - "Bestehende localStorage-Daten werden beim App-Start automatisch migriert"
    - "App startet mit Async Boot und zeigt Auth-Bildschirm waehrend Initialisierung"
    - "Axios Request-Interceptor liest Token synchron aus Memory-Cache"
  artifacts:
    - path: "frontend/src/services/tokenStore.ts"
      provides: "In-Memory-Cache + async Capacitor Preferences fuer Auth-Daten"
      exports: ["getToken", "getUser", "getDeviceId", "getPushTokenTimestamp", "setToken", "setUser", "setDeviceId", "setPushTokenTimestamp", "clearAuth", "initTokenStore"]
    - path: "frontend/src/services/migrateStorage.ts"
      provides: "Einmalige localStorage->Preferences Migration"
      exports: ["migrateToPreferences"]
    - path: "frontend/src/main.tsx"
      provides: "Async Boot-Sequenz"
      contains: "migrateToPreferences"
    - path: "frontend/src/services/auth.ts"
      provides: "Auth-Funktionen via TokenStore statt localStorage"
      contains: "tokenStore"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/services/migrateStorage.ts"
      via: "migrateToPreferences() vor initTokenStore()"
      pattern: "migrateToPreferences.*initTokenStore"
    - from: "frontend/src/services/api.ts"
      to: "frontend/src/services/tokenStore.ts"
      via: "getToken() im Request-Interceptor"
      pattern: "getToken\\(\\)"
    - from: "frontend/src/services/auth.ts"
      to: "frontend/src/services/tokenStore.ts"
      via: "setToken/setUser/clearAuth bei Login/Logout"
      pattern: "import.*tokenStore"
---

<objective>
TokenStore-Service erstellen, Storage-Migration implementieren und Boot-Sequenz in main.tsx auf async umstellen. Core-Services (auth.ts, api.ts, AppContext) auf TokenStore migrieren.

Purpose: iOS WKWebView kann localStorage jederzeit loeschen — Capacitor Preferences sind persistent. Der TokenStore bietet synchrone Reads (fuer Interceptor/Socket.io) mit async Writes nach Preferences.
Output: tokenStore.ts, migrateStorage.ts, aktualisierte main.tsx + auth.ts + api.ts + AppContext.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-fundament/55-CONTEXT.md
@frontend/src/main.tsx
@frontend/src/services/auth.ts
@frontend/src/services/api.ts
@frontend/src/contexts/AppContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TokenStore + Migration + Async Boot + Capacitor Deps</name>
  <files>
    frontend/package.json,
    frontend/src/services/tokenStore.ts,
    frontend/src/services/migrateStorage.ts,
    frontend/src/main.tsx
  </files>
  <read_first>
    frontend/package.json,
    frontend/src/main.tsx,
    frontend/src/services/auth.ts,
    frontend/src/services/api.ts
  </read_first>
  <action>
1. `@capacitor/preferences` und `@capacitor/network` installieren (per D-12):
   ```bash
   cd frontend && npm install @capacitor/preferences @capacitor/network
   ```

2. `frontend/src/services/tokenStore.ts` erstellen (per D-04, D-05, D-06, D-07):
   - In-Memory-Cache-Variablen: `_token: string | null`, `_user: User | null`, `_deviceId: string | null`, `_pushTokenTimestamp: number`
   - User-Interface gleich wie in auth.ts (id, type, display_name, username?, email?, organization?, organization_id?, roles?, role_name?, jahrgang?, is_super_admin?)
   - Synchrone Getter (per D-05): `getToken(): string | null`, `getUser(): User | null`, `getDeviceId(): string | null`, `getPushTokenTimestamp(): number` — lesen nur aus Memory
   - Async Setter (per D-06): `setToken(token: string): Promise<void>` — schreibt in Memory + `Preferences.set({ key: 'konfi_token', value: token })`
   - `setUser(user: User): Promise<void>` — schreibt in Memory + `Preferences.set({ key: 'konfi_user', value: JSON.stringify(user) })`
   - `setDeviceId(id: string): Promise<void>` — Memory + `Preferences.set({ key: 'device_id', value: id })`
   - `setPushTokenTimestamp(ts: number): Promise<void>` — Memory + `Preferences.set({ key: 'push_token_last_refresh', value: ts.toString() })`
   - `clearAuth(): Promise<void>` — setzt _token und _user auf null, ruft `Preferences.remove` fuer konfi_token und konfi_user auf. Device-ID NICHT loeschen.
   - `initTokenStore(): Promise<void>` (per D-07) — laedt alle 4 Keys aus Preferences in Memory-Cache. Bei konfi_user: JSON.parse mit try/catch (bei Parse-Fehler: null + Preferences.remove).
   - Alle Preferences-Aufrufe via `import { Preferences } from '@capacitor/preferences'`

3. `frontend/src/services/migrateStorage.ts` erstellen (per D-08, D-09, D-10):
   - `migrateToPreferences(): Promise<void>`
   - Prueft zuerst `Preferences.get({ key: 'storage_migrated_v1' })` — wenn vorhanden, sofort return (per D-09)
   - Migriert 4 Keys: `konfi_token`, `konfi_user`, `device_id`, `push_token_last_refresh` von localStorage nach Preferences
   - Fuer jeden Key: `const val = localStorage.getItem(key); if (val) await Preferences.set({ key, value: val });`
   - localStorage-Keys werden NICHT geloescht (per D-10 — Fallback fuer Rollback)
   - Am Ende: `Preferences.set({ key: 'storage_migrated_v1', value: 'true' })`
   - Gesamte Funktion in try/catch: Bei Fehler nur console.error, kein throw (App soll trotzdem starten)

4. `frontend/src/main.tsx` aktualisieren (per D-01, D-02):
   - Import: `migrateToPreferences` aus `./services/migrateStorage`, `initTokenStore` aus `./services/tokenStore`
   - Bestehenden synchronen render-Code in async IIFE wrappen:
   ```typescript
   const container = document.getElementById('root');
   const root = createRoot(container!);

   (async () => {
     await migrateToPreferences();
     await initTokenStore();
     root.render(<App />);
   })();
   ```
   - KEIN separater Boot-Screen noetig — per D-02 zeigt die App den bestehenden Auth-Bildschirm. Der render() Call startet die App mit dem normalen Login-Screen der per AppContext entscheidet was angezeigt wird.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -q "@capacitor/preferences" package.json && grep -q "@capacitor/network" package.json && test -f src/services/tokenStore.ts && test -f src/services/migrateStorage.ts && grep -q "migrateToPreferences" src/main.tsx && grep -q "initTokenStore" src/main.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json enthaelt `"@capacitor/preferences"` und `"@capacitor/network"` in dependencies
    - tokenStore.ts exportiert: getToken, getUser, getDeviceId, getPushTokenTimestamp, setToken, setUser, setDeviceId, setPushTokenTimestamp, clearAuth, initTokenStore
    - migrateStorage.ts exportiert: migrateToPreferences
    - migrateStorage.ts enthaelt `storage_migrated_v1` Check
    - migrateStorage.ts enthaelt KEINE `localStorage.removeItem` Aufrufe (per D-10)
    - main.tsx enthaelt `await migrateToPreferences()` VOR `await initTokenStore()`
    - main.tsx enthaelt `root.render` NACH initTokenStore
    - tokenStore.ts verwendet `Preferences` aus `@capacitor/preferences`
    - Alle Setter in tokenStore.ts sind async und schreiben in Memory UND Preferences
    - Alle Getter in tokenStore.ts sind synchron und lesen nur aus Memory
  </acceptance_criteria>
  <done>tokenStore.ts und migrateStorage.ts existieren mit korrekten Exports. main.tsx hat async Boot-Sequenz. @capacitor/preferences und @capacitor/network installiert.</done>
</task>

<task type="auto">
  <name>Task 2: Core-Services auf TokenStore migrieren (auth.ts, api.ts, AppContext.tsx)</name>
  <files>
    frontend/src/services/auth.ts,
    frontend/src/services/api.ts,
    frontend/src/contexts/AppContext.tsx
  </files>
  <read_first>
    frontend/src/services/tokenStore.ts,
    frontend/src/services/auth.ts,
    frontend/src/services/api.ts,
    frontend/src/contexts/AppContext.tsx
  </read_first>
  <action>
1. `frontend/src/services/auth.ts` aktualisieren:
   - Import: `import { getToken, getUser, setToken, setUser, clearAuth, getDeviceId, setDeviceId } from './tokenStore';`
   - `loginWithAutoDetection`: Ersetze `localStorage.setItem('konfi_token', token)` durch `await setToken(token)` und `localStorage.setItem('konfi_user', JSON.stringify(user))` durch `await setUser(user)`
   - `logout`: Ersetze `localStorage.getItem('device_id')` Zugriffe (Zeilen 71, 74) durch `getDeviceId()`. Ersetze die localStorage.setItem('device_id', ...) Fallback-Logik: Wenn getDeviceId() null, dann generiere ID und `await setDeviceId(fallbackId)`. Ersetze `localStorage.removeItem('konfi_token')` und `localStorage.removeItem('konfi_user')` (Zeilen 102-103) durch `await clearAuth()`
   - `checkAuth` wird zu `checkAuthAsync` (per D-03): Signature aendern zu `export const checkAuth = (): User | null => { return getUser(); }` — synchron aus Memory lesen. ZUSAETZLICH: `export const checkAuthAsync = async (): Promise<User | null> => { return getUser(); }` fuer Zukunftssicherheit. Die Funktion bleibt synchron im Kern weil initTokenStore() schon in main.tsx gelaufen ist.
   - KEIN localStorage Import mehr noetig — alle localStorage-Zugriffe entfernen

2. `frontend/src/services/api.ts` aktualisieren:
   - Import: `import { getToken } from './tokenStore';` und `import { networkMonitor } from './networkMonitor';` — ACHTUNG: networkMonitor existiert noch nicht in Wave 1, daher NICHT importieren. Nur TokenStore in diesem Task.
   - Request-Interceptor (Zeile 13-18): Ersetze `localStorage.getItem('konfi_token')` durch `getToken()` (synchron, per D-05)
   - Response-Interceptor 401 (Zeile 25-33): Ersetze `localStorage.removeItem('konfi_token')` und `localStorage.removeItem('konfi_user')` durch Import und Aufruf von `clearAuth()` — ABER clearAuth ist async. Da der Interceptor eine rejected Promise zurueckgibt, einfach `clearAuth()` aufrufen (fire-and-forget, der redirect passiert sofort). Alternativ: `clearAuth().then(() => { window.location.href = '/'; })` statt sofortigem redirect.
   - 401-Handler Offline-Fix kommt in Plan 03 (NET-04), hier nur TokenStore-Migration

3. `frontend/src/contexts/AppContext.tsx` aktualisieren (per D-03):
   - Import: `import { getUser, getDeviceId, setDeviceId, getPushTokenTimestamp, setPushTokenTimestamp } from '../services/tokenStore';`
   - Zeile 93: Ersetze `useState<User | null>(checkAuth())` durch `useState<User | null>(getUser())` — synchron, weil initTokenStore schon gelaufen ist
   - Zeile 42-44 (sendTokenToServer Fallback): Ersetze `localStorage.getItem('device_id')` durch `getDeviceId()` und `localStorage.setItem('device_id', fallbackDeviceId)` durch `await setDeviceId(fallbackDeviceId)`
   - Zeile 266: Ersetze `localStorage.getItem('push_token_last_refresh')` durch `getPushTokenTimestamp().toString()` — nein, getPushTokenTimestamp gibt schon number zurueck: `const lastTokenRefresh = getPushTokenTimestamp();`
   - Zeile 271: Ersetze `localStorage.setItem('push_token_last_refresh', now.toString())` durch `await setPushTokenTimestamp(now)`
   - checkAuth Import aus auth.ts kann entfernt werden, getUser aus tokenStore uebernimmt
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && ! grep -n "localStorage" src/services/auth.ts && ! grep -n "localStorage" src/services/api.ts && grep -q "getToken" src/services/api.ts && grep -q "getUser" src/contexts/AppContext.tsx && grep -q "tokenStore" src/services/auth.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts enthaelt KEINEN `localStorage` Zugriff mehr
    - auth.ts importiert aus `./tokenStore`: getToken, getUser, setToken, setUser, clearAuth, getDeviceId, setDeviceId
    - api.ts Request-Interceptor verwendet `getToken()` statt `localStorage.getItem('konfi_token')`
    - api.ts Response-Interceptor 401 verwendet `clearAuth()` statt `localStorage.removeItem`
    - api.ts enthaelt KEINEN `localStorage` Zugriff mehr
    - AppContext.tsx verwendet `getUser()` statt `checkAuth()` fuer initialen State
    - AppContext.tsx verwendet `getPushTokenTimestamp()` statt `localStorage.getItem('push_token_last_refresh')`
    - AppContext.tsx verwendet `setPushTokenTimestamp()` statt `localStorage.setItem('push_token_last_refresh')`
    - AppContext.tsx verwendet `getDeviceId()` und `setDeviceId()` statt localStorage device_id Zugriffe
    - `npm run build` (im Frontend-Verzeichnis) laeuft ohne TypeScript-Fehler durch — HINWEIS: Build-Check erst nach Plan 02 sinnvoll da andere Dateien noch localStorage verwenden
  </acceptance_criteria>
  <done>auth.ts, api.ts und AppContext.tsx verwenden TokenStore statt localStorage. Keine localStorage-Zugriffe mehr in diesen 3 Dateien.</done>
</task>

</tasks>

<verification>
- `grep -rn "localStorage" frontend/src/services/auth.ts frontend/src/services/api.ts frontend/src/contexts/AppContext.tsx` gibt keine Treffer
- `grep -q "getToken\|getUser\|setToken\|setUser\|clearAuth" frontend/src/services/auth.ts` findet TokenStore-Nutzung
- `grep -q "initTokenStore" frontend/src/main.tsx` findet Boot-Sequenz
- `grep -q "migrateToPreferences" frontend/src/main.tsx` findet Migration
</verification>

<success_criteria>
- TokenStore-Service erstellt mit synchronen Gettern und async Settern
- Migration-Service erstellt mit einmaligem storage_migrated_v1 Flag
- main.tsx hat async Boot: migrateToPreferences -> initTokenStore -> render
- auth.ts, api.ts, AppContext.tsx nutzen TokenStore statt localStorage
- @capacitor/preferences und @capacitor/network als Dependencies installiert
</success_criteria>

<output>
After completion, create `.planning/phases/55-fundament/55-01-SUMMARY.md`
</output>
