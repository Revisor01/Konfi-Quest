---
phase: 16-konfi-views-profil-verlinkungen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/konfi/modals/EditProfileModal.tsx
  - backend/routes/konfi.js
autonomous: true
requirements:
  - KUI-04
  - KUI-06
  - KUI-10
  - KUI-11

must_haves:
  truths:
    - "Konfirmationstermin-Card im Profil ist Lila statt Blau"
    - "Bibelübersetzung-Ändern im Profil speichert erfolgreich (kein Validierungsfehler)"
    - "UnregisterModal ist über EventDetailView erreichbar"
    - "EditProfileModal ist als toter Code entfernt"
  artifacts:
    - path: "frontend/src/components/konfi/views/ProfileView.tsx"
      provides: "Konfirmationstermin-Card mit Lila-Gradient"
      contains: "#5b21b6"
    - path: "backend/routes/konfi.js"
      provides: "Korrekte Validierung fuer bible-translation Endpoint"
      contains: "body('translation')"
  key_links:
    - from: "frontend/src/components/konfi/views/ProfileView.tsx"
      to: "PUT /konfi/bible-translation"
      via: "api.put('/konfi/bible-translation', { translation })"
      pattern: "handleTranslationChange"
---

<objective>
Konfi-Profil-Farbe auf Lila vereinheitlichen, Bibelübersetzung-Bug fixen und toten Code entfernen

Purpose: Phase 16 schliesst die Konfi-Profil-Ansicht ab -- Farbkonsistenz, funktionierende Actions und sauberer Code.
Output: ProfileView mit durchgehend lila Theme, funktionierender Bible-Translation-Endpoint, kein ungenutztes EditProfileModal
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/konfi/views/ProfileView.tsx
@frontend/src/components/konfi/modals/EditProfileModal.tsx
@frontend/src/components/konfi/views/EventDetailView.tsx (zeigt: UnregisterModal ist bereits verlinkt)
@backend/routes/konfi.js

<interfaces>
<!-- Aus SectionHeader.tsx: konfis-Preset ist bereits Lila -->
From frontend/src/components/shared/SectionHeader.tsx:
```typescript
const PRESET_COLORS = {
  konfis: { primary: '#5b21b6', secondary: '#4c1d95' },
  // ...
};
```

<!-- ProfileView verwendet bereits preset="konfis" fuer SectionHeader -->
<!-- Aber Konfirmationstermin-Card hat eigene blaue Farben: #1e3a8a / #1e40af -->
<!-- Ziel: Diese auf Lila (#5b21b6 / #4c1d95) aendern -->

<!-- Backend-Bug: Validierung prueft 'bible_translation' aber Frontend sendet 'translation' -->
From backend/routes/konfi.js:
```javascript
const validateBibleTranslation = [
  body('bible_translation').trim().notEmpty().withMessage('...'), // BUG: falscher Feldname
  handleValidationErrors
];
// Handler benutzt: const { translation } = req.body; // RICHTIG
```

From frontend/src/components/konfi/views/ProfileView.tsx:
```typescript
await api.put('/konfi/bible-translation', { translation }); // sendet 'translation', nicht 'bible_translation'
```

<!-- UnregisterModal ist bereits in EventDetailView.tsx eingebunden via useIonModal -->
From frontend/src/components/konfi/views/EventDetailView.tsx:
```typescript
import UnregisterModal from '../modals/UnregisterModal';
const [presentUnregisterModal, dismissUnregisterModal] = useIonModal(UnregisterModal, { ... });
```

<!-- EditProfileModal wird nirgendwo importiert -- toter Code -->
<!-- Kein PUT /konfi/profile Endpoint im Backend -->
<!-- Funktionalitaet bereits aufgeteilt: ChangeEmailModal, ChangePasswordModal, ActionSheet fuer Bibelübersetzung -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileView Konfirmationstermin-Card von Blau auf Lila + EditProfileModal entfernen</name>
  <files>frontend/src/components/konfi/views/ProfileView.tsx, frontend/src/components/konfi/modals/EditProfileModal.tsx</files>
  <action>
In ProfileView.tsx die Konfirmationstermin-Card Farben aendern:

1. Zeile 267: `background: profile.confirmation_date ? 'linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%)'` aendern zu `background: profile.confirmation_date ? 'linear-gradient(135deg, #5b21b6 0%, #4c1d95 100%)'`

2. Zeile 269: `boxShadow: profile.confirmation_date ? '0 10px 40px rgba(30, 58, 138, 0.3)'` aendern zu `boxShadow: profile.confirmation_date ? '0 10px 40px rgba(91, 33, 182, 0.3)'`

3. Datei `frontend/src/components/konfi/modals/EditProfileModal.tsx` komplett loeschen -- wird nirgendwo importiert, kein Backend-Endpoint existiert, Funktionalitaet ist bereits in einzelne Modals aufgeteilt (ChangeEmailModal, ChangePasswordModal, ActionSheet fuer Bibelübersetzung).

NICHT aendern: SectionHeader preset="konfis" ist bereits korrekt lila. Nur die Konfirmationstermin-Card hat falsche blaue Farben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "#5b21b6" frontend/src/components/konfi/views/ProfileView.tsx && grep -c "#1e3a8a" frontend/src/components/konfi/views/ProfileView.tsx && test ! -f frontend/src/components/konfi/modals/EditProfileModal.tsx && echo "PASS"</automated>
  </verify>
  <done>Konfirmationstermin-Card zeigt Lila-Gradient (#5b21b6/#4c1d95) statt Blau (#1e3a8a/#1e40af). EditProfileModal.tsx geloescht. Kein Blau mehr in ProfileView ausser Event-Aktivitaets-Farbe (#3880ff fuer Events in recent activities bleibt).</done>
</task>

<task type="auto">
  <name>Task 2: Backend bible-translation Validierung fixen</name>
  <files>backend/routes/konfi.js</files>
  <action>
In backend/routes/konfi.js Zeile 27 den Feldnamen in der Validierung korrigieren:

ALT: `body('bible_translation').trim().notEmpty().withMessage('Bibelübersetzung ist erforderlich')`
NEU: `body('translation').trim().notEmpty().withMessage('Bibelübersetzung ist erforderlich')`

Das Frontend sendet `{ translation: 'LUT' }` und der Handler liest `const { translation } = req.body` -- nur die Validierungs-Middleware hat den falschen Feldnamen `bible_translation` statt `translation`.

KUI-10 (UnregisterModal erreichbar): Bereits in EventDetailView.tsx via useIonModal eingebunden. Keine Aenderung noetig -- nur verifizieren dass der Import und die Verlinkung existieren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "body('translation')" backend/routes/konfi.js | head -1 && grep "import UnregisterModal" frontend/src/components/konfi/views/EventDetailView.tsx && echo "PASS"</automated>
  </verify>
  <done>Backend-Validierung prueft korrektes Feld 'translation' (nicht 'bible_translation'). Bibelübersetzung-Ändern im Profil funktioniert ohne Validierungsfehler. UnregisterModal ist in EventDetailView.tsx importiert und via useIonModal verlinkt.</done>
</task>

</tasks>

<verification>
1. ProfileView.tsx enthaelt keine blauen Gradient-Farben (#1e3a8a, #1e40af) mehr in der Konfirmationstermin-Card
2. ProfileView.tsx verwendet Lila-Farben (#5b21b6, #4c1d95) fuer die Konfirmationstermin-Card
3. EditProfileModal.tsx existiert nicht mehr
4. Backend konfi.js validiert `body('translation')` statt `body('bible_translation')`
5. UnregisterModal wird in EventDetailView.tsx importiert und verwendet

```bash
cd /Users/simonluthe/Documents/Konfipoints
# Keine blauen Farben in Konfirmationstermin-Card
grep "#1e3a8a" frontend/src/components/konfi/views/ProfileView.tsx | wc -l  # erwarte: 0
# Lila Farben vorhanden
grep "#5b21b6" frontend/src/components/konfi/views/ProfileView.tsx | wc -l  # erwarte: >= 1
# EditProfileModal geloescht
test ! -f frontend/src/components/konfi/modals/EditProfileModal.tsx && echo "GELOESCHT"
# Backend-Fix
grep "body('translation')" backend/routes/konfi.js | wc -l  # erwarte: >= 1
# UnregisterModal verlinkt
grep "UnregisterModal" frontend/src/components/konfi/views/EventDetailView.tsx | wc -l  # erwarte: >= 2
```
</verification>

<success_criteria>
- Konfirmationstermin-Card im Profil zeigt durchgehend Lila-Gradient (#5b21b6/#4c1d95)
- Bibelübersetzung kann erfolgreich geaendert werden (kein 400-Validierungsfehler)
- UnregisterModal ist ueber EventDetailView erreichbar (bereits implementiert)
- EditProfileModal als toter Code entfernt
- Frontend baut ohne Fehler (kein Import von EditProfileModal irgendwo)
</success_criteria>

<output>
After completion, create `.planning/phases/16-konfi-views-profil-verlinkungen/16-01-SUMMARY.md`
</output>
