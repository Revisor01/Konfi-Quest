---
phase: 59-online-only-buttons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/chat.ts
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/chat/ChatRoom.tsx
autonomous: true
requirements: [OUI-08, OUI-09, OUI-10]

must_haves:
  truths:
    - "Pending Chat-Nachricht zeigt Uhr-Icon neben dem Zeitstempel"
    - "Nach Zustellung verschwindet die Uhr ohne Haekchen"
    - "Fehlgeschlagene Nachricht zeigt rotes Ausrufezeichen mit Tap-Optionen"
  artifacts:
    - path: "frontend/src/types/chat.ts"
      provides: "Message type mit queueStatus Feld"
      contains: "queueStatus"
    - path: "frontend/src/components/chat/MessageBubble.tsx"
      provides: "Uhr-Icon und Fehler-Icon Rendering neben Zeitstempel"
      contains: "timeOutline"
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "ActionSheet fuer fehlgeschlagene Nachrichten"
      contains: "Erneut senden"
  key_links:
    - from: "frontend/src/components/chat/MessageBubble.tsx"
      to: "frontend/src/types/chat.ts"
      via: "Message.queueStatus field"
      pattern: "queueStatus"
    - from: "frontend/src/components/chat/ChatRoom.tsx"
      to: "frontend/src/components/chat/MessageBubble.tsx"
      via: "onRetry and onDeleteQueued callbacks"
      pattern: "onRetry|onDeleteQueued"
---

<objective>
Chat-Nachrichten zeigen Queue-Status als Icon neben dem Zeitstempel. Pending = Uhr, Fehler = rotes Ausrufezeichen mit ActionSheet.

Purpose: Phase 60 (Queue) braucht diese UI-Infrastruktur um queued Messages visuell darzustellen.
Output: Erweiterte Message-Bubble mit Status-Icons, ActionSheet bei Fehler-Tap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-online-only-buttons/59-CONTEXT.md
@frontend/src/types/chat.ts
@frontend/src/components/chat/MessageBubble.tsx
@frontend/src/components/chat/ChatRoom.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message-Type erweitern + MessageBubble Queue-Status UI</name>
  <files>frontend/src/types/chat.ts, frontend/src/components/chat/MessageBubble.tsx</files>
  <read_first>
    - frontend/src/types/chat.ts (Message interface)
    - frontend/src/components/chat/MessageBubble.tsx (komplett)
  </read_first>
  <action>
1. In `frontend/src/types/chat.ts` — Message interface erweitern:
   - Neues optionales Feld: `queueStatus?: 'pending' | 'error'`
   - Neues optionales Feld: `localId?: string` (fuer Queue-Identifikation, wird in Phase 60 genutzt)

2. In `frontend/src/components/chat/MessageBubble.tsx`:
   - Import hinzufuegen: `timeOutline, alertCircleOutline` aus `ionicons/icons`
   - MessageBubbleProps erweitern um:
     - `onRetry?: (message: Message) => void`
     - `onDeleteQueued?: (message: Message) => void`
   - Im Zeitstempel-Bereich (Zeile ~488-495, das `div` mit `fontSize: '0.7rem'`):
     - Nach dem Zeitstempel-Text ein Icon rendern wenn `message.queueStatus` gesetzt:
     - `queueStatus === 'pending'`: `<IonIcon icon={timeOutline}` style `fontSize: '0.75rem', marginLeft: '4px', verticalAlign: 'middle'` />
     - `queueStatus === 'error'`: `<IonIcon icon={alertCircleOutline}` style `fontSize: '0.75rem', marginLeft: '4px', color: '#dc3545', verticalAlign: 'middle', cursor: 'pointer'` onClick stopPropagation + Bubble onClick Tap-Handler aufrufen />
   - Wenn `queueStatus === 'error'`, soll ein Tap auf die GESAMTE Bubble (nicht nur das Icon) die Fehler-Aktionen triggern. Dazu am aeusseren Bubble-div einen onClick hinzufuegen:
     ```
     onClick={(e) => {
       if (message.queueStatus === 'error' && onRetry) {
         // Wird in ChatRoom als ActionSheet gehandelt
         onRetry(message);
       }
     }}
     ```
   - Wenn `queueStatus === 'pending'`: opacity 0.7 auf die gesamte Bubble (inline style auf dem aeusseren Container-div)
   - KEINE Emojis verwenden, nur IonIcon mit ionicons/icons
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "queueStatus" src/types/chat.ts && grep -c "timeOutline" src/components/chat/MessageBubble.tsx && grep -c "alertCircleOutline" src/components/chat/MessageBubble.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "queueStatus" frontend/src/types/chat.ts findet mindestens 2 Treffer (Feld + Typ)
    - grep "timeOutline" frontend/src/components/chat/MessageBubble.tsx findet mindestens 1 Treffer
    - grep "alertCircleOutline" frontend/src/components/chat/MessageBubble.tsx findet mindestens 1 Treffer
    - grep "onRetry" frontend/src/components/chat/MessageBubble.tsx findet mindestens 2 Treffer (Props + Usage)
    - grep "onDeleteQueued" frontend/src/components/chat/MessageBubble.tsx findet mindestens 1 Treffer
    - grep "localId" frontend/src/types/chat.ts findet mindestens 1 Treffer
  </acceptance_criteria>
  <done>Message-Type hat queueStatus + localId Felder, MessageBubble zeigt Uhr bei pending und rotes Ausrufezeichen bei error neben dem Zeitstempel</done>
</task>

<task type="auto">
  <name>Task 2: ChatRoom ActionSheet fuer fehlgeschlagene Nachrichten</name>
  <files>frontend/src/components/chat/ChatRoom.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatRoom.tsx (komplett lesen — besonders die MessageBubble-Verwendung und bestehende useIonActionSheet Nutzung)
  </read_first>
  <action>
1. In `frontend/src/components/chat/ChatRoom.tsx`:
   - Import `useIonActionSheet` falls nicht vorhanden (Ionic Hook)
   - `const [presentActionSheet] = useIonActionSheet()` im Komponenten-Body
   - Neue Handler-Funktion `handleRetryMessage`:
     ```typescript
     const handleRetryMessage = (message: Message) => {
       presentActionSheet({
         header: 'Nachricht fehlgeschlagen',
         buttons: [
           {
             text: 'Erneut senden',
             handler: () => {
               // Phase 60 wird hier die Queue-Retry-Logik einhaengen
               console.log('Retry message:', message.localId);
             }
           },
           {
             text: 'Nachricht löschen',
             role: 'destructive',
             handler: () => {
               // Phase 60 wird hier die Queue-Remove-Logik einhaengen
               console.log('Delete queued message:', message.localId);
             }
           },
           {
             text: 'Abbrechen',
             role: 'cancel'
           }
         ]
       });
     };
     ```
   - Neue Handler-Funktion `handleDeleteQueuedMessage`:
     ```typescript
     const handleDeleteQueuedMessage = (message: Message) => {
       // Phase 60 wird hier die Queue-Remove-Logik einhaengen
       console.log('Delete queued message:', message.localId);
     };
     ```
   - An der MessageBubble-Komponenten-Verwendung (wo `<MessageBubble` gerendert wird) die neuen Props durchreichen:
     - `onRetry={handleRetryMessage}`
     - `onDeleteQueued={handleDeleteQueuedMessage}`
   - KEINE Emojis verwenden
   - useIonActionSheet ist der korrekte Ionic-Hook (NICHT useIonModal oder presentAlert)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "handleRetryMessage" src/components/chat/ChatRoom.tsx && grep -c "Erneut senden" src/components/chat/ChatRoom.tsx && grep -c "onRetry=" src/components/chat/ChatRoom.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "handleRetryMessage" frontend/src/components/chat/ChatRoom.tsx findet mindestens 2 Treffer
    - grep "Erneut senden" frontend/src/components/chat/ChatRoom.tsx findet mindestens 1 Treffer
    - grep "onRetry=" frontend/src/components/chat/ChatRoom.tsx findet mindestens 1 Treffer
    - grep "onDeleteQueued=" frontend/src/components/chat/ChatRoom.tsx findet mindestens 1 Treffer
    - npx tsc --noEmit zeigt keine Fehler in ChatRoom.tsx oder MessageBubble.tsx
  </acceptance_criteria>
  <done>ChatRoom zeigt ActionSheet mit "Erneut senden" und "Nachricht loeschen" bei Tap auf fehlgeschlagene Nachricht. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — keine TypeScript-Fehler
- `grep -r "queueStatus" frontend/src/types/chat.ts` — Feld existiert
- `grep -r "timeOutline\|alertCircleOutline" frontend/src/components/chat/MessageBubble.tsx` — Icons importiert und verwendet
- `grep -r "Erneut senden" frontend/src/components/chat/ChatRoom.tsx` — ActionSheet Text vorhanden
</verification>

<success_criteria>
- Message type hat queueStatus und localId Felder
- MessageBubble zeigt Uhr-Icon bei pending, rotes Ausrufezeichen bei error
- Pending-Nachrichten haben opacity 0.7
- ChatRoom hat ActionSheet mit "Erneut senden" / "Nachricht loeschen" bei error-Tap
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/59-online-only-buttons/59-01-SUMMARY.md`
</output>
