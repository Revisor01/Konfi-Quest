---
phase: 04-admin-views-core
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/components/konfi/modals/ActivityRequestModal.tsx
  - frontend/src/components/konfi/modals/ChangeEmailModal.tsx
  - frontend/src/components/konfi/modals/PointsHistoryModal.tsx
  - frontend/src/components/chat/modals/PollModal.tsx
  - frontend/src/components/admin/modals/ChangeEmailModal.tsx
  - frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx
  - frontend/src/components/admin/modals/LevelManagementModal.tsx
  - frontend/src/components/chat/constants.ts
autonomous: true
requirements: [ADM-03, ADM-06]

must_haves:
  truths:
    - "Konfi-Modals und Chat-Modals haben reduzierte Inline-Styles"
    - "Admin-Modals haben reduzierte Inline-Styles"
    - "CSS-Klassen aus Plan 04-01 werden in Modals genutzt wo sinnvoll"
    - "Alle bestehende Funktionalitaet ist vollstaendig erhalten"
    - "Alle Modals nutzen weiterhin useIonModal (kein isOpen-Pattern)"
  artifacts:
    - path: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      provides: "Punkte-Historie-Modal mit CSS-Klassen statt Inline-Styles"
      contains: "app-list-item"
    - path: "frontend/src/components/admin/modals/LevelManagementModal.tsx"
      provides: "Level-Modal mit CSS-Klassen statt Inline-Styles"
      contains: "app-list-item"
  key_links:
    - from: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS-Klassen app-list-item und app-info-box"
      pattern: "app-list-item|app-info-box"
---

<objective>
Inline-Styles in Konfi-Modals, Chat-Modals und Admin-Modals durch die CSS-Klassen aus Plan 04-01 ersetzen.

Purpose: Visuell konsistentes Styling in allen Modals mit wartbaren CSS-Klassen statt verstreuter Inline-Styles.
Output: 8 bereinigten Modal-Dateien mit CSS-Klassen statt Inline-Styles.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-views-core/04-CONTEXT.md
@.planning/phases/04-admin-views-core/04-01-SUMMARY.md

Referenz fuer bestehende Patterns:
@frontend/src/theme/variables.css

<interfaces>
<!-- Neue CSS-Klassen aus Plan 04-01 (in variables.css): -->

.app-status-chip                     -- Status-Badge Basis
.app-status-chip--success/warning/danger/info/purple/neutral  -- Farbvarianten
.app-settings-item                   -- Settings-Eintrag (cursor, flex, gap)
.app-settings-item__title            -- Settings-Titel (fontWeight, fontSize, color)
.app-settings-item__subtitle         -- Settings-Untertitel (fontSize, color)
.app-info-box                        -- Info-Hinweis Basis
.app-info-box--blue/neutral          -- Farbvarianten

<!-- Bestehende CSS-Klassen (bereits in Nutzung): -->
.app-list-item + Varianten           -- Listenelemente
.app-icon-circle + Groessen          -- Runde Icons
.app-corner-badge                    -- Eselsohr Badge
.app-card                            -- Karten Container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Konfi-Modals und Chat-Modal Inline-Styles bereinigen</name>
  <files>
    frontend/src/components/konfi/modals/ActivityRequestModal.tsx
    frontend/src/components/konfi/modals/ChangeEmailModal.tsx
    frontend/src/components/konfi/modals/PointsHistoryModal.tsx
    frontend/src/components/chat/modals/PollModal.tsx
    frontend/src/components/chat/constants.ts
  </files>
  <action>
Inline-Styles in den Konfi-Modals und Chat-Modal durch CSS-Klassen ersetzen:

**A) ActivityRequestModal.tsx:**
- Info-Boxen -> `app-info-box`
- Settings-artige Items -> `app-settings-item`
- Listen-Items mit Farbborder -> `app-list-item` Varianten
- IonItem Custom Properties (`--background`, `--padding-start`) bleiben inline
- Formular-Styles (Ionic-spezifisch) bleiben inline

**B) ChangeEmailModal.tsx (Konfi):**
- Aehnlich wie ActivityRequestModal: CSS-Klassen wo moeglich
- Formular-Input-Styles bleiben (Ionic-spezifisch)

**C) PointsHistoryModal.tsx:**
- Diese hat die meisten Inline-Styles (~34)
- Punkte-Eintraege -> `app-list-item` Varianten
- Info-Hinweise -> `app-info-box`
- Punkte-Werte -> `app-points-display` wo passend
- Die dynamischen Farben (basierend auf Punkte-Typ) bleiben inline

**D) PollModal.tsx (Chat):**
- Info-Boxen und Formular-Styles bereinigen
- IonItem Custom Properties bleiben

**E) constants.ts (Chat):**
- Falls Inline-Style-Konstanten vorhanden sind, durch CSS-Klassen-Referenzen ersetzen

WICHTIG:
- Dynamische Werte (berechnete Farben, bedingte Opacity) bleiben IMMER inline
- Ionic CSS Custom Properties (--background, --padding-start, etc.) bleiben inline
- `style={{ flex: 1 }}` bleibt inline (Layout-spezifisch)
- Keine Emojis
- Echte Umlaute in deutschen UI-Texten
- ALLE bestehende Funktionalitaet muss erhalten bleiben
- Alle Modals muessen weiterhin useIonModal nutzen (kein isOpen-Pattern!)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && echo "=== Konfi/Chat Modal Inline-Style Counts ===" && for f in konfi/modals/ActivityRequestModal.tsx konfi/modals/ChangeEmailModal.tsx konfi/modals/PointsHistoryModal.tsx chat/modals/PollModal.tsx; do echo "$f: $(grep -c 'style={{' src/components/$f)"; done && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Konfi-Modals und Chat-Modal haben reduzierte Inline-Styles und nutzen CSS-Klassen wo sinnvoll. Alle Funktionalitaet ist erhalten. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Admin-Modals Inline-Styles bereinigen</name>
  <files>
    frontend/src/components/admin/modals/ChangeEmailModal.tsx
    frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx
    frontend/src/components/admin/modals/LevelManagementModal.tsx
  </files>
  <action>
Inline-Styles in Admin-Modals durch CSS-Klassen ersetzen:

**A) ChangeEmailModal.tsx (Admin):**
- CSS-Klassen wo moeglich, Ionic Custom Properties bleiben
- `app-list-item` und `app-icon-circle` Klassen nutzen wo noch nicht vorhanden
- Formular-Input-Styles bleiben (Ionic-spezifisch)

**B) ChangeRoleTitleModal.tsx:**
- Aehnlich wie ChangeEmailModal
- Settings-artige Items -> `app-settings-item` wo passend

**C) LevelManagementModal.tsx:**
- Die groesste Admin-Modal-Datei
- Level-Eintraege -> `app-list-item` Varianten
- Info-Hinweise -> `app-info-box`
- Farb-Picker und Formular-Styles bleiben (Ionic-spezifisch)
- `app-icon-circle` Klassen fuer Level-Icons

WICHTIG:
- Dynamische Werte bleiben inline
- Ionic CSS Custom Properties bleiben inline
- Keine Emojis
- Echte Umlaute in deutschen UI-Texten
- ALLE bestehende Funktionalitaet muss erhalten bleiben
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && echo "=== Admin Modal Inline-Style Counts ===" && for f in admin/modals/ChangeEmailModal.tsx admin/modals/ChangeRoleTitleModal.tsx admin/modals/LevelManagementModal.tsx; do echo "$f: $(grep -c 'style={{' src/components/$f)"; done && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Admin-Modals haben reduzierte Inline-Styles und nutzen CSS-Klassen wo sinnvoll. Alle Funktionalitaet ist erhalten. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. Konfi-Modals: Inline-Style-Count pro Modal ist reduziert
2. Admin-Modals: Inline-Style-Count pro Modal ist reduziert
3. Keine Emojis in den Dateien
4. `npx tsc --noEmit` kompiliert ohne Fehler
5. Alle Modals nutzen weiterhin useIonModal (kein isOpen-Pattern)
</verification>

<success_criteria>
- Konfi-Modals und Chat-Modals haben reduzierte Inline-Styles
- Admin-Modals haben reduzierte Inline-Styles
- CSS-Klassen aus Plan 04-01 werden in Modals genutzt wo sinnvoll
- Alle bestehende Funktionalitaet ist in allen Modals vollstaendig erhalten
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-views-core/04-04-SUMMARY.md`
</output>
