---
phase: 04-admin-views-core
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/components/admin/pages/AdminSettingsPage.tsx
  - frontend/src/components/admin/pages/AdminGoalsPage.tsx
  - frontend/src/components/admin/pages/AdminInvitePage.tsx
  - frontend/src/components/admin/KonfisView.tsx
  - frontend/src/components/admin/ActivitiesView.tsx
  - frontend/src/components/admin/EventsView.tsx
  - frontend/src/components/admin/BadgesView.tsx
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/admin/pages/AdminCategoriesPage.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
autonomous: true
requirements: [ADM-02, ADM-03, ADM-06]
# NOTE: ADM-01 (KonfisView SectionHeader/ListSection), ADM-04 (BadgesPage SectionHeader),
# und ADM-05 (JahrgaengePage SectionHeader) wurden bereits in Phase 3 implementiert.
# Dieser Plan bereinigt nur noch Inline-Styles in diesen Dateien.
# IonItemSliding ist bereits in allen relevanten Views vorhanden (ActivitiesView, BadgesView,
# AdminCategoriesPage, AdminJahrgaengeePage, KonfisView, EventsView).
# IonRefresher ist bereits in allen Admin-Pages (Wrapper-Pages) vorhanden.

must_haves:
  truths:
    - "AdminSettingsPage nutzt app-settings-item CSS-Klassen statt Inline-Styles fuer alle Eintraege"
    - "AdminGoalsPage und AdminInvitePage nutzen CSS-Klassen statt Inline-Styles wo moeglich"
    - "Admin-Views (KonfisView, ActivitiesView, EventsView, BadgesView) nutzen app-segment-wrapper und app-search-bar CSS-Klassen"
    - "Inline-Styles in Admin-Dateien sind insgesamt deutlich unter 100 (Ziel per User Decision)"
    - "Alle bestehende Funktionalitaet ist vollstaendig erhalten"
    - "IonItemSliding ist bereits in allen Views mit loeschbaren/editierbaren Items vorhanden (Phase 3)"
    - "IonRefresher ist bereits in allen Admin-Pages (Wrapper-Pages) vorhanden (Phase 3)"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      provides: "Settings-Page mit CSS-Klassen statt Inline-Styles"
      contains: "app-settings-item"
    - path: "frontend/src/components/admin/ActivitiesView.tsx"
      provides: "Activities-View mit app-segment-wrapper und app-search-bar"
      contains: "app-segment-wrapper"
    - path: "frontend/src/components/admin/KonfisView.tsx"
      provides: "Konfis-View mit app-progress-bar CSS-Klassen"
      contains: "app-progress-bar"
  key_links:
    - from: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS-Klassen app-settings-item"
      pattern: "app-settings-item"
    - from: "frontend/src/components/admin/ActivitiesView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS-Klassen app-segment-wrapper und app-search-bar"
      pattern: "app-segment-wrapper|app-search-bar"
---

<objective>
Inline-Styles in Admin-Pages und Admin-Views durch die CSS-Klassen aus Plan 04-01 ersetzen. Ziel: deutlich unter 100 Inline-Styles in Admin-Dateien (per User Decision).

HINWEIS: SectionHeader, ListSection, IonItemSliding und IonRefresher sind bereits in allen relevanten Views/Pages aus Phase 3 vorhanden. Dieser Plan bereinigt NUR Inline-Styles.

Purpose: Visuell konsistentes Styling ueber alle Admin-Bereiche mit wartbaren CSS-Klassen statt verstreuter Inline-Styles.
Output: 10 bereinigten Admin-Dateien mit CSS-Klassen statt Inline-Styles.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-views-core/04-CONTEXT.md
@.planning/phases/04-admin-views-core/04-01-SUMMARY.md

Referenz fuer bestehende Patterns:
@frontend/src/theme/variables.css

<interfaces>
<!-- Neue CSS-Klassen aus Plan 04-01 (in variables.css): -->

.app-status-chip                     -- Status-Badge Basis
.app-status-chip--success/warning/danger/info/purple/neutral  -- Farbvarianten
.app-search-bar                      -- Suchleisten-Container
.app-search-bar__icon                -- Such-Icon Styling
.app-settings-item                   -- Settings-Eintrag (cursor, flex, gap)
.app-settings-item__title            -- Settings-Titel (fontWeight, fontSize, color)
.app-settings-item__subtitle         -- Settings-Untertitel (fontSize, color)
.app-segment-wrapper                 -- Segment-Tab Container (margin: 16px)
.app-info-box                        -- Info-Hinweis Basis
.app-info-box--blue/neutral          -- Farbvarianten
.app-progress-bar                    -- Fortschrittsbalken Basis (height 4px)
.app-progress-bar--thick             -- Dickerer Balken (height 6px)
.app-progress-bar__track             -- Hintergrund-Track
.app-progress-bar__fill              -- Gefuellter Bereich
.app-points-display                  -- Punkte-Anzeige Container
.app-points-display__value           -- Grosser Punkte-Wert
.app-points-display__target          -- Kleiner Ziel-Wert
.app-detail-header                   -- Detail-Seiten Header
.app-detail-header__content/title/subtitle/info-row/info-chip  -- Sub-Klassen

<!-- Bestehende CSS-Klassen (bereits in Nutzung): -->
.app-list-item + Varianten           -- Listenelemente
.app-icon-circle + Groessen          -- Runde Icons
.app-corner-badge                    -- Eselsohr Badge
.app-card                            -- Karten Container
.app-section-icon + Varianten        -- Sektion Icons

<!-- Bereits vorhandene Patterns aus Phase 3 (NICHT AENDERN): -->
SectionHeader mit preset/colors      -- in allen Views vorhanden
ListSection mit EmptyState            -- in allen Views vorhanden
IonItemSliding                        -- in allen Views mit editierbaren Items
IonRefresher                          -- in allen Admin-Pages (Wrapper)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminSettingsPage, AdminGoalsPage und AdminInvitePage Inline-Styles bereinigen</name>
  <files>
    frontend/src/components/admin/pages/AdminSettingsPage.tsx
    frontend/src/components/admin/pages/AdminGoalsPage.tsx
    frontend/src/components/admin/pages/AdminInvitePage.tsx
  </files>
  <action>
**A) AdminSettingsPage.tsx (58 Inline-Styles -> Ziel: unter 5):**

Die Settings-Page hat viele Eintraege mit identischem Pattern:
```tsx
<div
  className="app-list-item app-list-item--users"
  onClick={() => history.push('/admin/users')}
  style={{ cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '12px' }}
>
  <div className="app-icon-circle app-icon-circle--lg app-icon-circle--users">
    <IonIcon icon={people} />
  </div>
  <div style={{ flex: 1 }}>
    <h2 style={{ fontWeight: '500', fontSize: '0.95rem', margin: 0, color: '#333' }}>Benutzer:innen</h2>
    <p style={{ fontSize: '0.75rem', color: '#8e8e93', margin: '2px 0 0 0' }}>Admins und Rollen</p>
  </div>
</div>
```

Ersetzen durch:
```tsx
<div
  className="app-list-item app-list-item--users app-settings-item"
  onClick={() => history.push('/admin/users')}
>
  <div className="app-icon-circle app-icon-circle--lg app-icon-circle--users">
    <IonIcon icon={people} />
  </div>
  <div style={{ flex: 1 }}>
    <h2 className="app-settings-item__title">Benutzer:innen</h2>
    <p className="app-settings-item__subtitle">Admins, Teamer:innen und Rollen verwalten</p>
  </div>
</div>
```

Dieses Pattern fuer JEDEN Eintrag in der Settings-Page anwenden:
- Verwaltungs-Block: Benutzer, Punkte-Ziele, Konfis einladen
- System-Admin-Block: Organisationen
- Inhalt-Block: Aktivitaeten, Kategorien, Jahrgaenge, Level-System, Badges (neu aus Plan 04-01)
- Konto-Block: Profil, Push-Benachrichtigungen

Fuer den Push-Benachrichtigungen-Eintrag: Der hat ein `app-corner-badge` -- das `position: 'relative', overflow: 'hidden'` Inline-Style bleibt, da es fuer das Corner-Badge benoetigt wird.

Der `style={{ padding: '16px' }}` auf IonCardContent bleibt (ueberschreibt Ionic Default).

Die `style={{ display: 'flex', flexDirection: 'column' }}` Wrapper-Divs INNERHALB der Cards: Pruefen ob die app-list-item Klasse bereits margin-bottom hat. Falls ja, kann das Wrapper-div entfallen und die Items direkt in den IonCardContent.

Abmelden-Button: Die `style={{ height: '48px', borderRadius: '12px', fontWeight: '600' }}` koennen als inline bleiben (einmalig, Button-spezifisch).

**B) AdminGoalsPage.tsx (21 Inline-Styles -> Ziel: unter 10):**

- Die Stepper-Buttons (`width: '48px', height: '48px'`) koennen bleiben (einmalig)
- Die grossen Zahlenwerte (`fontSize: '2.5rem', fontWeight: '700'`) koennen bleiben (einmalig)
- Die Beschreibungstexte (`fontSize: '0.8rem', color: '#8e8e93'`) -> durch `app-settings-item__subtitle` oder passende Klasse ersetzen
- Die Info-Box am Ende -> durch `app-info-box app-info-box--neutral` ersetzen:
  ```tsx
  <IonCard className="app-card app-info-box--neutral">
    <IonCardContent className="app-info-box">
      <p style={{ margin: 0 }}>Diese Ziele werden in den Activity Rings...</p>
    </IonCardContent>
  </IonCard>
  ```

**C) AdminInvitePage.tsx (29 Inline-Styles -> Ziel: unter 15):**

- Suchleisten/Formular-Styles bleiben groesstenteils (Ionic IonItem Patterns)
- QR-Code Sektion: Die `textAlign: 'center'`, `width: '200px'` etc. koennen bleiben (einmalig, QR-spezifisch)
- Die Info-Box am Ende -> `app-info-box app-info-box--blue`
- Die Einladungscode-Anzeige (`fontFamily: 'monospace'`, `letterSpacing`) -> bleiben (spezifisch fuer Code-Anzeige)
- Die bestehenden `app-list-item` und `app-corner-badge` Klassen werden bereits genutzt

WICHTIG:
- Alle Funktionalitaet (Modal-Oeffnung, Navigation, Logout, Push-Benachrichtigungen) muss erhalten bleiben
- `style={{ flex: 1 }}` auf dem Text-Container ist ein legitimer Inline-Style (dynamisches Layout) -- BLEIBT
- Keine Emojis
- Echte Umlaute in deutschen UI-Texten
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "style={{" src/components/admin/pages/AdminSettingsPage.tsx && grep -c "app-settings-item" src/components/admin/pages/AdminSettingsPage.tsx && grep -c "style={{" src/components/admin/pages/AdminGoalsPage.tsx && grep -c "style={{" src/components/admin/pages/AdminInvitePage.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>AdminSettingsPage hat unter 10 Inline-Styles und nutzt app-settings-item CSS-Klassen. AdminGoalsPage und AdminInvitePage haben reduzierte Inline-Styles und nutzen app-info-box CSS-Klassen. Alle Funktionalitaet ist erhalten. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Admin-Views Inline-Styles bereinigen</name>
  <files>
    frontend/src/components/admin/KonfisView.tsx
    frontend/src/components/admin/ActivitiesView.tsx
    frontend/src/components/admin/EventsView.tsx
    frontend/src/components/admin/BadgesView.tsx
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
    frontend/src/components/admin/pages/AdminCategoriesPage.tsx
    frontend/src/components/konfi/views/EventsView.tsx
  </files>
  <action>
Inline-Styles in den Admin-Views durch CSS-Klassen ersetzen. Pro Datei:

HINWEIS: Alle diese Views haben bereits SectionHeader, ListSection und IonItemSliding aus Phase 3.
Hier wird NUR die Inline-Style-Bereinigung durchgefuehrt.

**A) KonfisView.tsx (~30 Inline-Styles zu bereinigen):**
- `style={{ margin: '16px' }}` auf IonList und Segment-Wrapper -> Segment-div bekommt `className="app-segment-wrapper"` statt `style={{ margin: '16px' }}`
- Suche-Icons `style={{ color: '#8e8e93', fontSize: '1rem' }}` -> bleiben (Ionic slot-spezifisch)
- Progress-Bars: Die drei Fortschrittsbalken (Godi, Gemeinde, Gesamt) durch CSS-Klassen ersetzen:
  - Track-div -> `className="app-progress-bar"` (oder `app-progress-bar--thick` fuer Gesamt)
  - Track-Hintergrund -> `className="app-progress-bar__track"` mit `style={{ backgroundColor: 'rgba(...)' }}`
  - Fill-div -> `className="app-progress-bar__fill"` mit `style={{ width: 'X%', backgroundColor: '#...' }}`
  - Die `width` und `backgroundColor` bleiben inline (dynamische Werte)
- Punkte-Anzeige rechts -> `className="app-points-display"` etc.
- Die Labels (`fontSize: '0.65rem'`, `fontWeight: '600'`) -> koennen inline bleiben (zu spezifisch fuer eigene Klasse)

**B) ActivitiesView.tsx (~15 Inline-Styles zu bereinigen):**
- Segment-Wrapper `style={{ margin: '16px' }}` -> `className="app-segment-wrapper"`
- Suchleisten-Block -> `className="app-search-bar"` statt `style={{ display: 'flex', alignItems: 'center', gap: '12px' }}`
- Such-Icon -> `className="app-search-bar__icon"` statt `style={{ color: '#8e8e93', fontSize: '1.2rem', flexShrink: 0 }}`
- `style={{ '--padding-start': '0' }}` auf IonInput bleibt (Ionic Custom Property)

**C) EventsView.tsx (~20 Inline-Styles zu bereinigen):**
- Segment-Wrapper `style={{ margin: '16px' }}` -> `className="app-segment-wrapper"`
- Die dynamischen Status-Farben (`statusColor`) bleiben inline (berechnet pro Event)
- `paddingRight: '70px'` auf Titel (Platz fuer Corner-Badge) -> bleibt inline

**D) BadgesView.tsx (~15 Inline-Styles zu bereinigen):**
- Segment-Wrapper `style={{ margin: '16px' }}` -> `className="app-segment-wrapper"`
- Die Dual Corner Badges (Sichtbarkeit + Aktiv) haben viel Inline-Style -> die aeusseren Container-Styles koennen durch `app-corner-badge`-Varianten ergaenzt werden. Falls zu komplex, bleiben lassen.
- Suche-Bereich -> app-search-bar Klassen wo moeglich

**E) AdminJahrgaengeePage.tsx und AdminCategoriesPage.tsx (~5 je):**
- Diese Views nutzen bereits SectionHeader, ListSection und IonItemSliding aus Phase 3
- Verbleibende Inline-Styles (IonItem Custom Properties) bleiben

**F) Konfi-EventsView.tsx:**
- Segment-Wrapper und Such-Bereich analog zu Admin-EventsView bereinigen

WICHTIG:
- SectionHeader, ListSection, IonItemSliding und IonRefresher NICHT AENDERN (aus Phase 3)
- Dynamische Werte (berechnete Farben, Prozentwerte, bedingte Opacity) bleiben IMMER inline
- Ionic CSS Custom Properties (--background, --padding-start, etc.) bleiben inline
- `style={{ flex: 1 }}` bleibt inline (Layout-spezifisch)
- Keine Emojis
- Echte Umlaute in deutschen UI-Texten
- ALLE bestehende Funktionalitaet muss erhalten bleiben
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && echo "=== Inline-Style Counts ===" && for f in admin/KonfisView.tsx admin/ActivitiesView.tsx admin/EventsView.tsx admin/BadgesView.tsx admin/pages/AdminJahrgaengeePage.tsx admin/pages/AdminCategoriesPage.tsx; do echo "$f: $(grep -c 'style={{' src/components/$f)"; done && echo "=== CSS-Klassen ===" && grep -c "app-segment-wrapper" src/components/admin/ActivitiesView.tsx && grep -c "app-search-bar" src/components/admin/ActivitiesView.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Admin-Views nutzen app-segment-wrapper, app-search-bar und app-progress-bar CSS-Klassen. Inline-Styles in Admin-Dateien sind insgesamt deutlich unter 100. Alle Funktionalitaet ist erhalten. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. AdminSettingsPage: `grep -c "app-settings-item" file` liefert mehrere Treffer
2. Views: `grep "app-segment-wrapper" file` liefert Treffer in allen Views mit Segment-Tabs
3. ActivitiesView: `grep "app-search-bar" file` liefert Treffer
4. Gesamt-Inline-Style-Count fuer alle Admin-Dateien liegt deutlich unter 100:
   ```bash
   for f in admin/KonfisView.tsx admin/ActivitiesView.tsx admin/EventsView.tsx admin/BadgesView.tsx admin/pages/Admin*.tsx; do grep -c 'style={{' src/components/$f; done | paste -sd+ - | bc
   ```
5. IonItemSliding ist in allen relevanten Views vorhanden (Verifizierung aus Phase 3)
6. IonRefresher ist in allen Admin-Pages vorhanden (Verifizierung aus Phase 3)
7. `npx tsc --noEmit` kompiliert ohne Fehler
</verification>

<success_criteria>
- AdminSettingsPage nutzt app-settings-item CSS-Klassen fuer alle Navigations-Eintraege
- Admin-Views nutzen app-segment-wrapper fuer Segment-Tab Container
- ActivitiesView nutzt app-search-bar fuer die Suchleiste
- KonfisView nutzt app-progress-bar und app-points-display fuer Fortschrittsbalken und Punkte
- AdminGoalsPage und AdminInvitePage nutzen app-info-box fuer Hinweis-Boxen
- Gesamt-Inline-Styles in Admin-Dateien liegen deutlich unter 100
- Alle bestehende Funktionalitaet ist in allen Dateien vollstaendig erhalten
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-views-core/04-03-SUMMARY.md`
</output>
