---
phase: 82-backend-sicherheit-cron
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/server.js
autonomous: true
requirements:
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "Ein Nutzer aus Org A kann nicht dem Socket.IO-Room einer Org-B-Chatroom beitreten"
    - "joinRoom prueft die Organization-Zugehoerigkeit des Rooms in der Datenbank"
    - "Ungueltige Room-Joins werden im Serverlog mit Warnung protokolliert"
  artifacts:
    - path: "backend/server.js"
      provides: "Abgesicherter joinRoom-Handler"
      contains: "organization_id"
  key_links:
    - from: "backend/server.js socket.on('joinRoom')"
      to: "db.query chat_rooms"
      via: "Organization-Isolation-Check"
      pattern: "chat_rooms.*organization_id"
---

<objective>
Den Socket.IO `joinRoom`-Handler in server.js mit einer Organization-Isolation absichern: Bevor `socket.join(room_${roomId})` ausgefuehrt wird, wird geprueft ob der Room zur selben Organisation wie der Socket-User gehoert.

Purpose: Verhindert, dass ein authentifizierter Nutzer aus Org A die Room-ID einer Org-B-Chatroom errät und dort Nachrichten mithört.
Output: Geaendertes server.js mit abgesichertem joinRoom-Handler + leaveRoom-Absicherung
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Vorhandene Socket.IO-Struktur aus backend/server.js (Zeilen 52-106) -->

```js
// socket.user nach JWT-Decode enthalt:
// { id, type ('konfi'|'admin'|'teamer'), display_name, organization_id, role_id }

// Aktueller joinRoom-Handler (unsicher):
socket.on('joinRoom', (roomId) => {
  socket.join(`room_${roomId}`);
});

// DB-Zugriff: db ist in server.js verfuegbar als:
const db = require('./database');

// chat_rooms Tabelle:
// id, name, type, jahrgang_id, created_by, organization_id
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: joinRoom mit Organization-Check absichern</name>
  <files>backend/server.js</files>
  <read_first>
    - backend/server.js (Zeilen 1-115 vollstaendig lesen -- Socket.IO Setup, Auth-Middleware, Connection-Handler)
  </read_first>
  <action>
Den `joinRoom`-Handler (aktuell Zeile ~77-79) durch eine async-Funktion ersetzen, die vorher die Organization des Rooms aus der DB prueft.

**Kontext:** `db` ist in server.js als `require('./database')` vorhanden, aber erst ab Zeile ~115 eingebunden. Da der Socket.IO-Handler im gleichen File lebt, kann db als spaet-gebundene Variable genutzt werden -- ODER direkt den require-Aufruf am Anfang des Connection-Handlers platzieren.

Beachte: `db` wird in server.js nach den Socket.IO-Event-Handlern mit `const db = require('./database')` eingebunden (Zeile ~115). Um `db` im Socket.IO-Handler zu nutzen, muss der `require` VOR den io.on()-Definitionen stehen ODER `db` wird lazy im Handler aufgerufen. Sicherste Loesung: Den `db`-require nach oben verschieben (bereits vor Zeile 52), da er ohnehin gloal gebraucht wird.

**Implementierung:**

Schritt 1: Pruefen ob `const db = require('./database')` VOR dem Socket.IO-Block (Zeile 52) steht. Falls nicht, den require-Aufruf nach oben verschieben (direkt nach den anderen requires, vor dem io-Setup).

Schritt 2: Den `joinRoom`-Handler ersetzen:

```js
socket.on('joinRoom', async (roomId) => {
  try {
    // Sicherheits-Check: Room zur selben Organisation?
    const { rows } = await db.query(
      'SELECT organization_id FROM chat_rooms WHERE id = $1',
      [roomId]
    );

    if (rows.length === 0) {
      console.warn(`Socket joinRoom: Room ${roomId} nicht gefunden (User ${socket.user.id})`);
      return;
    }

    const roomOrgId = rows[0].organization_id;
    const userOrgId = socket.user.organization_id;

    if (roomOrgId !== userOrgId) {
      console.warn(`Socket joinRoom: Org-Isolation-Verletzung! User ${socket.user.id} (Org ${userOrgId}) versucht Room ${roomId} (Org ${roomOrgId}) beizutreten`);
      return;
    }

    socket.join(`room_${roomId}`);
  } catch (err) {
    console.error('Socket joinRoom Fehler:', err.message);
  }
});
```

Den `leaveRoom`-Handler NICHT mit DB-Check absichern -- Verlassen eines Rooms ist harmlos und soll immer funktionieren.

Die `typing` und `stopTyping` Handler ebenfalls nicht veraendern -- sie senden nur an bestehende Room-Mitglieder, kein Sicherheitsrisiko.
  </action>
  <verify>
    <automated>node --check /Users/simonluthe/Documents/Konfipoints/backend/server.js && grep -n "organization_id" /Users/simonluthe/Documents/Konfipoints/backend/server.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check backend/server.js` gibt keinen Fehler aus (Syntax OK)
    - `grep -n "organization_id" backend/server.js` zeigt Treffer im Socket.IO-Bereich
    - `grep -n "chat_rooms" backend/server.js` zeigt den DB-Query im joinRoom-Handler
    - `grep -n "Org-Isolation" backend/server.js` zeigt die Warn-Zeile
    - Der alte `socket.join(\`room_\${roomId}\`)` ohne Check ist nicht mehr als alleinige Anweisung vorhanden (nur innerhalb des if-Blocks nach dem Check)
  </acceptance_criteria>
  <done>joinRoom prueft Organization-Zugehoerigkeit via DB-Query und verwirft Joins von Nutzern fremder Organisationen mit console.warn-Eintrag.</done>
</task>

</tasks>

<verification>
Nach dem Task:
- `node --check backend/server.js` laeuft fehlerfrei durch
- Der joinRoom-Handler ist async und enthaelt einen `db.query('SELECT organization_id FROM chat_rooms...')`-Call
- Kein `socket.join(...)` ohne vorherigen Organization-Check im joinRoom-Handler
</verification>

<success_criteria>
- SEC-02: joinRoom prueft Organization-Zugehoerigkeit (verifizierbar per grep auf 'chat_rooms' im Socket.IO-Block)
- SEC-03: Cross-Org-Joins werden mit console.warn geblockt (verifizierbar per grep auf 'Org-Isolation')
- Kein Syntax-Fehler in server.js (`node --check backend/server.js`)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/82-backend-sicherheit-cron/82-02-SUMMARY.md` erstellen.
</output>
