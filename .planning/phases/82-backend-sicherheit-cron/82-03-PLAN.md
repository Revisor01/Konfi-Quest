---
phase: 82-backend-sicherheit-cron
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/backgroundService.js
  - backend/package.json
autonomous: true
requirements:
  - CRON-01
  - CRON-02

must_haves:
  truths:
    - "node-cron ist als Dependency in package.json eingetragen"
    - "Der Wrapped-Cron laeuft mit echtem Cron-Ausdruck '0 6 1 * *' statt setInterval"
    - "Nach einem Container-Neustart wird der naechste 1. des Monats korrekt abgewartet"
    - "checkWrappedTriggers wird nicht mehr im Cron aufgerufen -- node-cron steuert die Ausfuehrung direkt"
  artifacts:
    - path: "backend/services/backgroundService.js"
      provides: "node-cron basierter Wrapped-Cron"
      contains: "node-cron"
    - path: "backend/package.json"
      provides: "node-cron Dependency"
      contains: "node-cron"
  key_links:
    - from: "backend/services/backgroundService.js startWrappedCron"
      to: "node-cron.schedule"
      via: "Cron-Ausdruck '0 6 1 * *'"
      pattern: "cron.schedule"
---

<objective>
Den Wrapped-Cron in backgroundService.js von `setInterval(24h)` auf `node-cron` mit echtem Cron-Ausdruck umstellen. node-cron wird in package.json als Dependency eingetragen.

Purpose: Nach einem Container-Neustart driftet der setInterval-Cron -- node-cron berechnet den naechsten 1. des Monats korrekt, unabhaengig vom Startzeitpunkt.
Output: Aktualisiertes backgroundService.js + package.json mit node-cron
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Vorhandene backgroundService.js-Struktur (Zeilen 400-435) -->

```js
// Aktueller setInterval-Cron:
static startWrappedCron(db) {
  if (this.wrappedCronInterval) {
    return;
  }
  // Sofort einmal pruefen
  this.checkWrappedTriggers(db);

  const TWENTY_FOUR_HOURS = 24 * 60 * 60 * 1000;
  this.wrappedCronInterval = setInterval(async () => {
    try {
      await this.checkWrappedTriggers(db);
    } catch (error) {
      console.error('Wrapped-Cron service failed:', error);
    }
  }, TWENTY_FOUR_HOURS);
}

static stopWrappedCron() {
  if (this.wrappedCronInterval) {
    clearInterval(this.wrappedCronInterval);
    this.wrappedCronInterval = null;
  }
}

// checkWrappedTriggers prueft intern ob today.getDate() === 1
// Nach Migration: node-cron steuert Ausfuehrungszeit, der interne Check kann bleiben
// (Sicherheitsnetz) oder wird vereinfacht
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: node-cron als Dependency eintragen</name>
  <files>backend/package.json</files>
  <read_first>
    - backend/package.json (vollstaendig lesen -- besonders dependencies-Block)
  </read_first>
  <action>
`node-cron` in den `dependencies`-Block von backend/package.json eintragen. Aktuelle stabile Version ist `3.0.3`.

Neuer dependencies-Block (alphabetisch einsortieren, nach `multer`, vor `nodemailer`):

```json
"dependencies": {
  "bcrypt": "^5.1.1",
  "cors": "^2.8.5",
  "express": "^4.18.2",
  "express-rate-limit": "^8.3.1",
  "express-validator": "^7.3.1",
  "firebase-admin": "^13.7.0",
  "helmet": "^8.1.0",
  "jsonwebtoken": "^9.0.2",
  "multer": "^2.1.1",
  "node-cron": "^3.0.3",
  "nodemailer": "^8.0.2",
  "pg": "^8.16.3",
  "sqlite3": "^5.1.6"
}
```

Hinweis: `sqlite3` bleibt vorerst stehen -- dessen Entfernung ist Phase 85 (CLN-01). Nicht in dieser Phase entfernen.
  </action>
  <verify>
    <automated>grep -c "node-cron" /Users/simonluthe/Documents/Konfipoints/backend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "node-cron" backend/package.json` gibt eine Zeile im dependencies-Block aus
    - JSON ist syntaktisch valide: `node -e "require('./backend/package.json')" 2>&1` gibt keinen Fehler
  </acceptance_criteria>
  <done>node-cron ist als Dependency in backend/package.json eingetragen.</done>
</task>

<task type="auto">
  <name>Task 2: setInterval in backgroundService.js durch node-cron ersetzen</name>
  <files>backend/services/backgroundService.js</files>
  <read_first>
    - backend/services/backgroundService.js (Zeilen 395-480 vollstaendig lesen -- startWrappedCron, stopWrappedCron, checkWrappedTriggers)
  </read_first>
  <action>
Am Anfang der Datei (nach den vorhandenen requires/imports) `node-cron` einbinden und die Methoden `startWrappedCron` und `stopWrappedCron` ersetzen.

**Schritt 1: Require-Zeile einfuegen**

Am Anfang der Datei (in der Naehe der anderen require-Aufrufe):
```js
const cron = require('node-cron');
```

**Schritt 2: startWrappedCron ersetzen**

Die gesamte `startWrappedCron`-Methode (Zeilen ~409-425) ersetzen durch:

```js
static startWrappedCron(db) {
  if (this.wrappedCronTask) {
    return;
  }

  console.log('Wrapped-Cron: Starte node-cron (0 6 1 * * -- jeden 1. des Monats um 06:00 Uhr)');

  // '0 6 1 * *' = Am 1. jeden Monats um 06:00 Uhr
  // node-cron berechnet nach Neustart den naechsten Trigger korrekt
  this.wrappedCronTask = cron.schedule('0 6 1 * *', async () => {
    console.log('Wrapped-Cron: Ausfuehrung gestartet (1. des Monats)');
    try {
      await this.checkWrappedTriggers(db);
    } catch (error) {
      console.error('Wrapped-Cron service failed:', error);
    }
  }, {
    timezone: 'Europe/Berlin'
  });
}
```

**Schritt 3: stopWrappedCron ersetzen**

Die gesamte `stopWrappedCron`-Methode (Zeilen ~430-435) ersetzen durch:

```js
static stopWrappedCron() {
  if (this.wrappedCronTask) {
    this.wrappedCronTask.stop();
    this.wrappedCronTask = null;
  }
}
```

**Schritt 4: Klassenvariable anpassen**

Falls `wrappedCronInterval` als statische Klasseneigenschaft initialisiert ist (z.B. `BackgroundService.wrappedCronInterval = null`), die Zeile zu `BackgroundService.wrappedCronTask = null` umbenennen ODER einfach entfernen (node-cron-Task wird lazy angelegt).

Suche nach `wrappedCronInterval` in der Datei und ersetze alle Vorkommen durch `wrappedCronTask`.

**WICHTIG:** Die Methode `checkWrappedTriggers` (ab Zeile ~441) NICHT veraendern -- sie bleibt unveraendert. Der interne `today.getDate() !== 1` Check in checkWrappedTriggers kann als Sicherheitsnetz bleiben.

**WICHTIG:** Den sofortigen Aufruf `this.checkWrappedTriggers(db)` beim Start (war im alten Code vorhanden) in der neuen Variante WEGLASSEN -- node-cron wartet korrekt bis zum naechsten 1. des Monats.
  </action>
  <verify>
    <automated>node --check /Users/simonluthe/Documents/Konfipoints/backend/services/backgroundService.js && grep -n "node-cron\|cron.schedule\|wrappedCronTask" /Users/simonluthe/Documents/Konfipoints/backend/services/backgroundService.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check backend/services/backgroundService.js` gibt keinen Syntax-Fehler aus
    - `grep "cron.schedule" backend/services/backgroundService.js` zeigt den Cron-Ausdruck '0 6 1 * *'
    - `grep "setInterval" backend/services/backgroundService.js` zeigt KEINE Treffer mehr im startWrappedCron-Bereich
    - `grep "wrappedCronInterval" backend/services/backgroundService.js` zeigt keine Treffer (vollstaendig umbenannt)
    - `grep "timezone.*Berlin\|Berlin.*timezone" backend/services/backgroundService.js` bestaetigt Zeitzone-Konfiguration
  </acceptance_criteria>
  <done>backgroundService.js nutzt node-cron mit Ausdruck '0 6 1 * *' und Zeitzone Europe/Berlin. setInterval ist vollstaendig entfernt. wrappedCronTask ersetzt wrappedCronInterval.</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
- `node --check backend/services/backgroundService.js` fehlerfrei
- `node -e "require('./backend/package.json')"` fehlerfrei (JSON valide)
- `grep "node-cron" backend/package.json backend/services/backgroundService.js` zeigt Treffer in beiden Dateien
- `grep "setInterval" backend/services/backgroundService.js` gibt keinen Treffer mehr im startWrappedCron-Kontext
</verification>

<success_criteria>
- CRON-01: node-cron ist in package.json und backgroundService.js aktiv mit Ausdruck '0 6 1 * *'
- CRON-02: Nach Container-Neustart berechnet node-cron korrekt den naechsten 1. des Monats (kein setInterval-Drift mehr)
- Keine Syntax-Fehler in beiden geaenderten Dateien
</success_criteria>

<output>
Nach Abschluss `.planning/phases/82-backend-sicherheit-cron/82-03-SUMMARY.md` erstellen.
</output>
