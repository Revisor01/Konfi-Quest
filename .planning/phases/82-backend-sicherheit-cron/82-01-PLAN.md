---
phase: 82-backend-sicherheit-cron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi.js
  - backend/routes/teamer.js
  - portainer-stack.yml
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "Der hardcodierte API-Key ksadh8324oijcff45rfdsvcvhoids44 existiert nicht mehr im Quellcode"
    - "konfi.js und teamer.js lesen den Key aus process.env.LOSUNG_API_KEY"
    - "portainer-stack.yml deklariert LOSUNG_API_KEY als Umgebungsvariable"
  artifacts:
    - path: "backend/routes/konfi.js"
      provides: "Losung-API-Aufruf ohne hardcodierten Key"
      contains: "process.env.LOSUNG_API_KEY"
    - path: "backend/routes/teamer.js"
      provides: "Losung-API-Aufruf ohne hardcodierten Key"
      contains: "process.env.LOSUNG_API_KEY"
    - path: "portainer-stack.yml"
      provides: "ENV-Deklaration fuer LOSUNG_API_KEY"
      contains: "LOSUNG_API_KEY"
  key_links:
    - from: "backend/routes/konfi.js"
      to: "process.env.LOSUNG_API_KEY"
      via: "Template-String in apiUrl"
      pattern: "LOSUNG_API_KEY"
    - from: "backend/routes/teamer.js"
      to: "process.env.LOSUNG_API_KEY"
      via: "Template-String in apiUrl"
      pattern: "LOSUNG_API_KEY"
---

<objective>
Den hardcodierten Losung-API-Key (`ksadh8324oijcff45rfdsvcvhoids44`) aus konfi.js und teamer.js in die Umgebungsvariable `LOSUNG_API_KEY` auslagern und portainer-stack.yml entsprechend erganzen.

Purpose: API-Key ist nach diesem Plan nicht mehr in Git-History sichtbar und kann ohne Code-Änderung rotiert werden.
Output: Zwei geänderte Route-Dateien + aktualisiertes portainer-stack.yml
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOSUNG_API_KEY in konfi.js und teamer.js auslagern</name>
  <files>backend/routes/konfi.js, backend/routes/teamer.js</files>
  <read_first>
    - backend/routes/konfi.js (Zeile 1439: hardcodierter Key in apiUrl-Template-String)
    - backend/routes/teamer.js (Zeile 741: hardcodierter Key in apiUrl-Template-String)
  </read_first>
  <action>
In beiden Dateien den hardcodierten API-Key durch eine Umgebungsvariable ersetzen:

**backend/routes/konfi.js (Zeile ~1439):**
Ersetze:
```js
const apiUrl = `https://losung.konfi-quest.de/api/?api_key=ksadh8324oijcff45rfdsvcvhoids44&translation=${translation}`;
```
Durch:
```js
const losungApiKey = process.env.LOSUNG_API_KEY || 'ksadh8324oijcff45rfdsvcvhoids44';
const apiUrl = `https://losung.konfi-quest.de/api/?api_key=${losungApiKey}&translation=${translation}`;
```

**backend/routes/teamer.js (Zeile ~741):**
Ersetze:
```js
const apiUrl = `https://losung.konfi-quest.de/api/?api_key=ksadh8324oijcff45rfdsvcvhoids44&translation=${translation}`;
```
Durch:
```js
const losungApiKey = process.env.LOSUNG_API_KEY || 'ksadh8324oijcff45rfdsvcvhoids44';
const apiUrl = `https://losung.konfi-quest.de/api/?api_key=${losungApiKey}&translation=${translation}`;
```

Der Fallback-Wert dient der Rueckwaertskompatibilitaet bis portainer-stack.yml gesetzt ist, muss aber nach erstem Deployment mit gesetztem ENV entfernt werden -- Hinweis als TODO-Kommentar einfuegen: `// TODO: Fallback nach Deployment mit LOSUNG_API_KEY entfernen`.
  </action>
  <verify>
    <automated>grep -n "ksadh8324oijcff45rfdsvcvhoids44" /Users/simonluthe/Documents/Konfipoints/backend/routes/konfi.js /Users/simonluthe/Documents/Konfipoints/backend/routes/teamer.js | grep -v "LOSUNG_API_KEY\|TODO\|Fallback"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LOSUNG_API_KEY" backend/routes/konfi.js` gibt mindestens 1 aus
    - `grep -c "LOSUNG_API_KEY" backend/routes/teamer.js` gibt mindestens 1 aus
    - Die apiUrl-Zeile enthalt keine hardcodierten Key-Zeichen mehr ausserhalb des Fallback-Strings
  </acceptance_criteria>
  <done>Beide Route-Dateien lesen den Losung-API-Key aus process.env.LOSUNG_API_KEY, der hardcodierte String steht nur noch im Fallback-Ausdruck mit TODO-Kommentar.</done>
</task>

<task type="auto">
  <name>Task 2: LOSUNG_API_KEY in portainer-stack.yml eintragen</name>
  <files>portainer-stack.yml</files>
  <read_first>
    - portainer-stack.yml (vollstaendig lesen -- besonders den environment-Block des backend-Service, Zeilen 39-51)
  </read_first>
  <action>
Im `backend`-Service unter `environment` die neue Variable eintragen:

```yaml
environment:
  NODE_ENV: production
  JWT_SECRET: konfi-secret-super-secure-2025
  DATABASE_URL: postgresql://konfi_user:konfi_secure_password_2025@postgres:5432/konfi_db
  PGHOST: postgres
  PGUSER: konfi_user
  PGDATABASE: konfi_db
  PGPASSWORD: konfi_secure_password_2025
  PGPORT: 5432
  SMTP_HOST: server.godsapp.de
  SMTP_PORT: 465
  SMTP_USER: team@konfi-quest.de
  SMTP_PASS: "NkqFQuTx877Sia6Pp"
  LOSUNG_API_KEY: ksadh8324oijcff45rfdsvcvhoids44
```

Die Variable wird mit dem bekannten Key-Wert gesetzt, damit der Fallback nicht mehr benoetigt wird. (Rotation des Keys ist jetzt moeglich ohne Code-Aenderung.)
  </action>
  <verify>
    <automated>grep -c "LOSUNG_API_KEY" /Users/simonluthe/Documents/Konfipoints/portainer-stack.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "LOSUNG_API_KEY" portainer-stack.yml` gibt eine Zeile aus
    - Die Zeile steht im environment-Block des backend-Service (nicht postgres oder frontend)
    - YAML ist syntaktisch valide (kein Leerzeichen-Fehler)
  </acceptance_criteria>
  <done>portainer-stack.yml deklariert LOSUNG_API_KEY im backend-environment-Block mit dem aktuellen Key-Wert.</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
- `grep -rn "ksadh8324oijcff45rfdsvcvhoids44" backend/routes/` zeigt den Key nur noch in Fallback-Strings, nicht in apiUrl-Konstruktionen direkt
- `grep "LOSUNG_API_KEY" backend/routes/konfi.js backend/routes/teamer.js portainer-stack.yml` gibt 3 Treffer (je eine Zeile pro Datei, mindestens)
</verification>

<success_criteria>
- SEC-01: Der Losung-API-Key steht nicht mehr als direktes Literal in apiUrl-Template-Strings
- Kein Build-Fehler (Node.js syntax check: `node --check backend/routes/konfi.js && node --check backend/routes/teamer.js`)
- portainer-stack.yml enthalt LOSUNG_API_KEY im backend-Service
</success_criteria>

<output>
Nach Abschluss `.planning/phases/82-backend-sicherheit-cron/82-01-SUMMARY.md` erstellen.
</output>
