---
phase: 45-event-sichtbarkeit-filterung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminEventsPage.tsx
  - frontend/src/components/admin/EventsView.tsx
autonomous: true
requirements:
  - EVT-v19-09

must_haves:
  truths:
    - "Admin sieht einen Jahrgangs-Filter-Dropdown in der Event-Liste"
    - "Admin kann Events nach einzelnem Jahrgang filtern"
    - "Jahrgangsname(n) werden als Subtitle unter jedem Event-Namen angezeigt"
    - "Filter wirkt auf alle Tabs (Aktuell, Alle, Konfi)"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminEventsPage.tsx"
      provides: "Jahrgangs-State und Filter-Logik"
      contains: "selectedJahrgang"
    - path: "frontend/src/components/admin/EventsView.tsx"
      provides: "IonSelect Dropdown fuer Jahrgangs-Filter"
      contains: "IonSelect"
  key_links:
    - from: "frontend/src/components/admin/pages/AdminEventsPage.tsx"
      to: "frontend/src/components/admin/EventsView.tsx"
      via: "selectedJahrgang prop und jahrgaenge prop"
      pattern: "selectedJahrgang"
---

<objective>
Admin-Event-Liste um Jahrgangs-Filter erweitern: IonSelect Dropdown unter dem Header mit "Alle Jahrgaenge" + einzelne Jahrgaenge, wirkt auf alle Tabs.

Purpose: Admin kann schnell Events eines bestimmten Jahrgangs finden.
Output: Funktionierender Jahrgangs-Filter in der Admin-Event-Ansicht.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-event-sichtbarkeit-filterung/45-CONTEXT.md

<interfaces>
<!-- Admin Events Backend liefert bereits jahrgang_ids und jahrgang_names pro Event -->
From backend/routes/events.js (GET /events):
- Jedes Event-Objekt enthaelt `jahrgang_ids: string` (komma-separierte IDs) und `jahrgang_names: string` (komma-separierte Namen)
- Werden im Frontend als `jahrgang_names?: string` und `jahrgang_ids` (nach Transform) verfuegbar

From frontend/src/components/admin/EventsView.tsx:
```typescript
interface Event {
  // ... existing fields ...
  jahrgang_names?: string;
}

interface EventsViewProps {
  events: Event[];
  onUpdate: () => void;
  onAddEventClick: () => void;
  onSelectEvent: (event: Event) => void;
  onDeleteEvent?: (event: Event) => void;
  onCopyEvent?: (event: Event) => void;
  onCancelEvent?: (event: Event) => void;
  activeTab?: 'all' | 'upcoming' | 'konfirmation';
  onTabChange?: (tab: 'all' | 'upcoming' | 'konfirmation') => void;
  eventCounts?: { all: number; upcoming: number; konfirmation: number; };
}
```

From backend/routes/jahrgaenge.js (GET /jahrgaenge):
- Liefert Array von `{ id, name, organization_id, ... }`
- Bereits im Frontend ueber `api.get('/jahrgaenge')` aufrufbar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin-Event-Liste Jahrgangs-Filter Dropdown + Filterung</name>
  <files>frontend/src/components/admin/pages/AdminEventsPage.tsx, frontend/src/components/admin/EventsView.tsx</files>
  <read_first>
    - frontend/src/components/admin/pages/AdminEventsPage.tsx (komplett — State, loadEvents, Filter-Logik, EventsView Props)
    - frontend/src/components/admin/EventsView.tsx (komplett — Props, Segment-Rendering, Event-Liste, jahrgang_names Anzeige Zeile 349-351)
  </read_first>
  <action>
**1. AdminEventsPage.tsx — Jahrgaenge laden und Filter-State:**

a) Neuen State hinzufuegen:
```typescript
const [jahrgaenge, setJahrgaenge] = useState<Array<{id: number; name: string}>>([]);
const [selectedJahrgang, setSelectedJahrgang] = useState<number | null>(null);
```

b) `loadJahrgaenge` Funktion hinzufuegen:
```typescript
const loadJahrgaenge = async () => {
  try {
    const response = await api.get('/jahrgaenge');
    setJahrgaenge(response.data);
  } catch (err) {
    console.error('Error loading jahrgaenge:', err);
  }
};
```

c) In useEffect `loadJahrgaenge()` aufrufen (neben loadEvents etc.).

d) Filter-Funktion erstellen die Events nach selectedJahrgang filtert:
```typescript
const filterByJahrgang = (eventList: Event[]) => {
  if (!selectedJahrgang) return eventList;
  return eventList.filter(event => {
    if (!event.jahrgang_ids) return false;
    const ids = event.jahrgang_ids.split(',').map(id => parseInt(id.trim(), 10));
    return ids.includes(selectedJahrgang);
  });
};
```

e) Event-Interface um `jahrgang_ids?: string` erweitern (falls nicht vorhanden — pruefen ob Backend es bereits als String mitgibt, was es tut via STRING_AGG).

f) `filterByJahrgang` auf alle Event-Listen anwenden bevor sie an EventsView uebergeben werden:
- `getAllEvents()` -> `filterByJahrgang(getAllEvents())`
- `getFutureEvents()` -> `filterByJahrgang(getFutureEvents())`
- `getKonfirmationEvents()` -> `filterByJahrgang(getKonfirmationEvents())`
- eventCounts ebenso mit gefilterten Listen berechnen

g) Neue Props an EventsView uebergeben:
```tsx
<EventsView
  // ... bestehende Props ...
  jahrgaenge={jahrgaenge}
  selectedJahrgang={selectedJahrgang}
  onJahrgangChange={setSelectedJahrgang}
/>
```

**2. EventsView.tsx (Admin) — IonSelect Dropdown rendern:**

a) Props-Interface erweitern:
```typescript
interface EventsViewProps {
  // ... bestehende Props ...
  jahrgaenge?: Array<{id: number; name: string}>;
  selectedJahrgang?: number | null;
  onJahrgangChange?: (jahrgangId: number | null) => void;
}
```

b) IonSelect importieren: `import { IonSelect, IonSelectOption } from '@ionic/react';` (zu bestehenden Imports hinzufuegen)

c) IonSelect Dropdown ZWISCHEN dem SectionHeader und dem Segment einfuegen:
```tsx
{jahrgaenge && jahrgaenge.length > 0 && onJahrgangChange && (
  <div style={{ padding: '0 16px 8px' }}>
    <IonSelect
      value={selectedJahrgang}
      placeholder="Alle Jahrgaenge"
      interface="popover"
      onIonChange={(e) => onJahrgangChange(e.detail.value || null)}
      style={{
        '--padding-start': '12px',
        '--padding-end': '12px',
        fontSize: '0.9rem',
        background: 'var(--ion-color-light)',
        borderRadius: '8px'
      }}
    >
      <IonSelectOption value={null}>Alle Jahrgaenge</IonSelectOption>
      {jahrgaenge.map(j => (
        <IonSelectOption key={j.id} value={j.id}>{j.name}</IonSelectOption>
      ))}
    </IonSelect>
  </div>
)}
```

d) Bestehende jahrgang_names Anzeige (Zeile 349-351) bleibt wie sie ist — sie zeigt bereits den Jahrgangsnamen als Subtitle unter dem Event-Namen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "selectedJahrgang" frontend/src/components/admin/pages/AdminEventsPage.tsx | head -5 && grep -n "IonSelect" frontend/src/components/admin/EventsView.tsx | head -3 && grep -n "onJahrgangChange" frontend/src/components/admin/EventsView.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - AdminEventsPage.tsx enthaelt `useState<number | null>(null)` fuer selectedJahrgang
    - AdminEventsPage.tsx enthaelt `loadJahrgaenge` Funktion mit `api.get('/jahrgaenge')`
    - AdminEventsPage.tsx enthaelt `filterByJahrgang` Funktion die nach jahrgang_ids filtert
    - AdminEventsPage.tsx uebergibt `jahrgaenge`, `selectedJahrgang`, `onJahrgangChange` als Props an EventsView
    - EventsView.tsx (Admin) importiert `IonSelect, IonSelectOption`
    - EventsView.tsx (Admin) rendert IonSelect mit `placeholder="Alle Jahrgaenge"` und `interface="popover"`
    - EventsView.tsx (Admin) Props-Interface enthaelt `jahrgaenge?`, `selectedJahrgang?`, `onJahrgangChange?`
    - Event Interface in AdminEventsPage.tsx enthaelt `jahrgang_ids?: string`
  </acceptance_criteria>
  <done>Admin-Event-Liste hat funktionierenden Jahrgangs-Filter-Dropdown der auf alle Tabs wirkt, mit "Alle Jahrgaenge" als Default-Option.</done>
</task>

</tasks>

<verification>
1. Admin sieht IonSelect Dropdown mit "Alle Jahrgaenge" + einzelne Jahrgaenge
2. Auswahl eines Jahrgangs filtert Events in allen drei Tabs (Aktuell, Alle, Konfi)
3. Zuruecksetzen auf "Alle Jahrgaenge" zeigt wieder alle Events
4. Jahrgangsname(n) werden weiterhin als Subtitle in der Event-Liste angezeigt
5. Event-Counts aktualisieren sich passend zum gewaehlten Filter
</verification>

<success_criteria>
- Jahrgangs-Filter-Dropdown ist sichtbar in der Admin-Event-Liste
- Filter wirkt auf alle Tabs und aktualisiert Event-Counts
- "Alle Jahrgaenge" zeigt alle Events (kein Filter aktiv)
- Einzelne Jahrgangs-Auswahl zeigt nur Events dieses Jahrgangs
</success_criteria>

<output>
After completion, create `.planning/phases/45-event-sichtbarkeit-filterung/45-02-SUMMARY.md`
</output>
