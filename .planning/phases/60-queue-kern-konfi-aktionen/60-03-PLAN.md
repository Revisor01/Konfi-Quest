---
phase: 60-queue-kern-konfi-aktionen
plan: 03
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - frontend/src/components/konfi/modals/ActivityRequestModal.tsx
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/konfi/pages/KonfiRequestsPage.tsx
autonomous: true
requirements: [QUE-K03, QUE-K04, QUE-K05]

must_haves:
  truths:
    - "Konfi kann offline einen Aktivitaets-Antrag ohne Foto stellen — erscheint sofort in Antrags-Liste mit Uhr-Icon"
    - "Konfi kann offline einen Aktivitaets-Antrag mit Foto stellen — Foto wird lokal gespeichert, Upload im Vordergrund"
    - "Konfi kann offline Opt-out bei Pflicht-Event machen — Event zeigt Uhr-Icon"
    - "Optimistic Items verschwinden nach erfolgreichem Queue-Flush und echtem Reload"
  artifacts:
    - path: "frontend/src/components/konfi/modals/ActivityRequestModal.tsx"
      provides: "Antrag-Submit mit Queue-Fallback bei offline"
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "Opt-out mit Queue-Fallback bei offline"
    - path: "frontend/src/components/konfi/pages/KonfiRequestsPage.tsx"
      provides: "Anzeige von pending Queue-Antraegen in der Liste"
  key_links:
    - from: "frontend/src/components/konfi/modals/ActivityRequestModal.tsx"
      to: "frontend/src/services/writeQueue.ts"
      via: "writeQueue.enqueue bei offline submitRequest"
      pattern: "writeQueue\\.enqueue"
    - from: "frontend/src/components/konfi/views/EventDetailView.tsx"
      to: "frontend/src/services/writeQueue.ts"
      via: "writeQueue.enqueue bei offline opt-out"
      pattern: "writeQueue\\.enqueue"
---

<objective>
Aktivitaets-Antraege (mit/ohne Foto) und Opt-out bei Pflicht-Events offline-faehig machen.

Purpose: Konfis koennen auch ohne Internet Antraege stellen und sich von Pflicht-Events abmelden — alles wird bei Reconnect automatisch gesendet.
Output: ActivityRequestModal + EventDetailView mit Queue-Fallback + KonfiRequestsPage mit pending-Anzeige
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-queue-kern-konfi-aktionen/60-CONTEXT.md

@frontend/src/components/konfi/modals/ActivityRequestModal.tsx
@frontend/src/components/konfi/views/EventDetailView.tsx
@frontend/src/components/konfi/pages/KonfiRequestsPage.tsx
@frontend/src/services/writeQueue.ts

<interfaces>
From frontend/src/services/writeQueue.ts (Plan 01):
```typescript
export const writeQueue = {
  enqueue(item): Promise<QueueItem>,
  getByMetadata(filter): Promise<QueueItem[]>,
};
```

From frontend/src/services/networkMonitor.ts:
```typescript
export const networkMonitor = { get isOnline(): boolean };
```

From frontend/src/components/konfi/modals/ActivityRequestModal.tsx:
```typescript
// Zeile ~172: submitRequest() — nutzt guard() + api.post('/konfi/requests', requestData)
// Zeile ~126: uploadPhoto() — api.post('/konfi/upload-photo', FormData)
// Zeile ~133: response.data.filename wird als photo_filename an requestData uebergeben
```

From frontend/src/components/konfi/views/EventDetailView.tsx:
```typescript
// Zeile ~122: handleOptOut(reason) — api.post(`/konfi/events/${eventData.id}/opt-out`, { reason })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityRequestModal offline-faehig machen</name>
  <files>frontend/src/components/konfi/modals/ActivityRequestModal.tsx</files>
  <read_first>
    - frontend/src/components/konfi/modals/ActivityRequestModal.tsx (komplett)
    - frontend/src/services/writeQueue.ts (enqueue API)
    - frontend/src/services/networkMonitor.ts
  </read_first>
  <action>
Per D-10, D-11, D-12:

1. Imports hinzufuegen:
```typescript
import { writeQueue } from '../../../services/writeQueue';
import { networkMonitor } from '../../../services/networkMonitor';
import { Filesystem, Directory } from '@capacitor/filesystem';
import { v4 as uuidv4 } from 'uuid';
```

2. submitRequest Funktion (Zeile ~172) erweitern — Offline-Pfad hinzufuegen:

In der bestehenden `submitRequest` async Funktion, INNERHALB des guard()-Callbacks:

```typescript
const clientId = uuidv4();

if (networkMonitor.isOnline) {
  // Bestehender Online-Code bleibt unveraendert:
  // uploadPhoto() + api.post('/konfi/requests', requestData)
  // Nur client_id zum requestData hinzufuegen:
  requestData.client_id = clientId;
  // ... rest wie bisher
} else {
  // Offline-Pfad (per D-10)
  let hasFileUpload = false;
  const queueBody: any = {
    activity_id: parseInt(formData.activity_id),
    description: formData.description.trim(),
    requested_date: formData.requested_date,
    client_id: clientId,
  };

  if (formData.photo_file) {
    // Foto lokal speichern (per D-11)
    hasFileUpload = true;
    try {
      const reader = new FileReader();
      const base64 = await new Promise<string>((resolve, reject) => {
        reader.onload = () => resolve(reader.result as string);
        reader.onerror = reject;
        reader.readAsDataURL(formData.photo_file!);
      });
      const fileName = `queue_${clientId}_photo.jpg`;
      await Filesystem.writeFile({
        path: `queue-uploads/${fileName}`,
        data: base64,
        directory: Directory.Data,
      });
      queueBody._localPhotoPath = `queue-uploads/${fileName}`;
      queueBody._photoFileName = formData.photo_file.name;
    } catch (err) {
      setError('Foto konnte nicht lokal gespeichert werden');
      return;
    }
  }

  await writeQueue.enqueue({
    method: 'POST',
    url: '/konfi/requests',
    body: queueBody,
    maxRetries: 5,
    hasFileUpload,
    metadata: {
      type: 'request',
      clientId,
      label: 'Aktivitaets-Antrag',
    },
  });

  setSuccess('Antrag wird gesendet sobald du wieder online bist');
  onSuccess(); // Modal schliessen
}
```

WICHTIG: Die Queue-Flush-Logik in writeQueue.ts muss bei Items mit `_localPhotoPath` den Zwei-Schritt-Prozess machen: Erst Foto uploaden (POST /konfi/upload-photo), dann Request mit photo_filename senden. Diese Logik in writeQueue.ts ergaenzen falls nicht schon vorhanden:
- Pruefen ob `body._localPhotoPath` existiert
- Datei aus Filesystem lesen
- FormData mit Foto an `/konfi/upload-photo` senden
- Response filename in body.photo_filename setzen
- `_localPhotoPath` und `_photoFileName` aus body entfernen
- Dann normalen POST an body.url senden
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "writeQueue" frontend/src/components/konfi/modals/ActivityRequestModal.tsx
    - grep -q "networkMonitor.isOnline" frontend/src/components/konfi/modals/ActivityRequestModal.tsx
    - grep -q "client_id\|clientId" frontend/src/components/konfi/modals/ActivityRequestModal.tsx
    - grep -q "Filesystem" frontend/src/components/konfi/modals/ActivityRequestModal.tsx
    - grep -q "queue-uploads" frontend/src/components/konfi/modals/ActivityRequestModal.tsx
  </acceptance_criteria>
  <done>ActivityRequestModal sendet offline Antraege via writeQueue, speichert Fotos lokal, zeigt Erfolgs-Meldung</done>
</task>

<task type="auto">
  <name>Task 2: Opt-out Queue + KonfiRequestsPage pending-Anzeige</name>
  <files>frontend/src/components/konfi/views/EventDetailView.tsx, frontend/src/components/konfi/pages/KonfiRequestsPage.tsx</files>
  <read_first>
    - frontend/src/components/konfi/views/EventDetailView.tsx (handleOptOut ab Zeile ~122)
    - frontend/src/components/konfi/pages/KonfiRequestsPage.tsx (Antrags-Liste)
    - frontend/src/services/writeQueue.ts
  </read_first>
  <action>
**EventDetailView.tsx — Opt-out Queue (per D-13):**

1. Imports hinzufuegen:
```typescript
import { writeQueue } from '../../../services/writeQueue';
import { networkMonitor } from '../../../services/networkMonitor';
import { v4 as uuidv4 } from 'uuid';
```

2. handleOptOut erweitern (Zeile ~122):
```typescript
const handleOptOut = async (reason: string) => {
  if (!eventData || reason.trim().length < 5) {
    setError('Bitte gib einen Grund fuer die Abmeldung an (mind. 5 Zeichen)');
    return;
  }

  const clientId = uuidv4();

  if (networkMonitor.isOnline) {
    // Bestehender Code bleibt:
    try {
      await api.post(`/konfi/events/${eventData.id}/opt-out`, {
        reason: reason.trim(),
        client_id: clientId,
      });
      setSuccess(`Von "${eventData.name}" abgemeldet`);
      await loadEventData();
      window.dispatchEvent(new CustomEvent('events-updated'));
    } catch (err: any) {
      setError(err.response?.data?.error || 'Fehler bei der Abmeldung');
    }
  } else {
    // Offline: Queue
    await writeQueue.enqueue({
      method: 'POST',
      url: `/konfi/events/${eventData.id}/opt-out`,
      body: { reason: reason.trim(), client_id: clientId },
      maxRetries: 5,
      hasFileUpload: false,
      metadata: {
        type: 'opt-out',
        clientId,
        label: `Abmeldung von "${eventData.name}"`,
      },
    });
    setSuccess('Abmeldung wird gesendet sobald du wieder online bist');
    // Optimistic UI: Event-Status lokal aktualisieren
    // EventDetailView laedt nach — der Cache zeigt den alten Status,
    // aber ein Corner-Badge Uhr-Icon zeigt den pending Status
  }
};
```

**KonfiRequestsPage.tsx — Pending Queue-Antraege anzeigen:**

1. Imports hinzufuegen:
```typescript
import { writeQueue } from '../../../services/writeQueue';
import { timeOutline } from 'ionicons/icons';
```

2. Neuen State fuer pending Queue-Items:
```typescript
const [pendingRequests, setPendingRequests] = useState<any[]>([]);
```

3. In loadData (oder useEffect): Queue-Items vom Typ 'request' laden:
```typescript
const loadPendingFromQueue = async () => {
  const queueItems = await writeQueue.getByMetadata({ type: 'request' });
  setPendingRequests(queueItems.map(qi => ({
    id: `pending-${qi.id}`,
    activity_name: `Antrag (wird gesendet)`,
    description: qi.body?.description || '',
    status: 'pending_queue',
    requested_date: new Date(qi.createdAt).toISOString(),
    _queueId: qi.id,
  })));
};
```

4. In der Render-Liste: pendingRequests VOR den echten Requests anzeigen, jeweils mit Corner-Badge (Uhr-Icon timeOutline, orange). Das genaue Rendering haengt vom bestehenden Listen-Pattern ab — die pendingRequests als IonItem mit dem `.app-corner-badges` Container und dem Queue-Badge rendern.

WICHTIG: Den bestehenden Code der KonfiRequestsPage NICHT umbauen. Nur die pending-Items oben in die Liste einfuegen. Beim naechsten Reload (nach Queue-Flush) sind die echten Items da und die pending-Items verschwinden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "writeQueue" frontend/src/components/konfi/views/EventDetailView.tsx
    - grep -q "networkMonitor.isOnline" frontend/src/components/konfi/views/EventDetailView.tsx
    - grep -q "opt-out" frontend/src/components/konfi/views/EventDetailView.tsx
    - grep -q "writeQueue\|pendingRequests" frontend/src/components/konfi/pages/KonfiRequestsPage.tsx
    - grep -q "pending_queue\|timeOutline" frontend/src/components/konfi/pages/KonfiRequestsPage.tsx
  </acceptance_criteria>
  <done>Opt-out wird offline via Queue gesendet mit Erfolgs-Meldung. KonfiRequestsPage zeigt pending Queue-Antraege mit Uhr-Icon.</done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei
- ActivityRequestModal hat Online/Offline-Pfad mit Queue-Fallback
- EventDetailView hat Opt-out Queue-Fallback
- KonfiRequestsPage zeigt pending Queue-Items
</verification>

<success_criteria>
- Offline Aktivitaets-Antraege werden gequeued und optimistisch angezeigt
- Fotos werden lokal gespeichert fuer spaeteres Upload
- Opt-out wird offline gequeued
- QUE-K03, QUE-K04, QUE-K05 adressiert
</success_criteria>

<output>
After completion, create `.planning/phases/60-queue-kern-konfi-aktionen/60-03-SUMMARY.md`
</output>
