---
phase: 60-queue-kern-konfi-aktionen
plan: 04
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - frontend/src/contexts/BadgeContext.tsx
  - frontend/src/components/chat/ChatRoom.tsx
  - frontend/src/components/konfi/pages/KonfiBadgesPage.tsx
  - frontend/src/components/teamer/views/TeamerBadgesView.tsx
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
  - frontend/src/components/admin/settings/ChatPermissionsSettings.tsx
  - frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx
autonomous: true
requirements: [QUE-FF01, QUE-FF02, QUE-FF03, QUE-FF04, QUE-FF05, QUE-FF06, QUE-FF07, QUE-FF08, OUI-13]

must_haves:
  truths:
    - "Chat mark-as-read wird offline gequeued ohne UI-Feedback"
    - "Emoji-Reaktionen werden sofort optimistisch angezeigt und offline gequeued"
    - "Badges-mark-seen wird offline gequeued ohne UI-Feedback"
    - "Poll-Abstimmung wird sofort optimistisch angezeigt und offline gequeued"
    - "Bibeluebersetzung aendern wird offline gequeued mit optimistischem UI-Update"
    - "Dashboard-Settings Toggles werden offline gequeued mit optimistischem UI-Update"
    - "Chat-Permissions Toggles werden offline gequeued mit optimistischem UI-Update"
    - "Funktionsbeschreibung aendern wird offline gequeued mit optimistischem UI-Update"
    - "Kein Queue-Feedback bei Fire-and-Forget — rein optimistisch"
  artifacts:
    - path: "frontend/src/contexts/BadgeContext.tsx"
      provides: "mark-read mit Queue-Fallback"
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "Reaktionen + Poll mit Queue-Fallback"
  key_links:
    - from: "frontend/src/contexts/BadgeContext.tsx"
      to: "frontend/src/services/writeQueue.ts"
      via: "writeQueue.enqueue bei offline mark-read"
      pattern: "writeQueue\\.enqueue"
    - from: "frontend/src/components/chat/ChatRoom.tsx"
      to: "frontend/src/services/writeQueue.ts"
      via: "writeQueue.enqueue bei offline reaction/poll"
      pattern: "writeQueue\\.enqueue"
---

<objective>
Alle 8 Fire-and-Forget-Aktionen offline-faehig machen — rein optimistisch ohne Queue-Feedback.

Purpose: Mark-Read, Reaktionen, Poll-Votes, Badges-Mark-Seen, Bibeluebersetzung, Dashboard-Settings, Chat-Permissions und Funktionsbeschreibung funktionieren offline ohne Fehler — der User merkt keinen Unterschied.
Output: 8 Dateien mit Queue-Fallback fuer Fire-and-Forget-Aktionen
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-queue-kern-konfi-aktionen/60-CONTEXT.md

@frontend/src/contexts/BadgeContext.tsx
@frontend/src/components/chat/ChatRoom.tsx
@frontend/src/components/konfi/pages/KonfiBadgesPage.tsx
@frontend/src/components/teamer/views/TeamerBadgesView.tsx
@frontend/src/components/konfi/views/ProfileView.tsx
@frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
@frontend/src/components/admin/settings/ChatPermissionsSettings.tsx
@frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx
@frontend/src/services/writeQueue.ts

<interfaces>
From frontend/src/services/writeQueue.ts (Plan 01):
```typescript
export const writeQueue = {
  enqueue(item): Promise<QueueItem>,
};
```

From frontend/src/services/networkMonitor.ts:
```typescript
export const networkMonitor = { get isOnline(): boolean };
```

Fire-and-Forget API-Endpunkte:
```
POST /chat/rooms/:id/mark-read          — BadgeContext Zeile ~106
POST /chat/messages/:id/reactions        — ChatRoom Zeile ~543
POST /chat/polls/:id/vote               — ChatRoom Zeile ~500
POST /konfi/badges/mark-seen             — KonfiBadgesPage Zeile ~80
PUT  /teamer/badges/mark-seen            — TeamerBadgesView Zeile ~233
PUT  /konfi/bible-translation            — ProfileView Zeile ~125
PUT  /settings                           — AdminDashboardSettingsPage Zeile ~91/100
PUT  /admin/settings                     — ChatPermissionsSettings Zeile ~42
POST /auth/update-role-title             — ChangeRoleTitleModal Zeile ~54
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat Fire-and-Forget (mark-read, Reaktionen, Poll)</name>
  <files>frontend/src/contexts/BadgeContext.tsx, frontend/src/components/chat/ChatRoom.tsx</files>
  <read_first>
    - frontend/src/contexts/BadgeContext.tsx (markRoomAsRead Funktion, ca. Zeile 90-110)
    - frontend/src/components/chat/ChatRoom.tsx (toggleReaction ca. Zeile ~540, voteInPoll ca. Zeile ~497)
    - frontend/src/services/writeQueue.ts
  </read_first>
  <action>
Per D-14, D-15 — kein Queue-Feedback, rein optimistisch:

**BadgeContext.tsx — mark-read Queue-Fallback (QUE-FF01):**

1. Import hinzufuegen:
```typescript
import { writeQueue } from '../services/writeQueue';
import { networkMonitor } from '../services/networkMonitor';
```

2. Die markRoomAsRead Funktion (die `api.post('/chat/rooms/${roomId}/mark-read')` aufruft, ca. Zeile ~106):
- Den bestehenden api.post-Call wrappen: Wenn networkMonitor.isOnline, normal ausfuehren. Wenn offline, stattdessen writeQueue.enqueue:
```typescript
if (!networkMonitor.isOnline) {
  writeQueue.enqueue({
    method: 'POST',
    url: `/chat/rooms/${roomId}/mark-read`,
    maxRetries: 3,
    hasFileUpload: false,
    metadata: { type: 'fire-and-forget', clientId: `mark-read-${roomId}-${Date.now()}`, label: 'Mark-Read' },
  });
  return; // Fire-and-forget: kein Warten
}
// Bestehender api.post Code bleibt
```
WICHTIG: Das optimistische Badge-Update (lokal Counts auf 0 setzen) passiert SCHON vor dem API-Call — das bleibt so. Nur der API-Call wird gequeued.

**ChatRoom.tsx — Reaktionen Queue-Fallback (QUE-FF02):**

3. In der toggleReaction Funktion (die `api.post('/chat/messages/${messageId}/reactions', { emoji })` aufruft):
- Die optimistische UI-Aenderung (Zeile ~549-572) passiert SCHON vor dem API-Call — das bleibt so.
- Den api.post wrappen:
```typescript
if (!networkMonitor.isOnline) {
  writeQueue.enqueue({
    method: 'POST',
    url: `/chat/messages/${messageId}/reactions`,
    body: { emoji },
    maxRetries: 3,
    hasFileUpload: false,
    metadata: { type: 'fire-and-forget', clientId: `reaction-${messageId}-${emoji}-${Date.now()}`, label: 'Reaktion' },
  });
  return; // Optimistic UI ist bereits gemacht
}
// Bestehender api.post Code bleibt
```

**ChatRoom.tsx — Poll-Abstimmung Queue-Fallback (QUE-FF04):**

4. In der voteInPoll Funktion (Zeile ~497):
- setShouldAutoScroll(false) bleibt.
- Den api.post wrappen:
```typescript
if (!networkMonitor.isOnline) {
  // Optimistic UI: Lokal den Vote anzeigen
  setMessages(prev => prev.map(m => {
    if (m.id === messageId && m.votes) {
      // Lokalen Vote hinzufuegen (optimistisch)
      const newVote: PollVote = {
        user_id: user?.id ?? 0,
        user_type: (user?.role_name === 'konfi' ? 'konfi' : 'admin') as 'admin' | 'konfi',
        option_index: optionIndex,
      };
      return { ...m, votes: [...m.votes, newVote] };
    }
    return m;
  }));

  writeQueue.enqueue({
    method: 'POST',
    url: `/chat/polls/${messageId}/vote`,
    body: { option_index: optionIndex },
    maxRetries: 3,
    hasFileUpload: false,
    metadata: { type: 'fire-and-forget', clientId: `poll-${messageId}-${optionIndex}-${Date.now()}`, label: 'Abstimmung' },
  });
  setTimeout(() => setShouldAutoScroll(true), 1000);
  return;
}
// Bestehender api.post Code bleibt
```

WICHTIG: Bei allen Fire-and-Forget-Aktionen: Den bestehenden try/catch NICHT entfernen. Nur einen `if (!networkMonitor.isOnline)` Block VOR den try-Block setzen mit return. Der Online-Pfad bleibt unveraendert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "writeQueue" frontend/src/contexts/BadgeContext.tsx
    - grep -q "networkMonitor.isOnline" frontend/src/contexts/BadgeContext.tsx
    - grep -q "fire-and-forget" frontend/src/contexts/BadgeContext.tsx
    - grep -q "fire-and-forget.*reaction\|reaction.*fire-and-forget" frontend/src/components/chat/ChatRoom.tsx
    - grep -q "fire-and-forget.*poll\|poll.*fire-and-forget" frontend/src/components/chat/ChatRoom.tsx
  </acceptance_criteria>
  <done>mark-read, Reaktionen und Poll-Votes werden offline gequeued, optimistische UI bleibt unveraendert, kein Queue-Feedback</done>
</task>

<task type="auto">
  <name>Task 2: Remaining Fire-and-Forget (Badges, Bible, Settings, RoleTitle)</name>
  <files>frontend/src/components/konfi/pages/KonfiBadgesPage.tsx, frontend/src/components/teamer/views/TeamerBadgesView.tsx, frontend/src/components/konfi/views/ProfileView.tsx, frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx, frontend/src/components/admin/settings/ChatPermissionsSettings.tsx, frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx</files>
  <read_first>
    - frontend/src/components/konfi/pages/KonfiBadgesPage.tsx (mark-seen ca. Zeile 80)
    - frontend/src/components/teamer/views/TeamerBadgesView.tsx (mark-seen ca. Zeile 233)
    - frontend/src/components/konfi/views/ProfileView.tsx (bible-translation ca. Zeile 125)
    - frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx (settings toggle ca. Zeile 91)
    - frontend/src/components/admin/settings/ChatPermissionsSettings.tsx (settings ca. Zeile 42)
    - frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx (role-title ca. Zeile 54)
    - frontend/src/services/writeQueue.ts
  </read_first>
  <action>
Per D-14, D-15 — gleiches Pattern fuer alle 5 verbleibenden Fire-and-Forget-Aktionen. Jede Datei bekommt:

1. Imports:
```typescript
import { writeQueue } from '...path.../services/writeQueue';
import { networkMonitor } from '...path.../services/networkMonitor';
```

2. Pattern fuer jeden API-Call: `if (!networkMonitor.isOnline) { writeQueue.enqueue(...); return; }` VOR dem bestehenden try-Block.

**KonfiBadgesPage.tsx — Badges mark-seen (QUE-FF03):**
- API-Call: `api.post('/konfi/badges/mark-seen')` (Zeile ~80)
- Der Call ist schon in einem `.catch()` (fire-and-forget Pattern bereits vorhanden!)
- Offline: writeQueue.enqueue mit type 'fire-and-forget', method 'POST', url '/konfi/badges/mark-seen'
- Optimistic UI: Badges werden SCHON beim Laden als "gesehen" behandelt

**TeamerBadgesView.tsx — Teamer Badges mark-seen (QUE-FF03):**
- API-Call: `api.put('/teamer/badges/mark-seen')` (Zeile ~233)
- Offline: writeQueue.enqueue mit type 'fire-and-forget', method 'PUT', url '/teamer/badges/mark-seen'

**ProfileView.tsx — Bibeluebersetzung (QUE-FF05):**
- API-Call: `api.put('/konfi/bible-translation', { translation })` (Zeile ~125)
- Offline: writeQueue.enqueue mit body { translation }
- Optimistic UI: `setSelectedTranslation(translation)` und `setSuccess(...)` AUCH im Offline-Pfad ausfuehren
- Den `onReload()` Aufruf im Offline-Pfad NICHT machen (kein Netzwerk)

**AdminDashboardSettingsPage.tsx — Dashboard-Settings Toggle (QUE-FF06):**
- API-Calls: `api.put('/settings', { [key]: value })` (Zeile ~91 und ~100)
- Offline: writeQueue.enqueue mit body { [key]: value }
- Optimistic UI: Die lokalen State-Updates passieren SCHON vor dem API-Call (Toggle-State)

**ChatPermissionsSettings.tsx — Chat-Permissions (QUE-FF07):**
- API-Call: `api.put('/admin/settings', { ... })` (Zeile ~42)
- Offline: writeQueue.enqueue mit body
- Optimistic UI: Lokaler State-Update vor API-Call

**ChangeRoleTitleModal.tsx — Funktionsbeschreibung (QUE-FF08):**
- API-Call: `api.post('/auth/update-role-title', { ... })` (Zeile ~54)
- Offline: writeQueue.enqueue
- Optimistic UI: Erfolgs-Meldung + onSuccess() auch im Offline-Pfad

Fuer ALLE 6 Dateien gilt:
- maxRetries: 3 (weniger als Standard-Queue, da Fire-and-Forget unkritisch)
- hasFileUpload: false
- metadata.type: 'fire-and-forget'
- clientId: Eindeutiger String mit Aktion + Timestamp
- Kein Uhr-Icon, kein Corner-Badge, kein Queue-Feedback (per OUI-13, D-14)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "writeQueue" frontend/src/components/konfi/pages/KonfiBadgesPage.tsx
    - grep -q "writeQueue" frontend/src/components/teamer/views/TeamerBadgesView.tsx
    - grep -q "writeQueue" frontend/src/components/konfi/views/ProfileView.tsx
    - grep -q "writeQueue" frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
    - grep -q "writeQueue" frontend/src/components/admin/settings/ChatPermissionsSettings.tsx
    - grep -q "writeQueue" frontend/src/components/admin/modals/ChangeRoleTitleModal.tsx
    - grep -q "networkMonitor.isOnline" frontend/src/components/konfi/views/ProfileView.tsx
    - grep -q "fire-and-forget" frontend/src/components/admin/pages/AdminDashboardSettingsPage.tsx
  </acceptance_criteria>
  <done>Alle 5 verbleibenden Fire-and-Forget-Aktionen (Badges mark-seen, Bibeluebersetzung, Dashboard-Settings, Chat-Permissions, Funktionsbeschreibung) werden offline gequeued mit rein optimistischer UI</done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei
- Alle 8 Fire-and-Forget-Aktionen haben Offline-Pfad mit writeQueue.enqueue
- Kein Queue-Feedback (keine Uhr-Icons, keine Badges) bei Fire-and-Forget
- Optimistic UI funktioniert in allen Faellen
</verification>

<success_criteria>
- Alle 8 Fire-and-Forget-Endpunkte haben Queue-Fallback
- Rein optimistisch — kein Queue-Feedback sichtbar (per OUI-13)
- TypeScript kompiliert fehlerfrei
- QUE-FF01 bis QUE-FF08 und OUI-13 adressiert
</success_criteria>

<output>
After completion, create `.planning/phases/60-queue-kern-konfi-aktionen/60-04-SUMMARY.md`
</output>
