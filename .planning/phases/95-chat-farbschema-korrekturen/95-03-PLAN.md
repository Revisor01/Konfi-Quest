---
phase: 95-chat-farbschema-korrekturen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/ChatRoom.tsx
  - frontend/src/components/chat/modals/ChatOptionsModal.tsx
  - backend/routes/chat.js
autonomous: true
requirements: [ACH-05, ACH-06]

must_haves:
  truths:
    - "Admins sehen keinen Verlassen-Button in Chats"
    - "Admins sehen Hinweistext warum sie nicht verlassen koennen"
    - "Backend blockiert Leave-Versuch fuer Admins mit 403 bei ALLEN Raumtypen (nicht nur admin)"
    - "Chat-Loesch-Dialog zeigt Info-Text ueber Auswirkung auf andere Teilnehmer:innen"
  artifacts:
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "canLeaveChat blockiert Admins komplett"
      contains: "admin.*false"
    - path: "backend/routes/chat.js"
      provides: "Admin-Leave-Sperre bei allen Raumtypen"
    - path: "frontend/src/components/chat/modals/ChatOptionsModal.tsx"
      provides: "Loesch-Dialog mit Teilnehmer-Hinweis"
  key_links:
    - from: "frontend/src/components/chat/ChatRoom.tsx"
      to: "backend/routes/chat.js"
      via: "DELETE /chat/rooms/:roomId/leave"
      pattern: "leave"
---

<objective>
Admin Chat-Regeln implementieren: Admins duerfen NIEMALS einen Chat verlassen (mit Hinweistext), und der Loesch-Dialog informiert ueber Auswirkungen auf andere Teilnehmer:innen.

Purpose: Konsistente Admin-Chat-Regeln verhindern versehentlichen Datenverlust
Output: Gesperrte Leave-Funktion fuer Admins, informativer Loesch-Dialog
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-chat-farbschema-korrekturen/95-CONTEXT.md

@frontend/src/components/chat/ChatRoom.tsx
@frontend/src/components/chat/modals/ChatOptionsModal.tsx
@backend/routes/chat.js

<interfaces>
<!-- canLeaveChat in ChatRoom.tsx Zeile 948-953 — aktuell: Admins koennen group-Chats verlassen! -->
```typescript
const canLeaveChat = (): boolean => {
  if (!room) return false;
  if (room.type === 'group') return true;                     // BUG: Auch Admins!
  if (room.type === 'admin' && user?.type !== 'admin') return true;
  return false;
};
```

<!-- Backend chat.js Zeile 1020-1027 — prueft nur admin-Raumtyp, nicht group -->
```typescript
if (room.type === 'jahrgang' || room.type === 'direct') {
  return res.status(400).json({ error: 'Dieser Chat kann nicht verlassen werden' });
}
if (room.type === 'admin' && userType === 'admin') {
  return res.status(403).json({ error: 'Admins können diesen Chat nicht verlassen' });
}
// BUG: Admin kann group-Chats verlassen!
```

<!-- ChatHeader bekommt canLeave prop (ChatRoom.tsx Zeile 983): -->
```typescript
<ChatHeader
  canLeave={canLeaveChat()}
  onLeaveChat={handleLeaveChat}
  ...
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Leave-Sperre (Frontend + Backend) + Loesch-Dialog</name>
  <files>frontend/src/components/chat/ChatRoom.tsx, backend/routes/chat.js, frontend/src/components/chat/modals/ChatOptionsModal.tsx</files>
  <read_first>frontend/src/components/chat/ChatRoom.tsx, backend/routes/chat.js, frontend/src/components/chat/modals/ChatOptionsModal.tsx</read_first>
  <action>
**Frontend — Admin Leave komplett sperren (ACH-05):**

1. In ChatRoom.tsx die `canLeaveChat()` Funktion (Zeile 948-953) aendern:

```typescript
const canLeaveChat = (): boolean => {
  if (!room) return false;
  // Admins duerfen NIEMALS einen Chat verlassen
  if (user?.type === 'admin') return false;
  // Teamer:innen duerfen group und admin-Chats verlassen
  if (room.type === 'group') return true;
  if (room.type === 'admin') return true;
  return false;
};
```

2. Einen Hinweistext einbauen fuer Admins. Im ChatRoom-Return, NACH dem ChatHeader-Block (oder als Teil davon), wenn `user?.type === 'admin'` und der Raum-Typ `group` oder `admin` ist, einen kleinen Info-Hinweis anzeigen. Alternativ: Im ChatOptionsModal (falls vorhanden) einen deaktivierten "Verlassen"-Button mit Erklaerungstext zeigen.

3. Pruefen ob ChatOptionsModal.tsx einen "Verlassen"-Button hat. Falls ja, dort fuer Admins den Button ausblenden und stattdessen einen IonNote/Hinweis anzeigen: "Admins koennen Chats nicht verlassen. Chats koennen nur geloescht werden."

**Backend — Admin Leave bei ALLEN Raumtypen sperren (ACH-05):**

4. In backend/routes/chat.js die Leave-Route (Zeile 1009-1043) anpassen. Die Admin-Pruefung MUSS VOR der Raumtyp-Pruefung kommen und fuer ALLE Raumtypen gelten:

```javascript
// Admin darf NIEMALS irgendeinen Chat verlassen
if (userType === 'admin') {
  return res.status(403).json({ error: 'Admins können Chats nicht verlassen' });
}

// Jahrgang und Direct sind generell nicht verlassbar
if (room.type === 'jahrgang' || room.type === 'direct') {
  return res.status(400).json({ error: 'Dieser Chat kann nicht verlassen werden' });
}
```

**Loesch-Dialog Info-Text (ACH-06):**

5. In ChatOptionsModal.tsx (oder wo der Loesch-Dialog getriggert wird) den Bestaetigungs-Dialog erweitern. Im Alert/Dialog fuer Chat-Loeschen den Message-Text ergaenzen: "Chat wird fuer alle Teilnehmer:innen geloescht. Alle Nachrichten und Dateien gehen unwiderruflich verloren."

6. Falls der Loesch-Dialog in ChatRoom.tsx statt ChatOptionsModal.tsx ist: Dort den presentAlert message-Text anpassen.

7. Pruefen ob FK-Kaskade korrekt funktioniert (laut CONTEXT.md bereits OK). Kein Code-Change noetig, nur verifizieren dass DELETE FROM chat_rooms die Kaskade auf participants, messages, polls, poll_votes ausfuehrt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "admin.*false\|type.*admin.*return false" frontend/src/components/chat/ChatRoom.tsx && grep "userType.*admin.*403\|admin.*nicht verlassen" backend/routes/chat.js && grep -c "alle Teilnehmer" frontend/src/components/chat/modals/ChatOptionsModal.tsx frontend/src/components/chat/ChatRoom.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - canLeaveChat() gibt false zurueck wenn user.type === 'admin'
    - Backend Leave-Route gibt 403 fuer ALLE Admins zurueck (nicht nur bei admin-Raumtyp)
    - Hinweistext "Admins koennen Chats nicht verlassen" ist im Frontend sichtbar
    - Loesch-Dialog enthaelt "alle Teilnehmer:innen" Info-Text
    - TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
  </acceptance_criteria>
  <done>Admins koennen keinen Chat verlassen (Frontend + Backend), Loesch-Dialog informiert ueber Auswirkungen auf alle Teilnehmer:innen</done>
</task>

</tasks>

<verification>
- Admin-User sieht keinen "Verlassen"-Button in keinem Chat-Typ
- Teamer:innen sehen "Verlassen"-Button in group- und admin-Chats
- Backend gibt 403 bei Admin Leave-Versuch (curl -X DELETE .../leave mit Admin-Token)
- Loesch-Dialog zeigt Hinweis auf alle Teilnehmer:innen
- Frontend kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- ACH-05: Admins koennen Chat nicht verlassen, Hinweistext erklaert warum
- ACH-06: Chat-Loeschen-Dialog informiert ueber Auswirkung auf alle Teilnehmer:innen, FK-Kaskade funktioniert korrekt
</success_criteria>

<output>
After completion, create `.planning/phases/95-chat-farbschema-korrekturen/95-03-SUMMARY.md`
</output>
