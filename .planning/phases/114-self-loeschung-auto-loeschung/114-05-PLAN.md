---
phase: 114-self-loeschung-auto-loeschung
plan: 05
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - backend/routes/events.js
  - backend/routes/chat.js
  - backend/routes/wrapped.js
  - backend/routes/badges.js
  - backend/routes/teamer.js
  - backend/tests/routes/events.test.js
  - backend/tests/routes/teamer.test.js
autonomous: true
requirements: [D-08, D-12]
must_haves:
  truths:
    - "Ein soft-geloeschter Konfi erscheint NICHT in Event-Teilnehmer-/Buchungslisten und wird nicht in neue Pflicht-Events auto-enrollt"
    - "Ein soft-geloeschter Konfi erscheint NICHT in Chat-Teilnehmer-/Empfaenger-Listen und Wrapped-Aggregaten"
    - "Ein soft-geloeschter Konfi erscheint NICHT in der Teamer-Konfi-Uebersicht und Badge-Empfaengerlisten"
  artifacts:
    - path: "backend/routes/events.js"
      provides: "deleted_at-Filter in Buchungs-/Teilnehmer-Listen + Auto-Enroll"
      contains: "deleted_at IS NULL"
    - path: "backend/routes/chat.js"
      provides: "deleted_at-Filter in Konfi-Empfaenger-/Teilnehmer-Quellen"
      contains: "deleted_at IS NULL"
  key_links:
    - from: "backend/routes/events.js"
      to: "users.deleted_at"
      via: "AND u.deleted_at IS NULL in Booking-Participant-Queries und Enroll-INSERT"
      pattern: "deleted_at IS NULL"
---

<objective>
Soft-Delete-Filter-Sweep Teil 2 — sekundaere Sichtbarkeitsflaechen (D-12). Ergaenzt
Plan 114-04 um Events, Chat, Wrapped, Badges und Teamer-Uebersicht. Zusammen mit 114-04
ist damit die D-12-Vollstaendigkeit erreicht: soft-geloeschte Konfis sind ueberall
unsichtbar (D-08).

Purpose: D-12 verlangt explizit, dass KEINE Query den Filter vergisst. Diese beiden Sweep-
Plans mappen die gesamte Konfi-Sichtbarkeit vollstaendig.
Output: deleted_at IS NULL Filter an allen sekundaeren Konfi-Query-Stellen + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/114-self-loeschung-auto-loeschung/114-CONTEXT.md
@CLAUDE.md

@backend/routes/events.js
@backend/routes/chat.js
@backend/routes/teamer.js

<interfaces>
<!-- users.deleted_at TIMESTAMP NULL (Migration 082). Filter `AND u.deleted_at IS NULL`
     in WHERE-Bedingung, Alias je Query. -->

KONKRETE STELLEN (Zeilennummern Stand jetzt — beim Editieren am Alias orientieren):

events.js (Buchungs-/Teilnehmer-Sichtbarkeit + Enroll):
  - Z.544   Booking-Teilnehmerliste (JOIN users u ON eb.user_id)      -> AND u.deleted_at IS NULL
  - Z.612   Abmeldungen (JOIN users u ON eu.user_id)                  -> AND u.deleted_at IS NULL
  - Z.742   Auto-Enroll INSERT ... SELECT FROM users u (Pflicht-Event)-> AND u.deleted_at IS NULL
  - Z.763   Push-Empfaenger-Auswahl (SELECT u.id FROM users u)        -> AND u.deleted_at IS NULL
  - Z.872   Auto-Enroll bei Event-Create (SELECT FROM users u)        -> AND u.deleted_at IS NULL
  - Z.1046, 1265, 1524, 1979, 2049  weitere Booking-Teilnehmerlisten  -> AND u.deleted_at IS NULL
  (jede Stelle pruefen: liefert sie Konfis als sichtbare Personen? dann filtern. Reine
   Schreib-/Status-Checks auf eigene user_id brauchen keinen Filter.)

chat.js (Konfi-Empfaenger/-Teilnehmer; Teamer/Admin-Listen NICHT filtern):
  - Z.347   Konfis-Auswahl fuer Chat (SELECT u.id FROM users u, role konfi) -> AND u.deleted_at IS NULL
  - Z.412   Nachrichten-Autor-Join (JOIN users u ON m.user_id)         -> AND u.deleted_at IS NULL nur wenn Autorenliste sichtbar Konfis ausschliessen soll; pruefen
  - Z.425   Teilnehmer-Anzeige (JOIN users u ON dp.user_id)            -> AND u.deleted_at IS NULL
  - Z.215/265/266 (admin/org_admin/teamer-Listen)  NICHT filtern (keine Konfis bzw. Teamer nie soft-geloescht)

wrapped.js (Konfi-Aggregate; verhindert Wrapped fuer Archivierte):
  - Z.476   JOIN users u ON kp.user_id  -> AND u.deleted_at IS NULL
  - Z.539   SELECT u.id FROM users u    -> AND u.deleted_at IS NULL
  - Z.694   JOIN users u ON kp.user_id  -> AND u.deleted_at IS NULL
  - Z.746   SELECT u.id FROM users u    -> AND u.deleted_at IS NULL

badges.js (Konfi-Badge-Empfaenger):
  - Z.122   JOIN users u ON kp.user_id  -> AND u.deleted_at IS NULL
  (Z.687/711 LEFT JOIN users u ON cb.created_by = u.id  -> NICHT filtern: das ist der
   Badge-Ersteller/Admin, kein sichtbarer Konfi)

teamer.js (Konfi-Uebersicht fuer Teamer):
  - Z.142   Konfi-Liste (FROM users u, r.name='konfi')  -> AND u.deleted_at IS NULL
  (Z.54 self-profile + Z.568 teamer-check  -> NICHT filtern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: events.js — Filter in Teilnehmer-/Buchungslisten + Auto-Enroll</name>
  <files>backend/routes/events.js, backend/tests/routes/events.test.js</files>
  <action>
Ergaenze `AND u.deleted_at IS NULL` an den events.js-Stellen aus dem interfaces-Block, an
denen Konfis als sichtbare Teilnehmende geliefert oder in Pflicht-Events auto-enrollt werden
(Z.544, 612, 742, 763, 872, 1046, 1265, 1524, 1979, 2049). Pruefe jede Stelle einzeln: nur
filtern, wenn die Query Konfis als Personen sichtbar macht oder zu Buchungen fuehrt. Reine
Status-/Schreib-Checks auf die eigene user_id brauchen keinen Filter. Filter in WHERE, nicht in
JOIN-ON (LEFT JOIN nicht zu INNER machen).

Erweitere tests/routes/events.test.js: seede Konfi mit Event-Buchung, soft-loesche den Konfi,
pruefe dass er nicht mehr in der Teilnehmerliste des Events erscheint; ein aktiver Konfi bleibt.
  </action>
  <verify>
    <automated>cd backend && grep -c "deleted_at IS NULL" routes/events.js | grep -qvE "^0$" && npx vitest run tests/routes/events.test.js</automated>
  </verify>
  <done>Event-Teilnehmerlisten + Auto-Enroll schliessen soft-geloeschte Konfis aus; Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: chat.js, wrapped.js, badges.js — Filter in Konfi-Quellen</name>
  <files>backend/routes/chat.js, backend/routes/wrapped.js, backend/routes/badges.js</files>
  <action>
Ergaenze `AND u.deleted_at IS NULL` an den im interfaces-Block gelisteten Konfi-sichtbar-machenden
Stellen in chat.js (Z.347, 425, ggf. 412), wrapped.js (Z.476, 539, 694, 746) und badges.js (Z.122).
NICHT filtern: admin/org_admin/teamer-Listen in chat.js (Z.215/265/266), Badge-Ersteller-Joins in
badges.js (Z.687/711) — das sind keine sichtbaren Konfis bzw. Teamer werden nie soft-geloescht.

Pruefe chat.js:412 (Nachrichten-Autor-Join) im Kontext: nur filtern, wenn dadurch keine
bestehenden Nachrichten verschwinden sollen, die ein archivierter Konfi geschrieben hat
(historische Nachrichten bleiben sichtbar, nur die Person nicht mehr aktiv). Im Zweifel die
Teilnehmer-/Empfaenger-Listen filtern, die Nachrichten-Historie unangetastet lassen.
  </action>
  <verify>
    <automated>cd backend && for f in chat wrapped badges; do grep -c "deleted_at IS NULL" routes/$f.js | grep -qvE "^0$" || { echo "FEHLT in $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Chat-Teilnehmer/-Empfaenger, Wrapped-Aggregate und Badge-Empfaenger schliessen soft-geloeschte Konfis aus.</done>
</task>

<task type="auto">
  <name>Task 3: teamer.js — Filter in Teamer-Konfi-Uebersicht</name>
  <files>backend/routes/teamer.js, backend/tests/routes/teamer.test.js</files>
  <action>
Ergaenze `AND u.deleted_at IS NULL` an der Konfi-Listen-Query in teamer.js (~Z.142,
r.name='konfi'). NICHT filtern: self-profile (~Z.54) und teamer-check (~Z.568).

Erweitere tests/routes/teamer.test.js (falls Konfi-Uebersicht getestet wird): soft-geloeschter
Konfi erscheint nicht in der Teamer-Konfi-Uebersicht. Falls keine passende Test-Stelle existiert,
genuegt der Code-Filter (durch Plan-04-Tests + events-Test ist das Muster bereits abgesichert).
  </action>
  <verify>
    <automated>cd backend && grep -c "deleted_at IS NULL" routes/teamer.js | grep -qvE "^0$" && npx vitest run tests/routes/teamer.test.js</automated>
  </verify>
  <done>Teamer-Konfi-Uebersicht schliesst soft-geloeschte Konfis aus; self-profile/teamer-check unveraendert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soft-geloeschter Konfi -> sekundaere Sichtbarkeit | Events/Chat/Wrapped/Badges/Teamer-Uebersicht duerfen archivierte Konfis nicht zeigen (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-11 | Information Disclosure | archivierter Konfi in Event-/Chat-Liste sichtbar | mitigate | `u.deleted_at IS NULL` an allen sekundaeren Sichtbarkeitsquellen |
| T-114-12 | Tampering | archivierter Konfi wird in neues Pflicht-Event enrollt | mitigate | Auto-Enroll-SELECTs filtern deleted_at -> keine Buchungen fuer Archivierte |
| T-114-13 | Information Disclosure | Chat-Nachrichten-Historie versehentlich entfernt | accept | Nachrichten-Historie bleibt; nur Personen-/Teilnehmerlisten gefiltert (bewusst) |
</threat_model>

<verification>
- `cd backend && npx vitest run tests/routes/events.test.js tests/routes/teamer.test.js` gruen.
- Gesamt-Sweep (Plan 04 + 05): soft-geloeschter Konfi taucht in KEINER Ansicht auf.
- `grep -rc "deleted_at IS NULL" backend/routes/` zeigt Treffer in konfi-management, konfi, levels, events, chat, wrapped, badges, teamer.
</verification>

<success_criteria>
- deleted_at IS NULL Filter an allen sekundaeren Konfi-Sichtbarkeitsquellen gesetzt.
- Auto-Enroll erstellt keine Buchungen fuer archivierte Konfis.
- Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/114-self-loeschung-auto-loeschung/114-05-SUMMARY.md` when done
</output>
