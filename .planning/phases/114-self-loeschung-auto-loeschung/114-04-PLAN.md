---
phase: 114-self-loeschung-auto-loeschung
plan: 04
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - backend/routes/konfi-management.js
  - backend/routes/konfi.js
  - backend/routes/levels.js
  - backend/tests/routes/konfi-management.test.js
  - backend/tests/routes/konfi.test.js
autonomous: true
requirements: [D-08, D-12]
must_haves:
  truths:
    - "Ein Konfi mit gesetztem users.deleted_at erscheint NICHT mehr in der Admin-Konfi-Liste"
    - "Ein soft-geloeschter Konfi erscheint NICHT mehr im Jahrgang-Ranking (Konfi-Dashboard) und veraendert die Rangberechnung anderer Konfis nicht"
    - "Ein soft-geloeschter Konfi erscheint NICHT in Level-/Statistik-Aggregaten"
    - "Aktive (deleted_at IS NULL) Konfis bleiben in allen genannten Ansichten unveraendert sichtbar"
  artifacts:
    - path: "backend/routes/konfi-management.js"
      provides: "deleted_at-Filter in Konfi-Liste, Detail-Lookup, Ranking-Quellen"
      contains: "deleted_at IS NULL"
    - path: "backend/routes/konfi.js"
      provides: "deleted_at-Filter in Dashboard-Ranking + Profil-Ranking-Quellen"
      contains: "deleted_at IS NULL"
  key_links:
    - from: "backend/routes/konfi-management.js"
      to: "users.deleted_at"
      via: "WHERE u.deleted_at IS NULL in Konfi-Listen-Query"
      pattern: "deleted_at IS NULL"
---

<objective>
Soft-Delete-Filter-Sweep Teil 1 — Kern-Sichtbarkeit (D-12). Soft-geloeschte Konfis
(users.deleted_at IS NOT NULL, gesetzt ab Tag 60 durch den Cron-Job) muessen ueberall
unsichtbar werden (D-08: "komplett unsichtbar, auch fuer die Leitung").

Dieser Plan deckt die Kern-Sichtbarkeitsquellen ab: Admin-Konfi-Listen + Detail-Lookups
(konfi-management.js), Konfi-Dashboard + Profil-Ranking (konfi.js), und Level-/Statistik-
Aggregate (levels.js). Sekundaere Flaechen (Events, Chat, Wrapped, Badges, Aktivitaeten)
behandelt Plan 114-05.

Purpose: Ohne vollstaendigen Filter wuerden archivierte Konfis im Ranking auftauchen oder
die Rangberechnung verfaelschen — D-12 verlangt explizit Vollstaendigkeit.
Output: deleted_at IS NULL Filter an allen Kern-Konfi-Query-Stellen + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/114-self-loeschung-auto-loeschung/114-CONTEXT.md
@CLAUDE.md

@backend/routes/konfi-management.js
@backend/routes/konfi.js
@backend/routes/levels.js

<interfaces>
<!-- users.deleted_at TIMESTAMP NULL existiert nach Migration 082 (Plan 114-01).
     Filter-Muster wie chat_messages: jede Query, die Konfis als sichtbare Personen
     liefert, ergaenzt `AND u.deleted_at IS NULL` (Alias je Query anpassen). -->

KONKRETE STELLEN (Zeilennummern Stand jetzt — beim Editieren am Alias orientieren):

konfi-management.js:
  - Zeile 76   GET '/'        Konfi-Liste (FROM users u, r.name='konfi')   -> AND u.deleted_at IS NULL
  - Zeile 349  DELETE check   (SELECT u.id ... r.name='konfi')             -> AND u.deleted_at IS NULL
  - Zeile 434  GET '/:id'     Konfi-Detail (FROM users u, role IN konfi)   -> AND u.deleted_at IS NULL
  - Zeile 100  GET '/teamer'  Teamer-Liste (KEIN Filter noetig: Teamer sind nie soft-geloescht, D-10)
  - Zeile 923  promote-teamer (KEIN Filter: arbeitet vor Tag 60, Rettungsfenster D-07)

konfi.js (Ranking-Quellen — Filter verhindert Phantom-Raenge):
  - Zeile 52   dashboard konfi-Lookup (FROM users u)        -> AND u.deleted_at IS NULL
  - Zeile 106  dashboard ranking (FROM users u)             -> AND u.deleted_at IS NULL
  - Zeile 141 + 148  RANK()-Subqueries (users u / users u2) -> AND u.deleted_at IS NULL / u2.deleted_at IS NULL
  - Zeile 347 / 375  profile-Lookups (FROM users u)         -> AND u.deleted_at IS NULL
  - Zeile 442 + 449  profile RANK()-Subqueries              -> AND u.deleted_at IS NULL / u2.deleted_at IS NULL

levels.js:
  - Zeile 187  JOIN users u ON kp.user_id = u.id  (Aggregat) -> AND u.deleted_at IS NULL
  - Zeile 232  FROM users u                                  -> AND u.deleted_at IS NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: konfi-management.js — Filter in Liste, Detail, Delete-Check</name>
  <files>backend/routes/konfi-management.js, backend/tests/routes/konfi-management.test.js</files>
  <action>
Ergaenze `AND u.deleted_at IS NULL` (bzw. den passenden Alias) an den im interfaces-Block
genannten Stellen von konfi-management.js: GET '/' (Konfi-Liste, ~Z.76), DELETE-Existenz-Check
(~Z.349), GET '/:id' (Konfi-Detail, ~Z.434). NICHT filtern: GET '/teamer' (~Z.100) und
promote-teamer (~Z.923) — Teamer werden nie soft-geloescht (D-10) und das Promote arbeitet im
Rettungsfenster vor Tag 60 (D-07).

Pruefe beim Editieren den tatsaechlichen Alias jeder WHERE-Klausel (u, u.). Setze den Filter in
die WHERE-Bedingung, nicht in JOIN-ON, damit LEFT JOINs nicht unbeabsichtigt zu INNER werden.

Erweitere tests/routes/konfi-management.test.js: setze bei einem geseedeten Konfi
`UPDATE users SET deleted_at = NOW()` und pruefe: GET '/' enthaelt diesen Konfi NICHT mehr;
GET '/:id' fuer ihn -> 404; ein aktiver Konfi bleibt sichtbar.
  </action>
  <verify>
    <automated>cd backend && grep -c "deleted_at IS NULL" routes/konfi-management.js | grep -qvE "^0$" && npx vitest run tests/routes/konfi-management.test.js</automated>
  </verify>
  <done>Konfi-Liste/Detail/Delete-Check filtern deleted_at; Teamer-Liste + Promote unveraendert; Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: konfi.js — Filter in Dashboard- und Profil-Ranking</name>
  <files>backend/routes/konfi.js, backend/tests/routes/konfi.test.js</files>
  <action>
Ergaenze `AND u.deleted_at IS NULL` (bzw. u2 in den RANK()-Subqueries) an den im
interfaces-Block genannten konfi.js-Stellen: dashboard-Lookup (~Z.52), dashboard-ranking
(~Z.106), RANK()-Subqueries (~Z.141, ~Z.148), profile-Lookups (~Z.347, ~Z.375), profile-RANK()
(~Z.442, ~Z.449). Kritisch: BEIDE Seiten der RANK()-Berechnung filtern, sonst zaehlt ein
archivierter Konfi noch in die Rang-Gesamtzahl/Position hinein.

Erweitere tests/routes/konfi.test.js: seede zwei Konfis im selben Jahrgang, soft-loesche einen,
pruefe dass das Ranking des verbleibenden Konfis nur noch 1 Person zaehlt (total_in_jahrgang)
und der archivierte nicht im ranking-Array erscheint.
  </action>
  <verify>
    <automated>cd backend && grep -c "deleted_at IS NULL" routes/konfi.js | grep -qvE "^0$" && npx vitest run tests/routes/konfi.test.js</automated>
  </verify>
  <done>Dashboard- und Profil-Ranking ignorieren soft-geloeschte Konfis auf beiden RANK-Seiten; Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 3: levels.js — Filter in Level-/Statistik-Aggregaten</name>
  <files>backend/routes/levels.js</files>
  <action>
Ergaenze `AND u.deleted_at IS NULL` an den levels.js-Stellen (~Z.187 JOIN-Aggregat, ~Z.232
FROM users u). Filter gehoert in die WHERE-Bedingung. Da levels.js bisher keine dedizierte
Test-Datei mit Soft-Delete-Szenario braucht und die Aggregate ueber konfi.js/konfi-management.js
mitgetestet werden, genuegt hier der Code-Filter. Falls eine levels.test.js existiert, ergaenze
einen Soft-Delete-Ausschluss-Test analog.
  </action>
  <verify>
    <automated>cd backend && grep -c "deleted_at IS NULL" routes/levels.js | grep -qvE "^0$" && npx vitest run tests/routes/levels.test.js</automated>
  </verify>
  <done>Level-/Statistik-Aggregate schliessen soft-geloeschte Konfis aus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soft-geloeschter Konfi -> Sichtbarkeit | Archivierte Konfis duerfen ab Tag 60 nirgends mehr erscheinen (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-09 | Information Disclosure | archivierter Konfi sichtbar in Liste/Ranking | mitigate | `u.deleted_at IS NULL` an allen Kern-Sichtbarkeitsquellen; Tests pruefen Ausschluss |
| T-114-10 | Tampering | RANK() nur einseitig gefiltert | mitigate | beide RANK()-Subqueries (u und u2) filtern, damit Rang-Gesamtzahl korrekt bleibt |
</threat_model>

<verification>
- `cd backend && npx vitest run tests/routes/konfi-management.test.js tests/routes/konfi.test.js tests/routes/levels.test.js` gruen.
- Soft-geloeschter Konfi taucht in keiner Kern-Ansicht auf; aktive Konfis unveraendert.
</verification>

<success_criteria>
- deleted_at IS NULL Filter an allen im interfaces-Block gelisteten Kern-Stellen.
- RANK()-Berechnung beidseitig gefiltert.
- Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/114-self-loeschung-auto-loeschung/114-04-SUMMARY.md` when done
</output>
