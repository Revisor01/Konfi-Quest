---
phase: 96-konfi-ui
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/konfi/modals/PointsHistoryModal.tsx
autonomous: true
requirements: [KHI-01, KHI-02, KPR-01, KPR-02]
must_haves:
  truths:
    - "Punkte-Uebersicht und Badges sind als IonAccordionGroup direkt im Profil eingebaut"
    - "SectionHeader zeigt 6 Stats in 2 Reihen (3+3): Punkte, GD, Gemeinde, Events, Badges, Bonus"
    - "PointsHistoryModal wird nicht mehr aufgerufen (Akkordeons ersetzen es)"
    - "Bibeluebersetzung oeffnet ein Modal mit Erklaerungen statt ActionSheet"
  artifacts:
    - path: "frontend/src/components/konfi/views/ProfileView.tsx"
      provides: "Profile with accordion history and 6-stat SectionHeader and Bible modal"
      contains: "IonAccordionGroup"
    - path: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      provides: "Unchanged file, no longer called from ProfileView"
  key_links:
    - from: "ProfileView.tsx"
      to: "api /konfi/points-history"
      via: "fetch in useEffect for accordion data"
      pattern: "points-history"
---

<objective>
Profil-Bereich ueberarbeiten: 6 Stats im SectionHeader, Punkte-Historie als Akkordeons statt Modal, neues Bibeluebersetzungs-Modal mit Erklaerungen.

Purpose: Konfis sehen ihre Punkte-Historie direkt im Profil als ausklappbare Akkordeons (kein Extra-Modal-Klick). Bibeluebersetzungs-Auswahl bekommt Erklaerungen.
Output: Profilseite mit 6 Stats, Akkordeon-Historie, Bibel-Modal.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/konfi/views/ProfileView.tsx
@frontend/src/components/konfi/modals/PointsHistoryModal.tsx
@frontend/src/components/shared/SectionHeader.tsx
@frontend/src/theme/variables.css
</context>

<interfaces>
<!-- SectionHeader akzeptiert ein stats-Array mit beliebig vielen Items -->
From frontend/src/components/shared/SectionHeader.tsx:
```typescript
interface SectionHeaderProps {
  title: string;
  subtitle: string;
  icon: string;
  preset?: 'events' | 'activities' | 'konfis' | ...;
  stats: Array<{ value: number; label: string }>;
}
```

<!-- Stats-Row CSS: flex, gap 8px, max-width 100px pro Item -->
<!-- NOTE: flex-wrap: wrap wird von Plan 96-02 in variables.css ergaenzt -->
From variables.css:
```css
.app-stats-row { display: flex; gap: 8px; flex-wrap: wrap; }
.app-stats-row__item { flex: 1 1 0; max-width: 100px; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SectionHeader 6 Stats + Akkordeon-Historie (KHI-01, KHI-02, KPR-01)</name>
  <files>frontend/src/components/konfi/views/ProfileView.tsx</files>
  <read_first>frontend/src/components/konfi/views/ProfileView.tsx, frontend/src/components/konfi/modals/PointsHistoryModal.tsx</read_first>
  <action>
**1. SectionHeader auf 6 Stats in 2 Reihen erweitern (KPR-01):**

VORHER (Zeile ~306-310):
```tsx
stats={[
  { value: profile.total_points || 0, label: 'PUNKTE' },
  { value: earnedBadgesCount, label: 'BADGES' },
  { value: (profile.event_count || 0) + (profile.activity_count || 0), label: 'AKTIONEN' }
]}
```

NACHHER:
```tsx
stats={[
  { value: profile.total_points || 0, label: 'PUNKTE' },
  { value: profile.gottesdienst_points || 0, label: 'GD' },
  { value: profile.gemeinde_points || 0, label: 'GEMEINDE' },
  { value: profile.event_count || 0, label: 'EVENTS' },
  { value: earnedBadgesCount, label: 'BADGES' },
  { value: profile.bonus_points || 0, label: 'BONUS' }
]}
```

NOTE: Die CSS-Aenderung fuer `.app-stats-row { flex-wrap: wrap }` wird von Plan 96-02 in variables.css gemacht. Dieses Plan beruehrt variables.css NICHT.

**2. Punkte-Historie als IonAccordionGroup direkt im Profil (KHI-01, KHI-02):**

Neuen State + Effect fuer Punkte-Historie hinzufuegen:
```tsx
const [pointsHistory, setPointsHistory] = useState<any[]>([]);
const [pointsTotals, setPointsTotals] = useState<{ gottesdienst: number; gemeinde: number; total: number }>({ gottesdienst: 0, gemeinde: 0, total: 0 });

React.useEffect(() => {
  api.get('/konfi/points-history')
    .then(res => {
      setPointsHistory(res.data.history || []);
      setPointsTotals(res.data.totals || { gottesdienst: 0, gemeinde: 0, total: 0 });
    })
    .catch(() => {});
}, []);
```

Den "Punkte-Uebersicht" IonItem-Button (Zeile ~552-588) ersetzen durch eine IonAccordionGroup:

```tsx
{/* Punkte-Uebersicht als Akkordeon */}
<IonAccordionGroup>
  <IonAccordion value="punkte">
    <IonItem slot="header" lines="none" style={{ '--background': 'transparent', '--padding-start': '0', '--inner-padding-end': '0' }}>
      <div className="app-list-item app-list-item--purple" style={{ width: '100%' }}>
        <div className="app-list-item__row">
          <div className="app-list-item__main">
            <div className="app-icon-circle app-icon-circle--purple">
              <IonIcon icon={starOutline} />
            </div>
            <div className="app-list-item__content">
              <div className="app-list-item__title">Punkte-Uebersicht</div>
              <div className="app-list-item__meta">
                <span className="app-list-item__meta-item">{profile.total_points || 0} Punkte gesamt</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </IonItem>
    <div slot="content" style={{ padding: '0 16px 16px' }}>
      {/* Stats Grid */}
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '8px', marginBottom: '12px' }}>
        <div className="app-info-box app-info-box--neutral" style={{ textAlign: 'center', padding: '8px' }}>
          <div style={{ fontSize: '1.1rem', fontWeight: '700' }}>{pointsTotals.gottesdienst}</div>
          <div style={{ fontSize: '0.7rem', color: '#666' }}>GD</div>
        </div>
        <div className="app-info-box app-info-box--neutral" style={{ textAlign: 'center', padding: '8px' }}>
          <div style={{ fontSize: '1.1rem', fontWeight: '700' }}>{pointsTotals.gemeinde}</div>
          <div style={{ fontSize: '0.7rem', color: '#666' }}>Gemeinde</div>
        </div>
        <div className="app-info-box app-info-box--neutral" style={{ textAlign: 'center', padding: '8px' }}>
          <div style={{ fontSize: '1.1rem', fontWeight: '700' }}>{profile.bonus_points || 0}</div>
          <div style={{ fontSize: '0.7rem', color: '#666' }}>Bonus</div>
        </div>
      </div>
      {/* Letzte Eintraege */}
      {pointsHistory.slice(0, 10).map((entry: any) => (
        <div key={`${entry.source_type}-${entry.id}`} className={`app-list-item ${entry.category === 'gottesdienst' ? 'app-list-item--info' : 'app-list-item--activities'}`} style={{ marginBottom: '4px' }}>
          <div className="app-list-item__row">
            <div className="app-list-item__main">
              <div className={`app-icon-circle ${entry.category === 'gottesdienst' ? 'app-icon-circle--info' : 'app-icon-circle--activities'}`}>
                <IonIcon icon={entry.source_type === 'bonus' ? giftOutline : entry.source_type === 'event' ? calendarOutline : starOutline} />
              </div>
              <div className="app-list-item__content">
                <div className="app-list-item__title">{entry.title}</div>
                <div className="app-list-item__meta">
                  <span className="app-list-item__meta-item">+{entry.points}P</span>
                  <span className="app-list-item__meta-item">{new Date(entry.date).toLocaleDateString('de-DE', { day: 'numeric', month: 'short' })}</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
      {pointsHistory.length === 0 && (
        <div className="app-info-box app-info-box--neutral" style={{ textAlign: 'center' }}>
          Noch keine Punkte-Eintraege
        </div>
      )}
    </div>
  </IonAccordion>
</IonAccordionGroup>
```

**3. PointsHistoryModal nicht mehr aufrufen:**
- Den `useIonModal(PointsHistoryModal, ...)` Hook und den zugehoerigen `presentPointsHistoryModal`-Aufruf entfernen
- Den Import von `PointsHistoryModal` entfernen
- Die Datei `PointsHistoryModal.tsx` NICHT loeschen (wird vielleicht noch von Teamer genutzt)

**4. Imports ergaenzen:**
`IonAccordion, IonAccordionGroup` aus `@ionic/react` importieren.
`giftOutline` aus `ionicons/icons` importieren (fuer Bonus-Eintraege).

Per D-06 (KHI-01): IonAccordionGroup direkt im Profil, kein Modal.
Per D-07 (KHI-02): Stats mit GD/Gemeinde/Bonus + Events.
Per D-08 (KPR-01): 6 Stats in 2 Reihen, Standard-SectionHeader-Groesse.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "IonAccordionGroup" frontend/src/components/konfi/views/ProfileView.tsx && ! grep -q "presentPointsHistoryModal" frontend/src/components/konfi/views/ProfileView.tsx && echo "OK_NO_MODAL"</automated>
  </verify>
  <acceptance_criteria>
    - IonAccordionGroup mindestens 1x in ProfileView.tsx vorhanden
    - presentPointsHistoryModal NICHT mehr in ProfileView.tsx (0 Treffer)
    - SectionHeader hat 6 Stats (PUNKTE, GD, GEMEINDE, EVENTS, BADGES, BONUS)
    - PointsHistoryModal Import entfernt aus ProfileView.tsx
    - giftOutline importiert
  </acceptance_criteria>
  <done>Profil zeigt 6 Stats im SectionHeader (2 Reihen), Punkte-Historie als Akkordeon direkt im Profil, PointsHistoryModal wird nicht mehr aufgerufen.</done>
</task>

<task type="auto">
  <name>Task 2: Bibeluebersetzungs-Modal mit Erklaerungen (KPR-02)</name>
  <files>frontend/src/components/konfi/views/ProfileView.tsx</files>
  <read_first>frontend/src/components/konfi/views/ProfileView.tsx</read_first>
  <action>
Den Bibeluebersetzungs-ActionSheet (Zeile ~671-684) durch ein useIonModal ersetzen, das ein inline-definiertes Modal-Component nutzt.

**1. Bibeluebersetzungs-Modal-Component definieren (innerhalb von ProfileView.tsx oder als Inline-Komponente):**

```tsx
const BibleTranslationModal: React.FC<{
  onClose: () => void;
  currentTranslation: string;
  onSelect: (code: string) => void;
}> = ({ onClose, currentTranslation, onSelect }) => {
  const translations = [
    { code: 'LUT', name: 'Lutherbibel 2017', description: 'Die klassische deutsche Standarduebersetzung, nah am Originaltext mit der Sprachkraft Martin Luthers. Weit verbreitet in evangelischen Gottesdiensten.' },
    { code: 'ELB', name: 'Elberfelder Bibel', description: 'Besonders woertliche Uebersetzung, die sich eng an den hebraeischen und griechischen Grundtext haelt. Ideal zum genauen Bibelstudium.' },
    { code: 'GNB', name: 'Gute Nachricht Bibel', description: 'Leicht verstaendliche Uebersetzung in modernem Deutsch. Gut geeignet fuer Einsteiger:innen und den Konfi-Unterricht.' },
    { code: 'BIGS', name: 'Bibel in gerechter Sprache', description: 'Uebersetzung mit Fokus auf Gerechtigkeit, Inklusion und die Vielfalt biblischer Gottesbilder. Gendersensibel und theologisch reflektiert.' },
    { code: 'NIV', name: 'New International Version', description: 'Die meistgelesene englische Bibeluebersetzung. Gute Balance zwischen Woertlichkeit und Verstaendlichkeit. Fuer alle die Englisch bevorzugen.' },
    { code: 'LSG', name: 'Louis Segond 1910', description: 'Franzoesische Standarduebersetzung, vergleichbar mit der Lutherbibel im deutschen Sprachraum. Klassisch und weit verbreitet.' },
    { code: 'RVR60', name: 'Reina-Valera 1960', description: 'Spanische Standarduebersetzung mit grosser Treue zum Grundtext. Die am haeufigsten verwendete spanische Bibel.' }
  ];

  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonTitle>Bibeluebersetzung</IonTitle>
          <IonButtons slot="start">
            <IonButton className="app-modal-close-btn" onClick={onClose}>
              <IonIcon icon={closeOutline} />
            </IonButton>
          </IonButtons>
        </IonToolbar>
      </IonHeader>
      <IonContent className="app-gradient-background">
        <IonList inset={true} style={{ margin: '16px' }}>
          <IonListHeader>
            <div className="app-section-icon app-section-icon--purple">
              <IonIcon icon={bookOutline} />
            </div>
            <IonLabel>Uebersetzung waehlen</IonLabel>
          </IonListHeader>
          <IonCard className="app-card">
            <IonCardContent style={{ padding: '8px' }}>
              <IonRadioGroup value={currentTranslation} onIonChange={(e) => onSelect(e.detail.value)}>
                {translations.map((t) => (
                  <IonItem key={t.code} lines="none" style={{ '--background': 'transparent', '--padding-start': '12px', marginBottom: '4px' }}>
                    <IonRadio slot="start" value={t.code} />
                    <IonLabel className="ion-text-wrap">
                      <h3 style={{ fontWeight: '600', fontSize: '0.95rem', margin: '0 0 4px 0' }}>{t.name}</h3>
                      <p style={{ fontSize: '0.8rem', color: '#666', lineHeight: '1.4', margin: '0' }}>{t.description}</p>
                    </IonLabel>
                  </IonItem>
                ))}
              </IonRadioGroup>
            </IonCardContent>
          </IonCard>
        </IonList>
      </IonContent>
    </IonPage>
  );
};
```

**2. useIonModal einrichten:**
```tsx
const [presentBibleModal, dismissBibleModal] = useIonModal(BibleTranslationModal, {
  onClose: () => dismissBibleModal(),
  currentTranslation: selectedTranslation,
  onSelect: (code: string) => {
    handleTranslationChange(code);
    dismissBibleModal();
  }
});
```

**3. Den ActionSheet-Aufruf ersetzen:**
Im Bibeluebersetzungs-IonItem den `onClick` Handler aendern:

VORHER:
```tsx
onClick={() => { presentActionSheet({ ... }); }}
```

NACHHER:
```tsx
onClick={() => {
  presentBibleModal({
    presentingElement: pageRef?.current || presentingElement || undefined
  });
}}
```

**4. Imports ergaenzen:**
- `IonPage, IonHeader, IonToolbar, IonTitle, IonContent, IonRadioGroup, IonRadio` aus `@ionic/react` importieren (pruefen welche bereits importiert sind)
- `closeOutline` aus `ionicons/icons` importieren
- `useIonActionSheet` Import entfernen falls nicht mehr anderweitig genutzt (wird nur fuer Bibeluebersetzung genutzt)
- Pruefen: `useIonActionSheet` wird nur fuer Bibeluebersetzung genutzt, also kann der Import weg

Per D-09 (KPR-02): Neues Modal mit Erklaerungen pro Uebersetzung statt einfache ActionSheet-Liste.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "BibleTranslationModal" frontend/src/components/konfi/views/ProfileView.tsx && ! grep -q "presentActionSheet" frontend/src/components/konfi/views/ProfileView.tsx && echo "OK_NO_ACTIONSHEET"</automated>
  </verify>
  <acceptance_criteria>
    - BibleTranslationModal mindestens 2x in ProfileView.tsx (Definition + useIonModal)
    - presentActionSheet NICHT mehr in ProfileView.tsx (0 Treffer) — es sei denn anderweitig genutzt
    - Jede Uebersetzung hat Name + Erklaerungstext (description)
    - IonRadioGroup mit IonRadio fuer Auswahl
    - closeOutline importiert fuer Modal-Close-Button
  </acceptance_criteria>
  <done>Bibeluebersetzung oeffnet ein Modal mit Erklaerungen zu jeder Uebersetzung und Radio-Buttons statt einfachem ActionSheet.</done>
</task>

</tasks>

<verification>
- Profil-SectionHeader zeigt 6 Stats (PUNKTE, GD, GEMEINDE, EVENTS, BADGES, BONUS) in 2 Reihen
- Punkte-Uebersicht ist ein Akkordeon im Profil (ausklappbar, kein Modal)
- Akkordeon zeigt GD/Gemeinde/Bonus Stats-Grid + letzte 10 Eintraege
- PointsHistoryModal wird nicht mehr geoeffnet
- Bibeluebersetzungs-Auswahl oeffnet Modal mit Erklaerungstexten + Radio-Buttons
</verification>

<success_criteria>
Profil zeigt 6 Stats in 2 Reihen, Punkte-Historie als Akkordeon direkt eingebaut, Bibeluebersetzung als Modal mit Erklaerungen pro Version.
</success_criteria>

<output>
After completion, create `.planning/phases/96-konfi-ui/96-04-SUMMARY.md`
</output>
