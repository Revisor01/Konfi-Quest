---
phase: 96-konfi-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/views/DashboardSections.tsx
autonomous: true
requirements: [KDB-01, KDB-02, KEV-01]
must_haves:
  truths:
    - "Events-Card im Dashboard ist IMMER sichtbar, auch wenn keine Events vorhanden"
    - "Leere Events-Card zeigt 'Buche dein naechstes Event' mit Link zur Events-Seite"
    - "Event-Layout zeigt Titel+Datum/Uhrzeit, Ort, Mitbringen auf eigenen Zeilen (wie EventsView)"
  artifacts:
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Always-visible Events section with empty state"
      contains: "Buche dein"
    - path: "frontend/src/components/konfi/views/DashboardSections.tsx"
      provides: "EventCard with separate lines for location and bring_items"
      contains: "app-description-text"
  key_links:
    - from: "DashboardView.tsx"
      to: "EventCard component"
      via: "import from DashboardSections"
      pattern: "regularEvents"
---

<objective>
Dashboard Events-Card immer sichtbar machen und Event-Layout angleichen.

Purpose: Konfis sollen immer sehen dass es Events gibt, auch wenn keine gebucht sind. Layout wird konsistent mit EventsView.
Output: Events-Card im Dashboard immer sichtbar, mit Empty-State und verbessertem Layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/konfi/views/DashboardView.tsx
@frontend/src/components/konfi/views/DashboardSections.tsx
@frontend/src/components/konfi/views/EventsView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Events-Card immer sichtbar + Empty State (KDB-01)</name>
  <files>frontend/src/components/konfi/views/DashboardView.tsx</files>
  <read_first>frontend/src/components/konfi/views/DashboardView.tsx, frontend/src/components/konfi/views/EventsView.tsx</read_first>
  <action>
In DashboardView.tsx die Events-Section-Bedingung aendern:

VORHER (Zeile ~368):
```
{dashboardConfig?.show_events !== false && regularEvents && regularEvents.length > 0 && (
```

NACHHER:
```
{dashboardConfig?.show_events !== false && (
```

Im Events-Section-Content: Wenn `regularEvents.length === 0`, einen Empty-State anzeigen statt EventCards:

```tsx
{regularEvents.length > 0 ? (
  <div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>
    {regularEvents.map((event) => (
      <EventCard key={event.id} event={event} onClick={() => router.push(`/konfi/events/${event.id}`)} />
    ))}
  </div>
) : (
  <div
    className="app-dashboard-glass-card"
    onClick={() => router.push('/konfi/events')}
    style={{ cursor: 'pointer', textAlign: 'center', padding: '20px 16px' }}
  >
    <div style={{ fontSize: '1rem', fontWeight: '600', color: 'white', marginBottom: '4px' }}>
      Buche dein naechstes Event
    </div>
    <div style={{ fontSize: '0.85rem', color: 'rgba(255, 255, 255, 0.7)' }}>
      Tippe hier um verfuegbare Events zu sehen
    </div>
  </div>
)}
```

Den Glass-Chip-Text im Header anpassen: Wenn `regularEvents.length === 0`, "EVENTS ENTDECKEN" anzeigen statt "DEINE 0 EVENTS". Per D-01 (KDB-01): Events-Card IMMER anzeigen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "Buche dein" frontend/src/components/konfi/views/DashboardView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "Buche dein" DashboardView.tsx findet mindestens 1 Treffer
    - grep "regularEvents.length > 0" DashboardView.tsx findet KEINE Treffer als alleinige Section-Bedingung
    - grep "show_events" DashboardView.tsx zeigt die Section-Bedingung ohne length-Check
  </acceptance_criteria>
  <done>Events-Card ist im Dashboard immer sichtbar. Leerer Zustand zeigt "Buche dein naechstes Event" mit Navigation zu Events-Seite.</done>
</task>

<task type="auto">
  <name>Task 2: EventCard Layout + app-description-text Klasse (KDB-02, KEV-01)</name>
  <files>frontend/src/components/konfi/views/DashboardSections.tsx</files>
  <read_first>frontend/src/components/konfi/views/DashboardSections.tsx, frontend/src/theme/variables.css</read_first>
  <action>
In DashboardSections.tsx den EventCard so anpassen, dass Ort und Mitbringen jeweils eigene Zeilen bekommen (wie in EventsView.tsx bereits vorhanden). Aktuell sind sie alle in einer Zeile mit `app-dashboard-meta` + flexWrap.

VORHER: Alles in einem div mit `app-dashboard-meta` und `flexWrap: 'wrap'`

NACHHER: Drei separate Meta-Zeilen:
1. Datum + Uhrzeit (erste Zeile, zusammen)
2. Ort (eigene Zeile, nur wenn vorhanden)
3. Mitbringen (eigene Zeile, nur wenn vorhanden)

```tsx
{/* Zeile 1: Datum + Uhrzeit */}
<div className="app-dashboard-meta">
  <IonIcon icon={calendar} style={{ fontSize: '0.9rem' }} />
  <span>{formatEventDate(event.event_date || event.date)}</span>
  <span className="app-dashboard-dot" />
  <IonIcon icon={time} style={{ fontSize: '0.9rem' }} />
  <span>
    {event.booked_timeslot_start
      ? `${formatEventTime(event.booked_timeslot_start)}${event.booked_timeslot_end ? ` - ${formatEventTime(event.booked_timeslot_end)}` : ''}`
      : formatEventTime(event.event_date || event.date)
    }
  </span>
</div>
{/* Zeile 2: Ort (eigene Zeile) */}
{event.location && (
  <div className="app-dashboard-meta" style={{ marginTop: '4px' }}>
    <IonIcon icon={location} style={{ fontSize: '0.9rem' }} />
    <span>{event.location}</span>
  </div>
)}
{/* Zeile 3: Mitbringen (eigene Zeile) */}
{event.bring_items && (
  <div className="app-dashboard-meta" style={{ marginTop: '4px', color: 'rgba(255,255,255,0.9)' }}>
    <IonIcon icon={bagHandle} style={{ fontSize: '0.9rem', color: '#c4b5fd' }} />
    <span style={{ fontWeight: '600' }}>Mitbringen: {event.bring_items}</span>
  </div>
)}
```

Die `flexWrap: 'wrap'` vom alten Datum/Ort-Meta-div entfernen, da Ort jetzt eine eigene Zeile hat. Per D-02 (KDB-02): Layout mit eigenen Zeilen.

Die bestehende `app-description-text` Klasse in variables.css ist bereits vorhanden (Zeile 1021). KEV-01 (Beschreibungstext groesser) wird hier nicht direkt geaendert, aber die globale Klasse existiert bereits fuer Event-Details.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "Zeile 2: Ort" frontend/src/components/konfi/views/DashboardSections.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EventCard hat separate div-Elemente fuer Datum/Uhrzeit, Ort und Mitbringen
    - Kein flexWrap mehr auf dem Haupt-Meta-div (Ort ist jetzt eigene Zeile)
    - location und bring_items haben jeweils eigene app-dashboard-meta divs
  </acceptance_criteria>
  <done>EventCard im Dashboard zeigt Titel+Datum/Uhrzeit, Ort und Mitbringen auf eigenen Zeilen. Layout konsistent mit EventsView.</done>
</task>

</tasks>

<verification>
- Events-Section im Dashboard sichtbar auch ohne registrierte Events
- EventCard-Layout zeigt 3 separate Zeilen statt einer umgebrochenen Zeile
- "Buche dein naechstes Event" Text erscheint bei leerer Event-Liste
- Klick auf Empty-State navigiert zu /konfi/events
</verification>

<success_criteria>
Events-Card im Dashboard ist immer sichtbar (auch leer). Layout-Zeilen sind getrennt: Datum/Uhrzeit, Ort, Mitbringen.
</success_criteria>

<output>
After completion, create `.planning/phases/96-konfi-ui/96-01-SUMMARY.md`
</output>
