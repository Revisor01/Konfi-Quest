---
phase: 110-konfi-events-details
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/shared/ListSection.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
autonomous: true
requirements: [KEV-01, KEV-02, KEV-03, KED-01]

must_haves:
  truths:
    - "ListSection rendert children in einem flex-div Container statt IonList"
    - "ListSection IonCardContent hat 12px Padding"
    - "Event-Liste in EventsView nutzt flex-div Layout ueber ListSection"
    - "Suche+Filter Card liegt unter SectionHeader mit ios26-searchbar-classic"
    - "SectionHeader ist das erste Element in EventsView"
    - "Beschreibungstext in EventDetailView nutzt app-description-text Klasse"
  artifacts:
    - path: "frontend/src/components/shared/ListSection.tsx"
      provides: "Globale ListSection mit flex-div statt IonList"
      contains: "display: 'flex'"
    - path: "frontend/src/components/konfi/views/EventsView.tsx"
      provides: "Events-View mit korrektem Listen-Stil"
      contains: "app-list-item"
  key_links:
    - from: "frontend/src/components/shared/ListSection.tsx"
      to: "frontend/src/theme/variables.css"
      via: "app-list-item CSS-Klassen"
      pattern: "app-list-item"
---

<objective>
ListSection-Komponente auf flex-div + 12px Padding umstellen und Events-View verifizieren.

Purpose: Konfi Events-View und Event-Details an die globalen CSS-Patterns aus Phase 108 anpassen (D-01 bis D-04).
Output: ListSection nutzt flex-div statt IonList, Events-View und Details sind pattern-konform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-globale-css-patterns/108-01-SUMMARY.md

<interfaces>
<!-- ListSection-Komponente: Muss IonList durch flex-div ersetzen -->

From frontend/src/components/shared/ListSection.tsx (aktuell Z.59):
```tsx
// ALT — wird ersetzt:
<IonList lines="none" style={{ background: 'transparent', padding: '0', margin: '0' }}>
  {children}
</IonList>

// NEU — flex-div wie Chat-Pattern:
<div style={{ display: 'flex', flexDirection: 'column' }}>
  {children}
</div>
```

From frontend/src/components/shared/ListSection.tsx (aktuell Z.50):
```tsx
// ALT — kein explizites Padding:
<IonCardContent>

// NEU — 12px Padding per D-03:
<IonCardContent style={{ padding: '12px' }}>
```

From frontend/src/components/konfi/views/EventsView.tsx:
```tsx
// Bestehende Struktur: SectionHeader > Segment > ListSection
// ListSection children enthalten IonItemSliding > IonItem > div.app-list-item
```

From frontend/src/components/konfi/pages/KonfiEventsPage.tsx:
```tsx
// Suche+Filter: IonList > IonListHeader > IonCard > IonSearchbar.ios26-searchbar-classic
// Bereits korrekt per D-01
```

From frontend/src/components/konfi/views/EventDetailView.tsx (Z.670):
```tsx
// Beschreibungstext: bereits korrekt per D-04
<p className="app-description-text">
  {eventData.description}
</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ListSection flex-div + 12px Padding (KEV-03, D-03)</name>
  <files>frontend/src/components/shared/ListSection.tsx</files>
  <read_first>
    - frontend/src/components/shared/ListSection.tsx
    - frontend/src/__tests__/components/ListSection.test.tsx
  </read_first>
  <action>
    In ListSection.tsx zwei Aenderungen vornehmen (per D-03: flex-div statt IonList, 12px CardContent Padding):

    1. Zeile 50: `<IonCardContent>` ersetzen durch `<IonCardContent style={{ padding: '12px' }}>` — bei showEmpty (EmptyState) den alten Padding beibehalten mit padding: '16px', nur im nicht-leeren Fall 12px.

    2. Zeile 59: Die innere `<IonList lines="none" style={{...}}>` ersetzen durch `<div style={{ display: 'flex', flexDirection: 'column' }}>`. IonList-Import entfernen falls nicht mehr benoetigt (IonList wird noch im aeusseren Wrapper Z.42 verwendet — also Import beibehalten).

    Konkret:
    - IonCardContent bekommt `style={{ padding: showEmpty ? '16px' : '12px' }}`
    - Der children-Wrapper wird von `<IonList>` zu `<div style={{ display: 'flex', flexDirection: 'column' }}>` geaendert

    NICHT aendern: aeusserer IonList-Wrapper (Z.42), IonListHeader, IonCard Struktur.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx vitest run src/__tests__/components/ListSection.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>ListSection rendert children in flex-div statt IonList, IonCardContent hat 12px Padding bei Inhalt und 16px bei EmptyState</acceptance_criteria>
  <done>ListSection nutzt flex-div Container fuer children, 12px Padding im nicht-leeren Zustand</done>
</task>

<task type="auto">
  <name>Task 2: Events-View Verifikation + IonItemSliding-Kompatibilitaet (KEV-01, KEV-02, KEV-03, KED-01)</name>
  <files>frontend/src/components/konfi/views/EventsView.tsx</files>
  <read_first>
    - frontend/src/components/konfi/views/EventsView.tsx
    - frontend/src/components/konfi/pages/KonfiEventsPage.tsx
    - frontend/src/components/konfi/views/EventDetailView.tsx (nur Z.660-676)
  </read_first>
  <action>
    Verifiziere und passe die Events-View an:

    1. **KEV-01 (D-01):** KonfiEventsPage.tsx hat bereits Suche+Filter mit ios26-searchbar-classic unter SectionHeader. KEINE Aenderung noetig — nur verifizieren.

    2. **KEV-02 (D-02):** SectionHeader ist erstes Element in EventsView. KEINE Aenderung noetig — nur verifizieren.

    3. **KEV-03 (D-03):** EventsView nutzt `IonItemSliding` innerhalb der ListSection. Da ListSection jetzt flex-div statt IonList rendert, muss IonItemSliding korrekt funktionieren. IonItemSliding braucht KEIN IonList als Parent — es funktioniert standalone. ABER: der inline-Style `marginBottom` auf IonItemSliding (Z.257) kollidiert mit `margin-bottom: 8px` aus `.app-list-item` CSS. Entferne den inline marginBottom-Style vom IonItemSliding (Z.257: `style={{ marginBottom: index < filteredEvents.length - 1 ? '4px' : '0' }}`), da die CSS-Klasse `app-list-item` bereits `margin-bottom: 8px` hat und `.app-list-item:last-child` mit `margin-bottom: 0`.

    4. **KED-01 (D-04):** EventDetailView.tsx Z.670 nutzt bereits `className="app-description-text"`. Variable `--app-description-font-size: 1rem` existiert in variables.css. KEINE Aenderung noetig.

    Zusammenfassung der Code-Aenderung: NUR Z.257 in EventsView.tsx — den marginBottom inline-Style entfernen, sodass IonItemSliding kein style-Attribut mehr hat (nur key bleibt).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>EventsView kompiliert fehlerfrei, kein doppelter marginBottom (CSS-Klasse uebernimmt), Suche/Filter/SectionHeader/Beschreibungstext sind pattern-konform</acceptance_criteria>
  <done>Events-View nutzt Listen-Stil ueber ListSection flex-div, SectionHeader oben, Suche+Filter wie Chat, Beschreibungstext nutzt app-description-text</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/__tests__/components/ListSection.test.tsx` — alle Tests gruen
- `cd frontend && npx tsc --noEmit` — keine TypeScript-Fehler
- ListSection.tsx enthaelt `display: 'flex'` statt `IonList lines="none"`
- ListSection.tsx IonCardContent hat `padding: '12px'` (bzw. showEmpty-abhaengig)
- EventsView.tsx hat keinen inline marginBottom auf IonItemSliding
- EventDetailView.tsx Z.670 hat `className="app-description-text"` (unveraendert)
</verification>

<success_criteria>
- ListSection rendert children in flex-div mit 12px Padding
- Event-Liste zeigt korrekte Abstaende ueber CSS-Klassen (kein doppelter margin)
- Alle 4 Requirements (KEV-01, KEV-02, KEV-03, KED-01) erfuellt
- TypeScript kompiliert fehlerfrei, ListSection-Tests gruen
</success_criteria>

<output>
After completion, create `.planning/phases/110-konfi-events-details/110-01-SUMMARY.md`
</output>
