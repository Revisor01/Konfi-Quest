---
phase: 97-teamer-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
autonomous: true
requirements:
  - TDB-01
  - TDB-02
  - TDB-03

must_haves:
  truths:
    - "Zertifikate-Card im Teamer-Dashboard ist lila statt rosa/pink"
    - "Tageslosung ist im Teamer-Dashboard sichtbar wenn Config nicht explizit false"
    - "Neue Badges (awarded_date < 7 Tage) sind im Teamer-Dashboard als 'Neu' markiert mit gruenem Ring und !-Badge"
    - "Geheime Badges werden im Teamer-Dashboard angezeigt (verdiente + Platzhalter)"
    - "Konfi-Dashboard zeigt ebenfalls 'Neu'-Markierung auf kuerzlich verdienten Badges"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "Lila Zertifikate, Losung-Config-Check, Badge-Sektion wie Konfi"
      contains: "#7c3aed"
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Neu-Markierung auf recent Badges"
      contains: "badgePulse"
  key_links:
    - from: "TeamerDashboardPage.tsx"
      to: "/teamer/dashboard API"
      via: "useOfflineQuery badges.recent + config.show_losung"
      pattern: "show_losung"
---

<objective>
Teamer-Dashboard polieren: Zertifikate-Card Lila statt Rosa, Losung-Config pruefen, und Badge-Sektion 1:1 wie Konfi-Dashboard mit geheimen Badges und "Neu"-Markierung.

Purpose: Teamer:innen sehen ein konsistentes, ansprechendes Dashboard mit korrekten Farben und vollstaendiger Badge-Uebersicht.
Output: Ueberarbeitete TeamerDashboardPage.tsx + DashboardView.tsx mit Neu-Badge-Markierung.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-teamer-ui/97-CONTEXT.md

@frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
@frontend/src/components/konfi/views/DashboardView.tsx
@frontend/src/components/konfi/pages/KonfiDashboardPage.tsx

<interfaces>
<!-- Konfi DashboardView Badge-Sektion (Referenz fuer Teamer) -->
<!-- Zeilen 443-565 in DashboardView.tsx: visibleBadges + secretEarned + secretNotEarnedCount -->
<!-- getBadgeColor, getIconFromString aus DashboardSections.tsx -->
<!-- badgePopoverRef mit DashboardBadgePopoverContent -->
<!-- isRecent Pruefung: recentBadgeIds.has(badge.id) -->
<!-- Neu-Indikator: gruener Ring (boxShadow 0 0 0 3px #10b981) + !-Kreis (top: -6px, right: -6px) -->
<!-- badgePulse Animation -->

<!-- Teamer Dashboard Config Interface -->
From TeamerDashboardPage.tsx:
```typescript
interface DashboardConfig {
  show_zertifikate: boolean;
  show_events: boolean;
  show_badges: boolean;
  show_losung: boolean;
}
```

<!-- Teamer Badge Interface -->
```typescript
interface Badge {
  id: number;
  name: string;
  icon?: string;
  awarded_date?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zertifikate Lila + Losung Config-Check</name>
  <files>frontend/src/components/teamer/pages/TeamerDashboardPage.tsx</files>
  <action>
    Per TDB-01: Zertifikate-Card Gradient von Rosa auf Lila aendern.
    - Zeile 318-319 (Begruessung Header): NICHT aendern — bleibt Rosa fuer Teamer-Identitaet.
    - Zeile 368-370 (Zertifikate-Sektion `app-dashboard-section`): Gradient aendern von `linear-gradient(135deg, #e11d48 0%, #be185d 100%)` auf `linear-gradient(135deg, #7c3aed 0%, #6d28d9 100%)`.

    Per TDB-02: Losung Config pruefen.
    - Zeile 603: Bedingung ist `config?.show_losung !== false` — das ist korrekt, Default ist "anzeigen" wenn undefined.
    - Zusaetzlich pruefen: Die DashboardConfig hat `show_losung: boolean` — wenn Backend `config` ohne `show_losung` Key liefert, ist `config.show_losung` undefined, und `undefined !== false` ist true. Das ist korrekt. Kein Fix noetig, nur verifizieren.
    - Falls `config` komplett null/undefined ist (kein Dashboard-Config im Backend): Zeile 282 `const config = dashboardData?.config;` — bei `config === undefined` wird `undefined?.show_losung !== false` zu `undefined !== false` = true. Korrekt.
  </action>
  <verify>
    grep -n "#7c3aed" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx | grep -q "7c3aed" && echo "TDB-01 OK" || echo "TDB-01 FEHLT"
    grep -n "show_losung !== false" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx | grep -q "show_losung" && echo "TDB-02 OK" || echo "TDB-02 FEHLT"
  </verify>
  <done>Zertifikate-Card zeigt lila Gradient (#7c3aed/#6d28d9). Losung-Config ist korrekt (Default = sichtbar).</done>
</task>

<task type="auto">
  <name>Task 2: Badge-Sektion 1:1 wie Konfi-Dashboard + Neu-Markierung BEIDE Dashboards</name>
  <files>frontend/src/components/teamer/pages/TeamerDashboardPage.tsx, frontend/src/components/konfi/views/DashboardView.tsx</files>
  <action>
    Per TDB-03: Teamer-Dashboard Badge-Sektion ueberarbeiten (Zeilen 638-736).

    Die aktuelle Teamer-Badge-Sektion zeigt nur `badges.recent` (verdiente) + graue Platzhalter.
    Die Konfi-DashboardView (Zeilen 443-565) zeigt stattdessen:
    1. Sichtbare Badges (verdiente farbig + nicht-verdiente als eyeOff-Icons halbtransparent)
    2. Geheime Badges (verdiente farbig + Platzhalter mit helpCircle)
    3. Stats-Chips: "X/Y sichtbar" + "X/Y geheim"
    4. Popover bei Klick auf jedes Badge
    5. "Neu"-Markierung: gruener Ring + !-Kreis fuer recent Badges
    6. badgePulse Animation

    AENDERUNGEN in TeamerDashboardPage.tsx:

    A) Badge-Daten vom Backend: Das aktuelle `dashboardData.badges` hat `recent`, `earned_count`, `total_count`.
       Das reicht NICHT fuer die Konfi-Logik (braucht alle Badges mit earned/not-earned Status).
       LOESUNG: Einen zweiten useOfflineQuery hinzufuegen fuer `/teamer/badges` (wie TeamerBadgesPage.tsx es tut).
       ```typescript
       const { data: allTeamerBadges } = useOfflineQuery<any[]>(
         'teamer:all-badges:' + user?.id,
         async () => { const res = await api.get('/teamer/badges'); return res.data || []; },
         { ttl: CACHE_TTL.BADGES }
       );
       ```

    B) Aus allTeamerBadges berechnen:
       ```typescript
       const earnedBadges = (allTeamerBadges || []).filter((b: any) => b.earned);
       const earnedIds = new Set(earnedBadges.map((b: any) => b.id));
       const visibleBadges = [
         ...earnedBadges.filter((b: any) => !b.is_hidden),
         ...(allTeamerBadges || []).filter((b: any) => !b.earned && !b.is_hidden)
       ];
       const secretEarned = earnedBadges.filter((b: any) => b.is_hidden);
       const secretTotal = (allTeamerBadges || []).filter((b: any) => b.is_hidden).length;
       const secretNotEarnedCount = secretTotal - secretEarned.length;
       const visibleEarned = earnedBadges.filter((b: any) => !b.is_hidden).length;
       const visibleTotal = (allTeamerBadges || []).filter((b: any) => !b.is_hidden).length;
       ```

    C) "Neu"-Erkennung (awarded_date < 7 Tage):
       ```typescript
       const isRecent = (badge: any) => {
         if (!badge.awarded_date) return false;
         const diff = Date.now() - new Date(badge.awarded_date).getTime();
         return diff < 7 * 24 * 60 * 60 * 1000;
       };
       ```

    D) Badge-Sektion ersetzen (Zeilen 638-736) mit Konfi-Pattern:
       - Stats-Chip: "X/Y sichtbar" (+ "Z neue" wenn vorhanden)
       - Sichtbare Badges: 44px Kreise, verdiente mit getBadgeColor Gradient, nicht-verdiente mit eyeOff halbtransparent
       - Neu-Markierung: gruener Ring (boxShadow: 0 0 0 3px #10b981) + !-Kreis oben rechts
       - Geheime Badges Sektion (wenn vorhanden): Stats-Chip "X/Y geheim", verdiente + helpCircle Platzhalter
       - badgePulse keyframe Animation am Ende
       - Klick oeffnet Popover (bestehenden certPopoverRef-Pattern oder neuen badgePopoverRef nutzen)
       Fuer den Badge-Popover: einen eigenen BadgePopoverContent erstellen (aehnlich CertPopoverContent, aber mit Badge-Name, Beschreibung, earned/not-earned Status). Oder die bestehende `DashboardBadgePopoverContent` aus DashboardSections importieren — ACHTUNG: diese braucht spezifische Props. Besser: inline Popover-Komponente erstellen wie CertPopoverContent.

    E) `eyeOff` und `helpCircle` Icons sind bereits importiert (Zeile 63-64). `chevronForward` ist auch da.

    AENDERUNGEN in DashboardView.tsx (Konfi):
    Per TDB-03 "Neu-Schriftzug fuer BEIDE Dashboards": Die DashboardView hat BEREITS die "Neu"-Markierung (gruener Ring + !-Kreis, badgePulse Animation) fuer `isRecent` Badges. KEIN weiterer Aenderungsbedarf — die isRecent Logik basiert auf `recentBadgeIds` aus `dashboardData.recent_badges`.

    WICHTIG: Keine Unicode Emojis verwenden! Nur IonIcons.
    WICHTIG: Echte Umlaute (ae -> ae ist OK in Code-Variablen, aber UI-Texte muessen echte Umlaute haben).
  </action>
  <verify>
    grep -n "eyeOff\|secretEarned\|isRecent\|badgePulse\|allTeamerBadges" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx | wc -l | xargs test 4 -le && echo "TDB-03 OK" || echo "TDB-03 FEHLT"
    cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5
  </verify>
  <done>Teamer-Dashboard Badge-Sektion zeigt alle sichtbaren + geheimen Badges mit Neu-Markierung (gruener Ring + ! fuer < 7 Tage). Popover bei Klick. Konfi-Dashboard hat Neu-Markierung bereits.</done>
</task>

</tasks>

<verification>
- Zertifikate-Card Gradient ist #7c3aed/#6d28d9 (nicht mehr #e11d48/#be185d)
- Losung-Config check: show_losung undefined -> sichtbar (Default true Verhalten)
- Badge-Sektion zeigt sichtbare + geheime Badges mit Neu-Markierung
- TypeScript kompiliert ohne Fehler
</verification>

<success_criteria>
1. Zertifikate-Card im Teamer-Dashboard zeigt lila Gradient
2. Tageslosung ist sichtbar bei Default-Config
3. Badges-Sektion ist 1:1 wie Konfi-Dashboard (sichtbar/geheim, Popover, Neu-Markierung)
4. Frontend kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/97-teamer-ui/97-01-SUMMARY.md`
</output>
