---
phase: 03-design-system-grundlagen
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/konfi/views/EventsView.tsx
  - frontend/src/components/konfi/views/RequestsView.tsx
  - frontend/src/components/admin/EventsView.tsx
  - frontend/src/components/admin/ActivitiesView.tsx
  - frontend/src/components/admin/KonfisView.tsx
  - frontend/src/components/admin/UsersView.tsx
  - frontend/src/components/admin/OrganizationView.tsx
  - frontend/src/components/admin/ActivityRequestsView.tsx
autonomous: true
requirements: [DES-01, DES-02, DES-03]

must_haves:
  truths:
    - "Alle 8 Haupt-Views nutzen SectionHeader statt Inline-Style Header-Banner"
    - "Leere Listen in diesen Views zeigen EmptyState via ListSection statt Inline-HTML"
    - "OrganizationView nutzt den kompakten SectionHeader statt des Sonderformats"
    - "Die bestehende Farblogik und Funktionalitaet jeder View ist unveraendert erhalten"
    - "Listen-Sektionen nutzen ListSection mit app-section-icon und Count-Anzeige"
  artifacts:
    - path: "frontend/src/components/konfi/views/EventsView.tsx"
      provides: "Referenz-View mit SectionHeader und ListSection"
      contains: "import.*SectionHeader"
    - path: "frontend/src/components/admin/OrganizationView.tsx"
      provides: "Normalisierte Organization-View mit kompaktem SectionHeader"
      contains: "import.*SectionHeader"
  key_links:
    - from: "frontend/src/components/konfi/views/EventsView.tsx"
      to: "frontend/src/components/shared/SectionHeader.tsx"
      via: "Import und Nutzung mit preset='events'"
      pattern: "import.*SectionHeader.*from.*shared"
    - from: "frontend/src/components/admin/ActivitiesView.tsx"
      to: "frontend/src/components/shared/ListSection.tsx"
      via: "Import und Nutzung fuer Aktivitaeten-Liste mit EmptyState"
      pattern: "import.*ListSection.*from.*shared"
---

<objective>
Die 8 Haupt-Views (Konfi-Bereich + Admin-Core-Views) auf SectionHeader, EmptyState und ListSection umstellen. Das Inline-Style Header-Banner (~50 Zeilen pro View) wird durch einen SectionHeader-Import (~5 Zeilen) ersetzt. Inline-EmptyState-HTML wird durch ListSection mit integrierten EmptyState-Props ersetzt.

Purpose: Groesster DRY-Gewinn bei den meistgenutzten Views -- ca. 400 Zeilen redundanter Inline-Styles werden eliminiert.
Output: 8 refactored Views die die neuen Shared Components nutzen.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-grundlagen/03-CONTEXT.md
@.planning/phases/03-design-system-grundlagen/03-01-SUMMARY.md

Referenz fuer bestehende Patterns:
@frontend/src/components/konfi/views/EventsView.tsx
@frontend/src/components/shared/SectionHeader.tsx
@frontend/src/components/shared/EmptyState.tsx
@frontend/src/components/shared/ListSection.tsx

<interfaces>
<!-- Von Plan 03-01 erstellte Components: -->

SectionHeader Props:
  title: string (required)
  subtitle: string (required)
  icon: string (ionicons icon)
  preset?: 'events' | 'activities' | 'konfis' | 'users' | 'organizations' | 'badges' | 'requests' | 'jahrgang'
  colors?: { primary: string; secondary: string }
  stats: Array<{ value: number; label: string }>

EmptyState Props:
  icon: string
  title: string
  message: string
  iconColor?: string

ListSection Props:
  icon: string
  title: string
  count: number
  iconColorClass?: string (wird zu app-section-icon--{value})
  children: React.ReactNode
  emptyIcon?: string
  emptyTitle?: string
  emptyMessage?: string
  emptyIconColor?: string
  isEmpty?: boolean

Import: import { SectionHeader, EmptyState, ListSection } from '../../shared';
(bzw. '../shared' oder './shared' je nach Verzeichnistiefe)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Konfi-Views auf Shared Components umstellen</name>
  <files>
    frontend/src/components/konfi/views/EventsView.tsx
    frontend/src/components/konfi/views/RequestsView.tsx
  </files>
  <action>
Beide Konfi-Views refactoren. Fuer JEDE View:

**A) SectionHeader einbauen:**
1. Import hinzufuegen: `import { SectionHeader, ListSection } from '../../shared';`
2. Den gesamten Inline-Style Header-Banner-Block (das div mit `background: 'linear-gradient(...)'` bis zum schliessenden Tag inkl. dekorativer Kreise, Icon-Box, Title, Subtitle, Stats-Row) durch einen SectionHeader-Aufruf ersetzen:

Fuer **EventsView.tsx** (Referenz):
```tsx
<SectionHeader
  title="Deine Events"
  subtitle="Termine und Veranstaltungen"
  icon={calendar}
  preset="events"
  stats={statsData.map(s => ({ value: s.count, label: s.label }))}
/>
```

Fuer **RequestsView.tsx**:
- Preset: `preset="requests"`
- Title/Subtitle und Stats aus der bestehenden View uebernehmen
- Icon aus der bestehenden View uebernehmen

**B) ListSection einbauen:**
Die bestehenden IonList + IonListHeader + IonCard + IonCardContent Bloecke durch ListSection ersetzen. Das Pattern ist:
```tsx
{/* VORHER: */}
<IonList inset={true} style={{ margin: '16px' }}>
  <IonListHeader>
    <div className="app-section-icon app-section-icon--events">
      <IonIcon icon={calendarOutline} />
    </div>
    <IonLabel>Events ({count})</IonLabel>
  </IonListHeader>
  <IonCard className="app-card">
    <IonCardContent>
      {items.length > 0 ? renderItems() : renderEmpty()}
    </IonCardContent>
  </IonCard>
</IonList>

{/* NACHHER: */}
<ListSection
  icon={calendarOutline}
  title="Events"
  count={filteredEvents.length}
  iconColorClass="events"
  isEmpty={filteredEvents.length === 0}
  emptyIcon={calendarOutline}
  emptyTitle="Keine Events gefunden"
  emptyMessage={activeTab === 'registered' ? 'Du bist noch fuer keine Events angemeldet' : 'Keine anstehenden Events'}
  emptyIconColor="#dc2626"
>
  {/* Die List-Items bleiben unveraendert */}
  <IonList lines="none" style={{ background: 'transparent', padding: '0', margin: '0' }}>
    {filteredEvents.map(event => (
      // ... bestehender Item-Code bleibt identisch ...
    ))}
  </IonList>
</ListSection>
```

**C) Nicht mehr benoetigte Inline-Styles entfernen:**
- Die gesamten ~50 Zeilen des Inline-Style Header-Banner-Blocks
- Die separate EmptyState-Bedingung (wird jetzt von ListSection gehandelt)

**D) Imports aufraeumen:**
- Nicht mehr benoetigte Ionic-Imports entfernen die nur fuer den Header-Banner genutzt wurden (z.B. IonListHeader, IonCard, IonCardContent -- ABER nur wenn sie wirklich nirgends anders in der Datei genutzt werden!)

WICHTIG:
- Die gesamte View-Logik (Filterung, Sortierung, Event-Status-Berechnung) bleibt UNVERAENDERT
- List-Items (app-list-item) bleiben exakt wie sie sind
- Corner-Badges, farbige Striche, Chips bleiben erhalten
- KEINE Emojis
- Echte Umlaute in deutschen Texten (ue -> ue ist ok in Variablennamen)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "SectionHeader" src/components/konfi/views/EventsView.tsx && grep -c "ListSection" src/components/konfi/views/EventsView.tsx && grep -c "SectionHeader" src/components/konfi/views/RequestsView.tsx && grep -c "boxShadow.*0 8px 32px" src/components/konfi/views/EventsView.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>EventsView.tsx und RequestsView.tsx nutzen SectionHeader fuer den Header-Banner und ListSection fuer Listen-Sektionen. Kein Inline-Style Header-Banner mehr vorhanden (boxShadow-Suche liefert 0 Treffer). Bestehende Farblogik, Event-Status-Berechnung und List-Item-Rendering sind unveraendert. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Admin-Core-Views auf Shared Components umstellen</name>
  <files>
    frontend/src/components/admin/EventsView.tsx
    frontend/src/components/admin/ActivitiesView.tsx
    frontend/src/components/admin/KonfisView.tsx
    frontend/src/components/admin/UsersView.tsx
    frontend/src/components/admin/OrganizationView.tsx
    frontend/src/components/admin/ActivityRequestsView.tsx
  </files>
  <action>
Alle 6 Admin-Views nach dem gleichen Muster wie Task 1 refactoren. Fuer JEDE View:

**A) SectionHeader einbauen:**
Import: `import { SectionHeader, ListSection } from '../shared';`

Preset-Zuordnung:
- **EventsView.tsx**: `preset="events"`, Icon und Stats aus bestehender View
- **ActivitiesView.tsx**: `preset="activities"`, Icon `flash`, Stats: Gesamt/Gottesdienst/Gemeinde Counts
- **KonfisView.tsx**: `preset="konfis"`, Icon `people` oder bestehend, Stats: Gesamt/Jahrgang-Counts
- **UsersView.tsx**: `preset="users"`, Icon `people`, Stats: Gesamt/Admins/Team
- **OrganizationView.tsx**: `preset="organizations"`, Icon `business`. ACHTUNG: Das bestehende Sonderformat (220px Hoehe, grosser Hintergrundtext) wird durch den normalen kompakten SectionHeader ersetzt (per User-Decision). Title, Subtitle und Stats aus den bestehenden Daten ableiten.
- **ActivityRequestsView.tsx**: `preset="requests"`, Icon und Stats aus bestehender View

**B) ListSection einbauen:**
Gleiche Logik wie Task 1: IonList+IonListHeader+IonCard+IonCardContent durch ListSection ersetzen. Fuer jede View:
- iconColorClass passend zum Preset
- isEmpty-Bedingung aus der bestehenden View-Logik
- emptyIcon, emptyTitle, emptyMessage aus den bestehenden Inline-EmptyState-Texten uebernehmen
- emptyIconColor passend zur Sektionsfarbe

**C) OrganizationView.tsx Sonderbehandlung:**
- Das grosse Header-Format (height: 220px, Hintergrundtext mit opacity 0.05, etc.) KOMPLETT entfernen
- Durch kompakten SectionHeader ersetzen mit 3 Stats-Werten aus den bestehenden Organization-Daten
- Alle anderen Funktionalitaeten (Suche, Filter, Listen, Slide-Actions) bleiben identisch

**D) Imports aufraeumen:**
Nicht mehr benoetigte Imports entfernen, aber NUR wenn sie wirklich nirgends anders in der Datei verwendet werden.

WICHTIG:
- Bestehende Such-Funktionalitaet, Filter, IonSegment Tabs, Slide-Actions bleiben ALLE erhalten
- List-Items (app-list-item, Corner-Badges, farbige Striche) bleiben exakt wie sie sind
- Admin-spezifische Features (Edit/Delete Buttons, Slide-Options) bleiben erhalten
- KEINE Emojis
- Echte Umlaute in deutschen Texten
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && for f in EventsView ActivitiesView KonfisView UsersView OrganizationView ActivityRequestsView; do echo "=== $f ===" && grep -c "SectionHeader" src/components/admin/$f.tsx && grep -c "boxShadow.*0 8px 32px" src/components/admin/$f.tsx; done && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Alle 6 Admin-Views nutzen SectionHeader und ListSection. Kein Inline-Style Header-Banner mehr vorhanden (boxShadow-Suche liefert 0 Treffer in allen 6 Dateien). OrganizationView nutzt kompakten SectionHeader statt Sonderformat. Bestehende Admin-Features (Suche, Filter, Slide-Actions, Edit/Delete) sind unveraendert. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. Fuer alle 8 Views: `grep "SectionHeader" {file}` liefert mindestens 1 Treffer
2. Fuer alle 8 Views: `grep "boxShadow.*0 8px 32px" {file}` liefert 0 Treffer (kein Inline-Banner mehr)
3. `npx tsc --noEmit` kompiliert ohne Fehler
4. OrganizationView: `grep "height.*220" frontend/src/components/admin/OrganizationView.tsx` liefert 0 Treffer
5. Stichprobe: `grep "app-list-item" frontend/src/components/konfi/views/EventsView.tsx` liefert weiterhin Treffer (List-Items unveraendert)
</verification>

<success_criteria>
- Alle 8 Haupt-Views importieren und nutzen SectionHeader aus shared/
- Kein Inline-Style Header-Banner (linear-gradient + boxShadow Pattern) mehr in diesen 8 Views
- EmptyState wird ueber ListSection automatisch angezeigt wenn Listen leer sind
- OrganizationView nutzt kompakten SectionHeader statt Sonderformat
- Bestehende Funktionalitaet (Events, Filter, Suche, Slide-Actions, Farblogik) ist in allen Views unveraendert
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-grundlagen/03-02-SUMMARY.md`
</output>
