---
phase: 03-design-system-grundlagen
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/admin/BadgesView.tsx
  - frontend/src/components/admin/pages/AdminLevelsPage.tsx
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  - frontend/src/components/admin/pages/AdminCategoriesPage.tsx
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/components/chat/ChatOverview.tsx
autonomous: true
requirements: [DES-01, DES-02, DES-03, DES-04]

must_haves:
  truths:
    - "Alle 8 restlichen Views mit Header-Banner nutzen SectionHeader statt Inline-Styles"
    - "Leere Listen in diesen Views zeigen EmptyState via ListSection oder direkt EmptyState"
    - "Die bestehende Farblogik und Funktionalitaet jeder View ist unveraendert erhalten"
    - "Nach diesem Plan gibt es KEINE View mehr im Projekt die das Inline-Style Header-Banner-Pattern verwendet"
  artifacts:
    - path: "frontend/src/components/admin/BadgesView.tsx"
      provides: "Badges-View mit SectionHeader preset badges"
      contains: "import.*SectionHeader"
    - path: "frontend/src/components/chat/ChatOverview.tsx"
      provides: "Chat-Uebersicht mit SectionHeader"
      contains: "import.*SectionHeader"
  key_links:
    - from: "frontend/src/components/admin/BadgesView.tsx"
      to: "frontend/src/components/shared/SectionHeader.tsx"
      via: "Import und Nutzung mit preset='badges'"
      pattern: "import.*SectionHeader.*from.*shared"
    - from: "frontend/src/components/chat/ChatOverview.tsx"
      to: "frontend/src/components/shared/SectionHeader.tsx"
      via: "Import und Nutzung mit colors fuer Chat"
      pattern: "import.*SectionHeader.*from.*shared"
---

<objective>
Die restlichen 8 Views mit Header-Banner-Pattern auf SectionHeader umstellen und verbleibende EmptyState-Stellen durch die EmptyState-Component ersetzen. Nach diesem Plan verwendet keine View mehr Inline-Style Header-Banner.

Purpose: Vollstaendige Eliminierung des redundanten Header-Banner-Inline-Styling aus der gesamten Codebase. Zusammen mit Plan 02 sind dann ALLE Views umgestellt.
Output: 8 weitere refactored Views, keine Inline-Banner mehr im Projekt.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-grundlagen/03-CONTEXT.md
@.planning/phases/03-design-system-grundlagen/03-01-SUMMARY.md

Referenz fuer das Refactoring-Pattern:
@frontend/src/components/shared/SectionHeader.tsx
@frontend/src/components/shared/EmptyState.tsx
@frontend/src/components/shared/ListSection.tsx

<interfaces>
<!-- Von Plan 03-01 erstellte Components: -->

SectionHeader Props:
  title: string (required)
  subtitle: string (required)
  icon: string (ionicons icon)
  preset?: 'events' | 'activities' | 'konfis' | 'users' | 'organizations' | 'badges' | 'requests' | 'jahrgang'
  colors?: { primary: string; secondary: string }
  stats: Array<{ value: number; label: string }>

EmptyState Props:
  icon: string
  title: string
  message: string
  iconColor?: string

ListSection Props:
  icon: string
  title: string
  count: number
  iconColorClass?: string
  children: React.ReactNode
  emptyIcon?: string
  emptyTitle?: string
  emptyMessage?: string
  emptyIconColor?: string
  isEmpty?: boolean

Import-Pfade:
  - Aus admin/: import { SectionHeader, ListSection, EmptyState } from '../shared';
  - Aus admin/pages/: import { SectionHeader, ListSection, EmptyState } from '../../shared';
  - Aus konfi/views/: import { SectionHeader, ListSection, EmptyState } from '../../shared';
  - Aus chat/: import { SectionHeader, ListSection, EmptyState } from '../shared';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin-Pages und BadgesView auf Shared Components umstellen</name>
  <files>
    frontend/src/components/admin/BadgesView.tsx
    frontend/src/components/admin/pages/AdminLevelsPage.tsx
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
    frontend/src/components/admin/pages/AdminCategoriesPage.tsx
  </files>
  <action>
Alle 4 Dateien refactoren. Gleiches Muster wie Plan 02:

**AdminBadgesView.tsx:**
- Import: `import { SectionHeader, ListSection } from '../shared';`
- SectionHeader mit `preset="badges"`, Icon und Stats aus bestehender View
- ListSection fuer die Badges-Liste mit EmptyState-Props
- Bestehende Badge-Darstellung und -Logik bleibt identisch

**AdminLevelsPage.tsx:**
- Import: `import { SectionHeader, ListSection } from '../../shared';`
- SectionHeader mit `preset="konfis"` oder `colors` passend zur bestehenden Level-Farbe
- Title, Subtitle, Stats aus der bestehenden View uebernehmen
- ACHTUNG: Dies ist eine Page (nicht View) -- hier wird nur der Header-Banner-Teil ersetzt, die Page-Struktur (IonPage, IonContent etc.) bleibt

**AdminJahrgaengeePage.tsx:**
- Import: `import { SectionHeader, ListSection } from '../../shared';`
- SectionHeader mit `preset="jahrgang"`, Stats aus bestehender View
- ListSection fuer die Jahrgaenge-Liste

**AdminCategoriesPage.tsx:**
- Import: `import { SectionHeader, ListSection } from '../../shared';`
- SectionHeader mit `preset="activities"` (Kategorien gehoeren zu Aktivitaeten), Stats aus bestehender View
- ListSection fuer die Kategorien-Liste

Fuer JEDE Datei:
1. Inline-Style Header-Banner-Block (div mit background: linear-gradient, boxShadow, dekorative Kreise, Icon-Box, Stats-Row) durch SectionHeader-Aufruf ersetzen
2. IonList+IonListHeader+IonCard+IonCardContent durch ListSection ersetzen wo passend
3. Inline-EmptyState-HTML durch EmptyState via ListSection ersetzen
4. Nicht mehr benoetigte Imports aufraeumen (NUR wenn wirklich nirgends anders genutzt)

WICHTIG:
- Bestehende Funktionalitaet (CRUD, Filter, Suche, Slide-Actions) bleibt KOMPLETT erhalten
- KEINE Emojis
- Echte Umlaute in deutschen Texten
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "SectionHeader" src/components/admin/BadgesView.tsx && grep -c "SectionHeader" src/components/admin/pages/AdminLevelsPage.tsx && grep -c "SectionHeader" src/components/admin/pages/AdminJahrgaengeePage.tsx && grep -c "SectionHeader" src/components/admin/pages/AdminCategoriesPage.tsx && grep -c "boxShadow.*0 8px 32px" src/components/admin/BadgesView.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Alle 4 Views nutzen SectionHeader und ListSection. Kein Inline-Style Header-Banner mehr. Bestehende CRUD-Funktionalitaet und Admin-Features sind unveraendert. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Konfi-Views und ChatOverview auf Shared Components umstellen</name>
  <files>
    frontend/src/components/konfi/views/ProfileView.tsx
    frontend/src/components/konfi/views/DashboardView.tsx
    frontend/src/components/konfi/views/BadgesView.tsx
    frontend/src/components/chat/ChatOverview.tsx
  </files>
  <action>
Alle 4 Dateien refactoren:

**ProfileView.tsx:**
- Import: `import { SectionHeader } from '../../shared';`
- SectionHeader mit `preset="konfis"` oder passende Farben, Stats aus bestehender View (z.B. Punkte-Werte)
- Hier moeglicherweise keine ListSection noetig (Profil hat keine Listen), nur Header ersetzen

**DashboardView.tsx:**
- Import: `import { SectionHeader, ListSection, EmptyState } from '../../shared';`
- SectionHeader mit passenden Farben/Preset fuer das Dashboard
- Wo Listen mit EmptyState vorkommen: ListSection nutzen
- ACHTUNG: DashboardView hat moeglicherweise mehrere Sektionen -- jede Sektion bekommt eigene ListSection

**BadgesView.tsx (Konfi):**
- Import: `import { SectionHeader, ListSection } from '../../shared';`
- SectionHeader mit `preset="badges"`, Stats aus bestehender View
- ListSection fuer die Badges-Liste mit EmptyState-Props

**ChatOverview.tsx:**
- Import: `import { SectionHeader, ListSection } from '../shared';`
- SectionHeader: Chat hat keinen Standard-Preset, daher `colors={{ primary: '#06b6d4', secondary: '#0891b2' }}` verwenden (oder Preset 'chat' falls in SectionHeader vorhanden -- falls nicht, colors nutzen)
- ListSection fuer Chat-Room-Listen mit EmptyState
- ACHTUNG: Chat-Logik und Echtzeit-Features bleiben KOMPLETT erhalten

Fuer JEDE Datei:
1. Inline-Style Header-Banner durch SectionHeader ersetzen
2. Inline-EmptyState-HTML durch EmptyState (direkt oder via ListSection) ersetzen
3. Imports aufraeumen

WICHTIG:
- Bestehende Funktionalitaet bleibt KOMPLETT erhalten
- Chat-Echtzeit-Features, Polling, WebSocket-Verbindungen nicht anfassen
- KEINE Emojis
- Echte Umlaute in deutschen Texten
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "SectionHeader" src/components/konfi/views/ProfileView.tsx && grep -c "SectionHeader" src/components/konfi/views/DashboardView.tsx && grep -c "SectionHeader" src/components/konfi/views/BadgesView.tsx && grep -c "SectionHeader" src/components/chat/ChatOverview.tsx && grep -rc "boxShadow.*0 8px 32px" src/components/ --include="*.tsx" | grep -v ":0$" | wc -l && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Alle 4 Views nutzen SectionHeader. Die Gesamtsuche nach Inline-Style Header-Banner (boxShadow Pattern) im gesamten components/ Verzeichnis liefert 0 Treffer (ausgenommen Nicht-View-Dateien wie auth/LoginView die nicht zum Phase-3-Scope gehoeren). TypeScript kompiliert fehlerfrei. Bestehende Chat-Features und Konfi-Profil-Logik sind unveraendert.</done>
</task>

</tasks>

<verification>
1. Fuer alle 8 Views: `grep "SectionHeader" {file}` liefert mindestens 1 Treffer
2. Gesamtsuche: `grep -rc "boxShadow.*0 8px 32px" frontend/src/components/ --include="*.tsx"` -- nur noch Treffer in auth/ Dateien (LoginView, RegisterPage etc. -- ausserhalb Scope) und PointsHistoryModal/KonfiDetailView (Modals/Detail-Views, Phase 4+)
3. `npx tsc --noEmit` kompiliert ohne Fehler
4. `grep "import.*SectionHeader" frontend/src/components/chat/ChatOverview.tsx` zeigt korrekten Import
</verification>

<success_criteria>
- Alle 8 restlichen Views importieren und nutzen SectionHeader
- Zusammen mit Plan 02: ALLE 16 Views mit Header-Banner sind umgestellt
- EmptyState wird in allen Listen konsistent angezeigt
- Bestehende Funktionalitaet ist in allen Views unveraendert
- TypeScript kompiliert fehlerfrei
- Die einzigen verbleibenden Inline-Header-Banner sind in auth/ Views (ausserhalb v1.1 Scope) und ggf. Modals (Phase 6)
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-grundlagen/03-03-SUMMARY.md`
</output>
