---
phase: 14-konfi-views-dashboard-events-badges
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/components/konfi/modals/PointsHistoryModal.tsx
autonomous: true
requirements: [KUI-01, KUI-03, KUI-05]

must_haves:
  truths:
    - "Erster Konfi-Tab heisst 'Start' statt 'Dashboard'"
    - "BadgesView zeigt EmptyState-Komponente wenn Filter 'Offen' keine Badges hat"
    - "BadgesView zeigt EmptyState-Komponente wenn Filter 'In Arbeit' keine Badges hat"
    - "PointsHistoryModal 'Gesamt'-Text ist nicht abgeschnitten und alle Labels sind vollstaendig sichtbar"
    - "PointsHistoryModal Stats-Boxen zeigen 3 in der ersten Reihe und 2 in der zweiten Reihe"
  artifacts:
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Tab-Umbenennung Dashboard -> Start"
      contains: "Start"
    - path: "frontend/src/components/konfi/views/BadgesView.tsx"
      provides: "EmptyState fuer leere Offen/In Arbeit Sektionen"
      contains: "EmptyState"
    - path: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      provides: "Korrigiertes Stats-Layout 3+2 statt 5 nebeneinander"
      contains: "flexWrap.*wrap"
  key_links:
    - from: "frontend/src/components/layout/MainTabs.tsx"
      to: "Konfi TabBar"
      via: "IonTabButton label"
      pattern: "IonLabel.*Start"
    - from: "frontend/src/components/konfi/views/BadgesView.tsx"
      to: "frontend/src/components/shared/EmptyState.tsx"
      via: "EmptyState import"
      pattern: "import.*EmptyState"
---

<objective>
Drei kleine Konfi-UI-Fixes: Tab-Umbenennung, BadgesView EmptyState und PointsHistoryModal Layout.

Purpose: Drei schnelle, unabhaengige visuelle Verbesserungen in den Konfi-Hauptansichten umsetzen.
Output: Aktualisierte MainTabs.tsx, BadgesView.tsx und PointsHistoryModal.tsx.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/layout/MainTabs.tsx
@frontend/src/components/konfi/views/BadgesView.tsx
@frontend/src/components/konfi/modals/PointsHistoryModal.tsx
@frontend/src/components/shared/EmptyState.tsx

<interfaces>
Aus frontend/src/components/layout/MainTabs.tsx (Zeile 299-302):
```tsx
<IonTabButton tab="dashboard" href="/konfi/dashboard">
  <IonIcon icon={home} />
  <IonLabel>Dashboard</IonLabel>
</IonTabButton>
```
-- IonLabel Text muss von "Dashboard" auf "Start" geaendert werden. Der tab-Name und href bleiben unveraendert (technischer Identifier).

Aus frontend/src/components/konfi/views/BadgesView.tsx (Zeile 297-327):
```tsx
const getBadgeCategories = () => {
  let filtered = badges;
  switch (selectedFilter) {
    case 'nicht_erhalten':
      filtered = badges.filter(badge => !badge.is_earned);
      break;
    case 'in_arbeit':
      filtered = badges.filter(badge => !badge.is_earned && badge.progress_percentage && badge.progress_percentage > 0);
      break;
    default:
      filtered = badges;
  }
  // ... categories werden erstellt und leere gefiltert (Zeile 326)
  return categories.filter(cat => cat.badges.length > 0);
};
```
-- Wenn getBadgeCategories() leer ist, wird aktuell NUR der bestehende EmptyState fuer "Alle" angezeigt (Zeile 387-397). Fuer "Offen" und "In Arbeit" fehlt ein eigener EmptyState.
-- Aktuell: EmptyState wird innerhalb der `badgeCategories.length === 0` Bedingung angezeigt, aber der Text ist generisch ("Keine Badges gefunden").

Aus frontend/src/components/konfi/modals/PointsHistoryModal.tsx (Zeile 203-217):
```tsx
{/* Stats Boxen - 5 nebeneinander */}
<div className="app-detail-header__info-row" style={{ justifyContent: 'center', gap: '6px', position: 'relative', zIndex: 1 }}>
  {[
    { value: calculatedTotals.total, label: 'GESAMT' },
    { value: calculatedTotals.gottesdienst, label: 'GD' },
    { value: calculatedTotals.gemeinde, label: 'GEM' },
    { value: calculatedTotals.eventCount, label: 'EVENTS' },
    { value: calculatedTotals.bonusCount, label: 'BONUS' }
  ].map((stat) => (
    <div key={stat.label} className="app-detail-header__info-chip" style={{ textAlign: 'center', flex: '1 1 0', maxWidth: '70px', padding: '10px 8px' }}>
      <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{stat.value}</div>
      <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>{stat.label}</div>
    </div>
  ))}
</div>
```
-- Problem: 5 Boxen nebeneinander (4+1 oder 5 gequetscht), "GESAMT" Label wird abgeschnitten.
-- Loesung: Zwei Reihen -- 3 oben (Gesamt, GD, Gemeinde), 2 unten (Events, Bonus). Chip-Breite erhoehen, Labels ausschreiben.

Aus frontend/src/components/shared/EmptyState.tsx:
```tsx
interface EmptyStateProps {
  icon: string;
  title: string;
  message: string;
  iconColor?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab "Dashboard" in "Start" umbenennen und BadgesView EmptyState hinzufuegen</name>
  <files>frontend/src/components/layout/MainTabs.tsx, frontend/src/components/konfi/views/BadgesView.tsx</files>
  <action>
  **KUI-01: Tab-Umbenennung**
  In `frontend/src/components/layout/MainTabs.tsx` (Zeile 301):
  - `<IonLabel>Dashboard</IonLabel>` aendern zu `<IonLabel>Start</IonLabel>`
  - NUR den sichtbaren Label-Text aendern. `tab="dashboard"` und `href="/konfi/dashboard"` bleiben unveraendert (technische Route).

  **KUI-03: BadgesView EmptyState fuer "Offen" und "In Arbeit"**
  In `frontend/src/components/konfi/views/BadgesView.tsx`:

  1. Der bestehende EmptyState (Zeile 387-397) wird angezeigt wenn `badgeCategories.length === 0`. Dieser muss filter-abhaengige Texte bekommen:

  2. Die EmptyState-Anzeige im `badgeCategories.length === 0` Block (Zeile 387-397) durch eine filter-abhaengige Variante ersetzen:
     ```tsx
     {badgeCategories.length === 0 ? (
       <IonCard className="app-card">
         <IonCardContent>
           {selectedFilter === 'nicht_erhalten' ? (
             <EmptyState
               icon={checkmarkCircle}
               title="Alle Badges erreicht!"
               message="Du hast alle sichtbaren Badges eingesammelt."
               iconColor="#22c55e"
             />
           ) : selectedFilter === 'in_arbeit' ? (
             <EmptyState
               icon={trophyOutline}
               title="Keine Badges in Arbeit"
               message="Sammle Punkte, um den Fortschritt bei Badges zu starten!"
               iconColor="#667eea"
             />
           ) : (
             <EmptyState
               icon={trophyOutline}
               title="Keine Badges gefunden"
               message="Sammle Punkte für deine ersten Badges!"
               iconColor="#f59e0b"
             />
           )}
         </IonCardContent>
       </IonCard>
     ) : (
     ```

  3. Sicherstellen dass `checkmarkCircle` bereits importiert ist (ist es -- Zeile 21). `trophyOutline` ist ebenfalls bereits importiert (Zeile 17).

  WICHTIG: Keine Emojis verwenden. Echte Umlaute in UI-Texten verwenden (ue -> ue ist hier korrekt, da es deutsche Woerter sind die kein ue-Umlaut haben).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - Konfi TabBar zeigt "Start" statt "Dashboard" als ersten Tab-Label
  - BadgesView zeigt bei Filter "Offen" den Text "Alle Badges erreicht!" mit gruenem Haekchen-Icon wenn keine offenen Badges vorhanden
  - BadgesView zeigt bei Filter "In Arbeit" den Text "Keine Badges in Arbeit" mit blauem Pokal-Icon wenn keine in-Arbeit Badges vorhanden
  - BadgesView zeigt bei Filter "Alle" weiterhin den bestehenden EmptyState
  </done>
</task>

<task type="auto">
  <name>Task 2: PointsHistoryModal Stats-Layout von 5-nebeneinander auf 3+2 aendern</name>
  <files>frontend/src/components/konfi/modals/PointsHistoryModal.tsx</files>
  <action>
  **KUI-05: PointsHistoryModal Layout-Fix**
  In `frontend/src/components/konfi/modals/PointsHistoryModal.tsx`:

  1. Den Stats-Bereich (Zeile 203-217) komplett ersetzen. Statt einem einzigen `app-detail-header__info-row` Container mit 5 Chips nebeneinander, zwei separate Reihen verwenden:

  ```tsx
  {/* Stats Boxen - 3 oben + 2 unten */}
  <div style={{ position: 'relative', zIndex: 1 }}>
    {/* Erste Reihe: Gesamt, Gottesdienst, Gemeinde */}
    <div className="app-detail-header__info-row" style={{ justifyContent: 'center', gap: '8px' }}>
      {[
        { value: calculatedTotals.total, label: 'GESAMT' },
        { value: calculatedTotals.gottesdienst, label: 'GOTTESDIENST' },
        { value: calculatedTotals.gemeinde, label: 'GEMEINDE' }
      ].map((stat) => (
        <div key={stat.label} className="app-detail-header__info-chip" style={{ textAlign: 'center', flex: '1 1 0', padding: '10px 8px' }}>
          <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{stat.value}</div>
          <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>{stat.label}</div>
        </div>
      ))}
    </div>
    {/* Zweite Reihe: Events, Bonus */}
    <div className="app-detail-header__info-row" style={{ justifyContent: 'center', gap: '8px', marginTop: '8px' }}>
      {[
        { value: calculatedTotals.eventCount, label: 'EVENTS' },
        { value: calculatedTotals.bonusCount, label: 'BONUS' }
      ].map((stat) => (
        <div key={stat.label} className="app-detail-header__info-chip" style={{ textAlign: 'center', flex: '1 1 0', maxWidth: '45%', padding: '10px 8px' }}>
          <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{stat.value}</div>
          <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>{stat.label}</div>
        </div>
      ))}
    </div>
  </div>
  ```

  2. Den Kommentar `{/* Stats Boxen - 5 nebeneinander */}` (Zeile 203) ersetzen durch `{/* Stats Boxen - 3 oben + 2 unten */}`.

  3. Die `maxWidth: '70px'` Beschraenkung entfernen (war noetig um 5 Chips zu quetschen, bei 3 bzw 2 pro Reihe nicht mehr noetig). Die erste Reihe braucht kein maxWidth, die zweite Reihe bekommt `maxWidth: '45%'` damit die 2 Chips nicht die volle Breite einnehmen.

  4. Labels ausschreiben: 'GD' -> 'GOTTESDIENST', 'GEM' -> 'GEMEINDE'. 'GESAMT', 'EVENTS' und 'BONUS' bleiben gleich.

  WICHTIG: Keine Emojis verwenden. Die Labels sind Grossbuchstaben-Abkuerzungen fuer die Stats-Anzeige.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - PointsHistoryModal zeigt Stats in 2 Reihen: 3 Chips (Gesamt, Gottesdienst, Gemeinde) oben, 2 Chips (Events, Bonus) unten
  - "GESAMT" Label ist vollstaendig sichtbar und nicht abgeschnitten
  - Labels sind ausgeschrieben statt abgekuerzt (GOTTESDIENST statt GD, GEMEINDE statt GEM)
  - Chips haben ausreichend Platz und sind gut lesbar
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` kompiliert fehlerfrei
2. Visuelle Pruefung: Erster Konfi-Tab zeigt "Start" statt "Dashboard"
3. Visuelle Pruefung: BadgesView "Offen"-Filter zeigt gruenen EmptyState wenn alle Badges erreicht
4. Visuelle Pruefung: BadgesView "In Arbeit"-Filter zeigt blauen EmptyState wenn keine Badges in Arbeit
5. Visuelle Pruefung: PointsHistoryModal Stats zeigen 3+2 Layout ohne abgeschnittene Labels
</verification>

<success_criteria>
- KUI-01: Tab heisst "Start" statt "Dashboard"
- KUI-03: EmptyState-Komponenten fuer leere "Offen"- und "In Arbeit"-Sektionen in BadgesView
- KUI-05: PointsHistoryModal Stats-Layout 3+2 mit vollstaendigen Labels
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/14-konfi-views-dashboard-events-badges/14-01-SUMMARY.md`
</output>
