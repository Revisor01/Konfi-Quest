---
phase: 14-konfi-views-dashboard-events-badges
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/EventDetailView.tsx
autonomous: true
requirements: [KUI-02]

must_haves:
  truths:
    - "Konfi EventDetailView zeigt Teilnehmer:innen als individuelle Listen-Eintraege statt Komma-getrennt"
    - "Jeder Teilnehmer-Eintrag hat ein Icon-Circle und den display_name"
    - "Die Teilnehmer-Sektion folgt dem Design-System (app-list-item Pattern)"
  artifacts:
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "Redesignte Teilnehmer:innen-Anzeige im Design-System Style"
      contains: "app-list-item"
  key_links:
    - from: "frontend/src/components/konfi/views/EventDetailView.tsx"
      to: "Design-System CSS-Klassen"
      via: "app-list-item, app-icon-circle Klassen"
      pattern: "app-list-item|app-icon-circle"
---

<objective>
Konfi EventDetailView Teilnehmer:innen-Anzeige ans Design-System anpassen.

Purpose: Die Teilnehmer-Anzeige der Konfi-EventDetailView ist aktuell nur ein Komma-getrennter Text aller Namen. Diese soll stattdessen individuelle Listen-Eintraege im Design-System Style zeigen, passend zum Rest der App.
Output: Aktualisierte EventDetailView.tsx mit Design-System-konformer Teilnehmer-Anzeige.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/konfi/views/EventDetailView.tsx
@frontend/src/components/admin/views/EventDetailView.tsx
@frontend/src/theme/variables.css

<interfaces>
Aus frontend/src/components/konfi/views/EventDetailView.tsx:

Teilnehmer-Interface (Zeile 96-99):
```tsx
interface Participant {
  id: number;
  display_name: string;
}
```

Aktuelle Teilnehmer-Anzeige (Zeile 585-605):
```tsx
{/* Teilnehmer-Liste */}
{participants.length > 0 && (
  <IonList className="app-section-inset" inset={true}>
    <IonListHeader>
      <div className="app-section-icon app-section-icon--success">
        <IonIcon icon={people} />
      </div>
      <IonLabel>Teilnehmer:innen ({participants.length})</IonLabel>
    </IonListHeader>
    <IonCard className="app-card">
      <IonCardContent>
        <div className="app-info-row">
          <IonIcon icon={people} className="app-info-row__icon app-icon-color--participants" />
          <div className="app-info-row__content">
            {participants.map(p => p.display_name).join(', ')}
          </div>
        </div>
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```
-- Problem: Teilnehmer werden nur als komma-getrennter Text angezeigt. Das passt nicht zum Design-System.
-- Loesung: Individuelle app-list-item Eintraege mit Icon-Circle und Namen, wie sie in der Admin-View und anderen Konfi-Views verwendet werden.

Referenz aus Admin EventDetailView (verkuerzt, Zeile 888-960):
Die Admin-View verwendet IonItemSliding mit app-list-item Klassen, Icon-Circles und Attendance-Status. Die Konfi-View braucht KEINE Attendance-Verwaltung oder Sliding-Actions -- nur die visuelle Darstellung als Liste.

Bereits importierte Icons (Zeile 22-37): arrowBack, calendar, location, people, time, trophy, checkmarkCircle, closeCircle, informationCircle, warning, hourglass, listOutline, home, pricetag
-- `personAdd` oder `person` fehlt im Import, `person` oder `personOutline` muss ggf. hinzugefuegt werden fuer individuelle Personen-Icons.

Design-System Klassen (aus variables.css):
- `.app-list-item` -- Basis-Listeneintrag
- `.app-list-item__row` -- Flex-Row
- `.app-list-item__main` -- Main-Content Bereich
- `.app-icon-circle` -- Runder Icon-Container
- `.app-icon-circle--events` -- Rote Event-Farbe
- `.app-list-item__content` -- Content neben dem Icon
- `.app-list-item__title` -- Name/Titel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teilnehmer:innen-Anzeige von Komma-Text auf Design-System Listen-Eintraege umbauen</name>
  <files>frontend/src/components/konfi/views/EventDetailView.tsx</files>
  <action>
  **KUI-02: EventDetailView Teilnehmer-Redesign**

  In `frontend/src/components/konfi/views/EventDetailView.tsx`:

  1. **Import erweitern** (Zeile 22-37): `personOutline` aus 'ionicons/icons' importieren (fuer individuelle Personen-Icons in der Liste).

  2. **Teilnehmer-Sektion komplett ersetzen** (Zeile 585-605). Den gesamten Block ab `{/* Teilnehmer-Liste */}` bis zum schliessenden `)}` ersetzen durch:

  ```tsx
  {/* Teilnehmer-Liste */}
  {participants.length > 0 && (
    <IonList className="app-section-inset" inset={true}>
      <IonListHeader>
        <div className="app-section-icon app-section-icon--events">
          <IonIcon icon={people} />
        </div>
        <IonLabel>Teilnehmer:innen ({participants.length})</IonLabel>
      </IonListHeader>
      <IonCard className="app-card">
        <IonCardContent style={{ padding: '8px' }}>
          <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
            {participants.map((participant) => (
              <div
                key={participant.id}
                className="app-list-item app-list-item--events"
              >
                <div className="app-list-item__row">
                  <div className="app-list-item__main">
                    <div className="app-icon-circle app-icon-circle--events">
                      <IonIcon icon={personOutline} />
                    </div>
                    <div className="app-list-item__content">
                      <div className="app-list-item__title">{participant.display_name}</div>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </IonCardContent>
      </IonCard>
    </IonList>
  )}
  ```

  3. **Design-Aenderungen gegenueber dem Original:**
     - `app-section-icon--success` auf `app-section-icon--events` aendern (Events-Farbe Rot, konsistent mit der View)
     - Statt einem `app-info-row` mit Komma-Text: individuelle `app-list-item` Eintraege pro Teilnehmer
     - Jeder Eintrag bekommt ein `app-icon-circle--events` mit `personOutline` Icon
     - Layout nutzt flexbox mit gap fuer gleichmaessigen Abstand
     - CardContent padding reduziert auf 8px fuer kompaktere Darstellung

  4. **NICHT aendern:**
     - Die Participant-Interface bleibt gleich (id, display_name)
     - Keine Attendance-Status oder Sliding-Actions (das ist Admin-only)
     - Der API-Call bleibt gleich
     - Die Bedingung `participants.length > 0` bleibt

  WICHTIG: Keine Emojis verwenden. Das `person` Icon MUSS als `personOutline` importiert werden (Outline-Variante fuer Listen-Icons, konsistent mit dem Design-System).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
  - Teilnehmer:innen werden als individuelle Listen-Eintraege angezeigt, nicht als Komma-getrennter Text
  - Jeder Eintrag hat ein rotes (Events-Farbe) Icon-Circle mit Personen-Icon und den Display-Namen
  - Die Sektion verwendet das app-list-item Pattern aus dem Design-System
  - Section-Icon nutzt Events-Farbe (Rot) statt Success-Farbe (Gruen)
  - TypeScript kompiliert fehlerfrei
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` kompiliert fehlerfrei
2. Visuelle Pruefung: Konfi EventDetailView zeigt Teilnehmer als individuelle Listeneintraege mit Icon und Namen
3. Visuelle Pruefung: Teilnehmer-Sektion nutzt Events-Farbschema (Rot) konsistent mit dem Rest der View
4. Visuelle Pruefung: Bei Events ohne Teilnehmer wird die Sektion weiterhin nicht angezeigt
</verification>

<success_criteria>
- KUI-02: Teilnehmer:innen-Anzeige folgt dem Design-System mit individuellen Listen-Eintraegen
- Keine funktionalen Regressionen (Anmeldung, Abmeldung weiterhin moeglich)
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/14-konfi-views-dashboard-events-badges/14-02-SUMMARY.md`
</output>
