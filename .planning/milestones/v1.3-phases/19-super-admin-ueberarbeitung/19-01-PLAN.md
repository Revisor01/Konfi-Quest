---
phase: 19-super-admin-ueberarbeitung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/components/admin/pages/AdminOrganizationsPage.tsx
  - frontend/src/components/admin/OrganizationView.tsx
  - frontend/src/theme/variables.css
  - frontend/src/components/shared/SectionHeader.tsx
autonomous: true
requirements: [SUA-01, SUA-02, SUA-03, SUA-04, SUA-05, SUA-07]

must_haves:
  truths:
    - "Super-Admin sieht keine TabBar mehr nach Login"
    - "Super-Admin wird nach Login direkt auf Organisationen-View geleitet (keine weisse Seite)"
    - "Organisationen-View hat durchgehend mattes Blau (#667eea/#5a67d8) als Farbschema"
    - "Super-Admin kann sich ueber sichtbaren Logout-Button abmelden"
    - "Statistik-SectionHeader ist als erstes Element oben in der View positioniert und zeigt mattes Blau statt Gruen"
    - "Organisationen-Listen-Elemente haben blaue border-left statt gruene"
  artifacts:
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Super-Admin Routing ohne TabBar"
    - path: "frontend/src/components/admin/AdminOrganizationsPage.tsx"
      provides: "Logout-Button im Header"
    - path: "frontend/src/components/admin/OrganizationView.tsx"
      provides: "Blaues Farbschema statt Gruen"
  key_links:
    - from: "MainTabs.tsx"
      to: "AdminOrganizationsPage"
      via: "IonRouterOutlet ohne IonTabs"
      pattern: "isSuperAdmin.*IonRouterOutlet"
---

<objective>
Super-Admin-Layout von TabBar auf einzelne Vollbild-View umbauen, mattes Blau als Farbschema durchsetzen, Logout einbauen.

Purpose: Super-Admin braucht keine Tab-Navigation -- er hat nur eine Aufgabe (Organisationen verwalten). Die TabBar nimmt unnoetig Platz weg und die gruene Farbe passt nicht zum Super-Admin-Kontext.
Output: Super-Admin sieht nach Login direkt die Organisationen-View ohne TabBar, in mattem Blau, mit Logout-Button.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/layout/MainTabs.tsx
@frontend/src/components/admin/pages/AdminOrganizationsPage.tsx
@frontend/src/components/admin/OrganizationView.tsx
@frontend/src/theme/variables.css

<interfaces>
<!-- Bestehende Super-Admin Farb-Klassen in variables.css -->
<!-- Aktuell: organizations = #2dd36f (Gruen) -->
<!-- Ziel: organizations = #667eea (mattes Blau, identisch mit "users" Klasse) -->

<!-- SectionHeader preset "organizations" in SectionHeader.tsx -->
From frontend/src/components/shared/SectionHeader.tsx:
```typescript
preset?: 'events' | 'activities' | 'konfis' | 'users' | 'organizations' | 'badges' | 'requests' | 'jahrgang' | 'konfi-requests';
// organizations: { primary: '#2dd36f', secondary: '#28ba62' }  <-- AKTUELL
// Aendern zu: { primary: '#667eea', secondary: '#5a67d8' }     <-- ZIEL (= users preset)
```

<!-- Logout Pattern aus AdminSettingsPage -->
From frontend/src/components/admin/pages/AdminSettingsPage.tsx:
```typescript
import { logout } from '../../../services/auth';
const handleLogout = () => {
  presentAlert({ header: 'Abmelden', message: '...', buttons: [
    { text: 'Abbrechen', role: 'cancel' },
    { text: 'Abmelden', role: 'destructive', handler: async () => {
      await logout(); window.location.href = '/';
    }}
  ]});
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Super-Admin TabBar entfernen, Routing vereinfachen, Logout einbauen</name>
  <files>
    frontend/src/components/layout/MainTabs.tsx,
    frontend/src/components/admin/pages/AdminOrganizationsPage.tsx
  </files>
  <action>
  **MainTabs.tsx:** Den `isSuperAdmin`-Block (Zeilen 167-191) umbauen:
  - Statt `IonTabs` mit `IonTabBar` nur noch ein einfaches `IonRouterOutlet` OHNE `IonTabs`/`IonTabBar` rendern
  - Routes beibehalten: `/admin` -> Redirect `/admin/organizations`, `/admin/organizations` -> AdminOrganizationsPage, `/` -> Redirect `/admin/organizations`
  - Die Route `/admin/settings` und `/admin/profile` und `/admin/users` ENTFERNEN -- Super-Admin braucht nur Organisationen
  - Den ganzen Block in `ModalProvider` wrappen (wie bisher), aber OHNE `IonTabs` und `IonTabBar`
  - Aufbau:
    ```tsx
    <ModalProvider>
      <IonRouterOutlet>
        <Route exact path="/admin" render={() => <Redirect to="/admin/organizations" />} />
        <Route exact path="/admin/organizations" component={AdminOrganizationsPage} />
        <Route exact path="/" render={() => <Redirect to="/admin/organizations" />} />
      </IonRouterOutlet>
    </ModalProvider>
    ```

  **AdminOrganizationsPage.tsx:** Logout-Button im Header einbauen:
  - Imports hinzufuegen: `useIonAlert` (falls nicht vorhanden), `logOut` von ionicons/icons, `logout` von `../../../services/auth`
  - `const [presentAlert] = useIonAlert();` hinzufuegen
  - `handleLogout` Funktion analog zu AdminSettingsPage implementieren (Alert mit Bestaetigung, dann `logout()` + `window.location.href = '/'`)
  - Im IonHeader: Den bisherigen "Zurueck"-Button (arrowBack, slot="start") ENTFERNEN -- Super-Admin hat keine Seite, zu der er zurueck kann
  - Stattdessen im slot="start" einen Logout-Button mit `logOut` Icon hinzufuegen: `<IonButton onClick={handleLogout}><IonIcon icon={logOut} /></IonButton>`
  - Der "Hinzufuegen"-Button (add, slot="end") bleibt wie er ist
  - Den condense-Header (collapse="condense") ENTFERNEN, da er in einer Single-View ohne Navigation nicht noetig ist
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Super-Admin sieht keine TabBar mehr, wird direkt auf Organisationen geleitet, kann sich ueber den Logout-Button im Header abmelden</done>
</task>

<task type="auto">
  <name>Task 2: Farbschema von Gruen auf mattes Blau umstellen</name>
  <files>
    frontend/src/components/shared/SectionHeader.tsx,
    frontend/src/theme/variables.css,
    frontend/src/components/admin/OrganizationView.tsx
  </files>
  <action>
  **SectionHeader.tsx:** Das `organizations` Preset von Gruen auf mattes Blau aendern:
  - `organizations: { primary: '#2dd36f', secondary: '#28ba62' }` aendern zu `organizations: { primary: '#667eea', secondary: '#5a67d8' }`
  - (Identisch mit dem "users" Preset)

  **variables.css:** Alle `--organizations` CSS-Klassen von Gruen (#2dd36f) auf mattes Blau (#667eea) aendern:
  - `.app-list-item--organizations` border-left-color: `#2dd36f` -> `#667eea`
  - `.app-list-item--organizations.app-list-item--selected` border-color: `#2dd36f` -> `#667eea`, background: `rgba(45, 211, 111, 0.08)` -> `rgba(102, 126, 234, 0.08)`
  - `.app-section-icon--organizations` background-color: `#2dd36f` -> `#667eea`
  - `.app-icon-circle--organizations` background-color: `#2dd36f` -> `#667eea`
  - `.app-icon-color--organizations` color: `#2dd36f` -> `#667eea`

  **OrganizationView.tsx:** Inline-Farben von Gruen auf mattes Blau aendern:
  - `app-section-icon--success` in der Filter-Sektion aendern zu `app-section-icon--organizations` (Zeile 132)
  - Die `ListSection` iconColorClass von `"success"` auf `"organizations"` aendern (Zeile 182)
  - Das `emptyIconColor` von `"#2dd36f"` auf `"#667eea"` aendern (Zeile 186)
  - Die Initialen-Avatar backgroundColor von `#2dd36f` (aktiv) auf `#667eea` aendern (Zeile 216)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Organisationen-View durchgehend in mattem Blau (#667eea): SectionHeader, Listen-Elemente, Icons, Avatare, Filter-Sektion</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` kompiliert ohne Fehler
2. Visuell: Super-Admin Login fuehrt direkt zur Organisationen-View ohne TabBar
3. Visuell: Gesamte View in mattem Blau statt Gruen
4. Visuell: Logout-Button oben links im Header sichtbar und funktional
5. Visuell: Statistik-SectionHeader ist als erstes Element oben in der View positioniert (vor der Organisationen-Liste), nicht weiter unten versteckt
</verification>

<success_criteria>
- Super-Admin sieht nach Login die Organisationen-View ohne TabBar
- Farbschema durchgehend mattes Blau (#667eea)
- Logout-Button funktioniert (Alert -> Abmelden -> Redirect zu Login)
- Kein weisser Bildschirm nach Login
</success_criteria>

<output>
After completion, create `.planning/phases/19-super-admin-ueberarbeitung/19-01-SUMMARY.md`
</output>
