---
phase: 54-teamer-dashboard-zertifikat-ansicht-anpassen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - backend/routes/organizations.js
autonomous: true
requirements:
  - CERT-GRID-01
  - CERT-GRID-02
  - CERT-SEED-01
  - CERT-SEED-02

must_haves:
  truths:
    - "Teamer-Dashboard zeigt Zertifikate in einem 2x2 Grid statt horizontalem Scroll"
    - "Zertifikat-Karten sind kompakter mit weniger Padding und kleineren Icons"
    - "Neue Organisationen erhalten automatisch 4 Standard-Zertifikat-Typen"
    - "Bestehende Organisationen ohne Zertifikat-Typen erhalten die 4 Standards beim naechsten Org-Erstellungs-Deploy"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "2x2 CSS Grid Zertifikat-Layout"
      contains: "grid-template-columns"
    - path: "backend/routes/organizations.js"
      provides: "Default-Zertifikat-Seed bei Org-Erstellung"
      contains: "certificate_types"
  key_links:
    - from: "backend/routes/organizations.js"
      to: "certificate_types"
      via: "INSERT in POST /organizations"
      pattern: "INSERT INTO certificate_types"
---

<objective>
Zertifikat-Sektion im Teamer-Dashboard von horizontalem Scroll auf 2x2 CSS Grid umstellen mit kompakteren Karten. Backend: 4 Standard-Zertifikat-Typen automatisch bei Org-Erstellung anlegen (Pattern wie Default-Badges).

Purpose: Bessere Uebersicht der Zertifikate auf dem Dashboard, Standard-Zertifikate sofort verfuegbar
Output: Ueberarbeitetes Grid-Layout + Backend-Seed fuer Zertifikate
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-teamer-dashboard-zertifikat-ansicht-anpassen/54-CONTEXT.md

<interfaces>
<!-- Bestehende Certificate Interface in TeamerDashboardPage.tsx Zeile 92-99 -->
From frontend/src/components/teamer/pages/TeamerDashboardPage.tsx:
```typescript
interface Certificate {
  id: number;
  name: string;
  icon: string;
  issued_date: string | null;
  expiry_date: string | null;
  status: 'valid' | 'expired' | 'not_earned';
}
```

<!-- Default-Badge-Seed Pattern in organizations.js Zeile 222-256 -->
From backend/routes/organizations.js:
```javascript
// Pattern: Array von Objekten, dann for-loop INSERT
const defaultBadges = [
  { name: "...", icon: "...", description: "...", criteria_type: "...", criteria_value: N },
  // ...
];
const badgeQuery = `INSERT INTO custom_badges (...) VALUES ($1, $2, $3, $4, $5, $6, true, false, $7)`;
for (const badge of defaultBadges) {
  await db.query(badgeQuery, [...]);
}
```

<!-- certificate_types Tabelle (aus backend/routes/teamer.js Migration) -->
```sql
CREATE TABLE IF NOT EXISTS certificate_types (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  icon VARCHAR(50) DEFAULT 'ribbon',
  organization_id INTEGER REFERENCES organizations(id),
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zertifikat-Sektion von Scroll auf 2x2 Grid umbauen</name>
  <files>frontend/src/components/teamer/pages/TeamerDashboardPage.tsx</files>
  <read_first>frontend/src/components/teamer/pages/TeamerDashboardPage.tsx (Zeilen 344-451 — Zertifikat-Sektion)</read_first>
  <action>
    In TeamerDashboardPage.tsx die Zertifikat-Sektion (Zeilen 365-448) umbauen:

    1. Container-Div (aktuell Zeile 366-372 mit `display: 'flex', overflowX: 'auto'`) ersetzen durch:
       ```
       display: 'grid',
       gridTemplateColumns: 'repeat(2, 1fr)',
       gap: '10px',
       ```
       Kein overflowX, kein WebkitOverflowScrolling, kein paddingBottom mehr.

    2. Einzelne Karten (aktuell Zeile 385-408) kompakter machen:
       - `minWidth` und `maxWidth` entfernen (Grid regelt die Breite)
       - `flexShrink: 0` entfernen
       - `padding: '12px 10px'` statt '16px 12px'
       - `borderRadius: '12px'` statt '16px'
       - `gap: '6px'` statt '8px'

    3. Icon-Container (Zeile 410-419) kleiner:
       - `width: '36px', height: '36px'` statt '44px'
       - Icon fontSize: `'1.1rem'` statt '1.3rem'

    4. Titel (Zeile 425-434):
       - `fontSize: '0.72rem'` statt '0.8rem'
       - `lineHeight: '1.15'`
       - Hinzufuegen: `overflow: 'hidden', textOverflow: 'ellipsis', display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical' as any`

    5. Section content padding (Zeile 365) auf `padding: '60px 16px 20px 16px'` reduzieren statt '60px 20px 24px 20px'.

    Farbcodierung (Gruen/Rot/Transparent-Dashed) und Popover-onClick beibehalten. Kein neues CSS-File noetig — alles inline styles.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "gridTemplateColumns.*repeat(2, 1fr)" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    - grep -q "display: 'grid'" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    - ! grep -q "overflowX: 'auto'" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx (innerhalb der Zertifikat-Sektion)
  </acceptance_criteria>
  <done>Zertifikat-Sektion zeigt 2x2 Grid mit kompakteren Karten, kein horizontales Scrollen mehr</done>
</task>

<task type="auto">
  <name>Task 2: Standard-Zertifikate bei Org-Erstellung + Seed fuer bestehende Orgs</name>
  <files>backend/routes/organizations.js</files>
  <read_first>backend/routes/organizations.js (Zeilen 222-262 — Default-Badge-Seed Pattern)</read_first>
  <action>
    In organizations.js nach dem Badge-Seed-Block (nach Zeile 256, vor `res.status(201).json`):

    1. Default-Zertifikat-Array definieren:
       ```javascript
       const defaultCertificates = [
         { name: 'Teamer:innen Card', icon: 'card' },
         { name: 'JuLeiCa', icon: 'ribbon' },
         { name: 'Rettungsschwimmer', icon: 'water' },
         { name: 'Erste Hilfe', icon: 'medkit' }
       ];
       ```

    2. INSERT-Loop (gleiches Pattern wie Badges):
       ```javascript
       const certQuery = `INSERT INTO certificate_types (name, icon, organization_id)
                          VALUES ($1, $2, $3)`;
       for (const cert of defaultCertificates) {
         await db.query(certQuery, [cert.name, cert.icon, organizationId]);
       }
       ```

    3. Response-Objekt erweitern um `default_certificates_created: defaultCertificates.length`.

    4. Fuer bestehende Orgs: Am Anfang des Moduls (nach `module.exports = ...`) eine idempotente Seed-Funktion hinzufuegen:
       ```javascript
       const seedDefaultCertificates = async () => {
         try {
           const { rows: orgs } = await db.query(
             `SELECT o.id FROM organizations o
              WHERE NOT EXISTS (
                SELECT 1 FROM certificate_types ct WHERE ct.organization_id = o.id
              )`
           );
           if (orgs.length === 0) return;
           const defaultCerts = [
             { name: 'Teamer:innen Card', icon: 'card' },
             { name: 'JuLeiCa', icon: 'ribbon' },
             { name: 'Rettungsschwimmer', icon: 'water' },
             { name: 'Erste Hilfe', icon: 'medkit' }
           ];
           for (const org of orgs) {
             for (const cert of defaultCerts) {
               await db.query(
                 'INSERT INTO certificate_types (name, icon, organization_id) VALUES ($1, $2, $3)',
                 [cert.name, cert.icon, org.id]
               );
             }
           }
           console.log(`Seeded default certificates for ${orgs.length} organization(s)`);
         } catch (err) {
           console.error('Error seeding default certificates:', err.message);
         }
       };
       seedDefaultCertificates();
       ```
       Diese Funktion laeuft einmal beim Server-Start und fuegt nur fuer Orgs OHNE certificate_types die 4 Standards ein. Idempotent — bei erneutem Start passiert nichts.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/backend && node -e "try { require('./routes/organizations.js'); console.log('OK'); } catch(e) { console.log('SYNTAX ERROR:', e.message); process.exit(1); }" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "defaultCertificates" backend/routes/organizations.js
    - grep -q "certificate_types" backend/routes/organizations.js
    - grep -q "seedDefaultCertificates" backend/routes/organizations.js
    - grep -q "Teamer:innen Card" backend/routes/organizations.js
    - grep -q "JuLeiCa" backend/routes/organizations.js
    - grep -q "Rettungsschwimmer" backend/routes/organizations.js
    - grep -q "Erste Hilfe" backend/routes/organizations.js
  </acceptance_criteria>
  <done>Neue Orgs erhalten automatisch 4 Standard-Zertifikat-Typen, bestehende Orgs werden beim naechsten Deploy nachgefuellt</done>
</task>

</tasks>

<verification>
1. TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
2. Backend-Syntax korrekt: `node -e "require('./routes/organizations.js')"`
3. Zertifikat-Sektion nutzt CSS Grid statt Scroll
4. Default-Zertifikate werden bei Org-Erstellung angelegt
5. Idempotenter Seed fuer bestehende Orgs vorhanden
</verification>

<success_criteria>
- Teamer-Dashboard zeigt Zertifikate in 2x2 Grid mit kompakten Karten
- Kein horizontales Scrollen in der Zertifikat-Sektion
- POST /organizations legt 4 Standard-Zertifikat-Typen an
- Bestehende Orgs ohne Zertifikate erhalten die Standards beim naechsten Server-Start
</success_criteria>

<output>
After completion, create `.planning/phases/54-teamer-dashboard-zertifikat-ansicht-anpassen/54-01-SUMMARY.md`
</output>
