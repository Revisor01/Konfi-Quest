---
phase: 49-badge-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/modals/BadgeManagementModal.tsx
  - frontend/src/components/admin/BadgesView.tsx
autonomous: true
requirements: [UI-05, UI-06, UI-07]
must_haves:
  truths:
    - "Badge-Modal-Auswahl zeigt backgroundColor-Change statt Umrandung"
    - "Badge-Segment (Konfi/Teamer) steht unter dem Header mit normalem Styling"
    - "UI-07 (Teamer-Badge-Ansicht) ist bereits durch Phase 52 erfuellt — keine Aenderung noetig"
  artifacts:
    - path: "frontend/src/components/admin/modals/BadgeManagementModal.tsx"
      provides: "Badge-Modal ohne app-list-item--selected Klasse"
      contains: "backgroundColor.*isSelected"
    - path: "frontend/src/components/admin/BadgesView.tsx"
      provides: "Segment unter Header mit korrektem Styling"
      contains: "IonSegment"
  key_links:
    - from: "BadgeManagementModal.tsx"
      to: "variables.css"
      via: "Keine app-list-item--selected Klasse mehr"
      pattern: "backgroundColor.*isSelected"
---

<objective>
Badge-Modal-Selection auf backgroundColor-Change umstellen und Badge-Segment korrekt positionieren/stylen.

Purpose: Konsistente Selection-Patterns in der gesamten App (kein border-basiertes Highlighting im Badge-Modal)
Output: Badge-Modal und Badge-Segment sind visuell korrekt
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-badge-ui/49-CONTEXT.md

<interfaces>
<!-- Bestehende Patterns fuer backgroundColor-Change bei Selection -->

From frontend/src/components/admin/modals/MaterialFormModal.tsx (Zeile 388):
```typescript
backgroundColor: isSelected ? 'rgba(217, 119, 6, 0.08)' : undefined
```

From frontend/src/components/admin/modals/ActivityModal.tsx (Zeile 211):
```typescript
backgroundColor: isSelected ? `${typeColor}10` : undefined,
```

<!-- Aktuelles app-list-item--selected Pattern (zu ersetzen): -->
From frontend/src/theme/variables.css:
```css
.app-list-item--info.app-list-item--selected {
  border-color: #007aff;
  background: rgba(0, 122, 255, 0.08);
}
.app-list-item--warning.app-list-item--selected {
  border-color: #ff9500;
  background: rgba(255, 149, 0, 0.08);
}
```

<!-- Aktuelles Segment in BadgesView.tsx (Zeile 288-301): -->
```typescript
<IonSegment
  value={targetRole}
  onIonChange={(e) => onRoleChange(e.detail.value as 'konfi' | 'teamer')}
  style={{ margin: '0 16px 8px', maxWidth: 'calc(100% - 32px)' }}
>
  <IonSegmentButton value="konfi">
    <IonLabel>Konfis</IonLabel>
  </IonSegmentButton>
  <IonSegmentButton value="teamer">
    <IonLabel>Teamer:innen</IonLabel>
  </IonSegmentButton>
</IonSegment>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge-Modal Selection auf backgroundColor-Change umstellen (UI-05)</name>
  <files>frontend/src/components/admin/modals/BadgeManagementModal.tsx</files>
  <read_first>
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx
    - frontend/src/components/admin/modals/MaterialFormModal.tsx (Referenz-Pattern Zeile 388)
  </read_first>
  <action>
An 3 Stellen in BadgeManagementModal.tsx die `app-list-item--selected` Klasse durch backgroundColor inline-Style ersetzen:

1. **Zeile 475** (Activity-Picker bei activity_count):
   - className von `app-list-item app-list-item--info ${isSelected ? 'app-list-item--selected' : ''}`
   - aendern zu `app-list-item app-list-item--info` (selected-Teil entfernen)
   - Im style-Objekt hinzufuegen: `backgroundColor: isSelected ? 'rgba(0, 122, 255, 0.08)' : undefined`
   - Die Farbe rgba(0, 122, 255, 0.08) entspricht dem --info Pattern (blau)

2. **Zeile 543** (Category-Picker bei category_activities):
   - className von `app-list-item app-list-item--warning ${isSelected ? 'app-list-item--selected' : ''}`
   - aendern zu `app-list-item app-list-item--warning` (selected-Teil entfernen)
   - Im style-Objekt hinzufuegen: `backgroundColor: isSelected ? 'rgba(255, 149, 0, 0.08)' : undefined`
   - Die Farbe rgba(255, 149, 0, 0.08) entspricht dem --warning Pattern (orange)

3. **Zeile 650** (Activity-Picker bei collection badge):
   - className von `app-list-item app-list-item--info ${isSelected ? 'app-list-item--selected' : ''}`
   - aendern zu `app-list-item app-list-item--info` (selected-Teil entfernen)
   - Im style-Objekt hinzufuegen: `backgroundColor: isSelected ? 'rgba(0, 122, 255, 0.08)' : undefined`

WICHTIG: Die `app-list-item--selected` Klasse setzt sowohl border-color ALS AUCH background. Durch den Wechsel zu backgroundColor inline-Style wird NUR der Hintergrund geaendert, ohne die Umrandung.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "app-list-item--selected" frontend/src/components/admin/modals/BadgeManagementModal.tsx | grep -q "^0$" && echo "PASS: keine app-list-item--selected mehr" || echo "FAIL: app-list-item--selected noch vorhanden"</automated>
  </verify>
  <acceptance_criteria>
    - grep "app-list-item--selected" BadgeManagementModal.tsx liefert 0 Treffer
    - grep "backgroundColor.*isSelected" BadgeManagementModal.tsx liefert mindestens 3 Treffer
    - grep "rgba(0, 122, 255, 0.08)" BadgeManagementModal.tsx liefert mindestens 2 Treffer (info-Farbe)
    - grep "rgba(255, 149, 0, 0.08)" BadgeManagementModal.tsx liefert mindestens 1 Treffer (warning-Farbe)
  </acceptance_criteria>
  <done>Badge-Modal zeigt bei Auswahl nur Hintergrund-Farbwechsel, keine Umrandung mehr</done>
</task>

<task type="auto">
  <name>Task 2: Badge-Segment Position und Styling pruefen/fixen (UI-06) + UI-07 Verifikation</name>
  <files>frontend/src/components/admin/BadgesView.tsx</files>
  <read_first>
    - frontend/src/components/admin/BadgesView.tsx
    - frontend/src/components/admin/pages/AdminBadgesPage.tsx
  </read_first>
  <action>
In BadgesView.tsx das IonSegment (Zeile 288-301) pruefen:

1. **Position:** Das Segment ist bereits unter dem Header (nach dem CompactHeader). Position ist korrekt — keine Aenderung noetig falls es nach `CompactHeader` kommt.

2. **Styling:** "Teamer:innen" darf bei Auswahl NICHT lila/fett sein. Standard-IonSegment-Styling von Ionic nutzen. Falls custom CSS die Segmente lila/fett macht, dieses ueberschreiben. Pruefen ob `--color-checked` oder aehnliche CSS-Variablen gesetzt sind die das Segment lila machen. Falls ja, auf Standard-Werte zuruecksetzen oder explizit `--color-checked: var(--ion-color-primary)` entfernen.

3. **UI-07 ist bereits durch Phase 52 erfuellt.** Keine Aenderung an TeamerBadgesPage noetig. Nur in der Verifikation bestaetigen, dass TeamerBadgesView existiert und funktioniert.

Falls das Segment bereits korrekt positioniert und gestyled ist (kein lila/fett bei Teamer:innen), dokumentiere dies im Summary als "bereits korrekt, verifiziert".
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "IonSegment" frontend/src/components/admin/BadgesView.tsx && echo "PASS: Segment vorhanden" || echo "FAIL: Segment fehlt"</automated>
  </verify>
  <acceptance_criteria>
    - grep "IonSegment" BadgesView.tsx liefert Treffer
    - grep "Teamer:innen" BadgesView.tsx liefert Treffer
    - Segment steht nach CompactHeader (nicht im Header)
    - TeamerBadgesView.tsx existiert (UI-07 Verifikation)
  </acceptance_criteria>
  <done>Badge-Segment steht unter Header mit normalem Styling, Teamer:innen ist nicht lila/fett. UI-07 verifiziert als erledigt durch Phase 52.</done>
</task>

</tasks>

<verification>
- Badge-Modal: `grep "app-list-item--selected" BadgeManagementModal.tsx` liefert 0 Treffer
- Badge-Modal: `grep "backgroundColor" BadgeManagementModal.tsx` zeigt isSelected-basierte Farbwechsel
- Badge-Segment: IonSegment in BadgesView.tsx nach CompactHeader positioniert
- UI-07: TeamerBadgesView.tsx existiert und ist funktional (Phase 52)
</verification>

<success_criteria>
- UI-05: Badge-Modal nutzt backgroundColor-Change statt app-list-item--selected
- UI-06: Segment korrekt positioniert und gestyled
- UI-07: Bestaetigt als erledigt (Phase 52)
- Frontend baut fehlerfrei: `cd frontend && npx tsc --noEmit` hat keine neuen Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/49-badge-ui/49-01-SUMMARY.md`
</output>
