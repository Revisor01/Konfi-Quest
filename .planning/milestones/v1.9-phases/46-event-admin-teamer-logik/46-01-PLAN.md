---
phase: 46-event-admin-teamer-logik
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/admin/EventsView.tsx
autonomous: true
requirements: [EVT-v19-06, EVT-v19-07]

must_haves:
  truths:
    - "Bei teamer_only Events sind Punkt-Typ, Teilnehmer-Limit, Warteliste und Jahrgangszuordnung ausgeblendet"
    - "Pflicht-Events zeigen rotes shieldCheckmark Icon in der Admin-Event-Liste"
    - "Mitbringen-Events zeigen lila bagHandle Icon in der Admin-Event-Liste und in Event-Details"
  artifacts:
    - path: "frontend/src/components/admin/modals/EventModal.tsx"
      provides: "Conditional field hiding for teamer_only events"
      contains: "teamerAccess === 'teamer_only'"
    - path: "frontend/src/components/admin/EventsView.tsx"
      provides: "Pflicht/Mitbringen color highlights in list"
      contains: "shieldCheckmark"
  key_links:
    - from: "EventModal.tsx"
      to: "teamerAccess state"
      via: "conditional render based on teamerAccess value"
      pattern: "teamerAccess.*teamer_only"
---

<objective>
EventModal Teamer-only Felder ausblenden und Pflicht/Mitbringen Hervorhebung konsistent machen

Purpose: Teamer-only Events brauchen keine Konfi-spezifischen Felder. Pflicht und Mitbringen muessen visuell hervorgehoben sein.
Output: EventModal blendet irrelevante Felder aus, EventsView zeigt konsistente Farbmarkierungen
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-event-admin-teamer-logik/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventModal — Teamer-only Felder conditional ausblenden</name>
  <files>frontend/src/components/admin/modals/EventModal.tsx</files>
  <read_first>
    - frontend/src/components/admin/modals/EventModal.tsx
  </read_first>
  <action>
Im EventModal muessen bei `teamerAccess === 'teamer_only'` folgende Sektionen komplett ausgeblendet werden (conditional render, nicht disabled):

1. **Punkte & Teilnehmer Sektion** (Zeile ~833-990): Die gesamte `{!formData.mandatory && (...)}` Sektion um "Punkte & Teilnehmer" muss zusaetzlich pruefen `&& teamerAccess !== 'teamer_only'`. Die Bedingung wird zu: `{!formData.mandatory && teamerAccess !== 'teamer_only' && (`.

2. **Warteliste Sektion** (Zeile ~1113-1114): Die bestehende Bedingung `{!formData.mandatory && (` muss erweitert werden zu `{!formData.mandatory && teamerAccess !== 'teamer_only' && (`.

3. **Jahrgangszuordnung** innerhalb "Kategorien & Zielgruppe" (Zeile ~1061-1107): Die Jahrgang-Auswahl (IonItem mit "Jahrgaenge (mehrere moeglich)" Label und die darunterliegende Jahrgang-Liste) muss in ein Fragment gewrappt werden mit Bedingung `{teamerAccess !== 'teamer_only' && (<>...</>)}`. Nur den Jahrgang-Teil ausblenden, die Kategorien-Auswahl bleibt sichtbar.

4. **Payload anpassen** (Zeile ~375-398): Bei teamer_only im Submit-Handler die ausgeblendeten Felder auf Defaults setzen:
   - Vor `const payload = {` einfuegen:
   ```typescript
   const isTeamerOnly = teamerAccess === 'teamer_only';
   ```
   - Im payload-Objekt:
     - `point_type: isTeamerOnly ? 'gemeinde' : formData.point_type,`
     - `jahrgang_ids: isTeamerOnly ? [] : formData.jahrgang_ids,`
     - `max_participants: (formData.mandatory || isTeamerOnly) ? 0 : formData.max_participants,`
     - `waitlist_enabled: (formData.mandatory || isTeamerOnly) ? false : formData.waitlist_enabled,`
     - `max_waitlist_size: (formData.mandatory || isTeamerOnly) ? 0 : formData.max_waitlist_size,`

5. **Formvalidierung anpassen** (Zeile ~429): Die Bedingung `(!formData.mandatory || formData.jahrgang_ids.length > 0)` muss bei teamer_only entfallen. Neue Bedingung:
   ```typescript
   const isFormValid = formData.name.trim().length > 0 && formData.event_date && (teamerAccess === 'teamer_only' || !formData.mandatory || formData.jahrgang_ids.length > 0);
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "teamer_only" frontend/src/components/admin/modals/EventModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "teamerAccess !== 'teamer_only'" EventModal.tsx findet mindestens 3 Treffer (Punkte-Sektion, Warteliste-Sektion, Jahrgang-Auswahl)
    - grep "isTeamerOnly" EventModal.tsx findet mindestens 5 Treffer (Variable + payload-Felder)
    - grep "teamerAccess === 'teamer_only' || !formData.mandatory" EventModal.tsx findet 1 Treffer (Formvalidierung)
  </acceptance_criteria>
  <done>Bei teamer_only Events sind Punkt-Typ, Max-Teilnehmer, Warteliste und Jahrgangszuordnung komplett ausgeblendet. Payload sendet sinnvolle Defaults.</done>
</task>

<task type="auto">
  <name>Task 2: EventsView — Pflicht und Mitbringen Hervorhebung konsistent</name>
  <files>frontend/src/components/admin/EventsView.tsx</files>
  <read_first>
    - frontend/src/components/admin/EventsView.tsx
    - frontend/src/components/admin/views/EventDetailView.tsx (Zeile 700-727 als Referenz fuer Details-Darstellung)
  </read_first>
  <action>
Sicherstellen, dass Pflicht und Mitbringen in der Admin-Event-Liste konsistent farbig hervorgehoben sind:

1. **Pflicht-Markierung in Liste** (bereits vorhanden via statusColor/statusText): Pflicht wird ueber statusColor '#dc2626' und statusText 'Pflicht' mit shieldCheckmark Icon angezeigt (Zeile 277, 294, 309). Das ist bereits korrekt implementiert — verifizieren und ggf. sicherstellen.

2. **Mitbringen-Markierung in Liste** (Zeile 438-445): Bereits vorhanden mit bagHandle Icon und Farbe '#8b5cf6'. Korrekt implementiert.

3. **Zusaetzliche Pflicht-Markierung in Event-Listen-Details**: Pflicht wird aktuell nur ueber den Status-Badge angezeigt. Um die Konsistenz mit den Details zu erhoehen, eine zusaetzliche Zeile fuer Pflicht-Events hinzufuegen — analog zur Mitbringen-Zeile. Nach der Mitbringen-Zeile (Zeile ~445) und vor dem Material-Bereich einfuegen:

   ```tsx
   {/* Zeile: Pflicht-Event Markierung */}
   {event.mandatory && !isCancelled && (
     <div className="app-list-item__meta" style={{ marginTop: '4px' }}>
       <span className="app-list-item__meta-item">
         <IonIcon icon={shieldCheckmark} style={{ color: shouldGrayOut ? '#999' : '#dc2626' }} />
         Pflicht-Event
       </span>
     </div>
   )}
   ```

   Sicherstellen dass `shieldCheckmark` bereits importiert ist (sollte es sein, da es fuer statusIcon verwendet wird — Zeile 309). Falls nicht im Import, hinzufuegen.

4. **Verifizieren** dass `shieldCheckmark` und `bagHandle` in den Imports vorhanden sind. Beide sollten bereits importiert sein. Falls `shieldCheckmark` fehlt, zum Import von ionicons/icons hinzufuegen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "shieldCheckmark\|bagHandle" frontend/src/components/admin/EventsView.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "Pflicht-Event" EventsView.tsx findet mindestens 1 Treffer (neue Pflicht-Zeile in Liste)
    - grep "shieldCheckmark" EventsView.tsx findet mindestens 3 Treffer (Import, statusIcon, Liste-Markierung)
    - grep "bagHandle" EventsView.tsx findet mindestens 2 Treffer (Import, Mitbringen-Zeile)
    - grep "#dc2626" EventsView.tsx findet mindestens 2 Treffer (Pflicht-Farbe in Status und Liste)
    - grep "#8b5cf6" EventsView.tsx findet mindestens 1 Treffer (Mitbringen-Farbe)
  </acceptance_criteria>
  <done>Pflicht und Mitbringen sind sowohl in der Event-Liste als auch in Event-Details konsistent farbig hervorgehoben</done>
</task>

</tasks>

<verification>
- EventModal blendet bei teamer_only die Sektionen Punkte, Warteliste und Jahrgang aus
- EventModal Submit sendet sinnvolle Defaults bei teamer_only
- EventsView zeigt Pflicht-Events mit rotem shieldCheckmark Icon in der Liste
- EventsView zeigt Mitbringen-Events mit lila bagHandle Icon in der Liste
- Frontend kompiliert ohne Fehler: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Teamer-only Events: 4 Feldbereiche sind ausgeblendet (Punkte, Teilnehmer-Limit, Warteliste, Jahrgang)
- Pflicht- und Mitbringen-Markierungen in Admin-Event-Liste sichtbar
- Keine TypeScript-Kompilierungsfehler
</success_criteria>

<output>
After completion, create `.planning/phases/46-event-admin-teamer-logik/46-01-SUMMARY.md`
</output>
