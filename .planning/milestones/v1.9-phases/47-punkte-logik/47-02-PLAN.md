---
phase: 47-punkte-logik
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/modals/PointsHistoryModal.tsx
  - frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
autonomous: true
requirements: [PKT-v19-04]

must_haves:
  truths:
    - "Punkte-History Header zeigt 6 Stats in 3x2 Grid"
    - "Bei nur einem aktiven Typ entfaellt Gesamt-Stat"
    - "Aktivitaeten-Count wird aus der History berechnet"
    - "Teamer-Konfi-History bekommt denselben 3x2 Header"
  artifacts:
    - path: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      provides: "3x2 Stats Grid im History-Header"
      contains: "AKTIVITAETEN"
    - path: "frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx"
      provides: "pointConfig mit echten enabled-Flags"
      contains: "pointConfig"
  key_links:
    - from: "PointsHistoryModal.tsx"
      to: "filteredHistory"
      via: "activityCount Berechnung aus source_type"
      pattern: "source_type.*activity"
    - from: "TeamerKonfiStatsPage.tsx"
      to: "PointsHistoryModal"
      via: "pointConfig Prop"
      pattern: "pointConfig"
---

<objective>
Punkte-History Header auf 3x2 Grid Layout umbauen mit 6 Stats (Gesamt, Gottesdienst, Gemeinde, Events, Aktivitaeten, Bonus). Auch fuer Teamer-Konfi-History anwenden.

Purpose: Bessere Uebersicht ueber alle Punkte-Quellen im History-Header, konsistent zwischen Konfi und Teamer.
Output: 3x2 Grid Stats in PointsHistoryModal, Teamer-History mit korrekten pointConfig-Flags.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-punkte-logik/47-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/components/konfi/modals/PointsHistoryModal.tsx:
```typescript
interface PointsHistoryModalProps {
  onClose: () => void;
  pointConfig?: { gottesdienst_enabled: boolean; gemeinde_enabled: boolean; };
  apiEndpoint?: string;
}
```
Aktueller Header (Zeile 203-238):
- Erste Reihe: Gesamt (wenn beide Typen), Gottesdienst, Gemeinde
- Zweite Reihe: Events, Bonus
- FEHLT: Aktivitaeten-Count

From frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx (Zeile 222-226):
```typescript
const [presentPointsModal, dismissPointsModal] = useIonModal(PointsHistoryModal, {
  onClose: () => dismissPointsModal(),
  pointConfig: { gottesdienst_enabled: true, gemeinde_enabled: true },  // HARDCODED!
  apiEndpoint: '/teamer/konfi-history'
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: History-Header 3x2 Grid + Aktivitaeten-Count</name>
  <files>frontend/src/components/konfi/modals/PointsHistoryModal.tsx</files>
  <read_first>
    frontend/src/components/konfi/modals/PointsHistoryModal.tsx,
    frontend/src/components/shared/SectionHeader.tsx
  </read_first>
  <action>
**PointsHistoryModal.tsx - Aktivitaeten-Count berechnen:**

1. Nach der bestehenden eventCount/bonusCount Berechnung (Zeile 158-159) eine activityCount Variable ergaenzen:
   ```typescript
   const activityCount = filteredHistory.filter(h => h.source_type === 'activity').length;
   ```

**PointsHistoryModal.tsx - Stats Header auf 3x2 Grid umbauen (Zeile 203-238):**

2. Die bestehenden zwei Reihen (Erste Reihe: Punkte-Typen, Zweite Reihe: Events+Bonus) ersetzen durch ein einheitliches 3x2 Grid:

   **Reihe 1** (Punkte-Typen):
   - Gesamt: `filteredTotals.total` — NUR anzeigen wenn `showBothTypes` true
   - Gottesdienst: `filteredTotals.gottesdienst` — NUR wenn `gottesdienstEnabled`
   - Gemeinde: `filteredTotals.gemeinde` — NUR wenn `gemeindeEnabled`

   **Reihe 2** (Quellen):
   - Events: `eventCount` — Label "EVENTS"
   - Aktivitaeten: `activityCount` — Label "AKTIVITAETEN"
   - Bonus: `bonusCount` — Label "BONUS"

   Layout: CSS Grid mit `display: grid`, `grid-template-columns: repeat(3, 1fr)`, `gap: 8px`.
   Bei nur einem aktiven Typ: Reihe 1 zeigt nur den aktiven Typ (1 Stat statt 3), zentriert. Kein "Gesamt" weil redundant.

   Konkreter Code fuer den Stats-Bereich:
   ```tsx
   <div style={{ position: 'relative', zIndex: 1 }}>
     {/* Reihe 1: Punkte-Typ-Stats */}
     <div style={{ display: 'grid', gridTemplateColumns: showBothTypes ? 'repeat(3, 1fr)' : '1fr', gap: '8px' }}>
       {showBothTypes && (
         <div className="app-detail-header__info-chip" style={{ textAlign: 'center', padding: '10px 8px' }}>
           <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{filteredTotals.total}</div>
           <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>GESAMT</div>
         </div>
       )}
       {gottesdienstEnabled && (
         <div className="app-detail-header__info-chip" style={{ textAlign: 'center', padding: '10px 8px' }}>
           <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{filteredTotals.gottesdienst}</div>
           <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>GOTTESDIENST</div>
         </div>
       )}
       {gemeindeEnabled && (
         <div className="app-detail-header__info-chip" style={{ textAlign: 'center', padding: '10px 8px' }}>
           <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{filteredTotals.gemeinde}</div>
           <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>GEMEINDE</div>
         </div>
       )}
     </div>
     {/* Reihe 2: Quellen-Stats (immer 3 Spalten) */}
     <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '8px', marginTop: '8px' }}>
       {[
         { value: eventCount, label: 'EVENTS' },
         { value: activityCount, label: 'AKTIVITAETEN' },
         { value: bonusCount, label: 'BONUS' }
       ].map((stat) => (
         <div key={stat.label} className="app-detail-header__info-chip" style={{ textAlign: 'center', padding: '10px 8px' }}>
           <div style={{ fontSize: '1.2rem', fontWeight: '800', color: 'white' }}>{stat.value}</div>
           <div style={{ fontSize: '0.6rem', color: 'rgba(255, 255, 255, 0.85)', fontWeight: '600', letterSpacing: '0.3px' }}>{stat.label}</div>
         </div>
       ))}
     </div>
   </div>
   ```

   Dies ersetzt den gesamten Block von `{/* Stats Boxen */}` (Zeile 203) bis zum schliessenden `</div>` (Zeile 238). Die bestehenden `app-detail-header__info-row` Wrapper werden durch das CSS Grid ersetzt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PointsHistoryModal.tsx enthaelt `activityCount` Variable mit `source_type === 'activity'` Filter
    - PointsHistoryModal.tsx enthaelt "AKTIVITAETEN" als Label im Stats-Grid
    - PointsHistoryModal.tsx nutzt CSS Grid (`grid-template-columns: repeat(3, 1fr)`) fuer die Quellen-Reihe
    - Stats-Header hat zwei Reihen: Punkte-Typen oben, Quellen unten
  </acceptance_criteria>
  <done>History-Header zeigt 3x2 Grid mit Gesamt/Godi/Gemeinde oben und Events/Aktivitaeten/Bonus unten. Bei nur einem Typ: Kein Gesamt, nur aktiver Typ.</done>
</task>

<task type="auto">
  <name>Task 2: Teamer-Konfi-History mit korrektem pointConfig</name>
  <files>frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx</files>
  <read_first>
    frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx,
    frontend/src/components/konfi/modals/PointsHistoryModal.tsx
  </read_first>
  <action>
**TeamerKonfiStatsPage.tsx - pointConfig dynamisch statt hardcoded:**

1. Aktuell (Zeile 222-226) ist `pointConfig: { gottesdienst_enabled: true, gemeinde_enabled: true }` hardcoded. Das muss dynamisch aus den Konfi-Daten kommen.

2. Die API `/teamer/profile` liefert `konfi_data` — pruefen ob dort `gottesdienst_enabled`/`gemeinde_enabled` enthalten sind. Falls nicht, ist der Fallback `true` korrekt (Teamer haben eingefrorene Konfi-Daten, daher beide Typen anzeigen). In dem Fall den Kommentar aktualisieren:
   ```typescript
   // Teamer-Konfi-Daten sind eingefroren — beide Typen immer sichtbar
   pointConfig: { gottesdienst_enabled: true, gemeinde_enabled: true },
   ```

3. Falls die API die enabled-Flags liefert, diese nutzen:
   ```typescript
   pointConfig: {
     gottesdienst_enabled: konfiData?.gottesdienst_enabled !== false,
     gemeinde_enabled: konfiData?.gemeinde_enabled !== false
   },
   ```

Da Teamer-Konfi-Daten eingefroren sind (v1.8 ROL-03), ist der Fallback `true` korrekt. Hauptsache der Kommentar dokumentiert warum und die PointsHistoryModal bekommt das Prop korrekt, damit der neue 3x2 Header auch in der Teamer-Ansicht funktioniert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TeamerKonfiStatsPage.tsx enthaelt einen Kommentar der erklaert warum beide Typen true sind
    - TeamerKonfiStatsPage.tsx uebergibt `pointConfig` an PointsHistoryModal
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Teamer-Konfi-History nutzt den neuen 3x2 Header aus PointsHistoryModal korrekt mit dokumentiertem pointConfig.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` kompiliert ohne Fehler
- PointsHistoryModal zeigt 6 Stats in 3x2 Grid (Gesamt/Godi/Gemeinde + Events/Aktivitaeten/Bonus)
- Bei nur einem Typ: Gesamt entfaellt, nur aktiver Typ in Reihe 1
- TeamerKonfiStatsPage nutzt PointsHistoryModal mit dokumentiertem pointConfig
</verification>

<success_criteria>
1. Punkte-History Header zeigt 6 Stats in 3x2 Grid Layout
2. Aktivitaeten-Count wird korrekt aus History berechnet und angezeigt
3. Bei nur einem aktiven Typ: Kein redundanter Gesamt-Stat
4. Teamer-Konfi-History bekommt denselben neuen Header
</success_criteria>

<output>
After completion, create `.planning/phases/47-punkte-logik/47-02-SUMMARY.md`
</output>
