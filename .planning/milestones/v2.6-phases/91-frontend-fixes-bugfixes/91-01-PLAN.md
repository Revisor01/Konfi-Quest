---
phase: 91-frontend-fixes-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/contexts/LiveUpdateContext.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/admin/views/EventDetailSections.tsx
  - backend/routes/konfi.js
  - backend/routes/badges.js
autonomous: true
requirements: [ARCH-04, ARCH-05, BUG-01, BUG-02]

must_haves:
  truths:
    - "LiveUpdateContext listeners Map lebt im Provider (useRef), nicht im Module-Scope"
    - "Event-Detail Chat-Navigation nutzt useIonRouter statt window.location.href"
    - "Event-Serie-Navigation nutzt useIonRouter statt window.location.href"
    - "Badge-Progress fuer streak-Kriterien zeigt den tatsaechlichen Fortschritt"
    - "Badge-Progress fuer time_based-Kriterien zeigt den tatsaechlichen Fortschritt"
  artifacts:
    - path: "frontend/src/contexts/LiveUpdateContext.tsx"
      provides: "LiveUpdate Provider mit useRef listeners"
      contains: "useRef"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "Chat-Navigation via useIonRouter"
      contains: "useIonRouter"
    - path: "frontend/src/components/admin/views/EventDetailSections.tsx"
      provides: "Serie-Navigation via Callback statt window.location"
    - path: "backend/routes/konfi.js"
      provides: "streak + time_based Progress-Berechnung"
  key_links:
    - from: "frontend/src/components/admin/views/EventDetailView.tsx"
      to: "/admin/chat/:roomId"
      via: "router.push"
      pattern: "router\\.push.*chat"
    - from: "frontend/src/components/admin/views/EventDetailSections.tsx"
      to: "EventDetailView"
      via: "onNavigate callback prop"
      pattern: "onNavigate"
    - from: "backend/routes/konfi.js"
      to: "backend/routes/badges.js"
      via: "checkStreakCriteria + checkTimeBasedProgress"
      pattern: "checkStreakCriteria|checkTimeBasedProgress"
---

<objective>
Frontend-Architektur-Fixes und Bugfixes: LiveUpdateContext Module-Scope-Leak beseitigen, window.location.href Navigation durch useIonRouter ersetzen (Chat + Serie), Badge-Progress fuer streak/time_based Kriterien implementieren.

Purpose: Schwarze Seiten bei Navigation verhindern, korrekten Badge-Fortschritt anzeigen, Shared-State-Bug zwischen Provider-Mounts beseitigen.
Output: 5 geaenderte Dateien (3 Frontend, 2 Backend), alle 4 Requirements (ARCH-04, ARCH-05, BUG-01, BUG-02) erledigt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-frontend-fixes-bugfixes/91-CONTEXT.md

@frontend/src/contexts/LiveUpdateContext.tsx
@frontend/src/components/admin/views/EventDetailView.tsx
@frontend/src/components/admin/views/EventDetailSections.tsx
@backend/routes/konfi.js
@backend/routes/badges.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveUpdateContext listeners in useRef verschieben (ARCH-04)</name>
  <files>frontend/src/contexts/LiveUpdateContext.tsx</files>
  <read_first>frontend/src/contexts/LiveUpdateContext.tsx</read_first>
  <action>
Die listeners Map auf Zeile 40 ist im Module-Scope definiert:
```
const listeners: Map<LiveUpdateType, Set<(event: LiveUpdateEvent) => void>> = new Map();
```

Das verursacht Shared-State zwischen Provider-Mounts. Fix (per ARCH-04):

1. Die Module-Scope-Variable `const listeners: Map<...> = new Map();` (Zeile 40) ENTFERNEN.

2. Im `LiveUpdateProvider` einen useRef einfuegen:
```typescript
const listenersRef = useRef<Map<LiveUpdateType, Set<(event: LiveUpdateEvent) => void>>>(new Map());
```

3. Alle Zugriffe auf `listeners` innerhalb des Providers durch `listenersRef.current` ersetzen:
   - In `handleLiveUpdate`: `listenersRef.current.get(event.type)` statt `listeners.get(event.type)`
   - In `subscribe`: `listenersRef.current.has(type)`, `listenersRef.current.set(type, new Set())`, `listenersRef.current.get(type)!.add(callback)`, `listenersRef.current.get(type)?.delete(callback)`
   - In `triggerRefresh`: `listenersRef.current.get(type)` statt `listeners.get(type)`

4. `useRef` zum React-Import hinzufuegen (falls nicht vorhanden).

WICHTIG: Die subscribe/triggerRefresh useCallback-Deps bleiben leer `[]` -- useRef aendert sich nicht zwischen Renders.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "useRef" frontend/src/contexts/LiveUpdateContext.tsx && grep -c "listenersRef.current" frontend/src/contexts/LiveUpdateContext.tsx && ! grep -n "^const listeners:" frontend/src/contexts/LiveUpdateContext.tsx && cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "useRef" LiveUpdateContext.tsx findet mindestens 1 Treffer
    - grep "listenersRef.current" LiveUpdateContext.tsx findet mindestens 5 Treffer (subscribe, triggerRefresh, handleLiveUpdate)
    - grep "^const listeners:" LiveUpdateContext.tsx findet 0 Treffer (Module-Scope entfernt)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>listeners Map lebt in useRef innerhalb des Providers, kein Module-Scope Shared-State mehr</done>
</task>

<task type="auto">
  <name>Task 2: window.location.href durch useIonRouter ersetzen (ARCH-05 + BUG-01)</name>
  <files>frontend/src/components/admin/views/EventDetailView.tsx, frontend/src/components/admin/views/EventDetailSections.tsx</files>
  <read_first>frontend/src/components/admin/views/EventDetailView.tsx, frontend/src/components/admin/views/EventDetailSections.tsx</read_first>
  <action>
Zwei Stellen verwenden window.location.href, was Full-Reload verursacht und Ionic App-State zerstoert:

**A) EventDetailView.tsx -- Chat-Navigation (BUG-01 + ARCH-05):**

1. `useIonRouter` importieren: `import { ..., useIonRouter } from '@ionic/react';`
2. Im Komponenten-Body: `const router = useIonRouter();`
3. Zeile 396-398 die `handleNavigateToChat` Funktion aendern von:
```typescript
const handleNavigateToChat = () => {
  window.location.href = `/admin/chat/${eventData?.chat_room_id}`;
};
```
zu:
```typescript
const handleNavigateToChat = () => {
  router.push(`/admin/chat/${eventData?.chat_room_id}`, 'forward');
};
```

**B) EventDetailSections.tsx -- Serie-Navigation (ARCH-05):**

EventDetailSections ist eine React.memo Komponente ohne Hook-Zugang. Loesung: onNavigate Callback-Prop einfuehren.

1. Im `SeriesEventsSectionProps` Interface ein neues Prop hinzufuegen:
```typescript
onNavigate: (eventId: number) => void;
```

2. In der `SeriesEventsSection` Komponente den onClick aendern von:
```typescript
onClick={() => window.location.href = `/admin/events/${seriesEvent.id}`}
```
zu:
```typescript
onClick={() => onNavigate(seriesEvent.id)}
```

3. In EventDetailView.tsx wo `<SeriesEventsSection>` gerendert wird (ca. Zeile 588), das neue Prop durchreichen:
```typescript
<SeriesEventsSection
  seriesEvents={eventData.series_events as any}
  formatDate={formatDate}
  formatTime={formatTime}
  onNavigate={(eventId) => router.push(`/admin/events/${eventId}`, 'forward')}
/>
```

KEIN window.location.href darf in diesen Dateien verbleiben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && ! grep -n "window.location.href" frontend/src/components/admin/views/EventDetailView.tsx && ! grep -n "window.location.href" frontend/src/components/admin/views/EventDetailSections.tsx && grep -n "useIonRouter" frontend/src/components/admin/views/EventDetailView.tsx && grep -n "onNavigate" frontend/src/components/admin/views/EventDetailSections.tsx && cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "window.location.href" EventDetailView.tsx findet 0 Treffer
    - grep "window.location.href" EventDetailSections.tsx findet 0 Treffer
    - grep "useIonRouter" EventDetailView.tsx findet mindestens 1 Treffer
    - grep "onNavigate" EventDetailSections.tsx findet mindestens 2 Treffer (Props + Usage)
    - grep "router.push" EventDetailView.tsx findet mindestens 2 Treffer (Chat + Serie)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>Keine window.location.href mehr in Event-Views, Chat-Navigation und Serie-Navigation nutzen useIonRouter, kein schwarzer Bildschirm mehr</done>
</task>

<task type="auto">
  <name>Task 3: Badge-Progress fuer streak und time_based implementieren (BUG-02)</name>
  <files>backend/routes/konfi.js, backend/routes/badges.js</files>
  <read_first>backend/routes/konfi.js (Zeilen 1040-1070), backend/routes/badges.js (Zeilen 546-591, 218-237)</read_first>
  <action>
In konfi.js (Badge-Progress-Endpoint) stehen bei streak und time_based nur TODOs mit `progress.current = 0`. Die Logik existiert bereits in badges.js.

**A) badges.js -- Zwei Helper exportieren:**

1. Die bestehende `checkStreakCriteria` Funktion (Zeilen 546-591) ist bereits standalone. Zusaetzlich eine neue Funktion `getStreakCount` erstellen, die den aktuellen Streak-Wert zurueckgibt (statt nur boolean). Die Logik ist identisch zu `checkStreakCriteria`, aber statt `return currentStreak >= criteriaValue` wird `return currentStreak` zurueckgegeben:

```javascript
async function getStreakCount(db, userId, organizationId) {
  const streakQuery = `
    SELECT completed_date as date FROM user_activities WHERE user_id = $1 AND organization_id = $2
    UNION ALL
    SELECT e.event_date as date FROM event_bookings eb
    JOIN events e ON eb.event_id = e.id
    WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND eb.organization_id = $2
    ORDER BY date DESC
  `;
  const { rows: streakResults } = await db.query(streakQuery, [userId, organizationId]);

  function getYearWeek(date) {
    const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
    const dayNum = d.getUTCDay() || 7;
    d.setUTCDate(d.getUTCDate() + 4 - dayNum);
    const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
    const weekNo = Math.ceil((((d - yearStart) / 86400000) + 1) / 7);
    return `${d.getUTCFullYear()}-W${weekNo.toString().padStart(2, '0')}`;
  }

  const activityWeeks = new Set(streakResults.map(r => getYearWeek(new Date(r.date))).filter(week => week && !week.includes('NaN')));
  const sortedWeeks = Array.from(activityWeeks).sort().reverse();

  let currentStreak = 0;
  if (sortedWeeks.length > 0) {
    currentStreak = 1;
    for (let i = 0; i < sortedWeeks.length - 1; i++) {
      const thisWeek = sortedWeeks[i];
      const nextWeek = sortedWeeks[i + 1];
      const [year, week] = thisWeek.split('-W').map(Number);
      let expectedYear = year;
      let expectedWeek = week - 1;
      if (expectedWeek === 0) {
        expectedYear -= 1;
        expectedWeek = getISOWeeksInYear(expectedYear);
      }
      const expectedWeekStr = `${expectedYear}-W${expectedWeek.toString().padStart(2, '0')}`;
      if (nextWeek === expectedWeekStr) {
        currentStreak++;
      } else {
        break;
      }
    }
  }
  return currentStreak;
}
```

2. Neue Funktion `getTimeBasedCount` erstellen:

```javascript
async function getTimeBasedCount(db, userId, organizationId, criteria) {
  const days = criteria.days || (criteria.weeks ? criteria.weeks * 7 : null);
  if (!days) return 0;
  const timeBasedQuery = `
    SELECT completed_date as date FROM user_activities WHERE user_id = $1 AND organization_id = $2
    UNION ALL
    SELECT e.event_date as date FROM event_bookings eb
    JOIN events e ON eb.event_id = e.id
    WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND eb.organization_id = $2
    ORDER BY date DESC
  `;
  const { rows: results } = await db.query(timeBasedQuery, [userId, organizationId]);
  const now = new Date();
  const cutoff = new Date(now.getTime() - (days * 24 * 60 * 60 * 1000));
  return results.filter(r => new Date(r.date) >= cutoff).length;
}
```

3. Beide Funktionen am Ende der Datei (VOR dem `module.exports`) platzieren. Dann im `module.exports` Block exportieren -- dazu am Ende der Datei nach dem `return router;` und vor der schliessenden Klammer von module.exports hinzufuegen:

Suche das Pattern wie badges.js seine Funktionen exportiert. Die Datei nutzt `module.exports = (db, rbacVerifier, ...) => { ... return router; };`. Die Helper muessen AUSSERHALB des module.exports stehen (sie sind schon dort -- `checkStreakCriteria` steht vor `module.exports`). Die neuen Funktionen `getStreakCount` und `getTimeBasedCount` ebenfalls VOR `module.exports` platzieren.

4. Am Ende der Datei (nach `module.exports = ...`) die Helper separat exportieren:
```javascript
module.exports.getStreakCount = getStreakCount;
module.exports.getTimeBasedCount = getTimeBasedCount;
```

ACHTUNG: Das geht nicht wenn module.exports eine Funktion ist. Stattdessen: In konfi.js direkt die Funktionen aus badges.js requiren. Da badges.js `module.exports = function(db, ...)` ist, koennen wir die Helper nicht direkt exportieren.

ALTERNATIVE LOESUNG (besser): Die Helper-Funktionen in eine neue Datei `backend/routes/helpers/badgeProgress.js` extrahieren und sowohl aus badges.js als auch aus konfi.js importieren.

NOCH BESSERE LOESUNG (minimal-invasiv): Die Logik direkt in konfi.js implementieren, da die Queries fuer den Progress-Kontext leicht anders sind (kein target_role Filter noetig, Konfi-Kontext). Einfach die TODO-Bloecke in konfi.js ersetzen:

**B) konfi.js -- streak und time_based Progress implementieren:**

Zeile 1055-1058 ersetzen (case 'streak'):
```javascript
case 'streak': {
  // Streak: Wochen mit mindestens einer Aktivitaet/Event in Folge
  const streakQuery = `
    SELECT completed_date as date FROM user_activities WHERE user_id = $1 AND organization_id = $2
    UNION ALL
    SELECT e.event_date as date FROM event_bookings eb
    JOIN events e ON eb.event_id = e.id
    WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND eb.organization_id = $2
    ORDER BY date DESC
  `;
  const { rows: streakResults } = await db.query(streakQuery, [konfiId, req.user.organization_id]);

  function getYearWeek(date) {
    const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
    const dayNum = d.getUTCDay() || 7;
    d.setUTCDate(d.getUTCDate() + 4 - dayNum);
    const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1));
    const weekNo = Math.ceil((((d - yearStart) / 86400000) + 1) / 7);
    return `${d.getUTCFullYear()}-W${weekNo.toString().padStart(2, '0')}`;
  }

  const activityWeeks = new Set(streakResults.map(r => getYearWeek(new Date(r.date))).filter(w => w && !w.includes('NaN')));
  const sortedWeeks = Array.from(activityWeeks).sort().reverse();

  let currentStreak = 0;
  if (sortedWeeks.length > 0) {
    currentStreak = 1;
    for (let i = 0; i < sortedWeeks.length - 1; i++) {
      const [year, week] = sortedWeeks[i].split('-W').map(Number);
      let ey = year, ew = week - 1;
      if (ew === 0) { ey -= 1; const dec28 = new Date(Date.UTC(ey, 11, 28)); const doy = Math.ceil((dec28 - new Date(Date.UTC(ey, 0, 1))) / 86400000) + 1; ew = Math.ceil((doy - (dec28.getUTCDay() || 7) + 10) / 7); }
      if (sortedWeeks[i + 1] === `${ey}-W${ew.toString().padStart(2, '0')}`) { currentStreak++; } else { break; }
    }
  }
  progress.current = currentStreak;
  break;
}
```

Zeile 1060-1063 ersetzen (case 'time_based'):
```javascript
case 'time_based': {
  // Time-based: Anzahl Aktivitaeten/Events im konfigurierten Zeitraum
  const criteria = badge.criteria_details || {};
  const days = criteria.days || (criteria.weeks ? criteria.weeks * 7 : null);
  if (days) {
    const tbQuery = `
      SELECT completed_date as date FROM user_activities WHERE user_id = $1 AND organization_id = $2
      UNION ALL
      SELECT e.event_date as date FROM event_bookings eb
      JOIN events e ON eb.event_id = e.id
      WHERE eb.user_id = $1 AND eb.attendance_status = 'present' AND eb.organization_id = $2
      ORDER BY date DESC
    `;
    const { rows: tbResults } = await db.query(tbQuery, [konfiId, req.user.organization_id]);
    const now = new Date();
    const cutoff = new Date(now.getTime() - (days * 24 * 60 * 60 * 1000));
    progress.current = tbResults.filter(r => new Date(r.date) >= cutoff).length;
  }
  break;
}
```

WICHTIG: Sicherstellen dass `badge.criteria_details` in der Query oben auch fuer die Progress-Berechnung mitgeladen wird. Pruefen ob das Badge-Query im Progress-Endpoint `criteria_details` selektiert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -A2 "case 'streak'" backend/routes/konfi.js | head -6 && grep -A2 "case 'time_based'" backend/routes/konfi.js | head -6 && ! grep "TODO.*streak\|TODO.*time" backend/routes/konfi.js && node -c backend/routes/konfi.js && node -c backend/routes/badges.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "TODO" konfi.js fuer streak/time_based findet 0 Treffer
    - case 'streak' Block enthaelt streakQuery und currentStreak Berechnung
    - case 'time_based' Block enthaelt cutoff und tbResults.filter
    - node -c konfi.js und node -c badges.js sind syntaktisch korrekt
    - progress.current wird in beiden Cases gesetzt (nicht mehr hardcoded 0)
  </acceptance_criteria>
  <done>Badge-Progress fuer streak zeigt aktuelle Wochen-Streak an, time_based zeigt Aktivitaeten im Zeitfenster, kein 0% mehr bei vorhandenem Fortschritt</done>
</task>

</tasks>

<verification>
1. Frontend TypeScript kompiliert: `cd frontend && npx tsc --noEmit`
2. Backend Syntax: `node -c backend/routes/konfi.js && node -c backend/routes/badges.js`
3. Kein window.location.href in Event-Views: `! grep -r "window.location.href" frontend/src/components/admin/views/EventDetail*.tsx`
4. Kein Module-Scope listeners: `! grep "^const listeners:" frontend/src/contexts/LiveUpdateContext.tsx`
5. Kein TODO fuer streak/time_based: `! grep "TODO.*streak\|TODO.*time_based" backend/routes/konfi.js`
</verification>

<success_criteria>
- LiveUpdateContext listeners Map lebt in useRef, kein Module-Scope Shared-State
- Event-Detail Chat-Navigation und Serie-Navigation nutzen useIonRouter (kein schwarzer Bildschirm)
- Badge-Progress zeigt korrekten Fortschritt fuer streak- und time_based-Kriterien
- Frontend kompiliert fehlerfrei, Backend-Syntax korrekt
</success_criteria>

<output>
After completion, create `.planning/phases/91-frontend-fixes-bugfixes/91-01-SUMMARY.md`
</output>
