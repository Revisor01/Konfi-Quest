---
phase: 11-dokumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "CLAUDE.md enthaelt KEINE 'PostgreSQL Migration Status' Sektion mehr"
    - "CLAUDE.md enthaelt KEINE 'NOCH NICHT MIGRIERT' oder 'NAECHSTE' Eintraege"
    - "CLAUDE.md enthaelt KEINE 'MIGRATION VORGEHEN' Liste"
    - "CLAUDE.md enthaelt KEINE historischen Problem/Loesung Details"
    - "CLAUDE.md enthaelt KEINE spezifischen Daten wie 'Juli 2025'"
    - "CLAUDE.md enthaelt KEINE SQLite/backup_sqlite Referenzen"
    - "CLAUDE.md enthaelt KEINEN unvollstaendigen Database-Befehl"
    - "Systemarchitektur-Sektion enthaelt Hinweis dass alle 15 Routes auf PostgreSQL migriert sind"
    - "Alle kritischen Regeln (Emojis, Umlaute, RBAC, Modal-Pattern) sind weiterhin vorhanden"
    - "Entwicklungs- und Deployment-Kommandos sind korrekt und vollstaendig"
    - "DB-Schema (Kern-Tabellen) ist weiterhin vorhanden"
    - "Datei ist kompakt und enthaelt nur aktuell relevante Informationen"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Komplett neu geschriebene, aktuelle Projektdokumentation fuer Claude Code"
  key_links: []
---

<objective>
CLAUDE.md komplett neu schreiben, damit sie den tatsaechlichen Projektstatus korrekt widerspiegelt und Claude bei der taeglichen Arbeit maximal hilft. Veraltete Migrations-Details, geloeste Bugs, alte TODO-Listen und historische Daten entfernen. Nur aktuell relevante Informationen behalten.

Purpose: Claude Code bekommt bei jedem Gespraech eine praezise, kompakte und aktuelle Instruktionsdatei statt eines veralteten historischen Dokuments.
Output: Neu geschriebene CLAUDE.md ohne veraltete Inhalte, mit korrektem PostgreSQL-Status.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dokumentation/11-CONTEXT.md

<interfaces>
<!-- Aktuelle CLAUDE.md (wird komplett ersetzt): -->
Datei: CLAUDE.md (202 Zeilen, wird komplett neu geschrieben)

<!-- Bestehende 15 Backend-Routes (alle auf PostgreSQL): -->
Backend Routes (alle in /backend/routes/):
activities.js, auth.js, badges.js, categories.js, chat.js, events.js,
jahrgaenge.js, konfi-managment.js, konfi.js, levels.js, notifications.js,
organizations.js, roles.js, settings.js, users.js

<!-- Ergaenzende Dokumentation die NICHT dupliziert werden soll: -->
- MEMORY.md: Design-System Referenz, bekannte Probleme, Patterns
- PROJECT.md: Key Decisions, Milestone-History, GSD Planung
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md komplett neu schreiben</name>
  <files>
CLAUDE.md
  </files>
  <action>
**Die aktuelle CLAUDE.md lesen und dann KOMPLETT durch eine neue Version ersetzen.**

Die neue CLAUDE.md soll folgende Struktur haben (User-Entscheidungen aus 11-CONTEXT.md sind verbindlich):

**1. Kritische Regeln fuer Claude Code (ganz oben, MUSS bleiben)**
Alle 4 Regeln aus der aktuellen Datei uebernehmen:
- Neue RBAC-Struktur verwenden
- Deutsche Entwicklungssprache
- KEINE UNICODE EMOJIS (mit Beispielen was erlaubt/verboten ist)
- ECHTE UMLAUTE VERWENDEN (mit Beispielen)
Die Formulierung und Beispiele koennen 1:1 uebernommen werden -- diese Regeln sind bewaehrt.

**2. Systemarchitektur (OHNE Datumsangaben)**
Backend-Sektion:
- Node.js Express mit RBAC System
- PostgreSQL (Docker Container) -- **NEU: "Alle 15 Routes vollstaendig auf PostgreSQL migriert" ergaenzen**
- JWT mit verifyTokenRBAC middleware
- Port 5000 (Docker: 8623)
- API Base: https://konfi-points.de/api
- Routes Directory: backend/routes/ (OHNE absoluten lokalen Pfad)
- **ENTFERNEN: Backup Directory Zeile (backup_sqlite Referenz)**

Frontend-Sektion:
- React 19 + Ionic 8 + TypeScript
- Vite 5.2
- React Context (AppContext)
- Dev Port 5173

Ueberschrift: "Systemarchitektur" (OHNE "Juli 2025" oder "Aktuelle")

**3. RBAC Datenbankstruktur**
Kern-Tabellen SQL-Block uebernehmen -- identisch zur aktuellen Version.
Das ist aktiv genutztes Referenzmaterial fuer Route-Arbeit.
Ueberschrift: "RBAC Datenbankstruktur" (OHNE "Neue" -- ist nicht mehr neu)

**4. Patterns und Constraints**
Modal-Pattern (useIonModal) mit Code-Beispiel uebernehmen -- identisch zur aktuellen Version.
Das verhindert aktiv einen haeufigen Fehler.

**5. Entwicklungskommandos (korrigiert)**
- Backend starten: cd backend && npm start
- Frontend development: cd frontend && npm run dev
- Deployment: ssh + git pull + docker-compose (wie aktuell)
- Datenbankzugriff: Den KORREKTEN Befehl verwenden:
  `ssh root@server.godsapp.de "docker exec -it konfi-quest-db-1 psql -U konfi_user -d konfi_db"`
  (Dieser stand bisher nur in der "WICHTIGE ERKENNTNISSE" Sektion, nicht im Kommandos-Block)
- **ENTFERNEN: Den kaputten/unvollstaendigen Database-Befehl (Zeile 108 der alten Version)**

**6. System Status (aktualisiert)**
Statt "Juli 2025" und veralteten Status:
- Funktioniert: RBAC System, alle PostgreSQL Routes, Chat mit organization_id Filterung, Modal System mit useIonModal, Punkte-Anzeige, Badge Counts, Activity/Bonus CRUD
- **KEIN "IN ARBEIT" Block** (nichts ist aktuell in Arbeit aus Sicht des Dokuments)
- **KEIN "DEPRECATED" Block** -- die Constraints oben (RBAC verwenden, useIonModal) decken das ab
- Die Sicherheitsluecke-Behoben Info als normalen Eintrag unter "Funktioniert" integrieren

**KOMPLETT ENTFERNEN (diese Sektionen existieren in der neuen Version NICHT):**
- Gesamte "PostgreSQL Migration Status" Sektion (Zeilen 112-173)
- "BEREITS MIGRIERT UND GETESTET" mit allen Detail-Eintraegen
- "NOCH NICHT MIGRIERT" Liste
- "MIGRATION VORGEHEN" Liste
- "WICHTIGE ERKENNTNISSE" Sektion
- Alle historischen Problem/Loesung Details (Poll-Voting 404, fehlende Routes etc.)
- Alle "Juli 2025" Datumsangaben
- backup_sqlite Referenzen
- Alte SQLite Referenzen

**Stil-Vorgaben:**
- Keine spezifischen Daten verwenden -- bei Bedarf Versions-Referenzen ("seit v1.0")
- Kompakt halten -- CLAUDE.md wird bei jedem Gespraech geladen
- Nicht duplizieren was in MEMORY.md oder PROJECT.md steht
- Abschliessender Hinweis "Dieses System ist produktiv" beibehalten
- KEINE Emojis in der Datei (Projekt-Regel gilt auch fuer CLAUDE.md selbst)
  - Statt "## ⚠️ KRITISCHE REGELN" -> "## KRITISCHE REGELN FUER CLAUDE CODE"
  - Statt "### ✅ IMMER so" -> "### IMMER so (useIonModal Hook):"
  - Statt "### ❌ NIEMALS" -> "### NIEMALS ..."
  - Statt "### ✅ FUNKTIONIERT" -> "### Funktioniert:"
  Aber: checkmark/cross Zeichen in Markdown sind OK wenn sie keine Unicode Emojis sind
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && echo "=== Verbotene Inhalte ===" && grep -c "NOCH NICHT MIGRIERT" CLAUDE.md; grep -c "MIGRATION VORGEHEN" CLAUDE.md; grep -c "backup_sqlite" CLAUDE.md; grep -c "sqlite3" CLAUDE.md; grep -c "Juli 2025" CLAUDE.md; grep -c "psotgres" CLAUDE.md; grep -c "auf den docker zugreifen" CLAUDE.md; echo "=== Pflicht-Inhalte ===" && grep -c "15 Routes" CLAUDE.md; grep -c "verifyTokenRBAC" CLAUDE.md; grep -c "useIonModal" CLAUDE.md; grep -c "KEINE UNICODE EMOJIS" CLAUDE.md; grep -c "ECHTE UMLAUTE" CLAUDE.md; grep -c "konfi-quest-db-1 psql" CLAUDE.md; grep -c "konfi_profiles" CLAUDE.md</automated>
  </verify>
  <done>
- Alle verbotenen Inhalte (NOCH NICHT MIGRIERT, MIGRATION VORGEHEN, backup_sqlite, sqlite3, Juli 2025, kaputter DB-Befehl) ergeben 0 Treffer
- Alle Pflicht-Inhalte (15 Routes, verifyTokenRBAC, useIonModal, KEINE UNICODE EMOJIS, ECHTE UMLAUTE, korrekter psql-Befehl, konfi_profiles) ergeben mindestens 1 Treffer
- CLAUDE.md ist kompakter als die alte Version (unter 150 Zeilen)
- Keine Unicode-Emojis in der Datei
- Alle 4 kritischen Regeln sind vorhanden
- Entwicklungskommandos sind korrekt und vollstaendig
- DB-Schema (Kern-Tabellen) ist vorhanden
  </done>
</task>

</tasks>

<verification>
1. `grep -c "NOCH NICHT MIGRIERT\|Naechste.*badges\|MIGRATION VORGEHEN\|backup_sqlite\|sqlite3\|Juli 2025" CLAUDE.md` ergibt 0
2. `grep -c "15 Routes" CLAUDE.md` ergibt mindestens 1
3. `grep -c "konfi-quest-db-1 psql" CLAUDE.md` ergibt mindestens 1 (korrekter DB-Befehl)
4. `grep -c "useIonModal" CLAUDE.md` ergibt mindestens 1 (Modal-Pattern vorhanden)
5. `grep -c "KEINE UNICODE EMOJIS" CLAUDE.md` ergibt mindestens 1 (Emoji-Regel vorhanden)
6. `wc -l CLAUDE.md` ergibt weniger als 150 Zeilen (kompakter als die 202 Zeilen alte Version)
7. Keine Unicode-Emojis in der Datei: `grep -P '[\x{1F300}-\x{1F9FF}]' CLAUDE.md | wc -l` ergibt 0
</verification>

<success_criteria>
- CLAUDE.md spiegelt den tatsaechlichen Projektstatus korrekt wider
- Alle 15 PostgreSQL-Routes als vollstaendig migriert dokumentiert
- Keine veralteten Migrations-Details, geloesten Bugs oder TODO-Listen
- Keine spezifischen Daten, nur Versions-Referenzen
- Kompakte, aktuelle Instruktionsdatei fuer Claude Code
- Alle kritischen Regeln und Patterns beibehalten
- Korrekte Entwicklungs- und Deployment-Kommandos
</success_criteria>

<output>
After completion, create `.planning/phases/11-dokumentation/11-01-SUMMARY.md`
</output>
