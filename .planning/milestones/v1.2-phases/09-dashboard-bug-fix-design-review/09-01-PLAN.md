---
phase: 09-dashboard-bug-fix-design-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/views/ActivityRings.tsx
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "3. Runde hat gleiche Strichstaerke wie 2. Runde (70% der 1. Runde)"
    - "3. Runde nutzt hellere/leuchtendere Farbvariante als Highlight"
    - "3. Runde Radius ist etwas kleiner als 2. Runde (Schichteffekt beibehalten)"
    - "Maximum bei 300% -- keine 4. Runde, Ring bleibt voll"
    - "1. und 2. Runde bleiben visuell unveraendert"
  artifacts:
    - path: "frontend/src/components/admin/views/ActivityRings.tsx"
      provides: "Korrigierte Ring-Komponente mit 3-Runden-Logik"
      contains: "strokeWidth * 0.7"
  key_links:
    - from: "ActivityRings.tsx"
      to: "DashboardView.tsx"
      via: "import ActivityRings"
      pattern: "import ActivityRings from.*ActivityRings"
---

<objective>
ActivityRings 3. Runde Bug-Fix: Die dritte Runde (>200%) wird aktuell mit nur 35% Strichstaerke gerendert (viel zu duenn und kaum sichtbar). Fix: Gleiche Strichstaerke wie 2. Runde, hellere Farbvariante, Maximum bei 300%.

Purpose: Konfis die mehr als 200% ihrer Ziele erreichen, sollen das visuell korrekt als dritte Runde sehen -- aktuell ist der Ring so duenn, dass er fast unsichtbar ist.
Output: Korrigierte ActivityRings.tsx mit funktionierender 3-Runden-Darstellung.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/admin/views/ActivityRings.tsx

<interfaces>
<!-- ActivityRings Props Interface (bleibt unveraendert) -->
From frontend/src/components/admin/views/ActivityRings.tsx:
```typescript
interface ActivityRingsProps {
  totalPoints: number;
  gottesdienstPoints: number;
  gemeindePoints: number;
  gottesdienstGoal: number;
  gemeindeGoal: number;
  size?: number;
}
```

<!-- Farb-Konstanten (werden erweitert) -->
```typescript
const colors = {
  total: '#f59e0b',
  totalDark: '#b45309',
  gottesdienst: '#3b82f6',
  gottesdienstDark: '#1d4ed8',
  gemeinde: '#059669',
  gemeindeDark: '#047857',
  background: 'rgba(255, 255, 255, 0.12)'
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityRings 3. Runde Strichstaerke und Farbe fixen</name>
  <files>frontend/src/components/admin/views/ActivityRings.tsx</files>
  <action>
Im Ring-Component innerhalb von ActivityRings.tsx die 3. Runde (Zeilen 188-208) korrigieren:

1. **Strichstaerke fix (KERN-BUG):** Zeile 195 aendern von `strokeWidth * 0.35` auf `strokeWidth * 0.7` -- gleiche Staerke wie 2. Runde (Zeile 175). Die 3. Runde soll genauso prominent sein wie die 2. Runde.

2. **Radius-Versatz angleichen:** Zeile 192 aendern von `radius - strokeWidth * 0.35` auf `radius - strokeWidth * 0.3` -- der Versatz soll etwas kleiner als 2. Runde sein (die hat 0.15), aber nicht so extrem wie bisher. Der Ring soll noch innerhalb der 2. Runde liegen fuer den Schichteffekt.

3. **Farbe: Hellere Variante hinzufuegen:** Neue Farb-Properties zum colors-Objekt hinzufuegen:
   - `totalBright: '#fbbf24'` (helleres Amber/Gold)
   - `gottesdienstBright: '#60a5fa'` (helleres Blau)
   - `gemeindeBright: '#34d399'` (helleres Gruen)

   Die 3. Runde soll `colorBright` statt `color` verwenden. Dazu den Ring-Component erweitern:
   - Neuen Prop `colorBright: string` hinzufuegen
   - 3. Runde `stroke={colorBright}` statt `stroke={color}`
   - Glow filter auf colorBright anpassen: `drop-shadow(0 0 6px ${colorBright}90)`

4. **Maximum 300% durchsetzen:** In der Berechnung von `targetPercents` (Zeile 40-44) jede Prozentzahl auf maximal 300 begrenzen:
   ```typescript
   const targetPercents = {
     total: Math.min((totalPoints / effectiveTotalGoal) * 100, 300),
     gottesdienst: Math.min((gottesdienstPoints / effectiveGottesdienstGoal) * 100, 300),
     gemeinde: Math.min((gemeindePoints / effectiveGemeindeGoal) * 100, 300)
   };
   ```

5. **Ring-Aufruf aktualisieren:** Bei jedem Ring-Aufruf im SVG den neuen `colorBright`-Prop mitgeben:
   - Gesamt-Ring: `colorBright={colors.totalBright}`
   - Gottesdienst-Ring: `colorBright={colors.gottesdienstBright}`
   - Gemeinde-Ring: `colorBright={colors.gemeindeBright}`

6. **3. Runde Circumference:** Zeile 133 `circumference * 0.94` ist OK -- beibehalten fuer den Schichteffekt.

7. **Opacity entfernen:** Zeile 203 `opacity: 0.95` entfernen -- mit der helleren Farbe und korrekter Strichstaerke ist keine Transparenz mehr noetig.

WICHTIG: 1. und 2. Runde NICHT veraendern! Nur die 3. Runde wird korrigiert.
  </action>
  <verify>
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -30
  </verify>
  <done>
3. Runde hat strokeWidth * 0.7 (gleich wie 2. Runde), hellere Farbvariante (colorBright), Maximum 300%, keine Opacity-Reduktion. TypeScript kompiliert fehlerfrei.
  </done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
- ActivityRings.tsx enthaelt `strokeWidth * 0.7` fuer 3. Runde
- ActivityRings.tsx enthaelt `Math.min(... 300)` fuer Maximum
- Keine Aenderungen an 1. und 2. Runde (strokeWidth und Farben unveraendert)
- colorBright Properties existieren im colors-Objekt
</verification>

<success_criteria>
- ActivityRings 3. Runde hat korrekte Strichstaerke (70% der 1. Runde)
- Hellere Farbvariante fuer 3. Runde implementiert
- Maximum bei 300% begrenzt
- Keine visuellen Regressionen bei 1. und 2. Runde
- TypeScript fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-bug-fix-design-review/09-01-SUMMARY.md`
</output>
