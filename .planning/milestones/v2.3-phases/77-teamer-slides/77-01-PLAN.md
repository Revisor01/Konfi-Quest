---
phase: 77-teamer-slides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/wrapped/slides/teamer/TeamerIntroSlide.tsx
  - frontend/src/components/wrapped/slides/teamer/TeamerEventsSlide.tsx
  - frontend/src/components/wrapped/slides/teamer/TeamerKonfisSlide.tsx
  - frontend/src/components/wrapped/slides/teamer/TeamerBadgesSlide.tsx
  - frontend/src/components/wrapped/slides/teamer/TeamerZertifikateSlide.tsx
  - frontend/src/components/wrapped/slides/teamer/TeamerJahreSlide.tsx
  - frontend/src/components/wrapped/slides/teamer/TeamerAbschlussSlide.tsx
  - frontend/src/components/wrapped/WrappedModal.tsx
  - frontend/src/components/wrapped/WrappedModal.css
autonomous: true
requirements: [TS-01, TS-02, TS-03, TS-04, TS-05, TS-06, TS-07]

must_haves:
  truths:
    - "Teamer:in sieht Fullscreen-Slideshow mit rosa Akzentfarbe"
    - "Alle 7 Slides zeigen Teamer-spezifische Daten: Intro, Events geleitet, Konfis betreut, Badges, Zertifikate, Jahre aktiv, Abschluss mit Danke"
    - "Swiper, Progress-Bar, Animations-System werden vom Konfi-Wrapped wiederverwendet"
  artifacts:
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerIntroSlide.tsx"
      provides: "Teamer Intro Slide"
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerEventsSlide.tsx"
      provides: "Events geleitet Slide"
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerKonfisSlide.tsx"
      provides: "Konfis betreut Slide"
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerBadgesSlide.tsx"
      provides: "Teamer Badges Slide"
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerZertifikateSlide.tsx"
      provides: "Zertifikate Slide"
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerJahreSlide.tsx"
      provides: "Jahre aktiv Slide"
    - path: "frontend/src/components/wrapped/slides/teamer/TeamerAbschlussSlide.tsx"
      provides: "Abschluss + Danke Slide"
    - path: "frontend/src/components/wrapped/WrappedModal.tsx"
      provides: "Erweitert um type='teamer' Parameter"
    - path: "frontend/src/components/wrapped/WrappedModal.css"
      provides: "Rosa-Teamer CSS-Klassen"
  key_links:
    - from: "frontend/src/components/wrapped/WrappedModal.tsx"
      to: "/api/wrapped/me"
      via: "api.get, wrapped_type bestimmt Konfi vs Teamer"
      pattern: "wrapped_type.*teamer"
    - from: "frontend/src/components/wrapped/slides/teamer/*.tsx"
      to: "frontend/src/types/wrapped.ts"
      via: "TeamerWrappedData Interfaces importiert"
      pattern: "import.*Teamer.*from.*types/wrapped"
---

<objective>
7 Teamer-Slides erstellen und in WrappedModal mit type='teamer' integrieren.

Purpose: Teamer:innen bekommen ihren eigenen Jahresrueckblick mit rosa Akzentfarbe, basierend auf der in Phase 76 gebauten Konfi-Wrapped-Infrastruktur.
Output: 7 Teamer-Slide-Komponenten + erweitertes WrappedModal
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-slide-container-konfi-slides/76-01-SUMMARY.md

<interfaces>
<!-- Teamer-Interfaces bereits in Phase 76 definiert -->

From frontend/src/types/wrapped.ts:
```typescript
export interface TeamerEventsGeleitetSlide {
  total: number;
  meiste_teilnehmer_event: { name: string; teilnehmer: number } | null;
}
export interface TeamerKonfisBetreutSlide {
  total_konfis: number;
  jahrgaenge: string[];
}
export interface TeamerBadgesSlide {
  total_earned: number;
  badges: Array<{ name: string; icon: string; color: string }>;
}
export interface TeamerZertifikateSlide {
  total: number;
  zertifikate: Array<{ name: string; issued_date: string }>;
}
export interface TeamerEngagementSlide {
  teamer_seit: string;
  jahre_aktiv: number;
}
export interface TeamerZeitraumSlide {
  year: number;
}
export interface TeamerWrappedData {
  version: number;
  slides: {
    events_geleitet: TeamerEventsGeleitetSlide;
    konfis_betreut: TeamerKonfisBetreutSlide;
    badges: TeamerBadgesSlide;
    zertifikate: TeamerZertifikateSlide;
    engagement: TeamerEngagementSlide;
    zeitraum: TeamerZeitraumSlide;
  };
}
export interface WrappedResponse {
  data: KonfiWrappedData | TeamerWrappedData;
  computed_at: string;
  year: number;
  wrapped_type: 'konfi' | 'teamer';
}
export interface SlideProps { isActive: boolean; }
```

From frontend/src/components/wrapped/slides/SlideBase.tsx:
```typescript
interface SlideBaseProps { isActive: boolean; children: React.ReactNode; className?: string; }
// Rendert children nur bei isActive=true
```

From frontend/src/hooks/useCountUp.ts:
```typescript
export function useCountUp(target: number, isActive: boolean, duration?: number): number;
```

From frontend/src/components/konfi/views/DashboardSections.tsx:
```typescript
export const getIconFromString = (iconName: string | undefined): string => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 7 Teamer-Slide-Komponenten + CSS</name>
  <files>
    frontend/src/components/wrapped/slides/teamer/TeamerIntroSlide.tsx
    frontend/src/components/wrapped/slides/teamer/TeamerEventsSlide.tsx
    frontend/src/components/wrapped/slides/teamer/TeamerKonfisSlide.tsx
    frontend/src/components/wrapped/slides/teamer/TeamerBadgesSlide.tsx
    frontend/src/components/wrapped/slides/teamer/TeamerZertifikateSlide.tsx
    frontend/src/components/wrapped/slides/teamer/TeamerJahreSlide.tsx
    frontend/src/components/wrapped/slides/teamer/TeamerAbschlussSlide.tsx
    frontend/src/components/wrapped/WrappedModal.css
  </files>
  <read_first>
    frontend/src/components/wrapped/slides/IntroSlide.tsx
    frontend/src/components/wrapped/slides/EventsSlide.tsx
    frontend/src/components/wrapped/slides/BadgesSlide.tsx
    frontend/src/components/wrapped/slides/AbschlussSlide.tsx
    frontend/src/components/wrapped/WrappedModal.css
    frontend/src/types/wrapped.ts
    frontend/src/hooks/useCountUp.ts
    frontend/src/components/wrapped/slides/SlideBase.tsx
  </read_first>
  <action>
    Erstelle 7 Teamer-Slide-Komponenten im Unterordner `slides/teamer/`. Exakt gleiches Pattern wie Konfi-Slides: SlideBase + isActive + wrapped-anim-* CSS-Klassen.

    **Farbschema:** Rosa #e11d48 statt Lila #5b21b6/#7c3aed. Fuer Akzentfarben: #fb7185 (heller Rosa) statt #a78bfa. Hintergrund-Gradients: von #4c0519 (dunkel-rosa) nach #0a0a0a statt #1a0533/#2d1065.

    **1. TeamerIntroSlide.tsx** (TS-01)
    - Props: `isActive, displayName, year` (kein jahrgangName -- Teamer sind nicht in Jahrgaengen)
    - "Willkommen zu deinem" + "Teamer-Jahr {year}" + displayName
    - className="teamer-intro-slide"

    **2. TeamerEventsSlide.tsx** (TS-02)
    - Props: `isActive, events: TeamerEventsGeleitetSlide`
    - useCountUp fuer events.total
    - "Deine Events" + animierter Count + "Events geleitet"
    - Wenn meiste_teilnehmer_event vorhanden: Highlight-Box mit Event-Name + Teilnehmer-Zahl
    - Akzentfarbe: #fb7185 statt #a78bfa

    **3. TeamerKonfisSlide.tsx** (TS-03)
    - Props: `isActive, konfis: TeamerKonfisBetreutSlide`
    - useCountUp fuer konfis.total_konfis
    - "Deine Konfis" + animierter Count + "Konfis betreut"
    - Darunter: Jahrgangs-Liste (konfis.jahrgaenge als Chips/Tags)
    - className="teamer-konfis-slide", background: linear-gradient(135deg, #4c0519 0%, #0a0a0a 60%)

    **4. TeamerBadgesSlide.tsx** (TS-04)
    - Props: `isActive, badges: TeamerBadgesSlide`
    - Exakt gleiches Pattern wie Konfi BadgesSlide
    - getIconFromString aus DashboardSections re-usen
    - "Deine Badges" + Count + Grid mit max 6 Badges
    - badge-icon background: rosa-basiert statt lila

    **5. TeamerZertifikateSlide.tsx** (TS-05)
    - Props: `isActive, zertifikate: TeamerZertifikateSlide`
    - useCountUp fuer zertifikate.total
    - "Deine Zertifikate" + Count
    - Liste der Zertifikate mit Name und Datum (als Cards/Items)
    - className="teamer-zertifikate-slide"

    **6. TeamerJahreSlide.tsx** (TS-06)
    - Props: `isActive, engagement: TeamerEngagementSlide`
    - useCountUp fuer engagement.jahre_aktiv
    - "Dein Engagement" + animierter Count + "Jahre als Teamer:in"
    - Darunter: "Dabei seit {teamer_seit}" (formatiert als lesbares Datum)
    - className="teamer-jahre-slide"

    **7. TeamerAbschlussSlide.tsx** (TS-07)
    - Props: `isActive, data: TeamerWrappedData, year: number`
    - "Dein Teamer-Jahr {year}" + "auf einen Blick"
    - Summary-Box mit 3 Stats: Events geleitet, Konfis betreut, Badges
    - CTA-Text: "Danke fuer deinen Einsatz!" + "Du machst den Unterschied"
    - Akzentfarbe: #fb7185

    **CSS in WrappedModal.css ergaenzen:**
    Am Ende der Datei einen neuen Block `/* Teamer Slides */` hinzufuegen:
    - `.teamer-intro-slide { background: linear-gradient(135deg, #4c0519 0%, #0a0a0a 60%); }`
    - `.teamer-konfis-slide { background: linear-gradient(135deg, #4c0519 0%, #0a0a0a 60%); }`
    - `.teamer-zertifikate-slide { background: linear-gradient(135deg, #831843 0%, #0a0a0a 60%); }`
    - `.teamer-jahre-slide { background: linear-gradient(135deg, #881337 0%, #0a0a0a 60%); }`
    - `.teamer-abschluss-slide { background: linear-gradient(135deg, #4c0519 0%, #0a0a0a 60%); }`
    - `.teamer-abschluss-summary` analog zu `.abschluss-summary` aber mit rosa: `rgba(225, 29, 72, 0.1)` + `rgba(225, 29, 72, 0.2)` border
    - `.teamer-abschluss-stat-value { color: #fb7185; }`
    - `.teamer-konfis-tags { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 16px; justify-content: center; }`
    - `.teamer-konfis-tag { background: rgba(225, 29, 72, 0.2); border: 1px solid rgba(225, 29, 72, 0.4); border-radius: 12px; padding: 4px 12px; font-size: 0.85rem; color: rgba(255,255,255,0.8); }`
    - `.teamer-zertifikat-list { ... }` + `.teamer-zertifikat-item { ... }` fuer Zertifikate-Karten
    - Teamer Pagination bullet active color: Wird dynamisch via CSS-Variable in Task 2 geloest

    KEINE Unicode-Emojis verwenden! Echte Umlaute (ue -> ue ist ok in Code/Variablen, aber UI-Texte muessen ae, oe, ue als Umlaute haben).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "TeamerIntroSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep -r "TeamerEventsSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep -r "TeamerKonfisSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep -r "TeamerBadgesSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep -r "TeamerZertifikateSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep -r "TeamerJahreSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep -r "TeamerAbschlussSlide" frontend/src/components/wrapped/slides/teamer/ liefert Treffer
    - grep "teamer-intro-slide" frontend/src/components/wrapped/WrappedModal.css liefert Treffer
    - grep "#e11d48\|#fb7185\|#4c0519" frontend/src/components/wrapped/WrappedModal.css liefert Treffer (Rosa-Farben vorhanden)
    - grep -r "SlideBase" frontend/src/components/wrapped/slides/teamer/ | wc -l >= 7 (alle 7 Slides nutzen SlideBase)
    - grep -r "useCountUp" frontend/src/components/wrapped/slides/teamer/ | wc -l >= 3 (mindestens Events, Konfis, Zertifikate, Jahre)
    - Keine Unicode-Emojis in den Slide-Dateien
  </acceptance_criteria>
  <done>7 Teamer-Slide-Komponenten existieren mit SlideBase, Rosa-Farbschema, korrekten TypeScript-Interfaces und CSS. TypeScript kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: WrappedModal um type='teamer' erweitern</name>
  <files>
    frontend/src/components/wrapped/WrappedModal.tsx
    frontend/src/components/wrapped/WrappedModal.css
  </files>
  <read_first>
    frontend/src/components/wrapped/WrappedModal.tsx
    frontend/src/types/wrapped.ts
  </read_first>
  <action>
    WrappedModal erweitern, damit es sowohl Konfi- als auch Teamer-Wrapped anzeigen kann.

    **Props erweitern:**
    ```typescript
    interface WrappedModalProps {
      onClose: () => void;
      displayName: string;
      jahrgangName?: string;         // Optional -- Teamer haben keinen Jahrgang
      wrappedType?: 'konfi' | 'teamer';  // Default: 'konfi' (rueckwaertskompatibel)
    }
    ```

    **API-Response verarbeiten:**
    - Die API `/wrapped/me` liefert bereits `wrapped_type: 'konfi' | 'teamer'` zurueck
    - Im State: `const [wrappedType, setWrappedType] = useState<'konfi' | 'teamer'>(props.wrappedType || 'konfi');`
    - Datentyp bedingt: `data` kann `KonfiWrappedData | TeamerWrappedData` sein
    - Response liefert `wrapped_type` -- verwende diesen Wert um zwischen Konfi und Teamer zu unterscheiden

    **buildSlides() erweitern:**
    - Wenn `wrappedType === 'teamer'`: baue die 7 Teamer-Slides statt der Konfi-Slides
    - Imports fuer alle 7 Teamer-Slides aus `./slides/teamer/`
    - Cast: `const teamerData = data as TeamerWrappedData;`
    - Teamer-Slides: TeamerIntroSlide (displayName, year) -> TeamerEventsSlide (events_geleitet) -> TeamerKonfisSlide (konfis_betreut) -> TeamerBadgesSlide (badges) -> TeamerZertifikateSlide (zertifikate) -> TeamerJahreSlide (engagement) -> TeamerAbschlussSlide (data, year)

    **CSS: Rosa Pagination fuer Teamer:**
    - WrappedModal bekommt eine CSS-Klasse basierend auf Typ: `wrapped-overlay wrapped-overlay--teamer` wenn wrappedType === 'teamer'
    - In CSS: `.wrapped-overlay--teamer .wrapped-pagination .swiper-pagination-bullet-active { background: #e11d48; }`
    - In CSS: `.wrapped-overlay--teamer .wrapped-loading ion-spinner { --color: #e11d48; }`

    **Rueckwaertskompatibilitaet:** Ohne wrappedType-Prop verhaelt sich das Modal exakt wie bisher (Konfi-Wrapped mit Lila). Bestehende Konfi-Slides bleiben komplett unveraendert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "wrappedType" frontend/src/components/wrapped/WrappedModal.tsx liefert Treffer
    - grep "TeamerIntroSlide" frontend/src/components/wrapped/WrappedModal.tsx liefert Treffer
    - grep "TeamerAbschlussSlide" frontend/src/components/wrapped/WrappedModal.tsx liefert Treffer
    - grep "TeamerWrappedData" frontend/src/components/wrapped/WrappedModal.tsx liefert Treffer
    - grep "wrapped-overlay--teamer" frontend/src/components/wrapped/WrappedModal.css liefert Treffer
    - grep "#e11d48" frontend/src/components/wrapped/WrappedModal.css liefert Treffer (Rosa Pagination)
    - grep "IntroSlide" frontend/src/components/wrapped/WrappedModal.tsx liefert Treffer (Konfi-Slides noch vorhanden)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>WrappedModal akzeptiert wrappedType='teamer', zeigt dann 7 Teamer-Slides mit Rosa-Farbschema. Konfi-Wrapped funktioniert weiterhin unveraendert.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- Keine TypeScript-Fehler
2. Alle 7 Dateien in `slides/teamer/` existieren
3. WrappedModal.tsx importiert und verwendet alle 7 Teamer-Slides
4. CSS enthaelt Rosa-Farbvarianten (#e11d48, #fb7185, #4c0519)
5. Keine Unicode-Emojis in neuen Dateien
</verification>

<success_criteria>
- 7 Teamer-Slide-Komponenten erstellt, alle nutzen SlideBase und Teamer-TypeScript-Interfaces
- WrappedModal unterstuetzt wrappedType='teamer' und zeigt rosa-farbige Teamer-Slides
- Konfi-Wrapped bleibt unveraendert und funktional
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/77-teamer-slides/77-01-SUMMARY.md`
</output>
