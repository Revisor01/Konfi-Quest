---
phase: 80-wrapped-persistenz-individualisierung
plan: 03
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  - frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
  - frontend/src/components/wrapped/WrappedModal.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03

must_haves:
  truths:
    - "Konfi-Profil zeigt 'Meine Wrappeds' Sektion mit allen vergangenen Wrapped-Snapshots"
    - "Teamer-Profil zeigt 'Meine Wrappeds' Sektion mit allen vergangenen Wrapped-Snapshots"
    - "Teamer Konfi-History zeigt Card 'Dein Konfi-Wrapped' wenn alter Konfi-Snapshot existiert"
    - "Tap auf Wrapped-Eintrag oeffnet WrappedModal mit gespeicherten Daten"
  artifacts:
    - path: "frontend/src/components/konfi/views/ProfileView.tsx"
      provides: "Meine Wrappeds Sektion im Konfi-Profil"
      contains: "wrapped"
    - path: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      provides: "Meine Wrappeds Sektion im Teamer-Profil"
      contains: "wrapped"
    - path: "frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx"
      provides: "Konfi-Wrapped Card in Konfi-History"
      contains: "wrapped"
  key_links:
    - from: "ProfileView.tsx"
      to: "/api/wrapped/history/:userId"
      via: "api.get Aufruf fuer Wrapped-Liste"
      pattern: "wrapped/history"
    - from: "ProfileView.tsx / TeamerProfilePage.tsx"
      to: "WrappedModal"
      via: "useIonModal mit gespeicherten Snapshot-Daten"
      pattern: "WrappedModal"
---

<objective>
"Meine Wrappeds" Wiederansicht in Konfi-Profil und Teamer-Profil integrieren. Konfi-Wrapped Card in Teamer Konfi-History einbauen. WrappedModal erweitern, um gespeicherte Daten direkt zu akzeptieren (statt immer /api/wrapped/me zu laden).

Purpose: Nutzer:innen koennen ihre vergangenen Wrappeds jederzeit wieder anschauen.
Output: Profil-Sektionen + Konfi-History Card + WrappedModal mit Daten-Prop
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-wrapped-persistenz-individualisierung-wiederansicht-meine-wrappeds-konfi-history-wrapped-card-individualisierte-slides-farben-reihenfolge-highlights-formulierungen-pro-user/80-01-SUMMARY.md
@frontend/src/components/konfi/views/ProfileView.tsx
@frontend/src/components/teamer/pages/TeamerProfilePage.tsx
@frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
@frontend/src/components/wrapped/WrappedModal.tsx
@frontend/src/types/wrapped.ts
</context>

<interfaces>
<!-- Aus Plan 01: History-Endpoint und Types -->

From frontend/src/types/wrapped.ts (nach Plan 01):
```typescript
export interface WrappedHistoryEntry {
  id: number;
  wrapped_type: 'konfi' | 'teamer';
  year: number;
  data: KonfiWrappedData | TeamerWrappedData;
  computed_at: string;
}
```

API: GET /api/wrapped/history/:userId → WrappedHistoryEntry[]

From frontend/src/components/wrapped/WrappedModal.tsx:
```typescript
interface WrappedModalProps {
  onClose: () => void;
  displayName: string;
  jahrgangName?: string;
  wrappedType?: 'konfi' | 'teamer';
}
```

From frontend/src/components/konfi/views/ProfileView.tsx:
```typescript
interface ProfileViewProps {
  profile: KonfiProfile;
  onReload: () => void;
  presentingElement: HTMLElement | null;
  pageRef?: React.RefObject<HTMLElement | null>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WrappedModal um Daten-Prop erweitern + Profil-Sektionen</name>
  <files>
    frontend/src/components/wrapped/WrappedModal.tsx,
    frontend/src/components/konfi/views/ProfileView.tsx,
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  </files>
  <read_first>
    frontend/src/components/wrapped/WrappedModal.tsx,
    frontend/src/components/konfi/views/ProfileView.tsx,
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx,
    frontend/src/types/wrapped.ts
  </read_first>
  <action>
**A) WrappedModal erweitern** (per D-02):

Neue optionale Props hinzufuegen zu WrappedModalProps:
```typescript
interface WrappedModalProps {
  onClose: () => void;
  displayName: string;
  jahrgangName?: string;
  wrappedType?: 'konfi' | 'teamer';
  // NEU: Fuer Wiederansicht — wenn gesetzt, wird NICHT /api/wrapped/me geladen
  initialData?: KonfiWrappedData | TeamerWrappedData;
  initialYear?: number;
}
```

Im useEffect (Zeile ~49): Wenn `initialData` und `initialYear` gesetzt sind, diese direkt verwenden statt API-Call:
```typescript
useEffect(() => {
  if (initialData && initialYear) {
    setData(initialData);
    setYear(initialYear);
    if (initialType) setWrappedType(initialType);
    return;
  }
  // Bestehender API-Call...
}, []);
```

**B) "Meine Wrappeds" in ProfileView.tsx** (per D-01, D-02):

1. Imports hinzufuegen:
```typescript
import { useIonModal } from '@ionic/react';  // Falls nicht schon importiert
import { timeOutline } from 'ionicons/icons';
import WrappedModal from '../../wrapped/WrappedModal';
import type { WrappedHistoryEntry, KonfiWrappedData, TeamerWrappedData } from '../../../types/wrapped';
```

2. State + Laden der Wrapped-Historie:
Nach den bestehenden States (ca. Zeile 107):
```typescript
const [wrappedHistory, setWrappedHistory] = useState<WrappedHistoryEntry[]>([]);

React.useEffect(() => {
  if (!profile?.id) return;
  api.get(`/wrapped/history/${profile.id}`)
    .then(res => setWrappedHistory(res.data || []))
    .catch(() => {}); // Stille Fehlerbehandlung — optionales Feature
}, [profile?.id]);
```

3. WrappedModal per useIonModal mit dynamischen Daten:
```typescript
const [selectedWrapped, setSelectedWrapped] = useState<WrappedHistoryEntry | null>(null);

// Separater State fuer useIonModal — wird bei jedem Tap aktualisiert
const wrappedModalRef = React.useRef<WrappedHistoryEntry | null>(null);
const [presentWrappedModal, dismissWrappedModal] = useIonModal(WrappedModal, {
  onClose: () => dismissWrappedModal(),
  displayName: profile.display_name,
  jahrgangName: profile.jahrgang_name || '',
  wrappedType: wrappedModalRef.current?.wrapped_type || 'konfi',
  initialData: wrappedModalRef.current?.data,
  initialYear: wrappedModalRef.current?.year
});

const openWrapped = (entry: WrappedHistoryEntry) => {
  wrappedModalRef.current = entry;
  presentWrappedModal({ cssClass: 'wrapped-modal-fullscreen' });
};
```

4. UI-Sektion einfuegen — NACH dem "Konfirmationstermin" Card Block (ca. Zeile 390) und VOR dem "Next Badge Progress":

```tsx
{/* Meine Wrappeds */}
{wrappedHistory.length > 0 && (
  <IonList inset={true} style={{ margin: '16px' }}>
    <IonListHeader>
      <div className="app-section-icon app-section-icon--purple">
        <IonIcon icon={timeOutline} />
      </div>
      <IonLabel>Meine Wrappeds</IonLabel>
    </IonListHeader>
    <IonCard className="app-card">
      <IonCardContent style={{ padding: '16px' }}>
        {wrappedHistory.map((entry) => (
          <div
            key={entry.id}
            className="app-list-item app-list-item--purple"
            style={{ width: '100%', cursor: 'pointer', marginBottom: '8px' }}
            onClick={() => openWrapped(entry)}
          >
            <div className="app-list-item__row">
              <div className="app-list-item__main">
                <div className="app-icon-circle app-icon-circle--purple">
                  <IonIcon icon={timeOutline} />
                </div>
                <div className="app-list-item__content">
                  <div className="app-list-item__title">
                    {entry.wrapped_type === 'konfi' ? 'Konfi-Wrapped' : 'Teamer-Wrapped'} {entry.year}
                  </div>
                  <div className="app-list-item__meta">
                    <span className="app-list-item__meta-item">
                      {new Date(entry.computed_at).toLocaleDateString('de-DE', { day: 'numeric', month: 'short', year: 'numeric' })}
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        ))}
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```

Zusaetzliche Imports benoetigt in ProfileView: `IonListHeader` (pruefen ob schon importiert).

**C) "Meine Wrappeds" in TeamerProfilePage.tsx** (per D-01):

Gleiche Logik wie in ProfileView, aber:
- Farbe: #e11d48 (Teamer-Rosa statt Lila)
- Nach dem "Inhalt"-Block (Zeile ~376), VOR dem "Logout-Button" (Zeile ~379)
- user.id statt profile.id fuer den API-Call
- Wrapped-History laden:
```typescript
const [wrappedHistory, setWrappedHistory] = useState<WrappedHistoryEntry[]>([]);

React.useEffect(() => {
  if (!user?.id) return;
  api.get(`/wrapped/history/${user.id}`)
    .then(res => setWrappedHistory(res.data || []))
    .catch(() => {});
}, [user?.id]);
```

- useIonModal mit WrappedModal (gleicher Ref-Pattern wie ProfileView):
```typescript
const wrappedModalRef = React.useRef<WrappedHistoryEntry | null>(null);
const [presentWrappedModal, dismissWrappedModal] = useIonModal(WrappedModal, {
  onClose: () => dismissWrappedModal(),
  displayName: profile?.user?.display_name || '',
  wrappedType: wrappedModalRef.current?.wrapped_type || 'teamer',
  initialData: wrappedModalRef.current?.data,
  initialYear: wrappedModalRef.current?.year
});
```

- Sektion:
```tsx
{wrappedHistory.length > 0 && (
  <IonList inset={true} style={{ margin: '16px' }}>
    <IonListHeader>
      <div className="app-section-icon" style={{ backgroundColor: '#e11d48' }}>
        <IonIcon icon={timeOutline} />
      </div>
      <IonLabel>Meine Wrappeds</IonLabel>
    </IonListHeader>
    <IonCard className="app-card">
      <IonCardContent style={{ padding: '16px' }}>
        {wrappedHistory.map((entry) => (
          <div
            key={entry.id}
            className="app-list-item"
            style={{ width: '100%', cursor: 'pointer', marginBottom: '8px', borderLeftColor: '#e11d48' }}
            onClick={() => {
              wrappedModalRef.current = entry;
              presentWrappedModal({ cssClass: 'wrapped-modal-fullscreen' });
            }}
          >
            <div className="app-list-item__row">
              <div className="app-list-item__main">
                <div className="app-icon-circle" style={{ backgroundColor: '#e11d48' }}>
                  <IonIcon icon={timeOutline} />
                </div>
                <div className="app-list-item__content">
                  <div className="app-list-item__title">
                    {entry.wrapped_type === 'konfi' ? 'Konfi-Wrapped' : 'Teamer-Wrapped'} {entry.year}
                  </div>
                  <div className="app-list-item__meta">
                    <span className="app-list-item__meta-item">
                      {new Date(entry.computed_at).toLocaleDateString('de-DE', { day: 'numeric', month: 'short', year: 'numeric' })}
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        ))}
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```

Import hinzufuegen: `timeOutline` aus ionicons/icons, `WrappedModal`, `WrappedHistoryEntry`/Types, `useState` (pruefen ob schon vorhanden).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - WrappedModal akzeptiert initialData + initialYear Props und ueberspringt API-Call wenn gesetzt
    - ProfileView (Konfi-Profil) zeigt "Meine Wrappeds" Liste wenn wrappedHistory nicht leer
    - TeamerProfilePage zeigt "Meine Wrappeds" Liste in Teamer-Rosa
    - Tap auf Eintrag oeffnet WrappedModal mit den gespeicherten Snapshot-Daten
    - tsc kompiliert fehlerfrei
  </acceptance_criteria>
  <done>Profil-Seiten zeigen "Meine Wrappeds" Sektion, Tap oeffnet Wrapped-Modal mit gespeicherten Daten</done>
</task>

<task type="auto">
  <name>Task 2: Konfi-History Wrapped Card im Teamer-Profil</name>
  <files>frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx</files>
  <read_first>
    frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx,
    frontend/src/types/wrapped.ts
  </read_first>
  <action>
Per D-03: Auf der TeamerKonfiStatsPage eine Card "Dein Konfi-Wrapped [Jahr]" anzeigen, wenn der/die Teamer:in einen alten Konfi-Wrapped-Snapshot hat.

1. **Imports hinzufuegen:**
```typescript
import { useState } from 'react';  // Falls nicht schon vorhanden
import { timeOutline } from 'ionicons/icons';
import WrappedModal from '../../wrapped/WrappedModal';
import type { WrappedHistoryEntry } from '../../../types/wrapped';
```

2. **Konfi-Wrapped laden:**
Nach dem bestehenden `useOfflineQuery` Block (Zeile ~237):
```typescript
const [konfiWrapped, setKonfiWrapped] = useState<WrappedHistoryEntry | null>(null);

React.useEffect(() => {
  if (!user?.id) return;
  api.get(`/wrapped/history/${user.id}`)
    .then(res => {
      const entries: WrappedHistoryEntry[] = res.data || [];
      // Nur Konfi-Wrapped Eintraege, neuester zuerst
      const konfiEntry = entries.find(e => e.wrapped_type === 'konfi');
      if (konfiEntry) setKonfiWrapped(konfiEntry);
    })
    .catch(() => {});
}, [user?.id]);
```

3. **WrappedModal-Hook:**
```typescript
const wrappedModalRef = React.useRef<WrappedHistoryEntry | null>(null);
const [presentWrappedModal, dismissWrappedModal] = useIonModal(WrappedModal, {
  onClose: () => dismissWrappedModal(),
  displayName: user?.display_name || '',
  wrappedType: 'konfi' as const,
  initialData: wrappedModalRef.current?.data,
  initialYear: wrappedModalRef.current?.year
});
```

4. **Card einfuegen:**
NACH dem Punkte-Header (Zeile ~308), VOR den Konfi-Badges (Zeile ~311):

```tsx
{/* Konfi-Wrapped Card */}
{konfiWrapped && (
  <IonList inset={true} style={{ margin: '16px' }}>
    <IonCard className="app-card">
      <IonCardContent style={{ padding: '16px' }}>
        <div
          className="app-list-item"
          style={{ width: '100%', cursor: 'pointer', borderLeftColor: '#5b21b6' }}
          onClick={() => {
            wrappedModalRef.current = konfiWrapped;
            presentWrappedModal({ cssClass: 'wrapped-modal-fullscreen' });
          }}
        >
          <div className="app-list-item__row">
            <div className="app-list-item__main">
              <div className="app-icon-circle" style={{ backgroundColor: '#5b21b6' }}>
                <IonIcon icon={timeOutline} />
              </div>
              <div className="app-list-item__content">
                <div className="app-list-item__title">
                  Dein Konfi-Wrapped {konfiWrapped.year}
                </div>
                <div className="app-list-item__meta">
                  <span className="app-list-item__meta-item">
                    Dein persoenlicher Rueckblick aus der Konfi-Zeit
                  </span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```

WICHTIG: `useIonModal` aus @ionic/react ist bereits importiert in TeamerKonfiStatsPage. `useState` muss eventuell zu den React-Imports hinzugefuegt werden (pruefen).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TeamerKonfiStatsPage laedt Konfi-Wrapped per /api/wrapped/history/:userId
    - Card "Dein Konfi-Wrapped [Jahr]" wird angezeigt wenn Snapshot existiert
    - Tap oeffnet WrappedModal mit den Konfi-Wrapped Daten
    - Kein Fehler wenn kein Konfi-Wrapped existiert (Card einfach nicht sichtbar)
    - tsc kompiliert fehlerfrei
  </acceptance_criteria>
  <done>Teamer Konfi-History zeigt Konfi-Wrapped Card mit Tap-to-Open, wenn alter Snapshot existiert</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert fehlerfrei
- WrappedModal hat initialData/initialYear Props
- ProfileView und TeamerProfilePage laden /wrapped/history/:userId und zeigen Liste
- TeamerKonfiStatsPage zeigt Konfi-Wrapped Card wenn Daten vorhanden
- Alle Modal-Oeffnungen verwenden useIonModal (nicht IonModal isOpen)
</verification>

<success_criteria>
Nutzer:innen koennen in ihrem Profil (Konfi + Teamer) alle vergangenen Wrappeds sehen und per Tap erneut durchblaettern. Teamer:innen sehen in der Konfi-History eine Card fuer ihr altes Konfi-Wrapped.
</success_criteria>

<output>
After completion, create `.planning/phases/80-wrapped-persistenz-individualisierung-wiederansicht-meine-wrappeds-konfi-history-wrapped-card-individualisierte-slides-farben-reihenfolge-highlights-formulierungen-pro-user/80-03-SUMMARY.md`
</output>
