---
phase: 79-dashboard-integration-freischaltung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - backend/routes/wrapped.js
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04]
must_haves:
  truths:
    - "Konfi-Dashboard zeigt 'Dein Wrapped ist da!' Card wenn has_wrapped=true"
    - "Teamer-Dashboard zeigt 'Dein Teamer-Jahr!' Card wenn has_wrapped=true"
    - "Card oeffnet WrappedModal beim Tippen"
    - "Push-Notification wird gesendet wenn Wrapped generiert wird"
  artifacts:
    - path: "frontend/src/components/konfi/pages/KonfiDashboardPage.tsx"
      provides: "Wrapped-Card im Konfi-Dashboard"
      contains: "WrappedModal"
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "Wrapped-Card im Teamer-Dashboard"
      contains: "WrappedModal"
    - path: "backend/routes/wrapped.js"
      provides: "Push-Benachrichtigung bei Wrapped-Generierung"
      contains: "PushService"
  key_links:
    - from: "KonfiDashboardPage.tsx"
      to: "WrappedModal"
      via: "useIonModal + has_wrapped aus dashboardData"
      pattern: "useIonModal.*WrappedModal"
    - from: "TeamerDashboardPage.tsx"
      to: "WrappedModal"
      via: "useIonModal + has_wrapped aus dashboardData"
      pattern: "useIonModal.*WrappedModal"
    - from: "wrapped.js generate endpoints"
      to: "PushService.sendToUser"
      via: "Push nach erfolgreicher Generierung"
      pattern: "PushService\\.sendToUser"
---

<objective>
Dashboard-Cards "Dein Wrapped ist da!" fuer Konfi und Teamer sowie Push-Notification bei Wrapped-Generierung.

Purpose: Nutzer:innen sollen auf ihrem Dashboard sofort sehen, dass ihr Wrapped verfuegbar ist, und per Push darauf hingewiesen werden.
Output: Wrapped-Cards in beiden Dashboards + Push bei Generierung
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- WrappedModal Props -->
From frontend/src/components/wrapped/WrappedModal.tsx:
```typescript
interface WrappedModalProps {
  onClose: () => void;
  displayName: string;
  jahrgangName?: string;
  wrappedType?: 'konfi' | 'teamer';
}
```

<!-- Konfi DashboardData (bereits has_wrapped im Backend, fehlt im Frontend-Interface) -->
From frontend/src/components/konfi/pages/KonfiDashboardPage.tsx:
```typescript
interface DashboardData {
  konfi: { id: number; display_name: string; jahrgang_name: string; ... };
  total_points: number;
  // has_wrapped kommt vom Backend, ist aber noch nicht im Interface
}
```

<!-- Teamer DashboardData -->
From frontend/src/components/teamer/pages/TeamerDashboardPage.tsx:
```typescript
interface DashboardData {
  greeting: { display_name: string; hour: number };
  certificates: Certificate[];
  events: DashboardEvent[];
  badges: { recent: Badge[]; earned_count: number; total_count: number };
  config: DashboardConfig;
  // has_wrapped kommt vom Backend, ist aber noch nicht im Interface
}
```

<!-- PushService Pattern -->
From backend/services/pushService.js:
```javascript
static async sendToUser(db, userId, notification)
// notification = { title: string, body: string, badge?: number, data?: object }
static async sendToMultipleUsers(db, userIds, notification)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard-Cards (Konfi + Teamer) -- "Dein Wrapped ist da!"</name>
  <files>
    frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  </files>
  <read_first>
    frontend/src/components/konfi/pages/KonfiDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    frontend/src/components/wrapped/WrappedModal.tsx (erste 40 Zeilen fuer Props)
  </read_first>
  <action>
**KonfiDashboardPage.tsx:**
1. Import `useIonModal` ist bereits vorhanden. Zusaetzlich importieren: `WrappedModal` aus `../../wrapped/WrappedModal` und `sparkles` aus `ionicons/icons` sowie `IonIcon`.
2. `has_wrapped` zum `DashboardData` Interface hinzufuegen: `has_wrapped?: boolean;`
3. Nach dem `useIonModal(PointsHistoryModal, ...)` Block einen zweiten `useIonModal` fuer WrappedModal hinzufuegen:
```typescript
const [presentWrappedModal, dismissWrappedModal] = useIonModal(WrappedModal, {
  onClose: () => dismissWrappedModal(),
  displayName: dashboardData?.konfi?.display_name || '',
  jahrgangName: dashboardData?.konfi?.jahrgang_name || '',
  wrappedType: 'konfi' as const
});
```
4. Funktion `openWrapped` erstellen:
```typescript
const openWrapped = () => {
  presentWrappedModal({ cssClass: 'wrapped-modal-fullscreen' });
};
```
5. Im JSX direkt VOR dem `<DashboardView ...>` eine Wrapped-Card einfuegen, nur wenn `dashboardData.has_wrapped`:
```tsx
{dashboardData.has_wrapped && (
  <div onClick={openWrapped} style={{
    margin: '0 16px 16px',
    padding: '20px',
    borderRadius: '16px',
    background: 'linear-gradient(135deg, #7c3aed 0%, #a855f7 50%, #c084fc 100%)',
    color: 'white',
    cursor: 'pointer',
    position: 'relative',
    overflow: 'hidden'
  }}>
    <div style={{ position: 'relative', zIndex: 1, display: 'flex', alignItems: 'center', gap: '12px' }}>
      <IonIcon icon={sparkles} style={{ fontSize: '2rem' }} />
      <div>
        <h3 style={{ margin: 0, fontSize: '1.1rem', fontWeight: '700' }}>Dein Wrapped ist da!</h3>
        <p style={{ margin: '4px 0 0', fontSize: '0.85rem', opacity: 0.9 }}>Schau dir deinen Konfi-Jahresrueckblick an</p>
      </div>
      <IonIcon icon={chevronForward} style={{ fontSize: '1.2rem', marginLeft: 'auto' }} />
    </div>
  </div>
)}
```
Import `chevronForward` aus `ionicons/icons` (noch nicht importiert in KonfiDashboardPage).

**TeamerDashboardPage.tsx:**
1. Zusaetzlich importieren: `useIonModal` aus `@ionic/react`, `WrappedModal` aus `../../wrapped/WrappedModal`, `sparkles` aus `ionicons/icons` (ist schon importiert).
2. `has_wrapped` zum `DashboardData` Interface hinzufuegen: `has_wrapped?: boolean;`
3. Im Component Body (nach den useOfflineQuery Hooks) den WrappedModal Hook hinzufuegen:
```typescript
const [presentWrappedModal, dismissWrappedModal] = useIonModal(WrappedModal, {
  onClose: () => dismissWrappedModal(),
  displayName: dashboardData?.greeting?.display_name || '',
  wrappedType: 'teamer' as const
});

const openWrapped = () => {
  presentWrappedModal({ cssClass: 'wrapped-modal-fullscreen' });
};
```
4. Im JSX direkt NACH dem Begruessung-Block (`app-dashboard-header`) und VOR dem Zertifikate-Block eine Wrapped-Card einfuegen, nur wenn `dashboardData?.has_wrapped`:
```tsx
{dashboardData?.has_wrapped && (
  <div onClick={openWrapped} style={{
    marginBottom: '16px',
    padding: '20px',
    borderRadius: '16px',
    background: 'linear-gradient(135deg, #db2777 0%, #ec4899 50%, #f472b6 100%)',
    color: 'white',
    cursor: 'pointer',
    position: 'relative',
    overflow: 'hidden'
  }}>
    <div style={{ position: 'relative', zIndex: 1, display: 'flex', alignItems: 'center', gap: '12px' }}>
      <IonIcon icon={sparkles} style={{ fontSize: '2rem' }} />
      <div>
        <h3 style={{ margin: 0, fontSize: '1.1rem', fontWeight: '700' }}>Dein Teamer-Jahr ist da!</h3>
        <p style={{ margin: '4px 0 0', fontSize: '0.85rem', opacity: 0.9 }}>Schau dir deinen Jahresrueckblick an</p>
      </div>
      <IonIcon icon={chevronForward} style={{ fontSize: '1.2rem', marginLeft: 'auto' }} />
    </div>
  </div>
)}
```
Hinweis: `chevronForward` ist bei TeamerDashboardPage bereits importiert.

Card-Design:
- Konfi: Lila-Gradient (passt zum Wrapped-Theme)
- Teamer: Rosa-Gradient (passt zum Teamer-Wrapped-Theme)
- Card verschwindet NICHT nach dem Oeffnen (per Kontext-Entscheidung)
- KEINE Unicode-Emojis -- IonIcon sparkles als Eye-Catcher

Per INT-01: Konfi-Card zeigt "Dein Wrapped ist da!"
Per INT-02: Teamer-Card zeigt "Dein Teamer-Jahr ist da!"
Per INT-04: Einmaligkeit durch UNIQUE Constraint auf wrapped_snapshots (user_id, wrapped_type, year) -- keine Frontend-Aenderung noetig
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "WrappedModal" frontend/src/components/konfi/pages/KonfiDashboardPage.tsx && grep -q "WrappedModal" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx && grep -q "has_wrapped" frontend/src/components/konfi/pages/KonfiDashboardPage.tsx && grep -q "has_wrapped" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx && cd frontend && npx tsc --noEmit 2>&1 | head -20 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - KonfiDashboardPage zeigt Wrapped-Card wenn has_wrapped=true, oeffnet WrappedModal mit wrappedType='konfi'
    - TeamerDashboardPage zeigt Wrapped-Card wenn has_wrapped=true, oeffnet WrappedModal mit wrappedType='teamer'
    - DashboardData Interfaces enthalten has_wrapped?: boolean
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>Beide Dashboards zeigen eine farbige Wrapped-Card wenn has_wrapped=true ist, die beim Tippen das WrappedModal oeffnet</done>
</task>

<task type="auto">
  <name>Task 2: Push-Notification bei Wrapped-Generierung</name>
  <files>
    backend/routes/wrapped.js
  </files>
  <read_first>
    backend/routes/wrapped.js (Zeilen 1-10 fuer Imports, Zeilen 373-507 fuer generate Endpoints, Zeilen 555-655 fuer Batch-Funktionen)
    backend/services/pushService.js (Zeilen 55-125 fuer sendToUser/sendToMultipleUsers Pattern)
  </read_first>
  <action>
**Push-Integration in wrapped.js:**

1. PushService importieren: Am Anfang der module.exports-Funktion (nach `const { requireAdmin, requireOrgAdmin } = roleHelpers;`):
```javascript
const PushService = require('../services/pushService');
```

2. **POST /generate/:jahrgangId** (Konfi-Wrapped, Zeile ~434 nach COMMIT): Nach dem `await client.query('COMMIT');` und VOR dem `res.json(...)` Push an alle erfolgreich generierten Konfis senden:
```javascript
// Push-Notification an alle Konfis
try {
  const konfiIds = konfis.map(k => k.user_id);
  await PushService.sendToMultipleUsers(db, konfiIds, {
    title: 'Dein Konfi-Jahr ist da!',
    body: 'Schau dir jetzt deinen persoenlichen Jahresrueckblick an!',
    data: { type: 'wrapped', wrappedType: 'konfi' }
  });
} catch (pushErr) {
  console.error('Push-Notification fuer Konfi-Wrapped fehlgeschlagen:', pushErr);
}
```
WICHTIG: Push ist fire-and-forget (try/catch), darf die Generierung nicht blockieren oder zum Fehler fuehren.

3. **POST /generate-teamer** (Teamer-Wrapped, Zeile ~492 nach COMMIT): Gleich nach `await client.query('COMMIT');`:
```javascript
// Push-Notification an alle Teamer:innen
try {
  const teamerIds = teamers.map(t => t.user_id);
  await PushService.sendToMultipleUsers(db, teamerIds, {
    title: 'Dein Teamer-Jahr ist da!',
    body: 'Schau dir jetzt deinen persoenlichen Jahresrueckblick an!',
    data: { type: 'wrapped', wrappedType: 'teamer' }
  });
} catch (pushErr) {
  console.error('Push-Notification fuer Teamer-Wrapped fehlgeschlagen:', pushErr);
}
```

4. **Batch-Funktionen** (generateAllKonfiWrapped + generateAllTeamerWrapped): Gleiche Push-Logik nach dem COMMIT hinzufuegen. Fuer generateAllKonfiWrapped (Zeile ~601 nach COMMIT):
```javascript
// Push (fire-and-forget, dbRef statt client da client released wird)
try {
  const konfiIds = konfis.map(k => k.user_id);
  await PushService.sendToMultipleUsers(dbRef, konfiIds, {
    title: 'Dein Konfi-Jahr ist da!',
    body: 'Schau dir jetzt deinen persoenlichen Jahresrueckblick an!',
    data: { type: 'wrapped', wrappedType: 'konfi' }
  });
} catch (pushErr) {
  console.error('Wrapped-Cron Push fehlgeschlagen:', pushErr);
}
```
Fuer generateAllTeamerWrapped (nach COMMIT, vor return):
```javascript
try {
  const teamerIds = teamers.map(t => t.user_id);
  await PushService.sendToMultipleUsers(dbRef, teamerIds, {
    title: 'Dein Teamer-Jahr ist da!',
    body: 'Schau dir jetzt deinen persoenlichen Jahresrueckblick an!',
    data: { type: 'wrapped', wrappedType: 'teamer' }
  });
} catch (pushErr) {
  console.error('Wrapped-Cron Push fehlgeschlagen:', pushErr);
}
```

WICHTIG: Echte Umlaute in Push-Texten! "persoenlichen" = "persönlichen" im tatsaechlichen Code.
Per INT-03: Push wird bei jeder Wrapped-Generierung gesendet (Admin-Trigger + Cron).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "PushService" backend/routes/wrapped.js && grep -c "sendToMultipleUsers" backend/routes/wrapped.js | xargs test 4 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - PushService wird in wrapped.js importiert
    - 4x sendToMultipleUsers Aufrufe: je 1x in POST /generate/:jahrgangId, POST /generate-teamer, generateAllKonfiWrapped, generateAllTeamerWrapped
    - Push ist fire-and-forget (try/catch), blockiert nicht die Response
    - Push-Texte verwenden echte Umlaute
  </acceptance_criteria>
  <done>Bei jeder Wrapped-Generierung (Admin-Trigger und Cron) erhalten betroffene Nutzer:innen eine Push-Notification</done>
</task>

</tasks>

<verification>
1. `grep -q "WrappedModal" frontend/src/components/konfi/pages/KonfiDashboardPage.tsx` -- Konfi-Dashboard hat Wrapped-Integration
2. `grep -q "WrappedModal" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx` -- Teamer-Dashboard hat Wrapped-Integration
3. `grep -q "PushService" backend/routes/wrapped.js` -- Push in Wrapped-Route integriert
4. `grep -c "sendToMultipleUsers" backend/routes/wrapped.js` ergibt 4 -- alle 4 Generierungs-Pfade haben Push
5. `cd frontend && npx tsc --noEmit` -- TypeScript kompiliert fehlerfrei
</verification>

<success_criteria>
- Konfi-Dashboard zeigt lila "Dein Wrapped ist da!" Card wenn has_wrapped=true
- Teamer-Dashboard zeigt rosa "Dein Teamer-Jahr ist da!" Card wenn has_wrapped=true
- Beide Cards oeffnen WrappedModal mit korrektem wrappedType
- Push-Notification wird bei Admin-Trigger und Cron-Generierung gesendet
- Einmaligkeit durch bestehenden UNIQUE Constraint sichergestellt
</success_criteria>

<output>
After completion, create `.planning/phases/79-dashboard-integration-freischaltung/79-01-SUMMARY.md`
</output>
