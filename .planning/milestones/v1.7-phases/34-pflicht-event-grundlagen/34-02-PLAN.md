---
phase: 34-pflicht-event-grundlagen
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
  - frontend/src/types/dashboard.ts
autonomous: false
requirements: [PFL-01, PFL-03, EUI-01]

must_haves:
  truths:
    - "Admin sieht im EventModal einen Pflicht-Event-Toggle und bei Aktivierung werden Punkte/Kapazitaet/Waitlist-Felder ausgeblendet"
    - "Admin sieht im EventModal ein 'Was mitbringen'-Textfeld das fuer alle Events verfuegbar ist"
    - "Admin sieht in der EventDetailView das 'Was mitbringen'-Feld wenn befuellt"
    - "Konfi sieht in der EventDetailView das 'Was mitbringen'-Feld wenn befuellt"
    - "Konfi sieht in der EventsView einen visuellen Pflicht-Indikator bei Pflicht-Events"
  artifacts:
    - path: "frontend/src/components/admin/modals/EventModal.tsx"
      provides: "Mandatory-Toggle, bring_items-Textarea, Feld-Ausblendung bei mandatory"
      contains: "mandatory"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "bring_items Anzeige und Pflicht-Badge"
      contains: "bring_items"
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "bring_items Anzeige und Pflicht-Badge"
      contains: "bring_items"
    - path: "frontend/src/components/konfi/views/EventsView.tsx"
      provides: "Pflicht-Indikator in Event-Liste"
      contains: "mandatory"
  key_links:
    - from: "frontend/src/components/admin/modals/EventModal.tsx"
      to: "/api/events"
      via: "api.post/put mit mandatory und bring_items"
      pattern: "mandatory.*bring_items"
    - from: "frontend/src/components/konfi/views/EventDetailView.tsx"
      to: "event.bring_items"
      via: "Anzeige des bring_items Feldes"
      pattern: "bring_items"
---

<objective>
Frontend fuer Pflicht-Events: EventModal um mandatory-Toggle und bring_items-Feld erweitern, irrelevante Felder bei Pflicht-Events ausblenden, bring_items in Detail-Views anzeigen, Pflicht-Indikator in Event-Listen.

Purpose: Admin kann Pflicht-Events erstellen/bearbeiten, Konfis sehen Was-mitbringen-Info und Pflicht-Status.
Output: Vollstaendige Frontend-Unterstuetzung fuer Pflicht-Events.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-pflicht-event-grundlagen/34-RESEARCH.md
@.planning/phases/34-pflicht-event-grundlagen/34-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 -- Backend liefert diese neuen Felder -->

Event-Objekt vom Backend (erweitert):
```typescript
interface Event {
  // ... bestehende Felder
  mandatory?: boolean;        // NEU: true = Pflicht-Event
  bring_items?: string;       // NEU: Was mitbringen (nullable)
}
```

Bestehende Event-Interface in EventModal.tsx (Zeile 32-49):
```typescript
interface Event {
  id: number;
  name: string;
  description?: string;
  event_date: string;
  event_end_time?: string;
  location?: string;
  points: number;
  point_type?: 'gottesdienst' | 'gemeinde';
  category?: string;
  categories?: Category[];
  jahrgaenge?: Jahrgang[];
  type: string;
  max_participants: number;
  registration_opens_at?: string;
  registration_closes_at?: string;
  has_timeslots?: boolean;
}
```

DashboardEvent-Interface in frontend/src/types/dashboard.ts:
```typescript
export interface DashboardEvent {
  id: number;
  title?: string;
  name?: string;
  description?: string;
  event_date: string;
  date?: string;
  location?: string;
  is_registered?: boolean;
  booking_status?: string;
  // ... weitere Felder
}
```

Felder die bei mandatory=true AUSGEBLENDET werden muessen:
- points (IonInput)
- point_type (IonSelect)
- max_participants (IonInput)
- registration_opens_at / registration_closes_at (IonDatetime)
- waitlist_enabled / max_waitlist_size (IonToggle/IonInput)
- has_timeslots + timeslots-Bereich

Felder die bei mandatory=true SICHTBAR bleiben:
- name, description, event_date, event_end_time, location, location_maps_url
- category_ids, jahrgang_ids (PFLICHT bei mandatory!)
- bring_items (NEU)

WICHTIG aus CLAUDE.md: KEINE UNICODE EMOJIS! Nur IonIcon mit ionicons/icons verwenden.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventModal um mandatory-Toggle und bring_items erweitern</name>
  <files>frontend/src/components/admin/modals/EventModal.tsx</files>
  <action>
  **A) Event-Interface erweitern (Zeile 32-49):**
  Zwei neue optionale Felder hinzufuegen:
  ```typescript
  mandatory?: boolean;
  bring_items?: string;
  ```

  **B) State-Variablen hinzufuegen (im Component-Body):**
  ```typescript
  const [mandatory, setMandatory] = useState(event?.mandatory || false);
  const [bringItems, setBringItems] = useState(event?.bring_items || '');
  ```

  **C) Pflicht-Event-Toggle einfuegen (vor dem Punkte-Bereich):**
  Ein IonItem mit IonToggle fuer "Pflicht-Event". Label: "Pflicht-Event". Position: Nach den Basis-Feldern (Name, Beschreibung, Datum, Ort), vor den Punkte-Feldern.
  ```tsx
  <IonItem>
    <IonLabel>Pflicht-Event</IonLabel>
    <IonToggle
      checked={mandatory}
      onIonChange={(e) => setMandatory(e.detail.checked)}
    />
  </IonItem>
  ```

  **D) bring_items-Feld einfuegen (nach dem Toggle, immer sichtbar):**
  ```tsx
  <IonItem>
    <IonLabel position="stacked">Was mitbringen (optional)</IonLabel>
    <IonTextarea
      value={bringItems}
      onIonInput={(e) => setBringItems(e.detail.value || '')}
      placeholder="z.B. Bibel, Stift, Block"
      rows={2}
    />
  </IonItem>
  ```

  **E) Felder bei mandatory=true ausblenden:**
  Die folgenden Bereiche mit `{!mandatory && ( ... )}` wrappen:
  - Punkte-Eingabe (points IonInput)
  - Punkte-Typ-Auswahl (point_type IonSelect)
  - Max-Teilnehmer-Eingabe (max_participants IonInput)
  - Registrierungszeitraum (registration_opens_at, registration_closes_at)
  - Warteliste-Toggle und -Groesse (waitlist_enabled, max_waitlist_size)
  - Timeslot-Bereich (has_timeslots Toggle und Timeslot-Liste)

  **F) Jahrgang als Pflichtfeld bei mandatory:**
  Wenn mandatory=true und keine jahrgang_ids ausgewaehlt: Den Speichern-Button deaktivieren oder eine Fehlermeldung anzeigen. Am einfachsten: Jahrgang-Auswahl visuell als Pflicht markieren (z.B. Label mit Sternchen: "Jahrgaenge *").

  **G) Submit-Daten erweitern:**
  Beim API-Call (api.post/api.put) die Felder `mandatory` und `bring_items` mitsenden.
  Bei mandatory=true folgende Werte erzwingen (Frontend-seitig, Backend hat eigene Guards):
  - points: 0
  - max_participants: 0
  - waitlist_enabled: false
  - has_timeslots: false

  **H) Edit-Modus:**
  Beim Laden eines bestehenden Events (event prop) muessen mandatory und bring_items korrekt initialisiert werden (bereits durch useState-Default abgedeckt).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "mandatory" frontend/src/components/admin/modals/EventModal.tsx && grep -q "bring_items" frontend/src/components/admin/modals/EventModal.tsx && grep -q "Pflicht-Event" frontend/src/components/admin/modals/EventModal.tsx && grep -q "Was mitbringen" frontend/src/components/admin/modals/EventModal.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>EventModal hat mandatory-Toggle, bring_items-Textarea, Feld-Ausblendung bei mandatory, Pflicht-Jahrgang-Hinweis, und sendet beide Felder an die API</done>
</task>

<task type="auto">
  <name>Task 2: EventDetailViews und EventsView um Pflicht-Anzeige und bring_items erweitern</name>
  <files>frontend/src/components/admin/views/EventDetailView.tsx, frontend/src/components/konfi/views/EventDetailView.tsx, frontend/src/components/konfi/views/EventsView.tsx, frontend/src/types/dashboard.ts</files>
  <action>
  **A) DashboardEvent-Interface erweitern (frontend/src/types/dashboard.ts):**
  Zwei neue Felder hinzufuegen:
  ```typescript
  mandatory?: boolean;
  bring_items?: string;
  ```

  **B) Admin EventDetailView (frontend/src/components/admin/views/EventDetailView.tsx):**
  1. Pflicht-Badge: Wenn `event.mandatory === true`, einen visuellen Indikator anzeigen. Entweder als IonChip/IonBadge mit "Pflicht" oder ein IonItem mit IonIcon (alertCircleOutline oder shieldCheckmarkOutline aus ionicons/icons) und Text "Pflicht-Event". KEINE EMOJIS!
  2. bring_items-Anzeige: Wenn `event.bring_items` vorhanden und nicht leer, ein IonItem mit Icon (bagHandleOutline aus ionicons/icons) und dem Text anzeigen. Label: "Was mitbringen", darunter der Text. Position: Im Event-Details-Bereich, nach Location.
  3. Sicherstellen dass das Event-Interface in dieser Datei `mandatory` und `bring_items` enthalt (lokal definiert oder importiert).

  **C) Konfi EventDetailView (frontend/src/components/konfi/views/EventDetailView.tsx):**
  1. Pflicht-Badge: Gleicher visueller Indikator wie in Admin-View -- "Pflicht" Label.
  2. bring_items-Anzeige: Gleiche Darstellung wie in Admin-View. Wenn `event.bring_items` vorhanden, IonItem mit Icon und Text.
  3. Sicherstellen dass das Event-Interface `mandatory` und `bring_items` enthalt.
  4. Bei Pflicht-Events: Den "Anmelden"/"Abmelden"-Button NICHT anzeigen (Konfi ist automatisch enrolled, Opt-out kommt in Phase 35). Stattdessen einen Hinweis anzeigen: "Du bist automatisch angemeldet" (IonNote oder IonText mit color="medium").

  **D) Konfi EventsView (frontend/src/components/konfi/views/EventsView.tsx):**
  1. In der Event-Liste: Wenn ein Event `mandatory === true` hat, einen kleinen Pflicht-Indikator anzeigen. Das kann ein IonBadge mit "Pflicht" sein, ein Icon (shieldCheckmarkOutline), oder ein CSS-Akzent.
  2. Das Event-Interface in dieser Datei um `mandatory` und `bring_items` erweitern.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "mandatory" frontend/src/components/admin/views/EventDetailView.tsx && grep -q "bring_items" frontend/src/components/admin/views/EventDetailView.tsx && grep -q "mandatory" frontend/src/components/konfi/views/EventDetailView.tsx && grep -q "bring_items" frontend/src/components/konfi/views/EventDetailView.tsx && grep -q "mandatory" frontend/src/components/konfi/views/EventsView.tsx && grep -q "mandatory" frontend/src/types/dashboard.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Beide EventDetailViews zeigen Pflicht-Badge und bring_items an, Konfi-EventsView hat Pflicht-Indikator, Konfi-EventDetailView versteckt Anmelde-Button bei Pflicht-Events, DashboardEvent-Type erweitert</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Verifikation Pflicht-Event-UI</name>
  <action>Human verifies the UI changes visually</action>
  <what-built>Pflicht-Event-System: EventModal mit mandatory-Toggle und bring_items-Feld, Feld-Ausblendung bei Pflicht-Events, Pflicht-Anzeige in Detail-Views und Event-Listen</what-built>
  <how-to-verify>
    1. Frontend starten: `cd frontend && npm run dev`
    2. Als Admin einloggen
    3. Event erstellen -> "Pflicht-Event" Toggle aktivieren
    4. Pruefen: Punkte, Max-Teilnehmer, Warteliste, Registrierungszeitraum und Timeslots werden ausgeblendet
    5. "Was mitbringen" Feld ausfuellen (z.B. "Bibel, Stift")
    6. Jahrgang auswaehlen und Event speichern
    7. Event in der Admin-Detail-Ansicht oeffnen -> Pflicht-Badge und "Was mitbringen" sichtbar
    8. Als Konfi einloggen -> Event-Liste pruefen: Pflicht-Indikator sichtbar
    9. Event-Detail oeffnen: "Was mitbringen" und "automatisch angemeldet" Hinweis sichtbar, kein Anmelde-Button
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. EventModal: mandatory-Toggle vorhanden, bring_items-Textarea vorhanden
2. EventModal: Bei mandatory=true werden 6+ Felder ausgeblendet
3. EventModal: Sendet mandatory und bring_items an API
4. Admin EventDetailView: Pflicht-Badge und bring_items sichtbar
5. Konfi EventDetailView: Pflicht-Badge, bring_items, "automatisch angemeldet"-Hinweis
6. Konfi EventsView: Pflicht-Indikator in Event-Liste
7. DashboardEvent-Type: mandatory und bring_items Felder vorhanden
</verification>

<success_criteria>
- EventModal zeigt bei mandatory=true nur relevante Felder (Name, Datum, Ort, Jahrgang, bring_items)
- bring_items wird in beiden Detail-Views angezeigt wenn vorhanden
- Konfi sieht Pflicht-Indikator in Event-Liste und "automatisch angemeldet" in Detail-View
- Keine Unicode-Emojis verwendet -- nur IonIcons
</success_criteria>

<output>
After completion, create `.planning/phases/34-pflicht-event-grundlagen/34-02-SUMMARY.md`
</output>
