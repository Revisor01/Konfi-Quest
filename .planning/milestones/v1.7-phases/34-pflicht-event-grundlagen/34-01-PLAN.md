---
phase: 34-pflicht-event-grundlagen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - init-scripts/01-create-schema.sql
  - backend/routes/events.js
  - backend/routes/auth.js
  - backend/routes/konfi-managment.js
autonomous: true
requirements: [PFL-01, PFL-02, PFL-03, PFL-04, EUI-01]

must_haves:
  truths:
    - "POST /api/events mit mandatory=true erstellt ein Event mit points=0 und enrolled automatisch alle Jahrgangs-Konfis"
    - "PUT /api/events/:id mit mandatory=true erzwingt points=0 und stellt max_participants irrelevant"
    - "GET /api/events gibt mandatory und bring_items Felder zurueck"
    - "Neue Konfis die einem Jahrgang zugewiesen werden, werden automatisch in zukuenftige Pflicht-Events enrolled"
    - "Push-Benachrichtigung wird an alle enrollten Konfis gesendet bei Pflicht-Event-Erstellung"
  artifacts:
    - path: "init-scripts/01-create-schema.sql"
      provides: "mandatory und bring_items Spalten auf events, CHECK-Constraint-Fix auf event_bookings.status"
      contains: "mandatory BOOLEAN"
    - path: "backend/routes/events.js"
      provides: "Pflicht-Event CRUD mit Auto-Enrollment und Punkte-Guard"
      contains: "mandatory"
    - path: "backend/routes/auth.js"
      provides: "Nachtrags-Enrollment bei Konfi-Registrierung"
      contains: "Auto-enrollment"
    - path: "backend/routes/konfi-managment.js"
      provides: "Nachtrags-Enrollment bei Jahrgang-Wechsel"
      contains: "Auto-enrollment"
  key_links:
    - from: "backend/routes/events.js"
      to: "event_bookings"
      via: "Batch-INSERT mit ON CONFLICT DO NOTHING"
      pattern: "INSERT INTO event_bookings.*SELECT.*mandatory"
    - from: "backend/routes/auth.js"
      to: "event_bookings"
      via: "Nachtrags-Enrollment nach Konfi-Registrierung"
      pattern: "mandatory.*true.*event_date.*NOW"
    - from: "backend/routes/events.js"
      to: "pushService"
      via: "Push an enrollte Konfis"
      pattern: "sendToMultipleUsers\\|sendNewEventToOrgKonfis"
---

<objective>
DB-Schema um mandatory/bring_items erweitern, Event-CRUD anpassen mit Auto-Enrollment, Punkte-Guard einbauen, Nachtrags-Enrollment-Hooks und Push-Benachrichtigungen implementieren.

Purpose: Grundlage fuer Pflicht-Events schaffen -- ohne Backend-Logik funktioniert kein Frontend.
Output: Funktionierende API fuer Pflicht-Event-Erstellung mit Auto-Enrollment und Push.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-pflicht-event-grundlagen/34-RESEARCH.md

<interfaces>
<!-- Bestehende Patterns die der Executor braucht -->

From init-scripts/01-create-schema.sql (Zeile 289-312):
```sql
CREATE TABLE events (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    event_date TIMESTAMP NOT NULL,
    event_end_time TIMESTAMP,
    location VARCHAR(255),
    location_maps_url TEXT,
    points INTEGER DEFAULT 0 CHECK (points >= 0),
    point_type VARCHAR(20) DEFAULT 'gemeinde' CHECK (point_type IN ('gottesdienst', 'gemeinde')),
    type VARCHAR(50) DEFAULT 'event',
    max_participants INTEGER NOT NULL CHECK (max_participants > 0),
    -- ... weitere Felder
    organization_id INTEGER NOT NULL REFERENCES organizations(id) ON DELETE CASCADE
);
```

From init-scripts/01-create-schema.sql (Zeile 382-392):
```sql
CREATE TABLE event_bookings (
    -- ...
    status VARCHAR(20) DEFAULT 'confirmed' CHECK (status IN ('confirmed', 'pending', 'cancelled')),
    attendance_status VARCHAR(20) CHECK (attendance_status IN ('present', 'absent')),
    -- ...
);
```

From backend/routes/events.js (Zeile 364-397) -- POST route destructuring:
```javascript
const {
  name, description, event_date, event_end_time, location, location_maps_url,
  points, point_type, category_ids, jahrgang_ids, type, max_participants,
  registration_opens_at, registration_closes_at, has_timeslots,
  waitlist_enabled, max_waitlist_size, timeslots, is_series, series_id
} = req.body;
```

From backend/routes/events.js (Zeile 1314) -- bestehender Punkte-Guard:
```javascript
if (attendance_status === 'present' && eventData.points > 0) {
  // Punkte vergeben
}
```

From backend/routes/auth.js (Zeile 580-598) -- Konfi-Registrierung:
```javascript
const client = await db.connect();
await client.query('BEGIN');
// INSERT INTO users ... RETURNING id
// INSERT INTO konfi_profiles (user_id, jahrgang_id, ...) VALUES ($1, $2, ...)
await client.query('COMMIT');
// Push-Notification nach COMMIT
```

From backend/routes/konfi-managment.js (Zeile 232-303) -- Jahrgang-Wechsel:
```javascript
const client = await db.connect();
await client.query('BEGIN');
// currentProfile.jahrgang_id !== parseInt(jahrgang_id)
// UPDATE konfi_profiles SET jahrgang_id = $1 WHERE user_id = $2
await client.query('COMMIT');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Schema erweitern und Constraints fixen</name>
  <files>init-scripts/01-create-schema.sql</files>
  <action>
  In `init-scripts/01-create-schema.sql` folgende Aenderungen vornehmen:

  1. **events-Tabelle erweitern** (nach Zeile 311, vor dem schliessenden Semikolon):
     - `mandatory BOOLEAN DEFAULT false` hinzufuegen
     - `bring_items TEXT` hinzufuegen (nullable, fuer alle Events verfuegbar)

  2. **max_participants Constraint aendern** (Zeile 300):
     - VON: `max_participants INTEGER NOT NULL CHECK (max_participants > 0)`
     - ZU: `max_participants INTEGER NOT NULL DEFAULT 0 CHECK (max_participants >= 0)`
     - Bedeutung: 0 = unbegrenzte Teilnehmer (konsistent mit bestehender Logik in events.js Zeile 798)

  3. **event_bookings.status CHECK-Constraint aktualisieren** (Zeile 387):
     - VON: `CHECK (status IN ('confirmed', 'pending', 'cancelled'))`
     - ZU: `CHECK (status IN ('confirmed', 'waitlist', 'cancelled', 'opted_out'))`
     - 'pending' entfernen (wird nirgends im Code verwendet)
     - 'waitlist' hinzufuegen (wird bereits im Code verwendet aber fehlt im Schema)
     - 'opted_out' hinzufuegen (wird in Phase 35 benoetigt, Constraint-Aenderung aber jetzt vorbereiten)

  WICHTIG: Die Schema-Datei ist die Referenz fuer Fresh-Setups (Docker). Die Live-DB wird per SSH-Migration aktualisiert -- das ist NICHT Teil dieser Aufgabe, sondern wird vom Executor nach dem Plan erledigt (Deployment-Schritt).

  Die Live-DB Migration-Befehle als Kommentar am Ende der Schema-Datei dokumentieren:
  ```sql
  -- Migration v1.7 Phase 34 (auf Live-DB ausfuehren):
  -- ALTER TABLE events ADD COLUMN IF NOT EXISTS mandatory BOOLEAN DEFAULT false;
  -- ALTER TABLE events ADD COLUMN IF NOT EXISTS bring_items TEXT;
  -- ALTER TABLE events ALTER COLUMN max_participants SET DEFAULT 0;
  -- ALTER TABLE events DROP CONSTRAINT IF EXISTS events_max_participants_check;
  -- ALTER TABLE events ADD CONSTRAINT events_max_participants_check CHECK (max_participants >= 0);
  -- ALTER TABLE event_bookings DROP CONSTRAINT IF EXISTS event_bookings_status_check;
  -- ALTER TABLE event_bookings ADD CONSTRAINT event_bookings_status_check CHECK (status IN ('confirmed', 'waitlist', 'cancelled', 'opted_out'));
  ```
  </action>
  <verify>
    <automated>grep -q "mandatory BOOLEAN" init-scripts/01-create-schema.sql && grep -q "bring_items TEXT" init-scripts/01-create-schema.sql && grep -q "max_participants >= 0" init-scripts/01-create-schema.sql && grep -q "opted_out" init-scripts/01-create-schema.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>events-Tabelle hat mandatory und bring_items Spalten, max_participants erlaubt 0, event_bookings.status CHECK enthalt waitlist und opted_out</done>
</task>

<task type="auto">
  <name>Task 2: Backend Event-CRUD erweitern mit Auto-Enrollment, Punkte-Guard und Push</name>
  <files>backend/routes/events.js, backend/routes/auth.js, backend/routes/konfi-managment.js</files>
  <action>
  **A) events.js -- POST / Route erweitern (Zeile 364-444):**

  1. `mandatory` und `bring_items` zur destructuring-Liste hinzufuegen (Zeile 366-371)
  2. Validierung: `if (mandatory && (!jahrgang_ids || jahrgang_ids.length === 0))` return 400 mit Fehlermeldung "Pflicht-Events benoetigen mindestens einen Jahrgang"
  3. Bei mandatory: `max_participants` nicht als Pflichtfeld pruefen (Zeile 373 anpassen). Die Bedingung aendern zu: `if (!name || !event_date || (!mandatory && !max_participants))`
  4. Punkte-Guard: `const effectivePoints = mandatory ? 0 : (points || 0);`
  5. Max-Participants-Guard: `const effectiveMaxParticipants = mandatory ? 0 : max_participants;`
  6. Waitlist-Guard: `const effectiveWaitlist = mandatory ? false : (waitlist_enabled !== undefined ? waitlist_enabled : true);`
  7. INSERT-Query um `mandatory` und `bring_items` erweitern (2 neue Spalten + Werte)
  8. **Auto-Enrollment NACH dem Jahrgang-Insert und NACH `Promise.all(promises)`:**
     ```javascript
     // Auto-Enrollment fuer Pflicht-Events
     if (mandatory && jahrgang_ids && jahrgang_ids.length > 0) {
       const enrollQuery = `
         INSERT INTO event_bookings (event_id, user_id, status, booking_date, organization_id)
         SELECT $1, u.id, 'confirmed', NOW(), $3
         FROM users u
         JOIN konfi_profiles kp ON u.id = kp.user_id
         JOIN roles r ON u.role_id = r.id
         WHERE kp.jahrgang_id = ANY($2::int[])
           AND u.organization_id = $3
           AND r.name = 'konfi'
         ON CONFLICT (user_id, event_id) DO NOTHING
       `;
       await db.query(enrollQuery, [eventId, jahrgang_ids, req.user.organization_id]);
     }
     ```
  9. **Jahrgangs-spezifische Push statt Org-weite Push bei Pflicht-Events:**
     Bei mandatory=true: User-IDs aus dem Enrollment-Query extrahieren und `PushService.sendToMultipleUsers()` verwenden.
     Bei normalen Events: Bestehende `sendNewEventToOrgKonfis()` beibehalten.
     ```javascript
     if (mandatory && jahrgang_ids && jahrgang_ids.length > 0) {
       // Push nur an tatsaechlich enrollte Konfis (jahrgangs-spezifisch)
       const { rows: enrolledUsers } = await db.query(`
         SELECT u.id FROM users u
         JOIN konfi_profiles kp ON u.id = kp.user_id
         JOIN roles r ON u.role_id = r.id
         WHERE kp.jahrgang_id = ANY($1::int[])
           AND u.organization_id = $2
           AND r.name = 'konfi'
       `, [jahrgang_ids, req.user.organization_id]);

       if (enrolledUsers.length > 0) {
         const userIds = enrolledUsers.map(u => u.id);
         await PushService.sendToMultipleUsers(db, userIds, {
           title: 'Neues Pflicht-Event',
           body: `${name} am ${new Date(event_date).toLocaleDateString('de-DE')}`,
           data: { type: 'mandatory_event_created', eventId: String(eventId) }
         });
       }
     } else {
       await PushService.sendNewEventToOrgKonfis(db, req.user.organization_id, name, event_date);
     }
     ```

  **B) events.js -- PUT /:id Route erweitern (Zeile 447-539):**

  1. `mandatory` und `bring_items` zur destructuring-Liste hinzufuegen (Zeile 449-454)
  2. Punkte-Guard: `const effectivePoints = mandatory ? 0 : points;`
  3. UPDATE-Query um `mandatory = $X, bring_items = $Y` erweitern
  4. Bei Event-Update von non-mandatory zu mandatory: Auto-Enrollment ausfuehren (gleiche Query wie in POST), aber NUR wenn `mandatory` sich tatsaechlich geaendert hat. Dafuer VOR dem Update den alten mandatory-Wert lesen:
     ```javascript
     const { rows: [oldEvent] } = await db.query('SELECT mandatory FROM events WHERE id = $1', [id]);
     // ... nach UPDATE und Jahrgang-Inserts:
     if (mandatory && !oldEvent.mandatory && jahrgang_ids && jahrgang_ids.length > 0) {
       // Auto-Enrollment bei Umwandlung zu Pflicht-Event
       await db.query(enrollQuery, [id, jahrgang_ids, req.user.organization_id]);
     }
     ```

  **C) events.js -- GET Queries erweitern:**
  Alle SELECT-Queries die Events laden (GET /, GET /:id, etc.) muessen `e.mandatory` und `e.bring_items` im SELECT enthalten. Durchsuche alle SELECT-Queries in events.js und fuege die Felder hinzu.

  **D) events.js -- Attendance-Route (Zeile 1288-1334):**
  Die bestehende eventDataQuery (Zeile 1300) um `e.mandatory` erweitern. Der Punkte-Guard (Zeile 1314) wird automatisch durch points=0 geschuetzt, aber ein expliziter Guard ist sicherer:
  ```javascript
  if (attendance_status === 'present' && eventData.points > 0 && !eventData.mandatory) {
    // Punkte vergeben
  }
  ```

  **E) auth.js -- Nachtrags-Enrollment (nach Zeile 598, nach COMMIT):**
  Nach der Push-Notification fuer neue Registrierung, VOR dem JWT-Token-Generieren:
  ```javascript
  // Auto-Enrollment fuer zukuenftige Pflicht-Events
  try {
    const enrollFutureEventsQuery = `
      INSERT INTO event_bookings (event_id, user_id, status, booking_date, organization_id)
      SELECT e.id, $1, 'confirmed', NOW(), $2
      FROM events e
      JOIN event_jahrgang_assignments eja ON e.id = eja.event_id
      WHERE eja.jahrgang_id = $3
        AND e.mandatory = true
        AND e.event_date > NOW()
        AND e.organization_id = $2
        AND e.cancelled IS NOT TRUE
      ON CONFLICT (user_id, event_id) DO NOTHING
    `;
    await db.query(enrollFutureEventsQuery, [newUser.id, invite.organization_id, invite.jahrgang_id]);
  } catch (enrollErr) {
    console.error('Auto-enrollment fuer Pflicht-Events fehlgeschlagen:', enrollErr);
  }
  ```
  WICHTIG: db.query verwenden (nicht client.query), da dies NACH dem COMMIT passiert.

  **F) konfi-managment.js -- Nachtrags-Enrollment bei Jahrgang-Wechsel (nach Zeile 303, nach COMMIT):**
  Nur wenn der Jahrgang sich geaendert hat (gleiche Bedingung wie der Chat-Membership-Check Zeile 262):
  ```javascript
  // Auto-Enrollment fuer zukuenftige Pflicht-Events des neuen Jahrgangs
  if (currentProfile && currentProfile.jahrgang_id !== parseInt(jahrgang_id)) {
    try {
      const enrollFutureEventsQuery = `
        INSERT INTO event_bookings (event_id, user_id, status, booking_date, organization_id)
        SELECT e.id, $1, 'confirmed', NOW(), $2
        FROM events e
        JOIN event_jahrgang_assignments eja ON e.id = eja.event_id
        WHERE eja.jahrgang_id = $3
          AND e.mandatory = true
          AND e.event_date > NOW()
          AND e.organization_id = $2
          AND e.cancelled IS NOT TRUE
        ON CONFLICT (user_id, event_id) DO NOTHING
      `;
      await db.query(enrollFutureEventsQuery, [req.params.id, req.user.organization_id, jahrgang_id]);
    } catch (enrollErr) {
      console.error('Auto-enrollment fuer Pflicht-Events fehlgeschlagen:', enrollErr);
    }
  }
  ```
  WICHTIG: db.query verwenden (nicht client.query), da dies NACH dem COMMIT passiert. Die Variable `jahrgang_id` und `req.params.id` sind im Scope der PUT-Route verfuegbar.

  **G) events.js -- Validierungsregeln erweitern (Zeile 14-25):**
  Optional: `body('mandatory').optional().isBoolean()` und `body('bring_items').optional().isString()` zu beiden Validierungsarrays hinzufuegen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "mandatory" backend/routes/events.js && grep -q "bring_items" backend/routes/events.js && grep -q "Auto-enrollment" backend/routes/auth.js && grep -q "Auto-enrollment" backend/routes/konfi-managment.js && grep -q "effectivePoints" backend/routes/events.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>POST/PUT /events akzeptieren mandatory und bring_items, Auto-Enrollment wird bei mandatory Events ausgefuehrt, Punkte werden bei mandatory Events auf 0 erzwungen, Nachtrags-Enrollment in auth.js und konfi-managment.js aktiv, Push wird jahrgangs-spezifisch gesendet</done>
</task>

</tasks>

<verification>
1. Schema-Datei enthaelt mandatory, bring_items, max_participants >= 0, status-Constraint mit waitlist/opted_out
2. events.js POST-Route: mandatory + bring_items in destructuring, Auto-Enrollment-Query, Punkte-Guard
3. events.js PUT-Route: mandatory + bring_items in UPDATE, Punkte-Guard, bedingtes Auto-Enrollment
4. events.js GET-Queries: mandatory und bring_items in SELECT-Feldern
5. events.js Attendance-Route: mandatory-Guard bei Punkte-Vergabe
6. auth.js: Nachtrags-Enrollment nach Konfi-Registrierung
7. konfi-managment.js: Nachtrags-Enrollment nach Jahrgang-Wechsel
</verification>

<success_criteria>
- `grep -c "mandatory" backend/routes/events.js` >= 5
- `grep -c "bring_items" backend/routes/events.js` >= 3
- `grep -c "Auto-enrollment" backend/routes/auth.js` >= 1
- `grep -c "Auto-enrollment" backend/routes/konfi-managment.js` >= 1
- Schema enthalt alle neuen Spalten und gefixte Constraints
</success_criteria>

<output>
After completion, create `.planning/phases/34-pflicht-event-grundlagen/34-01-SUMMARY.md`
</output>
