---
phase: 37-dashboard-widget-anwesenheitsstatistik
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi-managment.js
autonomous: true
requirements: [ANW-01, ANW-02]

must_haves:
  truths:
    - "GET /admin/konfis/:id/attendance-stats liefert total_mandatory, attended, percentage und missed_events Array"
    - "Nur vergangene Pflicht-Events (mandatory=true, event_date < NOW()) werden gezaehlt"
    - "missed_events unterscheidet zwischen opted_out (mit opt_out_reason) und absent/no_show"
    - "Endpoint ist nur fuer Admins zugaenglich (verifyTokenRBAC + type-Check)"
  artifacts:
    - backend/routes/konfi-managment.js
  key_links:
    - "Endpoint nutzt event_jahrgang_assignments um Pflicht-Events des Konfi-Jahrgangs zu finden"
    - "LEFT JOIN auf event_bookings um auch Events ohne Booking als 'nicht erschienen' zu zaehlen"
---

<objective>
Backend-Endpoint fuer pro-Konfi Anwesenheitsstatistik bei Pflicht-Events.

Purpose: Admin braucht einen Endpoint der die Anwesenheitsquote und verpasste Pflicht-Events eines Konfis berechnet.
Output: Neuer GET /admin/konfis/:id/attendance-stats Endpoint in konfi-managment.js
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dashboard-widget-anwesenheitsstatistik/37-RESEARCH.md
@backend/routes/konfi-managment.js
@init-scripts/01-create-schema.sql
</context>

<interfaces>
<!-- Bestehende DB-Struktur (aus Schema und Phase 34-36): -->

events: id, name, event_date, location, mandatory (boolean), bring_items (text), organization_id, cancelled
event_bookings: id, user_id, event_id, status (confirmed|waitlist|cancelled|opted_out), attendance_status (present|absent|NULL), opt_out_reason (text), opt_out_date
event_jahrgang_assignments: event_id, jahrgang_id
konfi_profiles: user_id, jahrgang_id
users: id, display_name, role_id, organization_id

<!-- Bestehender Endpoint-Pattern (aus konfi-managment.js): -->
router.get('/konfis/:id/event-points', verifyTokenRBAC, async (req, res) => { ... });
<!-- Nutzt req.user.type Check, req.user.organization_id, req.params.id -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Attendance-Stats Endpoint erstellen</name>
  <files>backend/routes/konfi-managment.js</files>
  <action>
Neuen Endpoint `GET /konfis/:id/attendance-stats` in konfi-managment.js hinzufuegen (nach dem bestehenden `/konfis/:id/event-points` Endpoint).

**Admin-Check:**
```javascript
if (req.user.type !== 'admin' && req.user.type !== 'super_admin') {
  return res.status(403).json({ error: 'Admin-Zugriff erforderlich' });
}
```

**SQL-Query (ein einzelner Query mit allem):**

1. Finde den Jahrgang des Konfis ueber konfi_profiles
2. Finde alle vergangenen Pflicht-Events (mandatory=true, event_date < NOW(), cancelled IS NOT TRUE) die diesem Jahrgang zugewiesen sind (via event_jahrgang_assignments)
3. LEFT JOIN auf event_bookings fuer diesen Konfi
4. Zaehle:
   - total_mandatory: Anzahl dieser Events
   - attended: Anzahl mit attendance_status = 'present'
5. Sammle missed_events: Events wo attendance_status != 'present' (also absent, NULL, oder opted_out)

**Empfohlene Query-Struktur:**

```sql
-- Erst Jahrgang holen
SELECT jahrgang_id FROM konfi_profiles WHERE user_id = $1

-- Dann Stats berechnen
SELECT
  e.id as event_id,
  e.name as event_name,
  e.event_date,
  e.location,
  eb.status as booking_status,
  eb.attendance_status,
  eb.opt_out_reason
FROM events e
JOIN event_jahrgang_assignments eja ON e.id = eja.event_id
LEFT JOIN event_bookings eb ON e.id = eb.event_id AND eb.user_id = $1
WHERE eja.jahrgang_id = $2
  AND e.mandatory = true
  AND e.event_date < NOW()
  AND (e.cancelled IS NOT TRUE)
  AND e.organization_id = $3
ORDER BY e.event_date DESC
```

**Response berechnen:**
```javascript
const total_mandatory = rows.length;
const attended = rows.filter(r => r.attendance_status === 'present').length;
const percentage = total_mandatory > 0 ? Math.round((attended / total_mandatory) * 100) : 100;

const missed_events = rows
  .filter(r => r.attendance_status !== 'present')
  .map(r => ({
    event_id: r.event_id,
    event_name: r.event_name,
    event_date: r.event_date,
    location: r.location,
    status: r.booking_status === 'opted_out' ? 'opted_out' : 'absent',
    opt_out_reason: r.booking_status === 'opted_out' ? r.opt_out_reason : null
  }));

res.json({ total_mandatory, attended, percentage, missed_events });
```

**Error-Handling:** try/catch mit console.error und 500-Response, wie bei den anderen Endpoints.
  </action>
  <verify>
    <automated>cd backend && node -e "const r = require('./routes/konfi-managment.js'); console.log('Route loaded OK')" 2>&1 | head -5</automated>
  </verify>
  <done>
    - GET /admin/konfis/:id/attendance-stats gibt JSON mit total_mandatory, attended, percentage, missed_events zurueck
    - Nur vergangene Pflicht-Events werden gezaehlt (mandatory=true, event_date < NOW(), nicht cancelled)
    - missed_events enthaelt status "opted_out" (mit opt_out_reason) oder "absent"
    - Sortierung: neueste Events zuerst
    - Admin-only Zugriff
  </done>
</task>

</tasks>

<verification>
- Endpoint ist erreichbar und gibt korrektes JSON-Format zurueck
- Bei Konfi ohne Pflicht-Events: total_mandatory=0, attended=0, percentage=100, missed_events=[]
- Bei Konfi mit gemischter Anwesenheit: korrekte Zaehlung und Prozentberechnung
- Zukuenftige Pflicht-Events werden NICHT mitgezaehlt
- Abgesagte Events (cancelled=true) werden NICHT mitgezaehlt
</verification>

<success_criteria>
Backend-Endpoint liefert pro-Konfi Anwesenheitsstatistik fuer Pflicht-Events mit Quote und verpassten Events.
</success_criteria>

<output>
After completion, create `.planning/phases/37-dashboard-widget-anwesenheitsstatistik/37-01-SUMMARY.md`
</output>
