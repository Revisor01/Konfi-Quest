---
phase: 20-event-logik-debug
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/konfi.js
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03, EVT-04, EVT-07]

must_haves:
  truths:
    - "konfi.js verwendet einheitlich 'waitlist' Status (nicht 'pending' und nicht 'waitlist' gemischt)"
    - "POST /events/:id/register prueft registration_opens_at und registration_closes_at"
    - "DELETE /events/:id/register loest Nachruecken aus (Warteliste -> confirmed)"
    - "DELETE /events/:id/register: Bei Timeslot-Events rueckt nur innerhalb desselben Timeslots nach"
    - "Doppelte Booking-Route ist dokumentiert oder konsolidiert"
  artifacts:
    - path: "backend/routes/konfi.js"
      provides: "Korrigierte Konfi-Event-Logik mit Registrierungsfenster-Pruefung und Nachruecken"
  key_links:
    - from: "POST /events/:id/register"
      to: "events.registration_opens_at"
      via: "Date-Vergleich vor Buchung"
      pattern: "registration_opens_at"
    - from: "DELETE /events/:id/register"
      to: "event_bookings"
      via: "Waitlist-Promotion nach Stornierung"
      pattern: "waitlist.*confirmed"
---

<objective>
Korrektur der Konfi-Event-Logik in konfi.js: Registrierungsfenster-Pruefung, Nachrueck-Logik bei Stornierung, einheitlicher Wartelisten-Status und Konsolidierung mit events.js.

Purpose: Die Konfi-Buchungsroute hat kritische Luecken (keine Zeitfenster-Pruefung, kein Nachruecken bei Stornierung) die behoben werden muessen
Output: Korrigierte backend/routes/konfi.js
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/konfi.js
</context>

<interfaces>
<!-- konfi.js hat eigene Event-Routes unter /konfi Prefix: -->
<!-- GET /events - Konfi Event-Liste (Zeile ~1070) -->
<!-- GET /events/:id/status - Event-Status fuer Konfi (Zeile ~1174) -->
<!-- GET /events/:id/timeslots - Timeslots fuer ein Event (Zeile ~1460) -->
<!-- POST /events/:id/register - Konfi bucht Event (Zeile ~1496) -->
<!-- DELETE /events/:id/register - Konfi storniert (Zeile ~1647) -->

<!-- events.js hat parallele Routes unter /events Prefix: -->
<!-- POST /:id/book - Konfi bucht Event (mit Transaktion!) -->
<!-- DELETE /:id/book - Konfi storniert (mit Nachruecken!) -->
<!-- POST /:id/participants - Admin bucht fuer Konfi -->

<!-- PROBLEM: konfi.js POST /events/:id/register hat: -->
<!--   - Keine registration_opens_at/closes_at Pruefung -->
<!--   - Keine Transaktion (Race Conditions moeglich) -->
<!--   - Status 'waitlist' (korrekt), aber events.js benutzte 'pending' (wird in 20-01 gefixt) -->

<!-- PROBLEM: konfi.js DELETE /events/:id/register hat: -->
<!--   - KEIN Nachruecken von Warteliste -->
<!--   - Nur Loeschen + Protokollieren -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Registrierungsfenster-Pruefung und Transaktion in POST /events/:id/register</name>
  <files>backend/routes/konfi.js</files>
  <action>
  In backend/routes/konfi.js den POST /events/:id/register Endpunkt (Zeile ~1496) erweitern:

  1. **Transaktion hinzufuegen** fuer Race-Condition-Schutz:
     - `await db.query('BEGIN');` am Anfang des try-Blocks
     - Event-Query mit `FOR UPDATE` erweitern damit die Zeile gesperrt wird
     - Bei allen Error-Returns: `await db.query('ROLLBACK');` davor einfuegen
     - Nach erfolgreichem INSERT: `await db.query('COMMIT');`
     - Im catch-Block: `await db.query('ROLLBACK');`

  2. **Registration-Fenster pruefen** (EVT-02) -- nach dem Event-Query, vor der Kapazitaetspruefung:
     ```javascript
     const now = new Date();
     if (event.registration_opens_at && now < new Date(event.registration_opens_at)) {
       await db.query('ROLLBACK');
       return res.status(400).json({ error: 'Anmeldung noch nicht geoeffnet' });
     }
     if (event.registration_closes_at && now > new Date(event.registration_closes_at)) {
       await db.query('ROLLBACK');
       return res.status(400).json({ error: 'Anmeldung bereits geschlossen' });
     }
     ```

  3. **Bestehende 'waitlist' Status-Nutzung beibehalten** -- konfi.js verwendet bereits korrekt 'waitlist'. Sicherstellen dass dies so bleibt.

  4. **EVT-07 Dokumentation**: Am Anfang der register-Route einen Kommentar hinzufuegen:
     ```javascript
     // HINWEIS: Dieser Endpunkt ist die primaere Konfi-Buchungsroute.
     // events.js POST /:id/book ist die aeltere Route (wird vom Admin-Frontend genutzt).
     // Beide verwenden einheitlich status='waitlist' fuer Wartelisten-Eintraege.
     ```

  WICHTIG: Die bestehende Konfirmations-Pruefung (isKonfirmation) und Timeslot-Logik NICHT veraendern.
  WICHTIG: Die Push-Notifications und liveUpdate Aufrufe NACH dem COMMIT belassen (nicht innerhalb der Transaktion).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -A5 "Register for event" backend/routes/konfi.js | grep -q "BEGIN" && grep "registration_opens_at\|registration_closes_at" backend/routes/konfi.js | grep -v "SELECT\|WHEN\|GROUP\|LEFT\|CASE" | head -5 && echo "OK"</automated>
  </verify>
  <done>POST /events/:id/register hat Transaktion (BEGIN/COMMIT/ROLLBACK), prueft registration_opens_at und registration_closes_at, und hat Dokumentationskommentar zur Route-Duplizierung.</done>
</task>

<task type="auto">
  <name>Task 2: Nachrueck-Logik in DELETE /events/:id/register und Status-Konsistenz</name>
  <files>backend/routes/konfi.js</files>
  <action>
  In backend/routes/konfi.js den DELETE /events/:id/register Endpunkt (Zeile ~1647) erweitern:

  1. **Nachrueck-Logik nach Stornierung** (EVT-03, EVT-04):
     Nach dem DELETE der Buchung (Zeile ~1691) und VOR dem Unregistration-Logging, wenn die stornierte Buchung status='confirmed' war:

     ```javascript
     // Nachruecken von Warteliste wenn ein bestaetigter Platz frei wird
     if (registration.status === 'confirmed') {
       try {
         // Bei Timeslot-Events nur innerhalb desselben Timeslots nachruecken
         const promotionQuery = registration.timeslot_id
           ? "SELECT id, user_id FROM event_bookings WHERE event_id = $1 AND timeslot_id = $2 AND status = 'waitlist' ORDER BY created_at ASC LIMIT 1"
           : "SELECT id, user_id FROM event_bookings WHERE event_id = $1 AND status = 'waitlist' ORDER BY created_at ASC LIMIT 1";
         const promotionParams = registration.timeslot_id
           ? [eventId, registration.timeslot_id]
           : [eventId];

         const { rows: [nextInLine] } = await db.query(promotionQuery, promotionParams);

         if (nextInLine) {
           await db.query("UPDATE event_bookings SET status = 'confirmed' WHERE id = $1", [nextInLine.id]);

           // Push-Notification an nachgerueckten Konfi
           try {
             const { rows: [promotedEvent] } = await db.query(
               "SELECT name, event_date FROM events WHERE id = $1", [eventId]
             );
             if (promotedEvent) {
               await PushService.sendWaitlistPromotionToKonfi(db, nextInLine.user_id, promotedEvent.name, promotedEvent.event_date, eventId);
             }
           } catch (pushErr) {
             console.error('Error sending waitlist promotion push:', pushErr);
           }

           // Live-Update an nachgerueckten Konfi
           liveUpdate.sendToKonfi(nextInLine.user_id, 'events', 'update');
         }
       } catch (promotionErr) {
         console.error('Error promoting from waitlist:', promotionErr);
         // Stornierung war bereits erfolgreich, Promotion-Fehler nicht an User weitergeben
       }
     }
     ```

  2. **Status-Feld aus registration laden** -- Die bestehende Query `SELECT * FROM event_bookings WHERE user_id = $1 AND event_id = $2` liefert bereits alle Felder inkl. status und timeslot_id. Sicherstellen dass `registration.status` und `registration.timeslot_id` korrekt genutzt werden.

  3. **Waitlist-Status in Event-Liste und Status-Queries vereinheitlichen**:
     - GET /events (Zeile ~1084): Die Query verwendet bereits `status = 'waitlist'` -- korrekt, keine Aenderung noetig.
     - GET /events/:id/status (Zeile ~1174): Verwendet `status = 'waitlist'` -- korrekt.
     - Sicherstellen dass KEINE Stelle in konfi.js `status = 'pending'` fuer Wartelisten verwendet.

  4. **PushService.sendWaitlistPromotionToKonfi pruefen**: Falls diese Methode nicht existiert, stattdessen eine generische Push-Nachricht verwenden:
     ```javascript
     await PushService.sendToKonfi(db, nextInLine.user_id, {
       title: 'Nachgerueckt!',
       body: `Du bist fuer "${promotedEvent.name}" vom Warteliste nachgerueckt und jetzt angemeldet!`
     });
     ```
     Falls auch das nicht existiert, nur liveUpdate verwenden und einen TODO-Kommentar hinterlassen.

  WICHTIG: Die bestehende Storno-Logik (2-Tage-Frist, Unregistration-Logging, Push an Admins) NICHT veraendern.
  WICHTIG: Nachrueck-Logik NACH dem DELETE aber VOR dem Response einfuegen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -A30 "Delete registration" backend/routes/konfi.js | grep -q "status = 'waitlist' ORDER BY created_at ASC" && grep -c "status = 'pending'" backend/routes/konfi.js | grep -E "^0$" && echo "OK"</automated>
  </verify>
  <done>DELETE /events/:id/register loest Nachruecken aus bei confirmed-Stornierung. Bei Timeslot-Events wird nur innerhalb desselben Timeslots nachgerueckt. Kein 'pending' Status in konfi.js.</done>
</task>

</tasks>

<verification>
1. `grep -c "status = 'pending'" backend/routes/konfi.js` ergibt 0
2. `grep "registration_opens_at" backend/routes/konfi.js` findet die Fenster-Pruefung in POST /events/:id/register
3. `grep "BEGIN" backend/routes/konfi.js` zeigt Transaktion in der Register-Route
4. DELETE-Route enthaelt Nachrueck-Logik mit timeslot_id-Pruefung
5. Code-Review: Nachrueck-Logik unterscheidet zwischen Timeslot- und normalen Events
</verification>

<success_criteria>
- POST /events/:id/register prueft registration_opens_at und registration_closes_at
- POST /events/:id/register verwendet Transaktion mit FOR UPDATE
- DELETE /events/:id/register rueckt naechsten Wartelisten-Eintrag nach bei confirmed-Stornierung
- Nachruecken bei Timeslot-Events nur innerhalb desselben Timeslots
- Kein 'pending' Status-Wert in konfi.js (nur 'waitlist')
- Kommentar dokumentiert die Route-Duplizierung (EVT-07)
</success_criteria>

<output>
After completion, create `.planning/phases/20-event-logik-debug/20-02-SUMMARY.md`
</output>
