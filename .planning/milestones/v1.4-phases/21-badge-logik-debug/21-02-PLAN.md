---
phase: 21-badge-logik-debug
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/routes/organizations.js]
autonomous: true
requirements: [BDG-05]

must_haves:
  truths:
    - "Default-Badges bei neuer Organisation enthalten korrekte Umlaute (ue->ue, ae->ae, oe->oe, ss->ss)"
  artifacts:
    - path: "backend/routes/organizations.js"
      provides: "Default-Badges mit korrekten Umlauten"
      contains: "defaultBadges"
  key_links:
    - from: "backend/routes/organizations.js"
      to: "custom_badges Tabelle"
      via: "INSERT INTO custom_badges"
      pattern: "defaultBadges"
---

<objective>
Default-Badges bei Org-Erstellung mit korrekten Umlauten versehen

Purpose: Die Default-Badges verwenden aktuell "ae/oe/ue/ss" statt echte Umlaute. Laut Projekt-Regeln muessen immer echte Umlaute verwendet werden.
Output: Korrigierte defaultBadges in organizations.js
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/organizations.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default-Badges Umlaute korrigieren</name>
  <files>backend/routes/organizations.js</files>
  <action>
In der defaultBadges Array (Zeile 223-244) alle falschen Umlaute durch echte ersetzen:

**Zu korrigierende Eintraege:**
- Zeile 225: "fleissig" -> "fleißig"
- Zeile 226: "Fleissiger" -> "Fleißiger"
- Zeile 228: "grossartig" -> "großartig"
- Zeile 235: "fuer dein" -> "für dein"
- Zeile 236: "Gemeinde-Unterstuetzer" -> "Gemeinde-Unterstützer"
- Zeile 237: "grosse Hilfe" -> "große Hilfe"
- Zeile 240: "Aktivitaeten" -> "Aktivitäten" (in description)
- Zeile 241: "Aktivitaeten" -> "Aktivitäten" (in description)
- Zeile 242: "Aktivitaets-Sammler" -> "Aktivitäts-Sammler" (im name UND in description "Aktivitaeten" -> "Aktivitäten")

Alle anderen Texte pruefen und ebenfalls korrigieren falls noetig.

WICHTIG: Nur die defaultBadges Array aendern, keine anderen Teile der Datei.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && node -e "
      const orgs = require('fs').readFileSync('backend/routes/organizations.js', 'utf8');
      // Extract only the defaultBadges section
      const start = orgs.indexOf('const defaultBadges');
      const end = orgs.indexOf('];', start) + 2;
      const section = orgs.substring(start, end);
      // Check no more ae/oe/ue/ss patterns in badge texts
      const badPatterns = ['fleissig', 'Fleissiger', 'grossartig', 'fuer dein', 'Unterstuetzer', 'grosse', 'Aktivitaet'];
      const found = badPatterns.filter(p => section.includes(p));
      // Check correct umlauts exist
      const goodPatterns = ['fleißig', 'Fleißiger', 'großartig', 'für dein', 'Unterstützer', 'große', 'Aktivität'];
      const missing = goodPatterns.filter(p => !section.includes(p));
      console.log('Bad patterns remaining:', found);
      console.log('Good patterns missing:', missing);
      if (found.length > 0 || missing.length > 0) process.exit(1);
      console.log('PASS');
    "</automated>
  </verify>
  <done>
    - Alle Default-Badge-Texte verwenden echte Umlaute
    - Keine "ae/oe/ue/ss" Ersetzungen mehr in defaultBadges
    - Badges fuer neue Organisationen haben korrekte deutsche Texte
  </done>
</task>

</tasks>

<verification>
- defaultBadges Array enthaelt keine falschen Umlaut-Ersetzungen mehr
- Alle Badge-Namen und Beschreibungen nutzen echte Umlaute
- Keine anderen Teile der organizations.js veraendert
</verification>

<success_criteria>
- grep nach "fleissig|grossartig|fuer|Unterstuetzer|grosse|Aktivitaet" in defaultBadges liefert 0 Treffer
- grep nach echten Umlauten (fleißig, großartig, für, Unterstützer, große, Aktivität) findet die korrekten Texte
</success_criteria>

<output>
After completion, create `.planning/phases/21-badge-logik-debug/21-02-SUMMARY.md`
</output>
