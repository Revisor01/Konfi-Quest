---
phase: 39-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - init-scripts/01-create-schema.sql
  - backend/routes/events.js
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03, EVT-04, EVT-05]

must_haves:
  truths:
    - "Events-Tabelle hat teamer_needed und teamer_only Boolean-Spalten mit gegenseitigem Ausschluss-Constraint"
    - "Admin kann beim Erstellen/Bearbeiten eines Events teamer_needed oder teamer_only setzen"
    - "Teamer kann sich fuer teamer_needed und teamer_only Events einbuchen (vereinfacht, kein Timeslot)"
    - "Teamer kann sich von Events wieder austragen"
    - "Konfis sehen keine teamer_only Events"
    - "QR-Check-in funktioniert fuer Teamer ohne Punkte-Vergabe"
    - "GET /events liefert teamer_needed und teamer_only Felder mit"
    - "GET /:id Participants enthaelt role_name fuer Konfis/Teamer-Trennung"
  artifacts:
    - path: "init-scripts/01-create-schema.sql"
      provides: "teamer_needed und teamer_only Spalten in events Tabelle"
      contains: "teamer_needed BOOLEAN"
    - path: "backend/routes/events.js"
      provides: "Erweiterte Booking/Filter/Create/Update Logik fuer Teamer"
      exports: ["router"]
  key_links:
    - from: "POST /:id/book"
      to: "event_bookings Tabelle"
      via: "Teamer-Booking mit status=confirmed"
      pattern: "isTeamer.*teamer_needed.*teamer_only"
    - from: "GET /"
      to: "events Tabelle"
      via: "Teamer-Filter und teamer_only Ausschluss fuer Konfis"
      pattern: "teamer_only"
    - from: "POST /qr-checkin"
      to: "konfi_profiles"
      via: "Punkte nur fuer Konfis"
      pattern: "req.user.type.*konfi.*points"
---

<objective>
Backend-Erweiterung des Event-Systems um Teamer-Funktionalitaet: DB-Spalten, Booking-Logik, Filter und QR-Check-in.

Purpose: Teamer:innen koennen sich fuer Events einbuchen, Admin kann Events fuer Teamer:innen oeffnen, und alle bestehenden Endpoints (Booking, Cancel, Check-in, Participants) funktionieren korrekt mit der neuen Rolle.
Output: Erweiterte events.js mit Teamer-Support, DB-Schema-Migration
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-events/39-CONTEXT.md
@.planning/phases/39-events/39-RESEARCH.md
@.planning/phases/38-rolle-app-shell/38-01-SUMMARY.md

<interfaces>
<!-- Bestehende Backend-Struktur die erweitert wird -->

From backend/routes/events.js (Zeile 14):
```javascript
module.exports = (db, rbacVerifier, { requireTeamer }, checkAndAwardBadges) => {
```

From backend/routes/events.js - POST /:id/book (Zeile 1067-1073):
```javascript
router.post('/:id/book', rbacVerifier, async (req, res) => {
  const eventId = req.params.id;
  const konfiId = req.user.id;
  if (req.user.type !== 'konfi') {
    return res.status(403).json({ error: 'Nur Konfis können Events buchen' });
  }
```

From backend/routes/events.js - DELETE /:id/book (Zeile 1177-1183):
```javascript
router.delete('/:id/book', rbacVerifier, async (req, res) => {
  if (req.user.type !== 'konfi') {
    return res.status(403).json({ error: 'Nur Konfis können Buchungen stornieren' });
  }
```

From backend/routes/events.js - GET /user/bookings (Zeile 1429-1433):
```javascript
router.get('/user/bookings', rbacVerifier, async (req, res) => {
  if (req.user.type !== 'konfi') {
    return res.status(403).json({ error: 'Nur Konfis können ihre Buchungen einsehen' });
  }
```

From backend/routes/events.js - QR-Checkin Punkte (Zeile 332):
```javascript
if (event.points > 0 && !event.mandatory) {
  // Punkte-Vergabe -- aktuell auch fuer Teamer, muss auf Konfi beschraenkt werden
```

From backend/routes/events.js - POST / Create (Zeile 608-614):
```javascript
router.post('/', rbacVerifier, requireTeamer, validateCreateEvent, async (req, res) => {
  const {
    name, description, event_date, event_end_time, location, location_maps_url,
    points, point_type, category_ids, jahrgang_ids, type, max_participants,
    // ... teamer_needed und teamer_only muessen hier ergaenzt werden
  } = req.body;
```

From backend/routes/events.js - GET /:id Participants (Zeile 498-507):
```sql
SELECT eb.*, u.display_name as participant_name, kp.jahrgang_id,
       j.name as jahrgang_name, et.start_time, et.end_time
FROM event_bookings eb
JOIN users u ON eb.user_id = u.id
LEFT JOIN konfi_profiles kp ON u.id = kp.user_id
-- FEHLT: LEFT JOIN roles r ON u.role_id = r.id (fuer role_name)
```

From init-scripts/01-create-schema.sql - events Tabelle (Zeile 289-316):
```sql
CREATE TABLE events (
  id SERIAL PRIMARY KEY,
  -- ... bestehende Spalten ...
  -- FEHLT: teamer_needed BOOLEAN DEFAULT false,
  -- FEHLT: teamer_only BOOLEAN DEFAULT false,
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Schema und Backend Event-Erstellung/-Bearbeitung erweitern</name>
  <files>init-scripts/01-create-schema.sql, backend/routes/events.js</files>
  <action>
1. In `init-scripts/01-create-schema.sql` die events-Tabelle um zwei Spalten erweitern:
   - `teamer_needed BOOLEAN DEFAULT false` nach der Zeile `checkin_window INTEGER DEFAULT 30`
   - `teamer_only BOOLEAN DEFAULT false` direkt danach
   - CHECK-Constraint hinzufuegen: `ALTER TABLE events ADD CONSTRAINT events_teamer_exclusive CHECK (NOT (teamer_needed = true AND teamer_only = true));`
   - Partiellen Index: `CREATE INDEX idx_events_teamer ON events (teamer_needed, teamer_only) WHERE teamer_needed = true OR teamer_only = true;`

2. In `backend/routes/events.js` den POST / (Create) Endpoint erweitern:
   - `teamer_needed` und `teamer_only` aus req.body destructuren (Zeile 609-614)
   - Im INSERT-Statement die beiden Spalten ergaenzen
   - Validation: Wenn beide true, 400-Fehler zurueckgeben ("teamer_needed und teamer_only schliessen sich gegenseitig aus")

3. In `backend/routes/events.js` den PUT /:id (Update) Endpoint erweitern:
   - `teamer_needed` und `teamer_only` aus req.body destructuren (Zeile 744-749)
   - Im UPDATE-Statement die beiden Spalten ergaenzen
   - Gleiche Validation wie bei Create

4. In `backend/routes/events.js` den POST /series Endpoint erweitern:
   - `teamer_needed` und `teamer_only` aus req.body destructuren
   - In den Series-INSERT die beiden Spalten ergaenzen

5. WICHTIG: Die neuen Spalten muessen auch auf der Live-DB per ALTER TABLE hinzugefuegt werden. Am Ende der `01-create-schema.sql` einen Kommentar-Block hinzufuegen:
```sql
-- Phase 39 Migration (auf Live-DB ausfuehren):
-- ALTER TABLE events ADD COLUMN IF NOT EXISTS teamer_needed BOOLEAN DEFAULT false;
-- ALTER TABLE events ADD COLUMN IF NOT EXISTS teamer_only BOOLEAN DEFAULT false;
-- ALTER TABLE events ADD CONSTRAINT events_teamer_exclusive CHECK (NOT (teamer_needed = true AND teamer_only = true));
-- CREATE INDEX IF NOT EXISTS idx_events_teamer ON events (teamer_needed, teamer_only) WHERE teamer_needed = true OR teamer_only = true;
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "teamer_needed\|teamer_only" init-scripts/01-create-schema.sql && grep -c "teamer_needed\|teamer_only" backend/routes/events.js</automated>
  </verify>
  <done>events-Tabelle hat teamer_needed/teamer_only Spalten mit Constraint. POST /, PUT /:id und POST /series akzeptieren und speichern die neuen Felder.</done>
</task>

<task type="auto">
  <name>Task 2: Booking, Filter, Check-in und Participants fuer Teamer erweitern</name>
  <files>backend/routes/events.js</files>
  <action>
1. **POST /:id/book** (Zeile 1067): Konfi-only-Check erweitern auf Konfi ODER Teamer:
   - Statt `if (req.user.type !== 'konfi')` => `const isKonfi = req.user.type === 'konfi'; const isTeamer = req.user.type === 'teamer'; if (!isKonfi && !isTeamer) return 403;`
   - TEAMER-PFAD (vor der bestehenden Konfi-Logik einfuegen):
     ```javascript
     if (isTeamer) {
       if (!event.teamer_needed && !event.teamer_only) {
         return res.status(403).json({ error: 'Dieses Event ist nicht fuer Teamer:innen buchbar' });
       }
       // Duplikat-Check (bestehender Code wiederverwendbar)
       // Direkt confirmed einfuegen, KEIN Timeslot, KEINE Warteliste, KEIN Registration-Zeitfenster-Check
       // Push an Org-Admins: "Teamer:in {display_name} hat sich fuer '{event.name}' angemeldet"
       // LiveUpdate senden: liveUpdate.sendToUser('teamer', userId, 'events', 'update', ...)
       // liveUpdate.sendToOrgAdmins(...)
       return; // Frueh zurueckkehren
     }
     ```
   - KONFI-PFAD: Zusaetzlich pruefen `if (isKonfi && event.teamer_only)` => 403 "Dieses Event ist nur fuer Teamer:innen"
   - Registration-Zeitfenster-Check (registration_opens_at/closes_at) NUR fuer Konfis, NICHT fuer Teamer

2. **DELETE /:id/book** (Zeile 1177): Konfi-only-Check auf Konfi ODER Teamer erweitern:
   - Gleiche Logik: `if (!isKonfi && !isTeamer) return 403`
   - Bei Teamer-Storno: Push an Org-Admins "Teamer:in {display_name} hat sich von '{event.name}' abgemeldet"
   - LiveUpdate fuer Teamer senden

3. **GET /user/bookings** (Zeile 1429): Konfi-only-Check auf Konfi ODER Teamer erweitern:
   - `if (req.user.type !== 'konfi' && req.user.type !== 'teamer')` statt nur Konfi-Check

4. **GET /** (Zeile 40): Teamer-Filter und Konfi-Ausschluss erweitern:
   - Nach dem bestehenden Teamer-Jahrgang-Filter (Zeile 91-102): `teamer_only` Events einschliessen
     ```javascript
     if (req.user.role_name === 'teamer' && ...) {
       filteredRows = rows.filter(row => {
         if (row.teamer_only) return true; // Reine Teamer-Events immer sichtbar
         if (!row.jahrgang_ids) return true;
         // ... bestehender Jahrgang-Filter
       });
     }
     ```
   - Fuer Konfis (kein role_name === 'teamer'): `teamer_only` Events AUSSCHLIESSEN
     Nach dem Teamer-Filter-Block einen neuen Block fuer Konfis:
     ```javascript
     if (req.user.type === 'konfi') {
       filteredRows = filteredRows.filter(row => !row.teamer_only);
     }
     ```

5. **GET /:id Participants** (Zeile 498): role_name in Query ergaenzen:
   - `LEFT JOIN roles r ON u.role_id = r.id` zum Participants-Query hinzufuegen
   - `r.name as role_name` zum SELECT hinzufuegen
   - Damit kann das Frontend Konfis und Teamer:innen getrennt anzeigen

6. **POST /qr-checkin** (Zeile 332): Punkte-Vergabe auf Konfis beschraenken:
   - Von `if (event.points > 0 && !event.mandatory)` zu `if (event.points > 0 && !event.mandatory && req.user.type === 'konfi')`
   - Teamer erhalten keine Punkte beim Check-in (Out of Scope per REQUIREMENTS.md)
   - Push-Notification nach Check-in: Fuer Teamer trotzdem "Erfolgreich eingecheckt" senden, aber ohne Punkte-Info

7. **Teamer-Count im GET /** (Open Question aus RESEARCH.md): Separate Zaehlung ergaenzen:
   - Im Haupt-GET-Query zusaetzliche Counts:
     ```sql
     COUNT(DISTINCT CASE WHEN eb.status = 'confirmed' AND u_book.role_id = (SELECT id FROM roles WHERE name = 'teamer' LIMIT 1) THEN eb.id END) as teamer_count
     ```
   - Dafuer `LEFT JOIN users u_book ON eb.user_id = u_book.id` im Haupt-Query ergaenzen
   - Teamer-Bookings zaehlen NICHT gegen max_participants (Kapazitaet gilt nur fuer Konfis)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "isTeamer\|isKonfi" backend/routes/events.js | head -20 && grep -n "teamer_only" backend/routes/events.js | head -10 && grep -n "role_name" backend/routes/events.js | head -5</automated>
  </verify>
  <done>Teamer:innen koennen Events buchen/stornieren, sehen relevante Events, QR-Check-in vergibt keine Punkte an Teamer, Participants-Query liefert role_name, Konfi-Events filtern teamer_only aus.</done>
</task>

</tasks>

<verification>
- `grep -c "teamer_needed" backend/routes/events.js` ergibt mindestens 5 Treffer (Create, Update, Book-Check, Filter, Series)
- `grep -c "isTeamer" backend/routes/events.js` ergibt mindestens 3 Treffer (Book, Cancel, Filter)
- `grep "role_name" backend/routes/events.js` zeigt JOIN auf roles-Tabelle im Participants-Query
- `grep "req.user.type === 'konfi'" backend/routes/events.js` zeigt Punkte-Guard im QR-Checkin
</verification>

<success_criteria>
- Events-Tabelle hat teamer_needed/teamer_only mit gegenseitigem Ausschluss
- POST/PUT akzeptieren teamer_needed/teamer_only Felder
- Teamer koennen buchen (vereinfacht) und stornieren
- Konfis sehen keine teamer_only Events
- QR-Check-in vergibt keine Punkte an Teamer
- Participants-Query liefert role_name fuer Frontend-Trennung
- Push-Notifications bei Teamer-Buchung/Storno an Admins
</success_criteria>

<output>
After completion, create `.planning/phases/39-events/39-01-SUMMARY.md`
</output>
