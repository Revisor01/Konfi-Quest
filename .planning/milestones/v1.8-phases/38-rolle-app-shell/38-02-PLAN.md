---
phase: 38-rolle-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - frontend/src/contexts/AppContext.tsx
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
  - frontend/src/theme/variables.css
  - backend/routes/teamer.js
  - backend/server.js
autonomous: false
requirements: [ROL-01, ROL-02, ROL-03, ROL-04]

must_haves:
  truths:
    - "Teamer sieht nach Login eine eigene TabBar mit 5 Tabs"
    - "Teamer-UI nutzt Rose (#e11d48) als Akzentfarbe"
    - "Chat-Tab funktioniert sofort (wiederverwendet ChatOverviewPage)"
    - "Profil-Tab zeigt eingefrorene Konfi-Badges und Punkte"
    - "Dashboard, Events und Material zeigen EmptyState-Platzhalter"
    - "Admin sieht Teamer-befoerdern-Button in KonfiDetailView"
    - "Teamer kann keine Konfi- oder Admin-UI sehen"
  artifacts:
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Teamer-Branch mit 5-Tab-Navigation"
      contains: "teamer"
    - path: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      provides: "Profil mit eingefrorenen Konfi-Daten"
      min_lines: 50
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "EmptyState Dashboard Platzhalter"
      contains: "EmptyState"
    - path: "frontend/src/components/teamer/pages/TeamerEventsPage.tsx"
      provides: "EmptyState Events Platzhalter"
      contains: "EmptyState"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialPage.tsx"
      provides: "EmptyState Material Platzhalter"
      contains: "EmptyState"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "Teamer-befoerdern-Button mit Bestaetigungsdialog"
      contains: "promote-teamer"
    - path: "frontend/src/contexts/AppContext.tsx"
      provides: "User-Interface mit type 'teamer'"
      contains: "teamer"
    - path: "backend/routes/teamer.js"
      provides: "GET /teamer/profile Endpoint fuer eingefrorene Konfi-Daten"
      contains: "teamer/profile"
  key_links:
    - from: "frontend/src/components/layout/MainTabs.tsx"
      to: "user.type"
      via: "Conditional rendering basierend auf user.type === 'teamer'"
      pattern: "user\\.type.*teamer"
    - from: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      to: "/admin/konfis/:id/promote-teamer"
      via: "api.post call im Bestaetigungsdialog"
      pattern: "promote-teamer"
    - from: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      to: "backend/routes/teamer.js"
      via: "GET /teamer/profile API-Call"
      pattern: "api.*teamer.*profile"
---

<objective>
Frontend Teamer-UI mit 5-Tab-Navigation, Profil mit eingefrorenen Daten, EmptyState-Platzhalter und Transition-Button.

Purpose: Nach der Backend-Aenderung (user.type='teamer') muss das Frontend den neuen Typ erkennen und eine eigene Teamer-UI rendern. Chat und Profil funktionieren sofort, restliche Tabs sind Platzhalter fuer spaetere Phasen.
Output: Vollstaendige Teamer-Navigation, funktionierender Chat, Profil mit eingefrorenen Konfi-Daten, Transition-Button in KonfiDetailView.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-rolle-app-shell/38-CONTEXT.md
@.planning/phases/38-rolle-app-shell/38-01-SUMMARY.md

<interfaces>
<!-- Bestehende Interfaces -->

From frontend/src/contexts/AppContext.tsx (Zeile 62):
```typescript
interface User {
  id: number;
  type: 'admin' | 'konfi' | 'user';  // MUSS um 'teamer' erweitert werden
  display_name: string;
  username?: string;
  email?: string;
  organization?: string;
  organization_id?: number;
  roles?: string[];
  role_name?: string;
  jahrgang?: string;
  is_super_admin?: boolean;
}
```

From frontend/src/components/layout/MainTabs.tsx:
```typescript
// 3-Wege-Routing: isSuperAdmin -> admin -> konfi
// MUSS um teamer-Branch erweitert werden (zwischen admin und konfi)
// Pattern: user.type === 'teamer' ? <TeamerTabs /> : (konfi tabs)
```

From frontend/src/components/shared/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: string;
  title: string;
  message: string;
  iconColor?: string;
}
```

From frontend/src/components/konfi/pages/KonfiProfilePage.tsx:
```typescript
// Referenz fuer TeamerProfilePage - gleiche Datenstruktur
// API: GET /konfi/profile liefert KonfiProfile mit Badges, Punkten, etc.
```

From frontend/src/components/chat/pages/ChatOverviewPage.tsx:
```typescript
// Direkt wiederverwendbar fuer Teamer-Chat
// Muss nur unter /teamer/chat Route gemounted werden
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teamer-Seiten, AppContext-Erweiterung und MainTabs-Routing</name>
  <files>
    frontend/src/contexts/AppContext.tsx,
    frontend/src/components/layout/MainTabs.tsx,
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx,
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx,
    frontend/src/components/teamer/pages/TeamerMaterialPage.tsx,
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx,
    frontend/src/theme/variables.css,
    backend/routes/teamer.js,
    backend/server.js
  </files>
  <action>
1. **AppContext.tsx** -- User Interface erweitern:
   - Zeile 62: `type: 'admin' | 'konfi' | 'teamer' | 'user'` (teamer hinzufuegen)
   - In der Push-Notification-Navigation (Zeile 324ff) einen teamer-Fall hinzufuegen:
     ```typescript
     const userType = user?.type || 'konfi';
     // Teamer-Routes
     const routePrefix = userType === 'admin' ? '/admin' : userType === 'teamer' ? '/teamer' : '/konfi';
     ```
     Dann in den switch-cases statt einzelner if/else das routePrefix nutzen.

2. **variables.css** -- Teamer-Farb-Klasse hinzufuegen (am Ende der CSS-Datei):
   ```css
   /* === Teamer Domain Color === */
   .app-header-banner--teamer {
     background: linear-gradient(135deg, #e11d48 0%, #be123c 100%);
   }
   ```

3. **TeamerDashboardPage.tsx** (NEU erstellen):
   - IonPage mit IonHeader (Toolbar: Titel "Start"), IonContent
   - Header-Banner mit app-header-banner--teamer Klasse, home Icon, "Willkommen zurueck!"
   - EmptyState Component: icon=home, title="Dein Dashboard", message="Hier siehst du bald deine Zertifikate, naechste Events und Badges."

4. **TeamerEventsPage.tsx** (NEU erstellen):
   - IonPage mit IonHeader (Toolbar: Titel "Events"), IonContent
   - EmptyState Component: icon=calendar, title="Events", message="Hier findest du bald Events, bei denen du als Teamer helfen kannst."

5. **TeamerMaterialPage.tsx** (NEU erstellen):
   - IonPage mit IonHeader (Toolbar: Titel "Material"), IonContent
   - EmptyState Component: icon=documentIcon, title="Material", message="Hier findest du bald Materialien und Dokumente fuer deinen Jahrgang."

6. **backend/routes/teamer.js** (NEU erstellen):
   - Express Router mit GET /profile Endpoint
   - Middleware: verifyTokenRBAC + requireRole('teamer')
   - Query: konfi_profiles (gottesdienst_points, gemeinde_points, jahrgang_name) fuer req.user.id
   - Query: konfi_badges JOIN badges (badge_id, name, icon, color, awarded_date) fuer req.user.id
   - Query: User-Daten (display_name, username, organization_name) -- bereits in req.user vorhanden
   - Response: `{ user: { display_name, username, organization_name }, konfi_data: { gottesdienst_points, gemeinde_points, jahrgang_name, badges: [...] } }`
   - Folge das Pattern der bestehenden Routes (z.B. konfi.js) fuer module.exports Signatur

7. **backend/server.js** (oder index.js) -- Teamer-Route registrieren:
   - Pruefen wo die anderen Routes gemounted werden (z.B. `app.use('/api/admin', adminRoutes)`)
   - Teamer-Route analog mounten: `app.use('/api/teamer', teamerRoutes)`
   - Route-Datei importieren und mit gleichen Parametern wie andere Routes initialisieren

8. **TeamerProfilePage.tsx** (NEU erstellen):
   - IonPage mit IonHeader, IonContent, IonRefresher
   - API-Call: GET /teamer/profile (neuer Backend-Endpoint aus Schritt 6)
   - Layout:
     - Header-Banner mit Rose-Farbe (#e11d48), person Icon, User-Name
     - Abschnitt "Konfi-Badges" mit Badge-Liste (IonItem pro Badge: icon + name + datum)
     - Abschnitt "Konfi-Punkte" mit Gottesdienst/Gemeinde-Anzeige (readonly, IonItem mit Zahlenwert)
     - Falls keine Konfi-Daten: Hinweis "Keine Konfi-Historie vorhanden"
   - Nutze useModalPage('profile') und presentingElement Pattern wie in KonfiProfilePage

9. **MainTabs.tsx** -- Teamer-Branch einfuegen:
   - Importiere die 4 neuen Teamer-Pages + ChatOverviewPage + ChatRoomView (bereits importiert)
   - Nach `user.type === 'admin'` und vor dem Konfi-Branch:
   ```typescript
   ) : user.type === 'teamer' ? (
     // Teamer Tabs
     <ModalProvider>
       <IonTabs>
         <IonRouterOutlet>
           <Route exact path="/teamer" render={() => <Redirect to="/teamer/dashboard" />} />
           <Route exact path="/teamer/dashboard" component={TeamerDashboardPage} />
           <Route exact path="/teamer/chat" component={ChatOverviewPage} />
           <Route path="/teamer/chat/room/:roomId" render={(props) => {
             const roomId = parseInt(props.match.params.roomId);
             return <ChatRoomView roomId={roomId} onBack={() => props.history.goBack()} />;
           }} />
           <Route exact path="/teamer/events" component={TeamerEventsPage} />
           <Route exact path="/teamer/material" component={TeamerMaterialPage} />
           <Route exact path="/teamer/profile" component={TeamerProfilePage} />
           <Route exact path="/" render={() => <Redirect to="/teamer/dashboard" />} />
         </IonRouterOutlet>
         {!isTabBarHidden(location.pathname) && (
           <IonTabBar slot="bottom">
             <IonTabButton tab="teamer-dashboard" href="/teamer/dashboard">
               <IonIcon icon={home} />
               <IonLabel>Start</IonLabel>
             </IonTabButton>
             <IonTabButton tab="teamer-chat" href="/teamer/chat">
               <IonIcon icon={chatbubbles} />
               <IonLabel>Chat</IonLabel>
               {chatUnreadTotal > 0 && (
                 <IonBadge color="danger">
                   {chatUnreadTotal > 9 ? '9+' : chatUnreadTotal}
                 </IonBadge>
               )}
             </IonTabButton>
             <IonTabButton tab="teamer-events" href="/teamer/events">
               <IonIcon icon={calendar} />
               <IonLabel>Events</IonLabel>
             </IonTabButton>
             <IonTabButton tab="teamer-material" href="/teamer/material">
               <IonIcon icon={documentIcon} />
               <IonLabel>Material</IonLabel>
             </IonTabButton>
             <IonTabButton tab="teamer-profile" href="/teamer/profile">
               <IonIcon icon={person} />
               <IonLabel>Profil</IonLabel>
             </IonTabButton>
           </IonTabBar>
         )}
       </IonTabs>
     </ModalProvider>
   ```
   - In isTabBarHidden: `/teamer/chat/room/` hinzufuegen

WICHTIG: Keine Emojis verwenden! Echte Umlaute in allen UI-Texten! IonIcons statt Unicode-Symbole!
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Teamer-Navigation mit 5 Tabs funktioniert, Chat wiederverwendet bestehende Komponenten, Profil zeigt eingefrorene Konfi-Daten via /teamer/profile Endpoint, EmptyState fuer Dashboard/Events/Material.</done>
</task>

<task type="auto">
  <name>Task 2: Transition-Button in KonfiDetailView mit Bestaetigungsdialog</name>
  <files>frontend/src/components/admin/views/KonfiDetailView.tsx</files>
  <action>
In KonfiDetailView.tsx einen "Zum Teamer befoerdern"-Button hinzufuegen:

1. Neuen Import: `import { ribbon } from 'ionicons/icons';` (oder arrowUp falls ribbon nicht vorhanden)

2. Neue Handler-Funktion handlePromoteToTeamer:
   ```typescript
   const handlePromoteToTeamer = async () => {
     if (!konfi) return;
     presentAlert({
       header: 'Zum Teamer befördern',
       message: `${konfi.name} wirklich zum Teamer befördern?\n\n` +
         `Punkte: ${konfi.gottesdienst_points || 0} Gottesdienst, ${konfi.gemeinde_points || 0} Gemeinde\n` +
         `Badges: ${konfi.badgeCount || 0}\n\n` +
         `Konfi-Punkte und Badges bleiben als Historie erhalten. ` +
         `Event-Buchungen und offene Anträge werden gelöscht. ` +
         `Diese Aktion kann nicht rückgängig gemacht werden.`,
       buttons: [
         { text: 'Abbrechen', role: 'cancel' },
         {
           text: 'Befördern',
           handler: async () => {
             try {
               await api.post(`/admin/konfis/${konfiId}/promote-teamer`);
               setSuccess(`${konfi.name} wurde zum Teamer befördert`);
               window.dispatchEvent(new CustomEvent('konfis-updated'));
               onBack();
             } catch (err: any) {
               setError(err.response?.data?.error || 'Fehler beim Befördern');
             }
           }
         }
       ]
     });
   };
   ```

3. Button im Render-Bereich platzieren -- nach dem Passwort-Reset-Button (handlePasswordReset) und vor dem Loeschen-Bereich. Nutze das bestehende List-Pattern:
   ```tsx
   <IonItem button onClick={handlePromoteToTeamer} detail>
     <IonIcon icon={ribbon} slot="start" color="primary" />
     <IonLabel>
       <h2>Zum Teamer befördern</h2>
       <p>Konfi-Rolle wird dauerhaft gewechselt</p>
     </IonLabel>
   </IonItem>
   ```
   (In die bestehende Aktions-Liste einfuegen, je nach aktuellem Layout der Detail-View)

WICHTIG: useIonAlert ist bereits importiert (presentAlert). Keine neuen Modals noetig. Echte Umlaute verwenden!
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Admin sieht "Zum Teamer befördern" Button in KonfiDetailView mit Bestaetigungsdialog der Punkte und Badges anzeigt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manueller End-to-End-Test des Teamer-Flows</name>
  <files>n/a</files>
  <action>
Kompletter Teamer-Flow manuell testen: Backend-Transition, Teamer-Login mit eigener UI, 5-Tab-Navigation, Chat funktioniert, Profil zeigt eingefrorene Konfi-Daten, Transition-Button in KonfiDetailView.

Schritte:
1. Backend starten: `cd backend und npm start`
2. Frontend starten: `cd frontend und npm run dev`
3. Als Admin einloggen
4. Einen Test-Konfi oeffnen -> "Zum Teamer befördern" Button sollte sichtbar sein
5. Bestaetigungsdialog pruefen: Zeigt Punkte, Badges und Warnung
6. Konfi befoerdern
7. Ausloggen, als befoerderter Teamer einloggen
8. Pruefen: 5 Tabs sichtbar (Start, Chat, Events, Material, Profil)
9. Chat-Tab oeffnen -> bestehende Chat-Raeume sollten sichtbar sein
10. Profil-Tab oeffnen -> eingefrorene Konfi-Badges und Punkte sichtbar
11. Dashboard/Events/Material -> EmptyState Platzhalter
  </action>
  <verify>Manuelle Pruefung durch User</verify>
  <done>Alle 11 Pruefschritte bestanden, Teamer-Flow funktioniert end-to-end.</done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
- Backend startet ohne Fehler: `cd backend && npm start`
- Teamer-Route in MainTabs.tsx vorhanden
- user.type 'teamer' in AppContext Interface
- Transition-Endpoint in konfi-managment.js (aus Plan 01)
- Teamer-Profil-Endpoint in teamer.js
- EmptyState in allen 3 Platzhalter-Seiten
- Rose-Farbe (#e11d48) in variables.css
</verification>

<success_criteria>
- Teamer sieht nach Login 5 Tabs mit eigener Navigation
- Chat funktioniert sofort (bestehende Komponenten wiederverwendet)
- Profil zeigt eingefrorene Konfi-Badges und Punkte
- Dashboard, Events, Material zeigen EmptyState
- Admin kann Konfi zum Teamer befoerdern via Button in KonfiDetailView
- Teamer sieht keine Admin- oder Konfi-UI
</success_criteria>

<output>
After completion, create `.planning/phases/38-rolle-app-shell/38-02-SUMMARY.md`
</output>
