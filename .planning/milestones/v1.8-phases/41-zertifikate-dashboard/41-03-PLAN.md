---
phase: 41-zertifikate-dashboard
plan: 03
type: execute
wave: 1
depends_on: ["41-01", "41-02"]
files_modified:
  - backend/routes/teamer.js
autonomous: true
gap_closure: true
requirements: [ZRT-01, ZRT-02, ZRT-03, DSH-01, DSH-02, DSH-03, DSH-04]

must_haves:
  truths:
    - "Dashboard-Config-Toggles steuern Sektionen-Sichtbarkeit korrekt"
    - "Event-Titel werden im Dashboard korrekt angezeigt"
  artifacts:
    - path: "backend/routes/teamer.js"
      provides: "Korrigiertes Config-Key-Mapping und Event-Alias"
      contains: "show_zertifikate"
  key_links:
    - from: "backend/routes/teamer.js config object"
      to: "frontend DashboardConfig interface"
      via: "show_* key names"
      pattern: "show_zertifikate.*show_events.*show_badges.*show_losung"
    - from: "backend/routes/teamer.js event query"
      to: "frontend DashboardEvent.title"
      via: "SQL AS alias"
      pattern: "e\\.name AS title"
---

<objective>
Gap Closure: Config-Key-Mismatch und Event-Feld-Mismatch im Teamer-Dashboard beheben.

Purpose: Zwei Datenmapping-Bugs im Backend fixen, damit Dashboard-Config-Toggles wirken und Event-Titel angezeigt werden.
Output: Korrigiertes backend/routes/teamer.js
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-zertifikate-dashboard/41-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config-Keys und Event-Alias im Backend fixen</name>
  <files>backend/routes/teamer.js</files>
  <action>
Zwei gezielte Fixes im /teamer/dashboard Endpoint:

1. Config-Key-Mismatch (ca. Zeile 473-477): Das config-Objekt liefert Keys mit Prefix `teamer_dashboard_show_` zurueck, aber das Frontend-Interface erwartet `show_*`. Fix: Die Default-Keys und die DB-Row-Zuweisung so aendern, dass das resultierende Objekt `show_zertifikate`, `show_events`, `show_badges`, `show_losung` zurueckgibt. Konkret:
   - Default-Objekt aendern: `{ show_zertifikate: true, show_events: true, show_badges: true, show_losung: true }`
   - DB-Row-Zuweisung aendern: `config[row.key.replace('teamer_dashboard_show_', 'show_')] = row.value === 'true' || row.value === '1';`

2. Event-Feld-Mismatch (ca. Zeile 422): SQL-Query selektiert `e.name`, aber Frontend liest `event.title`. Fix: `e.name` durch `e.name AS title` ersetzen.

KEINE weiteren Aenderungen vornehmen. Beide Fixes sind reine Datenmapping-Korrekturen im Backend.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "show_zertifikate" backend/routes/teamer.js && grep -n "AS title" backend/routes/teamer.js</automated>
  </verify>
  <done>Backend liefert Config-Keys als show_* (ohne teamer_dashboard_-Prefix) und Events mit title-Feld. Frontend kann beides direkt ohne Mapping konsumieren.</done>
</task>

</tasks>

<verification>
- `grep "show_zertifikate" backend/routes/teamer.js` zeigt Keys ohne Prefix
- `grep "AS title" backend/routes/teamer.js` zeigt Event-Name-Alias
- Frontend DashboardConfig-Interface (show_zertifikate, show_events, show_badges, show_losung) stimmt mit Backend-Response ueberein
- Frontend DashboardEvent.title stimmt mit Backend event.title (aliased von e.name) ueberein
</verification>

<success_criteria>
- Dashboard-Config-Toggles aus Admin-Settings steuern Sektionen-Sichtbarkeit im Teamer-Dashboard korrekt
- Event-Titel werden im Dashboard nicht mehr leer angezeigt
- Beide VERIFICATION.md Gaps sind geschlossen
</success_criteria>

<output>
After completion, create `.planning/phases/41-zertifikate-dashboard/41-03-SUMMARY.md`
</output>
