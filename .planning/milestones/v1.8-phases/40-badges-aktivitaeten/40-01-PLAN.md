---
phase: 40-badges-aktivitaeten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/badges.js
  - backend/routes/activities.js
  - backend/routes/konfi-managment.js
  - backend/routes/konfi.js
  - backend/routes/organizations.js
  - backend/routes/teamer.js
autonomous: true
requirements: [BDG-01, BDG-07]
must_haves:
  truths:
    - "Tabellen konfi_badges und konfi_activities sind zu user_badges und user_activities umbenannt"
    - "activities und custom_badges haben target_role Spalte (konfi/teamer)"
    - "UNIQUE Constraint auf konfi_activities ist aufgehoben -- Mehrfachvergabe moeglich"
    - "Alle Backend-Routes verwenden die neuen Tabellennamen"
    - "activities GET-Endpoint unterstuetzt target_role Filter"
    - "Teamer-Aktivitaeten haben kein Punkte-Pflichtfeld"
  artifacts:
    - path: "backend/routes/badges.js"
      provides: "Badge-Routes mit user_badges Tabelle und target_role Filter"
    - path: "backend/routes/activities.js"
      provides: "Activities mit target_role Spalte und optionalen Punkten"
    - path: "backend/routes/konfi-managment.js"
      provides: "Konfi-Management mit user_activities/user_badges Tabellennamen"
    - path: "backend/routes/konfi.js"
      provides: "Konfi-Routes mit neuen Tabellennamen"
  key_links:
    - from: "backend/routes/activities.js"
      to: "activities Tabelle"
      via: "target_role Spalte"
      pattern: "target_role"
    - from: "backend/routes/badges.js"
      to: "user_badges Tabelle"
      via: "SQL Queries"
      pattern: "user_badges"
---

<objective>
DB-Migration und Backend-Route-Anpassung: Tabellen umbenennen (konfi_badges -> user_badges, konfi_activities -> user_activities), target_role Spalte einfuegen, alle Backend-Referenzen migrieren.

Purpose: Grundlage fuer rollenbasierte Badges und Aktivitaeten schaffen -- Teamer und Konfis teilen sich die gleichen Tabellen mit target_role Unterscheidung.
Output: Alle Backend-Routes verwenden neue Tabellennamen, activities/badges haben target_role Spalte.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-badges-aktivitaeten/40-CONTEXT.md

<interfaces>
<!-- Betroffene Backend-Route-Dateien und ihre Tabellen-Referenzen -->

Tabellen-Umbenennung:
- konfi_badges -> user_badges (Spalte konfi_id -> user_id)
- konfi_activities -> user_activities (Spalte konfi_id -> user_id)

Dateien mit konfi_badges Referenzen:
- backend/routes/badges.js (INSERT, SELECT, DELETE)
- backend/routes/konfi-managment.js (SELECT COUNT, DELETE bei promote-teamer)
- backend/routes/konfi.js (SELECT, LEFT JOIN, UPDATE seen)
- backend/routes/organizations.js (DELETE bei Org-Loeschung)
- backend/routes/teamer.js (SELECT fuer Teamer-Badge-Anzeige)

Dateien mit konfi_activities Referenzen:
- backend/routes/badges.js (SELECT fuer Badge-Checks)
- backend/routes/activities.js (INSERT, DELETE, SELECT COUNT)
- backend/routes/konfi-managment.js (INSERT, DELETE)
- backend/routes/konfi.js (SELECT, COUNT)
- backend/routes/organizations.js (DELETE bei Org-Loeschung)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration -- Tabellen umbenennen und Spalten hinzufuegen</name>
  <files>backend/routes/badges.js</files>
  <action>
Am Anfang der badges.js module.exports Funktion (vor allen Routes) eine Migrations-Funktion einfuegen, die beim Server-Start ausgefuehrt wird:

1. ALTER TABLE konfi_badges RENAME TO user_badges (IF EXISTS, mit Pruefung ob schon umbenannt)
2. ALTER TABLE konfi_activities RENAME TO user_activities (IF EXISTS)
3. In user_badges: ALTER COLUMN konfi_id RENAME TO user_id (IF EXISTS)
4. In user_activities: ALTER COLUMN konfi_id RENAME TO user_id (IF EXISTS)
5. ALTER TABLE activities ADD COLUMN IF NOT EXISTS target_role VARCHAR(10) DEFAULT 'konfi'
6. ALTER TABLE custom_badges ADD COLUMN IF NOT EXISTS target_role VARCHAR(10) DEFAULT 'konfi'
7. UNIQUE Constraint auf user_activities entfernen falls vorhanden (DROP CONSTRAINT IF EXISTS)

Pattern: Selbst-migrierende Funktion `runMigrations(db)` die idempotent ist (kann mehrfach aufgerufen werden). Am Anfang der module.exports aufrufen.

WICHTIG: Die Migration muss idempotent sein -- bei erneutem Server-Start darf nichts fehlschlagen. Verwende IF EXISTS / IF NOT EXISTS ueberall.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && node -e "const f = require('./backend/routes/badges.js'); console.log('badges.js loads OK')" 2>&1 | head -5 && grep -c 'runMigrations' backend/routes/badges.js && grep -c 'target_role' backend/routes/badges.js</automated>
  </verify>
  <done>Migration-Funktion existiert in badges.js, alle ALTER TABLE Statements sind idempotent, target_role Spalte wird hinzugefuegt</done>
</task>

<task type="auto">
  <name>Task 2: Alle Backend-Routes auf neue Tabellennamen migrieren</name>
  <files>backend/routes/badges.js, backend/routes/activities.js, backend/routes/konfi-managment.js, backend/routes/konfi.js, backend/routes/organizations.js, backend/routes/teamer.js</files>
  <action>
Globales Suchen-und-Ersetzen in allen 6 Dateien:

1. `konfi_badges` -> `user_badges` (alle SQL Queries)
2. `konfi_activities` -> `user_activities` (alle SQL Queries)
3. In SQL Queries: `konfi_id` Spalten-Referenzen in user_badges/user_activities -> `user_id` (NUR in Kontext der umbenannten Tabellen, NICHT in konfi_profiles oder anderen Tabellen!)

Zusaetzlich in activities.js:
- GET / Route: target_role als optionalen Query-Parameter hinzufuegen (req.query.target_role). Wenn gesetzt, WHERE a.target_role = $X hinzufuegen
- POST / Route: target_role aus req.body akzeptieren (default 'konfi'). Bei target_role='teamer' sind points und type optional (default 0 und null)
- PUT / Route: target_role aus req.body akzeptieren. Bei Teamer-Aktivitaeten sind points/type nicht pflicht
- POST /assign-activity Route: Bei Teamer-Aktivitaeten (activity.target_role === 'teamer') KEINE Punkte-Vergabe und KEINEN pointTypeGuard-Check. Stattdessen nur user_activities Eintrag erstellen

Zusaetzlich in badges.js:
- GET / Route: target_role als optionalen Query-Parameter (default: alle). Filter WHERE cb.target_role = $X
- POST / Route: target_role aus req.body (default 'konfi')
- earned_count Query: Muss user_badges statt konfi_badges verwenden

Zusaetzlich in konfi-managment.js:
- POST /:id/activities Route (Aktivitaets-Zuweisung): Bei Teamer-Aktivitaeten keine Punkte vergeben
- Teamer-Erkennung: Vor Punktevergabe pruefen ob User Teamer-Rolle hat (SELECT role_name)

WICHTIG: Nur die Tabellennamen und Spaltennamen in SQL aendern. Die JavaScript-Variablennamen (konfiId etc.) und Request-Parameter koennen bleiben wie sie sind.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -rn "konfi_badges\|konfi_activities" backend/routes/ | grep -v "// " | grep -v "node_modules" | wc -l && grep -c 'target_role' backend/routes/badges.js && grep -c 'target_role' backend/routes/activities.js</automated>
  </verify>
  <done>Grep nach konfi_badges/konfi_activities in backend/routes/ liefert 0 Treffer (ausser Kommentare). Alle Routes verwenden user_badges und user_activities. Activities-Endpoint unterstuetzt target_role Filter und optionale Punkte fuer Teamer. badges.js und activities.js enthalten target_role Referenzen.</done>
</task>

</tasks>

<verification>
- Kein konfi_badges oder konfi_activities mehr in SQL-Queries (nur in Kommentaren erlaubt)
- activities.js POST akzeptiert target_role Parameter
- badges.js GET unterstuetzt target_role Filter
- Server startet ohne Fehler (Migration laeuft durch)
</verification>

<success_criteria>
- Alle 6 Backend-Route-Dateien verwenden user_badges und user_activities
- target_role Spalte in activities und custom_badges Tabellen
- Teamer-Aktivitaeten ohne Punkte-Pflicht erstellbar
- Migration ist idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/40-badges-aktivitaeten/40-01-SUMMARY.md`
</output>
