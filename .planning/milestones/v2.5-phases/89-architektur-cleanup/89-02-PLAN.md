---
phase: 89-architektur-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/material.js
  - backend/database.js
  - backend/services/backgroundService.js
autonomous: true
requirements: [ARCH-02, ARCH-03, CLN-01]

must_haves:
  truths:
    - "material.js akzeptiert nur noch event_ids/jahrgang_ids Arrays, kein Singular-Format mehr"
    - "Ausgefuehrte Migrationen werden in schema_migrations getrackt und nicht erneut ausgefuehrt"
    - "Der doppelte Date-Guard im Wrapped-Cron ist entfernt"
  artifacts:
    - path: "backend/routes/material.js"
      provides: "POST und PUT ohne Legacy-Singular-Fallback"
      contains: "event_ids"
    - path: "backend/database.js"
      provides: "schema_migrations Tracking-Tabelle und idempotenter Migration-Runner"
      contains: "schema_migrations"
    - path: "backend/services/backgroundService.js"
      provides: "checkWrappedTriggers ohne manuellen Date-Guard"
  key_links:
    - from: "backend/database.js"
      to: "schema_migrations Tabelle"
      via: "CREATE TABLE IF NOT EXISTS + INSERT nach jedem Migration-File"
    - from: "backend/routes/material.js POST"
      to: "material_events / material_jahrgaenge"
      via: "event_ids Array direkt (kein Fallback)"
---

<objective>
Drei unabhaengige Cleanup-Items parallel abarbeiten: Legacy-Felder in material.js entfernen, Migrations-Versionstabelle in database.js einfuehren, doppelten Cron-Guard in backgroundService.js entfernen.

Purpose: Technischen Schulden beseitigen ohne Funktionsaenderung.
Output: material.js ohne Dual-Format-Logik, database.js mit idempotenten Migrations, backgroundService.js ohne redundanten Guard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- material.js aktueller Legacy-Dual-Format-Code (ZU ENTFERNEN): -->

```javascript
// POST (Zeilen 349-369) - aktuell:
const resolvedEventIds = event_ids || (event_id ? [event_id] : []);
// PUT (Zeilen 430, 444) - aktuell:
const resolvedEventIds = event_ids !== undefined ? event_ids : (event_id !== undefined ? (event_id ? [event_id] : []) : undefined);
const resolvedJahrgangIds = jahrgang_ids !== undefined ? jahrgang_ids : (jahrgang_id !== undefined ? (jahrgang_id ? [jahrgang_id] : []) : undefined);
```

<!-- database.js aktueller Migration-Runner (ZU ERSETZEN): -->
```javascript
// Aktuell (Zeilen 18-33): fuehrt ALLE .sql-Dateien bei jedem Start aus
async function runMigrations(pool) {
  const files = fs.readdirSync(migrationsDir).filter(f => f.endsWith('.sql')).sort();
  for (const file of files) {
    const sql = fs.readFileSync(path.join(migrationsDir, file), 'utf8');
    await pool.query(sql);
  }
  console.log(`Migrations applied: ${files.length} files`);
}
```

<!-- backgroundService.js redundanter Guard (ZU ENTFERNEN): -->
```javascript
// Zeile 450 - ZU ENTFERNEN (cron-schedule 0 6 1 * * macht das bereits):
if (today.getDate() !== 1) {
  return;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: material.js Legacy-Singular-Fallback entfernen (ARCH-02)</name>
  <files>backend/routes/material.js</files>
  <action>
**POST-Endpunkt (ca. Zeile 349-369):**
1. `const { title, description, event_id, event_ids, jahrgang_id, jahrgang_ids, tag_ids } = req.body;` aendern zu `const { title, description, event_ids, jahrgang_ids, tag_ids } = req.body;` (event_id und jahrgang_id entfernen).
2. `const resolvedEventIds = event_ids || (event_id ? [event_id] : []);` ersetzen durch `const resolvedEventIds = event_ids || [];`
3. `const resolvedJahrgangIds = jahrgang_ids || (jahrgang_id ? [jahrgang_id] : []);` ersetzen durch `const resolvedJahrgangIds = jahrgang_ids || [];`

**PUT-Endpunkt (ca. Zeile 391-455):**
1. `const { title, description, event_id, event_ids, jahrgang_id, jahrgang_ids, tag_ids } = req.body;` aendern zu `const { title, description, event_ids, jahrgang_ids, tag_ids } = req.body;`
2. `const resolvedEventIds = event_ids !== undefined ? event_ids : (event_id !== undefined ? (event_id ? [event_id] : []) : undefined);` ersetzen durch `const resolvedEventIds = event_ids;`
3. `const resolvedJahrgangIds = jahrgang_ids !== undefined ? jahrgang_ids : (jahrgang_id !== undefined ? (jahrgang_id ? [jahrgang_id] : []) : undefined);` ersetzen durch `const resolvedJahrgangIds = jahrgang_ids;`

Die Downstream-Logik (INSERT/DELETE + INSERT) bleibt unveraendert — `resolvedEventIds` und `resolvedJahrgangIds` werden bereits korrekt verwendet.
  </action>
  <verify>
    <automated>grep -n "event_id[^s]" /Users/simonluthe/Documents/Konfipoints/backend/routes/material.js | grep -v "material_events\|event_id_" | wc -l</automated>
  </verify>
  <done>Ausgabe ist 0 — kein singulaeres event_id oder jahrgang_id mehr im Verarbeitungscode. node --check besteht.</done>
</task>

<task type="auto">
  <name>Task 2: database.js schema_migrations Tracking einfuehren (ARCH-03)</name>
  <files>backend/database.js</files>
  <action>
`runMigrations`-Funktion (Zeilen 18-33) vollstaendig ersetzen:

```javascript
async function runMigrations(pool) {
  // Tracking-Tabelle sicherstellen (idempotent)
  await pool.query(`
    CREATE TABLE IF NOT EXISTS schema_migrations (
      name TEXT PRIMARY KEY,
      applied_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    )
  `);

  const migrationsDir = path.join(__dirname, 'migrations');
  const files = fs.readdirSync(migrationsDir)
    .filter(f => f.endsWith('.sql'))
    .sort();

  // Bereits ausgefuehrte Migrationen laden
  const { rows: applied } = await pool.query('SELECT name FROM schema_migrations');
  const appliedSet = new Set(applied.map(r => r.name));

  let newCount = 0;
  for (const file of files) {
    if (appliedSet.has(file)) {
      continue; // Bereits ausgefuehrt, ueberspringen
    }
    const sql = fs.readFileSync(path.join(migrationsDir, file), 'utf8');
    try {
      await pool.query(sql);
      await pool.query('INSERT INTO schema_migrations (name) VALUES ($1)', [file]);
      newCount++;
    } catch (err) {
      console.error(`Migration failed: ${file}`, err.message);
      throw err;
    }
  }
  if (newCount > 0) {
    console.log(`Migrations applied: ${newCount} new (${files.length} total)`);
  } else {
    console.log(`Migrations: alle ${files.length} bereits ausgefuehrt`);
  }
}
```

Die `pool.query('SELECT NOW()')` Zeile und der `.then(() => runMigrations(pool))` Aufruf bleiben unveraendert.

**WICHTIG:** Beim ersten Deployment nach diesem Aenderung laufen ALLE bestehenden Migrationen nochmal durch (da schema_migrations leer ist). Das ist korrekt und sicher, da alle bestehenden Migrations idempotent sind (IF NOT EXISTS Guards).
  </action>
  <verify>
    <automated>grep -n "schema_migrations" /Users/simonluthe/Documents/Konfipoints/backend/database.js | wc -l</automated>
  </verify>
  <done>Ausgabe ist mindestens 3 (CREATE TABLE, SELECT, INSERT). node --check besteht.</done>
</task>

<task type="auto">
  <name>Task 3: backgroundService.js doppelten Date-Guard entfernen (CLN-01)</name>
  <files>backend/services/backgroundService.js</files>
  <action>
In `checkWrappedTriggers` (ca. Zeile 445-452) den manuellen Date-Guard entfernen:

**Vor der Aenderung (Zeilen 447-452):**
```javascript
const today = new Date();

// Nur am 1. des Monats ausfuehren
if (today.getDate() !== 1) {
  return;
}
```

**Nach der Aenderung:**
```javascript
const today = new Date();
```

Die Variable `today` wird weiterhin benoetigt (sie wird in den nachfolgenden DB-Queries als Parameter verwendet: `today.getMonth() + 1`, `today.getFullYear()`). Nur der `if`-Block entfernen, nicht `const today = new Date();`.

Der node-cron Schedule `0 6 1 * *` (1. des Monats um 6 Uhr) ist die einzige Ausfuehrungs-Bedingung.
  </action>
  <verify>
    <automated>grep -n "getDate() !== 1" /Users/simonluthe/Documents/Konfipoints/backend/services/backgroundService.js | wc -l</automated>
  </verify>
  <done>Ausgabe ist 0 — Guard entfernt. node --check besteht.</done>
</task>

</tasks>

<verification>
```bash
# material.js: kein Legacy-Singular-Format
grep -n "event_id[^s_]" /Users/simonluthe/Documents/Konfipoints/backend/routes/material.js | grep -v "//\|material_event"

# database.js: schema_migrations vorhanden
grep "schema_migrations" /Users/simonluthe/Documents/Konfipoints/backend/database.js

# backgroundService.js: Guard weg
grep "getDate() !== 1" /Users/simonluthe/Documents/Konfipoints/backend/services/backgroundService.js
# Erwartet: (leer)

# Syntax-Check alle 3 Dateien
node --check /Users/simonluthe/Documents/Konfipoints/backend/routes/material.js
node --check /Users/simonluthe/Documents/Konfipoints/backend/database.js
node --check /Users/simonluthe/Documents/Konfipoints/backend/services/backgroundService.js
```
</verification>

<success_criteria>
- material.js: event_id/jahrgang_id Singular-Fallback komplett entfernt (ARCH-02)
- database.js: schema_migrations Tabelle wird erstellt, jede Migration nur einmal ausgefuehrt (ARCH-03)
- backgroundService.js: doppelter Date-Guard entfernt, today-Variable erhalten (CLN-01)
- Alle 3 Dateien bestehen node --check
</success_criteria>

<output>
Nach Abschluss `.planning/phases/89-architektur-cleanup/89-02-SUMMARY.md` erstellen.
</output>
