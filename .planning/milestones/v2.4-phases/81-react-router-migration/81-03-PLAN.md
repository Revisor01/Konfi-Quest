---
phase: 81-react-router-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 81-01
  - 81-02
files_modified:
  - frontend/package.json
autonomous: false
requirements:
  - RR-01
  - RR-02
  - RR-03

must_haves:
  truths:
    - "Kein useHistory Import im gesamten Frontend-Quellcode"
    - "@types/react-router und @types/react-router-dom koennen aus devDependencies entfernt werden"
    - "TypeScript-Build fehlerfrei nach Entfernung der @types-Packages"
    - "App startet ohne Fehler"
  artifacts:
    - path: "frontend/package.json"
      provides: "Bereinigte devDependencies ohne @types/react-router*"
      contains: "react-router-dom"
  key_links:
    - from: "frontend/package.json"
      to: "TypeScript-Build"
      via: "npx tsc --noEmit"
      pattern: "noEmit"
---

<objective>
Verifikation der vollstaendigen Migration und Bereinigung der @types-Packages.

Purpose: Nach Plan 01 und 02 ist kein useHistory mehr im Code. Dieser Plan verifiziert die vollstaendige Migration, entfernt die @types/react-router-Packages (die nur fuer useHistory relevant waren) und fuehrt einen manuellen Smoke-Test durch.
Output: Sauberes package.json, verifizierter TypeScript-Build, manuell getestete Navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-react-router-migration/81-01-SUMMARY.md
@.planning/phases/81-react-router-migration/81-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vollstaendige Migration verifizieren und @types-Packages entfernen</name>
  <files>
    frontend/package.json
  </files>
  <read_first>
    - frontend/package.json (vollstaendig lesen)
  </read_first>
  <action>
1. Pruefen ob useHistory noch irgendwo vorkommt:
```bash
grep -r "useHistory" /Users/simonluthe/Documents/Konfipoints/frontend/src/
```
Falls Treffer: Zuerst die verbliebenen Stellen aus Plan 01/02 nachmigrieren (selbe Patterns anwenden), bevor mit diesem Task fortgefahren wird.

2. TypeScript-Build pruefen:
```bash
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1
```
Falls Fehler: Fehler beheben bevor @types entfernt werden.

3. Nur wenn 0 useHistory-Treffer UND 0 TypeScript-Fehler:
   In frontend/package.json die devDependencies bereinigen:
   - `"@types/react-router": "^5.1.20"` entfernen
   - `"@types/react-router-dom": "^5.3.3"` entfernen

   BEACHTEN: `react-router-dom` (ohne @types) und `@ionic/react-router` BLEIBEN. Nur die TypeScript-Typen-Packages werden entfernt, weil useIonRouter eigene Typen aus @ionic/react mitbringt.

4. npm install ausfuehren:
```bash
cd /Users/simonluthe/Documents/Konfipoints/frontend && npm install
```

5. TypeScript-Build erneut pruefen nach Entfernung:
```bash
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1
```
Falls neue Fehler durch fehlende @types entstehen: @types-Packages wieder hinzufuegen und in SUMMARY dokumentieren warum sie benoetigt werden.
  </action>
  <verify>
    <automated>grep -r "useHistory" /Users/simonluthe/Documents/Konfipoints/frontend/src/ && echo "FEHLER: useHistory noch vorhanden" || echo "OK: Migration vollstaendig"</automated>
  </verify>
  <done>
    grep -r "useHistory" src/ gibt 0 Treffer.
    cd frontend && npx tsc --noEmit gibt 0 Fehler.
    @types/react-router und @types/react-router-dom sind aus package.json entfernt (oder mit Begruendung behalten).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    useHistory → useIonRouter Migration in allen 14 Frontend-Dateien + MainTabs.tsx Wrapper-Komponenten.
    @types/react-router-Packages aus devDependencies entfernt.
  </what-built>
  <how-to-verify>
    1. App starten: `cd /Users/simonluthe/Documents/Konfipoints/frontend && npm run dev`
    2. Als Konfi einloggen → pruefen ob Dashboard erscheint (history.replace-Migration)
    3. Events-Tab oeffnen → auf ein Event klicken → Back-Button testen (router.goBack)
    4. Als Admin einloggen → Settings-Seite oeffnen → 3-4 Navigationspunkte anklicken
    5. Als Teamer einloggen → Events-Tab, Profile-Tab testen
    6. Chat oeffnen → Raum betreten → zuruecknavigieren
    7. Logout → Login erneut (sicherstellen kein Zurueck-Button zur Login-Seite fuehrt nach erfolgreichem Login)

    Erwartetes Verhalten: Identisch zu vor der Migration. Keine weissen Screens, keine Navigationsfehler.
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn Navigation korrekt funktioniert, oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

<verification>
```bash
# Finale Gesamtpruefung
grep -r "useHistory" /Users/simonluthe/Documents/Konfipoints/frontend/src/

# TypeScript sauber
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit

# useIonRouter-Migrationen vorhanden
grep -r "useIonRouter" /Users/simonluthe/Documents/Konfipoints/frontend/src/components/ | wc -l
```
</verification>

<success_criteria>
- grep -r "useHistory" frontend/src/ gibt exakt 0 Treffer
- cd frontend && npx tsc --noEmit gibt 0 Fehler
- Manuelle Smoke-Tests: Login, Navigation, Back-Button in allen Bereichen funktioniert korrekt
- @types/react-router* nicht mehr in package.json (oder mit Kommentar-Begruendung behalten)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/81-react-router-migration/81-03-SUMMARY.md` erstellen.
</output>
