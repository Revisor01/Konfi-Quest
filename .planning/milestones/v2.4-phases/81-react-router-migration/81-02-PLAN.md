---
phase: 81-react-router-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminSettingsPage.tsx
  - frontend/src/components/admin/pages/AdminEventsPage.tsx
  - frontend/src/components/admin/pages/AdminKonfisPage.tsx
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  - frontend/src/components/chat/pages/ChatOverviewPage.tsx
  - frontend/src/components/layout/MainTabs.tsx
autonomous: true
requirements:
  - RR-01
  - RR-02
  - RR-03

must_haves:
  truths:
    - "Kein useHistory Import mehr in Admin-, Teamer-, Chat- und Layout-Dateien"
    - "AdminSettingsPage navigiert via useIonRouter (9 push-Aufrufe)"
    - "TeamerDashboardPage uebergibt State korrekt via useIonRouter fuenftem Parameter"
    - "MainTabs.tsx Route render-props nutzen Wrapper-Komponenten mit useIonRouter statt props.history"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      provides: "Admin Settings Navigation via useIonRouter"
      contains: "useIonRouter"
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "Teamer Navigation mit State via useIonRouter"
      contains: "useIonRouter"
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Route Wrapper-Komponenten ohne props.history"
      contains: "useIonRouter"
  key_links:
    - from: "MainTabs.tsx"
      to: "KonfiDetailView"
      via: "KonfiDetailRoute Wrapper-Komponente mit useIonRouter"
      pattern: "KonfiDetailRoute"
    - from: "TeamerDashboardPage.tsx"
      to: "useIonRouter"
      via: "router.push('/teamer/events', 'forward', 'push', undefined, state)"
      pattern: "forward.*push.*undefined"
---

<objective>
useHistory in Admin-, Teamer-, Chat-Komponenten und MainTabs.tsx durch useIonRouter ersetzen.

Purpose: Zweite Haelfte der Migration (parallel zu Plan 01). Admin + Teamer + Chat + Layout-Shell werden bereinigt. MainTabs.tsx erhaelt Wrapper-Komponenten statt props.history in Route render-props.
Output: 8 Dateien ohne useHistory, MainTabs mit sauberen Wrapper-Komponenten.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Migrations-API: Drop-In-Ersatz fuer useHistory -->

Migration Pattern (IMMER so umsetzen):
```typescript
// ENTFERNEN:
import { useHistory } from 'react-router-dom';
const history = useHistory();

// HINZUFUEGEN:
import { useIonRouter } from '@ionic/react';
const router = useIonRouter();
```

API-Aequivalenz:
- history.push('/pfad')              → router.push('/pfad')
- history.replace('/pfad')           → router.push('/pfad', 'root', 'replace')
- history.goBack()                   → router.goBack()
- history.push('/pfad', stateObj)    → router.push('/pfad', 'forward', 'push', undefined, stateObj)

MainTabs.tsx Wrapper-Pattern (Route render-prop → Wrapper-Komponente):
```typescript
// VORHER:
<Route path="/admin/konfis/:id" render={(props) => {
  const konfiId = parseInt(props.match.params.id);
  return <KonfiDetailView konfiId={konfiId} onBack={() => props.history.goBack()} />;
}} />

// NACHHER:
const KonfiDetailRoute: React.FC<RouteComponentProps<{id: string}>> = ({ match }) => {
  const router = useIonRouter();
  return <KonfiDetailView konfiId={parseInt(match.params.id)} onBack={() => router.goBack()} />;
};
// ...in JSX:
<Route path="/admin/konfis/:id" component={KonfiDetailRoute} />
```

RouteComponentProps bleibt aus 'react-router': `import { RouteComponentProps } from 'react-router'`

Kommentar in jede migrierte Datei:
// useIonRouter: Ionic 8 API - bei Ionic v9 ggf. auf useNavigate migrieren
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Admin + Teamer + Chat Komponenten migrieren</name>
  <files>
    frontend/src/components/admin/pages/AdminSettingsPage.tsx
    frontend/src/components/admin/pages/AdminEventsPage.tsx
    frontend/src/components/admin/pages/AdminKonfisPage.tsx
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx
    frontend/src/components/chat/pages/ChatOverviewPage.tsx
  </files>
  <read_first>
    - frontend/src/components/admin/pages/AdminSettingsPage.tsx (vollstaendig lesen - 9 history.push)
    - frontend/src/components/admin/pages/AdminEventsPage.tsx (vollstaendig lesen)
    - frontend/src/components/admin/pages/AdminKonfisPage.tsx (vollstaendig lesen)
    - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx (vollstaendig lesen - history.push mit State!)
    - frontend/src/components/teamer/pages/TeamerProfilePage.tsx (vollstaendig lesen)
    - frontend/src/components/teamer/pages/TeamerEventsPage.tsx (vollstaendig lesen - useHistory und useLocation gleichzeitig)
    - frontend/src/components/chat/pages/ChatOverviewPage.tsx (vollstaendig lesen)
  </read_first>
  <action>
In allen 7 Dateien:
1. Import-Zeile anpassen: useHistory aus 'react-router-dom' entfernen, useIonRouter aus '@ionic/react' hinzufuegen.
   ACHTUNG TeamerEventsPage: importiert BEIDE `useLocation` und `useHistory` aus 'react-router-dom' --
   nur useHistory entfernen, useLocation BLEIBT: `import { useLocation } from 'react-router-dom'` behalten.
2. Hook-Aufruf: `const history = useHistory()` → `const router = useIonRouter()`
3. Aufrufe ersetzen:

AdminSettingsPage.tsx (9 Stellen, alle history.push):
- history.push('/admin/profile')                → router.push('/admin/profile')
- history.push('/admin/users')                  → router.push('/admin/users')
- history.push('/admin/settings/dashboard')     → router.push('/admin/settings/dashboard')
- history.push('/admin/organizations')          → router.push('/admin/organizations')
- history.push('/admin/activities')             → router.push('/admin/activities')
- history.push('/admin/badges')                 → router.push('/admin/badges')
- history.push('/admin/settings/jahrgaenge')    → router.push('/admin/settings/jahrgaenge')
- history.push('/admin/settings/categories')    → router.push('/admin/settings/categories')
- history.push('/admin/settings/levels')        → router.push('/admin/settings/levels')
- history.push('/admin/material')               → router.push('/admin/material')
- history.push('/admin/settings/certificates')  → router.push('/admin/settings/certificates')
(Datei vollstaendig lesen, tatsaechliche Anzahl kann abweichen)

AdminEventsPage.tsx:
- history.push(`/admin/events/${event.id}`)  → router.push(`/admin/events/${event.id}`)

AdminKonfisPage.tsx:
- history.push(`/admin/konfis/${konfi.id}`)  → router.push(`/admin/konfis/${konfi.id}`)

TeamerDashboardPage.tsx (KRITISCH - State-Uebergabe):
- history.push('/teamer/events', {...})  → router.push('/teamer/events', 'forward', 'push', undefined, { selectedEventId: event.id })
  (Den genauen State-Shape aus der Datei lesen und exakt uebernehmen)
- history.push('/teamer/events')         → router.push('/teamer/events')
- history.push('/teamer/profile/badges') → router.push('/teamer/profile/badges')

TeamerProfilePage.tsx:
- history.push('/teamer/profile/badges')       → router.push('/teamer/profile/badges')
- history.push('/teamer/profile/konfi-stats')  → router.push('/teamer/profile/konfi-stats')

TeamerEventsPage.tsx:
- Datei vollstaendig lesen und alle history-Aufrufe durch router ersetzen.
  useLocation bleibt unveraendert aus 'react-router-dom'.

ChatOverviewPage.tsx:
- history.push(`${basePath}/chat/room/${room.id}`)  → router.push(`${basePath}/chat/room/${room.id}`)

4. Kommentar nach dem useIonRouter-Import in jede Datei:
   // useIonRouter: Ionic 8 API - bei Ionic v9 ggf. auf useNavigate migrieren
  </action>
  <verify>
    <automated>grep -r "useHistory" /Users/simonluthe/Documents/Konfipoints/frontend/src/components/admin/ /Users/simonluthe/Documents/Konfipoints/frontend/src/components/teamer/ /Users/simonluthe/Documents/Konfipoints/frontend/src/components/chat/ && echo "FEHLER: useHistory noch vorhanden" || echo "OK: kein useHistory in admin/teamer/chat"</automated>
  </verify>
  <done>
    Kein useHistory mehr in admin/, teamer/, chat/.
    TeamerEventsPage behaelt useLocation aus 'react-router-dom'.
    TeamerDashboardPage uebergibt State korrekt als fuenften Parameter.
  </done>
</task>

<task type="auto">
  <name>Task 2: MainTabs.tsx Route render-props zu Wrapper-Komponenten migrieren</name>
  <files>
    frontend/src/components/layout/MainTabs.tsx
  </files>
  <read_first>
    - frontend/src/components/layout/MainTabs.tsx (vollstaendig lesen - Route render-props mit props.history.goBack())
  </read_first>
  <action>
MainTabs.tsx hat 4-5 Route render-props die `props.history.goBack()` verwenden. Diese werden zu Wrapper-Komponenten umgebaut:

1. Am Anfang der Datei (nach bestehenden Imports) hinzufuegen:
```typescript
import { useIonRouter } from '@ionic/react';
import { RouteComponentProps } from 'react-router';
// useIonRouter: Ionic 8 API - bei Ionic v9 ggf. auf useNavigate migrieren
```

2. Vor der MainTabs-Komponente (aber in derselben Datei) fuer jede betroffene Route eine Wrapper-Komponente erstellen:

Fuer `/admin/konfis/:id` Route:
```typescript
const KonfiDetailRoute: React.FC<RouteComponentProps<{id: string}>> = ({ match }) => {
  const router = useIonRouter();
  const konfiId = parseInt(match.params.id);
  return <KonfiDetailView konfiId={konfiId} onBack={() => router.goBack()} />;
};
```

Fuer `/admin/events/:id` Route (EventDetailView):
```typescript
const AdminEventDetailRoute: React.FC<RouteComponentProps<{id: string}>> = ({ match }) => {
  const router = useIonRouter();
  const eventId = parseInt(match.params.id);
  return <EventDetailView eventId={eventId} onBack={() => router.goBack()} />;
};
```

Fuer Chat-Room-Routes (es gibt mehrere, ggf. wiederverwendbar):
```typescript
const ChatRoomRoute: React.FC<RouteComponentProps<{id: string}>> = ({ match }) => {
  const router = useIonRouter();
  const roomId = parseInt(match.params.id);
  return <ChatRoomView roomId={roomId} onBack={() => router.goBack()} />;
};
```

3. Die bestehenden Route render-props durch component={...} Syntax ersetzen:
```typescript
// VORHER:
<Route path="/admin/konfis/:id" render={(props) => {
  const konfiId = parseInt(props.match.params.id);
  return <KonfiDetailView konfiId={konfiId} onBack={() => props.history.goBack()} />;
}} />

// NACHHER:
<Route path="/admin/konfis/:id" component={KonfiDetailRoute} />
```

Gleiches Prinzip fuer alle anderen betroffenen Routes.

4. Den alten useHistory-Import aus 'react-router-dom' entfernen (falls MainTabs useHistory direkt importiert hat).
   Props.history in render-props entfernt sich automatisch durch den Wechsel zu component={...}.

Datei vollstaendig lesen und alle Stellen identifizieren bevor Aenderungen vorgenommen werden.
Wrapper-Komponenten-Namen aus dem tatsaechlichen Code ableiten (RouteComponentProps-Shape und View-Props beachten).
  </action>
  <verify>
    <automated>grep -n "props\.history\|useHistory" /Users/simonluthe/Documents/Konfipoints/frontend/src/components/layout/MainTabs.tsx && echo "FEHLER: alte Patterns noch vorhanden" || echo "OK: keine props.history / useHistory in MainTabs.tsx"</automated>
  </verify>
  <done>
    Keine props.history mehr in MainTabs.tsx.
    Alle Route render-props durch component={WrapperKomponente} ersetzt.
    Wrapper-Komponenten nutzen useIonRouter.
    TypeScript-Build fehlerfrei fuer MainTabs.tsx.
  </done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

```bash
# Kein useHistory in admin, teamer, chat, layout
grep -r "useHistory" /Users/simonluthe/Documents/Konfipoints/frontend/src/components/admin/ /Users/simonluthe/Documents/Konfipoints/frontend/src/components/teamer/ /Users/simonluthe/Documents/Konfipoints/frontend/src/components/chat/ /Users/simonluthe/Documents/Konfipoints/frontend/src/components/layout/

# Kein props.history in MainTabs.tsx
grep "props\.history" /Users/simonluthe/Documents/Konfipoints/frontend/src/components/layout/MainTabs.tsx

# TypeScript-Fehler pruefen
cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30
```
</verification>

<success_criteria>
- grep -r "useHistory" src/components/admin/ src/components/teamer/ src/components/chat/ src/components/layout/ gibt 0 Treffer
- grep "props.history" src/components/layout/MainTabs.tsx gibt 0 Treffer
- grep -r "useIonRouter" src/components/admin/ gibt >= 3 Treffer
- cd frontend && npx tsc --noEmit liefert 0 Fehler in den migrierten Dateien
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/81-react-router-migration/81-02-SUMMARY.md` erstellen.
</output>
