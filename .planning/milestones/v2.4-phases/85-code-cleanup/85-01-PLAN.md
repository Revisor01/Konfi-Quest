---
phase: 85-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/server.js
  - backend/data/konfi-points.db
  - backend/data/konfi.db
  - backend/.gitignore
  - frontend/src/components/chat/modals/FileViewerModal.tsx
  - backend/routes/konfi.js
  - backend/routes/activities.js
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04
  - CLN-05
  - CLN-06

must_haves:
  truths:
    - "sqlite3 ist nicht mehr in backend/package.json vorhanden"
    - "backend/data/*.db Dateien existieren nicht mehr im Repository und sind in .gitignore eingetragen"
    - "Legacy-Multer upload = multer ist aus server.js entfernt"
    - "konfi.js und activities.js erhalten upload nicht mehr als Parameter"
    - "FileViewerModal.tsx aus chat/modals/ existiert nicht mehr"
    - "crypto require steht am Anfang von server.js vor den Multer-Definitionen"
    - "SMTP_SECURE Bug behoben: secure gesetzt auf process.env.SMTP_SECURE !== 'false'"
  artifacts:
    - path: "backend/package.json"
      provides: "Dependencies ohne sqlite3"
    - path: "backend/.gitignore"
      provides: "*.db Pattern zum Ausschluss"
    - path: "backend/server.js"
      provides: "Bereinigte server.js: kein Legacy-Multer, crypto oben, SMTP-Bug geloest"
  key_links:
    - from: "backend/server.js"
      to: "backend/routes/konfi.js"
      via: "konfiRoutes(db, auth, requestUpload) ohne upload-Parameter"
    - from: "backend/server.js"
      to: "backend/routes/activities.js"
      via: "adminActivitiesRoutes ohne upload-Parameter"
---

<objective>
SQLite-Abhaengigkeit entfernen, Legacy-DB-Dateien loeschen, Legacy-Multer-Konfiguration bereinigen,
tote Frontend-Komponente loeschen, crypto-Reihenfolge-Bug und SMTP_SECURE-Bug in server.js fixen.

Purpose: Reduziert Docker-Image-Groesse, beseitigt toten Code und schliesst zwei stille Bugs
(SMTP immer TLS trotz ENV-Wert, crypto-Callback-Race-Condition).
Output: Bereinigte package.json, server.js ohne Legacy-Code, keine .db-Dateien im Repo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-code-cleanup/85-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQLite-Dependency entfernen und DB-Dateien aus Repository loeschen</name>
  <files>backend/package.json, backend/data/konfi-points.db, backend/data/konfi.db, backend/.gitignore</files>
  <action>
Umsetzt CLN-01 und CLN-02.

1. backend/package.json lesen, dann die Zeile mit "sqlite3" aus dem dependencies-Block entfernen.
   Keine anderen Felder aendern, package-lock.json nicht anfassen.

2. Dateien loeschen (via Bash-Tool oder Datei-Loeschung):
   - backend/data/konfi-points.db
   - backend/data/konfi.db

3. Root .gitignore (oder backend/.gitignore falls vorhanden) lesen und pruefen ob bereits ein *.db Eintrag
   vorhanden ist. Falls nicht: die Zeile "backend/data/*.db" zur Root-.gitignore ergaenzen.
  </action>
  <verify>
    <automated>node -e "const p=require('./backend/package.json'); console.log(p.dependencies.sqlite3 ? 'FAIL: sqlite3 noch da' : 'OK: sqlite3 entfernt')"</automated>
  </verify>
  <done>
    - sqlite3 nicht in backend/package.json dependencies
    - backend/data/*.db Dateien existieren nicht mehr als Git-getrackte Dateien
    - *.db oder backend/data/*.db in .gitignore
  </done>
</task>

<task type="auto">
  <name>Task 2: server.js bereinigen und FileViewerModal loeschen</name>
  <files>backend/server.js, backend/routes/konfi.js, backend/routes/activities.js, frontend/src/components/chat/modals/FileViewerModal.tsx</files>
  <action>
Umsetzt CLN-03, CLN-04, CLN-05, CLN-06. Datei lesen, dann gezielt aendern.

CLN-05 — crypto require nach oben verschieben:
In server.js die Zeile "const crypto = require('crypto');" von Zeile ~370 entfernen und
ganz oben nach den anderen require-Importen einfuegen (nach Zeile 14, nach helmet require).
Damit steht crypto garantiert vor chatUpload und materialUpload zur Verfuegung.

CLN-06 — SMTP_SECURE Bug fixen:
server.js Zeile ~147 aendern von:
  secure: process.env.SMTP_SECURE === 'true' || true,
zu:
  secure: process.env.SMTP_SECURE !== 'false',
Das entspricht dem Muster in emailService.js (SMTP_SECURE !== 'false').

CLN-03 — Legacy-Multer entfernen:
In server.js den Kommentarblock "Legacy upload for other parts" und die zugehoerige
const upload = multer({ dest: uploadsDir, ... }) Deklaration (ca. Zeilen 356-367) komplett loeschen.

Dann die beiden Aufrufstellen anpassen:
- adminActivitiesRoutes(db, rbacVerifier, roleHelpers, badgesRouter.checkAndAwardBadges, upload)
  => adminActivitiesRoutes(db, rbacVerifier, roleHelpers, badgesRouter.checkAndAwardBadges)
- konfiRoutes(db, { verifyTokenRBAC: rbacVerifier }, upload, requestUpload)
  => konfiRoutes(db, { verifyTokenRBAC: rbacVerifier }, requestUpload)

In backend/routes/konfi.js Signatur Zeile 16 anpassen:
  module.exports = (db, rbacMiddleware, upload, requestUpload) =>
  => module.exports = (db, rbacMiddleware, requestUpload) =>
(upload wird in konfi.js nirgendwo mit upload.* aufgerufen; nur requestUpload.single wird genutzt)

In backend/routes/activities.js Signatur Zeile 11 anpassen:
  module.exports = (db, rbacVerifier, { requireAdmin, requireTeamer }, checkAndAwardBadges, upload) =>
  => module.exports = (db, rbacVerifier, { requireAdmin, requireTeamer }, checkAndAwardBadges) =>
(upload wird in activities.js nirgendwo mit upload.* aufgerufen)

CLN-04 — FileViewerModal.tsx loeschen:
Vor dem Loeschen mit Grep pruefen, dass kein aktiver Import im Frontend existiert:
  grep -r "FileViewerModal" frontend/src --include="*.tsx" --include="*.ts"
Laut Codebase-Audit ist kein aktiver Import vorhanden. Datei loeschen.
Falls doch ein Import gefunden wird: diesen zuerst auf shared/FileViewerModal.tsx umbiegen.
  </action>
  <verify>
    <automated>grep -c "const upload = multer" backend/server.js || echo "OK: Legacy upload entfernt"; grep -n "SMTP_SECURE" backend/server.js; grep -n "const crypto" backend/server.js | head -3</automated>
  </verify>
  <done>
    - server.js: kein "const upload = multer({ dest:" Block mehr vorhanden
    - server.js: crypto require steht in Zeile 1-20 (Anfang der Datei)
    - server.js: SMTP_SECURE Ausdruck lautet "!== 'false'" statt "=== 'true' || true"
    - konfi.js Signatur: 3 Parameter (db, rbacMiddleware, requestUpload)
    - activities.js Signatur: 4 Parameter (db, rbacVerifier, roleHelpers, checkAndAwardBadges)
    - FileViewerModal.tsx existiert nicht mehr
  </done>
</task>

</tasks>

<verification>
Backend startet fehlerfrei: cd backend && node -e "require('./server')" bricht nicht ab.
Kein "upload is not defined" Fehler beim Laden der Route-Module.
</verification>

<success_criteria>
- sqlite3 nicht in backend/package.json
- backend/data/*.db nicht im Repository
- backend/server.js: kein Legacy-Multer-Block, crypto ganz oben, SMTP_SECURE korrekt
- frontend/src/components/chat/modals/FileViewerModal.tsx geloescht
- konfi.js und activities.js: upload-Parameter aus Signatur entfernt
</success_criteria>

<output>
Nach Abschluss: .planning/phases/85-code-cleanup/85-01-SUMMARY.md anlegen.
</output>
