---
phase: 83-performance-capacitor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/chat.js
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "GET /rooms/:id/messages fuehrt maximal 3 DB-Queries aus (Nachrichten + Reactions + Votes), nicht bis zu 400"
    - "Reactions werden korrekt den Nachrichten zugeordnet (gleiche Daten wie vorher)"
    - "Poll-Votes werden korrekt dem jeweiligen poll_id zugeordnet"
    - "Der Endpoint antwortet ohne Fehler bei Raeumen ohne Reactions"
  artifacts:
    - path: "backend/routes/chat.js"
      provides: "N+1-freier GET /rooms/:id/messages Handler"
      contains: "ANY($1::int[])"
  key_links:
    - from: "backend/routes/chat.js"
      to: "chat_message_reactions"
      via: "Bulk-Query mit message_id = ANY($1::int[])"
      pattern: "ANY\\(\\$1::int\\[\\]\\)"
    - from: "backend/routes/chat.js"
      to: "chat_poll_votes"
      via: "Bulk-Query mit poll_id = ANY($1::int[])"
      pattern: "poll_id = ANY"
---

<objective>
Chat-Nachrichten-Endpoint von N+1-Queries auf Bulk-Queries umstellen.

Purpose: Jeder Aufruf von /rooms/:id/messages fuehrt derzeit bis zu 400 separate DB-Queries aus (eine pro Nachricht fuer Reactions, eine fuer Poll-Votes). Das verlangsamt den Endpoint stark bei aktiven Chat-Raeumen.
Output: Ein refaktorierter Handler, der Reactions und Poll-Votes in je einer einzigen Bulk-Query laedt und im JS-Code den Nachrichten zuordnet.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aktueller N+1-Code in backend/routes/chat.js (Zeilen 596-633) -->
<!-- Jede Nachricht triggert eine eigene reactions-Query und ggf. eine votes-Query -->

Bestehende Queries (zu ersetzen):
```javascript
// AKTUELL (N+1 — bis zu 400 Queries):
const processedMessages = await Promise.all(messages.map(async (msg) => {
  const { rows: reactions } = await db.query(
    `SELECT r.id, r.emoji, r.user_id, r.user_type, u.display_name as user_name
     FROM chat_message_reactions r
     JOIN users u ON r.user_id = u.id
     WHERE r.message_id = $1
     ORDER BY r.created_at ASC`,
    [msg.id]
  );
  msg.reactions = reactions || [];

  if (msg.message_type === 'poll' && msg.options) {
    const { rows: votes } = await db.query(
      `SELECT v.*, u.display_name as voter_name
       FROM chat_poll_votes v
       LEFT JOIN users u ON v.user_id = u.id
       WHERE v.poll_id = $1`,
      [msg.poll_id]
    );
    msg.votes = votes || [];
  }
  return msg;
}));
```

Zielstruktur der Nachricht (unveraendert):
- msg.reactions: Array von { id, emoji, user_id, user_type, user_name }
- msg.votes: Array von { ..., voter_name } (nur bei poll-Nachrichten)
- msg.options: JSON-parsed Array (nur bei poll-Nachrichten)
- msg.multiple_choice: Boolean (nur bei poll-Nachrichten)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: N+1 Reactions-Query durch Bulk-Query ersetzen</name>
  <files>backend/routes/chat.js</files>
  <read_first>
    - backend/routes/chat.js (Zeilen 580-640) — vollstaendiger Kontext des GET /rooms/:id/messages Handlers
  </read_first>
  <action>
Ersetze den gesamten Promise.all-Block (Zeilen 598-630) durch folgende Bulk-Query-Logik:

1. Aus den geladenen Nachrichten alle IDs extrahieren:
   ```javascript
   const messageIds = messages.map(m => m.id);
   ```

2. Wenn messageIds leer ist (kein Nachrichten): processedMessages = [] setzen und Promise.all entfernen.

3. Reactions in einer einzigen Bulk-Query laden (statt N Queries):
   ```javascript
   const { rows: allReactions } = await db.query(
     `SELECT r.id, r.emoji, r.user_id, r.user_type, u.display_name as user_name, r.message_id
      FROM chat_message_reactions r
      JOIN users u ON r.user_id = u.id
      WHERE r.message_id = ANY($1::int[])
      ORDER BY r.created_at ASC`,
     [messageIds]
   );
   ```

4. Reactions-Map aufbauen (message_id -> reactions[]):
   ```javascript
   const reactionsMap = {};
   for (const r of allReactions) {
     if (!reactionsMap[r.message_id]) reactionsMap[r.message_id] = [];
     reactionsMap[r.message_id].push(r);
   }
   ```

5. Poll-IDs aus poll-Nachrichten ermitteln:
   ```javascript
   const pollMessages = messages.filter(m => m.message_type === 'poll' && m.options && m.poll_id);
   const pollIds = pollMessages.map(m => m.poll_id);
   ```

6. Votes in einer einzigen Bulk-Query laden (nur wenn pollIds.length > 0):
   ```javascript
   let votesMap = {};
   if (pollIds.length > 0) {
     const { rows: allVotes } = await db.query(
       `SELECT v.*, u.display_name as voter_name
        FROM chat_poll_votes v
        LEFT JOIN users u ON v.user_id = u.id
        WHERE v.poll_id = ANY($1::int[])`,
       [pollIds]
     );
     for (const v of allVotes) {
       if (!votesMap[v.poll_id]) votesMap[v.poll_id] = [];
       votesMap[v.poll_id].push(v);
     }
   }
   ```

7. Nachrichten mit Daten anreichern (synchron, kein await):
   ```javascript
   const processedMessages = messages.map(msg => {
     msg.reactions = reactionsMap[msg.id] || [];

     if (msg.message_type === 'poll' && msg.options) {
       try {
         msg.options = JSON.parse(msg.options);
       } catch (e) {
         console.error('Failed to parse poll options:', msg.options, e);
         msg.options = [];
       }
       msg.votes = votesMap[msg.poll_id] || [];
       msg.multiple_choice = Boolean(msg.multiple_choice);
     }
     return msg;
   });
   ```

8. Die letzte Zeile `res.json(after ? processedMessages : processedMessages.reverse())` unveraendert lassen.

WICHTIG: Das `message_id`-Feld wird in der SELECT-Clause der Reactions-Query benoetigt (`r.message_id`), damit die Zuordnung funktioniert. Die Spalte ist in der Tabelle vorhanden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && node -e "const chat = require('./backend/routes/chat.js'); console.log('Syntax OK')" 2>&1 | grep -E "OK|Error|SyntaxError"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ANY(\$1::int\[\])" backend/routes/chat.js` findet mindestens 1 Treffer (Reactions-Bulk-Query)
    - `grep -n "Promise.all(messages.map" backend/routes/chat.js` findet keinen Treffer mehr
    - `node -e "require('./backend/routes/chat.js')"` wirft keinen Syntaxfehler
    - Die Datei enthaelt den Text `reactionsMap` und `votesMap`
  </acceptance_criteria>
  <done>GET /rooms/:id/messages laedt Reactions und Poll-Votes mit maximal 3 DB-Queries statt bis zu 400. Bestehende Datenstruktur der Antwort ist unveraendert.</done>
</task>

</tasks>

<verification>
```bash
# Syntax-Check
cd /Users/simonluthe/Documents/Konfipoints && node -e "require('./backend/routes/chat.js')" 2>&1

# Bulk-Query vorhanden
grep -c "ANY(\$1::int\[\])" backend/routes/chat.js

# N+1-Pattern entfernt
grep -c "Promise.all(messages.map" backend/routes/chat.js  # muss 0 sein

# reactionsMap und votesMap vorhanden
grep -n "reactionsMap\|votesMap" backend/routes/chat.js
```
</verification>

<success_criteria>
- Maximal 3 DB-Queries pro GET /rooms/:id/messages Aufruf (Nachrichten + Reactions-Bulk + Votes-Bulk)
- Reactions und Poll-Votes werden korrekt den Nachrichten zugeordnet
- Keine TypeScript/Node.js Syntaxfehler
- Kein `Promise.all(messages.map(async` Pattern mehr im Handler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/83-performance-capacitor/83-01-SUMMARY.md` erstellen.
</output>
