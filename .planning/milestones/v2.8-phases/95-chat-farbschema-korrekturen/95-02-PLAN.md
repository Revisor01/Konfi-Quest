---
phase: 95-chat-farbschema-korrekturen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx
  - frontend/src/components/chat/modals/MembersModal.tsx
  - frontend/src/components/chat/modals/PollModal.tsx
autonomous: true
requirements: [TCH-01, TCH-02, TCH-04, ACH-02, ACH-03, ACH-04]

must_haves:
  truths:
    - "User-Liste in SimpleCreateChatModal zeigt korrekte Nutzer mit Jahrgangs-Filter"
    - "Teamer:innen koennen Gruppenchats erstellen (Segment sichtbar)"
    - "MembersModal laedt User mit Jahrgangs-Filter wie SimpleCreateChatModal"
    - "MembersModal nutzt gleiches visuelles Pattern wie SimpleCreateChatModal (app-list-item, Farben, Checkboxen)"
    - "PollModal hat kein doppeltes Padding"
    - "Chats werden zuverlaessig geladen ohne Reload-Probleme"
  artifacts:
    - path: "frontend/src/components/chat/modals/SimpleCreateChatModal.tsx"
      provides: "Teamer Gruppenchat-Erstellung"
      contains: "teamer"
    - path: "frontend/src/components/chat/modals/MembersModal.tsx"
      provides: "Performante User-Liste mit Jahrgangs-Filter"
    - path: "frontend/src/components/chat/modals/PollModal.tsx"
      provides: "Korrektes Padding ohne doppelte Abstaende"
  key_links:
    - from: "frontend/src/components/chat/modals/SimpleCreateChatModal.tsx"
      to: "backend/routes/chat.js"
      via: "POST /chat/rooms mit type group"
      pattern: "type.*group"
    - from: "frontend/src/components/chat/modals/MembersModal.tsx"
      to: "backend API"
      via: "GET /admin/users/me/jahrgaenge + GET /admin/konfis"
      pattern: "jahrgaenge"
---

<objective>
Chat-Bugs beheben: User-Liste korrekt laden, Gruppenchats fuer Teamer:innen freischalten, MembersModal auf SimpleCreateChatModal-Pattern umstellen, PollModal Doppel-Padding fixen, Chat-Ladeprobleme debuggen.

Purpose: Alle bekannten Chat-Bugs quer ueber die Rollen beheben
Output: Korrigierte Modals und zuverlaessiges Chat-Loading
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-chat-farbschema-korrekturen/95-CONTEXT.md

@frontend/src/components/chat/modals/SimpleCreateChatModal.tsx
@frontend/src/components/chat/modals/MembersModal.tsx
@frontend/src/components/chat/modals/PollModal.tsx
@backend/routes/chat.js

<interfaces>
<!-- SimpleCreateChatModal zeigt Segment NUR fuer Admins (Zeile 422): -->
```typescript
{isAdmin && (
  <IonToolbar>
    <IonSegment value={chatType} ...>
      <IonSegmentButton value="direct">Direktnachricht</IonSegmentButton>
      <IonSegmentButton value="group">Gruppenchat</IonSegmentButton>
    </IonSegment>
  </IonToolbar>
)}
```

<!-- MembersModal.loadAllUsers() nutzt KEINE Jahrgangs-Filterung (Zeile 103-127): -->
```typescript
const loadAllUsers = async () => {
  const [konfisRes, adminsRes] = await Promise.all([
    api.get('/admin/konfis'),
    api.get('/users').catch(() => ({ data: [] }))
  ]);
  // KEIN Jahrgangs-Filter! Laedt potentiell alle Konfis ohne Org-Filter
};
```

<!-- SimpleCreateChatModal.loadUsers() HAT Jahrgangs-Filter (Zeile 170-180): -->
```typescript
const [konfisRes, userJahrgangRes] = await Promise.all([
  api.get('/admin/konfis'),
  api.get('/admin/users/me/jahrgaenge').catch(() => ({ data: [] }))
]);
let allowedJahrgangIds: number[] = [];
if (userJahrgangRes.data.length > 0) {
  allowedJahrgangIds = userJahrgangRes.data.map((j: any) => j.jahrgang_id);
}
```

<!-- PollModal hat doppeltes Padding (Zeile 147-148): -->
```typescript
<IonContent className="app-gradient-background">
  <div style={{ padding: '16px', display: 'flex', flexDirection: 'column', gap: '16px' }}>
    <IonList inset={true}> <!-- IonList inset hat bereits eigenen Margin -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teamer Gruppenchats + MembersModal Jahrgangs-Filter + Pattern</name>
  <files>frontend/src/components/chat/modals/SimpleCreateChatModal.tsx, frontend/src/components/chat/modals/MembersModal.tsx</files>
  <read_first>frontend/src/components/chat/modals/SimpleCreateChatModal.tsx, frontend/src/components/chat/modals/MembersModal.tsx, frontend/src/types/user.ts</read_first>
  <action>
**SimpleCreateChatModal.tsx — Teamer Gruppenchats (TCH-02):**

1. Die Bedingung `{isAdmin && (` fuer das IonSegment (Zeile 422) aendern zu `{(isAdmin || user?.type === 'teamer') && (`. Damit sehen sowohl Admins als auch Teamer:innen die Auswahl zwischen Direktnachricht und Gruppenchat.

2. Die Konfis-only-Einschraenkung in Zeile 120-123 beruecksichtigt bereits `user?.type === 'konfi'`. Teamer:innen muessen wie Admins laden koennen. Pruefen ob der `loadUsers`-Block fuer `user?.type === 'admin'` auch fuer Teamer:innen greift. Falls es ein `else if (user?.type === 'admin')` gibt, zu `else if (user?.type === 'admin' || user?.type === 'teamer')` aendern.

3. Variable `isAdmin` pruefen — wenn definiert als `user?.type === 'admin'`, eine zusaetzliche Variable `const isTeamer = user?.type === 'teamer';` anlegen und ueberall wo `isAdmin` fuer Gruppenchat-Logik genutzt wird, zu `(isAdmin || isTeamer)` aendern.

**MembersModal.tsx — Jahrgangs-Filter (ACH-02, TCH-01):**

4. Die `loadAllUsers()` Funktion (Zeile 103-127) komplett ueberarbeiten. Statt `api.get('/admin/konfis')` ohne Filter, das gleiche Pattern wie SimpleCreateChatModal verwenden:

```typescript
const loadAllUsers = async () => {
  try {
    const [konfisRes, userJahrgangRes, adminsRes] = await Promise.all([
      api.get('/admin/konfis'),
      api.get('/admin/users/me/jahrgaenge').catch(() => ({ data: [] })),
      api.get('/admin/users').catch(() => ({ data: [] }))
    ]);

    // Jahrgangs-Filter wie SimpleCreateChatModal
    let allowedJahrgangIds: number[] = [];
    if (userJahrgangRes.data.length > 0) {
      allowedJahrgangIds = userJahrgangRes.data.map((j: any) => j.jahrgang_id);
    }

    const konfis: ChatUser[] = konfisRes.data
      .filter((konfi: any) => {
        if (allowedJahrgangIds.length > 0) {
          return konfi.jahrgang_id && allowedJahrgangIds.includes(konfi.jahrgang_id);
        }
        return true;
      })
      .map((konfi: any) => ({
        ...konfi,
        type: 'konfi' as const,
        jahrgang_name: konfi.jahrgang_name || konfi.jahrgang
      }));

    const adminUsers: ChatUser[] = adminsRes.data
      .filter((u: any) => u.role_name !== 'konfi')
      .map((admin: any) => ({
        ...admin,
        type: 'admin' as const,
        role_description: admin.role_title || admin.role_display_name
      }));

    setAllUsers([...konfis, ...adminUsers]);
  } catch (err) {
    console.error('Error loading users:', err);
  }
};
```

**MembersModal.tsx — Visuelles Pattern (ACH-03):**

5. Die User-Liste im Add-Mode soll das gleiche app-list-item Pattern wie SimpleCreateChatModal verwenden. Statt einfache IonCheckbox Items, konsistente Darstellung mit:
- `app-list-item` CSS-Klasse
- Farbige Akzente (Admin/Teamer: rosa, Konfi: lila)
- Jahrgangs-Anzeige als Subtitle
- IonCheckbox am Ende statt am Anfang

Pruefen was im Render-Bereich der verfuegbaren User steht und auf das Muster aus SimpleCreateChatModal angleichen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "isTeamer\|teamer" frontend/src/components/chat/modals/SimpleCreateChatModal.tsx && grep -c "jahrgaenge\|allowedJahrgangIds" frontend/src/components/chat/modals/MembersModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SimpleCreateChatModal zeigt IonSegment fuer Admins UND Teamer:innen
    - SimpleCreateChatModal loadUsers() funktioniert fuer Teamer:innen-Rolle
    - MembersModal.loadAllUsers() nutzt /admin/users/me/jahrgaenge fuer Jahrgangs-Filterung
    - MembersModal User-Liste nutzt app-list-item Pattern mit konsistenten Farben
  </acceptance_criteria>
  <done>Teamer:innen koennen Gruppenchats erstellen, MembersModal zeigt gefilterte User-Liste im korrekten Design-Pattern</done>
</task>

<task type="auto">
  <name>Task 2: PollModal Padding-Fix + Chat-Ladeprobleme debuggen</name>
  <files>frontend/src/components/chat/modals/PollModal.tsx, frontend/src/components/chat/ChatOverview.tsx</files>
  <read_first>frontend/src/components/chat/modals/PollModal.tsx, frontend/src/components/chat/ChatOverview.tsx, frontend/src/components/chat/ChatRoom.tsx</read_first>
  <action>
**PollModal Padding-Fix (ACH-04):**

1. In PollModal.tsx (Zeile 148) das aeussere Container-div hat `padding: '16px'` UND die IonList-Elemente darin haben `inset={true}` (was eigenen Margin/Padding mitbringt). Das doppelte Padding entfernen:

Aendere `<div style={{ padding: '16px', display: 'flex', flexDirection: 'column', gap: '16px' }}>` zu `<div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>`.

Die IonList inset liefert bereits korrekte Seitenabstaende. Der obere Abstand wird durch gap sichergestellt.

**Chat-Ladeprobleme debuggen (TCH-04):**

2. In ChatOverview.tsx pruefen ob SWR-Cache korrekt invalidiert wird:
   - Suche nach dem Daten-Lade-Hook (useOfflineQuery oder aehnlich) fuer `/chat/rooms`
   - Pruefen ob der Fetcher korrekt konfiguriert ist (refreshInterval, revalidateOnFocus)
   - Falls `revalidateOnFocus: false` gesetzt ist, aendern zu `revalidateOnFocus: true` damit Chats beim Zurueckkehren zur Liste neu geladen werden

3. In ChatRoom.tsx pruefen ob nach dem Senden einer Nachricht oder Verlassen eines Chats der SWR-Cache fuer `/chat/rooms` manuell invalidiert wird (`mutate('/chat/rooms')`). Falls nicht, an den relevanten Stellen `mutate` aufrufen.

4. Falls der Chat nicht laedt: Pruefen ob es Race Conditions gibt zwischen Socket.IO Events und SWR-Refetches. Socket-Listener die `setMessages` aufrufen sollten NICHT gleichzeitig einen SWR-Revalidate triggern.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "padding: '16px'" frontend/src/components/chat/modals/PollModal.tsx && cd frontend && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - PollModal Container-div hat KEIN padding: '16px' mehr (nur noch gap)
    - Doppeltes Padding (Container + IonList inset) ist eliminiert
    - Chat-Loading ist zuverlaessig (SWR revalidateOnFocus oder manuelles mutate)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>PollModal zeigt korrekte Abstaende, Chats laden zuverlaessig beim Navigieren und nach Aktionen</done>
</task>

</tasks>

<verification>
- PollModal: Kein doppeltes Padding sichtbar (nur IonList inset Margin)
- SimpleCreateChatModal: Teamer:innen sehen Direct/Group Segment
- MembersModal: User-Liste zeigt nur Konfis aus zugewiesenen Jahrgaengen
- Chat-Liste aktualisiert sich beim Zurueckkehren aus ChatRoom
- Frontend kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- TCH-01: Chat erstellen zeigt User-Liste korrekt an (Jahrgangs-Filter)
- TCH-02: Gruppenchats fuer Teamer:innen moeglich
- TCH-04: Chats werden zuverlaessig geladen
- ACH-02: User-Liste beim Chat-Erstellen performant laden
- ACH-03: MembersModal nutzt korrektes Pattern (app-list-item, Farben, Checkboxen)
- ACH-04: Poll-erstellen Modal hat korrekte Abstaende
</success_criteria>

<output>
After completion, create `.planning/phases/95-chat-farbschema-korrekturen/95-02-SUMMARY.md`
</output>
