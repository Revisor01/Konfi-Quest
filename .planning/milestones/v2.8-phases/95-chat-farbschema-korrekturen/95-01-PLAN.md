---
phase: 95-chat-farbschema-korrekturen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/variables.css
  - frontend/src/components/chat/ChatOverview.tsx
  - frontend/src/components/chat/ChatRoomSections.tsx
autonomous: true
requirements: [TCH-03, KCH-01, ACH-01]

must_haves:
  truths:
    - "Teamer/Team-Chats (type admin) erscheinen Rosa (#e11d48) in ChatOverview und ChatRoom Header"
    - "Konfi-Direktchats (type direct) erscheinen Lila (#5b21b6) in ChatOverview und ChatRoom Header"
    - "Jahrgangs-Chats (type jahrgang) erscheinen Tuerkis (#06b6d4) in ChatOverview und ChatRoom Header"
    - "Gruppen-Chats (type group) erscheinen Orange (#f97316) in ChatOverview und ChatRoom Header"
    - "Farben sind konsistent ueber alle drei Rollen (Admin, Teamer, Konfi)"
  artifacts:
    - path: "frontend/src/theme/variables.css"
      provides: "CSS-Klassen fuer Raumtyp-Farben"
      contains: "app-list-item--team"
    - path: "frontend/src/components/chat/ChatOverview.tsx"
      provides: "Farbzuordnung nach Raumtyp via CSS-Klassen"
    - path: "frontend/src/components/chat/ChatRoomSections.tsx"
      provides: "ChatHeader mit Raumtyp-Farbe"
  key_links:
    - from: "frontend/src/components/chat/ChatOverview.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS-Klassen app-list-item--team, app-list-item--jahrgang etc."
      pattern: "app-list-item--team|app-list-item--jahrgang"
    - from: "frontend/src/components/chat/ChatRoomSections.tsx"
      to: "frontend/src/components/chat/ChatRoom.tsx"
      via: "roomType prop an ChatHeader"
      pattern: "roomType"
---

<objective>
Einheitliches Chat-Farbschema ueber alle Rollen: Admin/Team-Chats Rosa, Konfi-Chats Lila, Jahrgangs-Chats Tuerkis, Gruppen-Chats Orange. Aktuell zeigen admin und jahrgang beide #06b6d4 (Tuerkis), Team-Chats fehlt Rosa komplett.

Purpose: Konsistente visuelle Unterscheidung der Chat-Typen fuer alle Nutzerrollen
Output: CSS-Klassen fuer Raumtyp-Farben, aktualisierte ChatOverview und ChatRoom Header
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-chat-farbschema-korrekturen/95-CONTEXT.md

@frontend/src/theme/variables.css
@frontend/src/components/chat/ChatOverview.tsx
@frontend/src/components/chat/ChatRoomSections.tsx
@frontend/src/components/chat/ChatRoom.tsx

<interfaces>
<!-- Aktuelle Farblogik in ChatOverview.tsx Zeile 382-385 (INLINE, muss ersetzt werden): -->
```typescript
const color = room.type === 'admin' ? '#06b6d4' :
              room.type === 'jahrgang' ? '#06b6d4' :
              room.type === 'group' ? '#f97316' : '#5b21b6';
```

<!-- ChatHeader in ChatRoomSections.tsx nimmt aktuell KEIN roomType prop -->
```typescript
interface ChatHeaderProps {
  roomName: string;
  isAdmin: boolean;
  canLeave: boolean;
  isOnline: boolean;
  onBack: () => void;
  onOpenMembers: () => void;
  onOpenPoll: () => void;
  onLeaveChat: () => void;
}
```

<!-- ChatRoom.tsx Zeile 980-989 uebergibt roomType NICHT an ChatHeader -->
```typescript
<ChatHeader
  roomName={getDisplayRoomName()}
  isAdmin={user?.type === 'admin'}
  canLeave={canLeaveChat()}
  isOnline={isOnline}
  onBack={onBack}
  onOpenMembers={openMembersModal}
  onOpenPoll={openPollModal}
  onLeaveChat={handleLeaveChat}
/>
```

<!-- Bestehende CSS-Klassen in variables.css: -->
```css
.app-list-item--chat { border-left-color: #06b6d4; }
.app-icon-circle--chat { background-color: #06b6d4; }
.app-corner-badge--chat { background-color: #06b6d4; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen fuer Raumtyp-Farben + ChatOverview Farblogik</name>
  <files>frontend/src/theme/variables.css, frontend/src/components/chat/ChatOverview.tsx</files>
  <read_first>frontend/src/theme/variables.css, frontend/src/components/chat/ChatOverview.tsx</read_first>
  <action>
1. In variables.css NEUE CSS-Klassen hinzufuegen (per Decision: Farben als CSS-Klassen statt inline). Nach dem bestehenden `.app-list-item--chat` Block:

```css
/* Chat-Raumtyp-Farben */
.app-list-item--team, ion-item.app-list-item--team { border-left-color: #e11d48; }
.app-list-item--jahrgang, ion-item.app-list-item--jahrgang { border-left-color: #06b6d4; }
.app-list-item--konfi, ion-item.app-list-item--konfi { border-left-color: #5b21b6; }
.app-list-item--group, ion-item.app-list-item--group { border-left-color: #f97316; }

.app-list-item--team.app-list-item--selected {
  border-color: #e11d48;
  background: rgba(225, 29, 72, 0.08);
}

.app-icon-circle--team { background-color: #e11d48; }
.app-icon-circle--jahrgang { background-color: #06b6d4; }
.app-icon-circle--konfi { background-color: #5b21b6; }
.app-icon-circle--group { background-color: #f97316; }

.app-corner-badge--team { background-color: #e11d48; }
.app-corner-badge--jahrgang { background-color: #06b6d4; }
.app-corner-badge--konfi { background-color: #5b21b6; }
.app-corner-badge--group { background-color: #f97316; }
```

2. In ChatOverview.tsx die INLINE Farblogik (Zeile 382-385) durch eine Helper-Funktion ersetzen. Oben im Component oder vor dem return:

```typescript
const getRoomColor = (type: string): string => {
  switch (type) {
    case 'admin': return '#e11d48';   // Rosa — Team/Admin-Chat
    case 'jahrgang': return '#06b6d4'; // Tuerkis
    case 'group': return '#f97316';    // Orange
    default: return '#5b21b6';         // Lila — Konfi/Direct
  }
};

const getRoomColorClass = (type: string): string => {
  switch (type) {
    case 'admin': return 'team';
    case 'jahrgang': return 'jahrgang';
    case 'group': return 'group';
    default: return 'konfi';
  }
};
```

3. Ersetze `const color = room.type === 'admin' ? ...` durch `const color = getRoomColor(room.type);`

4. Ersetze die inline `style={{ backgroundColor: color }}` am app-corner-badge durch die CSS-Klasse: `className={`app-corner-badge app-corner-badge--${getRoomColorClass(room.type)}`}`

5. Ersetze die inline `style={{ borderLeftColor: color }}` am app-list-item durch die CSS-Klasse: `className={`app-list-item app-list-item--${getRoomColorClass(room.type)}`}`

6. Ersetze die inline `style={{ backgroundColor: color }}` am app-icon-circle durch die CSS-Klasse: Finde den Icon-Circle und aendere zu `className={`app-icon-circle app-icon-circle--lg app-icon-circle--${getRoomColorClass(room.type)}`}`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "app-list-item--team" frontend/src/theme/variables.css && grep -c "getRoomColor\|getRoomColorClass" frontend/src/components/chat/ChatOverview.tsx && grep -c "#e11d48" frontend/src/theme/variables.css</automated>
  </verify>
  <acceptance_criteria>
    - variables.css enthaelt app-list-item--team mit #e11d48
    - variables.css enthaelt app-icon-circle--team mit #e11d48
    - variables.css enthaelt app-corner-badge--team mit #e11d48
    - ChatOverview.tsx nutzt getRoomColor() und getRoomColorClass() statt inline-Farben
    - admin-Raumtyp mapped zu #e11d48 (Rosa), jahrgang zu #06b6d4, group zu #f97316, default/direct zu #5b21b6
  </acceptance_criteria>
  <done>Alle Chat-Raumtypen haben korrekte CSS-Klassen und ChatOverview zeigt Farben per CSS-Klasse statt inline</done>
</task>

<task type="auto">
  <name>Task 2: ChatRoom Header-Farbe nach Raumtyp</name>
  <files>frontend/src/components/chat/ChatRoomSections.tsx, frontend/src/components/chat/ChatRoom.tsx</files>
  <read_first>frontend/src/components/chat/ChatRoomSections.tsx, frontend/src/components/chat/ChatRoom.tsx</read_first>
  <action>
1. In ChatRoomSections.tsx das ChatHeaderProps Interface erweitern um `roomType: string`.

2. In der ChatHeader-Komponente den roomType prop entgegennehmen und eine Farb-Logik einbauen. Dieselbe Mapping-Logik wie in Task 1:
```typescript
const headerColor = (() => {
  switch (roomType) {
    case 'admin': return '#e11d48';
    case 'jahrgang': return '#06b6d4';
    case 'group': return '#f97316';
    default: return '#5b21b6';
  }
})();
```

3. Den `headerColor` im IonToolbar als `--background` oder als dezenten Akzent verwenden. Konkreter Ansatz: Einen farbigen Streifen (3px) am unteren Rand der Toolbar als `borderBottom` setzen:
```typescript
style={{ borderBottom: `3px solid ${headerColor}` }}
```

4. In ChatRoom.tsx den roomType an ChatHeader uebergeben. Zeile ~980:
```typescript
<ChatHeader
  roomName={getDisplayRoomName()}
  roomType={room?.type ?? 'group'}
  isAdmin={user?.type === 'admin'}
  ...
/>
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "roomType" frontend/src/components/chat/ChatRoomSections.tsx && grep -c "roomType.*room.*type" frontend/src/components/chat/ChatRoom.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ChatHeaderProps enthaelt roomType: string
    - ChatHeader zeigt farbigen Akzent basierend auf roomType
    - ChatRoom.tsx uebergibt room.type als roomType prop
    - admin zeigt #e11d48, jahrgang #06b6d4, group #f97316, default #5b21b6
  </acceptance_criteria>
  <done>ChatRoom Header zeigt Raumtyp-spezifische Farbe konsistent mit ChatOverview</done>
</task>

</tasks>

<verification>
- Alle 4 Raumtypen (admin, jahrgang, group, direct) zeigen unterschiedliche Farben in ChatOverview UND ChatRoom
- Admin/Team-Chats zeigen Rosa (#e11d48) — NICHT mehr Tuerkis
- Keine inline style={{ backgroundColor/borderLeftColor }} mehr in ChatOverview fuer Raumtyp-Farben
- Frontend baut fehlerfrei: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- TCH-03: Teamer/Team Rosa, Konfis Lila, Jahrgang Tuerkis — in ChatOverview und ChatRoom
- KCH-01: Team-Chat in korrekter Rosa-Farbe
- ACH-01: Chat-Farblogik konsistent ueber alle drei Rollen
</success_criteria>

<output>
After completion, create `.planning/phases/95-chat-farbschema-korrekturen/95-01-SUMMARY.md`
</output>
