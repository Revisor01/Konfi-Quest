---
phase: 96-konfi-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/pages/KonfiEventsPage.tsx
  - frontend/src/components/konfi/views/EventsView.tsx
  - frontend/src/components/konfi/views/EventDetailView.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  - frontend/src/components/konfi/modals/ActivityRequestModal.tsx
autonomous: true
requirements: [KEV-01, KEV-02, KEV-03, KAK-01]
must_haves:
  truths:
    - "Events-Suchleiste hat 'Suche & Filter' Zwischenueberschrift mit searchOutline Icon"
    - "Teilnehmer:innen-Liste hat reduzierten Abstand (standard app-list-item Pattern)"
    - "Antrag-Modal zeigt keine Kategorien-Auswahl mehr"
    - "Event-Detail Beschreibungstext nutzt app-description-text CSS-Klasse"
    - "Material-Detail Beschreibungstext nutzt app-description-text CSS-Klasse"
  artifacts:
    - path: "frontend/src/components/konfi/pages/KonfiEventsPage.tsx"
      provides: "Events page with Suche & Filter header wrapping searchbar"
      contains: "Suche & Filter"
    - path: "frontend/src/components/konfi/modals/ActivityRequestModal.tsx"
      provides: "Request modal without category selector"
    - path: "frontend/src/components/konfi/views/EventDetailView.tsx"
      provides: "Event detail with app-description-text class on description"
      contains: "app-description-text"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx"
      provides: "Material detail with app-description-text class on description"
      contains: "app-description-text"
  key_links:
    - from: "KonfiEventsPage.tsx"
      to: "IonSearchbar"
      via: "searchText state"
      pattern: "searchText"
    - from: "EventDetailView.tsx"
      to: "variables.css"
      via: "app-description-text className"
      pattern: "app-description-text"
---

<objective>
Events-Suche mit "Suche & Filter" Header, Teilnehmer-Abstand reduzieren, Kategorien-Auswahl aus dem Antrag-Modal entfernen und globale app-description-text Klasse auf Beschreibungstexte anwenden.

Purpose: Konfi Events-Seite bekommt konsistentes Suchleisten-Pattern. Antrag-Modal wird vereinfacht. Beschreibungstexte werden global einheitlich gestylt (KEV-01).
Output: Verbesserte Suchleiste, reduzierter Listen-Abstand, kein Kategorie-Selector im Antrag, einheitliche Beschreibungstexte.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/konfi/pages/KonfiEventsPage.tsx
@frontend/src/components/konfi/views/EventsView.tsx
@frontend/src/components/konfi/views/EventDetailView.tsx
@frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
@frontend/src/components/konfi/modals/ActivityRequestModal.tsx
@frontend/src/theme/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Events-Suchleiste "Suche & Filter" + Teilnehmer-Abstand (KEV-02, KEV-03)</name>
  <files>frontend/src/components/konfi/pages/KonfiEventsPage.tsx, frontend/src/components/konfi/views/EventsView.tsx</files>
  <read_first>frontend/src/components/konfi/pages/KonfiEventsPage.tsx, frontend/src/components/konfi/views/EventsView.tsx</read_first>
  <action>
**1. KonfiEventsPage.tsx - Suchleiste in IonList mit Header wrappen:**

VORHER (Zeile ~151-160):
```tsx
{!loading && (
  <IonSearchbar
    className="ios26-searchbar-classic"
    value={searchText}
    onIonInput={(e) => setSearchText(e.detail.value || '')}
    placeholder="Events durchsuchen"
    debounce={300}
    style={{ padding: '0 16px' }}
  />
)}
```

NACHHER:
```tsx
{!loading && (
  <IonList inset={true} style={{ margin: '16px' }}>
    <IonListHeader>
      <div className="app-section-icon app-section-icon--events">
        <IonIcon icon={searchOutline} />
      </div>
      <IonLabel>Suche & Filter</IonLabel>
    </IonListHeader>
    <IonCard className="app-card">
      <IonCardContent style={{ padding: '8px 12px' }}>
        <IonSearchbar
          className="ios26-searchbar-classic"
          value={searchText}
          onIonInput={(e) => setSearchText(e.detail.value || '')}
          placeholder="Events durchsuchen"
          debounce={300}
          style={{ padding: '0' }}
        />
      </IonCardContent>
    </IonCard>
  </IonList>
)}
```

Imports ergaenzen: `IonList, IonListHeader, IonLabel, IonCard, IonCardContent` aus `@ionic/react` und `searchOutline` aus `ionicons/icons`.

**2. EventsView.tsx - Teilnehmer-Abstand reduzieren:**

Die IonItemSliding-Elemente in der Events-Liste haben aktuell `marginBottom: '8px'` (Zeile 257). Das ist der Standard-Abstand. Der Abstand zwischen Elementen auf `4px` reduzieren:

```tsx
style={{ marginBottom: index < filteredEvents.length - 1 ? '4px' : '0' }}
```

Dies betrifft die Teilnehmer:innen-Liste (Event-Eintraege), die damit enger zusammenruecken.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "Suche & Filter" frontend/src/components/konfi/pages/KonfiEventsPage.tsx && grep "marginBottom.*4px" frontend/src/components/konfi/views/EventsView.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - "Suche & Filter" in KonfiEventsPage.tsx als IonListHeader vorhanden
    - searchOutline aus ionicons/icons importiert
    - IonSearchbar in IonList > IonCard gewrappt
    - marginBottom auf 4px reduziert in EventsView.tsx
  </acceptance_criteria>
  <done>Events-Suchleiste hat "Suche & Filter" Header mit IonList-Pattern. Teilnehmer:innen-Abstand reduziert.</done>
</task>

<task type="auto">
  <name>Task 2: Kategorien-Auswahl aus Antrag-Modal entfernen (KAK-01)</name>
  <files>frontend/src/components/konfi/modals/ActivityRequestModal.tsx</files>
  <read_first>frontend/src/components/konfi/modals/ActivityRequestModal.tsx</read_first>
  <action>
In ActivityRequestModal.tsx den kompletten Kategorie-Filter-Block entfernen. Per D-05 (KAK-01): Kategorien-Auswahl komplett entfernen (nicht nur ausblenden).

**1. Kategorie-State entfernen:**
- Zeile ~72: `const [activeCategory, setActiveCategory] = useState<string>('all');` entfernen

**2. Kategorie-Berechnung entfernen:**
Zeile ~268-274 entfernen:
```tsx
const categories = [...new Set(
  activities
    .map(a => a.category_names)
    .filter(Boolean)
    .flatMap(names => names!.split(',').map(n => n.trim()))
)].sort();
```

**3. Gefilterte Aktivitaeten vereinfachen:**
VORHER (Zeile ~276-280):
```tsx
const filteredActivities = activeCategory === 'all'
  ? activities
  : activities.filter(a =>
      a.category_names?.split(',').map(n => n.trim()).includes(activeCategory)
    );
```

NACHHER:
```tsx
const filteredActivities = activities;
```

**4. IonSegment JSX-Block entfernen:**
Zeile ~306-323 (der komplette `{categories.length > 1 && (...)}` Block) entfernen.

**5. Unbenutzte Imports entfernen:**
`IonSegment, IonSegmentButton` aus @ionic/react entfernen (pruefen ob noch anderweitig genutzt).
`pricetag` aus ionicons/icons kann bleiben (wird noch in der Aktivitaeten-Liste fuer category_names genutzt).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && ! grep -q "activeCategory" frontend/src/components/konfi/modals/ActivityRequestModal.tsx && echo "OK_NO_CATEGORY"</automated>
  </verify>
  <acceptance_criteria>
    - Kein "activeCategory" State mehr in ActivityRequestModal.tsx
    - Kein IonSegment fuer Kategorien mehr im JSX
    - filteredActivities ist direkt activities (kein Kategoriefilter)
    - categories Variable entfernt
  </acceptance_criteria>
  <done>Kategorien-Auswahl ist komplett aus dem Antrag-Modal entfernt. Alle Aktivitaeten werden direkt angezeigt.</done>
</task>

<task type="auto">
  <name>Task 3: app-description-text Klasse auf Beschreibungstexte anwenden (KEV-01)</name>
  <files>frontend/src/components/konfi/views/EventDetailView.tsx, frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx</files>
  <read_first>frontend/src/components/konfi/views/EventDetailView.tsx, frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx, frontend/src/theme/variables.css</read_first>
  <action>
Die CSS-Klasse `app-description-text` existiert bereits in variables.css (Zeile ~1021). Diese Klasse muss jetzt auf alle Beschreibungstexte angewendet werden, damit die Schriftgroesse global einheitlich ist und zentral angepasst werden kann. Per KEV-01: Beschreibungstext groesser und konsistent.

**1. EventDetailView.tsx - Beschreibungstext mit app-description-text versehen:**

Finde den Beschreibungstext-Bereich (typischerweise `event.description` Darstellung). Ersetze die inline font-size/color Styles durch die CSS-Klasse:

VORHER (suche nach dem description-Render, z.B.):
```tsx
<div style={{ fontSize: '0.85rem', color: '#666', ... }}>
  {event.description}
</div>
```

NACHHER:
```tsx
<div className="app-description-text">
  {event.description}
</div>
```

Wenn der Beschreibungstext in einem `dangerouslySetInnerHTML` oder anderem Pattern gerendert wird, die Klasse auf den Container anwenden. Bestehende Margin/Padding Styles koennen beibehalten werden, aber font-size, color und line-height sollen ueber die CSS-Klasse kommen.

**2. TeamerMaterialDetailPage.tsx - Beschreibungstext mit app-description-text versehen:**

Gleiche Vorgehensweise: Den Material-Beschreibungstext (`material.description` oder aehnlich) finden und `className="app-description-text"` auf den Container anwenden.

VORHER:
```tsx
<div style={{ fontSize: '0.85rem', ... }}>
  {material.description}
</div>
```

NACHHER:
```tsx
<div className="app-description-text">
  {material.description}
</div>
```

WICHTIG: Nur die font-size/color/line-height inline Styles entfernen, die jetzt ueber die CSS-Klasse abgedeckt werden. Margin/Padding und andere Layout-Styles beibehalten.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "app-description-text" frontend/src/components/konfi/views/EventDetailView.tsx && grep -c "app-description-text" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EventDetailView.tsx hat mindestens 1x "app-description-text" className
    - TeamerMaterialDetailPage.tsx hat mindestens 1x "app-description-text" className
    - Inline font-size auf den Beschreibungstexten entfernt (wird jetzt ueber CSS-Klasse gesteuert)
  </acceptance_criteria>
  <done>Beschreibungstexte in Event-Details und Material-Details nutzen die globale app-description-text CSS-Klasse. Schriftgroesse kann jetzt zentral in variables.css angepasst werden.</done>
</task>

</tasks>

<verification>
- Events-Seite zeigt "Suche & Filter" IonListHeader ueber der Suchleiste
- Suchleiste ist in IonCard gewrappt (konsistentes Pattern)
- Event-Eintraege haben engeren Abstand (4px statt 8px)
- Antrag-Modal zeigt keine Kategorien-Tabs mehr
- Alle Aktivitaeten werden direkt in der Liste angezeigt
- EventDetailView.tsx Beschreibungstext hat app-description-text Klasse
- TeamerMaterialDetailPage.tsx Beschreibungstext hat app-description-text Klasse
</verification>

<success_criteria>
Events-Suchleiste hat "Suche & Filter" Header, Teilnehmer-Abstand ist reduziert, Antrag-Modal hat keinen Kategoriefilter mehr. Beschreibungstexte nutzen die globale app-description-text CSS-Klasse.
</success_criteria>

<output>
After completion, create `.planning/phases/96-konfi-ui/96-03-SUMMARY.md`
</output>
