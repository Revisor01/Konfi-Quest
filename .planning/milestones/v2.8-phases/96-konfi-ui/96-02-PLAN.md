---
phase: 96-konfi-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/components/konfi/pages/KonfiBadgesPage.tsx
  - frontend/src/theme/variables.css
autonomous: true
requirements: [KBD-01, KBD-02, KBD-03]
must_haves:
  truths:
    - "Badge-Kacheln sind exakt gleich gross, 3 pro Zeile mit CSS Grid repeat(3, 1fr)"
    - "Badge-Titel werden nach 1 Zeile mit Ellipsis abgeschnitten (nicht 2)"
    - "Popover-Breite passt sich dynamisch an den Titel an"
    - "'Suche & Filter' Zwischenueberschrift steht ueber den Segment-Buttons"
  artifacts:
    - path: "frontend/src/components/konfi/views/BadgesView.tsx"
      provides: "Badge grid with 1-line ellipsis titles and dynamic popover"
      contains: "WebkitLineClamp: 1"
    - path: "frontend/src/components/konfi/pages/KonfiBadgesPage.tsx"
      provides: "Badges page with Suche & Filter header"
      contains: "Suche & Filter"
    - path: "frontend/src/theme/variables.css"
      provides: "badge-popover-auto-width CSS class and app-stats-row flex-wrap"
      contains: "badge-popover-auto-width"
  key_links:
    - from: "BadgesView.tsx"
      to: "useIonPopover"
      via: "badge-detail-popover cssClass"
      pattern: "badge-detail-popover"
---

<objective>
Badge-Grid gleichmaessig machen, Titel auf 1 Zeile begrenzen, Popover-Breite dynamisch anpassen und "Suche & Filter" Ueberschrift einfuegen.

Purpose: Badge-Kacheln sollen optisch einheitlich sein, Titel nicht umbrechen und Popovers sich dem Inhalt anpassen.
Output: Gleichmaessiges Badge-Grid, 1-Zeilen-Titel, dynamisches Popover, "Suche & Filter" Header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/konfi/views/BadgesView.tsx
@frontend/src/components/konfi/pages/KonfiBadgesPage.tsx
@frontend/src/theme/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge-Kacheln gleich gross + 1-Zeilen-Titel + Popover dynamisch (KBD-02, KBD-03)</name>
  <files>frontend/src/components/konfi/views/BadgesView.tsx</files>
  <read_first>frontend/src/components/konfi/views/BadgesView.tsx, frontend/src/theme/variables.css</read_first>
  <action>
In BadgesView.tsx drei Aenderungen:

1. **Badge-Name auf 1 Zeile begrenzen** (Zeile ~558-570):
   Aendere `WebkitLineClamp: 2` auf `WebkitLineClamp: 1` im Badge-Name span:
   ```tsx
   <span style={{
     fontSize: '0.75rem',
     fontWeight: '600',
     color: isEarned ? '#333' : '#999',
     textAlign: 'center',
     lineHeight: '1.2',
     maxWidth: '100%',
     overflow: 'hidden',
     textOverflow: 'ellipsis',
     display: '-webkit-box',
     WebkitLineClamp: 1,
     WebkitBoxOrient: 'vertical'
   }}>
   ```

2. **Popover-Breite dynamisch** (Zeile ~348-354):
   Beim presentBadgePopover die cssClass um auto-width ergaenzen:
   ```tsx
   presentBadgePopover({
     event: e.nativeEvent,
     side: 'top',
     alignment: 'center',
     cssClass: 'badge-detail-popover badge-popover-auto-width'
   });
   ```

   Und in variables.css (oder direkt im presentBadgePopover per `--width` und `--max-width`):
   Da Ionic Popovers CSS Custom Properties nutzen, die cssClass `badge-popover-auto-width` hinzufuegen.
   Stattdessen nutzen wir die direkt im Popover-Aufruf einstellbaren CSS-Properties.
   Einfacher: In der BadgePopoverContent-Komponente den Container mit `whiteSpace: 'nowrap'` auf dem h3-Element belassen (ist bereits vorhanden auf Zeile 191).

   Fuer die Popover-Breite selbst: In variables.css folgende Regel ergaenzen:
   ```css
   .badge-popover-auto-width {
     --width: auto;
     --max-width: 80vw;
     --min-width: 200px;
   }
   ```

3. **Grid ist bereits korrekt** mit `gridTemplateColumns: 'repeat(3, 1fr)'` (Zeile ~462). Sicherstellen dass jede Badge-Kachel eine feste minHeight hat, damit alle gleich gross sind:
   ```tsx
   <div
     key={badge.id}
     onClick={(e) => handleBadgeClick(badge, e)}
     style={{
       display: 'flex',
       flexDirection: 'column',
       alignItems: 'center',
       padding: '12px 8px',
       borderRadius: '16px',
       background: isEarned ? `${badgeColor}10` : '#f5f5f5',
       border: isEarned ? `2px solid ${badgeColor}40` : '2px solid transparent',
       cursor: 'pointer',
       transition: 'transform 0.2s',
       position: 'relative',
       minHeight: '110px',
       justifyContent: 'flex-start'
     }}
   >
   ```

Per D-03 (KBD-02): repeat(3, 1fr) Grid, 1-Zeilen-Ellipsis.
Per D-04 (KBD-03): --width: auto; --max-width: 80vw auf Popover.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "WebkitLineClamp: 1" frontend/src/components/konfi/views/BadgesView.tsx && grep -c "badge-popover-auto-width" frontend/src/components/konfi/views/BadgesView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WebkitLineClamp ist 1 (nicht 2) im Badge-Name span
    - badge-popover-auto-width cssClass wird beim presentBadgePopover uebergeben
    - minHeight: '110px' auf Badge-Kacheln fuer gleiche Groesse
    - variables.css hat .badge-popover-auto-width mit --width: auto
  </acceptance_criteria>
  <done>Badge-Kacheln sind gleich gross, Titel 1-zeilig mit Ellipsis, Popover-Breite dynamisch.</done>
</task>

<task type="auto">
  <name>Task 2: "Suche & Filter" Zwischenueberschrift + CSS fuer Popover und Stats-Row (KBD-01)</name>
  <files>frontend/src/components/konfi/views/BadgesView.tsx, frontend/src/theme/variables.css</files>
  <read_first>frontend/src/components/konfi/views/BadgesView.tsx, frontend/src/theme/variables.css</read_first>
  <action>
**1. BadgesView.tsx - IonSegment Filter-Buttons in IonList mit Header wrappen:**

VORHER (Zeile ~371-377):
```tsx
{/* Filter */}
<div style={{ margin: '16px' }}>
  <IonSegment value={selectedFilter} onIonChange={...}>
    ...
  </IonSegment>
</div>
```

NACHHER:
```tsx
{/* Suche & Filter */}
<IonList inset={true} style={{ margin: '16px' }}>
  <IonListHeader>
    <div className="app-section-icon app-section-icon--warning">
      <IonIcon icon={searchOutline} />
    </div>
    <IonLabel>Suche & Filter</IonLabel>
  </IonListHeader>
  <IonCard className="app-card">
    <IonCardContent style={{ padding: '8px 16px' }}>
      <IonSegment value={selectedFilter} onIonChange={(e) => onFilterChange(e.detail.value as string)}>
        <IonSegmentButton value="alle"><IonLabel>Alle</IonLabel></IonSegmentButton>
        <IonSegmentButton value="nicht_erhalten"><IonLabel>Offen</IonLabel></IonSegmentButton>
        <IonSegmentButton value="in_arbeit"><IonLabel>In Arbeit</IonLabel></IonSegmentButton>
      </IonSegment>
    </IonCardContent>
  </IonCard>
</IonList>
```

Importiere `searchOutline` aus `ionicons/icons` und `IonCard, IonCardContent` aus `@ionic/react` (IonCard und IonCardContent sind bereits importiert).
Importiere `searchOutline` hinzu: es muss zu den bestehenden Imports von ionicons/icons hinzugefuegt werden.

**2. variables.css - Badge Popover auto-width und Stats-Row flex-wrap hinzufuegen:**

Am Ende des Utility-Bereichs in variables.css diese beiden CSS-Bloecke ergaenzen:

```css
/* Badge Popover auto-width */
.badge-popover-auto-width {
  --width: auto;
  --max-width: 80vw;
  --min-width: 200px;
}
```

Zusaetzlich die bestehende `.app-stats-row` Regel um `flex-wrap: wrap` ergaenzen, damit 6 Stats in 2 Reihen dargestellt werden koennen (wird von Plan 96-04 benoetigt):

```css
.app-stats-row {
  display: flex;
  justify-content: center;
  gap: 8px;
  position: relative;
  z-index: 1;
  flex-wrap: wrap;
}
```

Die `flex-wrap: wrap` Eigenschaft zur bestehenden `.app-stats-row` Regel hinzufuegen (nicht duplizieren). Die `max-width: 100px` auf den Items sorgt automatisch dafuer, dass bei 6 Items ein Umbruch nach 3 entsteht.

Per D-01 (KBD-01): "Suche & Filter" als SectionHeader mit searchOutline Icon.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "Suche & Filter" frontend/src/components/konfi/views/BadgesView.tsx && grep -c "badge-popover-auto-width" frontend/src/theme/variables.css && grep "flex-wrap" frontend/src/theme/variables.css | grep -c "wrap"</automated>
  </verify>
  <acceptance_criteria>
    - "Suche & Filter" Text in BadgesView.tsx als IonListHeader vorhanden
    - searchOutline importiert und als Icon im Header verwendet
    - Segment-Buttons in IonCard gewrappt (IonList inset Pattern)
    - .badge-popover-auto-width in variables.css definiert
    - .app-stats-row hat flex-wrap: wrap in variables.css
  </acceptance_criteria>
  <done>"Suche & Filter" Zwischenueberschrift mit searchOutline Icon ueber den Filter-Segment-Buttons. Popover CSS-Klasse und Stats-Row flex-wrap in variables.css definiert.</done>
</task>

</tasks>

<verification>
- Badge-Grid zeigt 3 gleichgrosse Kacheln pro Zeile
- Badge-Titel brechen nicht auf 2 Zeilen um, sondern werden mit ... abgeschnitten
- Popover oeffnet sich mit dynamischer Breite (nicht fixed 260px)
- "Suche & Filter" Ueberschrift mit searchOutline Icon steht ueber den Segment-Buttons
- .app-stats-row hat flex-wrap: wrap fuer 6-Stats-Layout
</verification>

<success_criteria>
Badge-Kacheln sind gleich gross (3/Zeile), Titel 1-zeilig mit Ellipsis, Popover-Breite dynamisch, "Suche & Filter" Header vorhanden. CSS fuer Stats-Row und Popover in variables.css.
</success_criteria>

<output>
After completion, create `.planning/phases/96-konfi-ui/96-02-SUMMARY.md`
</output>
