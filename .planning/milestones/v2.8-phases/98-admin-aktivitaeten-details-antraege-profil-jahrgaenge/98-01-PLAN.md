---
phase: 98-admin-aktivitaeten-details-antraege-profil-jahrgaenge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/ActivitiesView.tsx
  - frontend/src/components/admin/modals/ActivityManagementModal.tsx
autonomous: true
requirements: [AAK-01, AAK-02, AAK-03, AAK-04, AAK-05]

must_haves:
  truths:
    - "Aktivitaeten-Liste zeigt kein 'Invalid Date' — Datumsanzeige ist korrekt oder leer"
    - "Datumspicker im ActivityManagementModal funktioniert korrekt mit IonDatetimeButton"
    - "Teamer:innen-Aktivitaeten im Modal zeigen keinen Punkte-Bereich"
    - "Teamer:innen-Aktivitaeten koennen ohne Punkte-Pflicht erstellt werden"
    - "Kategorien-Symbole zeigen die korrekte SectionHeader-Kategorie-Farbe"
  artifacts:
    - path: "frontend/src/components/admin/ActivitiesView.tsx"
      provides: "Invalid-Date-Fix und Kategorien-Farbkorrektur in der Aktivitaeten-Liste"
    - path: "frontend/src/components/admin/modals/ActivityManagementModal.tsx"
      provides: "Datumspicker-Fix und Teamer-Punkte-Ausblendung"
  key_links:
    - from: "ActivitiesView.tsx"
      to: "variables.css"
      via: "app-section-icon--categories CSS-Klasse fuer Kategorie-Farbe"
      pattern: "app-section-icon--categories"
---

<objective>
Aktivitaeten-Liste und -Modal fixen: Invalid Date beheben, Datumspicker korrigieren, Teamer-Aktivitaeten ohne Punkte, Kategorie-Farben korrekt.

Purpose: Admins koennen Aktivitaeten fehlerfrei verwalten — keine falschen Datumsanzeigen, korrekte Farbgebung, Teamer-Logik stimmt.
Output: Bereinigte ActivitiesView.tsx und ActivityManagementModal.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-admin-aktivitaeten-details-antraege-profil-jahrgaenge/98-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivitiesView — Invalid Date Fix + Kategorien-Farbe</name>
  <files>frontend/src/components/admin/ActivitiesView.tsx</files>
  <read_first>
    frontend/src/components/admin/ActivitiesView.tsx
    frontend/src/theme/variables.css
  </read_first>
  <action>
Drei Aenderungen in ActivitiesView.tsx:

1. **AAK-01 — Invalid Date Fix**: Die ActivitiesView zeigt aktuell kein Datum an (kein `created_at` Rendering im Template). Der "Invalid Date"-Bug kommt wahrscheinlich aus einem anderen Rendering-Kontext. Pruefe ob `created_at` irgendwo im JSX als `new Date(activity.created_at)` gerendert wird. Falls ja, ersetze durch:
   ```typescript
   const formatDate = (dateString: string) => {
     if (!dateString) return '';
     const d = new Date(dateString);
     if (isNaN(d.getTime())) return '';
     return d.toLocaleDateString('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric' });
   };
   ```
   Falls kein Datum im Template gerendert wird, liegt der Bug moeglicherweise in der ActivityRequestsView.tsx — dort die gleiche Absicherung in `formatDate` (Zeile 83-89) einbauen: vor `toLocaleDateString` pruefen ob `dateString` truthy ist und `new Date(dateString).getTime()` nicht NaN.

2. **AAK-05 — Kategorien-Symbol Farbe**: In der Aktivitaeten-Liste (Zeile 291-296) wird das Kategorie-Icon `pricetag` mit fixem `color: '#ff9500'` gerendert. Aendern zu `color: '#0ea5e9'` — das ist die Farbe von `.app-section-icon--categories` aus variables.css (Zeile 379).

3. Sicherstellen dass die Kategorien-Anzeige in der Meta-Zeile das korrekte Icon mit der Kategorie-Farbe `#0ea5e9` nutzt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "ff9500" frontend/src/components/admin/ActivitiesView.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - ActivitiesView.tsx enthaelt KEINEN Verweis auf `#ff9500` fuer das pricetag-Icon (Ergebnis von grep = 0)
    - ActivitiesView.tsx enthaelt `#0ea5e9` als Farbe fuer das Kategorie-Icon
    - Falls formatDate existiert: enthaelt `isNaN` oder `!dateString` Guard
    - ActivityRequestsView.tsx formatDate enthaelt `if (!dateString) return ''` Guard
  </acceptance_criteria>
  <done>Kategorie-Farbe ist #0ea5e9, Invalid-Date-Bug ist durch Null-Guard abgesichert</done>
</task>

<task type="auto">
  <name>Task 2: ActivityManagementModal — Datumspicker + Teamer-Punkte ausblenden</name>
  <files>frontend/src/components/admin/modals/ActivityManagementModal.tsx</files>
  <read_first>
    frontend/src/components/admin/modals/ActivityManagementModal.tsx
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  </read_first>
  <action>
Zwei Aenderungen in ActivityManagementModal.tsx:

1. **AAK-02 — Datumspicker korrigieren**: Das Modal hat aktuell KEINEN Datumspicker (kein IonDatetime/IonDatetimeButton). Laut CONTEXT.md soll ein IonDatetimeButton + IonModal Pattern wie in AdminJahrgaengeePage (Zeile 230 + 315-328) eingebaut werden. Ergaenze:
   - Neuen State: `const [activityDate, setActivityDate] = useState('');`
   - Import von `IonDatetimeButton`, `IonDatetime`, `IonModal` (IonModal ist schon importiert? pruefen)
   - In der "Grunddaten"-Sektion (nach dem Namen-Feld, vor Punkte): ein neues IonItem mit Label "Datum" und `<IonDatetimeButton datetime="activity-date" />` einfuegen
   - Am Ende des IonContent (vor `</IonContent>`): `<IonModal keepContentsMounted={true}><IonDatetime id="activity-date" presentation="date" locale="de-DE" value={activityDate || undefined} onIonChange={(e) => { const value = e.detail.value; if (typeof value === 'string') setActivityDate(value.split('T')[0]); }} disabled={loading} /></IonModal>`
   - HINWEIS: Das Datum wird aktuell NICHT im Payload gesendet (activities-Tabelle hat kein separates Datum-Feld fuer die Aktivitaets-Definition). Der Datumspicker ist rein fuer die UI-Konsistenz — ggf. als optionales Feld behandeln. Wenn die API kein `date`-Feld erwartet, den Datumspicker NICHT in den Payload aufnehmen.

2. **AAK-03 + AAK-04 — Teamer:innen-Punkte ausblenden**: Im Modal wird der Punkte-Bereich bereits bei `formData.target_role !== 'teamer'` bedingt angezeigt (Zeile 363-379). Ebenso der Typ-Bereich (Zeile 382-422). Das ist korrekt. Zusaetzlich pruefen:
   - Beim Erstellen mit target_role='teamer' werden `points: 0` und `type: null` gesendet (Zeile 204-205 im handleSubmit). Das ist korrekt.
   - Sicherstellen, dass im "Grunddaten"-Header bei Teamer:innen ein Hinweis erscheint: Nach dem Namen-Feld und vor dem Schluss der Sektion, wenn `formData.target_role === 'teamer'`, eine Info-Zeile einfuegen:
     ```tsx
     {formData.target_role === 'teamer' && (
       <div style={{ padding: '8px 16px', color: '#666', fontSize: '0.85rem' }}>
         Teamer:innen-Aktivitaeten haben keine Punkte und keinen Typ.
       </div>
     )}
     ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "IonDatetimeButton\|activity-date\|Teamer:innen-Aktivitäten haben keine Punkte" frontend/src/components/admin/modals/ActivityManagementModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ActivityManagementModal.tsx enthaelt `IonDatetimeButton` Import
    - ActivityManagementModal.tsx enthaelt `id="activity-date"` fuer den IonDatetime
    - ActivityManagementModal.tsx enthaelt Hinweistext "Teamer:innen-Aktivitäten haben keine Punkte"
    - grep-Ergebnis >= 3 (mindestens 3 Treffer fuer die 3 Muster)
  </acceptance_criteria>
  <done>Datumspicker ist als IonDatetimeButton eingebaut, Teamer-Hinweis erscheint, Punkte/Typ sind bei Teamer:innen ausgeblendet</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler
- ActivitiesView zeigt Kategorie-Icons in Farbe #0ea5e9
- ActivityManagementModal hat funktionierenden Datumspicker
- Teamer-Aktivitaeten zeigen keinen Punkte-Bereich
</verification>

<success_criteria>
- Kein "Invalid Date" in der Aktivitaeten-Ansicht (Null-Guards vorhanden)
- Datumspicker im Modal funktioniert (IonDatetimeButton + IonDatetime Pattern)
- Teamer-Aktivitaeten: Punkte ausgeblendet, Hinweistext sichtbar
- Kategorie-Symbole in Farbe #0ea5e9 (categories-SectionHeader-Farbe)
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/98-admin-aktivitaeten-details-antraege-profil-jahrgaenge/98-01-SUMMARY.md`
</output>
