---
phase: 98-admin-aktivitaeten-details-antraege-profil-jahrgaenge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/views/KonfiDetailSections.tsx
  - frontend/src/components/admin/modals/ActivityRequestModal.tsx
autonomous: true
requirements: [ATD-01, AAN-01, AAN-02]

must_haves:
  truths:
    - "Teamer-Detail-Header zeigt 'Teamer:in' als Ueberschrift mit rosa Gradient"
    - "Teamer-Detail zeigt nur 3 Stats: Zertifikate, Events, Badges (keine Aktivitaeten)"
    - "Antrags-Modal hat Icons neben jedem Antragsdaten-Feld"
    - "Entscheidungs-Buttons sind outline-style mit gruenem und rotem Border"
  artifacts:
    - path: "frontend/src/components/admin/views/KonfiDetailSections.tsx"
      provides: "Teamer-Header rosa Gradient + 3 Stats"
    - path: "frontend/src/components/admin/modals/ActivityRequestModal.tsx"
      provides: "Icons in Antragsdaten + farbige Entscheidungs-Buttons"
  key_links:
    - from: "KonfiDetailSections.tsx"
      to: "KonfiDetailView.tsx"
      via: "isTeamer prop steuert Header-Gradient und Stats"
      pattern: "isTeamer"
---

<objective>
Teamer-Detail-Header korrigieren (rosa Gradient, 3 Stats) und Antrags-Modal verbessern (Icons + farbige Buttons).

Purpose: Admin-Detailansicht fuer Teamer:innen ist visuell korrekt; Antraege sind uebersichtlicher mit Icons und klaren Entscheidungs-Buttons.
Output: Aktualisierte KonfiDetailSections.tsx und ActivityRequestModal.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-admin-aktivitaeten-details-antraege-profil-jahrgaenge/98-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teamer-Detail-Header — Rosa Gradient + 3 Stats</name>
  <files>frontend/src/components/admin/views/KonfiDetailSections.tsx</files>
  <read_first>
    frontend/src/components/admin/views/KonfiDetailSections.tsx
    frontend/src/components/admin/views/KonfiDetailView.tsx
  </read_first>
  <action>
Aenderungen in KonfiDetailSections.tsx, Komponente `KonfiHeaderCard`:

1. **ATD-01 — Gradient-Farbe fuer Teamer:innen**: Der Header-Gradient (Zeile 107) ist aktuell immer `linear-gradient(135deg, #5b21b6 0%, #4c1d95 100%)` (lila). Aendern zu bedingt:
   ```tsx
   background: isTeamer
     ? 'linear-gradient(135deg, #e11d48 0%, #be185d 50%, #9f1239 100%)'
     : 'linear-gradient(135deg, #5b21b6 0%, #4c1d95 100%)',
   ```
   Auch den `boxShadow` anpassen:
   ```tsx
   boxShadow: isTeamer
     ? '0 20px 40px rgba(225, 29, 72, 0.3)'
     : '0 20px 40px rgba(91, 33, 182, 0.3)',
   ```

2. **ATD-01 — Ueberschrift "Teamer:in"**: Der grosse Overlay-Text (Zeile 139) zeigt aktuell `{(currentKonfi?.name || 'KONFI').toUpperCase()}`. Bei Teamer aendern zu:
   ```tsx
   {isTeamer ? 'TEAMER:IN' : (currentKonfi?.name || 'KONFI').toUpperCase()}
   ```

3. **ATD-01 — Nur 3 Stats fuer Teamer:innen**: Die Teamer-Stats (Zeile 219-237) zeigen aktuell 4 Chips (Badges, Zertifikate, Events, Aktivitaeten). Die "Aktivitaeten"-Stat entfernen. Das Stats-Array aendern zu:
   ```tsx
   {[
     { value: certificates.length, label: 'Zertifikate' },
     { value: teamerEvents.length, label: 'Events' },
     { value: currentKonfi?.badgeCount || 0, label: 'Badges' }
   ].map(stat => (
   ```
   Reihenfolge: Zertifikate, Events, Badges (wie in CONTEXT.md spezifiziert).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "#e11d48\|TEAMER:IN\|Zertifikate.*Events.*Badges" frontend/src/components/admin/views/KonfiDetailSections.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KonfiDetailSections.tsx enthaelt `#e11d48` (rosa Gradient-Start)
    - KonfiDetailSections.tsx enthaelt `#be185d` (rosa Gradient-Mitte)
    - KonfiDetailSections.tsx enthaelt `#9f1239` (rosa Gradient-Ende)
    - KonfiDetailSections.tsx enthaelt String `'TEAMER:IN'`
    - Teamer-Stats-Array hat genau 3 Eintraege: Zertifikate, Events, Badges (kein "Aktivitaeten")
    - grep-Ergebnis >= 2
  </acceptance_criteria>
  <done>Teamer-Header ist rosa mit 3 Stats (Zertifikate, Events, Badges), Ueberschrift "TEAMER:IN"</done>
</task>

<task type="auto">
  <name>Task 2: Antrags-Modal — Icons + farbige Entscheidungs-Buttons</name>
  <files>frontend/src/components/admin/modals/ActivityRequestModal.tsx</files>
  <read_first>
    frontend/src/components/admin/modals/ActivityRequestModal.tsx
  </read_first>
  <action>
Zwei Aenderungen in ActivityRequestModal.tsx:

1. **AAN-01 — Icons in Antragsdaten**: In der Antragsdaten-Sektion (Zeile 240-298) hat jedes IonItem kein Icon. Icons hinzufuegen mit `slot="start"` auf jedem IonItem:
   - Konfi (Zeile 242): `<IonIcon icon={personOutline} slot="start" style={{ color: '#5b21b6', fontSize: '1.2rem' }} />` — personOutline ist bereits importiert
   - Aktivitaet (Zeile 257): `<IonIcon icon={documentTextOutline} slot="start" style={{ color: '#047857', fontSize: '1.2rem' }} />` — documentTextOutline ist bereits importiert
   - Punkte (Zeile 266): Icon `trophyOutline` importieren und `<IonIcon icon={trophyOutline} slot="start" style={{ color: '#f59e0b', fontSize: '1.2rem' }} />`
   - Teilnahmedatum (Zeile 275): Icon `calendarOutline` importieren und `<IonIcon icon={calendarOutline} slot="start" style={{ color: '#007aff', fontSize: '1.2rem' }} />`
   - Eingereicht (Zeile 283): Icon `timeOutline` importieren und `<IonIcon icon={timeOutline} slot="start" style={{ color: '#8e8e93', fontSize: '1.2rem' }} />`
   - Kommentar (Zeile 292): Icon `chatbubbleOutline` importieren und `<IonIcon icon={chatbubbleOutline} slot="start" style={{ color: '#06b6d4', fontSize: '1.2rem' }} />`

   Neue Imports oben hinzufuegen: `trophyOutline, calendarOutline, timeOutline, chatbubbleOutline` aus `ionicons/icons`.

2. **AAN-02 — Entscheidungs-Buttons rot/gruen**: Die aktuellen Entscheidungs-Buttons (Zeile 411-449) sind als `app-list-item` Divs gestylt. Ersetze sie durch IonButton mit Outline-Style:
   ```tsx
   <div style={{ display: 'flex', gap: '12px' }}>
     <IonButton
       fill="outline"
       expand="block"
       onClick={() => !loading && setSelectedAction('approve')}
       disabled={loading}
       style={{
         flex: 1,
         '--border-color': '#059669',
         '--color': selectedAction === 'approve' ? 'white' : '#059669',
         '--background': selectedAction === 'approve' ? '#059669' : 'transparent',
         '--border-width': '2px',
         '--border-radius': '12px',
         fontWeight: '600'
       }}
     >
       <IonIcon icon={checkmarkCircle} slot="start" />
       Genehmigen
     </IonButton>
     <IonButton
       fill="outline"
       expand="block"
       onClick={() => !loading && setSelectedAction('reject')}
       disabled={loading}
       style={{
         flex: 1,
         '--border-color': '#dc3545',
         '--color': selectedAction === 'reject' ? 'white' : '#dc3545',
         '--background': selectedAction === 'reject' ? '#dc3545' : 'transparent',
         '--border-width': '2px',
         '--border-radius': '12px',
         fontWeight: '600'
       }}
     >
       <IonIcon icon={closeCircle} slot="start" />
       Ablehnen
     </IonButton>
   </div>
   ```
   Die alten div-basierten Buttons (Zeile 411-449) komplett ersetzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "trophyOutline\|calendarOutline\|--border-color.*#059669\|--border-color.*#dc3545" frontend/src/components/admin/modals/ActivityRequestModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ActivityRequestModal.tsx importiert `trophyOutline`, `calendarOutline`, `timeOutline`, `chatbubbleOutline`
    - Antragsdaten-IonItems haben jeweils ein `<IonIcon ... slot="start"` Element
    - Entscheidungs-Buttons sind IonButton mit `fill="outline"` und `--border-color: #059669` (gruen) bzw. `--border-color: #dc3545` (rot)
    - grep-Ergebnis >= 3
  </acceptance_criteria>
  <done>Antragsdaten haben Icons, Entscheidungs-Buttons sind outline gruen/rot mit Toggle-Effekt bei Auswahl</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler
- Teamer-Detail zeigt rosa Gradient, "TEAMER:IN" Overlay, 3 Stats
- Antrags-Modal zeigt Icons neben jedem Feld
- Entscheidungs-Buttons sind gruen (Genehmigen) und rot (Ablehnen) outline
</verification>

<success_criteria>
- Teamer-Header: rosa Gradient #e11d48 -> #be185d -> #9f1239
- Teamer-Header: Overlay-Text "TEAMER:IN"
- Teamer-Stats: genau 3 (Zertifikate, Events, Badges)
- Antrags-Modal: 6 Icons in Antragsdaten-Feldern
- Entscheidungs-Buttons: IonButton outline mit gruen/rot
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/98-admin-aktivitaeten-details-antraege-profil-jahrgaenge/98-02-SUMMARY.md`
</output>
