---
phase: 94-globale-ui-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/variables.css
  - frontend/src/components/admin/modals/BadgeManagementModal.tsx
  - frontend/src/components/admin/modals/ActivityManagementModal.tsx
  - frontend/src/components/admin/modals/LevelManagementModal.tsx
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/admin/modals/EventFormSections.tsx
  - frontend/src/components/admin/modals/BonusModal.tsx
  - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
autonomous: true
requirements: [AUI-01, AUI-02]

must_haves:
  truths:
    - "Slider zeigt aktuellen Wert prominent beim Ziehen (pin)"
    - "Slider hat Min-Wert links und Max-Wert rechts als kleine Beschriftung"
    - "Slider-Spannen sind sinnvoll begrenzt pro Kontext"
    - "Alle Listen-Cards haben identische Abstaende oben/unten wie rechts/links (16px)"
  artifacts:
    - path: "frontend/src/theme/variables.css"
      provides: "Symmetrisches Card-Padding"
      contains: "padding: 16px"
    - path: "frontend/src/components/admin/modals/EventFormSections.tsx"
      provides: "Slider mit Min/Max-Labels"
      contains: "ion-range-label"
  key_links:
    - from: "frontend/src/theme/variables.css"
      to: "alle Views mit ion-card.app-card"
      via: "CSS-Klasse app-card"
      pattern: "ion-card\\.app-card ion-card-content"
---

<objective>
Globale UI-Patterns fuer Slider-Verhalten und Listen-Card-Abstaende als Basis fuer alle v2.8 Phasen.

Purpose: Konsistente visuelle Basis, die alle nachfolgenden Phasen automatisch erben.
Output: Symmetrisches Card-Padding in variables.css, Slider mit Min/Max-Labels in allen 7 Admin-Modal-Dateien.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-globale-ui-patterns/94-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Listen-Card Padding symmetrisch machen (AUI-02)</name>
  <files>frontend/src/theme/variables.css</files>
  <read_first>
    - frontend/src/theme/variables.css (aktuelle app-card Styles, Zeile 41-73)
  </read_first>
  <action>
In `frontend/src/theme/variables.css` die Regel fuer `ion-card.app-card ion-card-content` aendern:

Aktuell (Zeile 48-50):
```css
ion-card.app-card ion-card-content {
  padding: 8px 16px;
}
```

Aendern zu:
```css
ion-card.app-card ion-card-content {
  padding: 16px;
}
```

Das ergibt symmetrisches Padding (16px oben, rechts, unten, links) statt asymmetrisch (8px oben/unten, 16px rechts/links).

HINWEIS: Es gibt bereits eine separate Klasse `ion-card-content.app-card-content` (Zeile ~996) mit `padding: 16px` — diese ist NICHT die gleiche Klasse und bleibt unveraendert.
  </action>
  <verify>
    <automated>grep -n "ion-card.app-card ion-card-content" frontend/src/theme/variables.css | head -1 && grep -A1 "ion-card.app-card ion-card-content {" frontend/src/theme/variables.css | grep "padding: 16px"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/theme/variables.css Zeile ~49 enthaelt `padding: 16px;` (nicht `8px 16px`)
    - Die Regel `ion-card.app-card ion-card-content` existiert weiterhin
    - Kein `8px 16px` mehr in der app-card ion-card-content Regel
  </acceptance_criteria>
  <done>Alle app-card Listen-Cards haben symmetrisches 16px Padding auf allen vier Seiten</done>
</task>

<task type="auto">
  <name>Task 2: Slider Min/Max-Labels und Spannen-Tuning (AUI-01)</name>
  <files>
    frontend/src/components/admin/modals/BadgeManagementModal.tsx
    frontend/src/components/admin/modals/ActivityManagementModal.tsx
    frontend/src/components/admin/modals/LevelManagementModal.tsx
    frontend/src/components/admin/modals/EventModal.tsx
    frontend/src/components/admin/modals/EventFormSections.tsx
    frontend/src/components/admin/modals/BonusModal.tsx
    frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx
  </files>
  <read_first>
    - frontend/src/components/admin/modals/EventFormSections.tsx (5 IonRange-Instanzen, groesste Datei)
    - frontend/src/components/admin/modals/BadgeManagementModal.tsx (2 IonRange-Instanzen)
    - frontend/src/components/admin/modals/BonusModal.tsx (1 IonRange-Instanz, Referenz fuer einfachen Fall)
    - frontend/src/components/admin/modals/ActivityManagementModal.tsx (1 IonRange)
    - frontend/src/components/admin/modals/LevelManagementModal.tsx (1 IonRange)
    - frontend/src/components/admin/modals/EventModal.tsx (1 IonRange)
    - frontend/src/components/admin/pages/AdminJahrgaengeePage.tsx (2 IonRange)
  </read_first>
  <action>
Fuer JEDE IonRange-Instanz in allen 7 Dateien:

1. **Min/Max-Labels hinzufuegen**: Einen Container um den IonRange legen, der links den Min-Wert und rechts den Max-Wert als kleine Zahl zeigt. Pattern:

```tsx
<div style={{ display: 'flex', alignItems: 'center', gap: '8px', width: '100%' }}>
  <span style={{ fontSize: '0.75rem', color: '#8e8e93', minWidth: '24px', textAlign: 'center' }}>{MIN}</span>
  <IonRange ... style={{ flex: 1 }} />
  <span style={{ fontSize: '0.75rem', color: '#8e8e93', minWidth: '24px', textAlign: 'center' }}>{MAX}</span>
</div>
```

2. **Spannen-Tuning** — folgende Aenderungen an min/max wo noetig:

| Datei | Slider | Aktuell | Neu | Grund |
|-------|--------|---------|-----|-------|
| EventFormSections.tsx | Check-in-Fenster (Minuten) | min=5, max=120, step=5 | min=5, max=60, step=5 | 120 Min unrealistisch |
| EventFormSections.tsx | Max. Teilnehmer | min=1, max=100, step=1 | min=1, max=50, step=1 | >50 unrealistisch fuer Konfi-Events |
| BonusModal.tsx | Punkte | min=1, max=50, step=1 | min=1, max=10, step=1 | Bonus-Punkte selten >10 |
| BadgeManagementModal.tsx | criteria_value | min=1, max=20 | BEHALTEN (OK) | |
| BadgeManagementModal.tsx | Zeitraum Wochen | min=1, max=52 | min=1, max=26, step=1 | Halbes Jahr reicht |
| ActivityManagementModal.tsx | Punkte | min=1, max=5 | BEHALTEN (OK) | Bereits sinnvoll |
| LevelManagementModal.tsx | Benoetigte Punkte | min=1, max=40 | BEHALTEN (OK) | |
| EventModal.tsx | Max Teilnehmer pro Slot | min=1, max=10 | BEHALTEN (OK) | |
| EventFormSections.tsx | Punkte | min=1, max=5 | BEHALTEN (OK) | |
| EventFormSections.tsx | Wartelisten-Plaetze | min=1, max=10 | BEHALTEN (OK) | |
| EventFormSections.tsx | Anzahl Events (Serie) | min=2, max=52 | min=2, max=26, step=1 | Halbes Jahr reicht |
| AdminJahrgaengeePage.tsx | Ziel Gottesdienst | min=1, max=40 | min=1, max=20, step=1 | 20 GD realistisch |
| AdminJahrgaengeePage.tsx | Ziel Gemeinde | min=1, max=40 | min=1, max=20, step=1 | 20 Gemeinde realistisch |

3. **pin={true}** ist bereits ueberall gesetzt — NICHTS aendern.

4. **KEINE Emojis** in Labels oder Code verwenden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "fontSize.*0.75rem.*color.*8e8e93.*minWidth.*24px" frontend/src/components/admin/modals/EventFormSections.tsx && grep -c "fontSize.*0.75rem.*color.*8e8e93.*minWidth.*24px" frontend/src/components/admin/modals/BonusModal.tsx && grep "max={60}" frontend/src/components/admin/modals/EventFormSections.tsx | head -1 && grep "max={10}" frontend/src/components/admin/modals/BonusModal.tsx | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Jede der 13 IonRange-Instanzen ist von einem flex-Container mit Min/Max-span-Elementen umgeben
    - Min-span hat style fontSize 0.75rem, color #8e8e93, minWidth 24px, textAlign center
    - Max-span hat identischen Style
    - EventFormSections.tsx: Check-in-Fenster hat max={60} (nicht max={120})
    - EventFormSections.tsx: Max. Teilnehmer hat max={50} (nicht max={100})
    - EventFormSections.tsx: Anzahl Events hat max={26} (nicht max={52})
    - BonusModal.tsx: Punkte hat max={10} (nicht max={50})
    - BadgeManagementModal.tsx: Zeitraum Wochen hat max={26} (nicht max={52})
    - AdminJahrgaengeePage.tsx: Ziel Gottesdienst hat max={20} (nicht max={40})
    - AdminJahrgaengeePage.tsx: Ziel Gemeinde hat max={20} (nicht max={40})
    - Alle anderen min/max Werte bleiben unveraendert
    - Keine Unicode-Emojis in den Dateien
    - `cd frontend && npx tsc --noEmit` hat keine neuen Fehler
  </acceptance_criteria>
  <done>Alle 13 Slider zeigen Min/Max-Werte links/rechts, Spannen sind fuer den jeweiligen Kontext sinnvoll begrenzt</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — keine neuen TypeScript-Fehler
2. `cd frontend && npm run build` — Build erfolgreich
3. Visuell: Alle app-card haben symmetrisches Padding
4. Visuell: Slider zeigen Min/Max-Labels links und rechts
</verification>

<success_criteria>
- Symmetrisches 16px Padding auf allen app-card ion-card-content Elementen
- 13 IonRange-Instanzen in 7 Dateien haben Min/Max-Labels
- Slider-Spannen sinnvoll begrenzt (Check-in max 60, Bonus max 10, etc.)
- Frontend baut fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/94-globale-ui-patterns/94-01-SUMMARY.md`
</output>
