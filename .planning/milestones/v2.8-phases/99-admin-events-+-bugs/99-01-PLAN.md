---
phase: 99-admin-events-+-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminEventsPage.tsx
  - frontend/src/components/admin/EventsView.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/admin/views/EventDetailSections.tsx
  - frontend/src/theme/variables.css
autonomous: true
requirements: [AEV-01, AEV-02, AEV-03, AEV-04, AEV-05]

must_haves:
  truths:
    - "Suchfeld hat IonListHeader mit app-section-icon Zwischenueberschrift 'Suche & Filter'"
    - "Jahrgangs-Filter sitzt in einer IonCard mit IonListHeader Zwischenueberschrift"
    - "Alle 'Kind hinzufuegen' Buttons zeigen 'Konfi hinzufuegen'"
    - "Chat-Button (chatbubbleOutline) erscheint im Header neben QR-Code-Button"
    - "Beschreibungstexte nutzen CSS-Variable --app-description-font-size mit Wert 1rem"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminEventsPage.tsx"
      provides: "Suchfeld mit Zwischenueberschrift"
      contains: "Suche & Filter"
    - path: "frontend/src/components/admin/EventsView.tsx"
      provides: "Jahrgangs-Filter mit IonListHeader"
      contains: "Jahrgang"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "Chat-Button im Header + Konfi hinzufuegen"
      contains: "chatbubbleOutline"
    - path: "frontend/src/theme/variables.css"
      provides: "CSS-Variable --app-description-font-size"
      contains: "--app-description-font-size"
  key_links:
    - from: "frontend/src/components/admin/views/EventDetailView.tsx"
      to: "handleCreateEventChat"
      via: "Chat-Button mit Bestaetigungs-Alert"
      pattern: "chatbubbleOutline"
---

<objective>
Admin Events UI-Polish: Suchfeld/Filter-Zwischenueberschriften, Chat-Button im Header, Beschreibungstext-Groesse und "Konfi hinzufuegen" Umbenennung.

Purpose: Konsistente UI-Patterns in der Admin-Event-Ansicht wie in anderen Admin-Views
Output: Verbesserte AdminEventsPage, EventsView, EventDetailView und globale CSS-Variable
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-admin-events-+-bugs/99-CONTEXT.md

<interfaces>
<!-- Aktuelles Suchfeld in AdminEventsPage.tsx (Zeile 363-370): -->
```tsx
<IonSearchbar
  className="ios26-searchbar-classic"
  value={searchText}
  onIonInput={(e) => setSearchText(e.detail.value || '')}
  placeholder="Events durchsuchen"
  debounce={300}
  style={{ padding: '0 16px' }}
/>
```

<!-- Aktueller Jahrgangs-Filter in EventsView.tsx (Zeile 166-187): -->
```tsx
{jahrgaenge && jahrgaenge.length > 0 && onJahrgangChange && (
  <div style={{ padding: '0 16px 8px' }}>
    <IonSelect ... />
  </div>
)}
```

<!-- Aktueller Header in EventDetailView.tsx (Zeile 504-512): -->
```tsx
<IonButtons slot="end">
  <IonButton onClick={() => presentQRDisplayModal(...)}>
    <IonIcon icon={qrCodeOutline} />
  </IonButton>
  <IonButton onClick={() => presentEventModalHook(...)}>
    <IonIcon icon={createOutline} />
  </IonButton>
</IonButtons>
```

<!-- "Kind hinzufuegen" in EventDetailView.tsx (Zeilen 618, 652, 685): -->
```tsx
Kind hinzufügen
```

<!-- app-description-text in variables.css (Zeile 1022): -->
```css
.app-description-text {
  font-size: 0.95rem;
  line-height: 1.5;
  ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suchfeld + Filter Zwischenueberschriften und Jahrgangs-Filter Card</name>
  <files>
    frontend/src/components/admin/pages/AdminEventsPage.tsx
    frontend/src/components/admin/EventsView.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminEventsPage.tsx
    frontend/src/components/admin/EventsView.tsx
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  </read_first>
  <action>
    **AdminEventsPage.tsx** (per AEV-01):
    1. Das IonSearchbar (Zeile 363-370) in eine IonList mit inset + IonListHeader wrappen:
       ```tsx
       <IonList className="app-section-inset" inset={true}>
         <IonListHeader>
           <div className="app-section-icon app-section-icon--events">
             <IonIcon icon={searchOutline} />
           </div>
           <IonLabel>Suche & Filter</IonLabel>
         </IonListHeader>
         <IonSearchbar ... />
       </IonList>
       ```
    2. `searchOutline` aus ionicons/icons importieren.
    3. Das `style={{ padding: '0 16px' }}` vom IonSearchbar entfernen (IonList inset uebernimmt Padding).

    **EventsView.tsx** (per AEV-02):
    1. Den Jahrgangs-Filter (Zeile 166-187) von `<div style={{ padding: '0 16px 8px' }}>` in eine IonList mit IonListHeader + IonCard umbauen:
       ```tsx
       {jahrgaenge && jahrgaenge.length > 0 && onJahrgangChange && (
         <IonList className="app-section-inset" inset={true}>
           <IonListHeader>
             <div className="app-section-icon app-section-icon--events">
               <IonIcon icon={peopleOutline} />
             </div>
             <IonLabel>Jahrgang</IonLabel>
           </IonListHeader>
           <IonCard className="app-card">
             <IonCardContent className="app-card-content">
               <IonSelect
                 value={selectedJahrgang}
                 placeholder="Alle Jahrgänge"
                 interface="popover"
                 onIonChange={(e) => onJahrgangChange(e.detail.value || null)}
               >
                 <IonSelectOption value={null}>Alle Jahrgänge</IonSelectOption>
                 {jahrgaenge.map(j => (
                   <IonSelectOption key={j.id} value={j.id}>{j.name}</IonSelectOption>
                 ))}
               </IonSelect>
             </IonCardContent>
           </IonCard>
         </IonList>
       )}
       ```
    2. Fehlende Imports ergaenzen: `IonList, IonListHeader, IonCard, IonCardContent` und `peopleOutline` aus ionicons/icons.
    3. Die alten Inline-Styles am IonSelect entfernen (kein --padding-start, fontSize, background, borderRadius mehr noetig).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "Suche & Filter" frontend/src/components/admin/pages/AdminEventsPage.tsx liefert mindestens 1 Treffer
    - grep "searchOutline" frontend/src/components/admin/pages/AdminEventsPage.tsx liefert mindestens 1 Treffer
    - grep "app-section-icon--events" frontend/src/components/admin/EventsView.tsx liefert mindestens 1 Treffer
    - grep "IonListHeader" frontend/src/components/admin/EventsView.tsx liefert mindestens 1 Treffer
    - grep "Jahrgang" frontend/src/components/admin/EventsView.tsx liefert mindestens 1 Treffer in IonListHeader-Kontext
    - grep -c "padding: '0 16px'" frontend/src/components/admin/pages/AdminEventsPage.tsx ergibt 0 (entfernt)
  </acceptance_criteria>
  <done>Suchfeld hat "Suche & Filter" Zwischenueberschrift im IonListHeader-Pattern. Jahrgangs-Filter sitzt in IonCard mit "Jahrgang" IonListHeader.</done>
</task>

<task type="auto">
  <name>Task 2: Chat-Button im Header + "Konfi hinzufuegen" + Beschreibungstext CSS-Variable</name>
  <files>
    frontend/src/components/admin/views/EventDetailView.tsx
    frontend/src/components/admin/views/EventDetailSections.tsx
    frontend/src/theme/variables.css
  </files>
  <read_first>
    frontend/src/components/admin/views/EventDetailView.tsx
    frontend/src/components/admin/views/EventDetailSections.tsx
    frontend/src/theme/variables.css
  </read_first>
  <action>
    **variables.css** (per AEV-05):
    1. Neue CSS-Variable `--app-description-font-size: 1rem;` im `:root`-Block hinzufuegen.
    2. Die bestehende `.app-description-text`-Klasse (Zeile 1022) aendern:
       ```css
       .app-description-text {
         font-size: var(--app-description-font-size, 1rem);
         line-height: 1.5;
         color: #374151;
         white-space: pre-wrap;
         margin: 0;
         padding: 0 4px;
       }
       ```

    **EventDetailView.tsx** (per AEV-04):
    1. `chatbubbleOutline` aus ionicons/icons importieren (zusaetzlich zu bestehenden Imports).
    2. Im Header `<IonButtons slot="end">` (Zeile 504-512) einen Chat-Button VOR dem QR-Button einfuegen:
       ```tsx
       <IonButtons slot="end">
         <IonButton onClick={handleChatButtonClick}>
           <IonIcon icon={chatbubbleOutline} />
         </IonButton>
         <IonButton onClick={() => presentQRDisplayModal(...)}>
           <IonIcon icon={qrCodeOutline} />
         </IonButton>
         <IonButton onClick={() => presentEventModalHook(...)}>
           <IonIcon icon={createOutline} />
         </IonButton>
       </IonButtons>
       ```
    3. Neue Funktion `handleChatButtonClick` hinzufuegen:
       ```tsx
       const handleChatButtonClick = () => {
         if (eventData?.chat_room_id) {
           handleNavigateToChat();
         } else {
           presentAlert({
             header: 'Chat erstellen?',
             message: `Möchtest du einen Chat für "${eventData?.name}" erstellen?`,
             buttons: [
               { text: 'Abbrechen', role: 'cancel' },
               { text: 'Erstellen', handler: () => handleCreateEventChat() }
             ]
           });
         }
       };
       ```

    **EventDetailView.tsx** (per AEV-03):
    4. Alle 3 Vorkommen von `Kind hinzufügen` (Zeilen 618, 652, 685) ersetzen durch `Konfi hinzufügen`.

    **EventDetailSections.tsx** — keine Aenderungen noetig (nutzt bereits `.app-description-text` Klasse).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "chatbubbleOutline" frontend/src/components/admin/views/EventDetailView.tsx liefert mindestens 2 Treffer (Import + Verwendung)
    - grep "handleChatButtonClick" frontend/src/components/admin/views/EventDetailView.tsx liefert mindestens 2 Treffer (Definition + Verwendung)
    - grep "Chat erstellen?" frontend/src/components/admin/views/EventDetailView.tsx liefert 1 Treffer
    - grep -c "Kind hinzufügen" frontend/src/components/admin/views/EventDetailView.tsx ergibt 0
    - grep "Konfi hinzufügen" frontend/src/components/admin/views/EventDetailView.tsx liefert mindestens 3 Treffer
    - grep "\-\-app-description-font-size" frontend/src/theme/variables.css liefert mindestens 2 Treffer (Definition + Verwendung)
    - grep "var(--app-description-font-size" frontend/src/theme/variables.css liefert 1 Treffer
  </acceptance_criteria>
  <done>Chat-Button mit chatbubbleOutline im Header vor QR-Button. Bestaetigungs-Alert wenn kein Chat existiert. Alle "Kind hinzufuegen" durch "Konfi hinzufuegen" ersetzt. Beschreibungstext nutzt globale CSS-Variable mit 1rem Default.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler
- AdminEventsPage zeigt "Suche & Filter" Zwischenueberschrift
- EventsView zeigt Jahrgangs-Filter in IonCard mit IonListHeader
- EventDetailView hat Chat-Button im Header
- Alle "Kind hinzufuegen" sind "Konfi hinzufuegen"
- .app-description-text nutzt CSS-Variable mit 1rem
</verification>

<success_criteria>
5 Requirements (AEV-01 bis AEV-05) sind implementiert. TypeScript kompiliert fehlerfrei.
</success_criteria>

<output>
After completion, create `.planning/phases/99-admin-events-+-bugs/99-01-SUMMARY.md`
</output>
