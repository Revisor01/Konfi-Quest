---
phase: 99-admin-events-+-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/pages/AdminEventsPage.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
autonomous: true
requirements: [AEV-06, AEV-07, AEV-08]

must_haves:
  truths:
    - "Event-Absagen in AdminEventsPage nutzt IonActionSheet statt prompt() mit Event-Details"
    - "Nach Absage wird refreshEvents() aufgerufen und das Event verschwindet aus der aktiven Liste"
    - "Event-Absagen in EventDetailView nutzt presentActionSheet statt presentAlert mit Event-Info"
    - "Beim Loeschen eines abgesagten Events zeigt der Dialog die Anzahl angemeldeter Konfis"
    - "Beim Loeschen eines abgesagten Events wird sendEventCancellationToKonfis aufgerufen"
  artifacts:
    - path: "frontend/src/components/admin/pages/AdminEventsPage.tsx"
      provides: "Verbesserter Absagen-Flow mit ActionSheet"
      contains: "presentActionSheet"
    - path: "frontend/src/components/admin/views/EventDetailView.tsx"
      provides: "Verbesserter Absagen-Dialog + Loesch-Push"
      contains: "Konfis angemeldet"
  key_links:
    - from: "frontend/src/components/admin/pages/AdminEventsPage.tsx"
      to: "/events/{id}/cancel"
      via: "api.put nach ActionSheet-Bestaetigung"
      pattern: "api\\.put.*cancel"
    - from: "frontend/src/components/admin/views/EventDetailView.tsx"
      to: "/events/{id}"
      via: "api.delete mit Push-Benachrichtigung"
      pattern: "api\\.delete.*events"
---

<objective>
Event-Absagen-Flow verbessern und Push-Benachrichtigung beim Loeschen abgesagter Events.

Purpose: Admins sehen einen informativen Absage-Dialog und Konfis werden benachrichtigt wenn ein abgesagtes Event geloescht wird
Output: Verbesserte handleCancelEvent in AdminEventsPage und EventDetailView + Loesch-mit-Push in EventDetailView
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-admin-events-+-bugs/99-CONTEXT.md

<interfaces>
<!-- Aktueller handleCancelEvent in AdminEventsPage.tsx (Zeile 256-279): -->
```tsx
const handleCancelEvent = async (event: Event) => {
  if (!isOnline) return;
  const message = prompt(
    `Event "${event.name}" absagen?\n\nNachricht an die Teilnehmer (optional):`,
    'Das Event wurde leider abgesagt...'
  );
  if (message === null) return;
  try {
    await api.put(`/events/${event.id}/cancel`, { notification_message: message });
    setSuccess(`Event "${event.name}" wurde abgesagt`);
    await refreshEvents();
    await refreshCancelled();
  } catch ...
};
```

<!-- Aktueller handleCancelEvent in EventDetailView.tsx (Zeile 362-384): -->
```tsx
const handleCancelEvent = async () => {
  if (!isOnline) return;
  presentAlert({
    header: 'Event absagen',
    message: 'Wirklich absagen? Alle Teilnehmer:innen werden benachrichtigt.',
    buttons: [
      { text: 'Abbrechen', role: 'cancel' },
      { text: 'Absagen', role: 'destructive', handler: async () => {
        await api.put(`/events/${eventData?.id}/cancel`);
        ...
      }}
    ]
  });
};
```

<!-- Event-Typ hat: registered_count, name, event_date -->
<!-- PushService.sendEventCancellationToKonfis(db, userIds, eventName, eventDate) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event-Absagen ActionSheet in AdminEventsPage + EventDetailView</name>
  <files>
    frontend/src/components/admin/pages/AdminEventsPage.tsx
    frontend/src/components/admin/views/EventDetailView.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminEventsPage.tsx
    frontend/src/components/admin/views/EventDetailView.tsx
  </read_first>
  <action>
    **AdminEventsPage.tsx** (per AEV-06, AEV-07):
    1. Den `handleCancelEvent` (Zeile 256-279) komplett ersetzen. Statt `prompt()` ein `presentActionSheet` verwenden:
       ```tsx
       const handleCancelEvent = async (event: Event) => {
         if (!isOnline) return;
         const eventDate = new Date(event.event_date).toLocaleDateString('de-DE', {
           weekday: 'short', day: '2-digit', month: '2-digit', year: 'numeric'
         });
         const konfiCount = (event.registered_count || 0) - (event.teamer_count || 0);
         presentActionSheet({
           header: `"${event.name}" absagen?`,
           subHeader: `${eventDate} | ${konfiCount} Konfis angemeldet`,
           buttons: [
             {
               text: 'Event absagen',
               role: 'destructive',
               icon: ban,
               handler: async () => {
                 try {
                   await api.put(`/events/${event.id}/cancel`, {
                     notification_message: 'Das Event wurde leider abgesagt.'
                   });
                   setSuccess(`Event "${event.name}" wurde abgesagt`);
                   await refreshEvents();
                   await refreshCancelled();
                 } catch (error: any) {
                   setError(error.response?.data?.error || 'Fehler beim Absagen');
                 }
               }
             },
             { text: 'Abbrechen', role: 'cancel' }
           ]
         });
       };
       ```
    2. `ban` ist bereits importiert. Sicherstellen dass `presentActionSheet` verwendet wird (ist bereits via `useIonActionSheet` vorhanden in Zeile 17/37).

    **EventDetailView.tsx** (per AEV-06, AEV-07):
    3. Den `handleCancelEvent` (Zeile 362-384) ersetzen durch ActionSheet-Variante:
       ```tsx
       const handleCancelEvent = async () => {
         if (!isOnline || !eventData) return;
         const eventDate = new Date(eventData.event_date).toLocaleDateString('de-DE', {
           weekday: 'short', day: '2-digit', month: '2-digit', year: 'numeric'
         });
         const konfiCount = participants.filter(p => p.role_name !== 'teamer').length;
         presentActionSheet({
           header: `"${eventData.name}" absagen?`,
           subHeader: `${eventDate} | ${konfiCount} Konfis angemeldet`,
           buttons: [
             {
               text: 'Event absagen',
               role: 'destructive',
               icon: ban,
               handler: async () => {
                 try {
                   await api.put(`/events/${eventData.id}/cancel`, {
                     notification_message: 'Das Event wurde leider abgesagt.'
                   });
                   setSuccess('Event wurde abgesagt');
                   onBack();
                 } catch (error: any) {
                   setError(error.response?.data?.error || 'Fehler beim Absagen');
                 }
               }
             },
             { text: 'Abbrechen', role: 'cancel' }
           ]
         });
       };
       ```
    4. `ban` aus ionicons/icons importieren (falls nicht vorhanden — pruefen ob bereits importiert).
    5. Nach Absage wird `onBack()` aufgerufen, was zurueck zur Liste navigiert. Da die Liste via useOfflineQuery `refreshEvents()` beim Sichtbar-Werden erneut laedt, wird das abgesagte Event automatisch aus der aktiven Liste entfernt (die aktive Liste filtert `cancelled` raus: `e.registration_status !== 'cancelled'`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "presentActionSheet" frontend/src/components/admin/pages/AdminEventsPage.tsx liefert mindestens 2 Treffer
    - grep "Konfis angemeldet" frontend/src/components/admin/pages/AdminEventsPage.tsx liefert 1 Treffer
    - grep -c "prompt(" frontend/src/components/admin/pages/AdminEventsPage.tsx ergibt 0 (prompt entfernt)
    - grep "presentActionSheet" frontend/src/components/admin/views/EventDetailView.tsx liefert mindestens 2 Treffer
    - grep "Konfis angemeldet" frontend/src/components/admin/views/EventDetailView.tsx liefert 1 Treffer
    - grep "Event absagen" frontend/src/components/admin/views/EventDetailView.tsx liefert mindestens 1 Treffer
  </acceptance_criteria>
  <done>Event-Absagen verwendet IonActionSheet mit Event-Details (Name, Datum, Konfi-Anzahl). AdminEventsPage nutzt kein prompt() mehr. EventDetailView navigiert nach Absage zurueck zur Liste.</done>
</task>

<task type="auto">
  <name>Task 2: Push-Benachrichtigung beim Loeschen abgesagter Events</name>
  <files>
    frontend/src/components/admin/pages/AdminEventsPage.tsx
  </files>
  <read_first>
    frontend/src/components/admin/pages/AdminEventsPage.tsx
    backend/routes/events.js
  </read_first>
  <action>
    **AdminEventsPage.tsx** (per AEV-08):
    Die Push-Benachrichtigung beim Loeschen wird bereits im Backend gehandhabt via `sendEventCancellationToKonfis`. Das Frontend muss nur den Loesch-Dialog informativer machen.

    1. In `deleteSingleEvent` (Zeile 176-202) den Alert-Text anpassen, um die Konfi-Anzahl anzuzeigen:
       ```tsx
       const deleteSingleEvent = async (event: Event) => {
         const konfiCount = (event.registered_count || 0) - (event.teamer_count || 0);
         const isCancelled = event.registration_status === 'cancelled';
         presentAlert({
           header: 'Event löschen',
           message: isCancelled && konfiCount > 0
             ? `Event "${event.name}" löschen? ${konfiCount} Konfis waren angemeldet und werden per Push benachrichtigt.`
             : `Event "${event.name}" wirklich löschen?`,
           buttons: [
             { text: 'Abbrechen', role: 'cancel' },
             {
               text: 'Löschen',
               role: 'destructive',
               handler: async () => {
                 try {
                   await api.delete(`/events/${event.id}`);
                   setSuccess(`Event "${event.name}" gelöscht`);
                   await refreshEvents();
                   await refreshCancelled();
                 } catch (error: any) {
                   setError(error.response?.data?.error || 'Fehler beim Löschen des Events');
                 }
               }
             }
           ]
         });
       };
       ```

    2. Im Backend (events.js) pruefen ob beim DELETE /events/:id bereits Push-Nachrichten gesendet werden. Falls nicht, muss das Backend angepasst werden: In der DELETE-Route fuer Events soll vor dem Loeschen geprueft werden ob es angemeldete Konfis gibt, und falls ja, `PushService.sendEventCancellationToKonfis` aufgerufen werden mit angepasstem Text.

    **WICHTIG**: Die Backend-Aenderung gehoert zu diesem Task. In `backend/routes/events.js` in der DELETE /events/:id Route (ca. Zeile 200-250) VOR dem eigentlichen Loeschen:
    ```js
    // Push an angemeldete Konfis wenn Event geloescht wird
    const { rows: bookedKonfis } = await db.query(
      `SELECT eb.user_id FROM event_bookings eb
       JOIN users u ON eb.user_id = u.id
       JOIN roles r ON u.role_id = r.id
       WHERE eb.event_id = $1 AND r.name = 'konfi' AND eb.status IN ('confirmed', 'waitlist')`,
      [eventId]
    );
    if (bookedKonfis.length > 0) {
      const userIds = bookedKonfis.map(b => b.user_id);
      await PushService.sendEventCancellationToKonfis(db, userIds, event.name, event.event_date);
    }
    ```
    Diesen Code VOR die DELETE-Query einfuegen, damit die Buchungen noch gelesen werden koennen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "Konfis waren angemeldet" frontend/src/components/admin/pages/AdminEventsPage.tsx liefert 1 Treffer
    - grep "per Push benachrichtigt" frontend/src/components/admin/pages/AdminEventsPage.tsx liefert 1 Treffer
    - grep "sendEventCancellationToKonfis" backend/routes/events.js liefert mindestens 2 Treffer (bestehend + neuer Aufruf)
    - grep "bookedKonfis" backend/routes/events.js liefert mindestens 1 Treffer
  </acceptance_criteria>
  <done>Loesch-Dialog zeigt Konfi-Anzahl bei abgesagten Events. Backend sendet Push-Benachrichtigung an angemeldete Konfis beim Loeschen.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler
- Event-Absagen nutzt ActionSheet mit Event-Details statt prompt()/Alert
- Loesch-Dialog zeigt Konfi-Info bei abgesagten Events
- Backend sendet Push beim Loeschen von Events mit angemeldeten Konfis
</verification>

<success_criteria>
3 Requirements (AEV-06, AEV-07, AEV-08) sind implementiert. Absagen-Flow nutzt ActionSheet. Push wird beim Loeschen gesendet.
</success_criteria>

<output>
After completion, create `.planning/phases/99-admin-events-+-bugs/99-02-SUMMARY.md`
</output>
