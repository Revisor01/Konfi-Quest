---
phase: 100-admin-zertifikate-material-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/modals/CertificateAssignModal.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements: [AZE-01, AZE-02, AZE-03, AZE-04]

must_haves:
  truths:
    - "Zertifikat-Zuweisen oeffnet ein vollstaendiges Modal statt IonAlert"
    - "Modal zeigt IonDatetimeButton fuer Start-Datum mit Default heute"
    - "Modal hat Laufzeit-Eingabe in Monaten"
    - "Gepicktes Zertifikat-Icon wird neben dem Typ angezeigt"
  artifacts:
    - path: "frontend/src/components/admin/modals/CertificateAssignModal.tsx"
      provides: "Zertifikat-Zuweisungs-Modal"
      contains: "IonDatetimeButton"
    - path: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      provides: "useIonModal statt presentAlert fuer Zertifikat-Zuweisung"
      contains: "CertificateAssignModal"
  key_links:
    - from: "KonfiDetailView.tsx"
      to: "CertificateAssignModal.tsx"
      via: "useIonModal Hook"
      pattern: "useIonModal.*CertificateAssignModal"
---

<objective>
Zertifikat-Zuweisen von IonAlert auf ein vollstaendiges Modal umbauen mit ordentlichem Datumspicker, Laufzeit-Eingabe und Icon-Anzeige.

Purpose: Die IonAlert-basierte Zuweisung hat eingeschraenkte UI-Moeglichkeiten (kein richtiger Datumspicker, kein Icon). Ein eigenes Modal bietet volle Kontrolle.
Output: CertificateAssignModal.tsx + angepasste KonfiDetailView.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/components/admin/views/KonfiDetailView.tsx
@frontend/src/components/admin/pages/AdminCertificatesPage.tsx
</context>

<interfaces>
<!-- CERT_ICONS Mapping aus AdminCertificatesPage.tsx (Zeile 102-140+) -->
<!-- 45+ Icons in 9 Kategorien: Erfolg, Engagement, Gemeinschaft, Lernen, Natur, Zeit, Musik, Reise, Kirche -->
<!-- Format: Record<string, { icon: any; name: string; category: string }> -->

<!-- Bestehende Zuweisung in KonfiDetailView.tsx Zeile 406-458: -->
<!-- presentAlert mit radio inputs fuer certificate_type_id + date inputs -->
<!-- API-Call: api.post(`/teamer/${konfiId}/certificates`, { certificate_type_id, issued_date, expiry_date }) -->

<!-- useIonModal Pattern aus CLAUDE.md: -->
<!-- const [presentModal, dismissModal] = useIonModal(MyModal, { onClose, onSuccess }); -->
<!-- presentModal({ presentingElement }); -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CertificateAssignModal erstellen</name>
  <files>frontend/src/components/admin/modals/CertificateAssignModal.tsx</files>
  <read_first>
    frontend/src/components/admin/views/KonfiDetailView.tsx
    frontend/src/components/admin/pages/AdminCertificatesPage.tsx
  </read_first>
  <action>
    Neue Datei `frontend/src/components/admin/modals/CertificateAssignModal.tsx` erstellen.

    Props-Interface:
    ```
    interface CertificateAssignModalProps {
      konfiId: number;
      availableTypes: Array<{ id: number; name: string; icon?: string }>;
      onClose: () => void;
      onSuccess: () => void;
    }
    ```

    Modal-Aufbau (per AZE-01):
    - IonPage mit IonHeader (Schliessen-Button links, Titel "Zertifikat zuweisen", Speichern-Button rechts)
    - IonContent mit app-gradient-background

    Sektion 1 - Zertifikat-Typ (per AZE-01):
    - IonList inset mit IonListHeader "Zertifikat-Typ"
    - IonCard mit IonRadioGroup
    - Jeder Typ als IonItem mit IonRadio
    - WICHTIG per AZE-04: Neben jedem Typ-Namen das zugehoerige Icon aus CERT_ICONS anzeigen (IonIcon). Den CERT_ICONS Record aus AdminCertificatesPage.tsx importieren oder als eigene Datei extrahieren. Falls der Typ ein icon-Feld hat, das Icon aus CERT_ICONS[icon] laden. Falls nicht vorhanden, ribbonOutline als Fallback verwenden.
    - Unter dem gewaehlten Typ eine Vorschau-Zeile: grosses Icon (32px) + Typ-Name in fett

    Sektion 2 - Datum (per AZE-02, AZE-03):
    - IonList inset mit IonListHeader "Zeitraum"
    - IonCard mit zwei Items:
      1. "Teamer seit" mit IonDatetimeButton (per AZE-02), Default: heutiges Datum im Format YYYY-MM-DD. IonDatetime in einem IonModal eingebettet (presentation="date", id="cert-start-date").
      2. "Laufzeit (Monate)" mit IonInput type="number" min="1" placeholder="z.B. 12" (per AZE-03). Optional - wenn leer, kein Ablaufdatum.

    Speichern-Handler:
    - certificate_type_id aus Radio-Selection
    - issued_date aus Datumspicker (Default: heute)
    - expiry_date: Wenn Laufzeit angegeben, issued_date + Laufzeit Monate berechnen (new Date, setMonth). Sonst null.
    - API-Call: api.post(`/teamer/${konfiId}/certificates`, { certificate_type_id, issued_date, expiry_date })
    - Erfolg: setSuccess('Zertifikat zugewiesen'), onSuccess()
    - Fehler: setError(err.response?.data?.error || 'Fehler beim Zuweisen')
    - useActionGuard fuer isSubmitting/guard Pattern

    KEINE Unicode Emojis! Nur IonIcon aus ionicons/icons.
    Echte Umlaute verwenden (fuer, Glueckwunsch etc. sind FALSCH).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -l "IonDatetimeButton" frontend/src/components/admin/modals/CertificateAssignModal.tsx && grep -l "CERT_ICONS\|certificate_type_id" frontend/src/components/admin/modals/CertificateAssignModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert unter frontend/src/components/admin/modals/CertificateAssignModal.tsx
    - Enthaelt "IonDatetimeButton" fuer Datumspicker
    - Enthaelt IonRadioGroup fuer Typ-Auswahl
    - Enthaelt IonInput type="number" fuer Laufzeit
    - Enthaelt "CERT_ICONS" oder vergleichbares Icon-Mapping
    - Enthaelt api.post(`/teamer/${konfiId}/certificates`)
    - Enthaelt useActionGuard
    - Keine Unicode Emojis
  </acceptance_criteria>
  <done>CertificateAssignModal als eigene Komponente mit Datumspicker, Laufzeit und Icon-Anzeige erstellt</done>
</task>

<task type="auto">
  <name>Task 2: KonfiDetailView auf neues Modal umstellen</name>
  <files>frontend/src/components/admin/views/KonfiDetailView.tsx</files>
  <read_first>
    frontend/src/components/admin/views/KonfiDetailView.tsx
    frontend/src/components/admin/modals/CertificateAssignModal.tsx
  </read_first>
  <action>
    In KonfiDetailView.tsx die bestehende IonAlert-basierte Zertifikat-Zuweisung (Zeile ~406-458) durch useIonModal ersetzen.

    1. Import hinzufuegen: `import CertificateAssignModal from '../modals/CertificateAssignModal';`

    2. useIonModal Hook aufsetzen (wie in CLAUDE.md beschrieben):
    ```
    const [presentCertModal, dismissCertModal] = useIonModal(CertificateAssignModal, {
      konfiId: konfiId,
      availableTypes: availableTypes,  // aus bestehendem certificateTypes Filter
      onClose: () => dismissCertModal(),
      onSuccess: () => {
        dismissCertModal();
        loadKonfiData();
      }
    });
    ```

    3. handleAssignCertificate Funktion vereinfachen:
    - Bisherige Logik fuer `availableTypes` beibehalten (Filter auf noch nicht zugewiesene Typen)
    - Statt presentAlert: `presentCertModal({ presentingElement: presentingElement || undefined })`
    - Den gesamten presentAlert-Block (Zeile 406-458) entfernen

    4. Falls presentingElement noch nicht existiert, das Pattern aus anderen Admin-Pages uebernehmen:
    ```
    const pageRef = useRef<HTMLElement>(null);
    const [presentingElement, setPresentingElement] = useState<HTMLElement | null>(null);
    useEffect(() => { setPresentingElement(pageRef.current); }, []);
    ```
    Und `<IonPage ref={pageRef}>` setzen (pruefen ob bereits vorhanden).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep "CertificateAssignModal" frontend/src/components/admin/views/KonfiDetailView.tsx && grep "presentCertModal" frontend/src/components/admin/views/KonfiDetailView.tsx && ! grep "type: 'radio' as any" frontend/src/components/admin/views/KonfiDetailView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - KonfiDetailView.tsx importiert CertificateAssignModal
    - useIonModal Hook fuer CertificateAssignModal vorhanden
    - Alter presentAlert-Block mit radio/date inputs ENTFERNT
    - presentCertModal wird in handleAssignCertificate aufgerufen
    - presentingElement korrekt eingerichtet
  </acceptance_criteria>
  <done>Zertifikat-Zuweisung nutzt das neue Modal statt IonAlert, alte Alert-Logik entfernt</done>
</task>

</tasks>

<verification>
- CertificateAssignModal.tsx existiert mit IonDatetimeButton, IonRadioGroup, IonInput Laufzeit, Icon-Anzeige
- KonfiDetailView.tsx nutzt useIonModal statt presentAlert fuer Zertifikate
- Keine "type: 'radio' as any" mehr in KonfiDetailView (alter Alert-Code)
- Frontend kompiliert: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Zertifikat-Zuweisung oeffnet ein vollstaendiges Modal (AZE-01)
- Datumspicker fuer "Teamer seit" mit Default heute (AZE-02)
- Laufzeit-Eingabe in Monaten berechnet Ablaufdatum automatisch (AZE-03)
- Gepicktes Icon wird im Modal angezeigt (AZE-04)
</success_criteria>

<output>
After completion, create `.planning/phases/100-admin-zertifikate-material-dashboard/100-01-SUMMARY.md`
</output>
