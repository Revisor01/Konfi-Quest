---
phase: 97-teamer-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  - frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
autonomous: true
requirements:
  - TEV-01
  - TEV-02
  - TEV-03
  - TEV-04

must_haves:
  truths:
    - "Suchleisten in TeamerEventsPage und TeamerBadgesPage folgen dem Chat-Pattern (IonList inset + 'Suche & Filter' Header)"
    - "MaterialModal Beschreibungstext nutzt app-description-text CSS-Klasse"
    - "Swipe-Back funktioniert nach Modal-Oeffnen"
    - "Anwesenheits-Info-Boxen haben konsistenten Stil"
    - "Material-Tab Beschreibungstext nutzt app-description-text CSS-Klasse"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerEventsPage.tsx"
      provides: "Suche & Filter Pattern + Anwesenheits-Boxen"
      contains: "Suche & Filter"
    - path: "frontend/src/components/teamer/pages/TeamerBadgesPage.tsx"
      provides: "Suche & Filter Pattern"
      contains: "Suche & Filter"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialPage.tsx"
      provides: "app-description-text auf Beschreibung"
      contains: "app-description-text"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx"
      provides: "app-description-text (bereits vorhanden, verifizieren)"
      contains: "app-description-text"
  key_links:
    - from: "TeamerEventsPage.tsx"
      to: "IonSearchbar"
      via: "IonList inset wrapper"
      pattern: "Suche & Filter"
    - from: "TeamerBadgesPage.tsx"
      to: "IonSearchbar"
      via: "IonList inset wrapper"
      pattern: "Suche & Filter"
---

<objective>
Teamer Events, Badges und Material Views polieren: Suchleisten ans Chat-Pattern anpassen, Beschreibungstexte mit app-description-text, Anwesenheits-Boxen konsistenter, Swipe-Back Fix.

Purpose: Teamer:innen erleben konsistente UI-Patterns ueber alle Views hinweg.
Output: Ueberarbeitete TeamerEventsPage, TeamerBadgesPage, TeamerMaterialPage, TeamerMaterialDetailPage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-teamer-ui/97-CONTEXT.md

@frontend/src/components/teamer/pages/TeamerEventsPage.tsx
@frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
@frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
@frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
@frontend/src/components/konfi/pages/KonfiEventsPage.tsx

<interfaces>
<!-- "Suche & Filter" Pattern aus KonfiEventsPage.tsx (Zeilen 157-178) -->
```tsx
<IonList inset={true} style={{ margin: '16px' }}>
  <IonListHeader>
    <div className="app-section-icon app-section-icon--events">
      <IonIcon icon={searchOutline} />
    </div>
    <IonLabel>Suche & Filter</IonLabel>
  </IonListHeader>
  <IonCard className="app-card">
    <IonCardContent style={{ padding: '8px 12px' }}>
      <IonSearchbar
        className="ios26-searchbar-classic"
        value={searchText}
        onIonInput={(e) => setSearchText(e.detail.value || '')}
        placeholder="Events durchsuchen"
        debounce={300}
      />
    </IonCardContent>
  </IonCard>
</IonList>
```

<!-- app-description-text CSS-Klasse (variables.css Zeile 1022) -->
<!-- Verwendung: <p className="app-description-text">{text}</p> -->

<!-- TeamerMaterialDetailPage hat bereits app-description-text (Zeile 201) -->

<!-- Anwesenheits-Boxen in TeamerEventsPage (Zeilen 620-648): -->
<!-- Anwesend: backgroundColor rgba(52, 199, 89, 0.12), color #34c759 -->
<!-- Abwesend: backgroundColor rgba(220, 53, 69, 0.12), color #dc3545 -->
<!-- Ausstehend: backgroundColor rgba(253, 126, 20, 0.12), color #fd7e14 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suche & Filter Pattern auf TeamerEventsPage + TeamerBadgesPage</name>
  <files>frontend/src/components/teamer/pages/TeamerEventsPage.tsx, frontend/src/components/teamer/pages/TeamerBadgesPage.tsx</files>
  <action>
    Per TEV-01: "Suche & Filter" SectionHeader mit searchOutline Icon + IonSearchbar in IonList inset — genau wie KonfiEventsPage.

    A) TeamerEventsPage.tsx (Zeilen 724-731):
    Die aktuelle Searchbar ist ein nacktes `<IonSearchbar>` mit `style={{ padding: '0 16px' }}`.
    Ersetzen durch das Chat-Pattern:
    1. `searchOutline` importieren aus ionicons/icons (pruefen ob schon importiert, sonst hinzufuegen)
    2. IonList, IonListHeader, IonCard, IonCardContent sind bereits importiert (Zeilen 16-18)
    3. Die nackte IonSearchbar (Zeilen 724-731) ersetzen durch:
    ```tsx
    <IonList inset={true} style={{ margin: '16px' }}>
      <IonListHeader>
        <div className="app-section-icon app-section-icon--events">
          <IonIcon icon={searchOutline} />
        </div>
        <IonLabel>Suche & Filter</IonLabel>
      </IonListHeader>
      <IonCard className="app-card">
        <IonCardContent style={{ padding: '8px 12px' }}>
          <IonSearchbar
            className="ios26-searchbar-classic"
            value={searchText}
            onIonInput={(e) => setSearchText(e.detail.value || '')}
            placeholder="Events durchsuchen"
            debounce={300}
          />
        </IonCardContent>
      </IonCard>
    </IonList>
    ```

    B) TeamerBadgesPage.tsx:
    Aktuell hat die Seite KEINE Searchbar. Hinzufuegen:
    1. `searchOutline` importieren, `IonSearchbar`, `IonCard`, `IonCardContent` pruefen (IonCard/IonCardContent bereits importiert)
    2. Einen `searchText` State hinzufuegen: `const [searchText, setSearchText] = useState('');`
    3. Nach dem Header-Banner (Zeile ~438) und VOR dem Filter-Segment (Zeile ~441), die Suche einfuegen:
    ```tsx
    <IonList inset={true} style={{ margin: '16px' }}>
      <IonListHeader>
        <div className="app-section-icon" style={{ backgroundColor: '#f59e0b' }}>
          <IonIcon icon={searchOutline} />
        </div>
        <IonLabel>Suche & Filter</IonLabel>
      </IonListHeader>
      <IonCard className="app-card">
        <IonCardContent style={{ padding: '8px 12px' }}>
          <IonSearchbar
            className="ios26-searchbar-classic"
            value={searchText}
            onIonInput={(e) => setSearchText(e.detail.value || '')}
            placeholder="Badges durchsuchen"
            debounce={300}
          />
        </IonCardContent>
      </IonCard>
    </IonList>
    ```
    4. In der `getCategories()` Funktion: vor dem Return, Badges nach searchText filtern.
       Am Anfang der Funktion, nach `let filtered = badges;`:
       ```typescript
       if (searchText.trim()) {
         const lower = searchText.toLowerCase();
         filtered = filtered.filter(b => b.name.toLowerCase().includes(lower) || (b.description || '').toLowerCase().includes(lower));
       }
       ```

    WICHTIG: `IonSearchbar` Import pruefen — in TeamerBadgesPage.tsx fehlt er wahrscheinlich, hinzufuegen.
    WICHTIG: `searchOutline` aus ionicons/icons importieren.
    WICHTIG: Keine Unicode Emojis!
  </action>
  <verify>
    grep -c "Suche & Filter" frontend/src/components/teamer/pages/TeamerEventsPage.tsx | xargs test 1 -le && echo "TEV-01 Events OK"
    grep -c "Suche & Filter" frontend/src/components/teamer/pages/TeamerBadgesPage.tsx | xargs test 1 -le && echo "TEV-01 Badges OK"
    grep -c "searchOutline" frontend/src/components/teamer/pages/TeamerEventsPage.tsx | xargs test 1 -le && echo "Icon OK"
  </verify>
  <done>Beide Teamer-Seiten (Events + Badges) haben "Suche & Filter" im Chat-Pattern (IonList inset + SectionHeader + IonCard).</done>
</task>

<task type="auto">
  <name>Task 2: Beschreibungstexte + Anwesenheits-Boxen + Swipe-Back Fix</name>
  <files>frontend/src/components/teamer/pages/TeamerMaterialPage.tsx, frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx, frontend/src/components/teamer/pages/TeamerEventsPage.tsx</files>
  <action>
    Per TEV-04: Material-Tab Beschreibungstext groesser.
    In TeamerMaterialPage.tsx Zeile 261: Die inline-Beschreibung hat `style={{ whiteSpace: 'pre-wrap', lineHeight: '1.5', color: '#374151', margin: 0 }}`.
    Ersetzen durch: `<p className="app-description-text">` (die CSS-Klasse sorgt fuer groessere, lesbarere Schrift).
    Sicherstellen, dass `whiteSpace: 'pre-wrap'` erhalten bleibt — entweder als zusaetzliches Style oder in der CSS-Klasse pruefen.
    Aenderung: `<p className="app-description-text" style={{ whiteSpace: 'pre-wrap' }}>`

    Per TEV-02: MaterialModal Beschreibungstext groesser.
    TeamerMaterialDetailPage.tsx Zeile 201: Hat BEREITS `className="app-description-text"`. VERIFIZIEREN, kein Aenderungsbedarf.

    Per TEV-02: Swipe-Back Fix nach Modal-Oeffnen.
    Das Problem: Wenn TeamerMaterialDetailPage als Modal geoeffnet wird (via useIonModal in TeamerEventsPage.tsx oder TeamerMaterialPage.tsx), kann man danach nicht mehr zurueck-swipen.
    FIX: Im TeamerMaterialDetailPage.tsx (ist ein Modal, kein Page — wird via `useIonModal` praesentiert) sicherstellen, dass die IonPage kein `canGoBack` blockiert.
    Pruefen: Das Modal wird ueber `useIonModal(TeamerMaterialDetailPage, {...})` geoeffnet. Der `presentingElement`-Parameter ist wichtig fuer korrektes Swipe-Verhalten.
    In TeamerEventsPage.tsx pruefen wo das MaterialDetail-Modal praesentiert wird und sicherstellen, dass `presentingElement` korrekt gesetzt ist:
    ```typescript
    presentMaterialDetail({
      presentingElement: presentingElement  // aus useModalPage
    });
    ```
    Falls `presentingElement` fehlt, hinzufuegen. Das ist der Hauptgrund fuer Swipe-Back-Probleme bei Ionic Sheet Modals.
    Ebenso in TeamerMaterialPage.tsx pruefen.

    Per TEV-03: Anwesenheits-Info-Boxen konsistenter stylen.
    In TeamerEventsPage.tsx Zeilen 620-648 gibt es drei Boxen (Anwesend/Abwesend/Ausstehend) mit inline-Styles.
    Konsistenter machen durch einheitliches Pattern:
    - Alle drei Boxen: gleiche borderRadius (12px), gleiche padding (12px 16px), gleiche fontSize (0.95rem), gleiche fontWeight (600)
    - Hinzufuegen: ein dezenter 1px border in der jeweiligen Farbe mit 0.3 opacity
    - Anwesend: border: '1px solid rgba(52, 199, 89, 0.3)'
    - Abwesend: border: '1px solid rgba(220, 53, 69, 0.3)'
    - Ausstehend: border: '1px solid rgba(253, 126, 20, 0.3)'
    - Icon fontSize einheitlich auf 1.2rem (statt 1.3rem) fuer kompakteren Look

    WICHTIG: Keine Unicode Emojis! Echte Umlaute in UI-Texten!
  </action>
  <verify>
    grep -c "app-description-text" frontend/src/components/teamer/pages/TeamerMaterialPage.tsx | xargs test 1 -le && echo "TEV-04 OK"
    grep -c "app-description-text" frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx | xargs test 1 -le && echo "TEV-02 text OK"
    grep -c "border.*rgba.*199.*89" frontend/src/components/teamer/pages/TeamerEventsPage.tsx | xargs test 1 -le && echo "TEV-03 OK"
    cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5
  </verify>
  <done>Material-Beschreibungen nutzen app-description-text. Anwesenheits-Boxen haben konsistenten Stil mit Borders. Swipe-Back nach Modal funktioniert durch korrektes presentingElement.</done>
</task>

</tasks>

<verification>
- "Suche & Filter" Header in TeamerEventsPage und TeamerBadgesPage vorhanden
- Searchbar in IonList inset mit IonCard gewrappt
- app-description-text auf Material-Beschreibungen (Page + Modal)
- Anwesenheits-Boxen mit einheitlichem Border-Stil
- TypeScript kompiliert ohne Fehler
</verification>

<success_criteria>
1. TeamerEventsPage + TeamerBadgesPage zeigen "Suche & Filter" SectionHeader mit Searchbar in IonList-Card
2. Material-Beschreibungstexte nutzen app-description-text CSS-Klasse
3. Anwesenheits-Boxen (Anwesend/Abwesend/Ausstehend) haben konsistenten Stil mit dezenten Borders
4. Swipe-Back funktioniert nach Modal-Oeffnen
5. Frontend kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/97-teamer-ui/97-02-SUMMARY.md`
</output>
