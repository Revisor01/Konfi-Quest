---
phase: 69-universeller-datei-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/shared/FileViewerModal.tsx
  - frontend/src/components/shared/FileViewerModal.css
autonomous: true
requirements: [FV-01, FV-02, FV-03, FV-04, FV-05, FV-06, FV-07]

must_haves:
  truths:
    - "Bilder werden fullscreen mit Zoom/Pan/Pinch angezeigt"
    - "Swipe zwischen mehreren Dateien funktioniert"
    - "PDF wird inline als iframe angezeigt"
    - "Videos werden als HTML5 video mit controls angezeigt"
    - "Unbekannte Dateitypen zeigen Download-Fallback mit Icon"
    - "Download-Button laedt Datei herunter (Web: anchor, Nativ: Filesystem)"
    - "Share-Button oeffnet natives Share-Sheet (nur auf Capacitor)"
    - "Dunkler Fullscreen-Hintergrund, Schliessen via X und Swipe-Down"
  artifacts:
    - path: "frontend/src/components/shared/FileViewerModal.tsx"
      provides: "Universeller Datei-Viewer Komponente"
      exports: ["default (FileViewerModal)"]
    - path: "frontend/src/components/shared/FileViewerModal.css"
      provides: "Styling fuer dunklen Fullscreen-Viewer"
  key_links:
    - from: "FileViewerModal.tsx"
      to: "@capacitor/share"
      via: "Share.share() Aufruf"
      pattern: "Share\\.share"
    - from: "FileViewerModal.tsx"
      to: "@capacitor/filesystem"
      via: "Filesystem.writeFile + getUri fuer nativen Download"
      pattern: "Filesystem\\.(writeFile|getUri)"
---

<objective>
Universelles FileViewerModal erstellen, das Bilder (Zoom/Pan/Pinch), PDFs (inline), Videos (HTML5 player), und unbekannte Dateitypen (Download-Fallback) unterstuetzt. Mit Swipe zwischen Dateien, Download/Share-Buttons, und dunklem Fullscreen-UI.

Purpose: Ersetzt das bestehende minimale FileViewerModal (chat/modals/FileViewerModal.tsx) durch einen vollwertigen Viewer mit nativem Look.
Output: Neue FileViewerModal-Komponente + CSS unter shared/
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-universeller-datei-viewer/69-CONTEXT.md
@frontend/src/components/chat/modals/FileViewerModal.tsx
@frontend/package.json

<interfaces>
<!-- Bestehende FileViewerModal Props (wird ersetzt): -->
From frontend/src/components/chat/modals/FileViewerModal.tsx:
```typescript
interface FileViewerModalProps {
  blobUrl: string;
  fileName: string;
  mimeType: string;
  onClose: () => void;
}
```

<!-- Capacitor Plugins (bereits installiert): -->
- @capacitor/share (^7.0.4) — Share.share()
- @capacitor/filesystem (^7.1.8) — Filesystem.writeFile, getUri
- @capacitor/haptics (7.0.1) — Haptics.impact

<!-- Ionic Pattern (useIonModal): -->
```typescript
const [presentModal, dismissModal] = useIonModal(FileViewerModal, { ...props });
presentModal({ cssClass: 'file-viewer-modal' });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FileViewerModal Komponente + CSS erstellen</name>
  <files>frontend/src/components/shared/FileViewerModal.tsx, frontend/src/components/shared/FileViewerModal.css</files>
  <read_first>
    - frontend/src/components/chat/modals/FileViewerModal.tsx (bestehendes Modal, wird ersetzt)
    - frontend/package.json (installierte Capacitor Plugins pruefen)
    - frontend/src/components/chat/ChatRoom.tsx (Zeilen 266-283: openInAppViewer Pattern)
    - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx (Zeilen 86-104: useIonModal Pattern)
  </read_first>
  <action>
    Neue Datei `frontend/src/components/shared/FileViewerModal.tsx` erstellen:

    **Interface (per D-14, D-15):**
    ```typescript
    interface FileItem {
      url: string;         // Blob-URL oder API-URL
      fileName: string;
      mimeType: string;
    }

    interface FileViewerModalProps {
      files: FileItem[];       // Alle Dateien im Kontext (per D-07 fuer Swipe)
      initialIndex: number;    // Start-Index in files[]
      onClose: () => void;
    }
    ```

    **Rueckwaerts-Kompatibilitaet:** Auch Einzel-Datei-Props unterstuetzen (blobUrl, fileName, mimeType) fuer einfache Nutzung. Wenn `files` leer/undefined und blobUrl gesetzt, automatisch in files-Array wrappen.

    **Bild-Viewer (per D-01, D-06):**
    - CSS transform fuer Zoom/Pan: useState fuer `scale`, `translateX`, `translateY`
    - Doppeltap zum Reinzoomen (scale toggle 1 ↔ 2.5)
    - Pinch-to-Zoom via Touch-Events (onTouchStart/onTouchMove mit 2 Fingern, Distanz berechnen)
    - Drag/Pan wenn gezoomt (onTouchMove mit 1 Finger wenn scale > 1)
    - Desktop: onWheel fuer Zoom, onMouseDown/onMouseMove fuer Pan
    - Reset Zoom bei Slide-Wechsel
    - `will-change: transform` fuer Performance
    - KEINE externe Library — CSS transform + Touch-Events reichen fuer Pinch-to-Zoom

    **Swipe zwischen Dateien (per D-07):**
    - useState fuer `currentIndex`
    - Touch-Swipe: onTouchStart/onTouchEnd X-Differenz > 50px → prev/next
    - Swipe NUR wenn scale === 1 (nicht gezoomt), sonst ist Swipe = Pan
    - Anzeige: "2 / 5" Counter unten mittig wenn files.length > 1
    - Uebergangs-Animation: CSS transition auf transform translateX

    **PDF Viewer (per D-02):**
    - iframe mit src={url}, width/height 100%, weisser Hintergrund
    - Kein externer PDF.js noetig — Browser-iframe reicht

    **Video Player (per D-03):**
    - HTML5 `<video>` Element mit controls, autoPlay={false}, playsInline
    - Voller Viewport, object-fit: contain
    - Poster-Attribut optional (kein Thumbnail vorhanden)

    **Office/Unbekannt Fallback (per D-04, D-05):**
    - Zentrierte Karte mit: grossem IonIcon (documentOutline), Dateiname, Dateityp-Label
    - "Herunterladen" Button prominent darunter

    **Download-Button (per D-08):**
    - Immer sichtbar oben rechts neben X-Button
    - Web: `<a>` Element mit download-Attribut + blob URL
    - Nativ (Capacitor.isNativePlatform()): Filesystem.writeFile → share oder Notification
    - Icon: downloadOutline aus ionicons

    **Share-Button (per D-09):**
    - Nur sichtbar wenn `Capacitor.isNativePlatform() === true`
    - Nutzt @capacitor/share: `Share.share({ title: fileName, url: fileUri, dialogTitle: 'Datei teilen' })`
    - Fuer nativen Share: Datei erst via Filesystem speichern, dann URI teilen
    - Icon: shareOutline aus ionicons

    **UI Layout (per D-10, D-16):**
    - Kein IonHeader/IonToolbar — eigenes minimales UI
    - Schwarzer Hintergrund (rgba(0,0,0,0.95))
    - Oben: halbtransparente Toolbar mit X-Button (links), Dateiname (mitte), Download + Share (rechts)
    - Toolbar: position absolute, z-index ueber Content, backdrop-filter: blur(8px), background: rgba(0,0,0,0.5)
    - Swipe-Down zum Schliessen: onTouchMove Y-Differenz > 150px nach unten → onClose()
    - X-Button oben links: onClose()

    **Zustandsmanagement:**
    - `currentIndex` (welche Datei aktiv)
    - `scale` (Zoom-Level, 1 = normal)
    - `position` ({x, y} fuer Pan)
    - `isDragging` (Pan aktiv)
    - Scale/Position reset bei Index-Wechsel

    KEINE Unicode Emojis! Nur IonIcons.
    Echte Umlaute in allen UI-Texten (Schliessen, Herunterladen, etc.).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "interface FileItem" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "interface FileViewerModalProps" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "pinch\|Pinch\|scale" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "Share.share" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "Filesystem" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "downloadOutline\|shareOutline" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "iframe" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "video" frontend/src/components/shared/FileViewerModal.tsx
    - grep -q "rgba(0.*0.*0" frontend/src/components/shared/FileViewerModal.css
    - test -f frontend/src/components/shared/FileViewerModal.css
  </acceptance_criteria>
  <done>
    FileViewerModal.tsx exportiert eine Komponente mit FileItem[]-Support, Zoom/Pan/Pinch fuer Bilder, Swipe zwischen Dateien, PDF iframe, Video player, Download + Share Buttons, dunklem Fullscreen-UI. CSS-Datei existiert mit dunklem Theme. TypeScript kompiliert fehlerfrei.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` kompiliert fehlerfrei
- FileViewerModal.tsx hat alle Dateityp-Handler (image, pdf, video, fallback)
- FileViewerModal.tsx hat Zoom/Pan Touch-Handler
- FileViewerModal.tsx hat Swipe-Navigation zwischen Dateien
- FileViewerModal.tsx hat Download und Share (Capacitor) Buttons
- FileViewerModal.css hat dunklen Fullscreen-Style
</verification>

<success_criteria>
- Neue FileViewerModal-Komponente unter shared/ mit vollstaendigem Feature-Set
- Alle 7 Requirements (FV-01 bis FV-07) abgedeckt
- TypeScript fehlerfrei
- Keine Emojis, echte Umlaute
</success_criteria>

<output>
After completion, create `.planning/phases/69-universeller-datei-viewer/69-01-SUMMARY.md`
</output>
