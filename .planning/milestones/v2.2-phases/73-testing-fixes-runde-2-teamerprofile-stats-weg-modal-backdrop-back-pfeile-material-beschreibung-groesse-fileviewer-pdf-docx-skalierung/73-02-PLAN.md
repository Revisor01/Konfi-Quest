---
phase: 73-testing-fixes-runde-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/shared/FileViewerModal.tsx
autonomous: true
requirements: [BUG-05]

must_haves:
  truths:
    - "PDF-Dateien werden auf nativen Plattformen (iOS/Android) im nativen Viewer geoeffnet"
    - "DOCX-Dateien werden auf nativen Plattformen im nativen Viewer geoeffnet"
    - "PDF/DOCX im Web-Browser behalten bisheriges Verhalten (iframe/Fallback)"
  artifacts:
    - path: "frontend/src/components/shared/FileViewerModal.tsx"
      provides: "Natives Oeffnen von PDF/DOCX per FileOpener"
  key_links:
    - from: "FileViewerModal.tsx"
      to: "@capacitor-community/file-opener"
      via: "FileOpener.open() nach Filesystem.writeFile()"
      pattern: "FileOpener\\.open"
---

<objective>
FileViewerModal so erweitern, dass PDF und DOCX auf nativen Plattformen (iOS/Android) per FileOpener nativ geoeffnet werden statt im iframe/Fallback.

Purpose: PDF/DOCX sind im iframe auf iOS nicht zoom-/scrollbar — nativer Viewer bietet Quick Look mit Zoom/Pan
Output: FileViewerModal mit nativem PDF/DOCX-Support
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-testing-fixes-runde-2-teamerprofile-stats-weg-modal-backdrop-back-pfeile-material-beschreibung-groesse-fileviewer-pdf-docx-skalierung/73-CONTEXT.md

@frontend/src/components/shared/FileViewerModal.tsx

<interfaces>
<!-- FileOpener (bereits installiert, @capacitor-community/file-opener ^7.0.1): -->
```typescript
import { FileOpener } from '@capacitor-community/file-opener';
// FileOpener.open({ filePath: string, contentType?: string })
```

<!-- Filesystem (bereits importiert in FileViewerModal): -->
```typescript
import { Filesystem, Directory } from '@capacitor/filesystem';
// Filesystem.writeFile({ path, data (base64), directory })
// returns { uri: string }
```

<!-- resolveUrl bereits vorhanden in FileViewerModal: -->
```typescript
const resolveUrl = async (url: string): Promise<string> => { ... }
// Liefert Blob-URL zurueck
```

<!-- getFileCategory bereits vorhanden: -->
```typescript
const getFileCategory = (mimeType: string): 'image' | 'pdf' | 'video' | 'fallback' => { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF/DOCX nativ oeffnen per FileOpener</name>
  <files>frontend/src/components/shared/FileViewerModal.tsx</files>
  <read_first>
    - frontend/src/components/shared/FileViewerModal.tsx (KOMPLETT lesen)
  </read_first>
  <action>
Per D-12 bis D-18: PDF und DOCX auf nativen Plattformen per FileOpener oeffnen.

1. **Import hinzufuegen** (oben bei den Imports):
   ```typescript
   import { FileOpener } from '@capacitor-community/file-opener';
   ```

2. **getFileCategory erweitern** — Neue Kategorie 'document' fuer DOCX/DOC/ODT:
   ```typescript
   const getFileCategory = (mimeType: string): 'image' | 'pdf' | 'video' | 'document' | 'fallback' => {
     if (mimeType.startsWith('image/')) return 'image';
     if (mimeType === 'application/pdf') return 'pdf';
     if (mimeType.startsWith('video/')) return 'video';
     if (mimeType === 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
       || mimeType === 'application/msword'
       || mimeType === 'application/vnd.oasis.opendocument.text') return 'document';
     return 'fallback';
   };
   ```

3. **Neue Hilfsfunktion** `openFileNatively` VOR der Komponente:
   ```typescript
   const openFileNatively = async (resolvedUrl: string, fileName: string, mimeType: string): Promise<boolean> => {
     if (!Capacitor.isNativePlatform()) return false;
     try {
       // Blob laden
       const response = await fetch(resolvedUrl);
       const blob = await response.blob();
       const reader = new FileReader();
       return new Promise<boolean>((resolve) => {
         reader.onload = async () => {
           try {
             const base64 = (reader.result as string).split(',')[1];
             const result = await Filesystem.writeFile({
               path: fileName,
               data: base64,
               directory: Directory.Cache
             });
             await FileOpener.open({
               filePath: result.uri,
               contentType: mimeType
             });
             resolve(true);
           } catch {
             resolve(false);
           }
         };
         reader.onerror = () => resolve(false);
         reader.readAsDataURL(blob);
       });
     } catch {
       return false;
     }
   };
   ```

4. **State fuer natives Oeffnen**: Innerhalb der Komponente, nach den bestehenden States:
   ```typescript
   const [nativeOpening, setNativeOpening] = useState(false);
   ```

5. **useEffect fuer automatisches natives Oeffnen** — NACH dem URL-Resolve useEffect:
   Wenn auf nativer Plattform UND Kategorie ist 'pdf' oder 'document', automatisch nativ oeffnen:
   ```typescript
   useEffect(() => {
     if (!resolvedUrl || !currentFile || !isNative) return;
     const cat = getFileCategory(currentFile.mimeType);
     if (cat !== 'pdf' && cat !== 'document') return;

     setNativeOpening(true);
     openFileNatively(resolvedUrl, currentFile.fileName, currentFile.mimeType).then((success) => {
       setNativeOpening(false);
       if (success) {
         // Native Viewer geoeffnet — Modal schliessen
         onClose();
       }
       // Bei Fehler: Fallback zum bestehenden Rendering (iframe/Download)
     });
   }, [resolvedUrl, currentFile, isNative]);
   ```
   WICHTIG: `isNative` muss VOR dem renderContent-Block definiert sein (ist es bereits: Zeile 365).

6. **renderContent anpassen** — Im 'pdf' case und neuen 'document' case:
   ```typescript
   case 'pdf':
     // Auf nativer Plattform: Ladeindikator waehrend FileOpener startet
     if (isNative) {
       return (
         <div className="file-viewer-fallback">
           <IonIcon icon={documentOutline} className="file-viewer-fallback-icon" />
           <p className="file-viewer-fallback-name">{currentFile.fileName}</p>
           <p className="file-viewer-fallback-type">
             {nativeOpening ? 'Wird geoeffnet...' : 'Tippe zum erneut Oeffnen'}
           </p>
           {!nativeOpening && (
             <button className="file-viewer-fallback-btn" onClick={() => {
               if (resolvedUrl) {
                 setNativeOpening(true);
                 openFileNatively(resolvedUrl, currentFile.fileName, currentFile.mimeType).then((success) => {
                   setNativeOpening(false);
                   if (success) onClose();
                 });
               }
             }}>
               <IonIcon icon={documentOutline} />
               Nativ oeffnen
             </button>
           )}
         </div>
       );
     }
     // Web: iframe beibehalten
     return (
       <iframe
         src={resolvedUrl}
         title={currentFile.fileName}
         className="file-viewer-pdf"
         allow="fullscreen"
       />
     );

   case 'document':
     // Auf nativer Plattform: Ladeindikator (wird automatisch per useEffect geoeffnet)
     if (isNative) {
       return (
         <div className="file-viewer-fallback">
           <IonIcon icon={documentOutline} className="file-viewer-fallback-icon" />
           <p className="file-viewer-fallback-name">{currentFile.fileName}</p>
           <p className="file-viewer-fallback-type">
             {nativeOpening ? 'Wird geoeffnet...' : 'Tippe zum erneut Oeffnen'}
           </p>
           {!nativeOpening && (
             <button className="file-viewer-fallback-btn" onClick={() => {
               if (resolvedUrl) {
                 setNativeOpening(true);
                 openFileNatively(resolvedUrl, currentFile.fileName, currentFile.mimeType).then((success) => {
                   setNativeOpening(false);
                   if (success) onClose();
                 });
               }
             }}>
               <IonIcon icon={documentOutline} />
               Nativ oeffnen
             </button>
           )}
         </div>
       );
     }
     // Web: Download-Fallback (DOCX kann nicht inline angezeigt werden)
     return (
       <div className="file-viewer-fallback">
         <IonIcon icon={documentOutline} className="file-viewer-fallback-icon" />
         <p className="file-viewer-fallback-name">{currentFile.fileName}</p>
         <p className="file-viewer-fallback-type">{currentFile.mimeType}</p>
         <button className="file-viewer-fallback-btn" onClick={handleDownload}>
           <IonIcon icon={downloadOutline} />
           Herunterladen
         </button>
       </div>
     );
   ```

7. **WICHTIG**: Die `isNative` Variable ist bereits in Zeile 365 definiert (`const isNative = Capacitor.isNativePlatform();`). Stelle sicher dass sie VOR dem useEffect in Schritt 5 definiert ist — falls nicht, verschiebe die Definition nach oben (nach den State-Definitionen, VOR die useEffects).

8. **UI-Texte**: Echte Umlaute verwenden: "geoeffnet" -> "geöffnet", "Öffnen" etc.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "FileOpener" frontend/src/components/shared/FileViewerModal.tsx | grep -v "^0$" && grep -c "openFileNatively" frontend/src/components/shared/FileViewerModal.tsx | grep -v "^0$" && grep -c "'document'" frontend/src/components/shared/FileViewerModal.tsx | grep -v "^0$" && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "FileOpener" FileViewerModal.tsx liefert mindestens 2 Treffer (import + usage)
    - grep "openFileNatively" FileViewerModal.tsx liefert mindestens 3 Treffer (Definition + Aufrufe)
    - grep "'document'" FileViewerModal.tsx liefert mindestens 2 Treffer (getFileCategory + case)
    - grep "isNativePlatform" FileViewerModal.tsx liefert mindestens 1 Treffer
    - case 'pdf' hat nativen Branch (if isNative) und Web-Branch (iframe)
    - case 'document' hat nativen Branch und Web-Branch (Download-Fallback)
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>PDF und DOCX werden auf nativen Plattformen per FileOpener im nativen Viewer geoeffnet, Web behaelt iframe/Download-Fallback</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler
- FileOpener Import vorhanden
- PDF-Case hat nativen und Web-Branch
- DOCX wird als 'document' Kategorie erkannt
- Keine Emojis in UI-Texten
</verification>

<success_criteria>
- PDF auf nativer Plattform: FileOpener.open() wird aufgerufen
- DOCX auf nativer Plattform: FileOpener.open() wird aufgerufen
- PDF im Web: iframe (unveraendert)
- DOCX im Web: Download-Fallback (unveraendert fuer existierende Fallback-Kategorie)
- TypeScript kompiliert ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/73-testing-fixes-runde-2-teamerprofile-stats-weg-modal-backdrop-back-pfeile-material-beschreibung-groesse-fileviewer-pdf-docx-skalierung/73-02-SUMMARY.md`
</output>
