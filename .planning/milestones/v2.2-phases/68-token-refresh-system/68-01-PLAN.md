---
phase: 68-token-refresh-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/068_refresh_tokens.sql
  - backend/middleware/rbac.js
  - backend/routes/auth.js
autonomous: true
requirements: [TOKEN-DB, TOKEN-REFRESH, TOKEN-REVOKE, TOKEN-LOGIN]

must_haves:
  truths:
    - "POST /auth/refresh gibt neuen Access-Token + neuen Refresh-Token zurueck"
    - "Login-Response enthaelt refresh_token neben token + user"
    - "verifyTokenRBAC prueft token_invalidated_at und gibt 401 bei zu altem Token"
    - "Access-Token laeuft nach 15 Minuten ab (nicht 90 Tage)"
    - "Refresh-Token-Rotation: altes Token wird bei Refresh sofort ungueltig"
  artifacts:
    - path: "backend/migrations/068_refresh_tokens.sql"
      provides: "refresh_tokens Tabelle + token_invalidated_at Spalte auf users"
      contains: "CREATE TABLE IF NOT EXISTS refresh_tokens"
    - path: "backend/routes/auth.js"
      provides: "/auth/refresh Endpoint + Login mit Refresh-Token"
      contains: "router.post('/refresh'"
    - path: "backend/middleware/rbac.js"
      provides: "token_invalidated_at Check in verifyTokenRBAC"
      contains: "token_invalidated_at"
  key_links:
    - from: "backend/routes/auth.js"
      to: "refresh_tokens Tabelle"
      via: "INSERT/SELECT auf refresh_tokens"
      pattern: "refresh_tokens"
    - from: "backend/middleware/rbac.js"
      to: "users.token_invalidated_at"
      via: "DB-Query vergleicht iat mit token_invalidated_at"
      pattern: "token_invalidated_at"
---

<objective>
Backend-Infrastruktur fuer das Token-Refresh-System: DB-Migration (refresh_tokens Tabelle + token_invalidated_at), neuer /auth/refresh Endpoint mit Token-Rotation, Login-Response erweitern um refresh_token, verifyTokenRBAC um Invalidierungs-Check erweitern, JWT-Laufzeit auf 15min reduzieren.

Purpose: Access-Tokens kurzlebig machen (15min statt 90 Tage) mit automatischem Refresh-Mechanismus — Security-Hardening ohne UX-Einbussen.
Output: Funktionierender Refresh-Endpoint, aktualisierte Auth-Middleware, DB-Schema.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/middleware/rbac.js
@backend/routes/auth.js
@frontend/src/services/tokenStore.ts
</context>

<interfaces>
<!-- Bestehende Interfaces die der Executor kennen muss -->

From backend/middleware/rbac.js:
```javascript
// verifyTokenRBAC(db) gibt Middleware-Funktion zurueck
// Aktuell: jwt.verify → DB-Query users → req.user setzen
// Muss erweitert werden um: token_invalidated_at Check nach DB-Query
const verifyTokenRBAC = (db) => {
  return async (req, res, next) => { ... };
};
```

From backend/routes/auth.js:
```javascript
// Login-Response aktuell:
res.json({ token, user: responseUser });
// Muss werden:
res.json({ token, refresh_token, user: responseUser });

// JWT aktuell: { expiresIn: '90d' }
// Muss werden: { expiresIn: '15m' }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration + verifyTokenRBAC Invalidierungs-Check</name>
  <files>backend/migrations/068_refresh_tokens.sql, backend/middleware/rbac.js</files>
  <read_first>
    - backend/middleware/rbac.js (verifyTokenRBAC Funktion, Zeilen 19-109)
    - backend/migrations/064_add_missing_indexes.sql (Migration-Format als Referenz)
  </read_first>
  <action>
    1. **Migration erstellen** (backend/migrations/068_refresh_tokens.sql) per D-04, D-07, D-20:
       ```sql
       -- refresh_tokens Tabelle
       CREATE TABLE IF NOT EXISTS refresh_tokens (
         id SERIAL PRIMARY KEY,
         user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
         token_hash VARCHAR(64) NOT NULL UNIQUE,
         expires_at TIMESTAMP NOT NULL,
         created_at TIMESTAMP DEFAULT NOW(),
         revoked_at TIMESTAMP
       );
       CREATE INDEX IF NOT EXISTS idx_refresh_tokens_user_id ON refresh_tokens(user_id);
       CREATE INDEX IF NOT EXISTS idx_refresh_tokens_token_hash ON refresh_tokens(token_hash);

       -- token_invalidated_at Spalte auf users
       ALTER TABLE users ADD COLUMN IF NOT EXISTS token_invalidated_at TIMESTAMP;
       ```
       SHA-256 Hash fuer token_hash (64 Zeichen hex), konsistent mit Phase 66.

    2. **verifyTokenRBAC erweitern** (backend/middleware/rbac.js) per D-08:
       - In der User-Query `token_invalidated_at` zum SELECT hinzufuegen (nach `u.is_super_admin`)
       - NACH dem `is_active` Check und VOR dem Organization-Check einfuegen:
         ```javascript
         // Soft-Revoke: Token vor Invalidierung ausgestellt → 401
         if (user.token_invalidated_at) {
           const tokenIssuedAt = decoded.iat; // Unix-Timestamp aus JWT
           const invalidatedAt = Math.floor(new Date(user.token_invalidated_at).getTime() / 1000);
           if (tokenIssuedAt < invalidatedAt) {
             return res.status(401).json({ error: 'Token invalidated' });
           }
         }
         ```
       - WICHTIG: `decoded.iat` ist automatisch in jedem JWT enthalten (Unix-Sekunden). Kein zusaetzliches Feld noetig.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS refresh_tokens" backend/migrations/068_refresh_tokens.sql && grep -q "token_invalidated_at" backend/middleware/rbac.js && grep -q "Token invalidated" backend/middleware/rbac.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "refresh_tokens" backend/migrations/068_refresh_tokens.sql findet CREATE TABLE + 2 Indizes
    - grep "token_invalidated_at" backend/middleware/rbac.js findet SELECT-Erweiterung + iat-Vergleich
    - grep "Token invalidated" backend/middleware/rbac.js findet 401-Response
    - grep "ON DELETE CASCADE" backend/migrations/068_refresh_tokens.sql findet FK-Constraint
  </acceptance_criteria>
  <done>Migration-Datei erstellt mit refresh_tokens Tabelle + token_invalidated_at Spalte. verifyTokenRBAC prueft iat gegen token_invalidated_at und gibt 401 bei invalidiertem Token.</done>
</task>

<task type="auto">
  <name>Task 2: /auth/refresh Endpoint + Login erweitern + JWT 15min</name>
  <files>backend/routes/auth.js</files>
  <read_first>
    - backend/routes/auth.js (Login-Endpoint Zeilen 85-157, register-konfi Zeilen 519-669)
  </read_first>
  <action>
    1. **Helper-Funktionen** am Anfang der module.exports-Funktion (nach generateResetToken) per D-03, D-04:
       ```javascript
       // Refresh-Token generieren (64 Bytes = 128 Hex-Zeichen)
       const generateRefreshToken = () => crypto.randomBytes(64).toString('hex');

       // SHA-256 Hash fuer DB-Speicherung (konsistent mit Phase 66)
       const hashToken = (token) => crypto.createHash('sha256').update(token).digest('hex');
       ```

    2. **Login-Endpoint aendern** (router.post('/login')) per D-01, D-02, D-18:
       - JWT expiresIn von `'90d'` auf `'15m'` aendern
       - Nach JWT-Signing, VOR responseUser-Erstellung:
         ```javascript
         // Refresh-Token erstellen und in DB speichern
         const refreshToken = generateRefreshToken();
         const refreshTokenHash = hashToken(refreshToken);
         const refreshExpiresAt = new Date(Date.now() + 90 * 24 * 60 * 60 * 1000); // 90 Tage
         await db.query(
           'INSERT INTO refresh_tokens (user_id, token_hash, expires_at) VALUES ($1, $2, $3)',
           [user.id, refreshTokenHash, refreshExpiresAt]
         );
         ```
       - Response aendern: `res.json({ token, refresh_token: refreshToken, user: responseUser });`

    3. **Register-Konfi Endpoint aendern** (router.post('/register-konfi')):
       - JWT expiresIn von `'90d'` auf `'15m'` aendern
       - Nach JWT-Signing, Refresh-Token erstellen + DB-Insert (gleicher Code wie Login)
       - Response um `refresh_token: refreshToken` erweitern

    4. **Neuer /auth/refresh Endpoint** per D-03, D-17:
       ```javascript
       router.post('/refresh', async (req, res) => {
         const { refresh_token } = req.body;
         if (!refresh_token) {
           return res.status(400).json({ error: 'Refresh-Token ist erforderlich' });
         }

         const tokenHash = hashToken(refresh_token);

         try {
           // Altes Token suchen (nicht revoked, nicht abgelaufen)
           const { rows: [existing] } = await db.query(
             'SELECT id, user_id, expires_at FROM refresh_tokens WHERE token_hash = $1 AND revoked_at IS NULL AND expires_at > NOW()',
             [tokenHash]
           );

           if (!existing) {
             return res.status(401).json({ error: 'Ungültiger oder abgelaufener Refresh-Token' });
           }

           // Altes Token sofort revoken (Rotation per D-03)
           await db.query('UPDATE refresh_tokens SET revoked_at = NOW() WHERE id = $1', [existing.id]);

           // User-Daten laden (gleiche Query wie Login)
           const { rows: [user] } = await db.query(`
             SELECT u.id, u.username, u.display_name, u.organization_id, u.email, u.role_id,
                    u.is_super_admin,
                    r.name as role_name,
                    kp.jahrgang_id, j.name as jahrgang_name,
                    kp.gottesdienst_points, kp.gemeinde_points
             FROM users u
             LEFT JOIN roles r ON u.role_id = r.id
             LEFT JOIN konfi_profiles kp ON u.id = kp.user_id
             LEFT JOIN jahrgaenge j ON kp.jahrgang_id = j.id
             WHERE u.id = $1
           `, [existing.user_id]);

           if (!user) {
             return res.status(401).json({ error: 'Benutzer nicht gefunden' });
           }

           const userType = user.role_name === 'konfi' ? 'konfi' : user.role_name === 'teamer' ? 'teamer' : 'admin';

           // Neuer Access-Token (15min per D-01)
           const newAccessToken = jwt.sign({
             id: user.id,
             type: userType,
             display_name: user.display_name,
             email: user.email,
             organization_id: user.organization_id,
             role_name: user.role_name,
             is_super_admin: user.is_super_admin || false
           }, JWT_SECRET, { expiresIn: '15m' });

           // Neuer Refresh-Token (Rotation per D-03)
           const newRefreshToken = generateRefreshToken();
           const newRefreshHash = hashToken(newRefreshToken);
           const newRefreshExpiry = new Date(Date.now() + 90 * 24 * 60 * 60 * 1000);
           await db.query(
             'INSERT INTO refresh_tokens (user_id, token_hash, expires_at) VALUES ($1, $2, $3)',
             [user.id, newRefreshHash, newRefreshExpiry]
           );

           res.json({ token: newAccessToken, refresh_token: newRefreshToken });
         } catch (err) {
           console.error('Database error in POST /api/auth/refresh:', err);
           res.status(500).json({ error: 'Fehler beim Token-Refresh' });
         }
       });
       ```

    5. **Cleanup-Job** (am Ende der module.exports, nach allen Routen):
       ```javascript
       // Abgelaufene + revoked Refresh-Tokens alle 24h aufräumen
       setInterval(async () => {
         try {
           const { rowCount } = await db.query(
             "DELETE FROM refresh_tokens WHERE expires_at < NOW() OR (revoked_at IS NOT NULL AND revoked_at < NOW() - INTERVAL '7 days')"
           );
           if (rowCount > 0) console.log(`Cleanup: ${rowCount} abgelaufene Refresh-Tokens entfernt`);
         } catch (err) {
           console.error('Refresh-Token Cleanup Fehler:', err);
         }
       }, 24 * 60 * 60 * 1000);
       ```
  </action>
  <verify>
    <automated>grep -q "router.post('/refresh'" backend/routes/auth.js && grep -q "refresh_token" backend/routes/auth.js && grep -q "expiresIn: '15m'" backend/routes/auth.js && grep -c "expiresIn: '90d'" backend/routes/auth.js | grep -q "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "router.post('/refresh'" backend/routes/auth.js findet den neuen Endpoint
    - grep "expiresIn: '15m'" backend/routes/auth.js findet mindestens 3 Treffer (Login, Register, Refresh)
    - grep "expiresIn: '90d'" backend/routes/auth.js findet 0 Treffer (alle ersetzt)
    - grep "refresh_token: refreshToken" backend/routes/auth.js findet Login + Register Response
    - grep "hashToken" backend/routes/auth.js findet SHA-256 Hash-Funktion
    - grep "revoked_at" backend/routes/auth.js findet Token-Rotation-Logik
    - grep "DELETE FROM refresh_tokens" backend/routes/auth.js findet Cleanup-Job
  </acceptance_criteria>
  <done>Login + Register geben refresh_token zurueck. JWT-Laufzeit ist 15min. /auth/refresh Endpoint rotiert Tokens. Cleanup-Job raeumt abgelaufene Tokens auf.</done>
</task>

</tasks>

<verification>
- Migration-Datei enthält refresh_tokens Tabelle + token_invalidated_at Spalte
- Kein `expiresIn: '90d'` mehr in auth.js (alle auf 15m)
- /auth/refresh Endpoint existiert mit Token-Rotation
- verifyTokenRBAC prueft token_invalidated_at
- SHA-256 Hash fuer Refresh-Token-Speicherung (nicht Klartext)
</verification>

<success_criteria>
Backend-seitig vollstaendiges Refresh-Token-System: DB-Schema, Refresh-Endpoint mit Rotation, 15min Access-Tokens, Soft-Revoke via token_invalidated_at. Frontend-Integration folgt in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/68-token-refresh-system/68-01-SUMMARY.md`
</output>
