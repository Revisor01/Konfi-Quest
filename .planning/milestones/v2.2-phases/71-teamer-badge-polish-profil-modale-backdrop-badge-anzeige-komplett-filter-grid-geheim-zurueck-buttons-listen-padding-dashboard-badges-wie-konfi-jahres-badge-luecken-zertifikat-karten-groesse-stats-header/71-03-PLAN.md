---
phase: 71-teamer-badge-polish
plan: 03
type: execute
wave: 2
depends_on: [71-01, 71-02]
files_modified:
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  - frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  - frontend/src/components/teamer/views/TeamerBadgesView.tsx
  - frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05, POLISH-06, POLISH-07, POLISH-09]

must_haves:
  truths:
    - "Teamer Profil-Modale (Email, Passwort, RoleTitle) haben iOS Backdrop"
    - "TeamerBadgesPage zeigt ALLE Teamer-Badges inkl. teamer_year Kategorie"
    - "Geheime Badges die nicht verdient sind werden als graue Kreise mit Fragezeichen angezeigt"
    - "Geheime Badges die verdient sind werden normal angezeigt mit GEHEIM Corner-Badge"
    - "TeamerBadgesPage und TeamerKonfiStatsPage haben Zurueck-Button"
    - "TeamerProfilePage Punkte-Stats nutzen SectionHeader mit app-stats-row"
    - "TeamerDashboardPage Badge-Sektion zeigt graue Kreise fuer nicht-verdiente Badges"
    - "TeamerDashboardPage Zertifikat-Karten nutzen app-cert-card Klasse"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerProfilePage.tsx"
      provides: "Modal presentingElement + Stats-Header"
    - path: "frontend/src/components/teamer/pages/TeamerBadgesPage.tsx"
      provides: "teamer_year Kategorie + geheime Badges + Segment sichtbar/geheim"
    - path: "frontend/src/components/teamer/views/TeamerBadgesView.tsx"
      provides: "Geheime Badges als graue Kreise + Badge-Grid CSS-Klassen"
    - path: "frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx"
      provides: "Zurueck-Button im Header"
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "Badge-Sektion mit grauen Kreisen + Zertifikat-Karten min-height"
  key_links:
    - from: "TeamerBadgesPage"
      to: "GET /teamer/badges"
      via: "useOfflineQuery"
      pattern: "api\\.get.*teamer/badges"
    - from: "TeamerDashboardPage"
      to: "GET /teamer/dashboard"
      via: "useOfflineQuery"
      pattern: "api\\.get.*teamer/dashboard"
---

<objective>
Frontend-Komponenten-Fixes fuer alle 8 verbleibenden UI-Issues.

Purpose: Issues 1 (Backdrop), 2 (Zurueck-Button Badges), 3 (Badge-Filter Frontend-Fix), 4 (Zurueck-Button History), 5 (Badge-Grid CSS anwenden), 6 (Geheime Badges), 7 (Stats-Header), 9 (Dashboard Badges) beheben.
Output: Alle Teamer-Pages mit korrekter Badge-Darstellung, Navigation und Styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-teamer-badge-polish-profil-modale-backdrop-badge-anzeige-komplett-filter-grid-geheim-zurueck-buttons-listen-padding-dashboard-badges-wie-konfi-jahres-badge-luecken-zertifikat-karten-groesse-stats-header/71-CONTEXT.md
@.planning/phases/71-teamer-badge-polish-profil-modale-backdrop-badge-anzeige-komplett-filter-grid-geheim-zurueck-buttons-listen-padding-dashboard-badges-wie-konfi-jahres-badge-luecken-zertifikat-karten-groesse-stats-header/71-01-SUMMARY.md
@.planning/phases/71-teamer-badge-polish-profil-modale-backdrop-badge-anzeige-komplett-filter-grid-geheim-zurueck-buttons-listen-padding-dashboard-badges-wie-konfi-jahres-badge-luecken-zertifikat-karten-groesse-stats-header/71-02-SUMMARY.md

<interfaces>
<!-- TeamerProfilePage.tsx: -->
<!-- Modals werden mit useIonModal + presentingElement praesentiert (Zeile 83-121) -->
<!-- presentingElement kommt von useModalPage('profile') Hook (Zeile 70) -->
<!-- ALLE 3 Modale nutzen bereits presentingElement — KEIN BUG hier! -->
<!-- Aber pruefe ob presentingElement korrekt an alle 3 Modal-Aufrufe uebergeben wird -->

<!-- TeamerBadgesPage.tsx getCategories() (Zeile 311-343): -->
<!-- FEHLENDE KATEGORIE: teamer_year ist NICHT in der Liste! -->
<!-- Badges mit criteria_type='teamer_year' fallen durch und werden NICHT angezeigt -->
<!-- FIX: teamer_year Kategorie hinzufuegen: -->
<!-- { key: 'teamer_year', title: 'Erfahrung', icon: ribbon, color: '#5b21b6', badges: ... } -->

<!-- TeamerBadgesView.tsx: Hat bereits is_hidden Handling im getFilteredBadges -->
<!-- Aber kein Segment fuer sichtbar/geheim -->

<!-- TeamerDashboardPage.tsx Badge-Sektion (Zeile 600-688): -->
<!-- Zeigt nur verdiente Badges als goldene Kreise -->
<!-- FEHLT: Graue Kreise fuer nicht-verdiente Badges (wie Konfi-Dashboard) -->
<!-- Referenz: KonfiDashboardPage/DashboardView Badge-Sektion -->

<!-- CSS-Klassen aus Plan 02: -->
<!-- .app-badge-grid, .app-badge-grid__item, .app-badge-grid__name -->
<!-- .app-cert-card (min-height: 120px) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeamerProfilePage Backdrop-Fix und Stats-Header (per D-01, D-02, D-14, D-15)</name>
  <files>frontend/src/components/teamer/pages/TeamerProfilePage.tsx</files>
  <read_first>frontend/src/components/teamer/pages/TeamerProfilePage.tsx (komplett)</read_first>
  <action>
Issue 1 (Backdrop): Die TeamerProfilePage nutzt bereits `presentingElement` korrekt bei allen 3 Modal-Aufrufen (Zeile 255, 282, 309). Pruefe ob `useModalPage('profile')` korrekt das presentingElement setzt. Falls presentingElement undefined ist, fehlt das iOS Backdrop.

PRUEFE: Ist das `pageRef` korrekt an `IonPage ref={pageRef}` gebunden (Zeile 185)? JA — das ist korrekt. Der Bug koennte sein, dass das pageRef beim ersten Render noch null ist. Pruefe ob `presentingElement || undefined` als Fallback ausreicht oder ob der Modal-Aufruf verzoegert werden muss.

Falls der Backdrop trotz korrektem Code fehlt: Der `useModalPage` Hook koennte das Element erst nach dem ersten Render setzen. In dem Fall ist das kein Code-Bug sondern ein Timing-Issue. Lasse den Code wie er ist — er ist korrekt implementiert.

Issue 7 (Stats-Header): Der TeamerProfilePage hat aktuell KEINEN Punkte-Stats-Bereich. Per D-14, D-15 soll ein SectionHeader mit Punkte-Uebersicht hinzugefuegt werden.

Fuege zwischen dem Detail-Header (Abschnitt A, Zeile 205-239) und den Konto-Einstellungen (Abschnitt B, Zeile 241) eine Stats-Sektion ein:

```tsx
{/* Punkte-Statistiken */}
{profile.konfi_data?.jahrgang_name && (
  <div className="app-stats-section" style={{ padding: '0 16px', marginTop: '-8px' }}>
    <div className="app-stats-row" style={{
      display: 'flex',
      gap: '8px',
      justifyContent: 'center'
    }}>
      <div className="app-stats-row__item">
        <span className="app-stats-row__value" style={{ color: '#e11d48' }}>
          {(profile.konfi_data.gottesdienst_points || 0) + (profile.konfi_data.gemeinde_points || 0)}
        </span>
        <span className="app-stats-row__label">Gesamt</span>
      </div>
      <div className="app-stats-row__item">
        <span className="app-stats-row__value">{profile.konfi_data.gottesdienst_points || 0}</span>
        <span className="app-stats-row__label">Gottesdienst</span>
      </div>
      <div className="app-stats-row__item">
        <span className="app-stats-row__value">{profile.konfi_data.gemeinde_points || 0}</span>
        <span className="app-stats-row__label">Gemeinde</span>
      </div>
    </div>
  </div>
)}
```

Nutze die bestehenden app-stats-row CSS-Klassen. Nur anzeigen wenn konfi_data vorhanden (Teamer war vorher Konfi).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "app-stats-row" frontend/src/components/teamer/pages/TeamerProfilePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TeamerProfilePage zeigt Punkte-Statistiken mit app-stats-row Klassen
    - Stats nur sichtbar wenn profile.konfi_data.jahrgang_name vorhanden
    - Alle 3 Modal-Aufrufe nutzen presentingElement (bereits korrekt)
  </acceptance_criteria>
  <done>Stats-Header mit Punkte-Uebersicht in TeamerProfilePage, Backdrop-Situation verifiziert</done>
</task>

<task type="auto">
  <name>Task 2: TeamerBadgesPage teamer_year Fix, geheime Badges, Zurueck-Buttons (per D-03, D-04, D-05, D-06, D-07, D-11, D-12, D-13)</name>
  <files>frontend/src/components/teamer/pages/TeamerBadgesPage.tsx, frontend/src/components/teamer/views/TeamerBadgesView.tsx, frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx</files>
  <read_first>
    frontend/src/components/teamer/pages/TeamerBadgesPage.tsx (Zeile 311-343 getCategories, Zeile 366-375 Header)
    frontend/src/components/teamer/views/TeamerBadgesView.tsx (Zeile 282-295 Filter)
    frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx (Zeile 1-50 Header)
  </read_first>
  <action>
KERN-BUG-FIX (Issue 3, per D-05, D-06): In TeamerBadgesPage.tsx `getCategories()` (Zeile 324-339) fehlt `teamer_year` als Kategorie. Dadurch werden Badges mit criteria_type='teamer_year' NICHT angezeigt.

1. **TeamerBadgesPage.tsx — teamer_year Kategorie hinzufuegen:**
   In getCategories() (Zeile 324-339) fuege VOR dem `collection`-Eintrag hinzu:
   ```typescript
   { key: 'teamer_year', title: 'Erfahrung', icon: ribbon, color: '#5b21b6',
     badges: filtered.filter(b => b.criteria_type === 'teamer_year').sort((a, b) => a.criteria_value - b.criteria_value) },
   ```
   WICHTIG: `ribbon` muss bereits importiert sein (ist es, Zeile 29).

2. **TeamerBadgesPage.tsx — Geheime Badges (per D-11, D-12, D-13):**
   Die Page hat bereits `isSecret` Handling (Zeile 508). Fuege ein Segment hinzu fuer sichtbar/geheim:

   a. Fuege State hinzu: `const [badgeVisibility, setBadgeVisibility] = useState<string>('sichtbar');`

   b. Pruefe ob es geheime Badges gibt: `const hasSecretBadges = badges.some(b => b.is_hidden);`

   c. Fuege UNTER dem bestehenden Filter-Segment (Zeile 427-433) ein zweites Segment hinzu, NUR wenn hasSecretBadges:
   ```tsx
   {hasSecretBadges && (
     <div style={{ margin: '0 16px 16px' }}>
       <IonSegment value={badgeVisibility} onIonChange={(e) => setBadgeVisibility(e.detail.value as string)}>
         <IonSegmentButton value="sichtbar"><IonLabel>Sichtbar</IonLabel></IonSegmentButton>
         <IonSegmentButton value="geheim"><IonLabel>Geheim</IonLabel></IonSegmentButton>
       </IonSegment>
     </div>
   )}
   ```

   d. Passe getCategories() an: Filtere zusaetzlich nach badgeVisibility:
   - 'sichtbar': nur Badges wo `!b.is_hidden || b.earned` (sichtbare + verdiente geheime)
   - 'geheim': nur Badges wo `b.is_hidden` (alle geheimen, verdient und unverdient)

3. **TeamerBadgesPage.tsx — Zurueck-Button (per D-03, D-04):**
   Der Header (Zeile 368-375) hat bereits einen IonBackButton (Zeile 371). Pruefe ob er funktioniert. Falls ja — kein Fix noetig.

4. **TeamerKonfiStatsPage.tsx — Zurueck-Button (per D-07):**
   Pruefe ob ein Zurueck-Button im Header vorhanden ist. Falls nicht, fuege hinzu:
   ```tsx
   <IonButtons slot="start">
     <IonBackButton defaultHref="/teamer/profile" />
   </IonButtons>
   ```

5. **TeamerBadgesView.tsx — Geheime Badges (per D-11, D-12, D-13):**
   Die TeamerBadgesView (im Dashboard eingebettet) zeigt aktuell alle Badges. Fuege geheime Badge Darstellung hinzu:
   - Nicht-verdiente geheime Badges: Grauer Kreis mit `helpCircle` Icon und "???" als Name
   - Verdiente geheime Badges: Normal anzeigen mit "GEHEIM" Corner-Badge (ist bereits implementiert in TeamerBadgesPage, Zeile 627-641 — uebernehme das Pattern)
   - Fuege `helpCircle` zum Import hinzu falls fehlend

6. **Badge-Grid CSS-Klassen anwenden:**
   In beiden Dateien (TeamerBadgesPage und TeamerBadgesView): Ersetze das inline `style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px' }}` durch `className="app-badge-grid"`.
   Fuege `className="app-badge-grid__item"` auf die Badge-Card divs hinzu.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "teamer_year" frontend/src/components/teamer/pages/TeamerBadgesPage.tsx && grep -c "app-badge-grid" frontend/src/components/teamer/pages/TeamerBadgesPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TeamerBadgesPage getCategories() enthaelt teamer_year Kategorie mit title 'Erfahrung'
    - TeamerBadgesPage zeigt Sichtbar/Geheim Segment wenn geheime Badges existieren
    - Geheime nicht-verdiente Badges werden als graue Kreise mit "???" angezeigt
    - Geheime verdiente Badges werden normal mit "GEHEIM" Corner-Badge angezeigt
    - TeamerKonfiStatsPage hat IonBackButton mit defaultHref="/teamer/profile"
    - Badge-Grid nutzt className="app-badge-grid" statt inline styles
  </acceptance_criteria>
  <done>Alle 6 Badges sichtbar, geheime Badges korrekt dargestellt, Zurueck-Buttons vorhanden</done>
</task>

<task type="auto">
  <name>Task 3: TeamerDashboardPage Badge-Sektion und Zertifikat-Karten (per D-18, D-19, D-20, D-24, D-25)</name>
  <files>frontend/src/components/teamer/pages/TeamerDashboardPage.tsx</files>
  <read_first>
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx (Zeile 600-688 Badge-Sektion, Zeile 329-435 Zertifikat-Sektion)
    frontend/src/components/konfi/views/DashboardView.tsx (Badge-Sektion als Referenz)
  </read_first>
  <action>
Issue 9 (Dashboard Badges, per D-18, D-19, D-20): Die Badge-Sektion (Zeile 600-688) zeigt nur verdiente Badges als goldene Kreise. Es fehlen graue Kreise fuer nicht-verdiente Badges.

1. **Badge-Sektion erweitern:**
   Das Backend GET /teamer/dashboard liefert `badges: { recent: Badge[], earned_count, total_count }`. Die `recent` Liste enthaelt nur verdiente Badges.

   PROBLEM: Das Backend liefert keine nicht-verdiente Badges fuer das Dashboard. Wir muessen mit den vorhandenen Daten arbeiten.

   LOESUNG: Zeige die vorhandenen verdiente Badges als farbige Kreise, und fuelle mit grauen Kreisen bis total_count erreicht ist.

   Ersetze die Badge-Icons Grid Sektion (Zeile 633-667) durch:
   ```tsx
   {/* Badge Icons Grid — verdiente + graue Platzhalter */}
   <div style={{
     display: 'flex',
     flexWrap: 'wrap',
     gap: '10px',
     justifyContent: 'center',
     marginBottom: '16px'
   }}>
     {/* Verdiente Badges */}
     {dashboardData.badges.recent.map((badge) => (
       <div key={badge.id} style={{
         width: '44px', height: '44px', borderRadius: '50%',
         background: 'linear-gradient(135deg, #f59e0b 0%, #d97706 100%)',
         display: 'flex', alignItems: 'center', justifyContent: 'center',
         boxShadow: '0 4px 12px rgba(245, 158, 11, 0.4)',
         border: '2px solid rgba(255, 255, 255, 0.3)'
       }}>
         <IonIcon icon={getIconFromString(badge.icon)} style={{ fontSize: '1.2rem', color: 'white' }} />
       </div>
     ))}
     {/* Graue Platzhalter fuer nicht-verdiente */}
     {Array.from({ length: Math.min(dashboardData.badges.total_count - dashboardData.badges.earned_count, 12) }).map((_, i) => (
       <div key={`placeholder-${i}`} style={{
         width: '44px', height: '44px', borderRadius: '50%',
         background: 'rgba(255, 255, 255, 0.1)',
         display: 'flex', alignItems: 'center', justifyContent: 'center',
         border: '2px dashed rgba(255, 255, 255, 0.2)'
       }}>
         <IonIcon icon={helpCircle} style={{ fontSize: '1.2rem', color: 'rgba(255,255,255,0.3)' }} />
       </div>
     ))}
   </div>
   ```

   Importiere `helpCircle` in die Imports (falls noch nicht vorhanden).
   Begrenze graue Platzhalter auf max. 12 damit das Grid nicht ueberlaeuft.

2. **Badge-Sektion auch bei 0 earned anzeigen (per D-18):**
   Entferne die Bedingung `dashboardData.badges.earned_count === 0` die den "Noch keine Badges" Text zeigt (Zeile 608-610). Stattdessen: Zeige immer das Grid, auch wenn nur graue Kreise.

   Falls total_count === 0 (keine Badges erstellt): Dann den "Noch keine Badges" Text beibehalten.

3. **Zertifikat-Karten CSS-Klasse anwenden (per D-24, D-25):**
   In der Zertifikat-Sektion (Zeile 355-432): Fuege `className="app-cert-card"` auf jede Zertifikat-Karte hinzu (das div mit key={cert.id}, Zeile 360).
   Die bestehenden inline-Styles koennen bleiben — die CSS-Klasse ueberschreibt nur min-height und display.

4. **"Alle Badges" Link korrigieren:**
   Zeile 673: `history.push('/teamer/profil')` — sollte `/teamer/profile/badges` sein (direkt zur Badge-Seite).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "placeholder\|helpCircle\|app-cert-card" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TeamerDashboardPage Badge-Sektion zeigt graue Kreise fuer nicht-verdiente Badges
    - Graue Platzhalter auf max. 12 begrenzt
    - Badge-Sektion wird auch bei earned_count=0 angezeigt (solange total_count > 0)
    - Zertifikat-Karten haben className="app-cert-card"
    - "Alle Badges" Link fuehrt zu /teamer/profile/badges
    - helpCircle ist importiert
  </acceptance_criteria>
  <done>Dashboard zeigt graue Badge-Platzhalter, Zertifikat-Karten haben gleiche Mindesthoehe, Links korrigiert</done>
</task>

</tasks>

<verification>
- Frontend baut fehlerfrei: `cd frontend && npx tsc --noEmit`
- TeamerBadgesPage zeigt teamer_year Kategorie: `grep "teamer_year" frontend/src/components/teamer/pages/TeamerBadgesPage.tsx`
- Geheime Badges Segment vorhanden: `grep "geheim\|sichtbar" frontend/src/components/teamer/pages/TeamerBadgesPage.tsx`
- Dashboard graue Kreise: `grep "placeholder" frontend/src/components/teamer/pages/TeamerDashboardPage.tsx`
- Alle Zurueck-Buttons: `grep "IonBackButton" frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx`
</verification>

<success_criteria>
- Alle 11 Issues aus dem Testing behoben
- Frontend kompiliert fehlerfrei
- Badge-Filter zeigt alle 6 Badges (teamer_year Kategorie inkludiert)
- Geheime Badges korrekt als graue Kreise / normal dargestellt
- Navigation (Zurueck-Buttons) konsistent
- Dashboard Badges mit Platzhaltern, Zertifikat-Karten gleich gross
</success_criteria>

<output>
After completion, create `.planning/phases/71-teamer-badge-polish-profil-modale-backdrop-badge-anzeige-komplett-filter-grid-geheim-zurueck-buttons-listen-padding-dashboard-badges-wie-konfi-jahres-badge-luecken-zertifikat-karten-groesse-stats-header/71-03-SUMMARY.md`
</output>
