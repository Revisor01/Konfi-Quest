---
phase: 67-performance-optimierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/views/KonfiDetailView.tsx
  - frontend/src/components/admin/views/KonfiDetailSections.tsx
  - frontend/src/components/admin/views/EventDetailView.tsx
  - frontend/src/components/admin/views/EventDetailSections.tsx
  - frontend/src/components/admin/modals/EventModal.tsx
  - frontend/src/components/admin/modals/EventFormSections.tsx
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]

must_haves:
  truths:
    - "KonfiDetailView ist unter 600 Zeilen, Sektionen sind in KonfiDetailSections.tsx"
    - "EventDetailView ist unter 600 Zeilen, Sektionen sind in EventDetailSections.tsx"
    - "EventModal ist unter 600 Zeilen, Formular-Sektionen in EventFormSections.tsx"
    - "Alle drei Views funktionieren identisch wie vorher (keine Verhaltensaenderung)"
  artifacts:
    - path: "frontend/src/components/admin/views/KonfiDetailSections.tsx"
      provides: "Extrahierte Sektionen: BonusSection, ActivitiesSection, EventPointsSection, CertificatesSection, HistorySection"
    - path: "frontend/src/components/admin/views/EventDetailSections.tsx"
      provides: "Extrahierte Sektionen: EventInfoCard, ParticipantsList, TimeslotsList, MaterialSection"
    - path: "frontend/src/components/admin/modals/EventFormSections.tsx"
      provides: "Extrahierte Sektionen: BasicInfoSection, TimeslotSection, VisibilitySection, RegistrationSection"
  key_links:
    - from: "frontend/src/components/admin/views/KonfiDetailView.tsx"
      to: "frontend/src/components/admin/views/KonfiDetailSections.tsx"
      via: "named imports"
      pattern: "import.*from.*KonfiDetailSections"
    - from: "frontend/src/components/admin/views/EventDetailView.tsx"
      to: "frontend/src/components/admin/views/EventDetailSections.tsx"
      via: "named imports"
      pattern: "import.*from.*EventDetailSections"
    - from: "frontend/src/components/admin/modals/EventModal.tsx"
      to: "frontend/src/components/admin/modals/EventFormSections.tsx"
      via: "named imports"
      pattern: "import.*from.*EventFormSections"
---

<objective>
Die drei groessten Admin-Komponenten (KonfiDetailView 1539z, EventDetailView 1432z, EventModal 1402z) in Haupt-Datei + Sektionen-Datei aufteilen.

Purpose: Mega-Dateien sind schwer wartbar und erzeugen hohen Kontextverbrauch beim Editieren. Splitting in logische Sektionen verbessert Wartbarkeit und reduziert Merge-Konflikte.
Output: 3 bestehende Dateien verkleinert, 3 neue Sektionen-Dateien erstellt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/admin/views/KonfiDetailView.tsx
@frontend/src/components/admin/views/EventDetailView.tsx
@frontend/src/components/admin/modals/EventModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: KonfiDetailView in Haupt-Datei + KonfiDetailSections aufteilen</name>
  <files>frontend/src/components/admin/views/KonfiDetailView.tsx, frontend/src/components/admin/views/KonfiDetailSections.tsx</files>
  <read_first>
    - frontend/src/components/admin/views/KonfiDetailView.tsx (vollstaendig lesen)
  </read_first>
  <action>
KonfiDetailView.tsx (1539 Zeilen) in zwei Dateien aufteilen:

1. **KonfiDetailSections.tsx** erstellen — extrahiere diese JSX-Sektionen als eigene Komponenten:
   - `KonfiHeaderCard` — Der grosse gradient Header mit Activity Rings und Punkte-Anzeige (ca. Zeile 609-770)
   - `BonusSection` — Die IonList mit Bonuspunkten und Sliding-Items (ca. Zeile 772-870)
   - `ActivitiesSection` — Die IonList mit Aktivitaeten und Sliding-Items (ca. Zeile ~870-1070)
   - `EventPointsSection` — Die IonList mit Event-Punkten (ca. Zeile ~1070-1200)
   - `CertificatesSection` — Die Zertifikate-Sektion (falls vorhanden, nur fuer Teamer)
   - `KonfiHistorySection` — Die Konfi-History-Sektion am Ende

   Jede Sub-Komponente bekommt Props fuer die Daten die sie braucht (z.B. `bonusEntries`, `formatDate`, `handleDeleteBonus` etc.) und wird mit `React.memo` gewrapped.

2. **KonfiDetailView.tsx** behalten — State, Effects, Handler-Funktionen, Modal-Hooks und das aeussere Layout (IonPage, IonHeader, IonContent) bleiben. Die Sektionen werden ueber `import { BonusSection, ActivitiesSection, ... } from './KonfiDetailSections'` eingebunden.

   Die lokalen Interfaces (Konfi, Activity etc.) in KonfiDetailView behalten — kein separater Types-File noetig.

WICHTIG: Keine funktionalen Aenderungen! Rein strukturelles Refactoring. Die PhotoModal-Inline-Komponente bleibt in KonfiDetailView (sie nutzt useIonModal-Hook).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - KonfiDetailView.tsx hat weniger als 650 Zeilen (grep -c '' KonfiDetailView.tsx)
    - KonfiDetailSections.tsx existiert und exportiert mindestens 4 Komponenten (grep -c "export const" KonfiDetailSections.tsx)
    - KonfiDetailView.tsx importiert aus KonfiDetailSections (grep "KonfiDetailSections" KonfiDetailView.tsx)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>KonfiDetailView unter 650 Zeilen, alle Sektionen in KonfiDetailSections.tsx, TypeScript fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: EventDetailView + EventModal in Haupt-Dateien + Sektionen aufteilen</name>
  <files>frontend/src/components/admin/views/EventDetailView.tsx, frontend/src/components/admin/views/EventDetailSections.tsx, frontend/src/components/admin/modals/EventModal.tsx, frontend/src/components/admin/modals/EventFormSections.tsx</files>
  <read_first>
    - frontend/src/components/admin/views/EventDetailView.tsx (vollstaendig lesen)
    - frontend/src/components/admin/modals/EventModal.tsx (vollstaendig lesen)
  </read_first>
  <action>
Gleiche Splitting-Strategie wie Task 1, fuer zwei weitere Mega-Dateien:

**EventDetailView.tsx (1432 Zeilen) aufteilen:**

1. **EventDetailSections.tsx** erstellen mit:
   - `EventInfoCard` — Die grosse gradient-Header-Card mit Event-Infos (Datum, Ort, Punkte, Status)
   - `ParticipantsList` — Die Teilnehmer-Liste mit Status-Icons und Sliding-Actions
   - `TimeslotsList` — Die Timeslot-Uebersicht mit Buchungen pro Slot
   - `EventMaterialSection` — Die Material-Sektion (falls Material vorhanden)
   - `UnregistrationsList` — Die Abmeldungen-Liste

   Jede Komponente mit React.memo wrappen. Props fuer Daten + Handler-Callbacks durchreichen.

2. **EventDetailView.tsx** behalten — State, Effects, Handler, Modal-Hooks, aeusseres Layout.

**EventModal.tsx (1402 Zeilen) aufteilen:**

1. **EventFormSections.tsx** erstellen mit:
   - `BasicInfoSection` — Name, Beschreibung, Kategorie, Punkte, Ort
   - `DateTimeSection` — Datum, Uhrzeit, Ende
   - `TimeslotFormSection` — Timeslot-Verwaltung (hinzufuegen, entfernen, bearbeiten)
   - `RegistrationSection` — Max-Teilnehmer, Warteliste, Registrierungszeitraum
   - `VisibilitySection` — Sichtbarkeit, Pflicht-Event, Jahrgang-Filter

2. **EventModal.tsx** behalten — formData State, submit-Handler, Validierung, aeusseres Modal-Layout.

WICHTIG: Keine funktionalen Aenderungen! Die IonDatetime und IonModal-Inline-Elemente in EventModal koennen im Haupt-File bleiben (sie sind an lokale Refs gebunden).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - EventDetailView.tsx hat weniger als 650 Zeilen (grep -c '' EventDetailView.tsx)
    - EventDetailSections.tsx existiert und exportiert mindestens 3 Komponenten (grep -c "export const" EventDetailSections.tsx)
    - EventModal.tsx hat weniger als 700 Zeilen (grep -c '' EventModal.tsx)
    - EventFormSections.tsx existiert und exportiert mindestens 3 Komponenten (grep -c "export const" EventFormSections.tsx)
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>EventDetailView unter 650z, EventModal unter 700z, Sektionen-Dateien erstellt, TypeScript fehlerfrei</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — keine TypeScript-Fehler
- Alle 6 Dateien existieren und sind nicht leer
- Keine der 3 Haupt-Dateien ueber 700 Zeilen
- Keine `any`-Casts in den neuen Sektionen-Dateien (die vorhandenen as-any in den Originalen duerfen bestehen bleiben)
</verification>

<success_criteria>
3 Mega-Komponenten (>1400 Zeilen) in je Haupt-Datei (<650z) + Sektionen-Datei aufgeteilt. TypeScript fehlerfrei. Keine funktionalen Aenderungen.
</success_criteria>

<output>
After completion, create `.planning/phases/67-performance-optimierung/67-01-SUMMARY.md`
</output>
