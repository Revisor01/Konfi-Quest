---
phase: 27-badge-count-single-source-of-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/contexts/BadgeContext.tsx
  - frontend/src/contexts/AppContext.tsx
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/components/chat/ChatOverview.tsx
  - frontend/src/components/chat/ChatRoom.tsx
autonomous: true
requirements:
  - BDG-01
  - BDG-03
  - BDG-04

must_haves:
  truths:
    - "BadgeContext ist die einzige Quelle fuer alle Badge-Counts (Chat, Antraege, Events)"
    - "MainTabs hat kein eigenes Polling mehr, liest nur aus useBadge()"
    - "AppContext hat keine chatNotifications mehr"
    - "ChatOverview zeigt per-Raum Unread-Counts aus BadgeContext"
    - "TabBar-Badges zeigen korrekte Werte aus BadgeContext"
  artifacts:
    - path: "frontend/src/contexts/BadgeContext.tsx"
      provides: "Zentraler Badge-Count Service mit chatUnreadByRoom, pendingRequestsCount, pendingEventsCount"
      exports: ["BadgeProvider", "useBadge"]
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Schlanke Tab-Navigation ohne eigenes Polling"
    - path: "frontend/src/contexts/AppContext.tsx"
      provides: "AppContext ohne chatNotifications State"
  key_links:
    - from: "frontend/src/contexts/BadgeContext.tsx"
      to: "/chat/rooms, /admin/activities/requests, /events"
      via: "refreshAllCounts mit Promise.all"
      pattern: "Promise\\.all"
    - from: "frontend/src/components/layout/MainTabs.tsx"
      to: "frontend/src/contexts/BadgeContext.tsx"
      via: "useBadge() Hook"
      pattern: "useBadge\\(\\)"
    - from: "frontend/src/components/chat/ChatOverview.tsx"
      to: "frontend/src/contexts/BadgeContext.tsx"
      via: "chatUnreadByRoom aus useBadge()"
      pattern: "chatUnreadByRoom"
---

<objective>
BadgeContext als Single Source of Truth fuer alle Badge-Counts aufbauen und alle anderen Systeme (AppContext chatNotifications, MainTabs Polling) konsolidieren.

Purpose: 4 unabhaengige Badge-Count-Systeme werden zu 1 — konsistente Zahlen ueberall, keine doppelten API-Calls.
Output: Erweiterter BadgeContext, bereinigte AppContext und MainTabs, ChatOverview mit BadgeContext-Integration.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-badge-count-single-source-of-truth/27-CONTEXT.md
@.planning/phases/27-badge-count-single-source-of-truth/27-RESEARCH.md

<interfaces>
<!-- Aktueller BadgeContext (wird erweitert) -->
From frontend/src/contexts/BadgeContext.tsx:
```typescript
interface BadgeContextType {
  badgeCount: number;
  setBadgeCount: React.Dispatch<React.SetStateAction<number>>;
  incrementBadge: () => void;
  decrementBadge: (amount?: number) => void;
  resetBadge: () => void;
  refreshFromAPI: () => Promise<void>;
}
```

<!-- BadgeProvider ist INNERHALB AppProvider (App.tsx:208-214) -->
<!-- Das bedeutet useApp() ist im BadgeContext verfuegbar -->

From frontend/src/App.tsx:
```typescript
<AppProvider>
  <BadgeProvider>
    ...
  </BadgeProvider>
</AppProvider>
```

<!-- AppContext chatNotifications (wird entfernt) -->
From frontend/src/contexts/AppContext.tsx (relevante Zeilen):
```typescript
chatNotifications: ChatNotifications;        // Zeile 85
chatNotificationsLoading: boolean;           // Zeile 86
refreshChatNotifications: () => Promise<void>; // Zeile 92
markChatRoomAsRead: (roomId: number) => void;  // Zeile 93
addUnreadChatMessage: (roomId: number, count?: number) => void; // Zeile 94
```

<!-- Consumer von AppContext chatNotifications -->
- ChatRoom.tsx:46 importiert markChatRoomAsRead aus useApp()
- ChatRoom.tsx:311 ruft markChatRoomAsRead(room.id) auf

<!-- MainTabs Polling (wird entfernt, nach BadgeContext verschoben) -->
From frontend/src/components/layout/MainTabs.tsx:
```typescript
const [pendingRequestsCount, setPendingRequestsCount] = useState(0); // Zeile 64
const [pendingEventsCount, setPendingEventsCount] = useState(0);     // Zeile 65
const [newBadgesCount, setNewBadgesCount] = useState(0);             // Zeile 66
// useEffect mit 30s Polling fuer requests (Zeilen 70-99)
// useEffect mit 60s Polling fuer events (Zeilen 102-132)
// useEffect mit 60s Polling fuer badges (Zeilen 135-154) -- BLEIBT (deferred)
```

<!-- ChatOverview Badge-Nutzung -->
From frontend/src/components/chat/ChatOverview.tsx:
```typescript
const { refreshFromAPI, badgeCount, setBadgeCount } = useBadge(); // Zeile 63
setBadgeCount(totalUnread);  // Zeile 121 -- setzt Badge direkt
room.unread_count            // Zeilen 328, 451, 460, 468 -- aus API Response
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BadgeContext erweitern und AppContext/MainTabs bereinigen</name>
  <files>
    frontend/src/contexts/BadgeContext.tsx,
    frontend/src/contexts/AppContext.tsx,
    frontend/src/components/layout/MainTabs.tsx,
    frontend/src/components/chat/ChatRoom.tsx
  </files>
  <action>
    **BadgeContext.tsx komplett umbauen:**

    1. `useApp()` importieren fuer User-Info (BadgeProvider ist innerhalb AppProvider, also verfuegbar)
    2. Neues Interface:
       ```typescript
       interface BadgeContextType {
         // Chat
         chatUnreadByRoom: Record<number, number>;
         chatUnreadTotal: number;
         // Admin-only
         pendingRequestsCount: number;
         pendingEventsCount: number;
         // Gesamt (Role-abhaengig)
         totalBadgeCount: number;
         // Actions
         refreshAllCounts: () => Promise<void>;
         markRoomAsRead: (roomId: number) => void;
         // Legacy Alias
         badgeCount: number; // = chatUnreadTotal (Abwaertskompatibilitaet fuer ChatOverview)
         refreshFromAPI: () => Promise<void>; // = refreshAllCounts Alias
       }
       ```
    3. State: `chatUnreadByRoom` (Record<number, number>), `chatUnreadTotal` (number), `pendingRequestsCount` (number), `pendingEventsCount` (number)
    4. `totalBadgeCount` als useMemo: Admin = chatUnreadTotal + pendingRequestsCount + pendingEventsCount, Konfi = chatUnreadTotal
    5. `refreshAllCounts`: Parallele API-Calls mit Promise.all — GET /chat/rooms immer, GET /admin/activities/requests + GET /events nur fuer admin (nicht super_admin). Wie in RESEARCH.md Zeile 156-199 beschrieben.
    6. `markRoomAsRead(roomId)`: Setzt chatUnreadByRoom[roomId] = 0, aktualisiert chatUnreadTotal, macht API PUT /chat/rooms/{roomId}/read
    7. WebSocket 'newMessage' Listener bleibt, ruft refreshAllCounts statt refreshFromAPI
    8. Window Event Listeners hinzufuegen: 'requestStatusChanged' und 'events-updated' triggern refreshAllCounts
    9. Polling: 30s Interval fuer Admin-Counts (requests + events), kein separates Polling fuer Chat (WebSocket reicht + refreshAllCounts bei App-Start)
    10. Badge.set(totalBadgeCount) bei Aenderung von totalBadgeCount (nicht nur chatUnreadTotal)
    11. refreshAllCounts nur aufrufen wenn user nicht null (Race Condition vermeiden)
    12. Legacy: `badgeCount` = `chatUnreadTotal`, `refreshFromAPI` = `refreshAllCounts`

    **AppContext.tsx bereinigen:**

    1. Entferne: chatNotifications State, chatNotificationsLoading State, refreshChatNotifications Funktion, markChatRoomAsRead Funktion, addUnreadChatMessage Funktion
    2. Entferne aus AppContextType Interface: chatNotifications, chatNotificationsLoading, refreshChatNotifications, markChatRoomAsRead, addUnreadChatMessage
    3. Entferne aus Provider value: alle 5 entfernten Felder
    4. Entferne ChatNotifications Type-Definition wenn vorhanden
    5. WICHTIG: Suche alle auskommentierten Referenzen auf refreshChatNotifications (Zeilen 321, 325, 397) und entferne die Kommentare ebenfalls

    **MainTabs.tsx schlank machen:**

    1. Entferne: pendingRequestsCount State + useEffect (Zeilen 64, 70-99)
    2. Entferne: pendingEventsCount State + useEffect (Zeilen 65, 102-132)
    3. BEHALTE: newBadgesCount State + useEffect (Zeilen 66, 135-154) — ist deferred laut CONTEXT.md
    4. Aendere: `const { badgeCount } = useBadge()` zu `const { chatUnreadTotal, pendingRequestsCount, pendingEventsCount } = useBadge()`
    5. Admin Chat-Tab Badge: `chatUnreadTotal` statt `badgeCount`
    6. Admin Events-Tab Badge: `pendingEventsCount` aus useBadge() (kein lokaler State)
    7. Admin Antraege-Tab Badge: `pendingRequestsCount` aus useBadge() (kein lokaler State)
    8. Konfi Chat-Tab Badge: `chatUnreadTotal` statt `badgeCount`
    9. Entferne api Import wenn nicht mehr benoetigt (nur noch fuer newBadgesCount)

    **ChatRoom.tsx migrieren:**

    1. Zeile 46: `markChatRoomAsRead` nicht mehr aus `useApp()` importieren, sondern aus `useBadge()`
    2. Import hinzufuegen: `import { useBadge } from '../../contexts/BadgeContext';`
    3. Zeile 311: `markChatRoomAsRead(room.id)` funktioniert weiterhin, kommt jetzt aus BadgeContext
    4. Entferne `markChatRoomAsRead` aus der useApp() Destructuring falls es der einzige verbleibende Grund war

    WICHTIG: Keine Emojis verwenden! Echte Umlaute verwenden (ue -> ue ist ok in Variablennamen, aber in Kommentaren/Strings immer ue -> ue)!
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - BadgeContext exportiert chatUnreadByRoom, chatUnreadTotal, pendingRequestsCount, pendingEventsCount, totalBadgeCount, refreshAllCounts, markRoomAsRead
    - AppContext hat keine chatNotifications-Referenzen mehr
    - MainTabs hat kein eigenes Polling fuer requests/events mehr, liest aus useBadge()
    - ChatRoom.tsx nutzt markRoomAsRead aus useBadge()
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatOverview auf BadgeContext umstellen</name>
  <files>frontend/src/components/chat/ChatOverview.tsx</files>
  <action>
    **ChatOverview.tsx anpassen:**

    1. useBadge() Destructuring erweitern: `const { chatUnreadByRoom, markRoomAsRead, refreshAllCounts } = useBadge()`
    2. Entferne `setBadgeCount(totalUnread)` aus loadRooms (Zeile 121) — BadgeContext berechnet das selbst
    3. Entferne `setBadgeCount` aus useBadge() Destructuring (Zeile 63)
    4. Unread-Count Anzeige pro Raum: Nutze `chatUnreadByRoom[room.id] || room.unread_count || 0` als Fallback-Kette — BadgeContext hat Prioriataet, API-Response als Fallback
    5. Stats-Row total unread (Zeile 328): Nutze `Object.values(chatUnreadByRoom).reduce((sum, c) => sum + c, 0)` oder behalte die rooms-basierte Berechnung (beides ok, solange konsistent)
    6. Beim Betreten eines Raums: `markRoomAsRead(room.id)` statt `markChatRoomAsRead` aus AppContext (falls hier aufgerufen)
    7. loadRooms kann weiterhin GET /chat/rooms fuer die Raum-LISTE aufrufen (Name, letzte Nachricht etc.), aber soll NICHT den Badge-Count setzen

    WICHTIG: `refreshFromAPI` Aufrufe in ChatOverview die zum Refresh des Badge-Counts dienten koennen durch `refreshAllCounts()` ersetzt werden, oder komplett entfernt werden wenn der WebSocket-Listener in BadgeContext das bereits abdeckt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ChatOverview setzt keinen Badge-Count mehr direkt (kein setBadgeCount)
    - Per-Raum Unread-Counts werden aus BadgeContext gelesen
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — keine TypeScript-Fehler
2. Grep-Pruefung: `grep -r "chatNotifications" frontend/src/` sollte keine Treffer mehr liefern
3. Grep-Pruefung: `grep -r "setPendingRequestsCount\|setPendingEventsCount" frontend/src/components/layout/MainTabs.tsx` sollte keine Treffer liefern
4. Grep-Pruefung: `grep -r "setBadgeCount" frontend/src/components/chat/ChatOverview.tsx` sollte keine Treffer liefern
</verification>

<success_criteria>
- BadgeContext ist die einzige Quelle fuer chatUnreadByRoom, chatUnreadTotal, pendingRequestsCount, pendingEventsCount
- MainTabs liest alle Badge-Zahlen aus useBadge(), hat kein eigenes Polling (ausser newBadgesCount das deferred ist)
- AppContext hat keine chatNotifications mehr
- ChatOverview zeigt Unread-Counts aus BadgeContext
- Badge.set() nutzt totalBadgeCount (Summe aller Kategorien, rolle-abhaengig)
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/27-badge-count-single-source-of-truth/27-01-SUMMARY.md`
</output>
