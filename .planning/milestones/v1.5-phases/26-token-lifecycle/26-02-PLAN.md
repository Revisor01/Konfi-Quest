---
phase: 26-token-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/contexts/AppContext.tsx
autonomous: true
requirements: [TKN-01, TKN-03, TKN-04]

must_haves:
  truths:
    - "Nach Logout erhaelt das Geraet keine Pushes mehr fuer den abgemeldeten User"
    - "Token wird bei App Resume nach >12h automatisch refreshed ohne User-Aktion"
    - "Bei User-Wechsel auf demselben Geraet erhaelt nur der neue User Pushes"
    - "localStorage-Key fuer Token-Refresh heisst 'push_token_last_refresh'"
  artifacts:
    - path: "frontend/src/contexts/AppContext.tsx"
      provides: "12h Token-Refresh mit korrektem localStorage-Key"
      contains: "push_token_last_refresh"
  key_links:
    - from: "frontend/src/contexts/AppContext.tsx:handleAppActive"
      to: "PushNotifications.register()"
      via: "12h-Check mit localStorage Timestamp"
      pattern: "push_token_last_refresh"
    - from: "frontend/src/services/auth.ts:logout"
      to: "/notifications/device-token DELETE"
      via: "API-Call mit device_id + platform"
      pattern: "notifications/device-token"
---

<objective>
Frontend Token-Lifecycle: localStorage-Key fuer 12h-Refresh korrigieren und Logout/User-Wechsel verifizieren.

Purpose: Token-Refresh muss konsistent mit der CONTEXT.md-Entscheidung ('push_token_last_refresh') sein. Logout und User-Wechsel funktionieren bereits korrekt — hier wird nur der Key umbenannt.
Output: AppContext.tsx mit korrektem localStorage-Key.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-token-lifecycle/26-CONTEXT.md

<interfaces>
<!-- Bestehende Logout-Logik in auth.ts — funktioniert bereits korrekt -->
From frontend/src/services/auth.ts:
```typescript
// logout() loescht Token via DELETE /notifications/device-token mit device_id + platform
// Best-effort: Logout geht immer durch, auch wenn Token-Loeschung fehlschlaegt
// Device ID: Native Device.getId() zuerst, localStorage Fallback
// device_id wird NICHT aus localStorage geloescht (geraetespezifisch)
```

<!-- Bestehender User-Wechsel in notifications.js — funktioniert bereits korrekt -->
From backend/routes/notifications.js:
```javascript
// Bei Token-Upsert: DELETE FROM push_tokens WHERE token = $1 AND user_id != $2
// Entfernt alte User-Zuordnungen fuer den gleichen FCM-Token
```

<!-- AppContext Token-Refresh Bereich -->
From frontend/src/contexts/AppContext.tsx:
```typescript
// Zeile 400-412: appStateChange Listener mit 12h-Check
// Nutzt aktuell 'lastTokenRefresh' — muss zu 'push_token_last_refresh' geaendert werden
// PushNotifications.register() triggert 'registration' Event → sendTokenToServer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: localStorage-Key umbenennen und Token-Refresh verifizieren (TKN-03)</name>
  <files>frontend/src/contexts/AppContext.tsx</files>
  <action>
In AppContext.tsx den localStorage-Key fuer den 12h Token-Refresh umbenennen (per CONTEXT.md Entscheidung):

1. **Zeile 402**: `localStorage.getItem('lastTokenRefresh')` aendern zu `localStorage.getItem('push_token_last_refresh')`
2. **Zeile 407**: `localStorage.setItem('lastTokenRefresh', ...)` aendern zu `localStorage.setItem('push_token_last_refresh', ...)`

Das ist die einzige Aenderung in diesem Task. Der bestehende 12h-Check (Zeile 400-412) ist korrekt implementiert:
- Wird bei appStateChange (isActive) getriggert
- Prueft ob >12h seit letztem Refresh
- Ruft PushNotifications.register() auf → triggert 'registration' Event → sendTokenToServer sendet Token an Backend
- Timestamp wird nach erfolgreichem register() gespeichert
- console.warn bei Fehler, kein UI-Feedback — korrekt per CONTEXT.md

KEINE weiteren Aenderungen an AppContext.tsx noetig. Bestehende Logik fuer:
- sendTokenToServer (Device.getId() + Fallback) — korrekt
- registration Listener (sendTokenToServer aufrufen) — korrekt
- appStateChange Listener Setup/Cleanup — korrekt
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "push_token_last_refresh" frontend/src/contexts/AppContext.tsx | xargs test 2 -eq && grep -c "lastTokenRefresh" frontend/src/contexts/AppContext.tsx | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>localStorage-Key ist 'push_token_last_refresh' (2 Vorkommen: getItem + setItem), alter Key 'lastTokenRefresh' existiert nicht mehr.</done>
</task>

<task type="auto">
  <name>Task 2: Logout und User-Wechsel Verifikation (TKN-01, TKN-04)</name>
  <files>frontend/src/services/auth.ts</files>
  <action>
Verifizieren dass die bestehende Logout- und User-Wechsel-Logik korrekt funktioniert. Laut Research und Code-Analyse sind KEINE Code-Aenderungen noetig — die Logik ist bereits korrekt implementiert.

**TKN-01 Verifikation (Logout):**
- auth.ts:58-98: Logout holt Device.getId() (mit localStorage Fallback), sendet DELETE /notifications/device-token mit device_id + platform
- Backend notifications.js loescht nur Tokens fuer user_id + platform + device_id (nicht alle User-Tokens)
- Logout geht immer durch (try/catch, best-effort) — korrekt per CONTEXT.md
- device_id bleibt in localStorage persistent — korrekt per CONTEXT.md

**TKN-04 Verifikation (User-Wechsel):**
- notifications.js:37-40: Bei Token-Upsert wird `DELETE FROM push_tokens WHERE token = $1 AND user_id != $2` ausgefuehrt
- Das entfernt die alte User-Zuordnung wenn der gleiche FCM-Token von einem neuen User registriert wird
- Bestehendes Verhalten beibehalten — korrekt per CONTEXT.md

Einzige Aenderung: Einen kurzen Kommentar in auth.ts hinzufuegen der die Token-Loeschungs-Logik dokumentiert (nach Zeile 57):
```typescript
// TKN-01: Loescht nur den Token des AKTUELLEN Devices (user_id + platform + device_id)
// Andere Geraete des Users behalten ihre Tokens
// Best-effort: Logout geht immer durch, auch bei Fehler
```

KEINE funktionale Aenderung an auth.ts oder notifications.js.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "TKN-01" frontend/src/services/auth.ts | xargs test 1 -eq && grep -c "device-token" frontend/src/services/auth.ts | xargs test 1 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Logout loescht nur den Token des aktuellen Devices (TKN-01). User-Wechsel entfernt alte User-Zuordnung automatisch im Backend (TKN-04). Kommentar dokumentiert das Verhalten.</done>
</task>

</tasks>

<verification>
- `grep "push_token_last_refresh" frontend/src/contexts/AppContext.tsx` liefert 2 Treffer
- `grep "lastTokenRefresh" frontend/src/contexts/AppContext.tsx` liefert 0 Treffer
- `grep "TKN-01" frontend/src/services/auth.ts` liefert 1 Treffer
- auth.ts Logout-Funktion hat weiterhin DELETE /notifications/device-token Call
- notifications.js hat weiterhin DELETE WHERE token = $1 AND user_id != $2
</verification>

<success_criteria>
1. localStorage-Key fuer Token-Refresh ist 'push_token_last_refresh' (TKN-03)
2. Logout loescht nur Token des aktuellen Devices, nicht alle User-Tokens (TKN-01)
3. User-Wechsel auf gleichem Geraet transferiert FCM-Token zum neuen User (TKN-04)
4. Alle bestehende Funktionalitaet bleibt erhalten
</success_criteria>

<output>
After completion, create `.planning/phases/26-token-lifecycle/26-02-SUMMARY.md`
</output>
