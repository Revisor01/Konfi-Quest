---
phase: 26-token-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/pushService.js
autonomous: true
requirements: [TKN-02, CLN-01]

must_haves:
  truths:
    - "Geraete mit Fallback-Device-IDs erhalten Pushes genauso wie Geraete mit nativer Device-ID"
    - "Firebase-Errors mit ungueltigen Tokens fuehren zur sofortigen Loeschung des Tokens aus der DB"
    - "Bei erfolgreichem Push wird error_count auf 0 zurueckgesetzt"
    - "Bei temporaeren Firebase-Errors wird error_count inkrementiert"
  artifacts:
    - path: "backend/services/pushService.js"
      provides: "getTokensForUser ohne __-Filter, sendToUser mit Result-Auswertung, sendChatNotification ohne __-Filter"
      contains: "messaging/registration-token-not-registered"
  key_links:
    - from: "backend/services/pushService.js:sendToUser"
      to: "backend/push/firebase.js:sendFirebasePushNotification"
      via: "Result-Objekt Auswertung statt try/catch"
      pattern: "result\\.success"
    - from: "backend/services/pushService.js:getTokensForUser"
      to: "push_tokens Tabelle"
      via: "SQL Query ohne device_id Filter"
      pattern: "SELECT.*FROM push_tokens.*WHERE user_id"
---

<objective>
Backend Push-Service Fixes: Fallback-Device-ID Filter entfernen und Firebase Error-Handling einbauen.

Purpose: Ohne diese Fixes erhalten Geraete mit Fallback-IDs keine Pushes und ungueltige Tokens bleiben in der DB.
Output: pushService.js mit korrektem Token-Querying und Error-Handling.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-token-lifecycle/26-CONTEXT.md
@.planning/phases/25-foundation-konfiguration/25-01-SUMMARY.md

<interfaces>
<!-- Phase 25 hat firebase.js auf Result-Pattern umgebaut -->
From backend/push/firebase.js:
```javascript
// sendFirebasePushNotification returns (does NOT throw):
// { success: true }
// { success: false, error: "...", errorCode: "messaging/..." | null }
```

From push_tokens Tabelle (Phase 25 Migration):
```sql
-- Spalten: id, user_id, user_type, token, platform, device_id, error_count, last_error_at, updated_at
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fallback-Device-ID Filter entfernen (TKN-02)</name>
  <files>backend/services/pushService.js</files>
  <action>
In pushService.js den `device_id NOT LIKE '%\_\_%'` Filter an BEIDEN Stellen entfernen:

1. **getTokensForUser** (Zeile 43 und 48): Die AND-Klausel `AND device_id NOT LIKE '%\_\_%'` komplett rausnehmen — sowohl in der aeusseren WHERE-Klausel als auch im Subquery. Der $2 Parameter im Subquery wird zu $1 (gleicher userId fuer beide Stellen). Ergebnis:
```javascript
static async getTokensForUser(db, userId) {
  const query = `
    SELECT * FROM push_tokens
    WHERE user_id = $1
      AND id IN (
        SELECT MAX(id)
        FROM push_tokens
        WHERE user_id = $1
        GROUP BY device_id, platform
      )
  `;
  const { rows: tokens } = await db.query(query, [userId]);
  return tokens || [];
}
```

2. **sendChatNotification** (Zeile 122 und 127): Gleicher Filter entfernen. ACHTUNG: Die Query nutzt $1 und $2 als userId (doppelt), plus dynamische Sender-Token Parameter ab $3. Nach Entfernung des Filters bleibt die Struktur gleich, nur ohne die AND-Klausel. Ergebnis:
```javascript
let query = `
  SELECT * FROM push_tokens
  WHERE user_id = $1
    AND id IN (
      SELECT MAX(id)
      FROM push_tokens
      WHERE user_id = $2
      GROUP BY device_id, platform
    )
`;
```
Alles andere in sendChatNotification (Sender-Ausschluss Logik) bleibt UNVERAENDERT.

Den Kommentar "NUR das neueste echte Device Token verwenden, Fallback-IDs ignorieren" (Zeile 117) aendern zu: "Neuestes Token pro Device verwenden".
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "NOT LIKE" backend/services/pushService.js | wc -l | xargs test 0 -eq && echo "PASS: Keine NOT LIKE Filter mehr" || echo "FAIL"</automated>
  </verify>
  <done>getTokensForUser und sendChatNotification liefern Tokens fuer ALLE Device-IDs zurueck, inklusive Fallback-IDs mit Underscores.</done>
</task>

<task type="auto">
  <name>Task 2: Firebase Error-Handling in sendToUser (CLN-01)</name>
  <files>backend/services/pushService.js</files>
  <action>
In pushService.js die sendToUser-Methode umbauen: Das innere try/catch (Zeile 72-84) durch Result-Auswertung ersetzen. sendFirebasePushNotification returned ein Objekt `{ success, error, errorCode }` — es wirft NICHT.

Per User-Entscheidung (CONTEXT.md):
- Sofortige Token-Loeschung bei: `messaging/registration-token-not-registered`, `messaging/invalid-registration-token`
- Bei anderen Errors: `error_count++` und `last_error_at = NOW()`
- Bei erfolgreichem Push: `error_count = 0` zuruecksetzen (nur wenn vorher > 0)
- Error-Handling direkt in sendToUser, keine separate Methode

Ergebnis fuer die Token-Loop in sendToUser:
```javascript
for (const token of tokens) {
  const result = await sendFirebasePushNotification(token.token, {
    title: notification.title,
    body: notification.body,
    badge: notification.badge || 1,
    sound: 'default',
    data: notification.data || {}
  });

  if (result.success) {
    successCount++;
    // Error-Count zuruecksetzen bei Erfolg (nur wenn vorher > 0)
    if (token.error_count > 0) {
      await db.query(
        'UPDATE push_tokens SET error_count = 0, last_error_at = NULL WHERE id = $1',
        [token.id]
      );
    }
  } else {
    // Fatale Errors: Token sofort loeschen
    const fatalCodes = [
      'messaging/registration-token-not-registered',
      'messaging/invalid-registration-token'
    ];
    if (fatalCodes.includes(result.errorCode)) {
      await db.query('DELETE FROM push_tokens WHERE id = $1', [token.id]);
      console.warn(`Token ${token.id} geloescht (${result.errorCode})`);
    } else {
      // Sonstige Errors: Counter erhoehen
      await db.query(
        'UPDATE push_tokens SET error_count = error_count + 1, last_error_at = NOW() WHERE id = $1',
        [token.id]
      );
      console.error('Push failed for token:', result.error);
    }
    errorCount++;
  }
}
```

Das aeussere try/catch (Zeile 88-91) bleibt fuer DB-Errors und andere unerwartete Fehler.

WICHTIG: Auch sendChatNotification hat eine eigene Token-Loop (Zeile 149-170) mit try/catch. Dort das gleiche Result-Pattern einbauen. Da sendChatNotification NICHT getTokensForUser nutzt, muss das error_count/token.id Handling hier AUCH eingebaut werden. Gleiche fatalCodes-Liste, gleiche SQL-Queries.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "result.success" backend/services/pushService.js | xargs test 2 -le && grep -c "fatalCodes" backend/services/pushService.js | xargs test 2 -le && grep -c "error_count" backend/services/pushService.js | xargs test 1 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>sendToUser und sendChatNotification werten Firebase Result-Objekte aus: fatale Errors loeschen Tokens sofort, temporaere Errors inkrementieren error_count, erfolgreiche Pushes setzen error_count auf 0 zurueck.</done>
</task>

</tasks>

<verification>
- `grep -n "NOT LIKE" backend/services/pushService.js` liefert 0 Treffer
- `grep -n "result.success" backend/services/pushService.js` liefert mindestens 2 Treffer (sendToUser + sendChatNotification)
- `grep -n "fatalCodes" backend/services/pushService.js` liefert mindestens 2 Treffer
- `grep -n "error_count" backend/services/pushService.js` liefert mehrere Treffer (Reset + Increment)
- Kein `catch (error)` mehr innerhalb der Token-Loops (innere try/catch entfernt)
</verification>

<success_criteria>
1. Fallback-Device-IDs werden nicht mehr aus Push-Queries gefiltert (TKN-02)
2. Fatale Firebase-Errors loeschen den Token sofort aus der DB (CLN-01)
3. Temporaere Firebase-Errors erhoehen error_count (CLN-01)
4. Erfolgreiche Pushes setzen error_count auf 0 zurueck (CLN-01)
5. Beide Stellen (sendToUser + sendChatNotification) haben das neue Error-Handling
</success_criteria>

<output>
After completion, create `.planning/phases/26-token-lifecycle/26-01-SUMMARY.md`
</output>
