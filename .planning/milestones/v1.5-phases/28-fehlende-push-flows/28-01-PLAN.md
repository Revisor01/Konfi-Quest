---
phase: 28-fehlende-push-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/pushService.js
  - backend/routes/auth.js
  - backend/services/backgroundService.js
autonomous: true
requirements:
  - FLW-01
  - FLW-02

must_haves:
  truths:
    - "Event-Reminder-Service laeuft alle 15 Minuten und sendet Erinnerungen an bestaetigte Teilnehmer"
    - "Admins erhalten Push-Benachrichtigung wenn ein neuer Konfi sich registriert"
    - "Wenn kein Admin dem Jahrgang zugewiesen ist, erhalten alle Org-Admins die Push"
  artifacts:
    - path: "backend/services/pushService.js"
      provides: "sendNewKonfiRegistrationToAdmins Methode"
      contains: "sendNewKonfiRegistrationToAdmins"
    - path: "backend/routes/auth.js"
      provides: "Push-Trigger nach Konfi-Registrierung"
      contains: "sendNewKonfiRegistrationToAdmins"
    - path: "backend/services/backgroundService.js"
      provides: "Event-Reminder-Service mit 15-Min-Intervall"
      contains: "sendEventReminders"
  key_links:
    - from: "backend/routes/auth.js"
      to: "backend/services/pushService.js"
      via: "PushService.sendNewKonfiRegistrationToAdmins nach COMMIT"
      pattern: "sendNewKonfiRegistrationToAdmins"
    - from: "backend/services/backgroundService.js"
      to: "backend/services/pushService.js"
      via: "PushService.sendEventReminderToKonfi"
      pattern: "sendEventReminderToKonfi"
---

<objective>
Event-Reminder-Logik verifizieren (FLW-01) und Admin-Alert bei Konfi-Registrierung implementieren (FLW-02).

Purpose: Konfis erhalten zeitgerechte Event-Erinnerungen, Admins werden sofort ueber neue Registrierungen informiert.
Output: Funktionierende Event-Reminders + neue sendNewKonfiRegistrationToAdmins Push-Methode mit Integration in auth.js.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-fehlende-push-flows/28-CONTEXT.md
@.planning/phases/28-fehlende-push-flows/28-RESEARCH.md

@backend/services/pushService.js
@backend/services/backgroundService.js
@backend/routes/auth.js

<interfaces>
<!-- Bestehende Push-Infrastruktur -->

From backend/services/pushService.js:
```javascript
static async sendToUser(db, userId, notification)
static async sendToMultipleUsers(db, userIds, notification)
static async sendEventReminderToKonfi(db, konfiId, eventName, eventDate, eventTime, reminderType)
// Pattern fuer Admin-Pushes (sendNewActivityRequestToAdmins):
// 1. Admin-IDs per SQL Query holen
// 2. notification Objekt bauen {title, body, data: {type: '...'}}
// 3. return await this.sendToMultipleUsers(db, adminIds, notification)
```

From backend/routes/auth.js (Zeile 597):
```javascript
// COMMIT passiert in Zeile 597
await client.query('COMMIT');
// Danach JWT Token Generierung — Push muss DAZWISCHEN
// WICHTIG: auth.js hat KEINEN PushService Import — muss hinzugefuegt werden
// Verfuegbare Variablen nach COMMIT: invite.organization_id, invite.jahrgang_id, display_name, invite.jahrgang_name
```

From backend/services/backgroundService.js:
```javascript
// Event-Reminder-Service bereits komplett implementiert (Zeile 113-239)
// startAllServices(db) wird in server.js:446 aufgerufen
// Intervall: alle 15 Minuten (900000ms)
// 1-Tag und 1-Stunde Erinnerungen mit event_reminders Duplikat-Schutz
```

Tabelle user_jahrgang_assignments:
```sql
-- Spalten: user_id, jahrgang_id, can_view, can_edit, assigned_by, assigned_at
-- Verwendet fuer Admin-Jahrgangs-Zuordnung
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event-Reminder-Logik verifizieren und ggf. fixen (FLW-01)</name>
  <files>backend/services/backgroundService.js</files>
  <action>
Die Event-Reminder-Logik in backgroundService.js (Zeile 113-239) ist bereits komplett implementiert. Verifiziere:

1. Lese backgroundService.js komplett und pruefe:
   - Intervall ist 15 Minuten (900000ms) — falls anders, auf 15 Min setzen
   - 1-Tag-Query: Events mit event_date::date = morgen, nur status='confirmed' Teilnehmer
   - 1-Stunde-Query: Events im Zeitfenster 45-75 Minuten in der Zukunft, nur status='confirmed'
   - event_reminders Duplikat-Schutz mit ON CONFLICT DO NOTHING
   - sendEventReminderToKonfi Aufruf mit korrekten Parametern

2. Pruefe ob event_reminders Tabelle in der Migration existiert (backend/migrations/add_push_foundation.sql)

3. Falls Bugs gefunden werden: fixen. Falls alles korrekt: keine Aenderungen noetig.

4. Stelle sicher dass der Push-Type 'event_reminder' in der Push-Type-Registry (Kommentar-Block am Anfang von pushService.js) eingetragen ist.

Keine neuen Features bauen — nur verifizieren dass die bestehende Implementierung korrekt ist.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "sendEventReminders\|event_reminders\|900000\|15.*min" backend/services/backgroundService.js | head -20</automated>
  </verify>
  <done>Event-Reminder-Logik ist verifiziert: 15-Min-Intervall, nur bestaetigte Teilnehmer, Duplikat-Schutz aktiv. Eventuelle Bugs gefixt.</done>
</task>

<task type="auto">
  <name>Task 2: sendNewKonfiRegistrationToAdmins implementieren und in auth.js integrieren (FLW-02)</name>
  <files>backend/services/pushService.js, backend/routes/auth.js</files>
  <action>
1. In pushService.js nach sendEventReminderToKonfi (ca. Zeile 572) neue statische Methode hinzufuegen:

```javascript
static async sendNewKonfiRegistrationToAdmins(db, organizationId, jahrgangId, konfiName, jahrgangName) {
  try {
    // Admins des Jahrgangs finden
    const { rows: admins } = await db.query(`
      SELECT DISTINCT u.id FROM users u
      JOIN roles r ON u.role_id = r.id
      JOIN user_jahrgang_assignments uja ON u.id = uja.user_id
      WHERE r.name IN ('admin', 'org_admin')
        AND u.organization_id = $1
        AND uja.jahrgang_id = $2
    `, [organizationId, jahrgangId]);

    // Fallback: Alle Org-Admins wenn kein Jahrgangs-Admin
    let adminIds;
    if (admins.length === 0) {
      const { rows: allAdmins } = await db.query(`
        SELECT u.id FROM users u
        JOIN roles r ON u.role_id = r.id
        WHERE r.name IN ('admin', 'org_admin') AND u.organization_id = $1
      `, [organizationId]);
      adminIds = allAdmins.map(a => a.id);
    } else {
      adminIds = admins.map(a => a.id);
    }

    if (adminIds.length === 0) return { success: false, message: 'No admins found' };

    const notification = {
      title: 'Neue Registrierung',
      body: `${konfiName} hat sich registriert (${jahrgangName})`,
      data: {
        type: 'new_konfi_registration',
        organization_id: organizationId.toString(),
        jahrgang_id: jahrgangId.toString()
      }
    };

    return await this.sendToMultipleUsers(db, adminIds, notification);
  } catch (error) {
    console.error('sendNewKonfiRegistrationToAdmins error:', error);
    return { success: false, error: error.message };
  }
}
```

2. Push-Type 'new_konfi_registration' in der Push-Type-Registry (Kommentar-Block am Anfang von pushService.js) eintragen.

3. In auth.js:
   - PushService Import am Anfang der Datei hinzufuegen: `const PushService = require('../services/pushService');`
   - Nach COMMIT (Zeile 597), VOR JWT Token Generierung (Zeile 599), Push-Trigger einbauen:

```javascript
// Push-Notification an Jahrgangs-Admins
try {
  await PushService.sendNewKonfiRegistrationToAdmins(
    db,
    invite.organization_id,
    invite.jahrgang_id,
    display_name,
    invite.jahrgang_name
  );
} catch (pushErr) {
  console.error('Push for new registration failed:', pushErr);
}
```

WICHTIG: Der Push darf NACH COMMIT aber VOR res.json() stehen. Falls der Push fehlschlaegt, darf die Registrierung NICHT fehlschlagen (try/catch).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -n "sendNewKonfiRegistrationToAdmins" backend/services/pushService.js backend/routes/auth.js</automated>
  </verify>
  <done>Neue Methode sendNewKonfiRegistrationToAdmins in pushService.js mit Jahrgangs-Admin-Lookup und Fallback auf alle Org-Admins. auth.js importiert PushService und ruft die Methode nach COMMIT auf. Push-Type in Registry eingetragen.</done>
</task>

</tasks>

<verification>
- Event-Reminder backgroundService Logik verifiziert (15-Min-Intervall, confirmed-only, Duplikat-Schutz)
- sendNewKonfiRegistrationToAdmins Methode existiert in pushService.js
- auth.js importiert PushService und ruft sendNewKonfiRegistrationToAdmins nach COMMIT auf
- Push-Typen event_reminder und new_konfi_registration in Registry eingetragen
</verification>

<success_criteria>
- FLW-01: Event-Reminder-Logik ist verifiziert und funktionsfaehig (keine Bugs, 15-Min-Intervall)
- FLW-02: Bei Konfi-Registrierung via Invite-Code erhalten die Jahrgangs-Admins (oder alle Org-Admins als Fallback) eine Push-Benachrichtigung
</success_criteria>

<output>
After completion, create `.planning/phases/28-fehlende-push-flows/28-01-SUMMARY.md`
</output>
