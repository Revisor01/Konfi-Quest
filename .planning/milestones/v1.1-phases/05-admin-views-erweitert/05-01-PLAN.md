---
phase: 05-admin-views-erweitert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/variables.css
  - frontend/src/components/admin/UsersView.tsx
  - frontend/src/components/admin/OrganizationView.tsx
autonomous: true
requirements: [ADM-07, ADM-08, ICO-01]

must_haves:
  truths:
    - "Icon-Farb-CSS-Klassen (app-icon-color--events, --participants, --users, etc.) existieren in variables.css"
    - "app-stepper CSS-Klasse fuer Plus/Minus-Stepper existiert in variables.css"
    - "app-avatar-initials CSS-Klasse fuer Initialen-Kreise existiert in variables.css"
    - "UsersView hat unter 5 Inline-Styles und nutzt app-search-bar, app-segment-wrapper, app-status-chip"
    - "OrganizationView hat unter 5 Inline-Styles und nutzt app-search-bar, app-segment-wrapper, app-avatar-initials, app-status-chip"
  artifacts:
    - path: "frontend/src/theme/variables.css"
      provides: "Icon-Farb-Klassen, Stepper, Avatar-Initialen CSS"
      contains: "app-icon-color--events"
    - path: "frontend/src/components/admin/UsersView.tsx"
      provides: "Bereinigte Benutzer-Ansicht"
    - path: "frontend/src/components/admin/OrganizationView.tsx"
      provides: "Bereinigte Organisations-Ansicht"
  key_links:
    - from: "frontend/src/components/admin/UsersView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS class usage"
      pattern: "app-search-bar|app-segment-wrapper|app-status-chip"
    - from: "frontend/src/components/admin/OrganizationView.tsx"
      to: "frontend/src/theme/variables.css"
      via: "CSS class usage"
      pattern: "app-avatar-initials|app-search-bar|app-segment-wrapper"
---

<objective>
CSS-Klassen fuer Icon-Farben, Stepper und Avatar-Initialen in variables.css definieren, dann UsersView und OrganizationView Inline-Styles durch diese CSS-Klassen ersetzen und funktional aufraeumen.

Purpose: Fundament fuer Icon-Farb-Konsistenz in der gesamten App legen und die beiden Daten-Views (UsersView, OrganizationView) auf CSS-Klassen-Standard bringen.
Output: Erweiterte variables.css, bereinigte UsersView und OrganizationView.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-views-erweitert/05-CONTEXT.md
@.planning/phases/04-admin-views-core/04-01-SUMMARY.md

<interfaces>
<!-- Bestehende CSS-Klassen in variables.css die als Vorlage dienen -->
<!-- app-status-chip: .app-status-chip { display, padding, border-radius, font } mit Varianten --success/--warning/--danger/--info/--purple/--neutral -->
<!-- app-search-bar: .app-search-bar { display, gap, padding, align-items } mit .app-search-bar__icon -->
<!-- app-segment-wrapper: .app-segment-wrapper { margin: 16px } -->
<!-- app-settings-item: .app-settings-item { cursor, display, flex, gap, padding, align-items, margin-bottom } -->
<!-- app-icon-circle: .app-icon-circle { width/height/border-radius/display/align/justify/flex-shrink } mit Farbvarianten -->

<!-- SectionHeader Presets: events, activities, konfis, users, organizations, badges, requests, jahrgang -->
<!-- SectionHeader Props: title, subtitle, icon, preset?, colors?, stats: Array<{value: number, label: string}> -->

<!-- Bestehende Farb-Custom-Properties in variables.css -->
<!-- --color-activities: #059669, --color-users: #667eea, --color-organizations: #2dd36f, --color-badges: #f59e0b, etc. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen fuer Icon-Farben, Stepper und Avatar-Initialen in variables.css ergaenzen</name>
  <files>frontend/src/theme/variables.css</files>
  <action>
Am Ende der bestehenden Utility-Klassen in variables.css (nach app-detail-header Sektion) drei neue Sektionen hinzufuegen:

**1. Icon-Farb-Klassen (app-icon-color--*):**
Definiere CSS-Klassen die nur `color` setzen -- fuer IonIcon-Elemente in Info-Rows, Listen, Detail-Views.
Jede Klasse setzt die Farbe des Icons gemaess dem Icon-Farb-Mapping aus CONTEXT.md:

```css
/* === Icon-Farb-Klassen === */
/* Gleiches Konzept = gleiches Icon = gleiche Farbe ueberall */
.app-icon-color--events { color: #dc2626; }      /* calendar, time, location */
.app-icon-color--participants { color: #34c759; } /* people (Teilnehmer/Konfis) */
.app-icon-color--users { color: #667eea; }        /* person (Team/Users) */
.app-icon-color--warning { color: #fd7e14; }      /* listOutline, hourglass (Warteliste) */
.app-icon-color--badges { color: #ff9500; }       /* trophy, ribbon */
.app-icon-color--category { color: #5b21b6; }     /* pricetag */
.app-icon-color--gottesdienst { color: #3b82f6; } /* school (Gottesdienst) */
.app-icon-color--gemeinde { color: #22c55e; }     /* people (Gemeinde) */
.app-icon-color--activities { color: #059669; }   /* flash */
.app-icon-color--organizations { color: #2dd36f; } /* business */
.app-icon-color--jahrgang { color: #007aff; }     /* school (Jahrgang) */
.app-icon-color--success { color: #34c759; }      /* checkmarkCircle */
.app-icon-color--danger { color: #dc3545; }       /* closeCircle */
```

**2. Stepper-Klasse (app-stepper):**
Extrahiere das Stepper-Pattern aus AdminGoalsPage (Plus/Minus-Buttons mit grosser Zahl):

```css
/* === Stepper === */
.app-stepper {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 16px;
  padding: 8px 0;
}
.app-stepper__value {
  font-size: 2.5rem;
  font-weight: 700;
  color: #333;
  min-width: 80px;
  text-align: center;
}
.app-stepper__button {
  width: 48px;
  height: 48px;
}
.app-stepper__button ion-icon {
  font-size: 1.5rem;
}
```

**3. Avatar-Initialen-Klasse (app-avatar-initials):**
Extrahiere das Initialen-Icon-Pattern aus OrganizationView (runder Kreis mit Buchstabe):

```css
/* === Avatar Initialen === */
.app-avatar-initials {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 1.1rem;
  color: white;
  flex-shrink: 0;
}
.app-avatar-initials--sm { width: 32px; height: 32px; font-size: 0.9rem; }
.app-avatar-initials--lg { width: 48px; height: 48px; font-size: 1.3rem; }
```

WICHTIG: Keine Emojis verwenden. Kommentare auf Deutsch.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "app-icon-color--events\|app-icon-color--participants\|app-icon-color--users\|app-icon-color--warning\|app-icon-color--badges\|app-stepper\|app-avatar-initials" frontend/src/theme/variables.css</automated>
  </verify>
  <done>Mindestens 13 app-icon-color--, 4 app-stepper, und 3 app-avatar-initials Klassen existieren in variables.css</done>
</task>

<task type="auto">
  <name>Task 2: UsersView und OrganizationView Inline-Styles bereinigen und funktional aufraeumen</name>
  <files>frontend/src/components/admin/UsersView.tsx, frontend/src/components/admin/OrganizationView.tsx</files>
  <action>
**UsersView.tsx (17 Inline-Styles -> unter 5):**

1. Suche/Filter-Block: Ersetze Inline-Styles auf dem Such-Container durch `className="app-search-bar"`, Such-Icon durch `className="app-search-bar__icon"`, Segment-Container durch `className="app-segment-wrapper"`.

2. Status-Anzeige: Ersetze Inline-Style-Spans fuer aktiv/inaktiv durch `<span className="app-status-chip app-status-chip--success">` bzw. `app-status-chip--danger`.

3. Rollen-Anzeige: Ersetze Inline-Style-Spans fuer Admin/Teamer/Konfi Rollen durch passende `app-status-chip`-Varianten (--purple fuer Admin/Org-Admin, --info fuer Teamer, --neutral fuer Konfi).

4. Icon-Farben: Ersetze alle `style={{ color: '#...' }}` auf IonIcon-Elementen durch die neuen `className="app-icon-color--*"` Klassen.

5. Remaining Inline-Styles: Ersetze `style={{ margin: '16px' }}` auf IonList durch `className="app-segment-wrapper"` und andere wiederkehrende Patterns durch CSS-Klassen.

6. Funktional: UsersView hat bereits IonItemSliding. Fuege IonRefresher hinzu falls fehlend (gleicher Pattern wie AdminLevelsPage: `<IonRefresher slot="fixed" onIonRefresh={handleRefresh}>` wobei handleRefresh `onUpdate()` aufruft und `event.detail.complete()` nach kurzem Timeout). Falls kein IonSearchbar vorhanden, ist der manuelle IonInput-basierte Such-Container OK (wird nur auf CSS-Klassen umgestellt).

**OrganizationView.tsx (31 Inline-Styles -> unter 5):**

1. Suche/Filter-Block: Gleich wie UsersView -- `app-search-bar`, `app-search-bar__icon`, `app-segment-wrapper`.

2. Initialen-Icon: Ersetze das Inline-Style `style={{ width: 40px, height: 40px, borderRadius: '50%', ... }}` fuer den Initialen-Kreis durch `className="app-avatar-initials"` mit dynamischer `style={{ backgroundColor: ... }}` (Farbe bleibt dynamisch inline).

3. Detail-Stats (Benutzer/Konfis/Aktivitaeten in expandiertem Org-Detail): Ersetze Inline-Styles auf den Stat-Paaren (Icon + Text) durch bestehende `app-info-row` / `app-info-row__icon` CSS-Klassen. Icon-Farben durch `app-icon-color--*` Klassen ersetzen.

4. Status-Chips: Ersetze aktiv/inaktiv Inline-Style-Spans durch `app-status-chip--success` / `app-status-chip--danger`.

5. Swipe-Action-Buttons: Falls die IonItemOption-Buttons Inline-Styles haben, diese durch Ionic-eigene CSS-Properties ersetzen (--background, --color).

6. Funktional: OrganizationView hat bereits IonItemSliding. Fuege IonRefresher hinzu (gleicher Pattern wie UsersView oben). Suche ist bereits vorhanden.

WICHTIG:
- Icons IMMER solid verwenden (nicht outline). Ausnahme: Navigations-Icons wie arrowBack, filterOutline.
- Keine Emojis.
- Dynamische Farben (backgroundColor basierend auf Daten) duerfen inline bleiben.
- Ionic CSS Custom Properties (--background, --padding-start) duerfen inline bleiben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && echo "UsersView:" && grep -c 'style={{' frontend/src/components/admin/UsersView.tsx && echo "OrganizationView:" && grep -c 'style={{' frontend/src/components/admin/OrganizationView.tsx && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>UsersView hat unter 5 Inline-Styles, OrganizationView hat unter 8 Inline-Styles (dynamische Farben bleiben). Beide haben IonRefresher. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `grep -c "app-icon-color--" frontend/src/theme/variables.css` zeigt mindestens 13
2. `grep -c "app-stepper" frontend/src/theme/variables.css` zeigt mindestens 4
3. `grep -c "app-avatar-initials" frontend/src/theme/variables.css` zeigt mindestens 3
4. `grep -c 'style={{' frontend/src/components/admin/UsersView.tsx` zeigt unter 5
5. `grep -c 'style={{' frontend/src/components/admin/OrganizationView.tsx` zeigt unter 8
6. `grep "IonRefresher" frontend/src/components/admin/UsersView.tsx` liefert Treffer
7. `grep "IonRefresher" frontend/src/components/admin/OrganizationView.tsx` liefert Treffer
8. TypeScript kompiliert fehlerfrei
</verification>

<success_criteria>
- 13+ Icon-Farb-CSS-Klassen, app-stepper und app-avatar-initials existieren in variables.css
- UsersView und OrganizationView nutzen CSS-Klassen statt Inline-Styles fuer wiederkehrende Patterns
- Beide Views haben IonRefresher fuer Pull-to-Refresh
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-views-erweitert/05-01-SUMMARY.md`
</output>
