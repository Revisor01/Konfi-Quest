---
phase: 06-modal-konsistenz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/variables.css
  - frontend/src/components/chat/modals/GroupChatModal.tsx
  - frontend/src/components/chat/modals/DirectMessageModal.tsx
  - frontend/src/components/chat/modals/CreateChatModal.tsx
  - frontend/src/components/chat/modals/SimpleCreateChatModal.tsx
  - frontend/src/components/chat/modals/MembersModal.tsx
  - frontend/src/components/chat/ChatOverview.tsx
  - frontend/src/components/konfi/views/BadgesView.tsx
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/admin/views/KonfiDetailView.tsx
autonomous: true
requirements: [MOD-01, MOD-02, MOD-04]

must_haves:
  truths:
    - "Kein IonModal mit isOpen-Pattern existiert mehr im Codebase"
    - "Kein IonAlert mit isOpen-Pattern existiert mehr im Codebase"
    - "Kein IonPopover mit isOpen-Pattern existiert mehr im Codebase"
    - "Chat-Modals oeffnen und schliessen korrekt ueber useIonModal"
    - "Modal-spezifische CSS-Klassen existieren in variables.css"
  artifacts:
    - path: "frontend/src/theme/variables.css"
      provides: "app-modal-section, app-modal-section-header, app-modal-footer CSS-Klassen"
      contains: "app-modal-section"
    - path: "frontend/src/components/chat/ChatOverview.tsx"
      provides: "useIonModal Aufrufe fuer Chat-Modals statt isOpen-Pattern"
  key_links:
    - from: "frontend/src/components/chat/ChatOverview.tsx"
      to: "frontend/src/components/chat/modals/SimpleCreateChatModal.tsx"
      via: "useIonModal Hook"
      pattern: "useIonModal"
---

<objective>
Modal-spezifische CSS-Klassen erstellen und alle isOpen-Patterns (IonModal, IonAlert, IonPopover) auf Hook-Varianten migrieren.

Purpose: Grundlage fuer einheitliches Modal-Design schaffen und alle veralteten isOpen-Patterns eliminieren, damit keine inkompatiblen Modal-Oeffnungsmuster mehr existieren.
Output: CSS-Klassen in variables.css, 4 Chat-Modals auf useIonModal migriert, 4 Views mit isOpen-Alerts/Popovers auf useIonAlert/useIonPopover migriert.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-modal-konsistenz/06-CONTEXT.md

Referenz fuer bestehende CSS-Klassen:
@frontend/src/theme/variables.css

Referenz fuer bestehendes useIonModal-Pattern:
@frontend/src/components/chat/ChatOverview.tsx (bereits SimpleCreateChatModal via useIonModal)

isOpen-Bestandsaufnahme aus CONTEXT.md:
| Datei | isOpen-Typ | Aktion |
|-------|-----------|--------|
| GroupChatModal.tsx | IonModal | -> useIonModal |
| DirectMessageModal.tsx | IonModal | -> useIonModal |
| CreateChatModal.tsx | IonModal | -> useIonModal |
| SimpleCreateChatModal.tsx | IonAlert | -> useIonAlert |
| MembersModal.tsx | IonAlert | -> useIonAlert |
| BadgesView.tsx | IonPopover isOpen | -> useIonPopover |
| DashboardView.tsx | IonPopover isOpen (x2) | -> useIonPopover |
| KonfiDetailView.tsx | IonAlert | -> useIonAlert |

<interfaces>
Bestehendes useIonModal-Pattern in ChatOverview.tsx:
```typescript
const [presentChatModalHook, dismissChatModalHook] = useIonModal(SimpleCreateChatModal, {
  onClose: () => dismissChatModalHook(),
  onSuccess: () => {
    dismissChatModalHook();
    loadChatRooms();
  }
});
// Oeffnen: presentChatModalHook({ presentingElement: presentingElement })
```

Bestehende app-section-icon Klassen in variables.css:
```css
.app-section-icon { width: 24px; height: 24px; border-radius: 50%; ... }
.app-section-icon--events { background-color: #dc2626; }
.app-section-icon--chat { background-color: #06b6d4; }
/* etc. */
.app-section-icon ion-icon { color: white; font-size: 0.8rem; }
```

Aktuelle Chat-Modal Props (isOpen-Pattern):
```typescript
interface GroupChatModalProps {
  isOpen: boolean;
  onClose: () => void;
  onSuccess: () => void;
}
interface DirectMessageModalProps {
  isOpen: boolean;
  onClose: () => void;
  onSuccess: () => void;
}
interface CreateChatModalProps {
  isOpen: boolean;
  onClose: () => void;
  onSuccess: () => void;
}
```

Ziel-Props nach Migration (useIonModal-Pattern):
```typescript
interface GroupChatModalProps {
  onClose: () => void;
  onSuccess: () => void;
  dismiss?: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen fuer Modal-Sektionen erstellen</name>
  <files>frontend/src/theme/variables.css</files>
  <action>
  Neue Modal-spezifische CSS-Klassen in variables.css erstellen (am Ende des bestehenden Abschnitts, nach den vorhandenen app-* Klassen). NICHT die View-Klassen (app-info-row etc.) in Modals wiederverwenden -- klare Trennung per User-Decision.

  Folgende Klassen erstellen:

  1. `app-modal-section` - Container fuer eine Formular-Sektion in einem Modal
     - margin: 16px
     - (Ersetzt wiederholtes `style={{ margin: '16px' }}` auf IonList inset)

  2. `app-modal-section-header` - Flexbox-Container fuer Section-Header in Modals
     - display: flex, align-items: center, gap: 8px, padding: 0 16px, margin-bottom: 8px
     - h3 child: margin: 0, font-size: 0.95rem, font-weight: 600, color: #333

  3. `app-modal-footer` - Footer-Bereich fuer Modal-Buttons
     - padding: 16px, background: var(--ion-background-color, #f4f5f8)
     - ion-button child: --border-radius: 12px, font-weight: 600

  4. `app-modal-close-btn` - Einheitlicher X-Button Style
     - --background: #f8f9fa, --background-hover: #e9ecef, --color: #6c757d, --border-radius: 8px

  5. `app-modal-submit-btn` - Basis-Submit-Button (Farbe wird per Variante gesetzt)
     - --color: white, --border-radius: 8px

  6. Farb-Varianten fuer Submit-Buttons passend zu Domain-Farben:
     - `app-modal-submit-btn--events`: --background: #dc2626, --background-hover: #b91c1c
     - `app-modal-submit-btn--activities`: --background: #059669, --background-hover: #047857
     - `app-modal-submit-btn--badges`: --background: #f59e0b, --background-hover: #d97706
     - `app-modal-submit-btn--konfi`: --background: #5b21b6, --background-hover: #4c1d95
     - `app-modal-submit-btn--settings`: --background: #667eea, --background-hover: #5a6fd6
     - `app-modal-submit-btn--chat`: --background: #06b6d4, --background-hover: #0891b2

  7. `app-modal-info-text` - Info-Text innerhalb von Modal-Sektionen
     - font-size: 0.85rem, color: #666, line-height: 1.4, margin: 0

  WICHTIG: Bestehendes app-section-icon System wird weiterverwendet fuer die Icon-Kreise in Sektionen. KEINE neuen Icon-Klassen erstellen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "app-modal-section\|app-modal-footer\|app-modal-close-btn\|app-modal-submit-btn" src/theme/variables.css</automated>
  </verify>
  <done>Mindestens 7 neue app-modal-* CSS-Klassen existieren in variables.css mit allen Domain-Farb-Varianten</done>
</task>

<task type="auto">
  <name>Task 2: Chat-Modals von isOpen auf useIonModal migrieren und isOpen-Alerts/Popovers bereinigen</name>
  <files>
    frontend/src/components/chat/modals/GroupChatModal.tsx,
    frontend/src/components/chat/modals/DirectMessageModal.tsx,
    frontend/src/components/chat/modals/CreateChatModal.tsx,
    frontend/src/components/chat/modals/SimpleCreateChatModal.tsx,
    frontend/src/components/chat/modals/MembersModal.tsx,
    frontend/src/components/chat/ChatOverview.tsx,
    frontend/src/components/konfi/views/BadgesView.tsx,
    frontend/src/components/konfi/views/DashboardView.tsx,
    frontend/src/components/admin/views/KonfiDetailView.tsx
  </files>
  <action>
  **A) Chat-Modals auf useIonModal migrieren (4 Dateien):**

  Fuer GroupChatModal, DirectMessageModal und CreateChatModal:
  1. Props-Interface aendern: `isOpen: boolean` entfernen, `dismiss?: () => void` hinzufuegen
  2. Wrapper-`<IonModal isOpen={isOpen}>` entfernen -- das Modal IS die IonPage (useIonModal rendert es selbst)
  3. `useEffect(() => { if (isOpen) loadData() }, [isOpen])` ersetzen durch `useEffect(() => { loadData() }, [])` (Modal wird erst gerendert wenn es praesentiert wird)
  4. handleClose: `if (dismiss) dismiss() else onClose()` (wie EventModal-Pattern)
  5. IonModal-Import entfernen wenn nicht mehr benoetigt

  Fuer SimpleCreateChatModal:
  1. IonAlert isOpen={showDuplicateAlert} -> useIonAlert Hook
  2. Bestehende useIonModal-Nutzung (bereits via ChatOverview) beibehalten
  3. IonAlert-Import durch useIonAlert ersetzen

  **B) ChatOverview.tsx aktualisieren:**
  - GroupChatModal, DirectMessageModal, CreateChatModal: Als useIonModal-Hooks registrieren (wie SimpleCreateChatModal bereits gemacht ist)
  - Bestehende JSX-Elemente `<GroupChatModal isOpen={...}>` etc. entfernen
  - State-Variablen fuer isOpen entfernen (showGroupChat, showDirectMessage, showCreateChat etc.)
  - WICHTIG: Nach erfolgreicher Chat-Erstellung soll das Modal schliessen und Chat-Rooms neu laden (Navigation in den Chat-Room erfolgt ueber onSelectRoom -- pruefen ob das bestehende Pattern das schon macht)

  **C) MembersModal.tsx:**
  - IonAlert isOpen={showRemoveAlert} -> useIonAlert Hook
  - IonAlert-Import anpassen

  **D) BadgesView.tsx:**
  - IonPopover isOpen={!!selectedBadge} -> useIonPopover Hook
  - State selectedBadge und popoverEvent in useIonPopover-Aufruf integrieren
  - Popover-Inhalt als separate Komponente oder inline-Funktion fuer useIonPopover

  **E) DashboardView.tsx:**
  - 2x IonPopover isOpen={levelPopover.isOpen} / isOpen={badgePopover.isOpen} -> useIonPopover
  - State levelPopover und badgePopover vereinfachen

  **F) KonfiDetailView.tsx:**
  - IonAlert isOpen={showPasswordAlert} -> useIonAlert Hook

  KEINE Emojis in Code oder Strings! Echte Umlaute verwenden!
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30 && echo "---" && grep -rn "isOpen={" src/components/chat/modals/ src/components/konfi/views/BadgesView.tsx src/components/konfi/views/DashboardView.tsx src/components/admin/views/KonfiDetailView.tsx 2>/dev/null | grep -v "node_modules" | head -20</automated>
  </verify>
  <done>Null Treffer fuer isOpen={ in Modal-Kontexten (Chat-Modals, BadgesView, DashboardView, KonfiDetailView). TypeScript kompiliert fehlerfrei. Chat-Modals oeffnen und schliessen korrekt via useIonModal/useIonAlert/useIonPopover.</done>
</task>

</tasks>

<verification>
1. `grep -rn "isOpen={" frontend/src/components/ | grep -v "node_modules" | grep -v "IonToggle\|IonAccordion\|IonSelect"` liefert null Modal/Alert/Popover-Treffer
2. `npx tsc --noEmit` kompiliert fehlerfrei
3. `grep -c "app-modal-section\|app-modal-footer" frontend/src/theme/variables.css` zeigt mindestens 5 Klassen
4. `grep -c "useIonModal" frontend/src/components/chat/ChatOverview.tsx` zeigt mindestens 4 (SimpleCreateChat + GroupChat + DirectMessage + CreateChat)
</verification>

<success_criteria>
- Kein einziges IonModal/IonAlert/IonPopover mit isOpen-Pattern im gesamten Codebase (ausser IonToggle, IonAccordion und aehnliche Ionic-Komponenten die isOpen-Props haben)
- Alle Chat-Modals funktionieren ueber useIonModal mit korrektem Open/Close/Success-Lifecycle
- Modal-CSS-Klassen stehen fuer Plan 06-02 und 06-03 bereit
</success_criteria>

<output>
After completion, create `.planning/phases/06-modal-konsistenz/06-01-SUMMARY.md`
</output>
