---
phase: 04-admin-views-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/variables.css
  - frontend/src/components/layout/MainTabs.tsx
  - frontend/src/components/admin/pages/AdminSettingsPage.tsx
autonomous: true
requirements: [ADM-06]
# NOTE: ADM-04 (BadgesPage SectionHeader) und ADM-05 (JahrgaengePage SectionHeader)
# wurden bereits in Phase 3 Plan 03-03 implementiert (SectionHeader preset="badges"
# bzw. preset="jahrgang"). AdminBadgesPage.tsx wird hier nicht modifiziert, nur verlinkt.

must_haves:
  truths:
    - "CSS-Klassen fuer Status-Farben (app-status-chip--success/warning/danger/info), Suche (app-search-bar), Settings (app-settings-item) und Segment-Tabs (app-segment) existieren in variables.css"
    - "Admin-TabBar hat 5 Tabs: Konfis, Chat, Events, Antraege, Mehr (Badges-Tab entfernt)"
    - "AdminSettingsPage enthaelt einen neuen Inhalt-Block mit Badge-Verwaltung (Link zu /admin/badges)"
    - "AdminBadgesPage ist weiterhin ueber /admin/badges erreichbar (Route bleibt)"
    - "Bestehende Funktionalitaet aller Badges-Features ist unveraendert"
    - "AdminBadgesPage nutzt bereits SectionHeader mit preset='badges' und Stats-Row (Phase 3 Plan 03-03)"
    - "AdminJahrgaengeePage nutzt bereits SectionHeader mit preset='jahrgang' und Stats-Row (Phase 3 Plan 03-03)"
  artifacts:
    - path: "frontend/src/theme/variables.css"
      provides: "Erweiterte CSS-Klassen fuer Status, Suche, Settings, Segment-Tabs"
      contains: "app-status-chip--success"
    - path: "frontend/src/components/layout/MainTabs.tsx"
      provides: "Admin-TabBar mit 5 statt 6 Tabs"
    - path: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      provides: "Badge-Verwaltung Link im Inhalt-Block"
      contains: "admin/badges"
  key_links:
    - from: "frontend/src/components/admin/pages/AdminSettingsPage.tsx"
      to: "frontend/src/components/admin/pages/AdminBadgesPage.tsx"
      via: "history.push('/admin/badges') im Inhalt-Block"
      pattern: "admin/badges"
    - from: "frontend/src/components/layout/MainTabs.tsx"
      to: "frontend/src/components/admin/pages/AdminBadgesPage.tsx"
      via: "Route bleibt, Tab-Button entfernt"
      pattern: "Route.*admin/badges"
---

<objective>
CSS-Klassen in variables.css fuer wiederkehrende Inline-Style-Patterns erweitern, Admin-TabBar von 6 auf 5 Tabs reduzieren (Badges-Tab entfernen) und Badge-Verwaltung in die Settings-Seite integrieren.

Purpose: Fundament fuer die Inline-Style-Bereinigung in den Folgeplaenen legen und die Tab-Navigation vereinfachen.
Output: Erweiterte CSS-Klassen, reduzierte TabBar, Badge-Zugang ueber Settings.
</objective>

<execution_context>
@/Users/simonluthe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simonluthe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-views-core/04-CONTEXT.md

Referenz fuer bestehende Patterns:
@frontend/src/theme/variables.css
@frontend/src/components/layout/MainTabs.tsx
@frontend/src/components/admin/pages/AdminSettingsPage.tsx

<interfaces>
<!-- Bestehende CSS-Klassen in variables.css: -->
.app-list-item               -- Basis List-Item mit farbiger Border
.app-list-item--events/activities/konfis/users/organizations/badges/requests/jahrgang  -- Farbvarianten
.app-icon-circle              -- Runde Icon-Container
.app-corner-badge             -- Eselsohr Corner Badge
.app-header-banner            -- SectionHeader Container
.app-stats-row                -- Stats Row im Header
.app-card                     -- Karten-Container
.app-section-icon             -- Sektion-Icon vor Titel
.app-gradient-background      -- Seiten-Hintergrund

<!-- AdminSettingsPage Bloecke: -->
Block 1: "Verwaltung" (Benutzer, Punkte-Ziele, Konfis einladen)
Block 2: "System-Administration" (Organisationen) - nur super_admin
Block 3: "Inhalt" (Aktivitaeten, Kategorien, Jahrgaenge, Level-System)
Block 4: "Konto" (Profil, Push-Benachrichtigungen)
Abmelden Button

<!-- MainTabs Admin-Tabs (aktuell 6): -->
1. Konfis (/admin/konfis)
2. Chat (/admin/chat)
3. Events (/admin/events)
4. Badges (/admin/badges)
5. Antraege (/admin/requests)
6. Mehr (/admin/settings)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen fuer wiederkehrende Inline-Patterns erweitern</name>
  <files>
    frontend/src/theme/variables.css
  </files>
  <action>
Am Ende des Abschnitts "WIEDERVERWENDBARE APP KOMPONENTEN" in variables.css folgende neue CSS-Klassen hinzufuegen:

**A) Status-Chip Klassen** (ersetzen Inline-Style Status-Badges in EventDetailView etc.):
```css
/* === Status Chips === */
.app-status-chip {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 0.75rem;
  font-weight: 600;
  white-space: nowrap;
}
.app-status-chip--success { background: rgba(52, 199, 89, 0.15); color: #34c759; }
.app-status-chip--warning { background: rgba(255, 149, 0, 0.15); color: #ff9500; }
.app-status-chip--danger { background: rgba(220, 53, 69, 0.15); color: #dc3545; }
.app-status-chip--info { background: rgba(0, 122, 255, 0.15); color: #007aff; }
.app-status-chip--purple { background: rgba(91, 33, 182, 0.15); color: #5b21b6; }
.app-status-chip--neutral { background: rgba(142, 142, 147, 0.15); color: #8e8e93; }
```

**B) Suchleisten-Klasse** (ersetzen Inline-Style Suchleisten in Views):
```css
/* === Suchleiste === */
.app-search-bar {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 16px;
}
.app-search-bar__icon {
  color: #8e8e93;
  font-size: 1.2rem;
  flex-shrink: 0;
}
```

**C) Settings-Item Klasse** (fuer AdminSettingsPage und AdminProfilePage):
```css
/* === Settings Items === */
.app-settings-item {
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 12px;
}
.app-settings-item__title {
  font-weight: 500;
  font-size: 0.95rem;
  margin: 0;
  color: #333;
}
.app-settings-item__subtitle {
  font-size: 0.75rem;
  color: #8e8e93;
  margin: 2px 0 0 0;
}
```

**D) Segment-Tabs Klasse** (einheitliches Spacing fuer IonSegment Wrapper):
```css
/* === Segment Tabs === */
.app-segment-wrapper {
  margin: 16px;
}
```

**E) Info-Box Klasse** (fuer Info-Hinweise in Modals/Pages):
```css
/* === Info Box === */
.app-info-box {
  padding: 16px;
  font-size: 0.9rem;
  line-height: 1.5;
  margin: 0;
}
.app-info-box--blue {
  background: rgba(0, 122, 255, 0.08);
  border: 1px solid rgba(0, 122, 255, 0.2);
  color: #007aff;
}
.app-info-box--neutral {
  background: rgba(142, 142, 147, 0.08);
  border: 1px solid rgba(142, 142, 147, 0.2);
  color: #8e8e93;
}
```

**F) Progress-Bar Klassen** (fuer KonfisView Fortschrittsbalken):
```css
/* === Progress Bars === */
.app-progress-bar {
  height: 4px;
  border-radius: 2px;
  overflow: hidden;
}
.app-progress-bar--thick {
  height: 6px;
  border-radius: 3px;
}
.app-progress-bar__track {
  width: 100%;
  height: 100%;
  border-radius: inherit;
}
.app-progress-bar__fill {
  height: 100%;
  border-radius: inherit;
  transition: width 0.3s ease;
}
```

**G) Punkte-Display Klasse** (fuer Punkte-Anzeige rechts in List-Items):
```css
/* === Punkte Display === */
.app-points-display {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  justify-content: center;
  min-width: 50px;
}
.app-points-display__value {
  font-size: 1.4rem;
  font-weight: 800;
}
.app-points-display__target {
  font-size: 0.7rem;
  color: #999;
}
```

**H) Detail-Header Klasse** (fuer EventDetailView Status-basierter Header):
```css
/* === Detail Header === */
.app-detail-header {
  border-radius: 24px;
  padding: 0;
  margin: 16px;
  margin-bottom: 16px;
  position: relative;
  overflow: hidden;
  min-height: 200px;
  display: flex;
  flex-direction: column;
}
.app-detail-header__content {
  position: relative;
  z-index: 2;
  padding: 24px;
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
}
.app-detail-header__title {
  margin: 0 0 8px 0;
  font-size: 1.5rem;
  font-weight: 600;
  color: white;
}
.app-detail-header__subtitle {
  margin: 0;
  color: rgba(255, 255, 255, 0.8);
  font-size: 0.9rem;
}
.app-detail-header__info-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 12px;
}
.app-detail-header__info-chip {
  background: rgba(255, 255, 255, 0.15);
  border-radius: 8px;
  padding: 6px 10px;
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.95);
  display: flex;
  align-items: center;
  gap: 4px;
}
```

WICHTIG:
- Keine Emojis
- Kommentare in variables.css auf Englisch (bestehendes Pattern fortfuehren)
- Echte Umlaute in deutschen Texten (hier keine deutschen Texte noetig)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "app-status-chip--success" src/theme/variables.css && grep -c "app-search-bar" src/theme/variables.css && grep -c "app-settings-item" src/theme/variables.css && grep -c "app-segment-wrapper" src/theme/variables.css && grep -c "app-info-box" src/theme/variables.css && grep -c "app-progress-bar" src/theme/variables.css && grep -c "app-detail-header" src/theme/variables.css</automated>
  </verify>
  <done>Alle neuen CSS-Klassen (Status-Chips, Suchleiste, Settings-Items, Segment-Tabs, Info-Box, Progress-Bars, Punkte-Display, Detail-Header) sind in variables.css vorhanden. Bestehende Klassen sind unveraendert.</done>
</task>

<task type="auto">
  <name>Task 2: Admin-TabBar auf 5 Tabs reduzieren und Badges in Settings integrieren</name>
  <files>
    frontend/src/components/layout/MainTabs.tsx
    frontend/src/components/admin/pages/AdminSettingsPage.tsx
  </files>
  <action>
**A) MainTabs.tsx: Badges-Tab entfernen**

In der Admin-TabBar den Badges-Tab-Button komplett entfernen:
```tsx
{/* DIESEN BLOCK ENTFERNEN: */}
<IonTabButton tab="admin-badges" href="/admin/badges">
  <IonIcon icon={star} />
  <IonLabel>Badges</IonLabel>
</IonTabButton>
```

Die **Route** fuer `/admin/badges` BLEIBT bestehen (weiterhin erreichbar ueber Settings-Link):
```tsx
<Route exact path="/admin/badges" component={AdminBadgesPage} />
```

Danach hat die Admin-TabBar genau 5 Tabs:
1. Konfis (/admin/konfis)
2. Chat (/admin/chat)
3. Events (/admin/events)
4. Antraege (/admin/requests)
5. Mehr (/admin/settings)

Den `star` Import kann entfernt werden, wenn er nur fuer den Badge-Tab genutzt wurde. Pruefen ob `star` noch anderswo in MainTabs.tsx verwendet wird (Konfi-TabBar nutzt es fuer Badges-Tab).

**B) AdminSettingsPage.tsx: Badge-Verwaltung in Inhalt-Block integrieren**

Im Block "Inhalt" (IonList mit pricetag-Icon als Section-Icon) einen neuen Eintrag fuer Badges hinzufuegen. Der Eintrag kommt nach "Level-System" und vor dem schliessenden `</div>`:

```tsx
<div
  className="app-list-item app-list-item--badges"
  onClick={() => history.push('/admin/badges')}
  style={{ cursor: 'pointer', display: 'flex', alignItems: 'center', gap: '12px' }}
>
  <div className="app-icon-circle app-icon-circle--lg app-icon-circle--badges">
    <IonIcon icon={ribbon} />
  </div>
  <div style={{ flex: 1 }}>
    <h2 style={{ fontWeight: '500', fontSize: '0.95rem', margin: 0, color: '#333' }}>Badges</h2>
    <p style={{ fontSize: '0.75rem', color: '#8e8e93', margin: '2px 0 0 0' }}>Auszeichnungen und Erfolge verwalten</p>
  </div>
</div>
```

Dafuer muss `ribbon` aus ionicons/icons importiert werden (falls noch nicht vorhanden).

HINWEIS: Die bestehenden Inline-Styles in AdminSettingsPage.tsx werden hier NOCH NICHT bereinigt -- das ist Aufgabe von Plan 04-03. Hier wird nur der Badge-Link eingefuegt.

WICHTIG:
- Die Route `/admin/badges` in MainTabs.tsx NICHT entfernen -- nur den Tab-Button!
- Der `star` Import in MainTabs.tsx wird noch von der Konfi-TabBar gebraucht -- pruefen!
- Keine Emojis
- Echte Umlaute in deutschen UI-Texten
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -c "admin-badges.*href" src/components/layout/MainTabs.tsx && grep "admin/badges" src/components/admin/pages/AdminSettingsPage.tsx | head -3 && grep "Route.*admin/badges" src/components/layout/MainTabs.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Admin-TabBar hat genau 5 Tab-Buttons (kein Badges-Tab mehr). Die Route /admin/badges existiert weiterhin in MainTabs.tsx. AdminSettingsPage enthaelt einen Badge-Verwaltungs-Link im Inhalt-Block. TypeScript kompiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
1. `grep -c "IonTabButton" frontend/src/components/layout/MainTabs.tsx` im Admin-Block liefert 5 (nicht 6)
2. `grep "admin/badges" frontend/src/components/admin/pages/AdminSettingsPage.tsx` liefert Treffer
3. `grep "Route.*admin/badges" frontend/src/components/layout/MainTabs.tsx` liefert Treffer (Route erhalten)
4. `grep "app-status-chip" frontend/src/theme/variables.css` liefert Treffer
5. `npx tsc --noEmit` kompiliert ohne Fehler
</verification>

<success_criteria>
- variables.css enthaelt alle neuen CSS-Klassen: Status-Chips, Suchleiste, Settings-Items, Segment-Tabs, Info-Box, Progress-Bars, Detail-Header
- Admin-TabBar hat 5 Tabs (Badges-Tab entfernt)
- Badge-Verwaltung ist ueber AdminSettingsPage erreichbar
- Route /admin/badges bleibt funktionsfaehig
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-views-core/04-01-SUMMARY.md`
</output>
