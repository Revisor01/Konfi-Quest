---
phase: 55-fundament
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - frontend/src/contexts/LiveUpdateContext.tsx
  - frontend/src/contexts/BadgeContext.tsx
  - frontend/src/components/chat/ChatRoom.tsx
  - frontend/src/components/chat/ChatOverview.tsx
  - frontend/src/components/auth/KonfiRegisterPage.tsx
  - frontend/src/components/konfi/views/ProfileView.tsx
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  - frontend/src/components/admin/pages/AdminProfilePage.tsx
  - frontend/src/components/admin/pages/AdminSettingsPage.tsx
autonomous: true
requirements: [STR-04]

must_haves:
  truths:
    - "Kein einziger localStorage-Zugriff fuer Auth-Daten mehr im gesamten Frontend"
    - "Alle Komponenten lesen Token und User aus TokenStore statt localStorage"
    - "Frontend baut ohne TypeScript-Fehler"
  artifacts:
    - path: "frontend/src/contexts/LiveUpdateContext.tsx"
      provides: "WebSocket-Init via getToken()"
      contains: "getToken"
    - path: "frontend/src/contexts/BadgeContext.tsx"
      provides: "Badge-Refresh via getToken()"
      contains: "getToken"
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "Chat-Socket via getToken()"
      contains: "getToken"
    - path: "frontend/src/components/chat/ChatOverview.tsx"
      provides: "Chat-Overview-Socket via getToken()"
      contains: "getToken"
  key_links:
    - from: "frontend/src/contexts/LiveUpdateContext.tsx"
      to: "frontend/src/services/tokenStore.ts"
      via: "getToken() fuer WebSocket-Init"
      pattern: "getToken\\(\\)"
    - from: "frontend/src/contexts/BadgeContext.tsx"
      to: "frontend/src/services/tokenStore.ts"
      via: "getToken() fuer WebSocket-Init"
      pattern: "getToken\\(\\)"
---

<objective>
Alle verbleibenden localStorage-Zugriffe (9 Dateien) auf TokenStore migrieren. Danach hat die gesamte Codebase keinen localStorage-Zugriff mehr fuer Auth-Daten.

Purpose: STR-04 verlangt dass ALLE 28 localStorage-Zugriffe ersetzt werden. Plan 01 hat die Core-Services migriert, dieser Plan migriert den Rest.
Output: 9 aktualisierte Dateien ohne localStorage-Zugriffe
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-fundament/55-CONTEXT.md
@.planning/phases/55-fundament/55-01-SUMMARY.md

<interfaces>
From frontend/src/services/tokenStore.ts (erstellt in Plan 01):
```typescript
export function getToken(): string | null;
export function getUser(): User | null;
export function getDeviceId(): string | null;
export function getPushTokenTimestamp(): number;
export function setToken(token: string): Promise<void>;
export function setUser(user: User): Promise<void>;
export function setDeviceId(id: string): Promise<void>;
export function setPushTokenTimestamp(ts: number): Promise<void>;
export function clearAuth(): Promise<void>;
export function initTokenStore(): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contexts + Chat-Komponenten auf TokenStore migrieren</name>
  <files>
    frontend/src/contexts/LiveUpdateContext.tsx,
    frontend/src/contexts/BadgeContext.tsx,
    frontend/src/components/chat/ChatRoom.tsx,
    frontend/src/components/chat/ChatOverview.tsx
  </files>
  <read_first>
    frontend/src/services/tokenStore.ts,
    frontend/src/contexts/LiveUpdateContext.tsx,
    frontend/src/contexts/BadgeContext.tsx,
    frontend/src/components/chat/ChatRoom.tsx,
    frontend/src/components/chat/ChatOverview.tsx
  </read_first>
  <action>
1. `frontend/src/contexts/LiveUpdateContext.tsx`:
   - Import hinzufuegen: `import { getToken } from '../services/tokenStore';`
   - Zeile 43: Ersetze `const token = localStorage.getItem('konfi_token');` durch `const token = getToken();`

2. `frontend/src/contexts/BadgeContext.tsx`:
   - Import hinzufuegen: `import { getToken } from '../services/tokenStore';`
   - Zeile 125 (ca.): Ersetze `const token = localStorage.getItem('konfi_token');` durch `const token = getToken();`

3. `frontend/src/components/chat/ChatRoom.tsx`:
   - Import hinzufuegen: `import { getToken } from '../../services/tokenStore';`
   - Zeile 205 (ca.): Ersetze `const token = localStorage.getItem('konfi_token');` durch `const token = getToken();`

4. `frontend/src/components/chat/ChatOverview.tsx`:
   - Import hinzufuegen: `import { getToken } from '../../services/tokenStore';`
   - Zeile 90 (ca.): Ersetze `const token = localStorage.getItem('konfi_token');` durch `const token = getToken();`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && ! grep -n "localStorage" src/contexts/LiveUpdateContext.tsx && ! grep -n "localStorage" src/contexts/BadgeContext.tsx && ! grep -n "localStorage" src/components/chat/ChatRoom.tsx && ! grep -n "localStorage" src/components/chat/ChatOverview.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - LiveUpdateContext.tsx enthaelt `getToken()` statt `localStorage.getItem('konfi_token')`
    - LiveUpdateContext.tsx enthaelt KEINEN `localStorage` Zugriff
    - BadgeContext.tsx enthaelt `getToken()` statt `localStorage.getItem('konfi_token')`
    - BadgeContext.tsx enthaelt KEINEN `localStorage` Zugriff
    - ChatRoom.tsx enthaelt `getToken()` statt `localStorage.getItem('konfi_token')`
    - ChatRoom.tsx enthaelt KEINEN `localStorage` Zugriff
    - ChatOverview.tsx enthaelt `getToken()` statt `localStorage.getItem('konfi_token')`
    - ChatOverview.tsx enthaelt KEINEN `localStorage` Zugriff
  </acceptance_criteria>
  <done>4 Dateien (2 Contexts + 2 Chat-Komponenten) verwenden getToken() aus TokenStore. Kein localStorage mehr.</done>
</task>

<task type="auto">
  <name>Task 2: Auth-Pages + Profile-Pages auf TokenStore migrieren + Build-Check</name>
  <files>
    frontend/src/components/auth/KonfiRegisterPage.tsx,
    frontend/src/components/konfi/views/ProfileView.tsx,
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx,
    frontend/src/components/admin/pages/AdminProfilePage.tsx,
    frontend/src/components/admin/pages/AdminSettingsPage.tsx
  </files>
  <read_first>
    frontend/src/services/tokenStore.ts,
    frontend/src/components/auth/KonfiRegisterPage.tsx,
    frontend/src/components/konfi/views/ProfileView.tsx,
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx,
    frontend/src/components/admin/pages/AdminProfilePage.tsx,
    frontend/src/components/admin/pages/AdminSettingsPage.tsx
  </read_first>
  <action>
1. `frontend/src/components/auth/KonfiRegisterPage.tsx`:
   - Import: `import { setToken, setUser } from '../../services/tokenStore';`
   - Zeile 236: Ersetze `localStorage.setItem('konfi_token', token)` durch `await setToken(token)`
   - Zeile 237: Ersetze `localStorage.setItem('konfi_user', JSON.stringify(user))` durch `await setUser(user)`
   - Sicherstellen dass die umgebende Funktion async ist (sollte bereits der Fall sein bei API-Call)

2. `frontend/src/components/konfi/views/ProfileView.tsx`:
   - Import: `import { clearAuth } from '../../services/tokenStore';`
   - Zeile 163-164: Ersetze `localStorage.removeItem('konfi_token')` und `localStorage.removeItem('konfi_user')` durch `await clearAuth()`
   - Falls die umgebende Funktion nicht async ist, in Promise wrappen oder `.then()` verwenden

3. `frontend/src/components/teamer/pages/TeamerProfilePage.tsx`:
   - Import: `import { setUser, clearAuth } from '../../services/tokenStore';`
   - Zeile 101: Ersetze `localStorage.setItem('konfi_user', JSON.stringify(updatedUser))` durch `await setUser(updatedUser)`
   - Zeile 148-149: Ersetze `localStorage.removeItem('konfi_token')` und `localStorage.removeItem('konfi_user')` durch `await clearAuth()`

4. `frontend/src/components/admin/pages/AdminProfilePage.tsx`:
   - Import: `import { setUser } from '../../services/tokenStore';`
   - Zeile 67: Ersetze `localStorage.setItem('konfi_user', JSON.stringify(updatedUser))` durch `await setUser(updatedUser)`

5. `frontend/src/components/admin/pages/AdminSettingsPage.tsx`:
   - Import: `import { clearAuth } from '../../services/tokenStore';`
   - Zeile 67-68: Ersetze `localStorage.removeItem('konfi_token')` und `localStorage.removeItem('konfi_user')` durch `await clearAuth()`

6. Nach allen Aenderungen: `cd frontend && npx tsc --noEmit` ausfuehren um TypeScript-Fehler zu pruefen. Bei Fehlern sofort fixen.

7. Finale Pruefung: `grep -rn "localStorage" frontend/src/` — sollte KEINE Treffer mehr geben (ausser evtl. Kommentare in migrateStorage.ts)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && ! grep -rn "localStorage\." src/ --include="*.ts" --include="*.tsx" | grep -v "migrateStorage" | grep -v "// " | grep -v "console\." && npx tsc --noEmit 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - KonfiRegisterPage.tsx verwendet `setToken()` und `setUser()` statt localStorage
    - ProfileView.tsx verwendet `clearAuth()` statt localStorage.removeItem
    - TeamerProfilePage.tsx verwendet `setUser()` und `clearAuth()` statt localStorage
    - AdminProfilePage.tsx verwendet `setUser()` statt localStorage
    - AdminSettingsPage.tsx verwendet `clearAuth()` statt localStorage
    - `grep -rn "localStorage\." frontend/src/ --include="*.ts" --include="*.tsx"` findet NUR noch Treffer in migrateStorage.ts (und evtl. Kommentare)
    - `npx tsc --noEmit` laeuft ohne Fehler durch
  </acceptance_criteria>
  <done>Alle 14 Dateien mit localStorage-Zugriff sind auf TokenStore migriert. TypeScript-Build erfolgreich. Kein localStorage mehr fuer Auth-Daten.</done>
</task>

</tasks>

<verification>
- `grep -rn "localStorage" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v migrateStorage | grep -v "//"` gibt keine funktionalen Treffer
- `cd frontend && npx tsc --noEmit` laeuft ohne Fehler
- Alle 9 Dateien importieren aus tokenStore
</verification>

<success_criteria>
- Null funktionale localStorage-Zugriffe fuer Auth-Daten im gesamten Frontend (ausser migrateStorage.ts)
- TypeScript baut fehlerfrei
- STR-04 vollstaendig erfuellt (alle 28 Zugriffe in 14 Dateien migriert)
</success_criteria>

<output>
After completion, create `.planning/phases/55-fundament/55-02-SUMMARY.md`
</output>
