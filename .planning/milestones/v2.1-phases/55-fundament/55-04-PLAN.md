---
phase: 55-fundament
plan: 04
type: execute
wave: 2
depends_on: ["55-01", "55-03"]
files_modified:
  - backend/routes/chat.js
  - frontend/src/services/websocket.ts
  - frontend/src/components/chat/ChatRoom.tsx
  - frontend/src/components/chat/ChatOverview.tsx
autonomous: true
requirements: [NET-03]

must_haves:
  truths:
    - "Nach einer Offline-Phase laedt Socket.io verpasste Chat-Nachrichten automatisch nach"
    - "Backend Chat-Route unterstuetzt ?after=lastMessageId Parameter"
    - "ChatRoom und ChatOverview laden bei Reconnect nur verpasste Nachrichten"
  artifacts:
    - path: "backend/routes/chat.js"
      provides: "?after=lastMessageId Filterung in GET /rooms/:roomId/messages"
      contains: "after"
    - path: "frontend/src/services/websocket.ts"
      provides: "Reconnect-Event das Listener benachrichtigt"
      contains: "reconnect"
    - path: "frontend/src/components/chat/ChatRoom.tsx"
      provides: "Reconnect-Handler der verpasste Nachrichten nachlaedt"
      contains: "after"
  key_links:
    - from: "frontend/src/components/chat/ChatRoom.tsx"
      to: "backend/routes/chat.js"
      via: "GET /chat/rooms/:id/messages?after=lastMessageId"
      pattern: "after=.*lastMessage"
    - from: "frontend/src/services/websocket.ts"
      to: "frontend/src/components/chat/ChatRoom.tsx"
      via: "Socket connect Event triggert Chat-Reload"
      pattern: "socket.*on.*connect"
---

<objective>
Backend Chat-Route um ?after Parameter erweitern und Frontend Chat-Komponenten bei Socket.io-Reconnect verpasste Nachrichten nachladen lassen.

Purpose: Nach einer Offline-Phase soll die App nur verpasste Chat-Nachrichten laden statt den gesamten Chat-Verlauf. Das ist die Grundlage fuer effizienten Sync in Phase 62.
Output: Backend ?after-Filter, Frontend Reconnect-Handler in ChatRoom + ChatOverview
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-fundament/55-CONTEXT.md
@.planning/phases/55-fundament/55-01-SUMMARY.md
@.planning/phases/55-fundament/55-03-SUMMARY.md
@backend/routes/chat.js
@frontend/src/services/websocket.ts
@frontend/src/components/chat/ChatRoom.tsx
@frontend/src/components/chat/ChatOverview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend ?after Parameter + Frontend websocket.ts Reconnect-Event</name>
  <files>
    backend/routes/chat.js,
    frontend/src/services/websocket.ts
  </files>
  <read_first>
    backend/routes/chat.js,
    frontend/src/services/websocket.ts
  </read_first>
  <action>
1. `backend/routes/chat.js` — GET `/rooms/:roomId/messages` Route erweitern (per D-17):
   - Aktuell (ca. Zeile 528): `const limit = parseInt(req.query.limit) || 50;` und `const offset = parseInt(req.query.offset) || 0;`
   - Neuen Query-Parameter parsen: `const after = parseInt(req.query.after) || null;` (Message-ID, nicht Timestamp)
   - Wenn `after` vorhanden ist, SQL-Query anpassen — statt LIMIT/OFFSET:
     ```sql
     WHERE m.room_id = $1 AND m.id > $2
     ORDER BY m.created_at ASC
     ```
     mit Parametern `[roomId, after]`
   - Wenn `after` NICHT vorhanden: bestehendes Verhalten beibehalten (LIMIT/OFFSET, DESC, reverse am Ende)
   - Implementierung: VOR der bestehenden messagesQuery eine Bedingung einfuegen:
     ```javascript
     let messagesQuery, queryParams;
     if (after) {
       messagesQuery = `
         SELECT m.*, /* ... gleiche Spalten wie bestehende Query ... */
         FROM chat_messages m
         LEFT JOIN users u ON m.user_id = u.id
         LEFT JOIN roles ro ON u.role_id = ro.id
         LEFT JOIN chat_polls p ON m.id = p.message_id
         LEFT JOIN chat_messages reply_msg ON m.reply_to = reply_msg.id
         LEFT JOIN users reply_user ON reply_msg.user_id = reply_user.id
         WHERE m.room_id = $1 AND m.id > $2
         ORDER BY m.created_at ASC
         LIMIT 200
       `;
       queryParams = [roomId, after];
     } else {
       // bestehende Query unveraendert
       messagesQuery = `...`;
       queryParams = [roomId, limit, offset];
     }
     ```
   - LIMIT 200 als Sicherheitsgrenze fuer ?after (bei sehr langer Offline-Phase nicht alle Nachrichten auf einmal laden)
   - Die Verarbeitung (processedMessages, reactions, polls) bleibt gleich
   - Bei ?after: NICHT `.reverse()` am Ende — Query ist schon ASC sortiert

2. `frontend/src/services/websocket.ts` erweitern:
   - Reconnect-Callback-System hinzufuegen (per D-18):
     ```typescript
     type ReconnectCallback = () => void;
     const reconnectCallbacks: Set<ReconnectCallback> = new Set();

     export const onReconnect = (callback: ReconnectCallback): (() => void) => {
       reconnectCallbacks.add(callback);
       return () => { reconnectCallbacks.delete(callback); };
     };
     ```
   - Im `socket.on('connect', ...)` Callback (ca. Zeile 24): Reconnect-Callbacks ausfuehren. Aber NUR bei RECONNECT, nicht beim ersten Connect. Dafuer:
     ```typescript
     let _hasConnectedOnce = false;

     socket.on('connect', () => {
       if (_hasConnectedOnce) {
         // Reconnect — Callbacks benachrichtigen
         reconnectCallbacks.forEach(cb => {
           try { cb(); } catch (e) { console.error('Reconnect callback error:', e); }
         });
       }
       _hasConnectedOnce = true;
     });
     ```
   - `disconnectWebSocket` zuruecksetzen: `_hasConnectedOnce = false;` hinzufuegen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -q "after" backend/routes/chat.js && grep -q "req.query.after" backend/routes/chat.js && grep -q "onReconnect" frontend/src/services/websocket.ts && grep -q "_hasConnectedOnce" frontend/src/services/websocket.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/routes/chat.js parst `req.query.after` als Integer
    - backend/routes/chat.js hat eine Bedingung `if (after)` die eine andere SQL-Query ausfuehrt mit `WHERE m.id > $2`
    - backend/routes/chat.js ?after Query ist ASC sortiert (nicht DESC)
    - backend/routes/chat.js ?after Query hat LIMIT 200 als Sicherheitsgrenze
    - websocket.ts exportiert `onReconnect` Funktion
    - websocket.ts unterscheidet ersten Connect von Reconnect via `_hasConnectedOnce`
    - websocket.ts ruft reconnectCallbacks nur bei Reconnect auf (nicht beim ersten Connect)
    - websocket.ts setzt `_hasConnectedOnce = false` in `disconnectWebSocket`
  </acceptance_criteria>
  <done>Backend unterstuetzt ?after=lastMessageId. websocket.ts hat Reconnect-Callback-System.</done>
</task>

<task type="auto">
  <name>Task 2: ChatRoom + ChatOverview Reconnect-Handler</name>
  <files>
    frontend/src/components/chat/ChatRoom.tsx,
    frontend/src/components/chat/ChatOverview.tsx
  </files>
  <read_first>
    frontend/src/services/websocket.ts,
    frontend/src/components/chat/ChatRoom.tsx,
    frontend/src/components/chat/ChatOverview.tsx
  </read_first>
  <action>
1. `frontend/src/components/chat/ChatRoom.tsx` erweitern (per D-18):
   - Import: `import { onReconnect } from '../../services/websocket';`
   - In der Komponente (innerhalb des Haupteffects oder als eigener useEffect):
     ```typescript
     useEffect(() => {
       const unsubReconnect = onReconnect(() => {
         // Verpasste Nachrichten nachladen
         if (messages.length > 0) {
           const lastMessageId = messages[messages.length - 1].id;
           loadMissedMessages(lastMessageId);
         } else {
           // Kein letztes Message — normales Laden
           loadMessages();
         }
       });
       return () => { unsubReconnect(); };
     }, [messages]); // messages dependency fuer aktuelle lastMessageId
     ```
   - Neue Funktion `loadMissedMessages(afterId: number)`:
     ```typescript
     const loadMissedMessages = async (afterId: number) => {
       try {
         const response = await api.get(`/chat/rooms/${roomId}/messages?after=${afterId}`);
         const missedMessages = response.data;
         if (missedMessages.length > 0) {
           setMessages(prev => [...prev, ...missedMessages]);
         }
       } catch (err) {
         console.error('Fehler beim Nachladen verpasster Nachrichten:', err);
       }
     };
     ```
   - ACHTUNG: `messages` in der Dependency kann zu haeufigen Re-Subscriptions fuehren. Besser: `useRef` fuer messages-Referenz:
     ```typescript
     const messagesRef = useRef(messages);
     useEffect(() => { messagesRef.current = messages; }, [messages]);

     useEffect(() => {
       const unsubReconnect = onReconnect(() => {
         const currentMessages = messagesRef.current;
         if (currentMessages.length > 0) {
           const lastId = currentMessages[currentMessages.length - 1].id;
           loadMissedMessages(lastId);
         } else {
           loadMessages();
         }
       });
       return () => { unsubReconnect(); };
     }, [roomId]); // Nur roomId als Dependency
     ```

2. `frontend/src/components/chat/ChatOverview.tsx` erweitern (per D-18):
   - Import: `import { onReconnect } from '../../services/websocket';`
   - useEffect fuer Reconnect — hier einfacher, da Overview nur die Raumliste braucht:
     ```typescript
     useEffect(() => {
       const unsubReconnect = onReconnect(() => {
         // Bei Reconnect komplette Raumliste neu laden (kleine Datenmenge)
         loadRooms();
       });
       return () => { unsubReconnect(); };
     }, []);
     ```
   - `loadRooms` ist die bestehende Funktion die `GET /chat/rooms` aufruft — einfach erneut aufrufen reicht hier.
   - KEIN ?after Parameter fuer Rooms noetig — nur fuer Messages innerhalb eines Rooms.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && grep -q "onReconnect" src/components/chat/ChatRoom.tsx && grep -q "onReconnect" src/components/chat/ChatOverview.tsx && grep -q "after=" src/components/chat/ChatRoom.tsx && grep -q "loadMissedMessages\|missedMessages" src/components/chat/ChatRoom.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ChatRoom.tsx importiert `onReconnect` aus websocket.ts
    - ChatRoom.tsx hat eine `loadMissedMessages` Funktion die `?after=` Parameter verwendet
    - ChatRoom.tsx registriert einen onReconnect-Callback der loadMissedMessages aufruft
    - ChatRoom.tsx verwendet useRef fuer messages-Referenz (verhindert haeufige Re-Subscriptions)
    - ChatRoom.tsx haengt neue Nachrichten an bestehende an (`[...prev, ...missedMessages]`), nicht ersetzen
    - ChatOverview.tsx importiert `onReconnect` aus websocket.ts
    - ChatOverview.tsx laedt bei Reconnect die Raumliste neu (loadRooms Aufruf)
    - ChatOverview.tsx Reconnect-useEffect hat leeres Dependency-Array `[]`
  </acceptance_criteria>
  <done>ChatRoom laedt verpasste Nachrichten via ?after Parameter bei Reconnect. ChatOverview laedt Raumliste neu. Keine verpassten Chat-Nachrichten mehr nach Offline-Phase.</done>
</task>

</tasks>

<verification>
- Backend: `grep -q "req.query.after" backend/routes/chat.js` findet after-Parameter
- Frontend: `grep -q "onReconnect" frontend/src/components/chat/ChatRoom.tsx` findet Reconnect-Handler
- Frontend: `grep -q "after=" frontend/src/components/chat/ChatRoom.tsx` findet after-Nutzung
- Build: `cd frontend && npx tsc --noEmit` laeuft ohne Fehler
</verification>

<success_criteria>
- Backend Chat-Route gibt bei ?after=ID nur neuere Nachrichten zurueck (ASC, LIMIT 200)
- Socket.io Reconnect triggert Chat-Nachladen im Frontend
- ChatRoom laedt nur verpasste Nachrichten (inkrementell, nicht komplett)
- ChatOverview laedt Raumliste bei Reconnect komplett neu
</success_criteria>

<output>
After completion, create `.planning/phases/55-fundament/55-04-SUMMARY.md`
</output>
