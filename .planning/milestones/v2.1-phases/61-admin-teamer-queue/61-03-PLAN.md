---
phase: 61-admin-teamer-queue
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
autonomous: true
requirements: [QUE-T01, QUE-T02]

must_haves:
  truths:
    - "Teamer kann offline ein Event buchen — Buchung wird bei Reconnect automatisch gesendet"
    - "Teamer kann sich offline von einem Event abmelden — Abmeldung wird bei Reconnect automatisch gesendet"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerEventsPage.tsx"
      provides: "Queue-Fallback fuer Event buchen + abmelden"
      contains: "writeQueue.enqueue"
  key_links:
    - from: "TeamerEventsPage.tsx"
      to: "writeQueue"
      via: "import { writeQueue }"
      pattern: "writeQueue\\.enqueue"
---

<objective>
TeamerEventsPage queue-faehig machen: Event buchen (POST /events/{id}/book) und Event abmelden (DELETE /events/{id}/book) funktionieren offline.

Purpose: Teamer koennen ihre Event-Teilnahme auch ohne Netzwerk verwalten.
Output: 1 Datei mit Online/Offline-Branching in handleBook und handleUnbook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-admin-teamer-queue/61-CONTEXT.md

<interfaces>
<!-- writeQueue API -->
```typescript
import { writeQueue } from '../../../services/writeQueue';
import { networkMonitor } from '../../../services/networkMonitor';
// networkMonitor.isOnline: boolean (synchron)
// writeQueue.enqueue({method, url, body, maxRetries, hasFileUpload, metadata}): Promise<QueueItem>
```

<!-- Aktuelle Handler in TeamerEventsPage.tsx: -->
```typescript
// Zeile ~271:
const handleBook = async (event: Event) => {
  setBookingLoading(true);
  try {
    await api.post(`/events/${event.id}/book`);
    setSuccess('Du bist dabei!');
    await refresh();
    const updated = (await api.get('/events')).data.find((e: Event) => e.id === event.id);
    if (updated) setSelectedEvent(updated);
  } catch (err: any) {
    setError(err.response?.data?.error || 'Fehler bei der Buchung');
  } finally {
    setBookingLoading(false);
  }
};

// Zeile ~287:
const handleUnbook = async (event: Event) => {
  setBookingLoading(true);
  try {
    await api.delete(`/events/${event.id}/book`);
    setSuccess('Du bist nicht mehr dabei');
    await refresh();
    const updated = (await api.get('/events')).data.find((e: Event) => e.id === event.id);
    if (updated) setSelectedEvent(updated);
  } catch (err: any) {
    setError(err.response?.data?.error || 'Fehler beim Stornieren');
  } finally {
    setBookingLoading(false);
  }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeamerEventsPage handleBook + handleUnbook queue-faehig</name>
  <files>
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  </files>
  <read_first>
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx
    frontend/src/services/writeQueue.ts (Zeilen 197-210, enqueue Signatur)
  </read_first>
  <action>
    1. Imports hinzufuegen:
       - `import { writeQueue } from '../../../services/writeQueue';`
       - `import { networkMonitor } from '../../../services/networkMonitor';`

    2. handleBook umbauen (QUE-T01):
       - `if (networkMonitor.isOnline) { ... bestehender try/catch ... } else { ... }`
       - Offline-Pfad:
         ```
         await writeQueue.enqueue({
           method: 'POST',
           url: `/events/${event.id}/book`,
           body: {},
           maxRetries: 5,
           hasFileUpload: false,
           metadata: {
             type: 'teamer',
             clientId: crypto.randomUUID(),
             label: 'Event buchen',
           },
         });
         setSuccess('Buchung wird gesendet sobald du wieder online bist');
         ```
       - KEIN refresh/selectedEvent-Update im Offline-Pfad (Daten kommen erst nach Flush)
       - setBookingLoading(false) im finally bleibt fuer beide Pfade

    3. handleUnbook umbauen (QUE-T02):
       - Gleiches Pattern wie handleBook
       - Offline-Pfad:
         ```
         await writeQueue.enqueue({
           method: 'DELETE',
           url: `/events/${event.id}/book`,
           body: {},
           maxRetries: 5,
           hasFileUpload: false,
           metadata: {
             type: 'teamer',
             clientId: crypto.randomUUID(),
             label: 'Event abmelden',
           },
         });
         setSuccess('Abmeldung wird gesendet sobald du wieder online bist');
         ```

    4. Booking-Buttons: Diese haben aktuell KEIN `!isOnline` disabled (TeamerEventsPage hatte keine Offline-Guards aus Phase 59). Das ist gut — Buttons bleiben klickbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "writeQueue.enqueue" frontend/src/components/teamer/pages/TeamerEventsPage.tsx liefert mindestens 2 (book + unbook)
    - grep -c "networkMonitor.isOnline" frontend/src/components/teamer/pages/TeamerEventsPage.tsx liefert mindestens 2
    - grep "type: 'teamer'" frontend/src/components/teamer/pages/TeamerEventsPage.tsx liefert mindestens 2 Treffer
    - TypeScript kompiliert fehlerfrei
  </acceptance_criteria>
  <done>TeamerEventsPage handleBook und handleUnbook haben Online/Offline-Branching. Buchung und Abmeldung werden offline via WriteQueue gequeued.</done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei: `cd frontend && npx tsc --noEmit`
- TeamerEventsPage.tsx enthaelt `writeQueue.enqueue` mindestens 2x
- TeamerEventsPage.tsx enthaelt `networkMonitor.isOnline` mindestens 2x
- TeamerEventsPage.tsx enthaelt metadata type 'teamer'
</verification>

<success_criteria>
2 Requirements abgedeckt: QUE-T01, QUE-T02.
Teamer kann Events offline buchen und sich abmelden.
</success_criteria>

<output>
After completion, create `.planning/phases/61-admin-teamer-queue/61-03-SUMMARY.md`
</output>
