---
phase: 56-lese-cache
plan: 04
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
  - frontend/src/components/teamer/pages/TeamerEventsPage.tsx
  - frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  - frontend/src/components/teamer/pages/TeamerProfilePage.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
  - frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
  - frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
autonomous: true
requirements: [CAC-08, CAC-09]

must_haves:
  truths:
    - "Teamer sieht Dashboard (Punkte, Stats, Tageslosung) auch offline"
    - "Teamer sieht Events-Liste auch offline"
    - "Teamer sieht Badge-Liste auch offline"
    - "Teamer sieht Profil auch offline"
    - "Teamer sieht Material-Liste (Metadaten) auch offline"
    - "Teamer sieht Konfi-Stats auch offline"
    - "Alle Teamer-Pages zeigen gecachte Daten sofort, revalidieren im Hintergrund"
  artifacts:
    - path: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      provides: "Dashboard mit useOfflineQuery"
      contains: "useOfflineQuery"
    - path: "frontend/src/components/teamer/pages/TeamerMaterialPage.tsx"
      provides: "Material-Liste mit useOfflineQuery"
      contains: "useOfflineQuery"
  key_links:
    - from: "frontend/src/components/teamer/pages/TeamerDashboardPage.tsx"
      to: "frontend/src/hooks/useOfflineQuery.ts"
      via: "import useOfflineQuery"
      pattern: "useOfflineQuery.*teamer:dashboard"
---

<objective>
Alle 7 Teamer-Pages auf useOfflineQuery migrieren — Teamer sehen Material, Badges, Events und Stats auch offline.

Purpose: Teamer nutzen die App oft unterwegs bei Gruppenaktivitaeten wo Netz schlecht sein kann. Material-Liste und Konfi-Stats muessen offline verfuegbar sein.
Output: 7 migrierte Teamer-Page-Dateien
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-lese-cache/56-CONTEXT.md
@.planning/phases/56-lese-cache/56-01-SUMMARY.md
@.planning/research/DEEP-OFFLINE-ANALYSIS.md (Section 1 fuer Teamer-Page-Details)

<interfaces>
From frontend/src/hooks/useOfflineQuery.ts:
```typescript
export function useOfflineQuery<T>(
  cacheKey: string,
  fetcher: () => Promise<T>,
  options?: { ttl?: number; enabled?: boolean; onSuccess?: (data: T) => void; select?: (data: T) => T; }
): { data: T | null; loading: boolean; error: string | null; isStale: boolean; isOffline: boolean; refresh: () => Promise<void>; };
```

From frontend/src/services/offlineCache.ts:
```typescript
export const CACHE_TTL = {
  DASHBOARD: 5 * 60 * 1000, EVENTS: 10 * 60 * 1000, STAMMDATEN: 60 * 60 * 1000,
  PROFILE: 15 * 60 * 1000, BADGES: 30 * 60 * 1000, TAGESLOSUNG: 24 * 60 * 60 * 1000,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teamer Dashboard + Events + Badges migrieren</name>
  <files>
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx
    frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  </files>
  <read_first>
    frontend/src/hooks/useOfflineQuery.ts
    frontend/src/services/offlineCache.ts
    frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
    frontend/src/components/teamer/pages/TeamerEventsPage.tsx
    frontend/src/components/teamer/pages/TeamerBadgesPage.tsx
  </read_first>
  <action>
Teamer-Pages haben meist KEIN useLiveRefresh (laut Research), ausser TeamerEventsPage.

**TeamerDashboardPage** (2 Calls, KEIN LiveRefresh):
- `api.get('/teamer/dashboard')` → `useOfflineQuery('teamer:dashboard:' + user?.id, () => api.get('/teamer/dashboard').then(r => r.data), { ttl: CACHE_TTL.DASHBOARD })`
- `api.get('/teamer/tageslosung')` → `useOfflineQuery('teamer:tageslosung:' + new Date().toISOString().split('T')[0], fetcher, { ttl: CACHE_TTL.TAGESLOSUNG })`
- Tageslosung-Transformation (wie KonfiDashboard) geht in den fetcher
- Random-Tageslosung-Anzeige-Logik bleibt in der UI

**TeamerEventsPage** (1 Call, HAT LiveRefresh):
- `api.get('/teamer/events')` → `useOfflineQuery('teamer:events:' + user?.id, ..., { ttl: CACHE_TTL.EVENTS })`
- useLiveRefresh('events', refresh) — refresh statt loadEvents

**TeamerBadgesPage** (1 Call, KEIN LiveRefresh):
- `api.get('/teamer/badges')` → `useOfflineQuery('teamer:badges:' + user?.id, ..., { ttl: CACHE_TTL.BADGES })`
- View-internes Badge-Loading: Pruefen ob Views eigene API-Calls haben und ggf. auch migrieren
- `PUT /teamer/badges/mark-seen` bleibt als normaler API-Call (Write-Aktion)

IonRefresher auf allen 3 Pages: `await refresh(); event.detail.complete();`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && for f in TeamerDashboardPage TeamerEventsPage TeamerBadgesPage; do echo "$f:"; grep -c "useOfflineQuery" "frontend/src/components/teamer/pages/${f}.tsx"; done</automated>
  </verify>
  <acceptance_criteria>
    - Alle 3 Pages enthalten `import { useOfflineQuery }`
    - TeamerDashboardPage hat 2 useOfflineQuery Aufrufe (dashboard + tageslosung)
    - TeamerEventsPage hat useLiveRefresh mit refresh()
    - Cache-Keys enthalten user?.id (teamer-spezifisch)
    - Write-Aktionen (mark-seen) sind NICHT in useOfflineQuery
  </acceptance_criteria>
  <done>Teamer Dashboard, Events und Badges sind offline lesbar</done>
</task>

<task type="auto">
  <name>Task 2: Teamer Profil + Material + KonfiStats migrieren</name>
  <files>
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx
    frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
    frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
    frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
  </files>
  <read_first>
    frontend/src/hooks/useOfflineQuery.ts
    frontend/src/services/offlineCache.ts
    frontend/src/components/teamer/pages/TeamerProfilePage.tsx
    frontend/src/components/teamer/pages/TeamerMaterialPage.tsx
    frontend/src/components/teamer/pages/TeamerMaterialDetailPage.tsx
    frontend/src/components/teamer/pages/TeamerKonfiStatsPage.tsx
  </read_first>
  <action>
Alle 4 Pages haben KEIN useLiveRefresh. Einfachste Migration.

**TeamerProfilePage** (2 Calls):
- `api.get('/teamer/profile')` → `useOfflineQuery('teamer:profile:' + user?.id, ..., { ttl: CACHE_TTL.PROFILE })`
- `api.get('/auth/me')` → `useOfflineQuery('user:me:' + user?.id, ..., { ttl: CACHE_TTL.SETTINGS })`

**TeamerMaterialPage** (2 Calls):
- `api.get('/jahrgaenge')` → `useOfflineQuery('teamer:jahrgaenge:' + user?.organization_id, ..., { ttl: CACHE_TTL.STAMMDATEN })`
  - organization_id weil Jahrgaenge org-weit sind
- `api.get('/material')` → `useOfflineQuery('teamer:material:' + user?.organization_id, ..., { ttl: CACHE_TTL.PROFILE })`
  - Nur Metadaten, keine Dateien (per Research)

**TeamerMaterialDetailPage** (1 Call):
- `api.get('/material/:id')` → `useOfflineQuery('teamer:material-detail:' + materialId, ..., { ttl: CACHE_TTL.PROFILE, enabled: !!materialId })`
- Datei-Downloads (`GET /material/files/:name`) bleiben als direkter Download — NICHT cachen (Dateien zu gross, per Out-of-Scope)

**TeamerKonfiStatsPage** (1 Call):
- `api.get('/teamer/profile')` → `useOfflineQuery('teamer:profile:' + user?.id, ..., { ttl: CACHE_TTL.PROFILE })`
- Gleicher Cache-Key wie TeamerProfilePage — gleiche Daten werden geteilt (SWR dedupliziert automatisch)

IonRefresher auf allen Pages: `await refresh(); event.detail.complete();`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && for f in TeamerProfilePage TeamerMaterialPage TeamerMaterialDetailPage TeamerKonfiStatsPage; do echo "$f:"; grep -c "useOfflineQuery" "frontend/src/components/teamer/pages/${f}.tsx"; done</automated>
  </verify>
  <acceptance_criteria>
    - Alle 4 Pages enthalten `import { useOfflineQuery }`
    - TeamerProfilePage hat 2 useOfflineQuery Aufrufe (profile + me)
    - TeamerMaterialPage hat 2 useOfflineQuery Aufrufe (jahrgaenge + material)
    - TeamerMaterialDetailPage nutzt enabled-Option mit materialId
    - Material-Datei-Downloads sind NICHT gecached (nur Metadaten)
    - Keine Page hat mehr `const loadData = async` Pattern fuer Reads
  </acceptance_criteria>
  <done>Teamer Profil, Material-Liste, Material-Detail und Konfi-Stats sind offline lesbar</done>
</task>

</tasks>

<verification>
- Alle 7 Teamer-Pages nutzen useOfflineQuery: `grep -r "useOfflineQuery" frontend/src/components/teamer/pages/`
- Frontend kompiliert: `cd frontend && npx tsc --noEmit`
- Material-Datei-Downloads bleiben als direkte API-Calls
</verification>

<success_criteria>
- Teamer sieht Dashboard, Events, Badges, Profil, Material-Liste und Konfi-Stats offline
- Material-Detail zeigt Metadaten offline, Datei-Downloads nur online
- SWR-Pattern auf allen Pages
- Keine Regression bei bestehenden Write-Aktionen
</success_criteria>

<output>
After completion, create `.planning/phases/56-lese-cache/56-04-SUMMARY.md`
</output>
