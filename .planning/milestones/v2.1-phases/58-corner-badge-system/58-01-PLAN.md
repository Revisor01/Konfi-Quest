---
phase: 58-corner-badge-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/variables.css
autonomous: true
requirements:
  - OUI-01
  - OUI-02
  - OUI-03
  - OUI-04
  - OUI-06
must_haves:
  truths:
    - "Neuer .app-corner-badges Flex-Container existiert in variables.css"
    - "Badge-Rundung folgt dem Referenz-Pattern: letztes Kind oben-rechts Card-Ecke, andere unten-beidseitig"
    - "2px weisser Trenner-Klasse existiert"
    - "Queue-Badge CSS-Klasse (orange, Uhr-Icon, schmaler Padding) existiert"
    - "Fehler-Badge CSS-Klasse (rot, cursor pointer) existiert"
    - "Bestehende .app-corner-badge verliert absolute Positionierung"
  artifacts:
    - path: "frontend/src/theme/variables.css"
      provides: ".app-corner-badges Container, .app-corner-badges__separator, .app-corner-badge--queue, .app-corner-badge--error, angepasste .app-corner-badge"
      contains: "app-corner-badges"
  key_links:
    - from: "frontend/src/theme/variables.css"
      to: "alle Komponenten mit Corner-Badges"
      via: "CSS-Klassen-Import ueber globales Stylesheet"
      pattern: "app-corner-badges"
---

<objective>
CSS-Infrastruktur fuer das Corner-Badge Flex-Container System erstellen.

Purpose: Grundlage fuer die Migration aller bestehenden Corner-Badges und fuer zukuenftige Queue-/Fehler-Badges (Phase 60+).
Output: Neue CSS-Klassen in variables.css — Container, Trenner, Queue-Badge, Fehler-Badge. Bestehende .app-corner-badge angepasst.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-corner-badge-system/58-CONTEXT.md
@.planning/phases/58-corner-badge-system/58-UI-SPEC.md

@frontend/src/theme/variables.css
@frontend/src/components/konfi/modals/PointsHistoryModal.tsx (Zeile 274-303 — Referenz-Implementierung)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen fuer Corner-Badge Flex-Container System</name>
  <files>frontend/src/theme/variables.css</files>
  <read_first>
    - frontend/src/theme/variables.css (Zeile 276-296 — bestehende .app-corner-badge Definition)
    - frontend/src/components/konfi/modals/PointsHistoryModal.tsx (Zeile 274-303 — Referenz inline-styles)
  </read_first>
  <action>
In `frontend/src/theme/variables.css` den Abschnitt `/* === Corner Badges === */` (Zeile 276-296) komplett ersetzen durch folgende CSS-Definitionen. Reihenfolge und Werte sind exakt einzuhalten (per D-01 bis D-16 aus CONTEXT.md):

1. **Neuen `.app-corner-badges` Container** (per D-01, D-02):
```css
.app-corner-badges {
  position: absolute;
  top: 0;
  right: 0;
  display: flex;
  flex-direction: row;
  z-index: 10;
}
```

2. **Trenner-Klasse** (per D-03):
```css
.app-corner-badges__separator {
  width: 2px;
  background: white;
  align-self: stretch;
}
```

3. **Bestehende `.app-corner-badge` anpassen** (per D-15):
- ENTFERNEN: `position: absolute;`, `top: 0;`, `right: 0;`, `border-radius: 0 10px 0 10px;`
- AENDERN: `font-weight` von `600` auf `700` (per UI-SPEC Typografie-Angleichung)
- BEIBEHALTEN: `font-size: 0.65rem;`, `padding: 4px 10px;`, `color: white;`, `text-transform: uppercase;`, `letter-spacing: 0.3px;`

Ergebnis fuer .app-corner-badge:
```css
.app-corner-badge {
  font-size: 0.65rem;
  font-weight: 700;
  padding: 4px 10px;
  color: white;
  text-transform: uppercase;
  letter-spacing: 0.3px;
}
```

4. **Badge-Rundung im Container** (per D-04, D-05, D-06):
```css
.app-corner-badges .app-corner-badge {
  border-radius: 0 0 10px 10px;
}
.app-corner-badges .app-corner-badge:last-child {
  border-radius: 0 10px 10px 10px;
}
.app-corner-badges .app-corner-badge:only-child {
  border-radius: 0 10px 10px 10px;
}
```

5. **Bestehende Farb-Varianten beibehalten** (per D-16) — unveraendert:
```css
.app-corner-badge--events { background-color: #dc2626; }
.app-corner-badge--success { background-color: #34c759; }
.app-corner-badge--warning { background-color: #ff9500; }
.app-corner-badge--danger { background-color: #dc3545; }
.app-corner-badge--info { background-color: #007aff; }
.app-corner-badge--chat { background-color: #06b6d4; }
.app-corner-badge--purple { background-color: #5b21b6; }
```

6. **Neue Queue-Badge Klasse** (per D-08, D-09, D-10):
```css
.app-corner-badge--queue {
  background-color: #ff9500;
  padding: 4px 6px;
}
.app-corner-badge--queue ion-icon {
  font-size: 0.75rem;
}
```

7. **Neue Fehler-Badge Klasse** (per D-12):
```css
.app-corner-badge--error {
  background-color: #dc3545;
  padding: 4px 6px;
  cursor: pointer;
}
.app-corner-badge--error ion-icon {
  font-size: 0.75rem;
}
```

WICHTIG: Die Queue- und Fehler-Badges werden in dieser Phase nur als CSS-Klassen definiert. Die tatsaechliche Logik (wann sie erscheinen/verschwinden) kommt erst in Phase 60.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints && grep -c "app-corner-badges" frontend/src/theme/variables.css && grep -c "app-corner-badge--queue" frontend/src/theme/variables.css && grep -c "app-corner-badge--error" frontend/src/theme/variables.css && grep -c "app-corner-badges__separator" frontend/src/theme/variables.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "app-corner-badges {" variables.css findet den neuen Container
    - grep "app-corner-badges__separator" variables.css findet die Trenner-Klasse
    - grep "app-corner-badge--queue" variables.css findet die Queue-Badge Klasse mit #ff9500
    - grep "app-corner-badge--error" variables.css findet die Fehler-Badge Klasse mit #dc3545
    - grep "position: absolute" in .app-corner-badge Block existiert NICHT mehr (nur im Container)
    - grep "font-weight: 700" in .app-corner-badge Block existiert
    - grep "border-radius: 0 0 10px 10px" existiert fuer Standard-Badges
    - grep "border-radius: 0 10px 10px 10px" existiert fuer :last-child und :only-child
    - Alle 7 bestehenden Farb-Varianten (--events, --success, --warning, --danger, --info, --chat, --purple) existieren weiterhin
  </acceptance_criteria>
  <done>
    variables.css enthaelt: .app-corner-badges Container, .app-corner-badges__separator Trenner, .app-corner-badge ohne absolute Positionierung mit font-weight 700, Rundungs-Regeln per :last-child/:only-child, .app-corner-badge--queue (orange), .app-corner-badge--error (rot), alle 7 bestehenden Farb-Varianten unveraendert.
  </done>
</task>

</tasks>

<verification>
- `grep -c "app-corner-badges" frontend/src/theme/variables.css` zeigt mindestens 5 Treffer (Container + Separator + Rundungs-Selektoren)
- `grep "position: absolute" frontend/src/theme/variables.css` zeigt nur den .app-corner-badges Container, NICHT .app-corner-badge
- `cd frontend && npx tsc --noEmit` kompiliert ohne Fehler (CSS-only Aenderung, kein TypeScript betroffen)
</verification>

<success_criteria>
Alle CSS-Klassen fuer das Corner-Badge Flex-Container System sind definiert. Die bestehende .app-corner-badge Klasse hat keine absolute Positionierung mehr. Queue- und Fehler-Badge Klassen existieren als CSS-Infrastruktur fuer Phase 60.
</success_criteria>

<output>
After completion, create `.planning/phases/58-corner-badge-system/58-01-SUMMARY.md`
</output>
