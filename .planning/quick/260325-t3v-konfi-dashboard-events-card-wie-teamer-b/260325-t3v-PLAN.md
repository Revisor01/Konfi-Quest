---
phase: quick
plan: 260325-t3v
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/konfi/views/DashboardView.tsx
  - frontend/src/components/konfi/views/DashboardSections.tsx
  - frontend/src/components/konfi/modals/PointsHistoryModal.tsx
autonomous: true
requirements: [QT-01, QT-02, QT-03]
must_haves:
  truths:
    - "Konfi Events-Card zeigt 'Erstes Event entdecken' Corner Badge bei 0 Events und 'Alle Events anzeigen' Link unter allen Events"
    - "Badge Popover im Konfi Dashboard skaliert auf Badge-Titelbreite mit minWidth"
    - "Punkte-Uebersicht Items haben kompakten Abstand wie Chatroom-Liste"
  artifacts:
    - path: "frontend/src/components/konfi/views/DashboardView.tsx"
      provides: "Events Section mit Teamer-Pattern"
    - path: "frontend/src/components/konfi/views/DashboardSections.tsx"
      provides: "Badge Popover mit minWidth"
    - path: "frontend/src/components/konfi/modals/PointsHistoryModal.tsx"
      provides: "Kompaktes List-Spacing"
  key_links:
    - from: "DashboardView.tsx events section"
      to: "Teamer events pattern"
      via: "Corner Badge + Alle Events Link"
---

<objective>
Drei Design-Anpassungen: (1) Konfi Events-Card 1:1 wie Teamer mit Corner Badge und "Alle Events" Link, (2) Badge Popover breiter, (3) Punkte-Uebersicht kompakteres Spacing.

Purpose: UI-Konsistenz zwischen Konfi und Teamer Dashboard herstellen + Popover/Spacing Fixes
Output: Drei angepasste Dateien
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/konfi/views/DashboardView.tsx
@frontend/src/components/teamer/pages/TeamerDashboardPage.tsx
@frontend/src/components/konfi/views/DashboardSections.tsx
@frontend/src/components/konfi/modals/PointsHistoryModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Konfi Events-Card wie Teamer + Badge Popover Breite</name>
  <files>frontend/src/components/konfi/views/DashboardView.tsx, frontend/src/components/konfi/views/DashboardSections.tsx</files>
  <action>
**DashboardView.tsx — Events Section (Zeilen ~374-418):**

1. Corner Badge (glass-chip oben rechts, Zeile 381-390) anpassen:
   - Bei `regularEvents.length === 0`: Text auf `'ERSTES EVENT ENTDECKEN'` aendern (wie Teamer-Pattern)
   - Rest bleibt gleich (DEIN EVENT / DEINE X EVENTS)

2. "Alle Events anzeigen" Link unter den EventCards einfuegen — 1:1 aus TeamerDashboardPage.tsx (Zeilen 701-713):
   Nach dem `regularEvents.map(...)` Block und innerhalb des `<div style={{ display: 'flex', flexDirection: 'column', gap: '12px' }}>` noch dieses Element einfuegen:
   ```tsx
   <div
     className="app-dashboard-glass-chip"
     onClick={() => router.push('/konfi/events')}
     style={{
       alignSelf: 'center',
       cursor: 'pointer',
       display: 'flex',
       alignItems: 'center',
       gap: '4px'
     }}
   >
     Alle Events anzeigen <IonIcon icon={chevronForward} />
   </div>
   ```
   Sicherstellen, dass `chevronForward` bereits importiert ist (ist es: Zeile 15). Der Link soll NUR angezeigt werden wenn `regularEvents.length > 0` (also innerhalb des bestehenden `regularEvents.length > 0` Blocks, nach der map).

3. Den leeren Zustand (regularEvents.length === 0, Zeilen 402-414) beibehalten — der Text "Buche dein naechstes Event" und "Tippe hier um verfuegbare Events zu sehen" bleibt.

**DashboardSections.tsx — DashboardBadgePopoverContent (Zeile 217):**

4. Dem aeusseren `<div>` der DashboardBadgePopoverContent Komponente `minWidth: '200px'` hinzufuegen:
   Zeile 217: `<div style={{ padding: '12px', background: 'white' }}>` aendern zu:
   `<div style={{ padding: '12px', background: 'white', minWidth: '200px' }}>`
   Das ist 1:1 wie die Teamer BadgePopoverContent (TeamerDashboardPage.tsx Zeile 134).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Events-Card Corner Badge zeigt "ERSTES EVENT ENTDECKEN" bei 0 Events
    - "Alle Events anzeigen" Link erscheint unter den EventCards (nicht im Leerzustand)
    - Badge Popover hat minWidth: 200px
  </done>
</task>

<task type="auto">
  <name>Task 2: Punkte-Uebersicht Spacing kompakter</name>
  <files>frontend/src/components/konfi/modals/PointsHistoryModal.tsx</files>
  <action>
In PointsHistoryModal.tsx die Verlauf-Liste (Zeilen 251-315) anpassen:

1. **IonCardContent Padding** (Zeile 252): Aktuell `padding: '8px'` wenn Items vorhanden. Das ist das allseitige Padding. Aendern auf `padding: '12px'` damit top/bottom/left/right gleichmaessig sind und dem Chatroom-Listen-Pattern entsprechen.

2. **Gap zwischen Items** (Zeile 258): Aktuell `gap: '8px'`. Das `.app-list-item` CSS hat bereits `margin-bottom: 8px` und `:last-child` hat `margin-bottom: 0`. Die `gap: '8px'` plus `margin-bottom: 8px` summieren sich zu 16px Abstand — das ist zu viel!
   Loesung: `gap: '0'` setzen (oder gap komplett entfernen), damit nur der CSS-eigene `margin-bottom: 8px` der `.app-list-item` Klasse greift. Das entspricht dem Chatroom-Listen-Pattern wo die Items auch nur durch den CSS-Margin getrennt sind.

   Also Zeile 258 aendern von:
   `<div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>`
   zu:
   `<div style={{ display: 'flex', flexDirection: 'column' }}>`

3. **Leerzustand Padding** (Zeile 252): Bei `filteredHistory.length === 0` ist padding `16px` — das kann bleiben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Konfipoints/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Punkte-Uebersicht Items haben 8px Abstand (nur CSS margin, kein doppelter gap)
    - Padding oben/unten/links/rechts der Liste ist gleichmaessig 12px
  </done>
</task>

</tasks>

<verification>
- TypeScript kompiliert fehlerfrei
- Konfi Dashboard Events-Card hat Corner Badge + "Alle Events" Link wie Teamer
- Badge Popover ist mindestens 200px breit
- Punkte-Uebersicht hat kompakten, gleichmaessigen Abstand
</verification>

<success_criteria>
- Alle drei Designanpassungen umgesetzt
- Keine TypeScript-Fehler
- UI-Konsistenz zwischen Konfi und Teamer Events-Card
</success_criteria>

<output>
After completion, create `.planning/quick/260325-t3v-konfi-dashboard-events-card-wie-teamer-b/260325-t3v-SUMMARY.md`
</output>
